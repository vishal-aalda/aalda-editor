(function(){var e;"use strict";try{if(typeof document<"u"){var o=document.createElement("style");o.nonce=(e=document.head.querySelector("meta[property=csp-nonce]"))==null?void 0:e.content,o.appendChild(document.createTextNode('h5{position:relative;bottom:-1rem}.tc-table:before{position:absolute;content:"";height:var(--toolbox-aiming-field-size);top:calc(var(--toolbox-aiming-field-size)*-1);left:0}.tc-add-column{padding:4px 0;justify-content:center;border-top:1px solid var(--color-border)}.tc-row:first-child{background:hsla(0,0%,50.2%,.47843137254901963);text-wrap:nowrap}.modal-container svg,.tc-cell svg{color:transparent}.tc-cell:last-child{overflow:visible}table{width:100%;border-collapse:collapse}table tr th{background:#ccc;text-align:left;font-weight:500}table tr td,table tr th{padding:10px;border:1px solid #b9b9b9}.upload-box{display:flex;width:100%}.upload-box .upload-file-list{flex:1}.upload-box .upload-file-list .upload-file-name{position:relative;display:inline-flex;background:#e0e9f7;font-size:12px;border-radius:4px;padding:3px 6px;margin-right:4px;overflow:hidden}.btn-icon-group{position:absolute;top:0;right:-36px;background:#36f;border-radius:4px;padding:2px 4px;transition:.3s ease-in-out}.upload-box .upload-file-list .upload-file-name:hover .btn-icon-group{right:0}.btn-icon-group .btn-icon{border:none;background:none;padding:2px;cursor:pointer;display:inline-flex;align-items:center}.drop-container{position:relative}.drop-container .dropdown-menu{position:absolute;right:0;z-index:1;min-width:140px;overflow:hidden;background:#fff;border:1px solid #dcdfe3;box-shadow:0 2px 2px #1f212433;border-radius:6px;transition:.3s ease-in-out;display:none}.drop-container .dropdown-menu.menu-open{display:block}.drop-container .dropdown-menu ul{list-style:none;width:100%;padding:0;margin:0}.drop-container .dropdown-menu li{position:relative;font-size:12px;font-style:normal;font-weight:400;line-height:20px;border-bottom:1px solid #dcdfe3;background:#fff;border-radius:6px 6px 0 0;height:36px;display:flex;align-items:center;padding:0 14px;align-self:stretch;cursor:pointer;transition:.3s ease-in-out}.drop-container .dropdown-menu li:last-child{border-radius:0 0 6px 6px;border:0}.drop-container .dropdown-menu li:hover{transition:.3s ease-in-out;background:#ebf0ff}.drop-container .dropdown-menu li:hover:before{content:"";position:absolute;left:0;background:#36f;height:100%;width:3px}.btn-drop-down{border:none;background:none;padding:2px;cursor:pointer}.modal-container{display:none;position:fixed;top:0;left:0;right:0;width:100%;height:100%;z-index:99;padding-top:100px;overflow:auto;background-color:#0003}.modal-container .modal-content{position:relative;background-color:#fefefe;margin:auto;padding:0;width:80%;border-radius:12px;box-shadow:0 4px 8px #0003,0 6px 20px #00000030;-webkit-animation-name:animatetop;-webkit-animation-duration:.4s;animation-name:animatetop;animation-duration:.4s}.modal-container .modal-content .modal-header{display:flex;justify-content:space-between;padding:1rem 1.5rem .6rem;border-bottom:1px solid #b9b9b9}.modal-container .modal-content .modal-header .modal-title h2{margin:0 0 5px;font-size:1.2rem;font-style:normal;font-weight:600;line-height:normal;letter-spacing:.36px;text-align:left}.modal-container .modal-content .modal-header .modal-title p{color:#a9acb4;font-size:14px;font-style:normal;font-weight:500;line-height:normal;margin:0}.close{color:#6c757d;float:right;font-size:18px}.close:focus,.close:hover{color:#000;text-decoration:none;cursor:pointer}.modal-body{padding:.6rem 1.5rem}.modal-body h2{text-align:left}.modal-footer{padding:.6rem 1.5rem;border-top:1px solid #b9b9b9}.btn-link{display:flex;justify-content:center;align-items:center;font-size:14px;font-style:normal;font-weight:600;line-height:130%;letter-spacing:.28px;text-transform:capitalize;padding:10px 14px;background:#36f;color:#fff;border:none;border-radius:4px;width:100%;transition:.3s linear;cursor:pointer}.btn-link:hover{background-color:#0342fc}.section-upload{position:relative;display:flex;justify-content:space-between;align-items:center;border:1px dashed #6c757d;border-radius:4px;background:#fefefe;padding:1.5rem;flex-wrap:wrap}.section-upload input[type=file]{position:absolute;width:100%;height:100%;opacity:0;cursor:pointer}.section-upload .upload-left{display:flex;align-items:center;flex-wrap:wrap;flex:1}.section-upload .upload-left .upload-icon{margin-right:10px}.section-upload .upload-left .upload-info h4{font-size:14px;font-style:normal;font-weight:500;line-height:normal;color:#1f2124;margin:0 0 8px}.section-upload .upload-left .upload-info p{font-size:12px;font-style:normal;font-weight:500;line-height:normal;color:#a9acb4;margin:0}.section-upload .upload-right .btn-browse{border-radius:8px;border:1px solid #1f2124;background:#fefefe;font-size:14px;font-style:normal;font-weight:500;line-height:normal;color:#36f;padding:8px 12px;transition:.3s ease-in-out}.section-upload .upload-right .btn-browse:hover{background-color:#36f;color:#fff;border:1px solid #36f}.uploaded-files-list{margin:1rem 0;width:100%;height:210px;overflow:auto}.uploaded-files-list .uploaded-item{display:flex;width:100%;margin-left:-1rem;margin-right:-1rem;margin-bottom:.5rem}.uploaded-files-list .uploaded-item .col-50{width:50%;padding:0 1rem}.uploaded-files-list .uploaded-item h4{color:#1f2124;font-size:14px;font-style:normal;font-weight:500;line-height:normal;letter-spacing:.24px;margin:0 0 .5rem}.uploaded-files-list .uploaded-item .uploaded-file-image{width:100%;height:200px;overflow:hidden;display:flex;justify-content:center;border:1px solid #acb0b8;border-radius:4px}.uploaded-files-list .uploaded-item .uploaded-file-image img{max-width:100%;height:100%}.uploaded-file-form .form-row{display:flex;margin-left:-.5rem;margin-right:-.5rem}.uploaded-file-form .form-row .form-input{width:50%;padding-left:.5rem;padding-right:.5rem}.form-input{margin-bottom:.5rem}.form-input .input-control{display:block;width:100%;height:calc(1.5rem + 2px);padding:.375rem .75rem;font-size:14px;line-height:1.5;color:#495057;background-color:#fff;background-clip:padding-box;border:1px solid #ced4da;border-radius:.25rem;transition:border-color .15s ease-in-out,box-shadow .15s ease-in-out}.form-input select.input-control{height:40px}.uploaded-file-form .form-input textarea{height:5rem;min-height:5rem}.uploaded-files-list:empty{display:none}.tc-header{margin:0;padding:0;position:relative;bottom:-1rem;text-align:left}.tc-wrap{--color-background:#f9f9fb;--color-text-secondary:#7b7e89;--color-border:#e8e8eb;--cell-size:34px;--toolbox-icon-size:18px;--toolbox-padding:6px;--toolbox-aiming-field-size:calc(var(--toolbox-icon-size) + var(--toolbox-padding)*2);border-left:0;margin-top:var(--toolbox-icon-size);box-sizing:border-box;grid-template-columns:calc(100% - var(--cell-size)) var(--cell-size)}.tc-wrap--readonly{grid-template-columns:100% var(--cell-size)}.tc-wrap svg{vertical-align:top}@media print{.tc-wrap{border-left-color:var(--color-border);border-left-style:solid;border-left-width:1px;grid-template-columns:100% var(--cell-size)}}@media print{.tc-wrap .tc-row:after{display:none}}.tc-table,.tc-wrap{position:relative;height:100%;width:100%;display:grid}.tc-table{font-size:14px;border-top:1px solid var(--color-border);line-height:1.4}.tc-table:after{width:calc(var(--cell-size));left:calc(var(--cell-size)*-1);top:0}.tc-table:after,.tc-table:before{position:absolute;content:""}.tc-table:before{height:var(--toolbox-aiming-field-size);top:calc(var(--toolbox-aiming-field-size)*-1);left:0}.tc-table--heading .tc-row:first-child{font-weight:600;border-bottom:2px solid var(--color-border)}.tc-table--heading .tc-row:first-child [contenteditable]:empty:before{content:attr(heading);color:var(--color-text-secondary)}.tc-table--heading .tc-row:first-child:after{bottom:-2px;border-bottom:2px solid var(--color-border)}.tc-add-column,.tc-add-row{display:flex;color:var(--color-text-secondary)}@media print{.tc-add{display:none}}.tc-add-column{padding:4px 0;justify-content:center;border-top:1px solid var(--color-border);visibility:hidden}@media print{.tc-add-column{display:none}}.tc-add-row{height:var(--cell-size);align-items:center;padding-left:4px;position:relative}.tc-add-row:before{content:"";position:absolute;right:calc(var(--cell-size)*-1);width:var(--cell-size);height:100%}@media print{.tc-add-row{display:none}}.tc-add-column,.tc-add-row{transition:0s;cursor:pointer;will-change:background-color}.tc-add-column:hover,.tc-add-row:hover{transition:background-color .1s ease;background-color:var(--color-background)}.tc-add-row{margin-top:1px}.tc-add-row:hover:before{transition:.1s;background-color:var(--color-background)}.tc-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(10px,1fr));position:relative;border-bottom:1px solid var(--color-border)}.tc-row:after{content:"";pointer-events:none;position:absolute;width:var(--cell-size);height:100%;bottom:-1px;right:calc(var(--cell-size)*-1);border-bottom:1px solid var(--color-border)}.tc-row--selected{background:var(--color-background)}.tc-row--selected:after{background:var(--color-background)}.tc-cell{border-right:1px solid var(--color-border);padding:6px 12px;overflow:hidden;outline:none;line-break:normal}.tc-cell input{outline:none;border:none;width:100%;height:100%;padding:0;margin:0}.tc-cell--selected{background:var(--color-background)}.tc-wrap--readonly .tc-row:after{display:none}.tc-toolbox{--toolbox-padding:6px;--popover-margin:30px;--toggler-click-zone-size:30px;--toggler-dots-color:#7b7e89;--toggler-dots-color-hovered:#1d202b;position:absolute;cursor:pointer;z-index:1;opacity:0;transition:opacity .1s;will-change:left,opacity}.tc-toolbox--column{top:calc(var(--toggler-click-zone-size)*-1);transform:translate(calc(var(--toggler-click-zone-size)*-1/2));will-change:left,opacity}.tc-toolbox--row{left:calc(var(--popover-margin)*-1);transform:translateY(calc(var(--toggler-click-zone-size)*-1/2));margin-top:-1px;will-change:top,opacity}.tc-toolbox--showed{opacity:1}.tc-toolbox .tc-popover{position:absolute;top:0;left:var(--popover-margin)}.tc-toolbox__toggler{display:flex;align-items:center;justify-content:center;width:var(--toggler-click-zone-size);height:var(--toggler-click-zone-size);color:var(--toggler-dots-color);opacity:0;transition:opacity .15s ease;will-change:opacity}.tc-toolbox__toggler:hover{color:var(--toggler-dots-color-hovered)}.tc-toolbox__toggler svg{fill:currentColor}.tc-wrap:hover .tc-toolbox__toggler{opacity:1}.tc-settings .cdx-settings-button{width:50%;margin:0}.tc-popover{--color-border:#eaeaea;--color-background:#fff;--color-background-hover:rgba(232,232,235,.49);--color-background-confirm:#e24a4a;--color-background-confirm-hover:#d54040;--color-text-confirm:#fff;background:var(--color-background);border:1px solid var(--color-border);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;padding:6px;display:none;will-change:opacity,transform}.tc-popover--opened{display:block;animation:menuShowing .1s cubic-bezier(.215,.61,.355,1) forwards}.tc-popover__item{display:flex;align-items:center;padding:2px 14px 2px 2px;border-radius:5px;cursor:pointer;white-space:nowrap;-webkit-user-select:none;-moz-user-select:none;user-select:none}.tc-popover__item:hover{background:var(--color-background-hover)}.tc-popover__item:not(:last-of-type){margin-bottom:2px}.tc-popover__item-icon{display:inline-flex;width:26px;height:26px;align-items:center;justify-content:center;background:var(--color-background);border-radius:5px;border:1px solid var(--color-border);margin-right:8px}.tc-popover__item-label{line-height:22px;font-size:14px;font-weight:500}.tc-popover__item--confirm{background:var(--color-background-confirm);color:var(--color-text-confirm)}.tc-popover__item--confirm:hover{background-color:var(--color-background-confirm-hover)}.tc-popover__item--confirm .tc-popover__item-icon{background:var(--color-background-confirm);border-color:#0000001a}.tc-popover__item--confirm .tc-popover__item-icon svg{transition:transform .2s ease-in;transform:rotate(90deg) scale(1.2)}.tc-popover__item--hidden{display:none}@keyframes menuShowing{0%{opacity:0;transform:translateY(-8px) scale(.9)}70%{opacity:1;transform:translateY(2px)}to{transform:translateY(0)}}')),document.head.appendChild(o)}}catch(t){console.error("vite-plugin-css-injected-by-js",t)}})();
var Qp = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Ua(o) {
  return o && o.__esModule && Object.prototype.hasOwnProperty.call(o, "default") ? o.default : o;
}
function xl() {
}
Object.assign(xl, {
  default: xl,
  register: xl,
  revert: function() {
  },
  __esModule: !0
});
Element.prototype.matches || (Element.prototype.matches = Element.prototype.matchesSelector || Element.prototype.mozMatchesSelector || Element.prototype.msMatchesSelector || Element.prototype.oMatchesSelector || Element.prototype.webkitMatchesSelector || function(o) {
  const e = (this.document || this.ownerDocument).querySelectorAll(o);
  let t = e.length;
  for (; --t >= 0 && e.item(t) !== this; )
    ;
  return t > -1;
});
Element.prototype.closest || (Element.prototype.closest = function(o) {
  let e = this;
  if (!document.documentElement.contains(e))
    return null;
  do {
    if (e.matches(o))
      return e;
    e = e.parentElement || e.parentNode;
  } while (e !== null);
  return null;
});
Element.prototype.prepend || (Element.prototype.prepend = function(o) {
  const e = document.createDocumentFragment();
  Array.isArray(o) || (o = [o]), o.forEach((t) => {
    const n = t instanceof Node;
    e.appendChild(n ? t : document.createTextNode(t));
  }), this.insertBefore(e, this.firstChild);
});
Element.prototype.scrollIntoViewIfNeeded || (Element.prototype.scrollIntoViewIfNeeded = function(o) {
  o = arguments.length === 0 ? !0 : !!o;
  const e = this.parentNode, t = window.getComputedStyle(e, null), n = parseInt(t.getPropertyValue("border-top-width")), r = parseInt(t.getPropertyValue("border-left-width")), a = this.offsetTop - e.offsetTop < e.scrollTop, c = this.offsetTop - e.offsetTop + this.clientHeight - n > e.scrollTop + e.clientHeight, u = this.offsetLeft - e.offsetLeft < e.scrollLeft, p = this.offsetLeft - e.offsetLeft + this.clientWidth - r > e.scrollLeft + e.clientWidth, g = a && !c;
  (a || c) && o && (e.scrollTop = this.offsetTop - e.offsetTop - e.clientHeight / 2 - n + this.clientHeight / 2), (u || p) && o && (e.scrollLeft = this.offsetLeft - e.offsetLeft - e.clientWidth / 2 - r + this.clientWidth / 2), (a || c || u || p) && !o && this.scrollIntoView(g);
});
window.requestIdleCallback = window.requestIdleCallback || function(o) {
  const e = Date.now();
  return setTimeout(function() {
    o({
      didTimeout: !1,
      timeRemaining: function() {
        return Math.max(0, 50 - (Date.now() - e));
      }
    });
  }, 1);
};
window.cancelIdleCallback = window.cancelIdleCallback || function(o) {
  clearTimeout(o);
};
let ef = (o = 21) => crypto.getRandomValues(new Uint8Array(o)).reduce((e, t) => (t &= 63, t < 36 ? e += t.toString(36) : t < 62 ? e += (t - 26).toString(36).toUpperCase() : t > 62 ? e += "-" : e += "_", e), "");
var kh = /* @__PURE__ */ ((o) => (o.VERBOSE = "VERBOSE", o.INFO = "INFO", o.WARN = "WARN", o.ERROR = "ERROR", o))(kh || {});
const ze = {
  BACKSPACE: 8,
  TAB: 9,
  ENTER: 13,
  SHIFT: 16,
  CTRL: 17,
  ALT: 18,
  ESC: 27,
  SPACE: 32,
  LEFT: 37,
  UP: 38,
  DOWN: 40,
  RIGHT: 39,
  DELETE: 46,
  META: 91,
  SLASH: 191
}, tf = {
  LEFT: 0,
  WHEEL: 1,
  RIGHT: 2,
  BACKWARD: 3,
  FORWARD: 4
};
function ua(o, e, t = "log", n, r = "color: inherit") {
  if (!("console" in window) || !window.console[t])
    return;
  const a = ["info", "log", "warn", "error"].includes(t), c = [];
  switch (ua.logLevel) {
    case "ERROR":
      if (t !== "error")
        return;
      break;
    case "WARN":
      if (!["error", "warn"].includes(t))
        return;
      break;
    case "INFO":
      if (!a || o)
        return;
      break;
  }
  n && c.push(n);
  const u = "Editor.js 2.29.0", p = `line-height: 1em;
            color: #006FEA;
            display: inline-block;
            font-size: 11px;
            line-height: 1em;
            background-color: #fff;
            padding: 4px 9px;
            border-radius: 30px;
            border: 1px solid rgba(56, 138, 229, 0.16);
            margin: 4px 5px 4px 0;`;
  o && (a ? (c.unshift(p, r), e = `%c${u}%c ${e}`) : e = `( ${u} )${e}`);
  try {
    a ? n ? console[t](`${e} %o`, ...c) : console[t](e, ...c) : console[t](e);
  } catch {
  }
}
ua.logLevel = "VERBOSE";
function of(o) {
  ua.logLevel = o;
}
const bt = ua.bind(window, !1), Zo = ua.bind(window, !0);
function Ar(o) {
  return Object.prototype.toString.call(o).match(/\s([a-zA-Z]+)/)[1].toLowerCase();
}
function Dt(o) {
  return Ar(o) === "function" || Ar(o) === "asyncfunction";
}
function ho(o) {
  return Ar(o) === "object";
}
function Qi(o) {
  return Ar(o) === "string";
}
function nf(o) {
  return Ar(o) === "boolean";
}
function cd(o) {
  return Ar(o) === "number";
}
function dd(o) {
  return Ar(o) === "undefined";
}
function sn(o) {
  return o ? Object.keys(o).length === 0 && o.constructor === Object : !0;
}
function xh(o) {
  return o > 47 && o < 58 || // number keys
  o === 32 || o === 13 || // Space bar & return key(s)
  o === 229 || // processing key input for certain languages — Chinese, Japanese, etc.
  o > 64 && o < 91 || // letter keys
  o > 95 && o < 112 || // Numpad keys
  o > 185 && o < 193 || // ;=,-./` (in order)
  o > 218 && o < 223;
}
async function rf(o, e = () => {
}, t = () => {
}) {
  async function n(r, a, c) {
    try {
      await r.function(r.data), await a(dd(r.data) ? {} : r.data);
    } catch {
      c(dd(r.data) ? {} : r.data);
    }
  }
  return o.reduce(async (r, a) => (await r, n(a, e, t)), Promise.resolve());
}
function Ch(o) {
  return Array.prototype.slice.call(o);
}
function Ra(o, e) {
  return function() {
    const t = this, n = arguments;
    window.setTimeout(() => o.apply(t, n), e);
  };
}
function sf(o) {
  return o.name.split(".").pop();
}
function af(o) {
  return /^[-\w]+\/([-+\w]+|\*)$/.test(o);
}
function hd(o, e, t) {
  let n;
  return (...r) => {
    const a = this, c = () => {
      n = null, t || o.apply(a, r);
    }, u = t && !n;
    window.clearTimeout(n), n = window.setTimeout(c, e), u && o.apply(a, r);
  };
}
function Hl(o, e, t = void 0) {
  let n, r, a, c = null, u = 0;
  t || (t = {});
  const p = function() {
    u = t.leading === !1 ? 0 : Date.now(), c = null, a = o.apply(n, r), c || (n = r = null);
  };
  return function() {
    const g = Date.now();
    !u && t.leading === !1 && (u = g);
    const v = e - (g - u);
    return n = this, r = arguments, v <= 0 || v > e ? (c && (clearTimeout(c), c = null), u = g, a = o.apply(n, r), c || (n = r = null)) : !c && t.trailing !== !1 && (c = setTimeout(p, v)), a;
  };
}
function lf() {
  const o = {
    win: !1,
    mac: !1,
    x11: !1,
    linux: !1
  }, e = Object.keys(o).find((t) => window.navigator.appVersion.toLowerCase().indexOf(t) !== -1);
  return e && (o[e] = !0), o;
}
function fs(o) {
  return o[0].toUpperCase() + o.slice(1);
}
function jl(o, ...e) {
  if (!e.length)
    return o;
  const t = e.shift();
  if (ho(o) && ho(t))
    for (const n in t)
      ho(t[n]) ? (o[n] || Object.assign(o, { [n]: {} }), jl(o[n], t[n])) : Object.assign(o, { [n]: t[n] });
  return jl(o, ...e);
}
function qa(o) {
  const e = lf();
  return o = o.replace(/shift/gi, "⇧").replace(/backspace/gi, "⌫").replace(/enter/gi, "⏎").replace(/up/gi, "↑").replace(/left/gi, "→").replace(/down/gi, "↓").replace(/right/gi, "←").replace(/escape/gi, "⎋").replace(/insert/gi, "Ins").replace(/delete/gi, "␡").replace(/\+/gi, " + "), e.mac ? o = o.replace(/ctrl|cmd/gi, "⌘").replace(/alt/gi, "⌥") : o = o.replace(/cmd/gi, "Ctrl").replace(/windows/gi, "WIN"), o;
}
function cf(o) {
  try {
    return new URL(o).href;
  } catch {
  }
  return o.substring(0, 2) === "//" ? window.location.protocol + o : window.location.origin + o;
}
function df() {
  return ef(10);
}
function hf(o) {
  window.open(o, "_blank");
}
function uf(o = "") {
  return `${o}${Math.floor(Math.random() * 1e8).toString(16)}`;
}
function $l(o, e, t) {
  const n = `«${e}» is deprecated and will be removed in the next major release. Please use the «${t}» instead.`;
  o && Zo(n, "warn");
}
function bs(o, e, t) {
  const n = t.value ? "value" : "get", r = t[n], a = `#${e}Cache`;
  if (t[n] = function(...c) {
    return this[a] === void 0 && (this[a] = r.apply(this, ...c)), this[a];
  }, n === "get" && t.set) {
    const c = t.set;
    t.set = function(u) {
      delete o[a], c.apply(this, u);
    };
  }
  return t;
}
const Sh = 650;
function Lr() {
  return window.matchMedia(`(max-width: ${Sh}px)`).matches;
}
const ud = typeof window < "u" && window.navigator && window.navigator.platform && (/iP(ad|hone|od)/.test(window.navigator.platform) || window.navigator.platform === "MacIntel" && window.navigator.maxTouchPoints > 1);
function pf(o, e) {
  const t = Array.isArray(o) || ho(o), n = Array.isArray(e) || ho(e);
  return t || n ? JSON.stringify(o) === JSON.stringify(e) : o === e;
}
let ae = class Eo {
  /**
   * Check if passed tag has no closed tag
   *
   * @param {HTMLElement} tag - element to check
   * @returns {boolean}
   */
  static isSingleTag(e) {
    return e.tagName && [
      "AREA",
      "BASE",
      "BR",
      "COL",
      "COMMAND",
      "EMBED",
      "HR",
      "IMG",
      "INPUT",
      "KEYGEN",
      "LINK",
      "META",
      "PARAM",
      "SOURCE",
      "TRACK",
      "WBR"
    ].includes(e.tagName);
  }
  /**
   * Check if element is BR or WBR
   *
   * @param {HTMLElement} element - element to check
   * @returns {boolean}
   */
  static isLineBreakTag(e) {
    return e && e.tagName && [
      "BR",
      "WBR"
    ].includes(e.tagName);
  }
  /**
   * Helper for making Elements with class name and attributes
   *
   * @param  {string} tagName - new Element tag name
   * @param  {string[]|string} [classNames] - list or name of CSS class name(s)
   * @param  {object} [attributes] - any attributes
   * @returns {HTMLElement}
   */
  static make(e, t = null, n = {}) {
    const r = document.createElement(e);
    Array.isArray(t) ? r.classList.add(...t) : t && r.classList.add(t);
    for (const a in n)
      Object.prototype.hasOwnProperty.call(n, a) && (r[a] = n[a]);
    return r;
  }
  /**
   * Creates Text Node with the passed content
   *
   * @param {string} content - text content
   * @returns {Text}
   */
  static text(e) {
    return document.createTextNode(e);
  }
  /**
   * Append one or several elements to the parent
   *
   * @param  {Element|DocumentFragment} parent - where to append
   * @param  {Element|Element[]|DocumentFragment|Text|Text[]} elements - element or elements list
   */
  static append(e, t) {
    Array.isArray(t) ? t.forEach((n) => e.appendChild(n)) : e.appendChild(t);
  }
  /**
   * Append element or a couple to the beginning of the parent elements
   *
   * @param {Element} parent - where to append
   * @param {Element|Element[]} elements - element or elements list
   */
  static prepend(e, t) {
    Array.isArray(t) ? (t = t.reverse(), t.forEach((n) => e.prepend(n))) : e.prepend(t);
  }
  /**
   * Swap two elements in parent
   *
   * @param {HTMLElement} el1 - from
   * @param {HTMLElement} el2 - to
   * @deprecated
   */
  static swap(e, t) {
    const n = document.createElement("div"), r = e.parentNode;
    r.insertBefore(n, e), r.insertBefore(e, t), r.insertBefore(t, n), r.removeChild(n);
  }
  /**
   * Selector Decorator
   *
   * Returns first match
   *
   * @param {Element} el - element we searching inside. Default - DOM Document
   * @param {string} selector - searching string
   * @returns {Element}
   */
  static find(e = document, t) {
    return e.querySelector(t);
  }
  /**
   * Get Element by Id
   *
   * @param {string} id - id to find
   * @returns {HTMLElement | null}
   */
  static get(e) {
    return document.getElementById(e);
  }
  /**
   * Selector Decorator.
   *
   * Returns all matches
   *
   * @param {Element|Document} el - element we searching inside. Default - DOM Document
   * @param {string} selector - searching string
   * @returns {NodeList}
   */
  static findAll(e = document, t) {
    return e.querySelectorAll(t);
  }
  /**
   * Returns CSS selector for all text inputs
   */
  static get allInputsSelector() {
    return "[contenteditable=true], textarea, input:not([type]), " + ["text", "password", "email", "number", "search", "tel", "url"].map((e) => `input[type="${e}"]`).join(", ");
  }
  /**
   * Find all contenteditable, textarea and editable input elements passed holder contains
   *
   * @param holder - element where to find inputs
   */
  static findAllInputs(e) {
    return Ch(e.querySelectorAll(Eo.allInputsSelector)).reduce((t, n) => Eo.isNativeInput(n) || Eo.containsOnlyInlineElements(n) ? [...t, n] : [...t, ...Eo.getDeepestBlockElements(n)], []);
  }
  /**
   * Search for deepest node which is Leaf.
   * Leaf is the vertex that doesn't have any child nodes
   *
   * @description Method recursively goes throw the all Node until it finds the Leaf
   * @param {Node} node - root Node. From this vertex we start Deep-first search
   *                      {@link https://en.wikipedia.org/wiki/Depth-first_search}
   * @param {boolean} [atLast] - find last text node
   * @returns {Node} - it can be text Node or Element Node, so that caret will able to work with it
   */
  static getDeepestNode(e, t = !1) {
    const n = t ? "lastChild" : "firstChild", r = t ? "previousSibling" : "nextSibling";
    if (e && e.nodeType === Node.ELEMENT_NODE && e[n]) {
      let a = e[n];
      if (Eo.isSingleTag(a) && !Eo.isNativeInput(a) && !Eo.isLineBreakTag(a))
        if (a[r])
          a = a[r];
        else if (a.parentNode[r])
          a = a.parentNode[r];
        else
          return a.parentNode;
      return this.getDeepestNode(a, t);
    }
    return e;
  }
  /**
   * Check if object is DOM node
   *
   * @param {*} node - object to check
   * @returns {boolean}
   */
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  static isElement(e) {
    return cd(e) ? !1 : e && e.nodeType && e.nodeType === Node.ELEMENT_NODE;
  }
  /**
   * Check if object is DocumentFragment node
   *
   * @param {object} node - object to check
   * @returns {boolean}
   */
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  static isFragment(e) {
    return cd(e) ? !1 : e && e.nodeType && e.nodeType === Node.DOCUMENT_FRAGMENT_NODE;
  }
  /**
   * Check if passed element is contenteditable
   *
   * @param {HTMLElement} element - html element to check
   * @returns {boolean}
   */
  static isContentEditable(e) {
    return e.contentEditable === "true";
  }
  /**
   * Checks target if it is native input
   *
   * @param {*} target - HTML element or string
   * @returns {boolean}
   */
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  static isNativeInput(e) {
    const t = [
      "INPUT",
      "TEXTAREA"
    ];
    return e && e.tagName ? t.includes(e.tagName) : !1;
  }
  /**
   * Checks if we can set caret
   *
   * @param {HTMLElement} target - target to check
   * @returns {boolean}
   */
  static canSetCaret(e) {
    let t = !0;
    if (Eo.isNativeInput(e))
      switch (e.type) {
        case "file":
        case "checkbox":
        case "radio":
        case "hidden":
        case "submit":
        case "button":
        case "image":
        case "reset":
          t = !1;
          break;
      }
    else
      t = Eo.isContentEditable(e);
    return t;
  }
  /**
   * Checks node if it is empty
   *
   * @description Method checks simple Node without any childs for emptiness
   * If you have Node with 2 or more children id depth, you better use {@link Dom#isEmpty} method
   * @param {Node} node - node to check
   * @param {string} [ignoreChars] - char or substring to treat as empty
   * @returns {boolean} true if it is empty
   */
  static isNodeEmpty(e, t) {
    let n;
    return this.isSingleTag(e) && !this.isLineBreakTag(e) ? !1 : (this.isElement(e) && this.isNativeInput(e) ? n = e.value : n = e.textContent.replace("​", ""), t && (n = n.replace(new RegExp(t, "g"), "")), n.trim().length === 0);
  }
  /**
   * checks node if it is doesn't have any child nodes
   *
   * @param {Node} node - node to check
   * @returns {boolean}
   */
  static isLeaf(e) {
    return e ? e.childNodes.length === 0 : !1;
  }
  /**
   * breadth-first search (BFS)
   * {@link https://en.wikipedia.org/wiki/Breadth-first_search}
   *
   * @description Pushes to stack all DOM leafs and checks for emptiness
   * @param {Node} node - node to check
   * @param {string} [ignoreChars] - char or substring to treat as empty
   * @returns {boolean}
   */
  static isEmpty(e, t) {
    e.normalize();
    const n = [e];
    for (; n.length > 0; )
      if (e = n.shift(), !!e) {
        if (this.isLeaf(e) && !this.isNodeEmpty(e, t))
          return !1;
        e.childNodes && n.push(...Array.from(e.childNodes));
      }
    return !0;
  }
  /**
   * Check if string contains html elements
   *
   * @param {string} str - string to check
   * @returns {boolean}
   */
  static isHTMLString(e) {
    const t = Eo.make("div");
    return t.innerHTML = e, t.childElementCount > 0;
  }
  /**
   * Return length of node`s text content
   *
   * @param {Node} node - node with content
   * @returns {number}
   */
  static getContentLength(e) {
    return Eo.isNativeInput(e) ? e.value.length : e.nodeType === Node.TEXT_NODE ? e.length : e.textContent.length;
  }
  /**
   * Return array of names of block html elements
   *
   * @returns {string[]}
   */
  static get blockElements() {
    return [
      "address",
      "article",
      "aside",
      "blockquote",
      "canvas",
      "div",
      "dl",
      "dt",
      "fieldset",
      "figcaption",
      "figure",
      "footer",
      "form",
      "h1",
      "h2",
      "h3",
      "h4",
      "h5",
      "h6",
      "header",
      "hgroup",
      "hr",
      "li",
      "main",
      "nav",
      "noscript",
      "ol",
      "output",
      "p",
      "pre",
      "ruby",
      "section",
      "table",
      "tbody",
      "thead",
      "tr",
      "tfoot",
      "ul",
      "video"
    ];
  }
  /**
   * Check if passed content includes only inline elements
   *
   * @param {string|HTMLElement} data - element or html string
   * @returns {boolean}
   */
  static containsOnlyInlineElements(e) {
    let t;
    Qi(e) ? (t = document.createElement("div"), t.innerHTML = e) : t = e;
    const n = (r) => !Eo.blockElements.includes(r.tagName.toLowerCase()) && Array.from(r.children).every(n);
    return Array.from(t.children).every(n);
  }
  /**
   * Find and return all block elements in the passed parent (including subtree)
   *
   * @param {HTMLElement} parent - root element
   * @returns {HTMLElement[]}
   */
  static getDeepestBlockElements(e) {
    return Eo.containsOnlyInlineElements(e) ? [e] : Array.from(e.children).reduce((t, n) => [...t, ...Eo.getDeepestBlockElements(n)], []);
  }
  /**
   * Helper for get holder from {string} or return HTMLElement
   *
   * @param {string | HTMLElement} element - holder's id or holder's HTML Element
   * @returns {HTMLElement}
   */
  static getHolder(e) {
    return Qi(e) ? document.getElementById(e) : e;
  }
  /**
   * Returns true if element is anchor (is A tag)
   *
   * @param {Element} element - element to check
   * @returns {boolean}
   */
  static isAnchor(e) {
    return e.tagName.toLowerCase() === "a";
  }
  /**
   * Return element's offset related to the document
   *
   * @todo handle case when editor initialized in scrollable popup
   * @param el - element to compute offset
   */
  static offset(e) {
    const t = e.getBoundingClientRect(), n = window.pageXOffset || document.documentElement.scrollLeft, r = window.pageYOffset || document.documentElement.scrollTop, a = t.top + r, c = t.left + n;
    return {
      top: a,
      left: c,
      bottom: a + t.height,
      right: c + t.width
    };
  }
};
const ff = {
  blockTunes: {
    toggler: {
      "Click to tune": "",
      "or drag to move": ""
    }
  },
  inlineToolbar: {
    converter: {
      "Convert to": ""
    }
  },
  toolbar: {
    toolbox: {
      Add: ""
    }
  },
  popover: {
    Filter: "",
    "Nothing found": ""
  }
}, mf = {
  Text: "",
  Link: "",
  Bold: "",
  Italic: ""
}, gf = {
  link: {
    "Add a link": ""
  },
  stub: {
    "The block can not be displayed correctly.": ""
  }
}, bf = {
  delete: {
    Delete: "",
    "Click to delete": ""
  },
  moveUp: {
    "Move up": ""
  },
  moveDown: {
    "Move down": ""
  }
}, Th = {
  ui: ff,
  toolNames: mf,
  tools: gf,
  blockTunes: bf
}, _h = class Jr {
  /**
   * Type-safe translation for internal UI texts:
   * Perform translation of the string by namespace and a key
   *
   * @example I18n.ui(I18nInternalNS.ui.blockTunes.toggler, 'Click to tune')
   * @param internalNamespace - path to translated string in dictionary
   * @param dictKey - dictionary key. Better to use default locale original text
   */
  static ui(e, t) {
    return Jr._t(e, t);
  }
  /**
   * Translate for external strings that is not presented in default dictionary.
   * For example, for user-specified tool names
   *
   * @param namespace - path to translated string in dictionary
   * @param dictKey - dictionary key. Better to use default locale original text
   */
  static t(e, t) {
    return Jr._t(e, t);
  }
  /**
   * Adjust module for using external dictionary
   *
   * @param dictionary - new messages list to override default
   */
  static setDictionary(e) {
    Jr.currentDictionary = e;
  }
  /**
   * Perform translation both for internal and external namespaces
   * If there is no translation found, returns passed key as a translated message
   *
   * @param namespace - path to translated string in dictionary
   * @param dictKey - dictionary key. Better to use default locale original text
   */
  static _t(e, t) {
    const n = Jr.getNamespace(e);
    return !n || !n[t] ? t : n[t];
  }
  /**
   * Find messages section by namespace path
   *
   * @param namespace - path to section
   */
  static getNamespace(e) {
    return e.split(".").reduce((t, n) => !t || !Object.keys(t).length ? {} : t[n], Jr.currentDictionary);
  }
};
_h.currentDictionary = Th;
let Ko = _h, Eh = class extends Error {
};
class Va {
  constructor() {
    this.subscribers = {};
  }
  /**
   * Subscribe any event on callback
   *
   * @param eventName - event name
   * @param callback - subscriber
   */
  on(e, t) {
    e in this.subscribers || (this.subscribers[e] = []), this.subscribers[e].push(t);
  }
  /**
   * Subscribe any event on callback. Callback will be called once and be removed from subscribers array after call.
   *
   * @param eventName - event name
   * @param callback - subscriber
   */
  once(e, t) {
    e in this.subscribers || (this.subscribers[e] = []);
    const n = (r) => {
      const a = t(r), c = this.subscribers[e].indexOf(n);
      return c !== -1 && this.subscribers[e].splice(c, 1), a;
    };
    this.subscribers[e].push(n);
  }
  /**
   * Emit callbacks with passed data
   *
   * @param eventName - event name
   * @param data - subscribers get this data when they were fired
   */
  emit(e, t) {
    sn(this.subscribers) || !this.subscribers[e] || this.subscribers[e].reduce((n, r) => {
      const a = r(n);
      return a !== void 0 ? a : n;
    }, t);
  }
  /**
   * Unsubscribe callback from event
   *
   * @param eventName - event name
   * @param callback - event handler
   */
  off(e, t) {
    if (this.subscribers[e] === void 0) {
      console.warn(`EventDispatcher .off(): there is no subscribers for event "${e.toString()}". Probably, .off() called before .on()`);
      return;
    }
    for (let n = 0; n < this.subscribers[e].length; n++)
      if (this.subscribers[e][n] === t) {
        delete this.subscribers[e][n];
        break;
      }
  }
  /**
   * Destroyer
   * clears subscribers list
   */
  destroy() {
    this.subscribers = {};
  }
}
function Er(o) {
  Object.setPrototypeOf(this, {
    /**
     * Block id
     *
     * @returns {string}
     */
    get id() {
      return o.id;
    },
    /**
     * Tool name
     *
     * @returns {string}
     */
    get name() {
      return o.name;
    },
    /**
     * Tool config passed on Editor's initialization
     *
     * @returns {ToolConfig}
     */
    get config() {
      return o.config;
    },
    /**
     * .ce-block element, that wraps plugin contents
     *
     * @returns {HTMLElement}
     */
    get holder() {
      return o.holder;
    },
    /**
     * True if Block content is empty
     *
     * @returns {boolean}
     */
    get isEmpty() {
      return o.isEmpty;
    },
    /**
     * True if Block is selected with Cross-Block selection
     *
     * @returns {boolean}
     */
    get selected() {
      return o.selected;
    },
    /**
     * Set Block's stretch state
     *
     * @param {boolean} state — state to set
     */
    set stretched(e) {
      o.stretched = e;
    },
    /**
     * True if Block is stretched
     *
     * @returns {boolean}
     */
    get stretched() {
      return o.stretched;
    },
    /**
     * True if Block has inputs to be focused
     */
    get focusable() {
      return o.focusable;
    },
    /**
     * Call Tool method with errors handler under-the-hood
     *
     * @param {string} methodName - method to call
     * @param {object} param - object with parameters
     * @returns {unknown}
     */
    call(e, t) {
      return o.call(e, t);
    },
    /**
     * Save Block content
     *
     * @returns {Promise<void|SavedData>}
     */
    save() {
      return o.save();
    },
    /**
     * Validate Block data
     *
     * @param {BlockToolData} data - data to validate
     * @returns {Promise<boolean>}
     */
    validate(e) {
      return o.validate(e);
    },
    /**
     * Allows to say Editor that Block was changed. Used to manually trigger Editor's 'onChange' callback
     * Can be useful for block changes invisible for editor core.
     */
    dispatchChange() {
      o.dispatchChange();
    }
  });
}
class ac {
  constructor() {
    this.allListeners = [];
  }
  /**
   * Assigns event listener on element and returns unique identifier
   *
   * @param {EventTarget} element - DOM element that needs to be listened
   * @param {string} eventType - event type
   * @param {Function} handler - method that will be fired on event
   * @param {boolean|AddEventListenerOptions} options - useCapture or {capture, passive, once}
   */
  on(e, t, n, r = !1) {
    const a = uf("l"), c = {
      id: a,
      element: e,
      eventType: t,
      handler: n,
      options: r
    };
    if (!this.findOne(e, t, n))
      return this.allListeners.push(c), e.addEventListener(t, n, r), a;
  }
  /**
   * Removes event listener from element
   *
   * @param {EventTarget} element - DOM element that we removing listener
   * @param {string} eventType - event type
   * @param {Function} handler - remove handler, if element listens several handlers on the same event type
   * @param {boolean|AddEventListenerOptions} options - useCapture or {capture, passive, once}
   */
  off(e, t, n, r) {
    const a = this.findAll(e, t, n);
    a.forEach((c, u) => {
      const p = this.allListeners.indexOf(a[u]);
      p > -1 && (this.allListeners.splice(p, 1), c.element.removeEventListener(c.eventType, c.handler, c.options));
    });
  }
  /**
   * Removes listener by id
   *
   * @param {string} id - listener identifier
   */
  offById(e) {
    const t = this.findById(e);
    t && t.element.removeEventListener(t.eventType, t.handler, t.options);
  }
  /**
   * Finds and returns first listener by passed params
   *
   * @param {EventTarget} element - event target
   * @param {string} [eventType] - event type
   * @param {Function} [handler] - event handler
   * @returns {ListenerData|null}
   */
  findOne(e, t, n) {
    const r = this.findAll(e, t, n);
    return r.length > 0 ? r[0] : null;
  }
  /**
   * Return all stored listeners by passed params
   *
   * @param {EventTarget} element - event target
   * @param {string} eventType - event type
   * @param {Function} handler - event handler
   * @returns {ListenerData[]}
   */
  findAll(e, t, n) {
    let r;
    const a = e ? this.findByEventTarget(e) : [];
    return e && t && n ? r = a.filter((c) => c.eventType === t && c.handler === n) : e && t ? r = a.filter((c) => c.eventType === t) : r = a, r;
  }
  /**
   * Removes all listeners
   */
  removeAll() {
    this.allListeners.map((e) => {
      e.element.removeEventListener(e.eventType, e.handler, e.options);
    }), this.allListeners = [];
  }
  /**
   * Module cleanup on destruction
   */
  destroy() {
    this.removeAll();
  }
  /**
   * Search method: looks for listener by passed element
   *
   * @param {EventTarget} element - searching element
   * @returns {Array} listeners that found on element
   */
  findByEventTarget(e) {
    return this.allListeners.filter((t) => {
      if (t.element === e)
        return t;
    });
  }
  /**
   * Search method: looks for listener by passed event type
   *
   * @param {string} eventType - event type
   * @returns {ListenerData[]} listeners that found on element
   */
  findByType(e) {
    return this.allListeners.filter((t) => {
      if (t.eventType === e)
        return t;
    });
  }
  /**
   * Search method: looks for listener by passed handler
   *
   * @param {Function} handler - event handler
   * @returns {ListenerData[]} listeners that found on element
   */
  findByHandler(e) {
    return this.allListeners.filter((t) => {
      if (t.handler === e)
        return t;
    });
  }
  /**
   * Returns listener data found by id
   *
   * @param {string} id - listener identifier
   * @returns {ListenerData}
   */
  findById(e) {
    return this.allListeners.find((t) => t.id === e);
  }
}
let rt = class Mh {
  /**
   * @class
   * @param options - Module options
   * @param options.config - Module config
   * @param options.eventsDispatcher - Common event bus
   */
  constructor({ config: e, eventsDispatcher: t }) {
    if (this.nodes = {}, this.listeners = new ac(), this.readOnlyMutableListeners = {
      /**
       * Assigns event listener on DOM element and pushes into special array that might be removed
       *
       * @param {EventTarget} element - DOM Element
       * @param {string} eventType - Event name
       * @param {Function} handler - Event handler
       * @param {boolean|AddEventListenerOptions} options - Listening options
       */
      on: (n, r, a, c = !1) => {
        this.mutableListenerIds.push(
          this.listeners.on(n, r, a, c)
        );
      },
      /**
       * Clears all mutable listeners
       */
      clearAll: () => {
        for (const n of this.mutableListenerIds)
          this.listeners.offById(n);
        this.mutableListenerIds = [];
      }
    }, this.mutableListenerIds = [], new.target === Mh)
      throw new TypeError("Constructors for abstract class Module are not allowed.");
    this.config = e, this.eventsDispatcher = t;
  }
  /**
   * Editor modules setter
   *
   * @param {EditorModules} Editor - Editor's Modules
   */
  set state(e) {
    this.Editor = e;
  }
  /**
   * Remove memorized nodes
   */
  removeAllNodes() {
    for (const e in this.nodes) {
      const t = this.nodes[e];
      t instanceof HTMLElement && t.remove();
    }
  }
  /**
   * Returns true if current direction is RTL (Right-To-Left)
   */
  get isRtl() {
    return this.config.i18n.direction === "rtl";
  }
}, Fe = class Fi {
  constructor() {
    this.instance = null, this.selection = null, this.savedSelectionRange = null, this.isFakeBackgroundEnabled = !1, this.commandBackground = "backColor", this.commandRemoveFormat = "removeFormat";
  }
  /**
   * Editor styles
   *
   * @returns {{editorWrapper: string, editorZone: string}}
   */
  static get CSS() {
    return {
      editorWrapper: "codex-editor",
      editorZone: "codex-editor__redactor"
    };
  }
  /**
   * Returns selected anchor
   * {@link https://developer.mozilla.org/ru/docs/Web/API/Selection/anchorNode}
   *
   * @returns {Node|null}
   */
  static get anchorNode() {
    const e = window.getSelection();
    return e ? e.anchorNode : null;
  }
  /**
   * Returns selected anchor element
   *
   * @returns {Element|null}
   */
  static get anchorElement() {
    const e = window.getSelection();
    if (!e)
      return null;
    const t = e.anchorNode;
    return t ? ae.isElement(t) ? t : t.parentElement : null;
  }
  /**
   * Returns selection offset according to the anchor node
   * {@link https://developer.mozilla.org/ru/docs/Web/API/Selection/anchorOffset}
   *
   * @returns {number|null}
   */
  static get anchorOffset() {
    const e = window.getSelection();
    return e ? e.anchorOffset : null;
  }
  /**
   * Is current selection range collapsed
   *
   * @returns {boolean|null}
   */
  static get isCollapsed() {
    const e = window.getSelection();
    return e ? e.isCollapsed : null;
  }
  /**
   * Check current selection if it is at Editor's zone
   *
   * @returns {boolean}
   */
  static get isAtEditor() {
    return this.isSelectionAtEditor(Fi.get());
  }
  /**
   * Check if passed selection is at Editor's zone
   *
   * @param selection - Selection object to check
   */
  static isSelectionAtEditor(e) {
    if (!e)
      return !1;
    let t = e.anchorNode || e.focusNode;
    t && t.nodeType === Node.TEXT_NODE && (t = t.parentNode);
    let n = null;
    return t && t instanceof Element && (n = t.closest(`.${Fi.CSS.editorZone}`)), n ? n.nodeType === Node.ELEMENT_NODE : !1;
  }
  /**
   * Check if passed range at Editor zone
   *
   * @param range - range to check
   */
  static isRangeAtEditor(e) {
    if (!e)
      return;
    let t = e.startContainer;
    t && t.nodeType === Node.TEXT_NODE && (t = t.parentNode);
    let n = null;
    return t && t instanceof Element && (n = t.closest(`.${Fi.CSS.editorZone}`)), n ? n.nodeType === Node.ELEMENT_NODE : !1;
  }
  /**
   * Methods return boolean that true if selection exists on the page
   */
  static get isSelectionExists() {
    return !!Fi.get().anchorNode;
  }
  /**
   * Return first range
   *
   * @returns {Range|null}
   */
  static get range() {
    return this.getRangeFromSelection(this.get());
  }
  /**
   * Returns range from passed Selection object
   *
   * @param selection - Selection object to get Range from
   */
  static getRangeFromSelection(e) {
    return e && e.rangeCount ? e.getRangeAt(0) : null;
  }
  /**
   * Calculates position and size of selected text
   *
   * @returns {DOMRect | ClientRect}
   */
  static get rect() {
    let e = document.selection, t, n = {
      x: 0,
      y: 0,
      width: 0,
      height: 0
    };
    if (e && e.type !== "Control")
      return e = e, t = e.createRange(), n.x = t.boundingLeft, n.y = t.boundingTop, n.width = t.boundingWidth, n.height = t.boundingHeight, n;
    if (!window.getSelection)
      return bt("Method window.getSelection is not supported", "warn"), n;
    if (e = window.getSelection(), e.rangeCount === null || isNaN(e.rangeCount))
      return bt("Method SelectionUtils.rangeCount is not supported", "warn"), n;
    if (e.rangeCount === 0)
      return n;
    if (t = e.getRangeAt(0).cloneRange(), t.getBoundingClientRect && (n = t.getBoundingClientRect()), n.x === 0 && n.y === 0) {
      const r = document.createElement("span");
      if (r.getBoundingClientRect) {
        r.appendChild(document.createTextNode("​")), t.insertNode(r), n = r.getBoundingClientRect();
        const a = r.parentNode;
        a.removeChild(r), a.normalize();
      }
    }
    return n;
  }
  /**
   * Returns selected text as String
   *
   * @returns {string}
   */
  static get text() {
    return window.getSelection ? window.getSelection().toString() : "";
  }
  /**
   * Returns window SelectionUtils
   * {@link https://developer.mozilla.org/ru/docs/Web/API/Window/getSelection}
   *
   * @returns {Selection}
   */
  static get() {
    return window.getSelection();
  }
  /**
   * Set focus to contenteditable or native input element
   *
   * @param element - element where to set focus
   * @param offset - offset of cursor
   */
  static setCursor(e, t = 0) {
    const n = document.createRange(), r = window.getSelection();
    return ae.isNativeInput(e) ? ae.canSetCaret(e) ? (e.focus(), e.selectionStart = e.selectionEnd = t, e.getBoundingClientRect()) : void 0 : (n.setStart(e, t), n.setEnd(e, t), r.removeAllRanges(), r.addRange(n), n.getBoundingClientRect());
  }
  /**
   * Check if current range exists and belongs to container
   *
   * @param container - where range should be
   */
  static isRangeInsideContainer(e) {
    const t = Fi.range;
    return t === null ? !1 : e.contains(t.startContainer);
  }
  /**
   * Adds fake cursor to the current range
   */
  static addFakeCursor() {
    const e = Fi.range;
    if (e === null)
      return;
    const t = ae.make("span", "codex-editor__fake-cursor");
    t.dataset.mutationFree = "true", e.collapse(), e.insertNode(t);
  }
  /**
   * Check if passed element contains a fake cursor
   *
   * @param el - where to check
   */
  static isFakeCursorInsideContainer(e) {
    return ae.find(e, ".codex-editor__fake-cursor") !== null;
  }
  /**
   * Removes fake cursor from a container
   *
   * @param container - container to look for
   */
  static removeFakeCursor(e = document.body) {
    const t = ae.find(e, ".codex-editor__fake-cursor");
    t && t.remove();
  }
  /**
   * Removes fake background
   */
  removeFakeBackground() {
    this.isFakeBackgroundEnabled && (this.isFakeBackgroundEnabled = !1, document.execCommand(this.commandRemoveFormat));
  }
  /**
   * Sets fake background
   */
  setFakeBackground() {
    document.execCommand(this.commandBackground, !1, "#a8d6ff"), this.isFakeBackgroundEnabled = !0;
  }
  /**
   * Save SelectionUtils's range
   */
  save() {
    this.savedSelectionRange = Fi.range;
  }
  /**
   * Restore saved SelectionUtils's range
   */
  restore() {
    if (!this.savedSelectionRange)
      return;
    const e = window.getSelection();
    e.removeAllRanges(), e.addRange(this.savedSelectionRange);
  }
  /**
   * Clears saved selection
   */
  clearSaved() {
    this.savedSelectionRange = null;
  }
  /**
   * Collapse current selection
   */
  collapseToEnd() {
    const e = window.getSelection(), t = document.createRange();
    t.selectNodeContents(e.focusNode), t.collapse(!1), e.removeAllRanges(), e.addRange(t);
  }
  /**
   * Looks ahead to find passed tag from current selection
   *
   * @param  {string} tagName       - tag to found
   * @param  {string} [className]   - tag's class name
   * @param  {number} [searchDepth] - count of tags that can be included. For better performance.
   * @returns {HTMLElement|null}
   */
  findParentTag(e, t, n = 10) {
    const r = window.getSelection();
    let a = null;
    return !r || !r.anchorNode || !r.focusNode ? null : ([
      /** the Node in which the selection begins */
      r.anchorNode,
      /** the Node in which the selection ends */
      r.focusNode
    ].forEach((c) => {
      let u = n;
      for (; u > 0 && c.parentNode && !(c.tagName === e && (a = c, t && c.classList && !c.classList.contains(t) && (a = null), a)); )
        c = c.parentNode, u--;
    }), a);
  }
  /**
   * Expands selection range to the passed parent node
   *
   * @param {HTMLElement} element - element which contents should be selected
   */
  expandToTag(e) {
    const t = window.getSelection();
    t.removeAllRanges();
    const n = document.createRange();
    n.selectNodeContents(e), t.addRange(n);
  }
};
function vf(o, e) {
  const { type: t, target: n, addedNodes: r, removedNodes: a } = o;
  if (n === e)
    return !0;
  if (["characterData", "attributes"].includes(t)) {
    const p = n.nodeType === Node.TEXT_NODE ? n.parentNode : n;
    return e.contains(p);
  }
  const c = Array.from(r).some((p) => e.contains(p)), u = Array.from(a).some((p) => e.contains(p));
  return c || u;
}
const Yl = "redactor dom changed", Dh = "block changed", Lh = "fake cursor is about to be toggled", Oh = "fake cursor have been set";
function pd(o, e) {
  return o.mergeable && o.name === e.name;
}
function yf(o, e) {
  const t = e == null ? void 0 : e.export;
  return Dt(t) ? t(o) : Qi(t) ? o[t] : (t !== void 0 && bt("Conversion «export» property must be a string or function. String means key of saved data object to export. Function should export processed string to export."), "");
}
function wf(o, e) {
  const t = e == null ? void 0 : e.import;
  return Dt(t) ? t(o) : Qi(t) ? {
    [t]: o
  } : (t !== void 0 && bt("Conversion «import» property must be a string or function. String means key of tool data to import. Function accepts a imported string and return composed tool data."), {});
}
var ui = /* @__PURE__ */ ((o) => (o.APPEND_CALLBACK = "appendCallback", o.RENDERED = "rendered", o.MOVED = "moved", o.UPDATED = "updated", o.REMOVED = "removed", o.ON_PASTE = "onPaste", o))(ui || {});
let pi = class zi extends Va {
  /**
   * @param options - block constructor options
   * @param [options.id] - block's id. Will be generated if omitted.
   * @param options.data - Tool's initial data
   * @param options.tool — block's tool
   * @param options.api - Editor API module for pass it to the Block Tunes
   * @param options.readOnly - Read-Only flag
   * @param [eventBus] - Editor common event bus. Allows to subscribe on some Editor events. Could be omitted when "virtual" Block is created. See BlocksAPI@composeBlockData.
   */
  constructor({
    id: e = df(),
    data: t,
    tool: n,
    api: r,
    readOnly: a,
    tunesData: c
  }, u) {
    super(), this.cachedInputs = [], this.toolRenderedElement = null, this.tunesInstances = /* @__PURE__ */ new Map(), this.defaultTunesInstances = /* @__PURE__ */ new Map(), this.unavailableTunesData = {}, this.inputIndex = 0, this.editorEventBus = null, this.handleFocus = () => {
      this.dropInputsCache(), this.updateCurrentInput();
    }, this.didMutated = (p = void 0) => {
      const g = p === void 0, v = p instanceof InputEvent;
      !g && !v && this.detectToolRootChange(p);
      let k;
      g || v ? k = !0 : k = !(p.length > 0 && p.every((y) => {
        const { addedNodes: T, removedNodes: x, target: M } = y;
        return [
          ...Array.from(T),
          ...Array.from(x),
          M
        ].some((L) => (ae.isElement(L) || (L = L.parentElement), L && L.closest('[data-mutation-free="true"]') !== null));
      })), k && (this.dropInputsCache(), this.updateCurrentInput(), this.call(
        "updated"
        /* UPDATED */
      ), this.emit("didMutated", this));
    }, this.name = n.name, this.id = e, this.settings = n.settings, this.config = n.settings.config || {}, this.api = r, this.editorEventBus = u || null, this.blockAPI = new Er(this), this.tool = n, this.toolInstance = n.create(t, this.blockAPI, a), this.tunes = n.tunes, this.composeTunes(c), this.holder = this.compose(), window.requestIdleCallback(() => {
      this.watchBlockMutations(), this.addInputEvents();
    });
  }
  /**
   * CSS classes for the Block
   *
   * @returns {{wrapper: string, content: string}}
   */
  static get CSS() {
    return {
      wrapper: "ce-block",
      wrapperStretched: "ce-block--stretched",
      content: "ce-block__content",
      selected: "ce-block--selected",
      dropTarget: "ce-block--drop-target"
    };
  }
  /**
   * Find and return all editable elements (contenteditable and native inputs) in the Tool HTML
   *
   * @returns {HTMLElement[]}
   */
  get inputs() {
    if (this.cachedInputs.length !== 0)
      return this.cachedInputs;
    const e = ae.findAllInputs(this.holder);
    return this.inputIndex > e.length - 1 && (this.inputIndex = e.length - 1), this.cachedInputs = e, e;
  }
  /**
   * Return current Tool`s input
   *
   * @returns {HTMLElement}
   */
  get currentInput() {
    return this.inputs[this.inputIndex];
  }
  /**
   * Set input index to the passed element
   *
   * @param {HTMLElement | Node} element - HTML Element to set as current input
   */
  set currentInput(e) {
    const t = this.inputs.findIndex((n) => n === e || n.contains(e));
    t !== -1 && (this.inputIndex = t);
  }
  /**
   * Return first Tool`s input
   *
   * @returns {HTMLElement}
   */
  get firstInput() {
    return this.inputs[0];
  }
  /**
   * Return first Tool`s input
   *
   * @returns {HTMLElement}
   */
  get lastInput() {
    const e = this.inputs;
    return e[e.length - 1];
  }
  /**
   * Return next Tool`s input or undefined if it doesn't exist
   *
   * @returns {HTMLElement}
   */
  get nextInput() {
    return this.inputs[this.inputIndex + 1];
  }
  /**
   * Return previous Tool`s input or undefined if it doesn't exist
   *
   * @returns {HTMLElement}
   */
  get previousInput() {
    return this.inputs[this.inputIndex - 1];
  }
  /**
   * Get Block's JSON data
   *
   * @returns {object}
   */
  get data() {
    return this.save().then((e) => e && !sn(e.data) ? e.data : {});
  }
  /**
   * Returns tool's sanitizer config
   *
   * @returns {object}
   */
  get sanitize() {
    return this.tool.sanitizeConfig;
  }
  /**
   * is block mergeable
   * We plugin have merge function then we call it mergeable
   *
   * @returns {boolean}
   */
  get mergeable() {
    return Dt(this.toolInstance.merge);
  }
  /**
   * If Block contains inputs, it is focusable
   */
  get focusable() {
    return this.inputs.length !== 0;
  }
  /**
   * Check block for emptiness
   *
   * @returns {boolean}
   */
  get isEmpty() {
    const e = ae.isEmpty(this.pluginsContent, "/"), t = !this.hasMedia;
    return e && t;
  }
  /**
   * Check if block has a media content such as images, iframe and other
   *
   * @returns {boolean}
   */
  get hasMedia() {
    const e = [
      "img",
      "iframe",
      "video",
      "audio",
      "source",
      "input",
      "textarea",
      "twitterwidget"
    ];
    return !!this.holder.querySelector(e.join(","));
  }
  /**
   * Set selected state
   * We don't need to mark Block as Selected when it is empty
   *
   * @param {boolean} state - 'true' to select, 'false' to remove selection
   */
  set selected(e) {
    var t, n;
    this.holder.classList.toggle(zi.CSS.selected, e);
    const r = e === !0 && Fe.isRangeInsideContainer(this.holder), a = e === !1 && Fe.isFakeCursorInsideContainer(this.holder);
    (r || a) && ((t = this.editorEventBus) == null || t.emit(Lh, { state: e }), r ? Fe.addFakeCursor() : Fe.removeFakeCursor(this.holder), (n = this.editorEventBus) == null || n.emit(Oh, { state: e }));
  }
  /**
   * Returns True if it is Selected
   *
   * @returns {boolean}
   */
  get selected() {
    return this.holder.classList.contains(zi.CSS.selected);
  }
  /**
   * Set stretched state
   *
   * @param {boolean} state - 'true' to enable, 'false' to disable stretched state
   */
  set stretched(e) {
    this.holder.classList.toggle(zi.CSS.wrapperStretched, e);
  }
  /**
   * Return Block's stretched state
   *
   * @returns {boolean}
   */
  get stretched() {
    return this.holder.classList.contains(zi.CSS.wrapperStretched);
  }
  /**
   * Toggle drop target state
   *
   * @param {boolean} state - 'true' if block is drop target, false otherwise
   */
  set dropTarget(e) {
    this.holder.classList.toggle(zi.CSS.dropTarget, e);
  }
  /**
   * Returns Plugins content
   *
   * @returns {HTMLElement}
   */
  get pluginsContent() {
    return this.toolRenderedElement;
  }
  /**
   * Calls Tool's method
   *
   * Method checks tool property {MethodName}. Fires method with passes params If it is instance of Function
   *
   * @param {string} methodName - method to call
   * @param {object} params - method argument
   */
  call(e, t) {
    if (Dt(this.toolInstance[e])) {
      e === "appendCallback" && bt(
        "`appendCallback` hook is deprecated and will be removed in the next major release. Use `rendered` hook instead",
        "warn"
      );
      try {
        this.toolInstance[e].call(this.toolInstance, t);
      } catch (n) {
        bt(`Error during '${e}' call: ${n.message}`, "error");
      }
    }
  }
  /**
   * Call plugins merge method
   *
   * @param {BlockToolData} data - data to merge
   */
  async mergeWith(e) {
    await this.toolInstance.merge(e);
  }
  /**
   * Extracts data from Block
   * Groups Tool's save processing time
   *
   * @returns {object}
   */
  async save() {
    const e = await this.toolInstance.save(this.pluginsContent), t = this.unavailableTunesData;
    [
      ...this.tunesInstances.entries(),
      ...this.defaultTunesInstances.entries()
    ].forEach(([a, c]) => {
      if (Dt(c.save))
        try {
          t[a] = c.save();
        } catch (u) {
          bt(`Tune ${c.constructor.name} save method throws an Error %o`, "warn", u);
        }
    });
    const n = window.performance.now();
    let r;
    return Promise.resolve(e).then((a) => (r = window.performance.now(), {
      id: this.id,
      tool: this.name,
      data: a,
      tunes: t,
      time: r - n
    })).catch((a) => {
      bt(`Saving process for ${this.name} tool failed due to the ${a}`, "log", "red");
    });
  }
  /**
   * Uses Tool's validation method to check the correctness of output data
   * Tool's validation method is optional
   *
   * @description Method returns true|false whether data passed the validation or not
   * @param {BlockToolData} data - data to validate
   * @returns {Promise<boolean>} valid
   */
  async validate(e) {
    let t = !0;
    return this.toolInstance.validate instanceof Function && (t = await this.toolInstance.validate(e)), t;
  }
  /**
   * Returns data to render in tunes menu.
   * Splits block tunes settings into 2 groups: popover items and custom html.
   */
  getTunes() {
    const e = document.createElement("div"), t = [], n = typeof this.toolInstance.renderSettings == "function" ? this.toolInstance.renderSettings() : [], r = [
      ...this.tunesInstances.values(),
      ...this.defaultTunesInstances.values()
    ].map((a) => a.render());
    return [n, r].flat().forEach((a) => {
      ae.isElement(a) ? e.appendChild(a) : Array.isArray(a) ? t.push(...a) : t.push(a);
    }), [t, e];
  }
  /**
   * Update current input index with selection anchor node
   */
  updateCurrentInput() {
    this.currentInput = ae.isNativeInput(document.activeElement) || !Fe.anchorNode ? document.activeElement : Fe.anchorNode;
  }
  /**
   * Allows to say Editor that Block was changed. Used to manually trigger Editor's 'onChange' callback
   * Can be useful for block changes invisible for editor core.
   */
  dispatchChange() {
    this.didMutated();
  }
  /**
   * Call Tool instance destroy method
   */
  destroy() {
    this.unwatchBlockMutations(), this.removeInputEvents(), super.destroy(), Dt(this.toolInstance.destroy) && this.toolInstance.destroy();
  }
  /**
   * Tool could specify several entries to be displayed at the Toolbox (for example, "Heading 1", "Heading 2", "Heading 3")
   * This method returns the entry that is related to the Block (depended on the Block data)
   */
  async getActiveToolboxEntry() {
    const e = this.tool.toolbox;
    if (e.length === 1)
      return Promise.resolve(this.tool.toolbox[0]);
    const t = await this.data;
    return e.find((n) => Object.entries(n.data).some(([r, a]) => t[r] && pf(t[r], a)));
  }
  /**
   * Exports Block data as string using conversion config
   */
  async exportDataAsString() {
    const e = await this.data;
    return yf(e, this.tool.conversionConfig);
  }
  /**
   * Make default Block wrappers and put Tool`s content there
   *
   * @returns {HTMLDivElement}
   */
  compose() {
    const e = ae.make("div", zi.CSS.wrapper), t = ae.make("div", zi.CSS.content), n = this.toolInstance.render();
    e.dataset.id = this.id, this.toolRenderedElement = n, t.appendChild(this.toolRenderedElement);
    let r = t;
    return [...this.tunesInstances.values(), ...this.defaultTunesInstances.values()].forEach((a) => {
      if (Dt(a.wrap))
        try {
          r = a.wrap(r);
        } catch (c) {
          bt(`Tune ${a.constructor.name} wrap method throws an Error %o`, "warn", c);
        }
    }), e.appendChild(r), e;
  }
  /**
   * Instantiate Block Tunes
   *
   * @param tunesData - current Block tunes data
   * @private
   */
  composeTunes(e) {
    Array.from(this.tunes.values()).forEach((t) => {
      (t.isInternal ? this.defaultTunesInstances : this.tunesInstances).set(t.name, t.create(e[t.name], this.blockAPI));
    }), Object.entries(e).forEach(([t, n]) => {
      this.tunesInstances.has(t) || (this.unavailableTunesData[t] = n);
    });
  }
  /**
   * Adds focus event listeners to all inputs and contenteditable
   */
  addInputEvents() {
    this.inputs.forEach((e) => {
      e.addEventListener("focus", this.handleFocus), ae.isNativeInput(e) && e.addEventListener("input", this.didMutated);
    });
  }
  /**
   * removes focus event listeners from all inputs and contenteditable
   */
  removeInputEvents() {
    this.inputs.forEach((e) => {
      e.removeEventListener("focus", this.handleFocus), ae.isNativeInput(e) && e.removeEventListener("input", this.didMutated);
    });
  }
  /**
   * Listen common editor Dom Changed event and detect mutations related to the  Block
   */
  watchBlockMutations() {
    var e;
    this.redactorDomChangedCallback = (t) => {
      const { mutations: n } = t;
      n.some((r) => vf(r, this.toolRenderedElement)) && this.didMutated(n);
    }, (e = this.editorEventBus) == null || e.on(Yl, this.redactorDomChangedCallback);
  }
  /**
   * Remove redactor dom change event listener
   */
  unwatchBlockMutations() {
    var e;
    (e = this.editorEventBus) == null || e.off(Yl, this.redactorDomChangedCallback);
  }
  /**
   * Sometimes Tool can replace own main element, for example H2 -> H4 or UL -> OL
   * We need to detect such changes and update a link to tools main element with the new one
   *
   * @param mutations - records of block content mutations
   */
  detectToolRootChange(e) {
    e.forEach((t) => {
      if (Array.from(t.removedNodes).includes(this.toolRenderedElement)) {
        const n = t.addedNodes[t.addedNodes.length - 1];
        this.toolRenderedElement = n;
      }
    });
  }
  /**
   * Clears inputs cached value
   */
  dropInputsCache() {
    this.cachedInputs = [];
  }
};
class kf extends rt {
  constructor() {
    super(...arguments), this.insert = (e = this.config.defaultBlock, t = {}, n = {}, r, a, c, u) => {
      const p = this.Editor.BlockManager.insert({
        id: u,
        tool: e,
        data: t,
        index: r,
        needToFocus: a,
        replace: c
      });
      return new Er(p);
    }, this.composeBlockData = async (e) => {
      const t = this.Editor.Tools.blockTools.get(e);
      return new pi({
        tool: t,
        api: this.Editor.API,
        readOnly: !0,
        data: {},
        tunesData: {}
      }).data;
    }, this.update = async (e, t) => {
      const { BlockManager: n } = this.Editor, r = n.getBlockById(e);
      if (r === void 0)
        throw new Error(`Block with id "${e}" not found`);
      const a = await n.update(r, t);
      return new Er(a);
    }, this.convert = (e, t, n) => {
      var r, a;
      const { BlockManager: c, Tools: u } = this.Editor, p = c.getBlockById(e);
      if (!p)
        throw new Error(`Block with id "${e}" not found`);
      const g = u.blockTools.get(p.name), v = u.blockTools.get(t);
      if (!v)
        throw new Error(`Block Tool with type "${t}" not found`);
      const k = ((r = g == null ? void 0 : g.conversionConfig) == null ? void 0 : r.export) !== void 0, y = ((a = v.conversionConfig) == null ? void 0 : a.import) !== void 0;
      if (k && y)
        c.convert(p, t, n);
      else {
        const T = [
          k ? !1 : fs(p.name),
          y ? !1 : fs(t)
        ].filter(Boolean).join(" and ");
        throw new Error(`Conversion from "${p.name}" to "${t}" is not possible. ${T} tool(s) should provide a "conversionConfig"`);
      }
    }, this.insertMany = (e, t = this.Editor.BlockManager.blocks.length - 1) => {
      this.validateIndex(t);
      const n = e.map(({ id: r, type: a, data: c }) => this.Editor.BlockManager.composeBlock({
        id: r,
        tool: a || this.config.defaultBlock,
        data: c
      }));
      return this.Editor.BlockManager.insertMany(n, t), n.map((r) => new Er(r));
    };
  }
  /**
   * Available methods
   *
   * @returns {Blocks}
   */
  get methods() {
    return {
      clear: () => this.clear(),
      render: (e) => this.render(e),
      renderFromHTML: (e) => this.renderFromHTML(e),
      delete: (e) => this.delete(e),
      swap: (e, t) => this.swap(e, t),
      move: (e, t) => this.move(e, t),
      getBlockByIndex: (e) => this.getBlockByIndex(e),
      getById: (e) => this.getById(e),
      getCurrentBlockIndex: () => this.getCurrentBlockIndex(),
      getBlockIndex: (e) => this.getBlockIndex(e),
      getBlocksCount: () => this.getBlocksCount(),
      stretchBlock: (e, t = !0) => this.stretchBlock(e, t),
      insertNewBlock: () => this.insertNewBlock(),
      insert: this.insert,
      insertMany: this.insertMany,
      update: this.update,
      composeBlockData: this.composeBlockData,
      convert: this.convert
    };
  }
  /**
   * Returns Blocks count
   *
   * @returns {number}
   */
  getBlocksCount() {
    return this.Editor.BlockManager.blocks.length;
  }
  /**
   * Returns current block index
   *
   * @returns {number}
   */
  getCurrentBlockIndex() {
    return this.Editor.BlockManager.currentBlockIndex;
  }
  /**
   * Returns the index of Block by id;
   *
   * @param id - block id
   */
  getBlockIndex(e) {
    const t = this.Editor.BlockManager.getBlockById(e);
    if (!t) {
      Zo("There is no block with id `" + e + "`", "warn");
      return;
    }
    return this.Editor.BlockManager.getBlockIndex(t);
  }
  /**
   * Returns BlockAPI object by Block index
   *
   * @param {number} index - index to get
   */
  getBlockByIndex(e) {
    const t = this.Editor.BlockManager.getBlockByIndex(e);
    if (t === void 0) {
      Zo("There is no block at index `" + e + "`", "warn");
      return;
    }
    return new Er(t);
  }
  /**
   * Returns BlockAPI object by Block id
   *
   * @param id - id of block to get
   */
  getById(e) {
    const t = this.Editor.BlockManager.getBlockById(e);
    return t === void 0 ? (Zo("There is no block with id `" + e + "`", "warn"), null) : new Er(t);
  }
  /**
   * Call Block Manager method that swap Blocks
   *
   * @param {number} fromIndex - position of first Block
   * @param {number} toIndex - position of second Block
   * @deprecated — use 'move' instead
   */
  swap(e, t) {
    bt(
      "`blocks.swap()` method is deprecated and will be removed in the next major release. Use `block.move()` method instead",
      "info"
    ), this.Editor.BlockManager.swap(e, t);
  }
  /**
   * Move block from one index to another
   *
   * @param {number} toIndex - index to move to
   * @param {number} fromIndex - index to move from
   */
  move(e, t) {
    this.Editor.BlockManager.move(e, t);
  }
  /**
   * Deletes Block
   *
   * @param {number} blockIndex - index of Block to delete
   */
  delete(e = this.Editor.BlockManager.currentBlockIndex) {
    try {
      const t = this.Editor.BlockManager.getBlockByIndex(e);
      this.Editor.BlockManager.removeBlock(t);
    } catch (t) {
      Zo(t, "warn");
      return;
    }
    this.Editor.BlockManager.blocks.length === 0 && this.Editor.BlockManager.insert(), this.Editor.BlockManager.currentBlock && this.Editor.Caret.setToBlock(this.Editor.BlockManager.currentBlock, this.Editor.Caret.positions.END), this.Editor.Toolbar.close();
  }
  /**
   * Clear Editor's area
   */
  async clear() {
    await this.Editor.BlockManager.clear(!0), this.Editor.InlineToolbar.close();
  }
  /**
   * Fills Editor with Blocks data
   *
   * @param {OutputData} data — Saved Editor data
   */
  async render(e) {
    if (e === void 0 || e.blocks === void 0)
      throw new Error("Incorrect data passed to the render() method");
    this.Editor.ModificationsObserver.disable(), await this.Editor.BlockManager.clear(), await this.Editor.Renderer.render(e.blocks), this.Editor.ModificationsObserver.enable();
  }
  /**
   * Render passed HTML string
   *
   * @param {string} data - HTML string to render
   * @returns {Promise<void>}
   */
  renderFromHTML(e) {
    return this.Editor.BlockManager.clear(), this.Editor.Paste.processText(e, !0);
  }
  /**
   * Stretch Block's content
   *
   * @param {number} index - index of Block to stretch
   * @param {boolean} status - true to enable, false to disable
   * @deprecated Use BlockAPI interface to stretch Blocks
   */
  stretchBlock(e, t = !0) {
    $l(
      !0,
      "blocks.stretchBlock()",
      "BlockAPI"
    );
    const n = this.Editor.BlockManager.getBlockByIndex(e);
    n && (n.stretched = t);
  }
  /**
   * Insert new Block
   * After set caret to this Block
   *
   * @todo remove in 3.0.0
   * @deprecated with insert() method
   */
  insertNewBlock() {
    bt("Method blocks.insertNewBlock() is deprecated and it will be removed in the next major release. Use blocks.insert() instead.", "warn"), this.insert();
  }
  /**
   * Validated block index and throws an error if it's invalid
   *
   * @param index - index to validate
   */
  validateIndex(e) {
    if (typeof e != "number")
      throw new Error("Index should be a number");
    if (e < 0)
      throw new Error("Index should be greater than or equal to 0");
    if (e === null)
      throw new Error("Index should be greater than or equal to 0");
  }
}
class xf extends rt {
  constructor() {
    super(...arguments), this.setToFirstBlock = (e = this.Editor.Caret.positions.DEFAULT, t = 0) => this.Editor.BlockManager.firstBlock ? (this.Editor.Caret.setToBlock(this.Editor.BlockManager.firstBlock, e, t), !0) : !1, this.setToLastBlock = (e = this.Editor.Caret.positions.DEFAULT, t = 0) => this.Editor.BlockManager.lastBlock ? (this.Editor.Caret.setToBlock(this.Editor.BlockManager.lastBlock, e, t), !0) : !1, this.setToPreviousBlock = (e = this.Editor.Caret.positions.DEFAULT, t = 0) => this.Editor.BlockManager.previousBlock ? (this.Editor.Caret.setToBlock(this.Editor.BlockManager.previousBlock, e, t), !0) : !1, this.setToNextBlock = (e = this.Editor.Caret.positions.DEFAULT, t = 0) => this.Editor.BlockManager.nextBlock ? (this.Editor.Caret.setToBlock(this.Editor.BlockManager.nextBlock, e, t), !0) : !1, this.setToBlock = (e, t = this.Editor.Caret.positions.DEFAULT, n = 0) => this.Editor.BlockManager.blocks[e] ? (this.Editor.Caret.setToBlock(this.Editor.BlockManager.blocks[e], t, n), !0) : !1, this.focus = (e = !1) => e ? this.setToLastBlock(this.Editor.Caret.positions.END) : this.setToFirstBlock(this.Editor.Caret.positions.START);
  }
  /**
   * Available methods
   *
   * @returns {Caret}
   */
  get methods() {
    return {
      setToFirstBlock: this.setToFirstBlock,
      setToLastBlock: this.setToLastBlock,
      setToPreviousBlock: this.setToPreviousBlock,
      setToNextBlock: this.setToNextBlock,
      setToBlock: this.setToBlock,
      focus: this.focus
    };
  }
}
let Cf = class extends rt {
  /**
   * Available methods
   *
   * @returns {Events}
   */
  get methods() {
    return {
      emit: (e, t) => this.emit(e, t),
      off: (e, t) => this.off(e, t),
      on: (e, t) => this.on(e, t)
    };
  }
  /**
   * Subscribe on Events
   *
   * @param {string} eventName - event name to subscribe
   * @param {Function} callback - event handler
   */
  on(e, t) {
    this.eventsDispatcher.on(e, t);
  }
  /**
   * Emit event with data
   *
   * @param {string} eventName - event to emit
   * @param {object} data - event's data
   */
  emit(e, t) {
    this.eventsDispatcher.emit(e, t);
  }
  /**
   * Unsubscribe from Event
   *
   * @param {string} eventName - event to unsubscribe
   * @param {Function} callback - event handler
   */
  off(e, t) {
    this.eventsDispatcher.off(e, t);
  }
};
class lc extends rt {
  /**
   * Return namespace section for tool or block tune
   *
   * @param tool - tool object
   */
  static getNamespace(e) {
    return e.isTune() ? `blockTunes.${e.name}` : `tools.${e.name}`;
  }
  /**
   * Return I18n API methods with global dictionary access
   */
  get methods() {
    return {
      t: () => {
        Zo("I18n.t() method can be accessed only from Tools", "warn");
      }
    };
  }
  /**
   * Return I18n API methods with tool namespaced dictionary
   *
   * @param tool - Tool object
   */
  getMethodsForTool(e) {
    return Object.assign(
      this.methods,
      {
        t: (t) => Ko.t(lc.getNamespace(e), t)
      }
    );
  }
}
let Sf = class extends rt {
  /**
   * Editor.js Core API modules
   */
  get methods() {
    return {
      blocks: this.Editor.BlocksAPI.methods,
      caret: this.Editor.CaretAPI.methods,
      events: this.Editor.EventsAPI.methods,
      listeners: this.Editor.ListenersAPI.methods,
      notifier: this.Editor.NotifierAPI.methods,
      sanitizer: this.Editor.SanitizerAPI.methods,
      saver: this.Editor.SaverAPI.methods,
      selection: this.Editor.SelectionAPI.methods,
      styles: this.Editor.StylesAPI.classes,
      toolbar: this.Editor.ToolbarAPI.methods,
      inlineToolbar: this.Editor.InlineToolbarAPI.methods,
      tooltip: this.Editor.TooltipAPI.methods,
      i18n: this.Editor.I18nAPI.methods,
      readOnly: this.Editor.ReadOnlyAPI.methods,
      ui: this.Editor.UiAPI.methods
    };
  }
  /**
   * Returns Editor.js Core API methods for passed tool
   *
   * @param tool - tool object
   */
  getMethodsForTool(e) {
    return Object.assign(
      this.methods,
      {
        i18n: this.Editor.I18nAPI.getMethodsForTool(e)
      }
    );
  }
}, Tf = class extends rt {
  /**
   * Available methods
   *
   * @returns {InlineToolbar}
   */
  get methods() {
    return {
      close: () => this.close(),
      open: () => this.open()
    };
  }
  /**
   * Open Inline Toolbar
   */
  open() {
    this.Editor.InlineToolbar.tryToShow();
  }
  /**
   * Close Inline Toolbar
   */
  close() {
    this.Editor.InlineToolbar.close();
  }
}, _f = class extends rt {
  /**
   * Available methods
   *
   * @returns {Listeners}
   */
  get methods() {
    return {
      on: (e, t, n, r) => this.on(e, t, n, r),
      off: (e, t, n, r) => this.off(e, t, n, r),
      offById: (e) => this.offById(e)
    };
  }
  /**
   * Ads a DOM event listener. Return it's id.
   *
   * @param {HTMLElement} element - Element to set handler to
   * @param {string} eventType - event type
   * @param {() => void} handler - event handler
   * @param {boolean} useCapture - capture event or not
   */
  on(e, t, n, r) {
    return this.listeners.on(e, t, n, r);
  }
  /**
   * Removes DOM listener from element
   *
   * @param {Element} element - Element to remove handler from
   * @param eventType - event type
   * @param handler - event handler
   * @param {boolean} useCapture - capture event or not
   */
  off(e, t, n, r) {
    this.listeners.off(e, t, n, r);
  }
  /**
   * Removes DOM listener by the listener id
   *
   * @param id - id of the listener to remove
   */
  offById(e) {
    this.listeners.offById(e);
  }
};
var Bh = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(window, function() {
    return function(t) {
      var n = {};
      function r(a) {
        if (n[a])
          return n[a].exports;
        var c = n[a] = { i: a, l: !1, exports: {} };
        return t[a].call(c.exports, c, c.exports, r), c.l = !0, c.exports;
      }
      return r.m = t, r.c = n, r.d = function(a, c, u) {
        r.o(a, c) || Object.defineProperty(a, c, { enumerable: !0, get: u });
      }, r.r = function(a) {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
      }, r.t = function(a, c) {
        if (1 & c && (a = r(a)), 8 & c || 4 & c && typeof a == "object" && a && a.__esModule)
          return a;
        var u = /* @__PURE__ */ Object.create(null);
        if (r.r(u), Object.defineProperty(u, "default", { enumerable: !0, value: a }), 2 & c && typeof a != "string")
          for (var p in a)
            r.d(u, p, (function(g) {
              return a[g];
            }).bind(null, p));
        return u;
      }, r.n = function(a) {
        var c = a && a.__esModule ? function() {
          return a.default;
        } : function() {
          return a;
        };
        return r.d(c, "a", c), c;
      }, r.o = function(a, c) {
        return Object.prototype.hasOwnProperty.call(a, c);
      }, r.p = "/", r(r.s = 0);
    }([function(t, n, r) {
      r(1), /*!
      * Codex JavaScript Notification module
      * https://github.com/codex-team/js-notifier
      */
      t.exports = function() {
        var a = r(6), c = "cdx-notify--bounce-in", u = null;
        return { show: function(p) {
          if (p.message) {
            (function() {
              if (u)
                return !0;
              u = a.getWrapper(), document.body.appendChild(u);
            })();
            var g = null, v = p.time || 8e3;
            switch (p.type) {
              case "confirm":
                g = a.confirm(p);
                break;
              case "prompt":
                g = a.prompt(p);
                break;
              default:
                g = a.alert(p), window.setTimeout(function() {
                  g.remove();
                }, v);
            }
            u.appendChild(g), g.classList.add(c);
          }
        } };
      }();
    }, function(t, n, r) {
      var a = r(2);
      typeof a == "string" && (a = [[t.i, a, ""]]);
      var c = { hmr: !0, transform: void 0, insertInto: void 0 };
      r(4)(a, c), a.locals && (t.exports = a.locals);
    }, function(t, n, r) {
      (t.exports = r(3)(!1)).push([t.i, `.cdx-notify--error{background:#fffbfb!important}.cdx-notify--error::before{background:#fb5d5d!important}.cdx-notify__input{max-width:130px;padding:5px 10px;background:#f7f7f7;border:0;border-radius:3px;font-size:13px;color:#656b7c;outline:0}.cdx-notify__input:-ms-input-placeholder{color:#656b7c}.cdx-notify__input::placeholder{color:#656b7c}.cdx-notify__input:focus:-ms-input-placeholder{color:rgba(101,107,124,.3)}.cdx-notify__input:focus::placeholder{color:rgba(101,107,124,.3)}.cdx-notify__button{border:none;border-radius:3px;font-size:13px;padding:5px 10px;cursor:pointer}.cdx-notify__button:last-child{margin-left:10px}.cdx-notify__button--cancel{background:#f2f5f7;box-shadow:0 2px 1px 0 rgba(16,19,29,0);color:#656b7c}.cdx-notify__button--cancel:hover{background:#eee}.cdx-notify__button--confirm{background:#34c992;box-shadow:0 1px 1px 0 rgba(18,49,35,.05);color:#fff}.cdx-notify__button--confirm:hover{background:#33b082}.cdx-notify__btns-wrapper{display:-ms-flexbox;display:flex;-ms-flex-flow:row nowrap;flex-flow:row nowrap;margin-top:5px}.cdx-notify__cross{position:absolute;top:5px;right:5px;width:10px;height:10px;padding:5px;opacity:.54;cursor:pointer}.cdx-notify__cross::after,.cdx-notify__cross::before{content:'';position:absolute;left:9px;top:5px;height:12px;width:2px;background:#575d67}.cdx-notify__cross::before{transform:rotate(-45deg)}.cdx-notify__cross::after{transform:rotate(45deg)}.cdx-notify__cross:hover{opacity:1}.cdx-notifies{position:fixed;z-index:2;bottom:20px;left:20px;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Oxygen,Ubuntu,Cantarell,"Fira Sans","Droid Sans","Helvetica Neue",sans-serif}.cdx-notify{position:relative;width:220px;margin-top:15px;padding:13px 16px;background:#fff;box-shadow:0 11px 17px 0 rgba(23,32,61,.13);border-radius:5px;font-size:14px;line-height:1.4em;word-wrap:break-word}.cdx-notify::before{content:'';position:absolute;display:block;top:0;left:0;width:3px;height:calc(100% - 6px);margin:3px;border-radius:5px;background:0 0}@keyframes bounceIn{0%{opacity:0;transform:scale(.3)}50%{opacity:1;transform:scale(1.05)}70%{transform:scale(.9)}100%{transform:scale(1)}}.cdx-notify--bounce-in{animation-name:bounceIn;animation-duration:.6s;animation-iteration-count:1}.cdx-notify--success{background:#fafffe!important}.cdx-notify--success::before{background:#41ffb1!important}`, ""]);
    }, function(t, n) {
      t.exports = function(r) {
        var a = [];
        return a.toString = function() {
          return this.map(function(c) {
            var u = function(p, g) {
              var v = p[1] || "", k = p[3];
              if (!k)
                return v;
              if (g && typeof btoa == "function") {
                var y = (x = k, "/*# sourceMappingURL=data:application/json;charset=utf-8;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(x)))) + " */"), T = k.sources.map(function(M) {
                  return "/*# sourceURL=" + k.sourceRoot + M + " */";
                });
                return [v].concat(T).concat([y]).join(`
`);
              }
              var x;
              return [v].join(`
`);
            }(c, r);
            return c[2] ? "@media " + c[2] + "{" + u + "}" : u;
          }).join("");
        }, a.i = function(c, u) {
          typeof c == "string" && (c = [[null, c, ""]]);
          for (var p = {}, g = 0; g < this.length; g++) {
            var v = this[g][0];
            typeof v == "number" && (p[v] = !0);
          }
          for (g = 0; g < c.length; g++) {
            var k = c[g];
            typeof k[0] == "number" && p[k[0]] || (u && !k[2] ? k[2] = u : u && (k[2] = "(" + k[2] + ") and (" + u + ")"), a.push(k));
          }
        }, a;
      };
    }, function(t, n, r) {
      var a, c, u = {}, p = (a = function() {
        return window && document && document.all && !window.atob;
      }, function() {
        return c === void 0 && (c = a.apply(this, arguments)), c;
      }), g = function(j) {
        var R = {};
        return function(V) {
          if (typeof V == "function")
            return V();
          if (R[V] === void 0) {
            var K = (function(J) {
              return document.querySelector(J);
            }).call(this, V);
            if (window.HTMLIFrameElement && K instanceof window.HTMLIFrameElement)
              try {
                K = K.contentDocument.head;
              } catch {
                K = null;
              }
            R[V] = K;
          }
          return R[V];
        };
      }(), v = null, k = 0, y = [], T = r(5);
      function x(j, R) {
        for (var V = 0; V < j.length; V++) {
          var K = j[V], J = u[K.id];
          if (J) {
            J.refs++;
            for (var A = 0; A < J.parts.length; A++)
              J.parts[A](K.parts[A]);
            for (; A < K.parts.length; A++)
              J.parts.push(Q(K.parts[A], R));
          } else {
            var se = [];
            for (A = 0; A < K.parts.length; A++)
              se.push(Q(K.parts[A], R));
            u[K.id] = { id: K.id, refs: 1, parts: se };
          }
        }
      }
      function M(j, R) {
        for (var V = [], K = {}, J = 0; J < j.length; J++) {
          var A = j[J], se = R.base ? A[0] + R.base : A[0], Z = { css: A[1], media: A[2], sourceMap: A[3] };
          K[se] ? K[se].parts.push(Z) : V.push(K[se] = { id: se, parts: [Z] });
        }
        return V;
      }
      function L(j, R) {
        var V = g(j.insertInto);
        if (!V)
          throw new Error("Couldn't find a style target. This probably means that the value for the 'insertInto' parameter is invalid.");
        var K = y[y.length - 1];
        if (j.insertAt === "top")
          K ? K.nextSibling ? V.insertBefore(R, K.nextSibling) : V.appendChild(R) : V.insertBefore(R, V.firstChild), y.push(R);
        else if (j.insertAt === "bottom")
          V.appendChild(R);
        else {
          if (typeof j.insertAt != "object" || !j.insertAt.before)
            throw new Error(`[Style Loader]

 Invalid value for parameter 'insertAt' ('options.insertAt') found.
 Must be 'top', 'bottom', or Object.
 (https://github.com/webpack-contrib/style-loader#insertat)
`);
          var J = g(j.insertInto + " " + j.insertAt.before);
          V.insertBefore(R, J);
        }
      }
      function Y(j) {
        if (j.parentNode === null)
          return !1;
        j.parentNode.removeChild(j);
        var R = y.indexOf(j);
        R >= 0 && y.splice(R, 1);
      }
      function I(j) {
        var R = document.createElement("style");
        return j.attrs.type === void 0 && (j.attrs.type = "text/css"), F(R, j.attrs), L(j, R), R;
      }
      function F(j, R) {
        Object.keys(R).forEach(function(V) {
          j.setAttribute(V, R[V]);
        });
      }
      function Q(j, R) {
        var V, K, J, A;
        if (R.transform && j.css) {
          if (!(A = R.transform(j.css)))
            return function() {
            };
          j.css = A;
        }
        if (R.singleton) {
          var se = k++;
          V = v || (v = I(R)), K = d.bind(null, V, se, !1), J = d.bind(null, V, se, !0);
        } else
          j.sourceMap && typeof URL == "function" && typeof URL.createObjectURL == "function" && typeof URL.revokeObjectURL == "function" && typeof Blob == "function" && typeof btoa == "function" ? (V = function(Z) {
            var ne = document.createElement("link");
            return Z.attrs.type === void 0 && (Z.attrs.type = "text/css"), Z.attrs.rel = "stylesheet", F(ne, Z.attrs), L(Z, ne), ne;
          }(R), K = (function(Z, ne, le) {
            var oe = le.css, fe = le.sourceMap, Me = ne.convertToAbsoluteUrls === void 0 && fe;
            (ne.convertToAbsoluteUrls || Me) && (oe = T(oe)), fe && (oe += `
/*# sourceMappingURL=data:application/json;base64,` + btoa(unescape(encodeURIComponent(JSON.stringify(fe)))) + " */");
            var je = new Blob([oe], { type: "text/css" }), Ae = Z.href;
            Z.href = URL.createObjectURL(je), Ae && URL.revokeObjectURL(Ae);
          }).bind(null, V, R), J = function() {
            Y(V), V.href && URL.revokeObjectURL(V.href);
          }) : (V = I(R), K = (function(Z, ne) {
            var le = ne.css, oe = ne.media;
            if (oe && Z.setAttribute("media", oe), Z.styleSheet)
              Z.styleSheet.cssText = le;
            else {
              for (; Z.firstChild; )
                Z.removeChild(Z.firstChild);
              Z.appendChild(document.createTextNode(le));
            }
          }).bind(null, V), J = function() {
            Y(V);
          });
        return K(j), function(Z) {
          if (Z) {
            if (Z.css === j.css && Z.media === j.media && Z.sourceMap === j.sourceMap)
              return;
            K(j = Z);
          } else
            J();
        };
      }
      t.exports = function(j, R) {
        if (typeof DEBUG < "u" && DEBUG && typeof document != "object")
          throw new Error("The style-loader cannot be used in a non-browser environment");
        (R = R || {}).attrs = typeof R.attrs == "object" ? R.attrs : {}, R.singleton || typeof R.singleton == "boolean" || (R.singleton = p()), R.insertInto || (R.insertInto = "head"), R.insertAt || (R.insertAt = "bottom");
        var V = M(j, R);
        return x(V, R), function(K) {
          for (var J = [], A = 0; A < V.length; A++) {
            var se = V[A];
            (Z = u[se.id]).refs--, J.push(Z);
          }
          for (K && x(M(K, R), R), A = 0; A < J.length; A++) {
            var Z;
            if ((Z = J[A]).refs === 0) {
              for (var ne = 0; ne < Z.parts.length; ne++)
                Z.parts[ne]();
              delete u[Z.id];
            }
          }
        };
      };
      var U, z = (U = [], function(j, R) {
        return U[j] = R, U.filter(Boolean).join(`
`);
      });
      function d(j, R, V, K) {
        var J = V ? "" : K.css;
        if (j.styleSheet)
          j.styleSheet.cssText = z(R, J);
        else {
          var A = document.createTextNode(J), se = j.childNodes;
          se[R] && j.removeChild(se[R]), se.length ? j.insertBefore(A, se[R]) : j.appendChild(A);
        }
      }
    }, function(t, n) {
      t.exports = function(r) {
        var a = typeof window < "u" && window.location;
        if (!a)
          throw new Error("fixUrls requires window.location");
        if (!r || typeof r != "string")
          return r;
        var c = a.protocol + "//" + a.host, u = c + a.pathname.replace(/\/[^\/]*$/, "/");
        return r.replace(/url\s*\(((?:[^)(]|\((?:[^)(]+|\([^)(]*\))*\))*)\)/gi, function(p, g) {
          var v, k = g.trim().replace(/^"(.*)"$/, function(y, T) {
            return T;
          }).replace(/^'(.*)'$/, function(y, T) {
            return T;
          });
          return /^(#|data:|http:\/\/|https:\/\/|file:\/\/\/|\s*$)/i.test(k) ? p : (v = k.indexOf("//") === 0 ? k : k.indexOf("/") === 0 ? c + k : u + k.replace(/^\.\//, ""), "url(" + JSON.stringify(v) + ")");
        });
      };
    }, function(t, n, r) {
      var a, c, u, p, g, v, k, y, T;
      t.exports = (a = "cdx-notifies", c = "cdx-notify", u = "cdx-notify__cross", p = "cdx-notify__button--confirm", g = "cdx-notify__button--cancel", v = "cdx-notify__input", k = "cdx-notify__button", y = "cdx-notify__btns-wrapper", { alert: T = function(x) {
        var M = document.createElement("DIV"), L = document.createElement("DIV"), Y = x.message, I = x.style;
        return M.classList.add(c), I && M.classList.add(c + "--" + I), M.innerHTML = Y, L.classList.add(u), L.addEventListener("click", M.remove.bind(M)), M.appendChild(L), M;
      }, confirm: function(x) {
        var M = T(x), L = document.createElement("div"), Y = document.createElement("button"), I = document.createElement("button"), F = M.querySelector("." + u), Q = x.cancelHandler, U = x.okHandler;
        return L.classList.add(y), Y.innerHTML = x.okText || "Confirm", I.innerHTML = x.cancelText || "Cancel", Y.classList.add(k), I.classList.add(k), Y.classList.add(p), I.classList.add(g), Q && typeof Q == "function" && (I.addEventListener("click", Q), F.addEventListener("click", Q)), U && typeof U == "function" && Y.addEventListener("click", U), Y.addEventListener("click", M.remove.bind(M)), I.addEventListener("click", M.remove.bind(M)), L.appendChild(Y), L.appendChild(I), M.appendChild(L), M;
      }, prompt: function(x) {
        var M = T(x), L = document.createElement("div"), Y = document.createElement("button"), I = document.createElement("input"), F = M.querySelector("." + u), Q = x.cancelHandler, U = x.okHandler;
        return L.classList.add(y), Y.innerHTML = x.okText || "Ok", Y.classList.add(k), Y.classList.add(p), I.classList.add(v), x.placeholder && I.setAttribute("placeholder", x.placeholder), x.default && (I.value = x.default), x.inputType && (I.type = x.inputType), Q && typeof Q == "function" && F.addEventListener("click", Q), U && typeof U == "function" && Y.addEventListener("click", function() {
          U(I.value);
        }), Y.addEventListener("click", M.remove.bind(M)), L.appendChild(I), L.appendChild(Y), M.appendChild(L), M;
      }, getWrapper: function() {
        var x = document.createElement("DIV");
        return x.classList.add(a), x;
      } });
    }]);
  });
})(Bh);
var Ef = Bh.exports;
const Mf = /* @__PURE__ */ Ua(Ef);
let Df = class {
  /**
   * Show web notification
   *
   * @param {NotifierOptions | ConfirmNotifierOptions | PromptNotifierOptions} options - notification options
   */
  show(e) {
    Mf.show(e);
  }
}, Lf = class extends rt {
  /**
   * @param moduleConfiguration - Module Configuration
   * @param moduleConfiguration.config - Editor's config
   * @param moduleConfiguration.eventsDispatcher - Editor's event dispatcher
   */
  constructor({ config: e, eventsDispatcher: t }) {
    super({
      config: e,
      eventsDispatcher: t
    }), this.notifier = new Df();
  }
  /**
   * Available methods
   */
  get methods() {
    return {
      show: (e) => this.show(e)
    };
  }
  /**
   * Show notification
   *
   * @param {NotifierOptions} options - message option
   */
  show(e) {
    return this.notifier.show(e);
  }
}, Of = class extends rt {
  /**
   * Available methods
   */
  get methods() {
    const e = () => this.isEnabled;
    return {
      toggle: (t) => this.toggle(t),
      get isEnabled() {
        return e();
      }
    };
  }
  /**
   * Set or toggle read-only state
   *
   * @param {boolean|undefined} state - set or toggle state
   * @returns {boolean} current value
   */
  toggle(e) {
    return this.Editor.ReadOnly.toggle(e);
  }
  /**
   * Returns current read-only state
   */
  get isEnabled() {
    return this.Editor.ReadOnly.isEnabled;
  }
};
var Ah = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(Qp, function() {
    function t(k) {
      var y = k.tags, T = Object.keys(y), x = T.map(function(M) {
        return typeof y[M];
      }).every(function(M) {
        return M === "object" || M === "boolean" || M === "function";
      });
      if (!x)
        throw new Error("The configuration was invalid");
      this.config = k;
    }
    var n = ["P", "LI", "TD", "TH", "DIV", "H1", "H2", "H3", "H4", "H5", "H6", "PRE"];
    function r(k) {
      return n.indexOf(k.nodeName) !== -1;
    }
    var a = ["A", "B", "STRONG", "I", "EM", "SUB", "SUP", "U", "STRIKE"];
    function c(k) {
      return a.indexOf(k.nodeName) !== -1;
    }
    t.prototype.clean = function(k) {
      const y = document.implementation.createHTMLDocument(), T = y.createElement("div");
      return T.innerHTML = k, this._sanitize(y, T), T.innerHTML;
    }, t.prototype._sanitize = function(k, y) {
      var T = u(k, y), x = T.firstChild();
      if (x)
        do {
          if (x.nodeType === Node.TEXT_NODE)
            if (x.data.trim() === "" && (x.previousElementSibling && r(x.previousElementSibling) || x.nextElementSibling && r(x.nextElementSibling))) {
              y.removeChild(x), this._sanitize(k, y);
              break;
            } else
              continue;
          if (x.nodeType === Node.COMMENT_NODE) {
            y.removeChild(x), this._sanitize(k, y);
            break;
          }
          var M = c(x), L;
          M && (L = Array.prototype.some.call(x.childNodes, r));
          var Y = !!y.parentNode, I = r(y) && r(x) && Y, F = x.nodeName.toLowerCase(), Q = p(this.config, F, x), U = M && L;
          if (U || g(x, Q) || !this.config.keepNestedBlockElements && I) {
            if (!(x.nodeName === "SCRIPT" || x.nodeName === "STYLE"))
              for (; x.childNodes.length > 0; )
                y.insertBefore(x.childNodes[0], x);
            y.removeChild(x), this._sanitize(k, y);
            break;
          }
          for (var z = 0; z < x.attributes.length; z += 1) {
            var d = x.attributes[z];
            v(d, Q, x) && (x.removeAttribute(d.name), z = z - 1);
          }
          this._sanitize(k, x);
        } while (x = T.nextSibling());
    };
    function u(k, y) {
      return k.createTreeWalker(
        y,
        NodeFilter.SHOW_TEXT | NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_COMMENT,
        null,
        !1
      );
    }
    function p(k, y, T) {
      return typeof k.tags[y] == "function" ? k.tags[y](T) : k.tags[y];
    }
    function g(k, y) {
      return typeof y > "u" ? !0 : typeof y == "boolean" ? !y : !1;
    }
    function v(k, y, T) {
      var x = k.name.toLowerCase();
      return y === !0 ? !1 : typeof y[x] == "function" ? !y[x](k.value, T) : typeof y[x] > "u" || y[x] === !1 ? !0 : typeof y[x] == "string" ? y[x] !== k.value : !1;
    }
    return t;
  });
})(Ah);
var Bf = Ah.exports;
const Af = /* @__PURE__ */ Ua(Bf);
function Rh(o, e) {
  return o.map((t) => {
    const n = Dt(e) ? e(t.tool) : e;
    return sn(n) || (t.data = cc(t.data, n)), t;
  });
}
function fi(o, e = {}) {
  const t = {
    tags: e
  };
  return new Af(t).clean(o);
}
function cc(o, e) {
  return Array.isArray(o) ? Rf(o, e) : ho(o) ? If(o, e) : Qi(o) ? Nf(o, e) : o;
}
function Rf(o, e) {
  return o.map((t) => cc(t, e));
}
function If(o, e) {
  const t = {};
  for (const n in o) {
    if (!Object.prototype.hasOwnProperty.call(o, n))
      continue;
    const r = o[n], a = Pf(e[n]) ? e[n] : e;
    t[n] = cc(r, a);
  }
  return t;
}
function Nf(o, e) {
  return ho(e) ? fi(o, e) : e === !1 ? fi(o, {}) : o;
}
function Pf(o) {
  return ho(o) || nf(o) || Dt(o);
}
let Hf = class extends rt {
  /**
   * Available methods
   *
   * @returns {SanitizerConfig}
   */
  get methods() {
    return {
      clean: (e, t) => this.clean(e, t)
    };
  }
  /**
   * Perform sanitizing of a string
   *
   * @param {string} taintString - what to sanitize
   * @param {SanitizerConfig} config - sanitizer config
   * @returns {string}
   */
  clean(e, t) {
    return fi(e, t);
  }
}, jf = class extends rt {
  /**
   * Available methods
   *
   * @returns {Saver}
   */
  get methods() {
    return {
      save: () => this.save()
    };
  }
  /**
   * Return Editor's data
   *
   * @returns {OutputData}
   */
  save() {
    const e = "Editor's content can not be saved in read-only mode";
    return this.Editor.ReadOnly.isEnabled ? (Zo(e, "warn"), Promise.reject(new Error(e))) : this.Editor.Saver.save();
  }
}, $f = class extends rt {
  /**
   * Available methods
   *
   * @returns {SelectionAPIInterface}
   */
  get methods() {
    return {
      findParentTag: (e, t) => this.findParentTag(e, t),
      expandToTag: (e) => this.expandToTag(e)
    };
  }
  /**
   * Looks ahead from selection and find passed tag with class name
   *
   * @param {string} tagName - tag to find
   * @param {string} className - tag's class name
   * @returns {HTMLElement|null}
   */
  findParentTag(e, t) {
    return new Fe().findParentTag(e, t);
  }
  /**
   * Expand selection to passed tag
   *
   * @param {HTMLElement} node - tag that should contain selection
   */
  expandToTag(e) {
    new Fe().expandToTag(e);
  }
}, Yf = class extends rt {
  /**
   * Exported classes
   */
  get classes() {
    return {
      /**
       * Base Block styles
       */
      block: "cdx-block",
      /**
       * Inline Tools styles
       */
      inlineToolButton: "ce-inline-tool",
      inlineToolButtonActive: "ce-inline-tool--active",
      /**
       * UI elements
       */
      input: "cdx-input",
      loader: "cdx-loader",
      button: "cdx-button",
      /**
       * Settings styles
       */
      settingsButton: "cdx-settings-button",
      settingsButtonActive: "cdx-settings-button--active"
    };
  }
}, Ff = class extends rt {
  /**
   * Available methods
   *
   * @returns {Toolbar}
   */
  get methods() {
    return {
      close: () => this.close(),
      open: () => this.open(),
      toggleBlockSettings: (e) => this.toggleBlockSettings(e),
      toggleBlockSettingsById: (e) => this.toggleBlockSettingsById(e),
      toggleToolbox: (e) => this.toggleToolbox(e)
    };
  }
  /**
   * Open toolbar
   */
  open() {
    this.Editor.Toolbar.moveAndOpen();
  }
  /**
   * Close toolbar and all included elements
   */
  close() {
    this.Editor.Toolbar.close();
  }
  /**
   * Toggles Block Setting of the current block
   *
   * @param {boolean} openingState —  opening state of Block Setting
   */
  toggleBlockSettings(e) {
    if (this.Editor.BlockManager.currentBlockIndex === -1) {
      Zo("Could't toggle the Toolbar because there is no block selected ", "warn");
      return;
    }
    e ?? !this.Editor.BlockSettings.opened ? (this.Editor.Toolbar.moveAndOpen(), this.Editor.BlockSettings.open()) : this.Editor.BlockSettings.close();
  }
  /**
   * Toggles Block Setting of the current block
   *
   * @param {boolean} openingState —  opening state of Block Setting
   */
  toggleBlockSettingsById(e) {
    const t = this.Editor.BlockManager.getBlockById(e);
    if (!t) {
      Zo("Block not found", "warn");
      return;
    }
    if (this.Editor.BlockSelection.selectBlock(t), this.Editor.BlockManager.currentBlock = t, this.Editor.BlockManager.currentBlockIndex === -1) {
      Zo("Could't toggle the Toolbar because there is no block selected ", "warn");
      return;
    }
    this.Editor.Toolbar.moveAndOpen(), this.Editor.BlockSettings.open();
  }
  /**
   * Open toolbox
   *
   * @param {boolean} openingState - Opening state of toolbox
   */
  toggleToolbox(e) {
    if (this.Editor.BlockManager.currentBlockIndex === -1) {
      Zo("Could't toggle the Toolbox because there is no block selected ", "warn");
      return;
    }
    e ?? !this.Editor.Toolbar.toolbox.opened ? (this.Editor.Toolbar.moveAndOpen(), this.Editor.Toolbar.toolbox.open()) : this.Editor.Toolbar.toolbox.close();
  }
};
var Ih = { exports: {} };
/*!
 * CodeX.Tooltips
 * 
 * @version 1.0.5
 * 
 * @licence MIT
 * @author CodeX <https://codex.so>
 * 
 * 
 */
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(window, function() {
    return function(t) {
      var n = {};
      function r(a) {
        if (n[a])
          return n[a].exports;
        var c = n[a] = { i: a, l: !1, exports: {} };
        return t[a].call(c.exports, c, c.exports, r), c.l = !0, c.exports;
      }
      return r.m = t, r.c = n, r.d = function(a, c, u) {
        r.o(a, c) || Object.defineProperty(a, c, { enumerable: !0, get: u });
      }, r.r = function(a) {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
      }, r.t = function(a, c) {
        if (1 & c && (a = r(a)), 8 & c || 4 & c && typeof a == "object" && a && a.__esModule)
          return a;
        var u = /* @__PURE__ */ Object.create(null);
        if (r.r(u), Object.defineProperty(u, "default", { enumerable: !0, value: a }), 2 & c && typeof a != "string")
          for (var p in a)
            r.d(u, p, (function(g) {
              return a[g];
            }).bind(null, p));
        return u;
      }, r.n = function(a) {
        var c = a && a.__esModule ? function() {
          return a.default;
        } : function() {
          return a;
        };
        return r.d(c, "a", c), c;
      }, r.o = function(a, c) {
        return Object.prototype.hasOwnProperty.call(a, c);
      }, r.p = "", r(r.s = 0);
    }([function(t, n, r) {
      t.exports = r(1);
    }, function(t, n, r) {
      r.r(n), r.d(n, "default", function() {
        return a;
      });
      class a {
        constructor() {
          this.nodes = { wrapper: null, content: null }, this.showed = !1, this.offsetTop = 10, this.offsetLeft = 10, this.offsetRight = 10, this.hidingDelay = 0, this.handleWindowScroll = () => {
            this.showed && this.hide(!0);
          }, this.loadStyles(), this.prepare(), window.addEventListener("scroll", this.handleWindowScroll, { passive: !0 });
        }
        get CSS() {
          return { tooltip: "ct", tooltipContent: "ct__content", tooltipShown: "ct--shown", placement: { left: "ct--left", bottom: "ct--bottom", right: "ct--right", top: "ct--top" } };
        }
        show(u, p, g) {
          this.nodes.wrapper || this.prepare(), this.hidingTimeout && clearTimeout(this.hidingTimeout);
          const v = Object.assign({ placement: "bottom", marginTop: 0, marginLeft: 0, marginRight: 0, marginBottom: 0, delay: 70, hidingDelay: 0 }, g);
          if (v.hidingDelay && (this.hidingDelay = v.hidingDelay), this.nodes.content.innerHTML = "", typeof p == "string")
            this.nodes.content.appendChild(document.createTextNode(p));
          else {
            if (!(p instanceof Node))
              throw Error("[CodeX Tooltip] Wrong type of «content» passed. It should be an instance of Node or String. But " + typeof p + " given.");
            this.nodes.content.appendChild(p);
          }
          switch (this.nodes.wrapper.classList.remove(...Object.values(this.CSS.placement)), v.placement) {
            case "top":
              this.placeTop(u, v);
              break;
            case "left":
              this.placeLeft(u, v);
              break;
            case "right":
              this.placeRight(u, v);
              break;
            case "bottom":
            default:
              this.placeBottom(u, v);
          }
          v && v.delay ? this.showingTimeout = setTimeout(() => {
            this.nodes.wrapper.classList.add(this.CSS.tooltipShown), this.showed = !0;
          }, v.delay) : (this.nodes.wrapper.classList.add(this.CSS.tooltipShown), this.showed = !0);
        }
        hide(u = !1) {
          if (this.hidingDelay && !u)
            return this.hidingTimeout && clearTimeout(this.hidingTimeout), void (this.hidingTimeout = setTimeout(() => {
              this.hide(!0);
            }, this.hidingDelay));
          this.nodes.wrapper.classList.remove(this.CSS.tooltipShown), this.showed = !1, this.showingTimeout && clearTimeout(this.showingTimeout);
        }
        onHover(u, p, g) {
          u.addEventListener("mouseenter", () => {
            this.show(u, p, g);
          }), u.addEventListener("mouseleave", () => {
            this.hide();
          });
        }
        destroy() {
          this.nodes.wrapper.remove(), window.removeEventListener("scroll", this.handleWindowScroll);
        }
        prepare() {
          this.nodes.wrapper = this.make("div", this.CSS.tooltip), this.nodes.content = this.make("div", this.CSS.tooltipContent), this.append(this.nodes.wrapper, this.nodes.content), this.append(document.body, this.nodes.wrapper);
        }
        loadStyles() {
          const u = "codex-tooltips-style";
          if (document.getElementById(u))
            return;
          const p = r(2), g = this.make("style", null, { textContent: p.toString(), id: u });
          this.prepend(document.head, g);
        }
        placeBottom(u, p) {
          const g = u.getBoundingClientRect(), v = g.left + u.clientWidth / 2 - this.nodes.wrapper.offsetWidth / 2, k = g.bottom + window.pageYOffset + this.offsetTop + p.marginTop;
          this.applyPlacement("bottom", v, k);
        }
        placeTop(u, p) {
          const g = u.getBoundingClientRect(), v = g.left + u.clientWidth / 2 - this.nodes.wrapper.offsetWidth / 2, k = g.top + window.pageYOffset - this.nodes.wrapper.clientHeight - this.offsetTop;
          this.applyPlacement("top", v, k);
        }
        placeLeft(u, p) {
          const g = u.getBoundingClientRect(), v = g.left - this.nodes.wrapper.offsetWidth - this.offsetLeft - p.marginLeft, k = g.top + window.pageYOffset + u.clientHeight / 2 - this.nodes.wrapper.offsetHeight / 2;
          this.applyPlacement("left", v, k);
        }
        placeRight(u, p) {
          const g = u.getBoundingClientRect(), v = g.right + this.offsetRight + p.marginRight, k = g.top + window.pageYOffset + u.clientHeight / 2 - this.nodes.wrapper.offsetHeight / 2;
          this.applyPlacement("right", v, k);
        }
        applyPlacement(u, p, g) {
          this.nodes.wrapper.classList.add(this.CSS.placement[u]), this.nodes.wrapper.style.left = p + "px", this.nodes.wrapper.style.top = g + "px";
        }
        make(u, p = null, g = {}) {
          const v = document.createElement(u);
          Array.isArray(p) ? v.classList.add(...p) : p && v.classList.add(p);
          for (const k in g)
            g.hasOwnProperty(k) && (v[k] = g[k]);
          return v;
        }
        append(u, p) {
          Array.isArray(p) ? p.forEach((g) => u.appendChild(g)) : u.appendChild(p);
        }
        prepend(u, p) {
          Array.isArray(p) ? (p = p.reverse()).forEach((g) => u.prepend(g)) : u.prepend(p);
        }
      }
    }, function(t, n) {
      t.exports = `.ct{z-index:999;opacity:0;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;pointer-events:none;-webkit-transition:opacity 50ms ease-in,-webkit-transform 70ms cubic-bezier(.215,.61,.355,1);transition:opacity 50ms ease-in,-webkit-transform 70ms cubic-bezier(.215,.61,.355,1);transition:opacity 50ms ease-in,transform 70ms cubic-bezier(.215,.61,.355,1);transition:opacity 50ms ease-in,transform 70ms cubic-bezier(.215,.61,.355,1),-webkit-transform 70ms cubic-bezier(.215,.61,.355,1);will-change:opacity,top,left;-webkit-box-shadow:0 8px 12px 0 rgba(29,32,43,.17),0 4px 5px -3px rgba(5,6,12,.49);box-shadow:0 8px 12px 0 rgba(29,32,43,.17),0 4px 5px -3px rgba(5,6,12,.49);border-radius:9px}.ct,.ct:before{position:absolute;top:0;left:0}.ct:before{content:"";bottom:0;right:0;background-color:#1d202b;z-index:-1;border-radius:4px}@supports(-webkit-mask-box-image:url("")){.ct:before{border-radius:0;-webkit-mask-box-image:url('data:image/svg+xml;charset=utf-8,<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"><path d="M10.71 0h2.58c3.02 0 4.64.42 6.1 1.2a8.18 8.18 0 013.4 3.4C23.6 6.07 24 7.7 24 10.71v2.58c0 3.02-.42 4.64-1.2 6.1a8.18 8.18 0 01-3.4 3.4c-1.47.8-3.1 1.21-6.11 1.21H10.7c-3.02 0-4.64-.42-6.1-1.2a8.18 8.18 0 01-3.4-3.4C.4 17.93 0 16.3 0 13.29V10.7c0-3.02.42-4.64 1.2-6.1a8.18 8.18 0 013.4-3.4C6.07.4 7.7 0 10.71 0z"/></svg>') 48% 41% 37.9% 53.3%}}@media (--mobile){.ct{display:none}}.ct__content{padding:6px 10px;color:#cdd1e0;font-size:12px;text-align:center;letter-spacing:.02em;line-height:1em}.ct:after{content:"";width:8px;height:8px;position:absolute;background-color:#1d202b;z-index:-1}.ct--bottom{-webkit-transform:translateY(5px);transform:translateY(5px)}.ct--bottom:after{top:-3px;left:50%;-webkit-transform:translateX(-50%) rotate(-45deg);transform:translateX(-50%) rotate(-45deg)}.ct--top{-webkit-transform:translateY(-5px);transform:translateY(-5px)}.ct--top:after{top:auto;bottom:-3px;left:50%;-webkit-transform:translateX(-50%) rotate(-45deg);transform:translateX(-50%) rotate(-45deg)}.ct--left{-webkit-transform:translateX(-5px);transform:translateX(-5px)}.ct--left:after{top:50%;left:auto;right:0;-webkit-transform:translate(41.6%,-50%) rotate(-45deg);transform:translate(41.6%,-50%) rotate(-45deg)}.ct--right{-webkit-transform:translateX(5px);transform:translateX(5px)}.ct--right:after{top:50%;left:0;-webkit-transform:translate(-41.6%,-50%) rotate(-45deg);transform:translate(-41.6%,-50%) rotate(-45deg)}.ct--shown{opacity:1;-webkit-transform:none;transform:none}`;
    }]).default;
  });
})(Ih);
var zf = Ih.exports;
const Wf = /* @__PURE__ */ Ua(zf);
let Sn = null;
function dc() {
  Sn || (Sn = new Wf());
}
function Uf(o, e, t) {
  dc(), Sn == null || Sn.show(o, e, t);
}
function Fl(o = !1) {
  dc(), Sn == null || Sn.hide(o);
}
function oa(o, e, t) {
  dc(), Sn == null || Sn.onHover(o, e, t);
}
function qf() {
  Sn == null || Sn.destroy(), Sn = null;
}
let Vf = class extends rt {
  /**
   * @class
   * @param moduleConfiguration - Module Configuration
   * @param moduleConfiguration.config - Editor's config
   * @param moduleConfiguration.eventsDispatcher - Editor's event dispatcher
   */
  constructor({ config: e, eventsDispatcher: t }) {
    super({
      config: e,
      eventsDispatcher: t
    });
  }
  /**
   * Available methods
   */
  get methods() {
    return {
      show: (e, t, n) => this.show(e, t, n),
      hide: () => this.hide(),
      onHover: (e, t, n) => this.onHover(e, t, n)
    };
  }
  /**
   * Method show tooltip on element with passed HTML content
   *
   * @param {HTMLElement} element - element on which tooltip should be shown
   * @param {TooltipContent} content - tooltip content
   * @param {TooltipOptions} options - tooltip options
   */
  show(e, t, n) {
    Uf(e, t, n);
  }
  /**
   * Method hides tooltip on HTML page
   */
  hide() {
    Fl();
  }
  /**
   * Decorator for showing Tooltip by mouseenter/mouseleave
   *
   * @param {HTMLElement} element - element on which tooltip should be shown
   * @param {TooltipContent} content - tooltip content
   * @param {TooltipOptions} options - tooltip options
   */
  onHover(e, t, n) {
    oa(e, t, n);
  }
}, Gf = class extends rt {
  /**
   * Available methods / getters
   */
  get methods() {
    return {
      nodes: this.editorNodes
      /**
       * There can be added some UI methods, like toggleThinMode() etc
       */
    };
  }
  /**
   * Exported classes
   */
  get editorNodes() {
    return {
      /**
       * Top-level editor instance wrapper
       */
      wrapper: this.Editor.UI.nodes.wrapper,
      /**
       * Element that holds all the Blocks
       */
      redactor: this.Editor.UI.nodes.redactor
    };
  }
};
function Nh(o, e) {
  const t = {};
  return Object.entries(o).forEach(([n, r]) => {
    if (ho(r)) {
      const a = e ? `${e}.${n}` : n;
      Object.values(r).every((c) => Qi(c)) ? t[n] = a : t[n] = Nh(r, a);
      return;
    }
    t[n] = r;
  }), t;
}
const kn = Nh(Th);
function Zf(o, e) {
  const t = {};
  return Object.keys(o).forEach((n) => {
    const r = e[n];
    r !== void 0 ? t[r] = o[n] : t[n] = o[n];
  }), t;
}
const Kf = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M9 12L9 7.1C9 7.04477 9.04477 7 9.1 7H10.4C11.5 7 14 7.1 14 9.5C14 9.5 14 12 11 12M9 12V16.8C9 16.9105 9.08954 17 9.2 17H12.5C14 17 15 16 15 14.5C15 11.7046 11 12 11 12M9 12H11"/></svg>', Ph = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M7 10L11.8586 14.8586C11.9367 14.9367 12.0633 14.9367 12.1414 14.8586L17 10"/></svg>', Xf = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M7 15L11.8586 10.1414C11.9367 10.0633 12.0633 10.0633 12.1414 10.1414L17 15"/></svg>', Jf = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 8L12 12M12 12L16 16M12 12L16 8M12 12L8 16"/></svg>', Qf = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><circle cx="12" cy="12" r="4" stroke="currentColor" stroke-width="2"/></svg>', em = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M13.34 10C12.4223 12.7337 11 17 11 17"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M14.21 7H14.2"/></svg>', fd = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M7.69998 12.6L7.67896 12.62C6.53993 13.7048 6.52012 15.5155 7.63516 16.625V16.625C8.72293 17.7073 10.4799 17.7102 11.5712 16.6314L13.0263 15.193C14.0703 14.1609 14.2141 12.525 13.3662 11.3266L13.22 11.12"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M16.22 11.12L16.3564 10.9805C17.2895 10.0265 17.3478 8.5207 16.4914 7.49733V7.49733C15.5691 6.39509 13.9269 6.25143 12.8271 7.17675L11.3901 8.38588C10.0935 9.47674 9.95706 11.4241 11.0888 12.6852L11.12 12.72"/></svg>', tm = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.40999 7.29999H9.4"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 7.29999H14.59"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.30999 12H9.3"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 12H14.59"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.40999 16.7H9.4"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 16.7H14.59"/></svg>', om = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M12 7V12M12 17V12M17 12H12M12 12H7"/></svg>', nm = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><circle cx="10.5" cy="10.5" r="5.5" stroke="currentColor" stroke-width="2"/><line x1="15.4142" x2="19" y1="15" y2="18.5858" stroke="currentColor" stroke-linecap="round" stroke-width="2"/></svg>', im = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M15.7795 11.5C15.7795 11.5 16.053 11.1962 16.5497 10.6722C17.4442 9.72856 17.4701 8.2475 16.5781 7.30145V7.30145C15.6482 6.31522 14.0873 6.29227 13.1288 7.25073L11.8796 8.49999"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8.24517 12.3883C8.24517 12.3883 7.97171 12.6922 7.47504 13.2161C6.58051 14.1598 6.55467 15.6408 7.44666 16.5869V16.5869C8.37653 17.5731 9.93744 17.5961 10.8959 16.6376L12.1452 15.3883"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M17.7802 15.1032L16.597 14.9422C16.0109 14.8624 15.4841 15.3059 15.4627 15.8969L15.4199 17.0818"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M6.39064 9.03238L7.58432 9.06668C8.17551 9.08366 8.6522 8.58665 8.61056 7.99669L8.5271 6.81397"/><line x1="12.1142" x2="11.7" y1="12.2" y2="11.7858" stroke="currentColor" stroke-linecap="round" stroke-width="2"/></svg>', rm = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/><line x1="12" x2="12" y1="9" y2="12" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M12 15.02V15.01"/></svg>';
let md = class Qt {
  /**
   * Constructs popover item instance
   *
   * @param params - popover item construction params
   */
  constructor(e) {
    this.nodes = {
      root: null,
      icon: null
    }, this.confirmationState = null, this.removeSpecialFocusBehavior = () => {
      this.nodes.root.classList.remove(Qt.CSS.noFocus);
    }, this.removeSpecialHoverBehavior = () => {
      this.nodes.root.classList.remove(Qt.CSS.noHover);
    }, this.onErrorAnimationEnd = () => {
      this.nodes.icon.classList.remove(Qt.CSS.wobbleAnimation), this.nodes.icon.removeEventListener("animationend", this.onErrorAnimationEnd);
    }, this.params = e, this.nodes.root = this.make(e);
  }
  /**
   * True if item is disabled and hence not clickable
   */
  get isDisabled() {
    return this.params.isDisabled;
  }
  /**
   * Exposes popover item toggle parameter
   */
  get toggle() {
    return this.params.toggle;
  }
  /**
   * Item title
   */
  get title() {
    return this.params.title;
  }
  /**
   * True if popover should close once item is activated
   */
  get closeOnActivate() {
    return this.params.closeOnActivate;
  }
  /**
   * True if confirmation state is enabled for popover item
   */
  get isConfirmationStateEnabled() {
    return this.confirmationState !== null;
  }
  /**
   * True if item is focused in keyboard navigation process
   */
  get isFocused() {
    return this.nodes.root.classList.contains(Qt.CSS.focused);
  }
  /**
   * Popover item CSS classes
   */
  static get CSS() {
    return {
      container: "ce-popover-item",
      title: "ce-popover-item__title",
      secondaryTitle: "ce-popover-item__secondary-title",
      icon: "ce-popover-item__icon",
      active: "ce-popover-item--active",
      disabled: "ce-popover-item--disabled",
      focused: "ce-popover-item--focused",
      hidden: "ce-popover-item--hidden",
      confirmationState: "ce-popover-item--confirmation",
      noHover: "ce-popover-item--no-hover",
      noFocus: "ce-popover-item--no-focus",
      wobbleAnimation: "wobble"
    };
  }
  /**
   * Returns popover item root element
   */
  getElement() {
    return this.nodes.root;
  }
  /**
   * Called on popover item click
   */
  handleClick() {
    if (this.isConfirmationStateEnabled) {
      this.activateOrEnableConfirmationMode(this.confirmationState);
      return;
    }
    this.activateOrEnableConfirmationMode(this.params);
  }
  /**
   * Toggles item active state
   *
   * @param isActive - true if item should strictly should become active
   */
  toggleActive(e) {
    this.nodes.root.classList.toggle(Qt.CSS.active, e);
  }
  /**
   * Toggles item hidden state
   *
   * @param isHidden - true if item should be hidden
   */
  toggleHidden(e) {
    this.nodes.root.classList.toggle(Qt.CSS.hidden, e);
  }
  /**
   * Resets popover item to its original state
   */
  reset() {
    this.isConfirmationStateEnabled && this.disableConfirmationMode();
  }
  /**
   * Method called once item becomes focused during keyboard navigation
   */
  onFocus() {
    this.disableSpecialHoverAndFocusBehavior();
  }
  /**
   * Constructs HTML element corresponding to popover item params
   *
   * @param params - item construction params
   */
  make(e) {
    const t = ae.make("div", Qt.CSS.container);
    return e.name && (t.dataset.itemName = e.name), this.nodes.icon = ae.make("div", Qt.CSS.icon, {
      innerHTML: e.icon || Qf
    }), t.appendChild(this.nodes.icon), t.appendChild(ae.make("div", Qt.CSS.title, {
      innerHTML: e.title || ""
    })), e.secondaryLabel && t.appendChild(ae.make("div", Qt.CSS.secondaryTitle, {
      textContent: e.secondaryLabel
    })), e.isActive && t.classList.add(Qt.CSS.active), e.isDisabled && t.classList.add(Qt.CSS.disabled), t;
  }
  /**
   * Activates confirmation mode for the item.
   *
   * @param newState - new popover item params that should be applied
   */
  enableConfirmationMode(e) {
    const t = {
      ...this.params,
      ...e,
      confirmation: e.confirmation
    }, n = this.make(t);
    this.nodes.root.innerHTML = n.innerHTML, this.nodes.root.classList.add(Qt.CSS.confirmationState), this.confirmationState = e, this.enableSpecialHoverAndFocusBehavior();
  }
  /**
   * Returns item to its original state
   */
  disableConfirmationMode() {
    const e = this.make(this.params);
    this.nodes.root.innerHTML = e.innerHTML, this.nodes.root.classList.remove(Qt.CSS.confirmationState), this.confirmationState = null, this.disableSpecialHoverAndFocusBehavior();
  }
  /**
   * Enables special focus and hover behavior for item in confirmation state.
   * This is needed to prevent item from being highlighted as hovered/focused just after click.
   */
  enableSpecialHoverAndFocusBehavior() {
    this.nodes.root.classList.add(Qt.CSS.noHover), this.nodes.root.classList.add(Qt.CSS.noFocus), this.nodes.root.addEventListener("mouseleave", this.removeSpecialHoverBehavior, { once: !0 });
  }
  /**
   * Disables special focus and hover behavior
   */
  disableSpecialHoverAndFocusBehavior() {
    this.removeSpecialFocusBehavior(), this.removeSpecialHoverBehavior(), this.nodes.root.removeEventListener("mouseleave", this.removeSpecialHoverBehavior);
  }
  /**
   * Executes item's onActivate callback if the item has no confirmation configured
   *
   * @param item - item to activate or bring to confirmation mode
   */
  activateOrEnableConfirmationMode(e) {
    if (e.confirmation === void 0)
      try {
        e.onActivate(e), this.disableConfirmationMode();
      } catch {
        this.animateError();
      }
    else
      this.enableConfirmationMode(e.confirmation);
  }
  /**
   * Animates item which symbolizes that error occured while executing 'onActivate()' callback
   */
  animateError() {
    this.nodes.icon.classList.contains(Qt.CSS.wobbleAnimation) || (this.nodes.icon.classList.add(Qt.CSS.wobbleAnimation), this.nodes.icon.addEventListener("animationend", this.onErrorAnimationEnd));
  }
};
const Hh = class Ks {
  /**
   * @param {HTMLElement[]} nodeList — the list of iterable HTML-items
   * @param {string} focusedCssClass - user-provided CSS-class that will be set in flipping process
   */
  constructor(e, t) {
    this.cursor = -1, this.items = [], this.items = e || [], this.focusedCssClass = t;
  }
  /**
   * Returns Focused button Node
   *
   * @returns {HTMLElement}
   */
  get currentItem() {
    return this.cursor === -1 ? null : this.items[this.cursor];
  }
  /**
   * Sets cursor to specified position
   *
   * @param cursorPosition - new cursor position
   */
  setCursor(e) {
    e < this.items.length && e >= -1 && (this.dropCursor(), this.cursor = e, this.items[this.cursor].classList.add(this.focusedCssClass));
  }
  /**
   * Sets items. Can be used when iterable items changed dynamically
   *
   * @param {HTMLElement[]} nodeList - nodes to iterate
   */
  setItems(e) {
    this.items = e;
  }
  /**
   * Sets cursor next to the current
   */
  next() {
    this.cursor = this.leafNodesAndReturnIndex(Ks.directions.RIGHT);
  }
  /**
   * Sets cursor before current
   */
  previous() {
    this.cursor = this.leafNodesAndReturnIndex(Ks.directions.LEFT);
  }
  /**
   * Sets cursor to the default position and removes CSS-class from previously focused item
   */
  dropCursor() {
    this.cursor !== -1 && (this.items[this.cursor].classList.remove(this.focusedCssClass), this.cursor = -1);
  }
  /**
   * Leafs nodes inside the target list from active element
   *
   * @param {string} direction - leaf direction. Can be 'left' or 'right'
   * @returns {number} index of focused node
   */
  leafNodesAndReturnIndex(e) {
    if (this.items.length === 0)
      return this.cursor;
    let t = this.cursor;
    return t === -1 ? t = e === Ks.directions.RIGHT ? -1 : 0 : this.items[t].classList.remove(this.focusedCssClass), e === Ks.directions.RIGHT ? t = (t + 1) % this.items.length : t = (this.items.length + t - 1) % this.items.length, ae.canSetCaret(this.items[t]) && Ra(() => Fe.setCursor(this.items[t]), 50)(), this.items[t].classList.add(this.focusedCssClass), t;
  }
};
Hh.directions = {
  RIGHT: "right",
  LEFT: "left"
};
let Ws = Hh, ms = class zl {
  /**
   * @param {FlipperOptions} options - different constructing settings
   */
  constructor(e) {
    this.iterator = null, this.activated = !1, this.flipCallbacks = [], this.onKeyDown = (t) => {
      if (this.isEventReadyForHandling(t))
        switch (zl.usedKeys.includes(t.keyCode) && t.preventDefault(), t.keyCode) {
          case ze.TAB:
            this.handleTabPress(t);
            break;
          case ze.LEFT:
          case ze.UP:
            this.flipLeft();
            break;
          case ze.RIGHT:
          case ze.DOWN:
            this.flipRight();
            break;
          case ze.ENTER:
            this.handleEnterPress(t);
            break;
        }
    }, this.iterator = new Ws(e.items, e.focusedItemClass), this.activateCallback = e.activateCallback, this.allowedKeys = e.allowedKeys || zl.usedKeys;
  }
  /**
   * True if flipper is currently activated
   */
  get isActivated() {
    return this.activated;
  }
  /**
   * Array of keys (codes) that is handled by Flipper
   * Used to:
   *  - preventDefault only for this keys, not all keydowns (@see constructor)
   *  - to skip external behaviours only for these keys, when filler is activated (@see BlockEvents@arrowRightAndDown)
   */
  static get usedKeys() {
    return [
      ze.TAB,
      ze.LEFT,
      ze.RIGHT,
      ze.ENTER,
      ze.UP,
      ze.DOWN
    ];
  }
  /**
   * Active tab/arrows handling by flipper
   *
   * @param items - Some modules (like, InlineToolbar, BlockSettings) might refresh buttons dynamically
   * @param cursorPosition - index of the item that should be focused once flipper is activated
   */
  activate(e, t) {
    this.activated = !0, e && this.iterator.setItems(e), t !== void 0 && this.iterator.setCursor(t), document.addEventListener("keydown", this.onKeyDown, !0);
  }
  /**
   * Disable tab/arrows handling by flipper
   */
  deactivate() {
    this.activated = !1, this.dropCursor(), document.removeEventListener("keydown", this.onKeyDown);
  }
  /**
   * Focus first item
   */
  focusFirst() {
    this.dropCursor(), this.flipRight();
  }
  /**
   * Focuses previous flipper iterator item
   */
  flipLeft() {
    this.iterator.previous(), this.flipCallback();
  }
  /**
   * Focuses next flipper iterator item
   */
  flipRight() {
    this.iterator.next(), this.flipCallback();
  }
  /**
   * Return true if some button is focused
   */
  hasFocus() {
    return !!this.iterator.currentItem;
  }
  /**
   * Registeres function that should be executed on each navigation action
   *
   * @param cb - function to execute
   */
  onFlip(e) {
    this.flipCallbacks.push(e);
  }
  /**
   * Unregisteres function that is executed on each navigation action
   *
   * @param cb - function to stop executing
   */
  removeOnFlip(e) {
    this.flipCallbacks = this.flipCallbacks.filter((t) => t !== e);
  }
  /**
   * Drops flipper's iterator cursor
   *
   * @see DomIterator#dropCursor
   */
  dropCursor() {
    this.iterator.dropCursor();
  }
  /**
   * This function is fired before handling flipper keycodes
   * The result of this function defines if it is need to be handled or not
   *
   * @param {KeyboardEvent} event - keydown keyboard event
   * @returns {boolean}
   */
  isEventReadyForHandling(e) {
    return this.activated && this.allowedKeys.includes(e.keyCode);
  }
  /**
   * When flipper is activated tab press will leaf the items
   *
   * @param {KeyboardEvent} event - tab keydown event
   */
  handleTabPress(e) {
    switch (e.shiftKey ? Ws.directions.LEFT : Ws.directions.RIGHT) {
      case Ws.directions.RIGHT:
        this.flipRight();
        break;
      case Ws.directions.LEFT:
        this.flipLeft();
        break;
    }
  }
  /**
   * Enter press will click current item if flipper is activated
   *
   * @param {KeyboardEvent} event - enter keydown event
   */
  handleEnterPress(e) {
    this.activated && (this.iterator.currentItem && (e.stopPropagation(), e.preventDefault(), this.iterator.currentItem.click()), Dt(this.activateCallback) && this.activateCallback(this.iterator.currentItem));
  }
  /**
   * Fired after flipping in any direction
   */
  flipCallback() {
    this.iterator.currentItem && this.iterator.currentItem.scrollIntoViewIfNeeded(), this.flipCallbacks.forEach((e) => e());
  }
}, sm = class Ta {
  /**
   * Styles
   */
  static get CSS() {
    return {
      wrapper: "cdx-search-field",
      icon: "cdx-search-field__icon",
      input: "cdx-search-field__input"
    };
  }
  /**
   * @param options - available config
   * @param options.items - searchable items list
   * @param options.onSearch - search callback
   * @param options.placeholder - input placeholder
   */
  constructor({ items: e, onSearch: t, placeholder: n }) {
    this.listeners = new ac(), this.items = e, this.onSearch = t, this.render(n);
  }
  /**
   * Returns search field element
   */
  getElement() {
    return this.wrapper;
  }
  /**
   * Sets focus to the input
   */
  focus() {
    this.input.focus();
  }
  /**
   * Clears search query and results
   */
  clear() {
    this.input.value = "", this.searchQuery = "", this.onSearch("", this.foundItems);
  }
  /**
   * Clears memory
   */
  destroy() {
    this.listeners.removeAll();
  }
  /**
   * Creates the search field
   *
   * @param placeholder - input placeholder
   */
  render(e) {
    this.wrapper = ae.make("div", Ta.CSS.wrapper);
    const t = ae.make("div", Ta.CSS.icon, {
      innerHTML: nm
    });
    this.input = ae.make("input", Ta.CSS.input, {
      placeholder: e,
      /**
       * Used to prevent focusing on the input by Tab key
       * (Popover in the Toolbar lays below the blocks,
       * so Tab in the last block will focus this hidden input if this property is not set)
       */
      tabIndex: -1
    }), this.wrapper.appendChild(t), this.wrapper.appendChild(this.input), this.listeners.on(this.input, "input", () => {
      this.searchQuery = this.input.value, this.onSearch(this.searchQuery, this.foundItems);
    });
  }
  /**
   * Returns list of found items for the current search query
   */
  get foundItems() {
    return this.items.filter((e) => this.checkItem(e));
  }
  /**
   * Contains logic for checking whether passed item conforms the search query
   *
   * @param item - item to be checked
   */
  checkItem(e) {
    var t;
    const n = ((t = e.title) == null ? void 0 : t.toLowerCase()) || "", r = this.searchQuery.toLowerCase();
    return n.includes(r);
  }
};
const jh = class Xs {
  /**
   * Locks body element scroll
   */
  lock() {
    ud ? this.lockHard() : document.body.classList.add(Xs.CSS.scrollLocked);
  }
  /**
   * Unlocks body element scroll
   */
  unlock() {
    ud ? this.unlockHard() : document.body.classList.remove(Xs.CSS.scrollLocked);
  }
  /**
   * Locks scroll in a hard way (via setting fixed position to body element)
   */
  lockHard() {
    this.scrollPosition = window.pageYOffset, document.documentElement.style.setProperty(
      "--window-scroll-offset",
      `${this.scrollPosition}px`
    ), document.body.classList.add(Xs.CSS.scrollLockedHard);
  }
  /**
   * Unlocks hard scroll lock
   */
  unlockHard() {
    document.body.classList.remove(Xs.CSS.scrollLockedHard), this.scrollPosition !== null && window.scrollTo(0, this.scrollPosition), this.scrollPosition = null;
  }
};
jh.CSS = {
  scrollLocked: "ce-scroll-locked",
  scrollLockedHard: "ce-scroll-locked--hard"
};
let am = jh;
var lm = Object.defineProperty, cm = Object.getOwnPropertyDescriptor, dm = (o, e, t, n) => {
  for (var r = n > 1 ? void 0 : n ? cm(e, t) : e, a = o.length - 1, c; a >= 0; a--)
    (c = o[a]) && (r = (n ? c(e, t, r) : c(r)) || r);
  return n && r && lm(e, t, r), r;
}, na = /* @__PURE__ */ ((o) => (o.Close = "close", o))(na || {});
const $h = class co extends Va {
  /**
   * Constructs the instance
   *
   * @param params - popover construction params
   */
  constructor(e) {
    super(), this.scopeElement = document.body, this.listeners = new ac(), this.scrollLocker = new am(), this.nodes = {
      wrapper: null,
      popover: null,
      nothingFoundMessage: null,
      customContent: null,
      items: null,
      overlay: null
    }, this.messages = {
      nothingFound: "Nothing found",
      search: "Search"
    }, this.onFlip = () => {
      this.items.find((t) => t.isFocused).onFocus();
    }, this.items = e.items.map((t) => new md(t)), e.scopeElement !== void 0 && (this.scopeElement = e.scopeElement), e.messages && (this.messages = {
      ...this.messages,
      ...e.messages
    }), e.customContentFlippableItems && (this.customContentFlippableItems = e.customContentFlippableItems), this.make(), e.customContent && this.addCustomContent(e.customContent), e.searchable && this.addSearch(), this.initializeFlipper();
  }
  /**
   * Popover CSS classes
   */
  static get CSS() {
    return {
      popover: "ce-popover",
      popoverOpenTop: "ce-popover--open-top",
      popoverOpened: "ce-popover--opened",
      search: "ce-popover__search",
      nothingFoundMessage: "ce-popover__nothing-found-message",
      nothingFoundMessageDisplayed: "ce-popover__nothing-found-message--displayed",
      customContent: "ce-popover__custom-content",
      customContentHidden: "ce-popover__custom-content--hidden",
      items: "ce-popover__items",
      overlay: "ce-popover__overlay",
      overlayHidden: "ce-popover__overlay--hidden"
    };
  }
  /**
   * Returns HTML element corresponding to the popover
   */
  getElement() {
    return this.nodes.wrapper;
  }
  /**
   * Returns true if some item inside popover is focused
   */
  hasFocus() {
    return this.flipper.hasFocus();
  }
  /**
   * Open popover
   */
  show() {
    this.shouldOpenBottom || (this.nodes.popover.style.setProperty("--popover-height", this.height + "px"), this.nodes.popover.classList.add(co.CSS.popoverOpenTop)), this.nodes.overlay.classList.remove(co.CSS.overlayHidden), this.nodes.popover.classList.add(co.CSS.popoverOpened), this.flipper.activate(this.flippableElements), this.search !== void 0 && requestAnimationFrame(() => {
      var e;
      (e = this.search) == null || e.focus();
    }), Lr() && this.scrollLocker.lock();
  }
  /**
   * Closes popover
   */
  hide() {
    this.nodes.popover.classList.remove(co.CSS.popoverOpened), this.nodes.popover.classList.remove(co.CSS.popoverOpenTop), this.nodes.overlay.classList.add(co.CSS.overlayHidden), this.flipper.deactivate(), this.items.forEach((e) => e.reset()), this.search !== void 0 && this.search.clear(), Lr() && this.scrollLocker.unlock(), this.emit(
      "close"
      /* Close */
    );
  }
  /**
   * Clears memory
   */
  destroy() {
    this.flipper.deactivate(), this.listeners.removeAll(), Lr() && this.scrollLocker.unlock();
  }
  /**
   * Constructs HTML element corresponding to popover
   */
  make() {
    this.nodes.popover = ae.make("div", [co.CSS.popover]), this.nodes.nothingFoundMessage = ae.make("div", [co.CSS.nothingFoundMessage], {
      textContent: this.messages.nothingFound
    }), this.nodes.popover.appendChild(this.nodes.nothingFoundMessage), this.nodes.items = ae.make("div", [co.CSS.items]), this.items.forEach((e) => {
      this.nodes.items.appendChild(e.getElement());
    }), this.nodes.popover.appendChild(this.nodes.items), this.listeners.on(this.nodes.popover, "click", (e) => {
      const t = this.getTargetItem(e);
      t !== void 0 && this.handleItemClick(t);
    }), this.nodes.wrapper = ae.make("div"), this.nodes.overlay = ae.make("div", [co.CSS.overlay, co.CSS.overlayHidden]), this.listeners.on(this.nodes.overlay, "click", () => {
      this.hide();
    }), this.nodes.wrapper.appendChild(this.nodes.overlay), this.nodes.wrapper.appendChild(this.nodes.popover);
  }
  /**
   * Adds search to the popover
   */
  addSearch() {
    this.search = new sm({
      items: this.items,
      placeholder: this.messages.search,
      onSearch: (t, n) => {
        this.items.forEach((a) => {
          const c = !n.includes(a);
          a.toggleHidden(c);
        }), this.toggleNothingFoundMessage(n.length === 0), this.toggleCustomContent(t !== "");
        const r = t === "" ? this.flippableElements : n.map((a) => a.getElement());
        this.flipper.isActivated && (this.flipper.deactivate(), this.flipper.activate(r));
      }
    });
    const e = this.search.getElement();
    e.classList.add(co.CSS.search), this.nodes.popover.insertBefore(e, this.nodes.popover.firstChild);
  }
  /**
   * Adds custom html content to the popover
   *
   * @param content - html content to append
   */
  addCustomContent(e) {
    this.nodes.customContent = e, this.nodes.customContent.classList.add(co.CSS.customContent), this.nodes.popover.insertBefore(e, this.nodes.popover.firstChild);
  }
  /**
   * Retrieves popover item that is the target of the specified event
   *
   * @param event - event to retrieve popover item from
   */
  getTargetItem(e) {
    return this.items.find((t) => e.composedPath().includes(t.getElement()));
  }
  /**
   * Handles item clicks
   *
   * @param item - item to handle click of
   */
  handleItemClick(e) {
    e.isDisabled || (this.items.filter((t) => t !== e).forEach((t) => t.reset()), e.handleClick(), this.toggleItemActivenessIfNeeded(e), e.closeOnActivate && this.hide());
  }
  /**
   * Creates Flipper instance which allows to navigate between popover items via keyboard
   */
  initializeFlipper() {
    this.flipper = new ms({
      items: this.flippableElements,
      focusedItemClass: md.CSS.focused,
      allowedKeys: [
        ze.TAB,
        ze.UP,
        ze.DOWN,
        ze.ENTER
      ]
    }), this.flipper.onFlip(this.onFlip);
  }
  /**
   * Returns list of elements available for keyboard navigation.
   * Contains both usual popover items elements and custom html content.
   */
  get flippableElements() {
    const e = this.items.map((t) => t.getElement());
    return (this.customContentFlippableItems || []).concat(e);
  }
  get height() {
    let e = 0;
    if (this.nodes.popover === null)
      return e;
    const t = this.nodes.popover.cloneNode(!0);
    return t.style.visibility = "hidden", t.style.position = "absolute", t.style.top = "-1000px", t.classList.add(co.CSS.popoverOpened), document.body.appendChild(t), e = t.offsetHeight, t.remove(), e;
  }
  /**
   * Checks if popover should be opened bottom.
   * It should happen when there is enough space below or not enough space above
   */
  get shouldOpenBottom() {
    const e = this.nodes.popover.getBoundingClientRect(), t = this.scopeElement.getBoundingClientRect(), n = this.height, r = e.top + n, a = e.top - n, c = Math.min(window.innerHeight, t.bottom);
    return a < t.top || r <= c;
  }
  /**
   * Toggles nothing found message visibility
   *
   * @param isDisplayed - true if the message should be displayed
   */
  toggleNothingFoundMessage(e) {
    this.nodes.nothingFoundMessage.classList.toggle(co.CSS.nothingFoundMessageDisplayed, e);
  }
  /**
   * Toggles custom content visibility
   *
   * @param isDisplayed - true if custom content should be displayed
   */
  toggleCustomContent(e) {
    var t;
    (t = this.nodes.customContent) == null || t.classList.toggle(co.CSS.customContentHidden, e);
  }
  /**
   * - Toggles item active state, if clicked popover item has property 'toggle' set to true.
   *
   * - Performs radiobutton-like behavior if the item has property 'toggle' set to string key.
   * (All the other items with the same key get inactive, and the item gets active)
   *
   * @param clickedItem - popover item that was clicked
   */
  toggleItemActivenessIfNeeded(e) {
    if (e.toggle === !0 && e.toggleActive(), typeof e.toggle == "string") {
      const t = this.items.filter((n) => n.toggle === e.toggle);
      if (t.length === 1) {
        e.toggleActive();
        return;
      }
      t.forEach((n) => {
        n.toggleActive(n === e);
      });
    }
  }
};
dm([
  bs
], $h.prototype, "height", 1);
let Yh = $h, hm = class extends rt {
  constructor() {
    super(...arguments), this.opened = !1, this.selection = new Fe(), this.onPopoverClose = () => {
      this.close();
    };
  }
  /**
   * Module Events
   *
   * @returns {{opened: string, closed: string}}
   */
  get events() {
    return {
      opened: "block-settings-opened",
      closed: "block-settings-closed"
    };
  }
  /**
   * Block Settings CSS
   */
  get CSS() {
    return {
      settings: "ce-settings"
    };
  }
  /**
   * Getter for inner popover's flipper instance
   *
   * @todo remove once BlockSettings becomes standalone non-module class
   */
  get flipper() {
    var e;
    return (e = this.popover) == null ? void 0 : e.flipper;
  }
  /**
   * Panel with block settings with 2 sections:
   *  - Tool's Settings
   *  - Default Settings [Move, Remove, etc]
   */
  make() {
    this.nodes.wrapper = ae.make("div", [this.CSS.settings]);
  }
  /**
   * Destroys module
   */
  destroy() {
    this.removeAllNodes();
  }
  /**
   * Open Block Settings pane
   *
   * @param targetBlock - near which Block we should open BlockSettings
   */
  open(e = this.Editor.BlockManager.currentBlock) {
    this.opened = !0, this.selection.save(), this.Editor.BlockSelection.selectBlock(e), this.Editor.BlockSelection.clearCache();
    const [t, n] = e.getTunes();
    this.eventsDispatcher.emit(this.events.opened), this.popover = new Yh({
      searchable: !0,
      items: t.map((r) => this.resolveTuneAliases(r)),
      customContent: n,
      customContentFlippableItems: this.getControls(n),
      scopeElement: this.Editor.API.methods.ui.nodes.redactor,
      messages: {
        nothingFound: Ko.ui(kn.ui.popover, "Nothing found"),
        search: Ko.ui(kn.ui.popover, "Filter")
      }
    }), this.popover.on(na.Close, this.onPopoverClose), this.nodes.wrapper.append(this.popover.getElement()), this.popover.show();
  }
  /**
   * Returns root block settings element
   */
  getElement() {
    return this.nodes.wrapper;
  }
  /**
   * Close Block Settings pane
   */
  close() {
    this.opened && (this.opened = !1, Fe.isAtEditor || this.selection.restore(), this.selection.clearSaved(), !this.Editor.CrossBlockSelection.isCrossBlockSelectionStarted && this.Editor.BlockManager.currentBlock && this.Editor.BlockSelection.unselectBlock(this.Editor.BlockManager.currentBlock), this.eventsDispatcher.emit(this.events.closed), this.popover && (this.popover.off(na.Close, this.onPopoverClose), this.popover.destroy(), this.popover.getElement().remove(), this.popover = null));
  }
  /**
   * Returns list of buttons and inputs inside specified container
   *
   * @param container - container to query controls inside of
   */
  getControls(e) {
    const { StylesAPI: t } = this.Editor, n = e.querySelectorAll(
      `.${t.classes.settingsButton}, ${ae.allInputsSelector}`
    );
    return Array.from(n);
  }
  /**
   * Resolves aliases in tunes menu items
   *
   * @param item - item with resolved aliases
   */
  resolveTuneAliases(e) {
    const t = Zf(e, { label: "title" });
    return e.confirmation && (t.confirmation = this.resolveTuneAliases(e.confirmation)), t;
  }
}, um = class on extends rt {
  constructor() {
    super(...arguments), this.opened = !1, this.tools = [], this.flipper = null, this.togglingCallback = null;
  }
  /**
   * CSS getter
   */
  static get CSS() {
    return {
      conversionToolbarWrapper: "ce-conversion-toolbar",
      conversionToolbarShowed: "ce-conversion-toolbar--showed",
      conversionToolbarTools: "ce-conversion-toolbar__tools",
      conversionToolbarLabel: "ce-conversion-toolbar__label",
      conversionTool: "ce-conversion-tool",
      conversionToolHidden: "ce-conversion-tool--hidden",
      conversionToolIcon: "ce-conversion-tool__icon",
      conversionToolSecondaryLabel: "ce-conversion-tool__secondary-label",
      conversionToolFocused: "ce-conversion-tool--focused",
      conversionToolActive: "ce-conversion-tool--active"
    };
  }
  /**
   * Create UI of Conversion Toolbar
   */
  make() {
    this.nodes.wrapper = ae.make("div", [
      on.CSS.conversionToolbarWrapper,
      ...this.isRtl ? [this.Editor.UI.CSS.editorRtlFix] : []
    ]), this.nodes.tools = ae.make("div", on.CSS.conversionToolbarTools);
    const e = ae.make("div", on.CSS.conversionToolbarLabel, {
      textContent: Ko.ui(kn.ui.inlineToolbar.converter, "Convert to")
    });
    return this.addTools(), this.enableFlipper(), ae.append(this.nodes.wrapper, e), ae.append(this.nodes.wrapper, this.nodes.tools), this.nodes.wrapper;
  }
  /**
   * Deactivates flipper and removes all nodes
   */
  destroy() {
    this.flipper && (this.flipper.deactivate(), this.flipper = null), this.removeAllNodes();
  }
  /**
   * Toggle conversion dropdown visibility
   *
   * @param {Function} [togglingCallback] — callback that will accept opening state
   */
  toggle(e) {
    this.opened ? this.close() : this.open(), Dt(e) && (this.togglingCallback = e);
  }
  /**
   * Shows Conversion Toolbar
   */
  open() {
    this.filterTools(), this.opened = !0, this.nodes.wrapper.classList.add(on.CSS.conversionToolbarShowed), window.requestAnimationFrame(() => {
      this.flipper.activate(this.tools.map((e) => e.button).filter((e) => !e.classList.contains(on.CSS.conversionToolHidden))), this.flipper.focusFirst(), Dt(this.togglingCallback) && this.togglingCallback(!0);
    });
  }
  /**
   * Closes Conversion Toolbar
   */
  close() {
    this.opened = !1, this.flipper.deactivate(), this.nodes.wrapper.classList.remove(on.CSS.conversionToolbarShowed), Dt(this.togglingCallback) && this.togglingCallback(!1);
  }
  /**
   * Returns true if it has more than one tool available for convert in
   */
  hasTools() {
    return this.tools.length === 1 ? this.tools[0].name !== this.config.defaultBlock : !0;
  }
  /**
   * Replaces one Block with another
   * For that Tools must provide import/export methods
   *
   * @param {string} replacingToolName - name of Tool which replaces current
   * @param blockDataOverrides - If this conversion fired by the one of multiple Toolbox items, extend converted data with this item's "data" overrides
   */
  async replaceWithBlock(e, t) {
    const { BlockManager: n, BlockSelection: r, InlineToolbar: a, Caret: c } = this.Editor;
    n.convert(this.Editor.BlockManager.currentBlock, e, t), r.clearSelection(), this.close(), a.close(), window.requestAnimationFrame(() => {
      c.setToBlock(this.Editor.BlockManager.currentBlock, c.positions.END);
    });
  }
  /**
   * Iterates existing Tools and inserts to the ConversionToolbar
   * if tools have ability to import
   */
  addTools() {
    const e = this.Editor.Tools.blockTools;
    Array.from(e.entries()).forEach(([t, n]) => {
      var r;
      const a = n.conversionConfig;
      !a || !a.import || (r = n.toolbox) == null || r.forEach(
        (c) => this.addToolIfValid(t, c)
      );
    });
  }
  /**
   * Inserts a tool to the ConversionToolbar if the tool's toolbox config is valid
   *
   * @param name - tool's name
   * @param toolboxSettings - tool's single toolbox setting
   */
  addToolIfValid(e, t) {
    sn(t) || !t.icon || this.addTool(e, t);
  }
  /**
   * Add tool to the Conversion Toolbar
   *
   * @param toolName - name of Tool to add
   * @param toolboxItem - tool's toolbox item data
   */
  addTool(e, t) {
    var n;
    const r = ae.make("div", [on.CSS.conversionTool]), a = ae.make("div", [on.CSS.conversionToolIcon]);
    r.dataset.tool = e, a.innerHTML = t.icon, ae.append(r, a), ae.append(r, ae.text(Ko.t(kn.toolNames, t.title || fs(e))));
    const c = (n = this.Editor.Tools.blockTools.get(e)) == null ? void 0 : n.shortcut;
    if (c) {
      const u = ae.make("span", on.CSS.conversionToolSecondaryLabel, {
        innerText: qa(c)
      });
      ae.append(r, u);
    }
    ae.append(this.nodes.tools, r), this.tools.push({
      name: e,
      button: r,
      toolboxItem: t
    }), this.listeners.on(r, "click", async () => {
      await this.replaceWithBlock(e, t.data);
    });
  }
  /**
   * Hide current Tool and show others
   */
  async filterTools() {
    const { currentBlock: e } = this.Editor.BlockManager, t = await e.getActiveToolboxEntry();
    function n(r, a) {
      return r.icon === a.icon && r.title === a.title;
    }
    this.tools.forEach((r) => {
      let a = !1;
      if (t) {
        const c = n(t, r.toolboxItem);
        a = r.button.dataset.tool === e.name && c;
      }
      r.button.hidden = a, r.button.classList.toggle(on.CSS.conversionToolHidden, a);
    });
  }
  /**
   * Prepare Flipper to be able to leaf tools by arrows/tab
   */
  enableFlipper() {
    this.flipper = new ms({
      focusedItemClass: on.CSS.conversionToolFocused
    });
  }
};
var Fh = { exports: {} };
/*!
 * Library for handling keyboard shortcuts
 * @copyright CodeX (https://codex.so)
 * @license MIT
 * @author CodeX (https://codex.so)
 * @version 1.2.0
 */
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(window, function() {
    return function(t) {
      var n = {};
      function r(a) {
        if (n[a])
          return n[a].exports;
        var c = n[a] = { i: a, l: !1, exports: {} };
        return t[a].call(c.exports, c, c.exports, r), c.l = !0, c.exports;
      }
      return r.m = t, r.c = n, r.d = function(a, c, u) {
        r.o(a, c) || Object.defineProperty(a, c, { enumerable: !0, get: u });
      }, r.r = function(a) {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
      }, r.t = function(a, c) {
        if (1 & c && (a = r(a)), 8 & c || 4 & c && typeof a == "object" && a && a.__esModule)
          return a;
        var u = /* @__PURE__ */ Object.create(null);
        if (r.r(u), Object.defineProperty(u, "default", { enumerable: !0, value: a }), 2 & c && typeof a != "string")
          for (var p in a)
            r.d(u, p, (function(g) {
              return a[g];
            }).bind(null, p));
        return u;
      }, r.n = function(a) {
        var c = a && a.__esModule ? function() {
          return a.default;
        } : function() {
          return a;
        };
        return r.d(c, "a", c), c;
      }, r.o = function(a, c) {
        return Object.prototype.hasOwnProperty.call(a, c);
      }, r.p = "", r(r.s = 0);
    }([function(t, n, r) {
      function a(p, g) {
        for (var v = 0; v < g.length; v++) {
          var k = g[v];
          k.enumerable = k.enumerable || !1, k.configurable = !0, "value" in k && (k.writable = !0), Object.defineProperty(p, k.key, k);
        }
      }
      function c(p, g, v) {
        return g && a(p.prototype, g), v && a(p, v), p;
      }
      r.r(n);
      var u = function() {
        function p(g) {
          var v = this;
          (function(k, y) {
            if (!(k instanceof y))
              throw new TypeError("Cannot call a class as a function");
          })(this, p), this.commands = {}, this.keys = {}, this.name = g.name, this.parseShortcutName(g.name), this.element = g.on, this.callback = g.callback, this.executeShortcut = function(k) {
            v.execute(k);
          }, this.element.addEventListener("keydown", this.executeShortcut, !1);
        }
        return c(p, null, [{ key: "supportedCommands", get: function() {
          return { SHIFT: ["SHIFT"], CMD: ["CMD", "CONTROL", "COMMAND", "WINDOWS", "CTRL"], ALT: ["ALT", "OPTION"] };
        } }, { key: "keyCodes", get: function() {
          return { 0: 48, 1: 49, 2: 50, 3: 51, 4: 52, 5: 53, 6: 54, 7: 55, 8: 56, 9: 57, A: 65, B: 66, C: 67, D: 68, E: 69, F: 70, G: 71, H: 72, I: 73, J: 74, K: 75, L: 76, M: 77, N: 78, O: 79, P: 80, Q: 81, R: 82, S: 83, T: 84, U: 85, V: 86, W: 87, X: 88, Y: 89, Z: 90, BACKSPACE: 8, ENTER: 13, ESCAPE: 27, LEFT: 37, UP: 38, RIGHT: 39, DOWN: 40, INSERT: 45, DELETE: 46, ".": 190 };
        } }]), c(p, [{ key: "parseShortcutName", value: function(g) {
          g = g.split("+");
          for (var v = 0; v < g.length; v++) {
            g[v] = g[v].toUpperCase();
            var k = !1;
            for (var y in p.supportedCommands)
              if (p.supportedCommands[y].includes(g[v])) {
                k = this.commands[y] = !0;
                break;
              }
            k || (this.keys[g[v]] = !0);
          }
          for (var T in p.supportedCommands)
            this.commands[T] || (this.commands[T] = !1);
        } }, { key: "execute", value: function(g) {
          var v, k = { CMD: g.ctrlKey || g.metaKey, SHIFT: g.shiftKey, ALT: g.altKey }, y = !0;
          for (v in this.commands)
            this.commands[v] !== k[v] && (y = !1);
          var T, x = !0;
          for (T in this.keys)
            x = x && g.keyCode === p.keyCodes[T];
          y && x && this.callback(g);
        } }, { key: "remove", value: function() {
          this.element.removeEventListener("keydown", this.executeShortcut);
        } }]), p;
      }();
      n.default = u;
    }]).default;
  });
})(Fh);
var pm = Fh.exports;
const fm = /* @__PURE__ */ Ua(pm);
let mm = class {
  constructor() {
    this.registeredShortcuts = /* @__PURE__ */ new Map();
  }
  /**
   * Register shortcut
   *
   * @param shortcut - shortcut options
   */
  add(e) {
    if (this.findShortcut(e.on, e.name))
      throw Error(
        `Shortcut ${e.name} is already registered for ${e.on}. Please remove it before add a new handler.`
      );
    const t = new fm({
      name: e.name,
      on: e.on,
      callback: e.handler
    }), n = this.registeredShortcuts.get(e.on) || [];
    this.registeredShortcuts.set(e.on, [...n, t]);
  }
  /**
   * Remove shortcut
   *
   * @param element - Element shortcut is set for
   * @param name - shortcut name
   */
  remove(e, t) {
    const n = this.findShortcut(e, t);
    if (!n)
      return;
    n.remove();
    const r = this.registeredShortcuts.get(e);
    this.registeredShortcuts.set(e, r.filter((a) => a !== n));
  }
  /**
   * Get Shortcut instance if exist
   *
   * @param element - Element shorcut is set for
   * @param shortcut - shortcut name
   * @returns {number} index - shortcut index if exist
   */
  findShortcut(e, t) {
    return (this.registeredShortcuts.get(e) || []).find(({ name: n }) => n === t);
  }
};
const gs = new mm();
var gm = Object.defineProperty, bm = Object.getOwnPropertyDescriptor, zh = (o, e, t, n) => {
  for (var r = n > 1 ? void 0 : n ? bm(e, t) : e, a = o.length - 1, c; a >= 0; a--)
    (c = o[a]) && (r = (n ? c(e, t, r) : c(r)) || r);
  return n && r && gm(e, t, r), r;
}, _a = /* @__PURE__ */ ((o) => (o.Opened = "toolbox-opened", o.Closed = "toolbox-closed", o.BlockAdded = "toolbox-block-added", o))(_a || {});
const hc = class Wh extends Va {
  /**
   * Toolbox constructor
   *
   * @param options - available parameters
   * @param options.api - Editor API methods
   * @param options.tools - Tools available to check whether some of them should be displayed at the Toolbox or not
   */
  constructor({ api: e, tools: t, i18nLabels: n }) {
    super(), this.opened = !1, this.nodes = {
      toolbox: null
    }, this.onPopoverClose = () => {
      this.opened = !1, this.emit(
        "toolbox-closed"
        /* Closed */
      );
    }, this.api = e, this.tools = t, this.i18nLabels = n;
  }
  /**
   * Returns True if Toolbox is Empty and nothing to show
   *
   * @returns {boolean}
   */
  get isEmpty() {
    return this.toolsToBeDisplayed.length === 0;
  }
  /**
   * CSS styles
   *
   * @returns {Object<string, string>}
   */
  static get CSS() {
    return {
      toolbox: "ce-toolbox"
    };
  }
  /**
   * Makes the Toolbox
   */
  make() {
    return this.popover = new Yh({
      scopeElement: this.api.ui.nodes.redactor,
      searchable: !0,
      messages: {
        nothingFound: this.i18nLabels.nothingFound,
        search: this.i18nLabels.filter
      },
      items: this.toolboxItemsToBeDisplayed
    }), this.popover.on(na.Close, this.onPopoverClose), this.enableShortcuts(), this.nodes.toolbox = this.popover.getElement(), this.nodes.toolbox.classList.add(Wh.CSS.toolbox), this.nodes.toolbox;
  }
  /**
   * Returns true if the Toolbox has the Flipper activated and the Flipper has selected button
   */
  hasFocus() {
    var e;
    return (e = this.popover) == null ? void 0 : e.hasFocus();
  }
  /**
   * Destroy Module
   */
  destroy() {
    var e;
    super.destroy(), this.nodes && this.nodes.toolbox && (this.nodes.toolbox.remove(), this.nodes.toolbox = null), this.removeAllShortcuts(), (e = this.popover) == null || e.off(na.Close, this.onPopoverClose);
  }
  /**
   * Toolbox Tool's button click handler
   *
   * @param toolName - tool type to be activated
   * @param blockDataOverrides - Block data predefined by the activated Toolbox item
   */
  toolButtonActivated(e, t) {
    this.insertNewBlock(e, t);
  }
  /**
   * Open Toolbox with Tools
   */
  open() {
    var e;
    this.isEmpty || ((e = this.popover) == null || e.show(), this.opened = !0, this.emit(
      "toolbox-opened"
      /* Opened */
    ));
  }
  /**
   * Close Toolbox
   */
  close() {
    var e;
    (e = this.popover) == null || e.hide(), this.opened = !1, this.emit(
      "toolbox-closed"
      /* Closed */
    );
  }
  /**
   * Close Toolbox
   */
  toggle() {
    this.opened ? this.close() : this.open();
  }
  get toolsToBeDisplayed() {
    const e = [];
    return this.tools.forEach((t) => {
      t.toolbox && e.push(t);
    }), e;
  }
  get toolboxItemsToBeDisplayed() {
    const e = (t, n) => ({
      icon: t.icon,
      title: Ko.t(kn.toolNames, t.title || fs(n.name)),
      name: n.name,
      onActivate: () => {
        this.toolButtonActivated(n.name, t.data);
      },
      secondaryLabel: n.shortcut ? qa(n.shortcut) : ""
    });
    return this.toolsToBeDisplayed.reduce((t, n) => (Array.isArray(n.toolbox) ? n.toolbox.forEach((r) => {
      t.push(e(r, n));
    }) : n.toolbox !== void 0 && t.push(e(n.toolbox, n)), t), []);
  }
  /**
   * Iterate all tools and enable theirs shortcuts if specified
   */
  enableShortcuts() {
    this.toolsToBeDisplayed.forEach((e) => {
      const t = e.shortcut;
      t && this.enableShortcutForTool(e.name, t);
    });
  }
  /**
   * Enable shortcut Block Tool implemented shortcut
   *
   * @param {string} toolName - Tool name
   * @param {string} shortcut - shortcut according to the ShortcutData Module format
   */
  enableShortcutForTool(e, t) {
    gs.add({
      name: t,
      on: this.api.ui.nodes.redactor,
      handler: (n) => {
        n.preventDefault();
        const r = this.api.blocks.getCurrentBlockIndex(), a = this.api.blocks.getBlockByIndex(r);
        if (a)
          try {
            this.api.blocks.convert(a.id, e), window.requestAnimationFrame(() => {
              this.api.caret.setToBlock(r, "end");
            });
            return;
          } catch {
          }
        this.insertNewBlock(e);
      }
    });
  }
  /**
   * Removes all added shortcuts
   * Fired when the Read-Only mode is activated
   */
  removeAllShortcuts() {
    this.toolsToBeDisplayed.forEach((e) => {
      const t = e.shortcut;
      t && gs.remove(this.api.ui.nodes.redactor, t);
    });
  }
  /**
   * Inserts new block
   * Can be called when button clicked on Toolbox or by ShortcutData
   *
   * @param {string} toolName - Tool name
   * @param blockDataOverrides - predefined Block data
   */
  async insertNewBlock(e, t) {
    const n = this.api.blocks.getCurrentBlockIndex(), r = this.api.blocks.getBlockByIndex(n);
    if (!r)
      return;
    const a = r.isEmpty ? n : n + 1;
    let c;
    if (t) {
      const p = await this.api.blocks.composeBlockData(e);
      c = Object.assign(p, t);
    }
    const u = this.api.blocks.insert(
      e,
      c,
      void 0,
      a,
      void 0,
      r.isEmpty
    );
    u.call(ui.APPEND_CALLBACK), this.api.caret.setToBlock(a), this.emit("toolbox-block-added", {
      block: u
    }), this.api.toolbar.close();
  }
};
zh([
  bs
], hc.prototype, "toolsToBeDisplayed", 1);
zh([
  bs
], hc.prototype, "toolboxItemsToBeDisplayed", 1);
let vm = hc;
const Uh = "block hovered";
let ym = class extends rt {
  /**
   * @class
   * @param moduleConfiguration - Module Configuration
   * @param moduleConfiguration.config - Editor's config
   * @param moduleConfiguration.eventsDispatcher - Editor's event dispatcher
   */
  constructor({ config: e, eventsDispatcher: t }) {
    super({
      config: e,
      eventsDispatcher: t
    }), this.toolboxInstance = null;
  }
  /**
   * CSS styles
   *
   * @returns {object}
   */
  get CSS() {
    return {
      toolbar: "ce-toolbar",
      content: "ce-toolbar__content",
      actions: "ce-toolbar__actions",
      actionsOpened: "ce-toolbar__actions--opened",
      toolbarOpened: "ce-toolbar--opened",
      openedToolboxHolderModifier: "codex-editor--toolbox-opened",
      plusButton: "ce-toolbar__plus",
      plusButtonShortcut: "ce-toolbar__plus-shortcut",
      settingsToggler: "ce-toolbar__settings-btn",
      settingsTogglerHidden: "ce-toolbar__settings-btn--hidden"
    };
  }
  /**
   * Returns the Toolbar opening state
   *
   * @returns {boolean}
   */
  get opened() {
    return this.nodes.wrapper.classList.contains(this.CSS.toolbarOpened);
  }
  /**
   * Public interface for accessing the Toolbox
   */
  get toolbox() {
    var e;
    return {
      opened: (e = this.toolboxInstance) == null ? void 0 : e.opened,
      close: () => {
        var t;
        (t = this.toolboxInstance) == null || t.close();
      },
      open: () => {
        if (this.toolboxInstance === null) {
          bt("toolbox.open() called before initialization is finished", "warn");
          return;
        }
        this.Editor.BlockManager.currentBlock = this.hoveredBlock, this.toolboxInstance.open();
      },
      toggle: () => {
        if (this.toolboxInstance === null) {
          bt("toolbox.toggle() called before initialization is finished", "warn");
          return;
        }
        this.toolboxInstance.toggle();
      },
      hasFocus: () => {
        var t;
        return (t = this.toolboxInstance) == null ? void 0 : t.hasFocus();
      }
    };
  }
  /**
   * Block actions appearance manipulations
   */
  get blockActions() {
    return {
      hide: () => {
        this.nodes.actions.classList.remove(this.CSS.actionsOpened);
      },
      show: () => {
        this.nodes.actions.classList.add(this.CSS.actionsOpened);
      }
    };
  }
  /**
   * Methods for working with Block Tunes toggler
   */
  get blockTunesToggler() {
    return {
      hide: () => this.nodes.settingsToggler.classList.add(this.CSS.settingsTogglerHidden),
      show: () => this.nodes.settingsToggler.classList.remove(this.CSS.settingsTogglerHidden)
    };
  }
  /**
   * Toggles read-only mode
   *
   * @param {boolean} readOnlyEnabled - read-only mode
   */
  toggleReadOnly(e) {
    e ? (this.destroy(), this.Editor.BlockSettings.destroy(), this.disableModuleBindings()) : window.requestIdleCallback(() => {
      this.drawUI(), this.enableModuleBindings();
    }, { timeout: 2e3 });
  }
  /**
   * Move Toolbar to the passed (or current) Block
   *
   * @param block - block to move Toolbar near it
   */
  moveAndOpen(e = this.Editor.BlockManager.currentBlock) {
    if (this.toolboxInstance === null) {
      bt("Can't open Toolbar since Editor initialization is not finished yet", "warn");
      return;
    }
    if (this.toolboxInstance.opened && this.toolboxInstance.close(), this.Editor.BlockSettings.opened && this.Editor.BlockSettings.close(), !e)
      return;
    this.hoveredBlock = e;
    const t = e.holder, { isMobile: n } = this.Editor.UI, r = e.pluginsContent, a = window.getComputedStyle(r), c = parseInt(a.paddingTop, 10), u = t.offsetHeight;
    let p;
    n ? p = t.offsetTop + u : p = t.offsetTop + c, this.nodes.wrapper.style.top = `${Math.floor(p)}px`, this.Editor.BlockManager.blocks.length === 1 && e.isEmpty ? this.blockTunesToggler.hide() : this.blockTunesToggler.show(), this.open();
  }
  /**
   * Close the Toolbar
   */
  close() {
    var e, t;
    this.Editor.ReadOnly.isEnabled || ((e = this.nodes.wrapper) == null || e.classList.remove(this.CSS.toolbarOpened), this.blockActions.hide(), (t = this.toolboxInstance) == null || t.close(), this.Editor.BlockSettings.close(), this.reset());
  }
  /**
   * Reset the Toolbar position to prevent DOM height growth, for example after blocks deletion
   */
  reset() {
    this.nodes.wrapper.style.top = "unset";
  }
  /**
   * Open Toolbar with Plus Button and Actions
   *
   * @param {boolean} withBlockActions - by default, Toolbar opens with Block Actions.
   *                                     This flag allows to open Toolbar without Actions.
   */
  open(e = !0) {
    this.nodes.wrapper.classList.add(this.CSS.toolbarOpened), e ? this.blockActions.show() : this.blockActions.hide();
  }
  /**
   * Draws Toolbar elements
   */
  make() {
    this.nodes.wrapper = ae.make("div", this.CSS.toolbar), ["content", "actions"].forEach((r) => {
      this.nodes[r] = ae.make("div", this.CSS[r]);
    }), ae.append(this.nodes.wrapper, this.nodes.content), ae.append(this.nodes.content, this.nodes.actions), this.nodes.plusButton = ae.make("div", this.CSS.plusButton, {
      innerHTML: om
    }), ae.append(this.nodes.actions, this.nodes.plusButton), this.readOnlyMutableListeners.on(this.nodes.plusButton, "click", () => {
      Fl(!0), this.plusButtonClicked();
    }, !1);
    const e = ae.make("div");
    e.appendChild(document.createTextNode(Ko.ui(kn.ui.toolbar.toolbox, "Add"))), e.appendChild(ae.make("div", this.CSS.plusButtonShortcut, {
      textContent: "/"
    })), oa(this.nodes.plusButton, e, {
      hidingDelay: 400
    }), this.nodes.settingsToggler = ae.make("span", this.CSS.settingsToggler, {
      innerHTML: tm
    }), ae.append(this.nodes.actions, this.nodes.settingsToggler);
    const t = ae.make("div"), n = ae.text(Ko.ui(kn.ui.blockTunes.toggler, "Click to tune"));
    t.appendChild(n), t.appendChild(ae.make("div", this.CSS.plusButtonShortcut, {
      textContent: qa("CMD + /")
    })), oa(this.nodes.settingsToggler, t, {
      hidingDelay: 400
    }), ae.append(this.nodes.actions, this.makeToolbox()), ae.append(this.nodes.actions, this.Editor.BlockSettings.getElement()), ae.append(this.Editor.UI.nodes.wrapper, this.nodes.wrapper);
  }
  /**
   * Creates the Toolbox instance and return it's rendered element
   */
  makeToolbox() {
    return this.toolboxInstance = new vm({
      api: this.Editor.API.methods,
      tools: this.Editor.Tools.blockTools,
      i18nLabels: {
        filter: Ko.ui(kn.ui.popover, "Filter"),
        nothingFound: Ko.ui(kn.ui.popover, "Nothing found")
      }
    }), this.toolboxInstance.on(_a.Opened, () => {
      this.Editor.UI.nodes.wrapper.classList.add(this.CSS.openedToolboxHolderModifier);
    }), this.toolboxInstance.on(_a.Closed, () => {
      this.Editor.UI.nodes.wrapper.classList.remove(this.CSS.openedToolboxHolderModifier);
    }), this.toolboxInstance.on(_a.BlockAdded, ({ block: e }) => {
      const { BlockManager: t, Caret: n } = this.Editor, r = t.getBlockById(e.id);
      r.inputs.length === 0 && (r === t.lastBlock ? (t.insertAtEnd(), n.setToBlock(t.lastBlock)) : n.setToBlock(t.nextBlock));
    }), this.toolboxInstance.make();
  }
  /**
   * Handler for Plus Button
   */
  plusButtonClicked() {
    var e;
    this.Editor.BlockManager.currentBlock = this.hoveredBlock, (e = this.toolboxInstance) == null || e.toggle();
  }
  /**
   * Enable bindings
   */
  enableModuleBindings() {
    this.readOnlyMutableListeners.on(this.nodes.settingsToggler, "mousedown", (e) => {
      var t;
      e.stopPropagation(), this.settingsTogglerClicked(), (t = this.toolboxInstance) != null && t.opened && this.toolboxInstance.close(), Fl(!0);
    }, !0), Lr() || this.eventsDispatcher.on(Uh, (e) => {
      var t;
      this.Editor.BlockSettings.opened || (t = this.toolboxInstance) != null && t.opened || this.moveAndOpen(e.block);
    });
  }
  /**
   * Disable bindings
   */
  disableModuleBindings() {
    this.readOnlyMutableListeners.clearAll();
  }
  /**
   * Clicks on the Block Settings toggler
   */
  settingsTogglerClicked() {
    this.Editor.BlockManager.currentBlock = this.hoveredBlock, this.Editor.BlockSettings.opened ? this.Editor.BlockSettings.close() : this.Editor.BlockSettings.open(this.hoveredBlock);
  }
  /**
   * Draws Toolbar UI
   *
   * Toolbar contains BlockSettings and Toolbox.
   * That's why at first we draw its components and then Toolbar itself
   *
   * Steps:
   *  - Make Toolbar dependent components like BlockSettings, Toolbox and so on
   *  - Make itself and append dependent nodes to itself
   *
   */
  drawUI() {
    this.Editor.BlockSettings.make(), this.make();
  }
  /**
   * Removes all created and saved HTMLElements
   * It is used in Read-Only mode
   */
  destroy() {
    this.removeAllNodes(), this.toolboxInstance && this.toolboxInstance.destroy();
  }
};
var Ga = /* @__PURE__ */ ((o) => (o[o.Block = 0] = "Block", o[o.Inline = 1] = "Inline", o[o.Tune = 2] = "Tune", o))(Ga || {}), Ea = /* @__PURE__ */ ((o) => (o.Shortcut = "shortcut", o.Toolbox = "toolbox", o.EnabledInlineTools = "inlineToolbar", o.EnabledBlockTunes = "tunes", o.Config = "config", o))(Ea || {}), qh = /* @__PURE__ */ ((o) => (o.Shortcut = "shortcut", o.SanitizeConfig = "sanitize", o))(qh || {}), Qr = /* @__PURE__ */ ((o) => (o.IsEnabledLineBreaks = "enableLineBreaks", o.Toolbox = "toolbox", o.ConversionConfig = "conversionConfig", o.IsReadOnlySupported = "isReadOnlySupported", o.PasteConfig = "pasteConfig", o))(Qr || {}), uc = /* @__PURE__ */ ((o) => (o.IsInline = "isInline", o.Title = "title", o))(uc || {}), Vh = /* @__PURE__ */ ((o) => (o.IsTune = "isTune", o))(Vh || {});
class pc {
  /**
   * @class
   * @param {ConstructorOptions} options - Constructor options
   */
  constructor({
    name: e,
    constructable: t,
    config: n,
    api: r,
    isDefault: a,
    isInternal: c = !1,
    defaultPlaceholder: u
  }) {
    this.api = r, this.name = e, this.constructable = t, this.config = n, this.isDefault = a, this.isInternal = c, this.defaultPlaceholder = u;
  }
  /**
   * Returns Tool user configuration
   */
  get settings() {
    const e = this.config.config || {};
    return this.isDefault && !("placeholder" in e) && this.defaultPlaceholder && (e.placeholder = this.defaultPlaceholder), e;
  }
  /**
   * Calls Tool's reset method
   */
  reset() {
    if (Dt(this.constructable.reset))
      return this.constructable.reset();
  }
  /**
   * Calls Tool's prepare method
   */
  prepare() {
    if (Dt(this.constructable.prepare))
      return this.constructable.prepare({
        toolName: this.name,
        config: this.settings
      });
  }
  /**
   * Returns shortcut for Tool (internal or specified by user)
   */
  get shortcut() {
    const e = this.constructable.shortcut;
    return this.config.shortcut || e;
  }
  /**
   * Returns Tool's sanitizer configuration
   */
  get sanitizeConfig() {
    return this.constructable.sanitize || {};
  }
  /**
   * Returns true if Tools is inline
   */
  isInline() {
    return this.type === 1;
  }
  /**
   * Returns true if Tools is block
   */
  isBlock() {
    return this.type === 0;
  }
  /**
   * Returns true if Tools is tune
   */
  isTune() {
    return this.type === 2;
  }
}
let wm = class extends rt {
  /**
   * @class
   * @param moduleConfiguration - Module Configuration
   * @param moduleConfiguration.config - Editor's config
   * @param moduleConfiguration.eventsDispatcher - Editor's event dispatcher
   */
  constructor({ config: e, eventsDispatcher: t }) {
    super({
      config: e,
      eventsDispatcher: t
    }), this.CSS = {
      inlineToolbar: "ce-inline-toolbar",
      inlineToolbarShowed: "ce-inline-toolbar--showed",
      inlineToolbarLeftOriented: "ce-inline-toolbar--left-oriented",
      inlineToolbarRightOriented: "ce-inline-toolbar--right-oriented",
      inlineToolbarShortcut: "ce-inline-toolbar__shortcut",
      buttonsWrapper: "ce-inline-toolbar__buttons",
      actionsWrapper: "ce-inline-toolbar__actions",
      inlineToolButton: "ce-inline-tool",
      inputField: "cdx-input",
      focusedButton: "ce-inline-tool--focused",
      conversionToggler: "ce-inline-toolbar__dropdown",
      conversionTogglerArrow: "ce-inline-toolbar__dropdown-arrow",
      conversionTogglerHidden: "ce-inline-toolbar__dropdown--hidden",
      conversionTogglerContent: "ce-inline-toolbar__dropdown-content",
      togglerAndButtonsWrapper: "ce-inline-toolbar__toggler-and-button-wrapper"
    }, this.opened = !1, this.toolbarVerticalMargin = Lr() ? 20 : 6, this.buttonsList = null, this.width = 0, this.flipper = null;
  }
  /**
   * Toggles read-only mode
   *
   * @param {boolean} readOnlyEnabled - read-only mode
   */
  toggleReadOnly(e) {
    e ? (this.destroy(), this.Editor.ConversionToolbar.destroy()) : window.requestIdleCallback(() => {
      this.make();
    }, { timeout: 2e3 });
  }
  /**
   *  Moving / appearance
   *  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
   */
  /**
   * Shows Inline Toolbar if something is selected
   *
   * @param [needToClose] - pass true to close toolbar if it is not allowed.
   *                                  Avoid to use it just for closing IT, better call .close() clearly.
   * @param [needToShowConversionToolbar] - pass false to not to show Conversion Toolbar
   */
  async tryToShow(e = !1, t = !0) {
    e && this.close(), this.allowedToShow() && (await this.addToolsFiltered(t), this.move(), this.open(t), this.Editor.Toolbar.close());
  }
  /**
   * Hides Inline Toolbar
   */
  close() {
    this.opened && (this.Editor.ReadOnly.isEnabled || (this.nodes.wrapper.classList.remove(this.CSS.inlineToolbarShowed), Array.from(this.toolsInstances.entries()).forEach(([e, t]) => {
      const n = this.getToolShortcut(e);
      n && gs.remove(this.Editor.UI.nodes.redactor, n), Dt(t.clear) && t.clear();
    }), this.reset(), this.opened = !1, this.flipper.deactivate(), this.Editor.ConversionToolbar.close()));
  }
  /**
   * Check if node is contained by Inline Toolbar
   *
   * @param {Node} node — node to check
   */
  containsNode(e) {
    return this.nodes.wrapper === void 0 ? !1 : this.nodes.wrapper.contains(e);
  }
  /**
   * Removes UI and its components
   */
  destroy() {
    this.flipper && (this.flipper.deactivate(), this.flipper = null), this.removeAllNodes();
  }
  /**
   * Making DOM
   */
  make() {
    this.nodes.wrapper = ae.make("div", [
      this.CSS.inlineToolbar,
      ...this.isRtl ? [this.Editor.UI.CSS.editorRtlFix] : []
    ]), this.nodes.togglerAndButtonsWrapper = ae.make("div", this.CSS.togglerAndButtonsWrapper), this.nodes.buttons = ae.make("div", this.CSS.buttonsWrapper), this.nodes.actions = ae.make("div", this.CSS.actionsWrapper), this.listeners.on(this.nodes.wrapper, "mousedown", (e) => {
      e.target.closest(`.${this.CSS.actionsWrapper}`) || e.preventDefault();
    }), ae.append(this.nodes.wrapper, [this.nodes.togglerAndButtonsWrapper, this.nodes.actions]), ae.append(this.Editor.UI.nodes.wrapper, this.nodes.wrapper), this.addConversionToggler(), ae.append(this.nodes.togglerAndButtonsWrapper, this.nodes.buttons), this.prepareConversionToolbar(), window.requestAnimationFrame(() => {
      this.recalculateWidth();
    }), this.enableFlipper();
  }
  /**
   * Shows Inline Toolbar
   */
  open() {
    if (this.opened)
      return;
    this.nodes.wrapper.classList.add(this.CSS.inlineToolbarShowed), this.buttonsList = this.nodes.buttons.querySelectorAll(`.${this.CSS.inlineToolButton}`), this.opened = !0;
    let e = Array.from(this.buttonsList);
    e.unshift(this.nodes.conversionToggler), e = e.filter((t) => !t.hidden), this.flipper.activate(e);
  }
  /**
   * Move Toolbar to the selected text
   */
  move() {
    const e = Fe.rect, t = this.Editor.UI.nodes.wrapper.getBoundingClientRect(), n = {
      x: e.x - t.x,
      y: e.y + e.height - // + window.scrollY
      t.top + this.toolbarVerticalMargin
    };
    n.x + this.width + t.x > this.Editor.UI.contentRect.right && (n.x = this.Editor.UI.contentRect.right - this.width - t.x), this.nodes.wrapper.style.left = Math.floor(n.x) + "px", this.nodes.wrapper.style.top = Math.floor(n.y) + "px";
  }
  /**
   * Clear orientation classes and reset position
   */
  reset() {
    this.nodes.wrapper.classList.remove(
      this.CSS.inlineToolbarLeftOriented,
      this.CSS.inlineToolbarRightOriented
    ), this.nodes.wrapper.style.left = "0", this.nodes.wrapper.style.top = "0";
  }
  /**
   * Need to show Inline Toolbar or not
   */
  allowedToShow() {
    const e = ["IMG", "INPUT"], t = Fe.get(), n = Fe.text;
    if (!t || !t.anchorNode || t.isCollapsed || n.length < 1)
      return !1;
    const r = ae.isElement(t.anchorNode) ? t.anchorNode : t.anchorNode.parentElement;
    if (t && e.includes(r.tagName) || r.closest('[contenteditable="true"]') === null)
      return !1;
    const a = this.Editor.BlockManager.getBlock(t.anchorNode);
    return a ? a.tool.inlineTools.size !== 0 : !1;
  }
  /**
   * Recalculate inline toolbar width
   */
  recalculateWidth() {
    this.width = this.nodes.wrapper.offsetWidth;
  }
  /**
   * Create a toggler for Conversion Dropdown
   * and prepend it to the buttons list
   */
  addConversionToggler() {
    this.nodes.conversionToggler = ae.make("div", this.CSS.conversionToggler), this.nodes.conversionTogglerContent = ae.make("div", this.CSS.conversionTogglerContent);
    const e = ae.make("div", this.CSS.conversionTogglerArrow, {
      innerHTML: Ph
    });
    this.nodes.conversionToggler.appendChild(this.nodes.conversionTogglerContent), this.nodes.conversionToggler.appendChild(e), this.nodes.togglerAndButtonsWrapper.appendChild(this.nodes.conversionToggler), this.listeners.on(this.nodes.conversionToggler, "click", () => {
      this.Editor.ConversionToolbar.toggle((t) => {
        !t && this.opened ? this.flipper.activate() : this.opened && this.flipper.deactivate();
      });
    }), Lr() === !1 && oa(this.nodes.conversionToggler, Ko.ui(kn.ui.inlineToolbar.converter, "Convert to"), {
      placement: "top",
      hidingDelay: 100
    });
  }
  /**
   * Changes Conversion Dropdown content for current block's Tool
   */
  async setConversionTogglerContent() {
    const { BlockManager: e } = this.Editor, { currentBlock: t } = e, n = t.name, r = t.tool.conversionConfig, a = r && r.export;
    this.nodes.conversionToggler.hidden = !a, this.nodes.conversionToggler.classList.toggle(this.CSS.conversionTogglerHidden, !a);
    const c = await t.getActiveToolboxEntry() || {};
    this.nodes.conversionTogglerContent.innerHTML = c.icon || c.title || fs(n);
  }
  /**
   * Makes the Conversion Dropdown
   */
  prepareConversionToolbar() {
    const e = this.Editor.ConversionToolbar.make();
    ae.append(this.nodes.wrapper, e);
  }
  /**
   *  Working with Tools
   *  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
   */
  /**
   * Append only allowed Tools
   *
   * @param {boolean} needToShowConversionToolbar - pass false to not to show Conversion Toolbar (e.g. for Footnotes-like tools)
   */
  async addToolsFiltered(e = !0) {
    const t = Fe.get(), n = this.Editor.BlockManager.getBlock(t.anchorNode);
    this.nodes.buttons.innerHTML = "", this.nodes.actions.innerHTML = "", this.toolsInstances = /* @__PURE__ */ new Map(), Array.from(n.tool.inlineTools.values()).forEach((r) => {
      this.addTool(r);
    }), e && this.Editor.ConversionToolbar.hasTools() ? await this.setConversionTogglerContent() : this.nodes.conversionToggler.hidden = !0, this.recalculateWidth();
  }
  /**
   * Add tool button and activate clicks
   *
   * @param {InlineTool} tool - InlineTool object
   */
  addTool(e) {
    const t = e.create(), n = t.render();
    if (!n) {
      bt("Render method must return an instance of Node", "warn", e.name);
      return;
    }
    if (n.dataset.tool = e.name, this.nodes.buttons.appendChild(n), this.toolsInstances.set(e.name, t), Dt(t.renderActions)) {
      const u = t.renderActions();
      this.nodes.actions.appendChild(u);
    }
    this.listeners.on(n, "click", (u) => {
      this.toolClicked(t), u.preventDefault();
    });
    const r = this.getToolShortcut(e.name);
    if (r)
      try {
        this.enableShortcuts(t, r);
      } catch {
      }
    const a = ae.make("div"), c = Ko.t(
      kn.toolNames,
      e.title || fs(e.name)
    );
    a.appendChild(ae.text(c)), r && a.appendChild(ae.make("div", this.CSS.inlineToolbarShortcut, {
      textContent: qa(r)
    })), Lr() === !1 && oa(n, a, {
      placement: "top",
      hidingDelay: 100
    }), t.checkState(Fe.get());
  }
  /**
   * Get shortcut name for tool
   *
   * @param toolName — Tool name
   */
  getToolShortcut(e) {
    const { Tools: t } = this.Editor, n = t.inlineTools.get(e), r = t.internal.inlineTools;
    return Array.from(r.keys()).includes(e) ? this.inlineTools[e][qh.Shortcut] : n.shortcut;
  }
  /**
   * Enable Tool shortcut with Editor Shortcuts Module
   *
   * @param {InlineTool} tool - Tool instance
   * @param {string} shortcut - shortcut according to the ShortcutData Module format
   */
  enableShortcuts(e, t) {
    gs.add({
      name: t,
      handler: (n) => {
        const { currentBlock: r } = this.Editor.BlockManager;
        r && r.tool.enabledInlineTools && (n.preventDefault(), this.toolClicked(e));
      },
      on: this.Editor.UI.nodes.redactor
    });
  }
  /**
   * Inline Tool button clicks
   *
   * @param {InlineTool} tool - Tool's instance
   */
  toolClicked(e) {
    const t = Fe.range;
    e.surround(t), this.checkToolsState(), e.renderActions !== void 0 && this.flipper.deactivate();
  }
  /**
   * Check Tools` state by selection
   */
  checkToolsState() {
    this.toolsInstances.forEach((e) => {
      e.checkState(Fe.get());
    });
  }
  /**
   * Get inline tools tools
   * Tools that has isInline is true
   */
  get inlineTools() {
    const e = {};
    return Array.from(this.Editor.Tools.inlineTools.entries()).forEach(([t, n]) => {
      e[t] = n.create();
    }), e;
  }
  /**
   * Allow to leaf buttons by arrows / tab
   * Buttons will be filled on opening
   */
  enableFlipper() {
    this.flipper = new ms({
      focusedItemClass: this.CSS.focusedButton,
      allowedKeys: [
        ze.ENTER,
        ze.TAB
      ]
    });
  }
}, km = class extends rt {
  /**
   * All keydowns on Block
   *
   * @param {KeyboardEvent} event - keydown
   */
  keydown(e) {
    switch (this.beforeKeydownProcessing(e), e.keyCode) {
      case ze.BACKSPACE:
        this.backspace(e);
        break;
      case ze.DELETE:
        this.delete(e);
        break;
      case ze.ENTER:
        this.enter(e);
        break;
      case ze.DOWN:
      case ze.RIGHT:
        this.arrowRightAndDown(e);
        break;
      case ze.UP:
      case ze.LEFT:
        this.arrowLeftAndUp(e);
        break;
      case ze.TAB:
        this.tabPressed(e);
        break;
      case ze.SLASH:
        e.ctrlKey || e.metaKey ? this.commandSlashPressed() : this.slashPressed();
        break;
    }
  }
  /**
   * Fires on keydown before event processing
   *
   * @param {KeyboardEvent} event - keydown
   */
  beforeKeydownProcessing(e) {
    this.needToolbarClosing(e) && xh(e.keyCode) && (this.Editor.Toolbar.close(), this.Editor.ConversionToolbar.close(), e.ctrlKey || e.metaKey || e.altKey || e.shiftKey || this.Editor.BlockSelection.clearSelection(e));
  }
  /**
   * Key up on Block:
   * - shows Inline Toolbar if something selected
   * - shows conversion toolbar with 85% of block selection
   *
   * @param {KeyboardEvent} event - keyup event
   */
  keyup(e) {
    e.shiftKey || this.Editor.UI.checkEmptiness();
  }
  /**
   * Add drop target styles
   *
   * @param {DragEvent} event - drag over event
   */
  dragOver(e) {
    const t = this.Editor.BlockManager.getBlockByChildNode(e.target);
    t.dropTarget = !0;
  }
  /**
   * Remove drop target style
   *
   * @param {DragEvent} event - drag leave event
   */
  dragLeave(e) {
    const t = this.Editor.BlockManager.getBlockByChildNode(e.target);
    t.dropTarget = !1;
  }
  /**
   * Copying selected blocks
   * Before putting to the clipboard we sanitize all blocks and then copy to the clipboard
   *
   * @param {ClipboardEvent} event - clipboard event
   */
  handleCommandC(e) {
    const { BlockSelection: t } = this.Editor;
    t.anyBlockSelected && t.copySelectedBlocks(e);
  }
  /**
   * Copy and Delete selected Blocks
   *
   * @param {ClipboardEvent} event - clipboard event
   */
  handleCommandX(e) {
    const { BlockSelection: t, BlockManager: n, Caret: r } = this.Editor;
    t.anyBlockSelected && t.copySelectedBlocks(e).then(() => {
      const a = n.removeSelectedBlocks(), c = n.insertDefaultBlockAtIndex(a, !0);
      r.setToBlock(c, r.positions.START), t.clearSelection(e);
    });
  }
  /**
   * Tab pressed inside a Block.
   *
   * @param {KeyboardEvent} event - keydown
   */
  tabPressed(e) {
    const { InlineToolbar: t, ConversionToolbar: n, Caret: r } = this.Editor;
    n.opened || t.opened || (e.shiftKey ? r.navigatePrevious(!0) : r.navigateNext(!0)) && e.preventDefault();
  }
  /**
   * '/' + 'command' keydown inside a Block
   */
  commandSlashPressed() {
    this.Editor.BlockSelection.selectedBlocks.length > 1 || this.activateBlockSettings();
  }
  /**
   * '/' keydown inside a Block
   */
  slashPressed() {
    this.Editor.BlockManager.currentBlock.isEmpty && this.activateToolbox();
  }
  /**
   * ENTER pressed on block
   *
   * @param {KeyboardEvent} event - keydown
   */
  enter(e) {
    const { BlockManager: t, UI: n } = this.Editor;
    if (t.currentBlock.tool.isLineBreaksEnabled || n.someToolbarOpened && n.someFlipperButtonFocused || e.shiftKey)
      return;
    let r = this.Editor.BlockManager.currentBlock;
    this.Editor.Caret.isAtStart && !this.Editor.BlockManager.currentBlock.hasMedia ? this.Editor.BlockManager.insertDefaultBlockAtIndex(this.Editor.BlockManager.currentBlockIndex) : this.Editor.Caret.isAtEnd ? r = this.Editor.BlockManager.insertDefaultBlockAtIndex(this.Editor.BlockManager.currentBlockIndex + 1) : r = this.Editor.BlockManager.split(), this.Editor.Caret.setToBlock(r), this.Editor.Toolbar.moveAndOpen(r), e.preventDefault();
  }
  /**
   * Handle backspace keydown on Block
   *
   * @param {KeyboardEvent} event - keydown
   */
  backspace(e) {
    const { BlockManager: t, Caret: n } = this.Editor, { currentBlock: r, previousBlock: a } = t;
    if (!(!Fe.isCollapsed || !n.isAtStart)) {
      if (e.preventDefault(), this.Editor.Toolbar.close(), r.currentInput !== r.firstInput) {
        n.navigatePrevious();
        return;
      }
      if (a !== null) {
        if (a.isEmpty) {
          t.removeBlock(a);
          return;
        }
        if (r.isEmpty) {
          t.removeBlock(r);
          const c = t.currentBlock;
          n.setToBlock(c, n.positions.END);
          return;
        }
        pd(r, a) ? this.mergeBlocks(a, r) : n.setToBlock(a, n.positions.END);
      }
    }
  }
  /**
   * Handles delete keydown on Block
   * Removes char after the caret.
   * If caret is at the end of the block, merge next block with current
   *
   * @param {KeyboardEvent} event - keydown
   */
  delete(e) {
    const { BlockManager: t, Caret: n } = this.Editor, { currentBlock: r, nextBlock: a } = t;
    if (!(!Fe.isCollapsed || !n.isAtEnd)) {
      if (e.preventDefault(), this.Editor.Toolbar.close(), r.currentInput !== r.lastInput) {
        n.navigateNext();
        return;
      }
      if (a !== null) {
        if (a.isEmpty) {
          t.removeBlock(a);
          return;
        }
        if (r.isEmpty) {
          t.removeBlock(r), n.setToBlock(a, n.positions.START);
          return;
        }
        pd(r, a) ? this.mergeBlocks(r, a) : n.setToBlock(a, n.positions.START);
      }
    }
  }
  /**
   * Merge passed Blocks
   *
   * @param targetBlock - to which Block we want to merge
   * @param blockToMerge - what Block we want to merge
   */
  mergeBlocks(e, t) {
    const { BlockManager: n, Caret: r, Toolbar: a } = this.Editor;
    r.createShadow(e.pluginsContent), n.mergeBlocks(e, t).then(() => {
      window.requestAnimationFrame(() => {
        r.restoreCaret(e.pluginsContent), e.pluginsContent.normalize(), a.close();
      });
    });
  }
  /**
   * Handle right and down keyboard keys
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  arrowRightAndDown(e) {
    const t = ms.usedKeys.includes(e.keyCode) && (!e.shiftKey || e.keyCode === ze.TAB);
    if (this.Editor.UI.someToolbarOpened && t)
      return;
    this.Editor.Toolbar.close();
    const n = this.Editor.Caret.isAtEnd || this.Editor.BlockSelection.anyBlockSelected;
    if (e.shiftKey && e.keyCode === ze.DOWN && n) {
      this.Editor.CrossBlockSelection.toggleBlockSelectedState();
      return;
    }
    if (e.keyCode === ze.DOWN || e.keyCode === ze.RIGHT && !this.isRtl ? this.Editor.Caret.navigateNext() : this.Editor.Caret.navigatePrevious()) {
      e.preventDefault();
      return;
    }
    Ra(() => {
      this.Editor.BlockManager.currentBlock && this.Editor.BlockManager.currentBlock.updateCurrentInput();
    }, 20)(), this.Editor.BlockSelection.clearSelection(e);
  }
  /**
   * Handle left and up keyboard keys
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  arrowLeftAndUp(e) {
    if (this.Editor.UI.someToolbarOpened) {
      if (ms.usedKeys.includes(e.keyCode) && (!e.shiftKey || e.keyCode === ze.TAB))
        return;
      this.Editor.UI.closeAllToolbars();
    }
    this.Editor.Toolbar.close();
    const t = this.Editor.Caret.isAtStart || this.Editor.BlockSelection.anyBlockSelected;
    if (e.shiftKey && e.keyCode === ze.UP && t) {
      this.Editor.CrossBlockSelection.toggleBlockSelectedState(!1);
      return;
    }
    if (e.keyCode === ze.UP || e.keyCode === ze.LEFT && !this.isRtl ? this.Editor.Caret.navigatePrevious() : this.Editor.Caret.navigateNext()) {
      e.preventDefault();
      return;
    }
    Ra(() => {
      this.Editor.BlockManager.currentBlock && this.Editor.BlockManager.currentBlock.updateCurrentInput();
    }, 20)(), this.Editor.BlockSelection.clearSelection(e);
  }
  /**
   * Cases when we need to close Toolbar
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  needToolbarClosing(e) {
    const t = e.keyCode === ze.ENTER && this.Editor.Toolbar.toolbox.opened, n = e.keyCode === ze.ENTER && this.Editor.BlockSettings.opened, r = e.keyCode === ze.ENTER && this.Editor.InlineToolbar.opened, a = e.keyCode === ze.ENTER && this.Editor.ConversionToolbar.opened, c = e.keyCode === ze.TAB;
    return !(e.shiftKey || c || t || n || r || a);
  }
  /**
   * If Toolbox is not open, then just open it and show plus button
   */
  activateToolbox() {
    this.Editor.Toolbar.opened || this.Editor.Toolbar.moveAndOpen(), this.Editor.Toolbar.toolbox.open();
  }
  /**
   * Open Toolbar and show BlockSettings before flipping Tools
   */
  activateBlockSettings() {
    this.Editor.Toolbar.opened || this.Editor.Toolbar.moveAndOpen(), this.Editor.BlockSettings.opened || this.Editor.BlockSettings.open();
  }
}, Cl = class {
  /**
   * @class
   * @param {HTMLElement} workingArea — editor`s working node
   */
  constructor(e) {
    this.blocks = [], this.workingArea = e;
  }
  /**
   * Get length of Block instances array
   *
   * @returns {number}
   */
  get length() {
    return this.blocks.length;
  }
  /**
   * Get Block instances array
   *
   * @returns {Block[]}
   */
  get array() {
    return this.blocks;
  }
  /**
   * Get blocks html elements array
   *
   * @returns {HTMLElement[]}
   */
  get nodes() {
    return Ch(this.workingArea.children);
  }
  /**
   * Proxy trap to implement array-like setter
   *
   * @example
   * blocks[0] = new Block(...)
   * @param {Blocks} instance — Blocks instance
   * @param {PropertyKey} property — block index or any Blocks class property key to set
   * @param {Block} value — value to set
   * @returns {boolean}
   */
  static set(e, t, n) {
    return isNaN(Number(t)) ? (Reflect.set(e, t, n), !0) : (e.insert(+t, n), !0);
  }
  /**
   * Proxy trap to implement array-like getter
   *
   * @param {Blocks} instance — Blocks instance
   * @param {PropertyKey} property — Blocks class property key
   * @returns {Block|*}
   */
  static get(e, t) {
    return isNaN(Number(t)) ? Reflect.get(e, t) : e.get(+t);
  }
  /**
   * Push new Block to the blocks array and append it to working area
   *
   * @param {Block} block - Block to add
   */
  push(e) {
    this.blocks.push(e), this.insertToDOM(e);
  }
  /**
   * Swaps blocks with indexes first and second
   *
   * @param {number} first - first block index
   * @param {number} second - second block index
   * @deprecated — use 'move' instead
   */
  swap(e, t) {
    const n = this.blocks[t];
    ae.swap(this.blocks[e].holder, n.holder), this.blocks[t] = this.blocks[e], this.blocks[e] = n;
  }
  /**
   * Move a block from one to another index
   *
   * @param {number} toIndex - new index of the block
   * @param {number} fromIndex - block to move
   */
  move(e, t) {
    const n = this.blocks.splice(t, 1)[0], r = e - 1, a = Math.max(0, r), c = this.blocks[a];
    e > 0 ? this.insertToDOM(n, "afterend", c) : this.insertToDOM(n, "beforebegin", c), this.blocks.splice(e, 0, n);
    const u = this.composeBlockEvent("move", {
      fromIndex: t,
      toIndex: e
    });
    n.call(ui.MOVED, u);
  }
  /**
   * Insert new Block at passed index
   *
   * @param {number} index — index to insert Block
   * @param {Block} block — Block to insert
   * @param {boolean} replace — it true, replace block on given index
   */
  insert(e, t, n = !1) {
    if (!this.length) {
      this.push(t);
      return;
    }
    e > this.length && (e = this.length), n && (this.blocks[e].holder.remove(), this.blocks[e].call(ui.REMOVED));
    const r = n ? 1 : 0;
    if (this.blocks.splice(e, r, t), e > 0) {
      const a = this.blocks[e - 1];
      this.insertToDOM(t, "afterend", a);
    } else {
      const a = this.blocks[e + 1];
      a ? this.insertToDOM(t, "beforebegin", a) : this.insertToDOM(t);
    }
  }
  /**
   * Replaces block under passed index with passed block
   *
   * @param index - index of existed block
   * @param block - new block
   */
  replace(e, t) {
    if (this.blocks[e] === void 0)
      throw Error("Incorrect index");
    this.blocks[e].holder.replaceWith(t.holder), this.blocks[e] = t;
  }
  /**
   * Inserts several blocks at once
   *
   * @param blocks - blocks to insert
   * @param index - index to insert blocks at
   */
  insertMany(e, t) {
    const n = new DocumentFragment();
    for (const r of e)
      n.appendChild(r.holder);
    if (this.length > 0) {
      if (t > 0) {
        const r = Math.min(t - 1, this.length - 1);
        this.blocks[r].holder.after(n);
      } else
        t === 0 && this.workingArea.prepend(n);
      this.blocks.splice(t, 0, ...e);
    } else
      this.blocks.push(...e), this.workingArea.appendChild(n);
    e.forEach((r) => r.call(ui.RENDERED));
  }
  /**
   * Remove block
   *
   * @param {number} index - index of Block to remove
   */
  remove(e) {
    isNaN(e) && (e = this.length - 1), this.blocks[e].holder.remove(), this.blocks[e].call(ui.REMOVED), this.blocks.splice(e, 1);
  }
  /**
   * Remove all blocks
   */
  removeAll() {
    this.workingArea.innerHTML = "", this.blocks.forEach((e) => e.call(ui.REMOVED)), this.blocks.length = 0;
  }
  /**
   * Insert Block after passed target
   *
   * @todo decide if this method is necessary
   * @param {Block} targetBlock — target after which Block should be inserted
   * @param {Block} newBlock — Block to insert
   */
  insertAfter(e, t) {
    const n = this.blocks.indexOf(e);
    this.insert(n + 1, t);
  }
  /**
   * Get Block by index
   *
   * @param {number} index — Block index
   * @returns {Block}
   */
  get(e) {
    return this.blocks[e];
  }
  /**
   * Return index of passed Block
   *
   * @param {Block} block - Block to find
   * @returns {number}
   */
  indexOf(e) {
    return this.blocks.indexOf(e);
  }
  /**
   * Insert new Block into DOM
   *
   * @param {Block} block - Block to insert
   * @param {InsertPosition} position — insert position (if set, will use insertAdjacentElement)
   * @param {Block} target — Block related to position
   */
  insertToDOM(e, t, n) {
    t ? n.holder.insertAdjacentElement(t, e.holder) : this.workingArea.appendChild(e.holder), e.call(ui.RENDERED);
  }
  /**
   * Composes Block event with passed type and details
   *
   * @param {string} type - event type
   * @param {object} detail - event detail
   */
  composeBlockEvent(e, t) {
    return new CustomEvent(e, {
      detail: t
    });
  }
};
const gd = "block-removed", bd = "block-added", xm = "block-moved", vd = "block-changed";
let Cm = class {
  constructor() {
    this.completed = Promise.resolve();
  }
  /**
   * Add new promise to queue
   *
   * @param operation - promise should be added to queue
   */
  add(e) {
    return new Promise((t, n) => {
      this.completed = this.completed.then(e).then(t).catch(n);
    });
  }
}, Sm = class extends rt {
  constructor() {
    super(...arguments), this._currentBlockIndex = -1, this._blocks = null;
  }
  /**
   * Returns current Block index
   *
   * @returns {number}
   */
  get currentBlockIndex() {
    return this._currentBlockIndex;
  }
  /**
   * Set current Block index and fire Block lifecycle callbacks
   *
   * @param {number} newIndex - index of Block to set as current
   */
  set currentBlockIndex(e) {
    this._currentBlockIndex = e;
  }
  /**
   * returns first Block
   *
   * @returns {Block}
   */
  get firstBlock() {
    return this._blocks[0];
  }
  /**
   * returns last Block
   *
   * @returns {Block}
   */
  get lastBlock() {
    return this._blocks[this._blocks.length - 1];
  }
  /**
   * Get current Block instance
   *
   * @returns {Block}
   */
  get currentBlock() {
    return this._blocks[this.currentBlockIndex];
  }
  /**
   * Set passed Block as a current
   *
   * @param block - block to set as a current
   */
  set currentBlock(e) {
    this.currentBlockIndex = this.getBlockIndex(e);
  }
  /**
   * Returns next Block instance
   *
   * @returns {Block|null}
   */
  get nextBlock() {
    return this.currentBlockIndex === this._blocks.length - 1 ? null : this._blocks[this.currentBlockIndex + 1];
  }
  /**
   * Return first Block with inputs after current Block
   *
   * @returns {Block | undefined}
   */
  get nextContentfulBlock() {
    return this.blocks.slice(this.currentBlockIndex + 1).find((e) => !!e.inputs.length);
  }
  /**
   * Return first Block with inputs before current Block
   *
   * @returns {Block | undefined}
   */
  get previousContentfulBlock() {
    return this.blocks.slice(0, this.currentBlockIndex).reverse().find((e) => !!e.inputs.length);
  }
  /**
   * Returns previous Block instance
   *
   * @returns {Block|null}
   */
  get previousBlock() {
    return this.currentBlockIndex === 0 ? null : this._blocks[this.currentBlockIndex - 1];
  }
  /**
   * Get array of Block instances
   *
   * @returns {Block[]} {@link Blocks#array}
   */
  get blocks() {
    return this._blocks.array;
  }
  /**
   * Check if each Block is empty
   *
   * @returns {boolean}
   */
  get isEditorEmpty() {
    return this.blocks.every((e) => e.isEmpty);
  }
  /**
   * Should be called after Editor.UI preparation
   * Define this._blocks property
   */
  prepare() {
    const e = new Cl(this.Editor.UI.nodes.redactor);
    this._blocks = new Proxy(e, {
      set: Cl.set,
      get: Cl.get
    }), this.listeners.on(
      document,
      "copy",
      (t) => this.Editor.BlockEvents.handleCommandC(t)
    );
  }
  /**
   * Toggle read-only state
   *
   * If readOnly is true:
   *  - Unbind event handlers from created Blocks
   *
   * if readOnly is false:
   *  - Bind event handlers to all existing Blocks
   *
   * @param {boolean} readOnlyEnabled - "read only" state
   */
  toggleReadOnly(e) {
    e ? this.disableModuleBindings() : this.enableModuleBindings();
  }
  /**
   * Creates Block instance by tool name
   *
   * @param {object} options - block creation options
   * @param {string} options.tool - tools passed in editor config {@link EditorConfig#tools}
   * @param {string} [options.id] - unique id for this block
   * @param {BlockToolData} [options.data] - constructor params
   * @returns {Block}
   */
  composeBlock({
    tool: e,
    data: t = {},
    id: n = void 0,
    tunes: r = {}
  }) {
    const a = this.Editor.ReadOnly.isEnabled, c = this.Editor.Tools.blockTools.get(e), u = new pi({
      id: n,
      data: t,
      tool: c,
      api: this.Editor.API,
      readOnly: a,
      tunesData: r
    }, this.eventsDispatcher);
    return a || window.requestIdleCallback(() => {
      this.bindBlockEvents(u);
    }, { timeout: 2e3 }), u;
  }
  /**
   * Insert new block into _blocks
   *
   * @param {object} options - insert options
   * @param {string} [options.id] - block's unique id
   * @param {string} [options.tool] - plugin name, by default method inserts the default block type
   * @param {object} [options.data] - plugin data
   * @param {number} [options.index] - index where to insert new Block
   * @param {boolean} [options.needToFocus] - flag shows if needed to update current Block index
   * @param {boolean} [options.replace] - flag shows if block by passed index should be replaced with inserted one
   * @returns {Block}
   */
  insert({
    id: e = void 0,
    tool: t = this.config.defaultBlock,
    data: n = {},
    index: r,
    needToFocus: a = !0,
    replace: c = !1,
    tunes: u = {}
  } = {}) {
    let p = r;
    p === void 0 && (p = this.currentBlockIndex + (c ? 0 : 1));
    const g = this.composeBlock({
      id: e,
      tool: t,
      data: n,
      tunes: u
    });
    return c && this.blockDidMutated(gd, this.getBlockByIndex(p), {
      index: p
    }), this._blocks.insert(p, g, c), this.blockDidMutated(bd, g, {
      index: p
    }), a ? this.currentBlockIndex = p : p <= this.currentBlockIndex && this.currentBlockIndex++, g;
  }
  /**
   * Inserts several blocks at once
   *
   * @param blocks - blocks to insert
   * @param index - index where to insert
   */
  insertMany(e, t = 0) {
    this._blocks.insertMany(e, t);
  }
  /**
   * Update Block data.
   *
   * Currently we don't have an 'update' method in the Tools API, so we just create a new block with the same id and type
   * Should not trigger 'block-removed' or 'block-added' events
   *
   * @param block - block to update
   * @param data - new data
   */
  async update(e, t) {
    const n = await e.data, r = this.composeBlock({
      id: e.id,
      tool: e.name,
      data: Object.assign({}, n, t),
      tunes: e.tunes
    }), a = this.getBlockIndex(e);
    return this._blocks.replace(a, r), this.blockDidMutated(vd, r, {
      index: a
    }), r;
  }
  /**
   * Replace passed Block with the new one with specified Tool and data
   *
   * @param block - block to replace
   * @param newTool - new Tool name
   * @param data - new Tool data
   */
  replace(e, t, n) {
    const r = this.getBlockIndex(e);
    this.insert({
      tool: t,
      data: n,
      index: r,
      replace: !0
    });
  }
  /**
   * Insert pasted content. Call onPaste callback after insert.
   *
   * @param {string} toolName - name of Tool to insert
   * @param {PasteEvent} pasteEvent - pasted data
   * @param {boolean} replace - should replace current block
   */
  paste(e, t, n = !1) {
    const r = this.insert({
      tool: e,
      replace: n
    });
    try {
      window.requestIdleCallback(() => {
        r.call(ui.ON_PASTE, t);
      });
    } catch (a) {
      bt(`${e}: onPaste callback call is failed`, "error", a);
    }
    return r;
  }
  /**
   * Insert new default block at passed index
   *
   * @param {number} index - index where Block should be inserted
   * @param {boolean} needToFocus - if true, updates current Block index
   *
   * TODO: Remove method and use insert() with index instead (?)
   * @returns {Block} inserted Block
   */
  insertDefaultBlockAtIndex(e, t = !1) {
    const n = this.composeBlock({ tool: this.config.defaultBlock });
    return this._blocks[e] = n, this.blockDidMutated(bd, n, {
      index: e
    }), t ? this.currentBlockIndex = e : e <= this.currentBlockIndex && this.currentBlockIndex++, n;
  }
  /**
   * Always inserts at the end
   *
   * @returns {Block}
   */
  insertAtEnd() {
    return this.currentBlockIndex = this.blocks.length - 1, this.insert();
  }
  /**
   * Merge two blocks
   *
   * @param {Block} targetBlock - previous block will be append to this block
   * @param {Block} blockToMerge - block that will be merged with target block
   * @returns {Promise} - the sequence that can be continued
   */
  async mergeBlocks(e, t) {
    const n = await t.data;
    sn(n) || await e.mergeWith(n), this.removeBlock(t), this.currentBlockIndex = this._blocks.indexOf(e);
  }
  /**
   * Remove passed Block
   *
   * @param block - Block to remove
   * @param addLastBlock - if true, adds new default block at the end. @todo remove this logic and use event-bus instead
   */
  removeBlock(e, t = !0) {
    return new Promise((n) => {
      const r = this._blocks.indexOf(e);
      if (!this.validateIndex(r))
        throw new Error("Can't find a Block to remove");
      e.destroy(), this._blocks.remove(r), this.blockDidMutated(gd, e, {
        index: r
      }), this.currentBlockIndex >= r && this.currentBlockIndex--, this.blocks.length ? r === 0 && (this.currentBlockIndex = 0) : (this.currentBlockIndex = -1, t && this.insert()), n();
    });
  }
  /**
   * Remove only selected Blocks
   * and returns first Block index where started removing...
   *
   * @returns {number|undefined}
   */
  removeSelectedBlocks() {
    let e;
    for (let t = this.blocks.length - 1; t >= 0; t--)
      this.blocks[t].selected && (this.removeBlock(this.blocks[t]), e = t);
    return e;
  }
  /**
   * Attention!
   * After removing insert the new default typed Block and focus on it
   * Removes all blocks
   */
  removeAllBlocks() {
    for (let e = this.blocks.length - 1; e >= 0; e--)
      this._blocks.remove(e);
    this.currentBlockIndex = -1, this.insert(), this.currentBlock.firstInput.focus();
  }
  /**
   * Split current Block
   * 1. Extract content from Caret position to the Block`s end
   * 2. Insert a new Block below current one with extracted content
   *
   * @returns {Block}
   */
  split() {
    const e = this.Editor.Caret.extractFragmentFromCaretPosition(), t = ae.make("div");
    t.appendChild(e);
    const n = {
      text: ae.isEmpty(t) ? "" : t.innerHTML
    };
    return this.insert({ data: n });
  }
  /**
   * Returns Block by passed index
   *
   * @param {number} index - index to get. -1 to get last
   * @returns {Block}
   */
  getBlockByIndex(e) {
    return e === -1 && (e = this._blocks.length - 1), this._blocks[e];
  }
  /**
   * Returns an index for passed Block
   *
   * @param block - block to find index
   */
  getBlockIndex(e) {
    return this._blocks.indexOf(e);
  }
  /**
   * Returns the Block by passed id
   *
   * @param id - id of block to get
   * @returns {Block}
   */
  getBlockById(e) {
    return this._blocks.array.find((t) => t.id === e);
  }
  /**
   * Get Block instance by html element
   *
   * @param {Node} element - html element to get Block by
   */
  getBlock(e) {
    ae.isElement(e) || (e = e.parentNode);
    const t = this._blocks.nodes, n = e.closest(`.${pi.CSS.wrapper}`), r = t.indexOf(n);
    if (r >= 0)
      return this._blocks[r];
  }
  /**
   * 1) Find first-level Block from passed child Node
   * 2) Mark it as current
   *
   * @param {Node} childNode - look ahead from this node.
   * @returns {Block | undefined} can return undefined in case when the passed child note is not a part of the current editor instance
   */
  setCurrentBlockByChildNode(e) {
    ae.isElement(e) || (e = e.parentNode);
    const t = e.closest(`.${pi.CSS.wrapper}`);
    if (!t)
      return;
    const n = t.closest(`.${this.Editor.UI.CSS.editorWrapper}`);
    if (n != null && n.isEqualNode(this.Editor.UI.nodes.wrapper))
      return this.currentBlockIndex = this._blocks.nodes.indexOf(t), this.currentBlock.updateCurrentInput(), this.currentBlock;
  }
  /**
   * Return block which contents passed node
   *
   * @param {Node} childNode - node to get Block by
   * @returns {Block}
   */
  getBlockByChildNode(e) {
    if (!e || !(e instanceof Node))
      return;
    ae.isElement(e) || (e = e.parentNode);
    const t = e.closest(`.${pi.CSS.wrapper}`);
    return this.blocks.find((n) => n.holder === t);
  }
  /**
   * Swap Blocks Position
   *
   * @param {number} fromIndex - index of first block
   * @param {number} toIndex - index of second block
   * @deprecated — use 'move' instead
   */
  swap(e, t) {
    this._blocks.swap(e, t), this.currentBlockIndex = t;
  }
  /**
   * Move a block to a new index
   *
   * @param {number} toIndex - index where to move Block
   * @param {number} fromIndex - index of Block to move
   */
  move(e, t = this.currentBlockIndex) {
    if (isNaN(e) || isNaN(t)) {
      bt("Warning during 'move' call: incorrect indices provided.", "warn");
      return;
    }
    if (!this.validateIndex(e) || !this.validateIndex(t)) {
      bt("Warning during 'move' call: indices cannot be lower than 0 or greater than the amount of blocks.", "warn");
      return;
    }
    this._blocks.move(e, t), this.currentBlockIndex = e, this.blockDidMutated(xm, this.currentBlock, {
      fromIndex: t,
      toIndex: e
    });
  }
  /**
   * Converts passed Block to the new Tool
   * Uses Conversion Config
   *
   * @param blockToConvert - Block that should be converted
   * @param targetToolName - name of the Tool to convert to
   * @param blockDataOverrides - optional new Block data overrides
   */
  async convert(e, t, n) {
    if (!await e.save())
      throw new Error("Could not convert Block. Failed to extract original Block data.");
    const r = this.Editor.Tools.blockTools.get(t);
    if (!r)
      throw new Error(`Could not convert Block. Tool «${t}» not found.`);
    const a = await e.exportDataAsString(), c = fi(
      a,
      r.sanitizeConfig
    );
    let u = wf(c, r.conversionConfig);
    n && (u = Object.assign(u, n)), this.replace(e, r.name, u);
  }
  /**
   * Sets current Block Index -1 which means unknown
   * and clear highlights
   */
  dropPointer() {
    this.currentBlockIndex = -1;
  }
  /**
   * Clears Editor
   *
   * @param {boolean} needToAddDefaultBlock - 1) in internal calls (for example, in api.blocks.render)
   *                                             we don't need to add an empty default block
   *                                        2) in api.blocks.clear we should add empty block
   */
  async clear(e = !1) {
    const t = new Cm();
    this.blocks.forEach((n) => {
      t.add(async () => {
        await this.removeBlock(n, !1);
      });
    }), await t.completed, this.dropPointer(), e && this.insert(), this.Editor.UI.checkEmptiness();
  }
  /**
   * Cleans up all the block tools' resources
   * This is called when editor is destroyed
   */
  async destroy() {
    await Promise.all(this.blocks.map((e) => e.destroy()));
  }
  /**
   * Bind Block events
   *
   * @param {Block} block - Block to which event should be bound
   */
  bindBlockEvents(e) {
    const { BlockEvents: t } = this.Editor;
    this.readOnlyMutableListeners.on(e.holder, "keydown", (n) => {
      t.keydown(n);
    }), this.readOnlyMutableListeners.on(e.holder, "keyup", (n) => {
      t.keyup(n);
    }), this.readOnlyMutableListeners.on(e.holder, "dragover", (n) => {
      t.dragOver(n);
    }), this.readOnlyMutableListeners.on(e.holder, "dragleave", (n) => {
      t.dragLeave(n);
    }), e.on("didMutated", (n) => this.blockDidMutated(vd, n, {
      index: this.getBlockIndex(n)
    }));
  }
  /**
   * Disable mutable handlers and bindings
   */
  disableModuleBindings() {
    this.readOnlyMutableListeners.clearAll();
  }
  /**
   * Enables all module handlers and bindings for all Blocks
   */
  enableModuleBindings() {
    this.readOnlyMutableListeners.on(
      document,
      "cut",
      (e) => this.Editor.BlockEvents.handleCommandX(e)
    ), this.blocks.forEach((e) => {
      this.bindBlockEvents(e);
    });
  }
  /**
   * Validates that the given index is not lower than 0 or higher than the amount of blocks
   *
   * @param {number} index - index of blocks array to validate
   * @returns {boolean}
   */
  validateIndex(e) {
    return !(e < 0 || e >= this._blocks.length);
  }
  /**
   * Block mutation callback
   *
   * @param mutationType - what happened with block
   * @param block - mutated block
   * @param detailData - additional data to pass with change event
   */
  blockDidMutated(e, t, n) {
    const r = new CustomEvent(e, {
      detail: {
        target: new Er(t),
        ...n
      }
    });
    return this.eventsDispatcher.emit(Dh, {
      event: r
    }), t;
  }
}, Tm = class extends rt {
  constructor() {
    super(...arguments), this.anyBlockSelectedCache = null, this.needToSelectAll = !1, this.nativeInputSelected = !1, this.readyToBlockSelection = !1;
  }
  /**
   * Sanitizer Config
   *
   * @returns {SanitizerConfig}
   */
  get sanitizerConfig() {
    return {
      p: {},
      h1: {},
      h2: {},
      h3: {},
      h4: {},
      h5: {},
      h6: {},
      ol: {},
      ul: {},
      li: {},
      br: !0,
      img: {
        src: !0,
        width: !0,
        height: !0
      },
      a: {
        href: !0
      },
      b: {},
      i: {},
      u: {}
    };
  }
  /**
   * Flag that identifies all Blocks selection
   *
   * @returns {boolean}
   */
  get allBlocksSelected() {
    const { BlockManager: e } = this.Editor;
    return e.blocks.every((t) => t.selected === !0);
  }
  /**
   * Set selected all blocks
   *
   * @param {boolean} state - state to set
   */
  set allBlocksSelected(e) {
    const { BlockManager: t } = this.Editor;
    t.blocks.forEach((n) => {
      n.selected = e;
    }), this.clearCache();
  }
  /**
   * Flag that identifies any Block selection
   *
   * @returns {boolean}
   */
  get anyBlockSelected() {
    const { BlockManager: e } = this.Editor;
    return this.anyBlockSelectedCache === null && (this.anyBlockSelectedCache = e.blocks.some((t) => t.selected === !0)), this.anyBlockSelectedCache;
  }
  /**
   * Return selected Blocks array
   *
   * @returns {Block[]}
   */
  get selectedBlocks() {
    return this.Editor.BlockManager.blocks.filter((e) => e.selected);
  }
  /**
   * Module Preparation
   * Registers Shortcuts CMD+A and CMD+C
   * to select all and copy them
   */
  prepare() {
    this.selection = new Fe(), gs.add({
      name: "CMD+A",
      handler: (e) => {
        const { BlockManager: t, ReadOnly: n } = this.Editor;
        if (n.isEnabled) {
          e.preventDefault(), this.selectAllBlocks();
          return;
        }
        t.currentBlock && this.handleCommandA(e);
      },
      on: this.Editor.UI.nodes.redactor
    });
  }
  /**
   * Toggle read-only state
   *
   *  - Remove all ranges
   *  - Unselect all Blocks
   */
  toggleReadOnly() {
    Fe.get().removeAllRanges(), this.allBlocksSelected = !1;
  }
  /**
   * Remove selection of Block
   *
   * @param {number?} index - Block index according to the BlockManager's indexes
   */
  unSelectBlockByIndex(e) {
    const { BlockManager: t } = this.Editor;
    let n;
    isNaN(e) ? n = t.currentBlock : n = t.getBlockByIndex(e), n.selected = !1, this.clearCache();
  }
  /**
   * Clear selection from Blocks
   *
   * @param {Event} reason - event caused clear of selection
   * @param {boolean} restoreSelection - if true, restore saved selection
   */
  clearSelection(e, t = !1) {
    const { BlockManager: n, Caret: r, RectangleSelection: a } = this.Editor;
    this.needToSelectAll = !1, this.nativeInputSelected = !1, this.readyToBlockSelection = !1;
    const c = e && e instanceof KeyboardEvent, u = c && xh(e.keyCode);
    if (this.anyBlockSelected && c && u && !Fe.isSelectionExists) {
      const p = n.removeSelectedBlocks();
      n.insertDefaultBlockAtIndex(p, !0), r.setToBlock(n.currentBlock), Ra(() => {
        const g = e.key;
        r.insertContentAtCaretPosition(g.length > 1 ? "" : g);
      }, 20)();
    }
    if (this.Editor.CrossBlockSelection.clear(e), !this.anyBlockSelected || a.isRectActivated()) {
      this.Editor.RectangleSelection.clearSelection();
      return;
    }
    t && this.selection.restore(), this.allBlocksSelected = !1;
  }
  /**
   * Reduce each Block and copy its content
   *
   * @param {ClipboardEvent} e - copy/cut event
   * @returns {Promise<void>}
   */
  copySelectedBlocks(e) {
    e.preventDefault();
    const t = ae.make("div");
    this.selectedBlocks.forEach((a) => {
      const c = fi(a.holder.innerHTML, this.sanitizerConfig), u = ae.make("p");
      u.innerHTML = c, t.appendChild(u);
    });
    const n = Array.from(t.childNodes).map((a) => a.textContent).join(`

`), r = t.innerHTML;
    return e.clipboardData.setData("text/plain", n), e.clipboardData.setData("text/html", r), Promise.all(this.selectedBlocks.map((a) => a.save())).then((a) => {
      try {
        e.clipboardData.setData(this.Editor.Paste.MIME_TYPE, JSON.stringify(a));
      } catch {
      }
    });
  }
  /**
   * Select Block by its index
   *
   * @param {number?} index - Block index according to the BlockManager's indexes
   */
  selectBlockByIndex(e) {
    const { BlockManager: t } = this.Editor, n = t.getBlockByIndex(e);
    n !== void 0 && this.selectBlock(n);
  }
  /**
   * Select passed Block
   *
   * @param {Block} block - Block to select
   */
  selectBlock(e) {
    this.selection.save(), Fe.get().removeAllRanges(), e.selected = !0, this.clearCache(), this.Editor.InlineToolbar.close();
  }
  /**
   * Remove selection from passed Block
   *
   * @param {Block} block - Block to unselect
   */
  unselectBlock(e) {
    e.selected = !1, this.clearCache();
  }
  /**
   * Clear anyBlockSelected cache
   */
  clearCache() {
    this.anyBlockSelectedCache = null;
  }
  /**
   * Module destruction
   * De-registers Shortcut CMD+A
   */
  destroy() {
    gs.remove(this.Editor.UI.nodes.redactor, "CMD+A");
  }
  /**
   * First CMD+A selects all input content by native behaviour,
   * next CMD+A keypress selects all blocks
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  handleCommandA(e) {
    if (this.Editor.RectangleSelection.clearSelection(), ae.isNativeInput(e.target) && !this.readyToBlockSelection) {
      this.readyToBlockSelection = !0;
      return;
    }
    const t = this.Editor.BlockManager.getBlock(e.target), n = t.inputs;
    if (n.length > 1 && !this.readyToBlockSelection) {
      this.readyToBlockSelection = !0;
      return;
    }
    if (n.length === 1 && !this.needToSelectAll) {
      this.needToSelectAll = !0;
      return;
    }
    this.needToSelectAll ? (e.preventDefault(), this.selectAllBlocks(), this.needToSelectAll = !1, this.readyToBlockSelection = !1, this.Editor.ConversionToolbar.close()) : this.readyToBlockSelection && (e.preventDefault(), this.selectBlock(t), this.needToSelectAll = !0);
  }
  /**
   * Select All Blocks
   * Each Block has selected setter that makes Block copyable
   */
  selectAllBlocks() {
    this.selection.save(), Fe.get().removeAllRanges(), this.allBlocksSelected = !0, this.Editor.InlineToolbar.close();
  }
}, _m = class Wl extends rt {
  /**
   * Allowed caret positions in input
   *
   * @static
   * @returns {{START: string, END: string, DEFAULT: string}}
   */
  get positions() {
    return {
      START: "start",
      END: "end",
      DEFAULT: "default"
    };
  }
  /**
   * Elements styles that can be useful for Caret Module
   */
  static get CSS() {
    return {
      shadowCaret: "cdx-shadow-caret"
    };
  }
  /**
   * Get's deepest first node and checks if offset is zero
   *
   * @returns {boolean}
   */
  get isAtStart() {
    const { currentBlock: e } = this.Editor.BlockManager;
    if (!e.focusable)
      return !0;
    const t = Fe.get(), n = ae.getDeepestNode(e.currentInput);
    let r = t.focusNode;
    if (ae.isNativeInput(n))
      return (n == null ? void 0 : n.selectionEnd) === 0;
    if (!t.anchorNode)
      return !1;
    let a = r.textContent.search(/\S/);
    a === -1 && (a = 0);
    let c = t.focusOffset;
    return r.nodeType !== Node.TEXT_NODE && r.childNodes.length && (r.childNodes[c] ? (r = r.childNodes[c], c = 0) : (r = r.childNodes[c - 1], c = r.textContent.length)), (ae.isLineBreakTag(n) || ae.isEmpty(n)) && this.getHigherLevelSiblings(r, "left").every((u) => {
      const p = ae.isLineBreakTag(u), g = u.children.length === 1 && ae.isLineBreakTag(u.children[0]), v = p || g;
      return ae.isEmpty(u) && !v;
    }) && c === a ? !0 : n === null || r === n && c <= a;
  }
  /**
   * Get's deepest last node and checks if offset is last node text length
   *
   * @returns {boolean}
   */
  get isAtEnd() {
    const { currentBlock: e } = this.Editor.BlockManager;
    if (!e.focusable)
      return !0;
    const t = Fe.get();
    let n = t.focusNode;
    const r = ae.getDeepestNode(e.currentInput, !0);
    if (ae.isNativeInput(r))
      return (r == null ? void 0 : r.selectionEnd) === r.value.length;
    if (!t.focusNode)
      return !1;
    let a = t.focusOffset;
    if (n.nodeType !== Node.TEXT_NODE && n.childNodes.length && (n.childNodes[a - 1] ? (n = n.childNodes[a - 1], a = n.textContent.length) : (n = n.childNodes[0], a = 0)), ae.isLineBreakTag(r) || ae.isEmpty(r)) {
      const u = this.getHigherLevelSiblings(n, "right");
      if (u.every((p, g) => g === u.length - 1 && ae.isLineBreakTag(p) || ae.isEmpty(p) && !ae.isLineBreakTag(p)) && a === n.textContent.length)
        return !0;
    }
    const c = r.textContent.replace(/\s+$/, "");
    return n === r && a >= c.length;
  }
  /**
   * Method gets Block instance and puts caret to the text node with offset
   * There two ways that method applies caret position:
   *   - first found text node: sets at the beginning, but you can pass an offset
   *   - last found text node: sets at the end of the node. Also, you can customize the behaviour
   *
   * @param {Block} block - Block class
   * @param {string} position - position where to set caret.
   *                            If default - leave default behaviour and apply offset if it's passed
   * @param {number} offset - caret offset regarding to the text node
   */
  setToBlock(e, t = this.positions.DEFAULT, n = 0) {
    var r;
    const { BlockManager: a, BlockSelection: c } = this.Editor;
    if (c.clearSelection(), !e.focusable) {
      (r = window.getSelection()) == null || r.removeAllRanges(), c.selectBlock(e), a.currentBlock = e;
      return;
    }
    let u;
    switch (t) {
      case this.positions.START:
        u = e.firstInput;
        break;
      case this.positions.END:
        u = e.lastInput;
        break;
      default:
        u = e.currentInput;
    }
    if (!u)
      return;
    const p = ae.getDeepestNode(u, t === this.positions.END), g = ae.getContentLength(p);
    switch (!0) {
      case t === this.positions.START:
        n = 0;
        break;
      case t === this.positions.END:
      case n > g:
        n = g;
        break;
    }
    this.set(p, n), a.setCurrentBlockByChildNode(e.holder), a.currentBlock.currentInput = u;
  }
  /**
   * Set caret to the current input of current Block.
   *
   * @param {HTMLElement} input - input where caret should be set
   * @param {string} position - position of the caret.
   *                            If default - leave default behaviour and apply offset if it's passed
   * @param {number} offset - caret offset regarding to the text node
   */
  setToInput(e, t = this.positions.DEFAULT, n = 0) {
    const { currentBlock: r } = this.Editor.BlockManager, a = ae.getDeepestNode(e);
    switch (t) {
      case this.positions.START:
        this.set(a, 0);
        break;
      case this.positions.END:
        this.set(a, ae.getContentLength(a));
        break;
      default:
        n && this.set(a, n);
    }
    r.currentInput = e;
  }
  /**
   * Creates Document Range and sets caret to the element with offset
   *
   * @param {HTMLElement} element - target node.
   * @param {number} offset - offset
   */
  set(e, t = 0) {
    const { top: n, bottom: r } = Fe.setCursor(e, t), { innerHeight: a } = window;
    n < 0 ? window.scrollBy(0, n - 30) : r > a && window.scrollBy(0, r - a + 30);
  }
  /**
   * Set Caret to the last Block
   * If last block is not empty, append another empty block
   */
  setToTheLastBlock() {
    const e = this.Editor.BlockManager.lastBlock;
    if (e)
      if (e.tool.isDefault && e.isEmpty)
        this.setToBlock(e);
      else {
        const t = this.Editor.BlockManager.insertAtEnd();
        this.setToBlock(t);
      }
  }
  /**
   * Extract content fragment of current Block from Caret position to the end of the Block
   */
  extractFragmentFromCaretPosition() {
    const e = Fe.get();
    if (e.rangeCount) {
      const t = e.getRangeAt(0), n = this.Editor.BlockManager.currentBlock.currentInput;
      if (t.deleteContents(), n)
        if (ae.isNativeInput(n)) {
          const r = n, a = document.createDocumentFragment(), c = r.value.substring(0, r.selectionStart), u = r.value.substring(r.selectionStart);
          return a.textContent = u, r.value = c, a;
        } else {
          const r = t.cloneRange();
          return r.selectNodeContents(n), r.setStart(t.endContainer, t.endOffset), r.extractContents();
        }
    }
  }
  /**
   * Set's caret to the next Block or Tool`s input
   * Before moving caret, we should check if caret position is at the end of Plugins node
   * Using {@link Dom#getDeepestNode} to get a last node and match with current selection
   *
   * @param {boolean} force - pass true to skip check for caret position
   */
  navigateNext(e = !1) {
    const { BlockManager: t } = this.Editor, { currentBlock: n, nextBlock: r } = t, { nextInput: a } = n, c = this.isAtEnd;
    let u = r;
    const p = e || c;
    if (a && p)
      return this.setToInput(a, this.positions.START), !0;
    if (u === null) {
      if (n.tool.isDefault || !p)
        return !1;
      u = t.insertAtEnd();
    }
    return p ? (this.setToBlock(u, this.positions.START), !0) : !1;
  }
  /**
   * Set's caret to the previous Tool`s input or Block
   * Before moving caret, we should check if caret position is start of the Plugins node
   * Using {@link Dom#getDeepestNode} to get a last node and match with current selection
   *
   * @param {boolean} force - pass true to skip check for caret position
   */
  navigatePrevious(e = !1) {
    const { currentBlock: t, previousBlock: n } = this.Editor.BlockManager;
    if (!t)
      return !1;
    const { previousInput: r } = t, a = e || this.isAtStart;
    return r && a ? (this.setToInput(r, this.positions.END), !0) : n !== null && a ? (this.setToBlock(n, this.positions.END), !0) : !1;
  }
  /**
   * Inserts shadow element after passed element where caret can be placed
   *
   * @param {Element} element - element after which shadow caret should be inserted
   */
  createShadow(e) {
    const t = document.createElement("span");
    t.classList.add(Wl.CSS.shadowCaret), e.insertAdjacentElement("beforeend", t);
  }
  /**
   * Restores caret position
   *
   * @param {HTMLElement} element - element where caret should be restored
   */
  restoreCaret(e) {
    const t = e.querySelector(`.${Wl.CSS.shadowCaret}`);
    if (!t)
      return;
    new Fe().expandToTag(t);
    const n = document.createRange();
    n.selectNode(t), n.extractContents();
  }
  /**
   * Inserts passed content at caret position
   *
   * @param {string} content - content to insert
   */
  insertContentAtCaretPosition(e) {
    const t = document.createDocumentFragment(), n = document.createElement("div"), r = Fe.get(), a = Fe.range;
    n.innerHTML = e, Array.from(n.childNodes).forEach((g) => t.appendChild(g)), t.childNodes.length === 0 && t.appendChild(new Text());
    const c = t.lastChild;
    a.deleteContents(), a.insertNode(t);
    const u = document.createRange(), p = c.nodeType === Node.TEXT_NODE ? c : c.firstChild;
    p !== null && p.textContent !== null && u.setStart(p, p.textContent.length), r.removeAllRanges(), r.addRange(u);
  }
  /**
   * Get all first-level (first child of [contenteditable]) siblings from passed node
   * Then you can check it for emptiness
   *
   * @example
   * <div contenteditable>
   * <p></p>                            |
   * <p></p>                            | left first-level siblings
   * <p></p>                            |
   * <blockquote><a><b>adaddad</b><a><blockquote>       <-- passed node for example <b>
   * <p></p>                            |
   * <p></p>                            | right first-level siblings
   * <p></p>                            |
   * </div>
   * @param {HTMLElement} from - element from which siblings should be searched
   * @param {'left' | 'right'} direction - direction of search
   * @returns {HTMLElement[]}
   */
  getHigherLevelSiblings(e, t) {
    let n = e;
    const r = [];
    for (; n.parentNode && n.parentNode.contentEditable !== "true"; )
      n = n.parentNode;
    const a = t === "left" ? "previousSibling" : "nextSibling";
    for (; n[a]; )
      n = n[a], r.push(n);
    return r;
  }
};
class Em extends rt {
  constructor() {
    super(...arguments), this.onMouseUp = () => {
      this.listeners.off(document, "mouseover", this.onMouseOver), this.listeners.off(document, "mouseup", this.onMouseUp);
    }, this.onMouseOver = (e) => {
      const { BlockManager: t, BlockSelection: n } = this.Editor;
      if (e.relatedTarget === null && e.target === null)
        return;
      const r = t.getBlockByChildNode(e.relatedTarget) || this.lastSelectedBlock, a = t.getBlockByChildNode(e.target);
      if (!(!r || !a) && a !== r) {
        if (r === this.firstSelectedBlock) {
          Fe.get().removeAllRanges(), r.selected = !0, a.selected = !0, n.clearCache();
          return;
        }
        if (a === this.firstSelectedBlock) {
          r.selected = !1, a.selected = !1, n.clearCache();
          return;
        }
        this.Editor.InlineToolbar.close(), this.toggleBlocksSelectedState(r, a), this.lastSelectedBlock = a;
      }
    };
  }
  /**
   * Module preparation
   *
   * @returns {Promise}
   */
  async prepare() {
    this.listeners.on(document, "mousedown", (e) => {
      this.enableCrossBlockSelection(e);
    });
  }
  /**
   * Sets up listeners
   *
   * @param {MouseEvent} event - mouse down event
   */
  watchSelection(e) {
    if (e.button !== tf.LEFT)
      return;
    const { BlockManager: t } = this.Editor;
    this.firstSelectedBlock = t.getBlock(e.target), this.lastSelectedBlock = this.firstSelectedBlock, this.listeners.on(document, "mouseover", this.onMouseOver), this.listeners.on(document, "mouseup", this.onMouseUp);
  }
  /**
   * return boolean is cross block selection started
   */
  get isCrossBlockSelectionStarted() {
    return !!this.firstSelectedBlock && !!this.lastSelectedBlock;
  }
  /**
   * Change selection state of the next Block
   * Used for CBS via Shift + arrow keys
   *
   * @param {boolean} next - if true, toggle next block. Previous otherwise
   */
  toggleBlockSelectedState(e = !0) {
    const { BlockManager: t, BlockSelection: n } = this.Editor;
    this.lastSelectedBlock || (this.lastSelectedBlock = this.firstSelectedBlock = t.currentBlock), this.firstSelectedBlock === this.lastSelectedBlock && (this.firstSelectedBlock.selected = !0, n.clearCache(), Fe.get().removeAllRanges());
    const r = t.blocks.indexOf(this.lastSelectedBlock) + (e ? 1 : -1), a = t.blocks[r];
    a && (this.lastSelectedBlock.selected !== a.selected ? (a.selected = !0, n.clearCache()) : (this.lastSelectedBlock.selected = !1, n.clearCache()), this.lastSelectedBlock = a, this.Editor.InlineToolbar.close(), a.holder.scrollIntoView({
      block: "nearest"
    }));
  }
  /**
   * Clear saved state
   *
   * @param {Event} reason - event caused clear of selection
   */
  clear(e) {
    const { BlockManager: t, BlockSelection: n, Caret: r } = this.Editor, a = t.blocks.indexOf(this.firstSelectedBlock), c = t.blocks.indexOf(this.lastSelectedBlock);
    if (n.anyBlockSelected && a > -1 && c > -1 && e && e instanceof KeyboardEvent)
      switch (e.keyCode) {
        case ze.DOWN:
        case ze.RIGHT:
          r.setToBlock(t.blocks[Math.max(a, c)], r.positions.END);
          break;
        case ze.UP:
        case ze.LEFT:
          r.setToBlock(t.blocks[Math.min(a, c)], r.positions.START);
          break;
        default:
          r.setToBlock(t.blocks[Math.max(a, c)], r.positions.END);
      }
    this.firstSelectedBlock = this.lastSelectedBlock = null;
  }
  /**
   * Enables Cross Block Selection
   *
   * @param {MouseEvent} event - mouse down event
   */
  enableCrossBlockSelection(e) {
    const { UI: t } = this.Editor;
    Fe.isCollapsed || this.Editor.BlockSelection.clearSelection(e), t.nodes.redactor.contains(e.target) ? this.watchSelection(e) : this.Editor.BlockSelection.clearSelection(e);
  }
  /**
   * Change blocks selection state between passed two blocks.
   *
   * @param {Block} firstBlock - first block in range
   * @param {Block} lastBlock - last block in range
   */
  toggleBlocksSelectedState(e, t) {
    const { BlockManager: n, BlockSelection: r } = this.Editor, a = n.blocks.indexOf(e), c = n.blocks.indexOf(t), u = e.selected !== t.selected;
    for (let p = Math.min(a, c); p <= Math.max(a, c); p++) {
      const g = n.blocks[p];
      g !== this.firstSelectedBlock && g !== (u ? e : t) && (n.blocks[p].selected = !n.blocks[p].selected, r.clearCache());
    }
  }
}
class Mm extends rt {
  constructor() {
    super(...arguments), this.isStartedAtEditor = !1;
  }
  /**
   * Toggle read-only state
   *
   * if state is true:
   *  - disable all drag-n-drop event handlers
   *
   * if state is false:
   *  - restore drag-n-drop event handlers
   *
   * @param {boolean} readOnlyEnabled - "read only" state
   */
  toggleReadOnly(e) {
    e ? this.disableModuleBindings() : this.enableModuleBindings();
  }
  /**
   * Add drag events listeners to editor zone
   */
  enableModuleBindings() {
    const { UI: e } = this.Editor;
    this.readOnlyMutableListeners.on(e.nodes.holder, "drop", async (t) => {
      await this.processDrop(t);
    }, !0), this.readOnlyMutableListeners.on(e.nodes.holder, "dragstart", () => {
      this.processDragStart();
    }), this.readOnlyMutableListeners.on(e.nodes.holder, "dragover", (t) => {
      this.processDragOver(t);
    }, !0);
  }
  /**
   * Unbind drag-n-drop event handlers
   */
  disableModuleBindings() {
    this.readOnlyMutableListeners.clearAll();
  }
  /**
   * Handle drop event
   *
   * @param {DragEvent} dropEvent - drop event
   */
  async processDrop(e) {
    const {
      BlockManager: t,
      Caret: n,
      Paste: r
    } = this.Editor;
    e.preventDefault(), t.blocks.forEach((c) => {
      c.dropTarget = !1;
    }), Fe.isAtEditor && !Fe.isCollapsed && this.isStartedAtEditor && document.execCommand("delete"), this.isStartedAtEditor = !1;
    const a = t.setCurrentBlockByChildNode(e.target);
    if (a)
      this.Editor.Caret.setToBlock(a, n.positions.END);
    else {
      const c = t.setCurrentBlockByChildNode(t.lastBlock.holder);
      this.Editor.Caret.setToBlock(c, n.positions.END);
    }
    await r.processDataTransfer(e.dataTransfer, !0);
  }
  /**
   * Handle drag start event
   */
  processDragStart() {
    Fe.isAtEditor && !Fe.isCollapsed && (this.isStartedAtEditor = !0), this.Editor.InlineToolbar.close();
  }
  /**
   * @param {DragEvent} dragEvent - drag event
   */
  processDragOver(e) {
    e.preventDefault();
  }
}
class Dm extends rt {
  /**
   * Prepare the module
   *
   * @param options - options used by the modification observer module
   * @param options.config - Editor configuration object
   * @param options.eventsDispatcher - common Editor event bus
   */
  constructor({ config: e, eventsDispatcher: t }) {
    super({
      config: e,
      eventsDispatcher: t
    }), this.disabled = !1, this.batchingTimeout = null, this.batchingOnChangeQueue = /* @__PURE__ */ new Map(), this.batchTime = 400, this.mutationObserver = new MutationObserver((n) => {
      this.redactorChanged(n);
    }), this.eventsDispatcher.on(Dh, (n) => {
      this.particularBlockChanged(n.event);
    }), this.eventsDispatcher.on(Lh, () => {
      this.disable();
    }), this.eventsDispatcher.on(Oh, () => {
      this.enable();
    });
  }
  /**
   * Enables onChange event
   */
  enable() {
    this.mutationObserver.observe(
      this.Editor.UI.nodes.redactor,
      {
        childList: !0,
        subtree: !0,
        characterData: !0,
        attributes: !0
      }
    ), this.disabled = !1;
  }
  /**
   * Disables onChange event
   */
  disable() {
    this.mutationObserver.disconnect(), this.disabled = !0;
  }
  /**
   * Call onChange event passed to Editor.js configuration
   *
   * @param event - some of our custom change events
   */
  particularBlockChanged(e) {
    this.disabled || !Dt(this.config.onChange) || (this.batchingOnChangeQueue.set(`block:${e.detail.target.id}:event:${e.type}`, e), this.batchingTimeout && clearTimeout(this.batchingTimeout), this.batchingTimeout = setTimeout(() => {
      let t;
      this.batchingOnChangeQueue.size === 1 ? t = this.batchingOnChangeQueue.values().next().value : t = Array.from(this.batchingOnChangeQueue.values()), this.config.onChange && this.config.onChange(this.Editor.API.methods, t), this.batchingOnChangeQueue.clear();
    }, this.batchTime));
  }
  /**
   * Fired on every blocks wrapper dom change
   *
   * @param mutations - mutations happened
   */
  redactorChanged(e) {
    this.eventsDispatcher.emit(Yl, {
      mutations: e
    });
  }
}
const Gh = class Zh extends rt {
  constructor() {
    super(...arguments), this.MIME_TYPE = "application/x-editor-js", this.toolsTags = {}, this.tagsByTool = {}, this.toolsPatterns = [], this.toolsFiles = {}, this.exceptionList = [], this.processTool = (e) => {
      try {
        const t = e.create({}, {}, !1);
        if (e.pasteConfig === !1) {
          this.exceptionList.push(e.name);
          return;
        }
        if (!Dt(t.onPaste))
          return;
        this.getTagsConfig(e), this.getFilesConfig(e), this.getPatternsConfig(e);
      } catch (t) {
        bt(
          `Paste handling for «${e.name}» Tool hasn't been set up because of the error`,
          "warn",
          t
        );
      }
    }, this.handlePasteEvent = async (e) => {
      const { BlockManager: t, Toolbar: n } = this.Editor, r = t.setCurrentBlockByChildNode(e.target);
      !r || this.isNativeBehaviour(e.target) && !e.clipboardData.types.includes("Files") || r && this.exceptionList.includes(r.name) || (e.preventDefault(), this.processDataTransfer(e.clipboardData), n.close());
    };
  }
  /**
   * Set onPaste callback and collect tools` paste configurations
   */
  async prepare() {
    this.processTools();
  }
  /**
   * Set read-only state
   *
   * @param {boolean} readOnlyEnabled - read only flag value
   */
  toggleReadOnly(e) {
    e ? this.unsetCallback() : this.setCallback();
  }
  /**
   * Handle pasted or dropped data transfer object
   *
   * @param {DataTransfer} dataTransfer - pasted or dropped data transfer object
   * @param {boolean} isDragNDrop - true if data transfer comes from drag'n'drop events
   */
  async processDataTransfer(e, t = !1) {
    const { Tools: n } = this.Editor, r = e.types;
    if ((r.includes ? r.includes("Files") : r.contains("Files")) && !sn(this.toolsFiles)) {
      await this.processFiles(e.files);
      return;
    }
    const a = e.getData(this.MIME_TYPE), c = e.getData("text/plain");
    let u = e.getData("text/html");
    if (a)
      try {
        this.insertEditorJSData(JSON.parse(a));
        return;
      } catch {
      }
    t && c.trim() && u.trim() && (u = "<p>" + (u.trim() ? u : c) + "</p>");
    const p = Object.keys(this.toolsTags).reduce((k, y) => (k[y.toLowerCase()] = this.toolsTags[y].sanitizationConfig ?? {}, k), {}), g = Object.assign({}, p, n.getAllInlineToolsSanitizeConfig(), { br: {} }), v = fi(u, g);
    !v.trim() || v.trim() === c || !ae.isHTMLString(v) ? await this.processText(c) : await this.processText(v, !0);
  }
  /**
   * Process pasted text and divide them into Blocks
   *
   * @param {string} data - text to process. Can be HTML or plain.
   * @param {boolean} isHTML - if passed string is HTML, this parameter should be true
   */
  async processText(e, t = !1) {
    const { Caret: n, BlockManager: r } = this.Editor, a = t ? this.processHTML(e) : this.processPlain(e);
    if (!a.length)
      return;
    if (a.length === 1) {
      a[0].isBlock ? this.processSingleBlock(a.pop()) : this.processInlinePaste(a.pop());
      return;
    }
    const c = r.currentBlock && r.currentBlock.tool.isDefault && r.currentBlock.isEmpty;
    a.map(
      async (u, p) => this.insertBlock(u, p === 0 && c)
    ), r.currentBlock && n.setToBlock(r.currentBlock, n.positions.END);
  }
  /**
   * Set onPaste callback handler
   */
  setCallback() {
    this.listeners.on(this.Editor.UI.nodes.holder, "paste", this.handlePasteEvent);
  }
  /**
   * Unset onPaste callback handler
   */
  unsetCallback() {
    this.listeners.off(this.Editor.UI.nodes.holder, "paste", this.handlePasteEvent);
  }
  /**
   * Get and process tool`s paste configs
   */
  processTools() {
    const e = this.Editor.Tools.blockTools;
    Array.from(e.values()).forEach(this.processTool);
  }
  /**
   * Get tags name list from either tag name or sanitization config.
   *
   * @param {string | object} tagOrSanitizeConfig - tag name or sanitize config object.
   * @returns {string[]} array of tags.
   */
  collectTagNames(e) {
    return Qi(e) ? [e] : ho(e) ? Object.keys(e) : [];
  }
  /**
   * Get tags to substitute by Tool
   *
   * @param tool - BlockTool object
   */
  getTagsConfig(e) {
    if (e.pasteConfig === !1)
      return;
    const t = e.pasteConfig.tags || [], n = [];
    t.forEach((r) => {
      const a = this.collectTagNames(r);
      n.push(...a), a.forEach((c) => {
        if (Object.prototype.hasOwnProperty.call(this.toolsTags, c)) {
          bt(
            `Paste handler for «${e.name}» Tool on «${c}» tag is skipped because it is already used by «${this.toolsTags[c].tool.name}» Tool.`,
            "warn"
          );
          return;
        }
        const u = ho(r) ? r[c] : null;
        this.toolsTags[c.toUpperCase()] = {
          tool: e,
          sanitizationConfig: u
        };
      });
    }), this.tagsByTool[e.name] = n.map((r) => r.toUpperCase());
  }
  /**
   * Get files` types and extensions to substitute by Tool
   *
   * @param tool - BlockTool object
   */
  getFilesConfig(e) {
    if (e.pasteConfig === !1)
      return;
    const { files: t = {} } = e.pasteConfig;
    let { extensions: n, mimeTypes: r } = t;
    !n && !r || (n && !Array.isArray(n) && (bt(`«extensions» property of the onDrop config for «${e.name}» Tool should be an array`), n = []), r && !Array.isArray(r) && (bt(`«mimeTypes» property of the onDrop config for «${e.name}» Tool should be an array`), r = []), r && (r = r.filter((a) => af(a) ? !0 : (bt(`MIME type value «${a}» for the «${e.name}» Tool is not a valid MIME type`, "warn"), !1))), this.toolsFiles[e.name] = {
      extensions: n || [],
      mimeTypes: r || []
    });
  }
  /**
   * Get RegExp patterns to substitute by Tool
   *
   * @param tool - BlockTool object
   */
  getPatternsConfig(e) {
    e.pasteConfig === !1 || !e.pasteConfig.patterns || sn(e.pasteConfig.patterns) || Object.entries(e.pasteConfig.patterns).forEach(([t, n]) => {
      n instanceof RegExp || bt(
        `Pattern ${n} for «${e.name}» Tool is skipped because it should be a Regexp instance.`,
        "warn"
      ), this.toolsPatterns.push({
        key: t,
        pattern: n,
        tool: e
      });
    });
  }
  /**
   * Check if browser behavior suits better
   *
   * @param {EventTarget} element - element where content has been pasted
   * @returns {boolean}
   */
  isNativeBehaviour(e) {
    return ae.isNativeInput(e);
  }
  /**
   * Get files from data transfer object and insert related Tools
   *
   * @param {FileList} items - pasted or dropped items
   */
  async processFiles(e) {
    const { BlockManager: t } = this.Editor;
    let n;
    n = await Promise.all(
      Array.from(e).map((a) => this.processFile(a))
    ), n = n.filter((a) => !!a);
    const r = t.currentBlock.tool.isDefault && t.currentBlock.isEmpty;
    n.forEach(
      (a, c) => {
        t.paste(a.type, a.event, c === 0 && r);
      }
    );
  }
  /**
   * Get information about file and find Tool to handle it
   *
   * @param {File} file - file to process
   */
  async processFile(e) {
    const t = sf(e), n = Object.entries(this.toolsFiles).find(([a, { mimeTypes: c, extensions: u }]) => {
      const [p, g] = e.type.split("/"), v = u.find((y) => y.toLowerCase() === t.toLowerCase()), k = c.find((y) => {
        const [T, x] = y.split("/");
        return T === p && (x === g || x === "*");
      });
      return !!v || !!k;
    });
    if (!n)
      return;
    const [r] = n;
    return {
      event: this.composePasteEvent("file", {
        file: e
      }),
      type: r
    };
  }
  /**
   * Split HTML string to blocks and return it as array of Block data
   *
   * @param {string} innerHTML - html string to process
   * @returns {PasteData[]}
   */
  processHTML(e) {
    const { Tools: t } = this.Editor, n = ae.make("DIV");
    return n.innerHTML = e, this.getNodes(n).map((r) => {
      let a, c = t.defaultTool, u = !1;
      switch (r.nodeType) {
        case Node.DOCUMENT_FRAGMENT_NODE:
          a = ae.make("div"), a.appendChild(r);
          break;
        case Node.ELEMENT_NODE:
          a = r, u = !0, this.toolsTags[a.tagName] && (c = this.toolsTags[a.tagName].tool);
          break;
      }
      const { tags: p } = c.pasteConfig || { tags: [] }, g = p.reduce((y, T) => (this.collectTagNames(T).forEach((x) => {
        const M = ho(T) ? T[x] : null;
        y[x.toLowerCase()] = M || {};
      }), y), {}), v = Object.assign({}, g, c.baseSanitizeConfig);
      if (a.tagName.toLowerCase() === "table") {
        const y = fi(a.outerHTML, v);
        a = ae.make("div", void 0, {
          innerHTML: y
        }).firstChild;
      } else
        a.innerHTML = fi(a.innerHTML, v);
      const k = this.composePasteEvent("tag", {
        data: a
      });
      return {
        content: a,
        isBlock: u,
        tool: c.name,
        event: k
      };
    }).filter((r) => {
      const a = ae.isEmpty(r.content), c = ae.isSingleTag(r.content);
      return !a || c;
    });
  }
  /**
   * Split plain text by new line symbols and return it as array of Block data
   *
   * @param {string} plain - string to process
   * @returns {PasteData[]}
   */
  processPlain(e) {
    const { defaultBlock: t } = this.config;
    if (!e)
      return [];
    const n = t;
    return e.split(/\r?\n/).filter((r) => r.trim()).map((r) => {
      const a = ae.make("div");
      a.textContent = r;
      const c = this.composePasteEvent("tag", {
        data: a
      });
      return {
        content: a,
        tool: n,
        isBlock: !1,
        event: c
      };
    });
  }
  /**
   * Process paste of single Block tool content
   *
   * @param {PasteData} dataToInsert - data of Block to insert
   */
  async processSingleBlock(e) {
    const { Caret: t, BlockManager: n } = this.Editor, { currentBlock: r } = n;
    if (!r || e.tool !== r.name || !ae.containsOnlyInlineElements(e.content.innerHTML)) {
      this.insertBlock(e, (r == null ? void 0 : r.tool.isDefault) && r.isEmpty);
      return;
    }
    t.insertContentAtCaretPosition(e.content.innerHTML);
  }
  /**
   * Process paste to single Block:
   * 1. Find patterns` matches
   * 2. Insert new block if it is not the same type as current one
   * 3. Just insert text if there is no substitutions
   *
   * @param {PasteData} dataToInsert - data of Block to insert
   */
  async processInlinePaste(e) {
    const { BlockManager: t, Caret: n } = this.Editor, { content: r } = e;
    if (t.currentBlock && t.currentBlock.tool.isDefault && r.textContent.length < Zh.PATTERN_PROCESSING_MAX_LENGTH) {
      const a = await this.processPattern(r.textContent);
      if (a) {
        const c = t.currentBlock && t.currentBlock.tool.isDefault && t.currentBlock.isEmpty, u = t.paste(a.tool, a.event, c);
        n.setToBlock(u, n.positions.END);
        return;
      }
    }
    if (t.currentBlock && t.currentBlock.currentInput) {
      const a = t.currentBlock.tool.baseSanitizeConfig;
      document.execCommand(
        "insertHTML",
        !1,
        fi(r.innerHTML, a)
      );
    } else
      this.insertBlock(e);
  }
  /**
   * Get patterns` matches
   *
   * @param {string} text - text to process
   * @returns {Promise<{event: PasteEvent, tool: string}>}
   */
  async processPattern(e) {
    const t = this.toolsPatterns.find((n) => {
      const r = n.pattern.exec(e);
      return r ? e === r.shift() : !1;
    });
    return t ? {
      event: this.composePasteEvent("pattern", {
        key: t.key,
        data: e
      }),
      tool: t.tool.name
    } : void 0;
  }
  /**
   * Insert pasted Block content to Editor
   *
   * @param {PasteData} data - data to insert
   * @param {boolean} canReplaceCurrentBlock - if true and is current Block is empty, will replace current Block
   * @returns {void}
   */
  insertBlock(e, t = !1) {
    const { BlockManager: n, Caret: r } = this.Editor, { currentBlock: a } = n;
    let c;
    if (t && a && a.isEmpty) {
      c = n.paste(e.tool, e.event, !0), r.setToBlock(c, r.positions.END);
      return;
    }
    c = n.paste(e.tool, e.event), r.setToBlock(c, r.positions.END);
  }
  /**
   * Insert data passed as application/x-editor-js JSON
   *
   * @param {Array} blocks — Blocks' data to insert
   * @returns {void}
   */
  insertEditorJSData(e) {
    const { BlockManager: t, Caret: n, Tools: r } = this.Editor;
    Rh(
      e,
      (a) => r.blockTools.get(a).sanitizeConfig
    ).forEach(({ tool: a, data: c }, u) => {
      let p = !1;
      u === 0 && (p = t.currentBlock && t.currentBlock.tool.isDefault && t.currentBlock.isEmpty);
      const g = t.insert({
        tool: a,
        data: c,
        replace: p
      });
      n.setToBlock(g, n.positions.END);
    });
  }
  /**
   * Fetch nodes from Element node
   *
   * @param {Node} node - current node
   * @param {Node[]} nodes - processed nodes
   * @param {Node} destNode - destination node
   */
  processElementNode(e, t, n) {
    const r = Object.keys(this.toolsTags), a = e, { tool: c } = this.toolsTags[a.tagName] || {}, u = this.tagsByTool[c == null ? void 0 : c.name] || [], p = r.includes(a.tagName), g = ae.blockElements.includes(a.tagName.toLowerCase()), v = Array.from(a.children).some(
      ({ tagName: y }) => r.includes(y) && !u.includes(y)
    ), k = Array.from(a.children).some(
      ({ tagName: y }) => ae.blockElements.includes(y.toLowerCase())
    );
    if (!g && !p && !v)
      return n.appendChild(a), [...t, n];
    if (p && !v || g && !k && !v)
      return [...t, n, a];
  }
  /**
   * Recursively divide HTML string to two types of nodes:
   * 1. Block element
   * 2. Document Fragments contained text and markup tags like a, b, i etc.
   *
   * @param {Node} wrapper - wrapper of paster HTML content
   * @returns {Node[]}
   */
  getNodes(e) {
    const t = Array.from(e.childNodes);
    let n;
    const r = (a, c) => {
      if (ae.isEmpty(c) && !ae.isSingleTag(c))
        return a;
      const u = a[a.length - 1];
      let p = new DocumentFragment();
      switch (u && ae.isFragment(u) && (p = a.pop()), c.nodeType) {
        case Node.ELEMENT_NODE:
          if (n = this.processElementNode(c, a, p), n)
            return n;
          break;
        case Node.TEXT_NODE:
          return p.appendChild(c), [...a, p];
        default:
          return [...a, p];
      }
      return [...a, ...Array.from(c.childNodes).reduce(r, [])];
    };
    return t.reduce(r, []);
  }
  /**
   * Compose paste event with passed type and detail
   *
   * @param {string} type - event type
   * @param {PasteEventDetail} detail - event detail
   */
  composePasteEvent(e, t) {
    return new CustomEvent(e, {
      detail: t
    });
  }
};
Gh.PATTERN_PROCESSING_MAX_LENGTH = 450;
let Lm = Gh;
class Om extends rt {
  constructor() {
    super(...arguments), this.toolsDontSupportReadOnly = [], this.readOnlyEnabled = !1;
  }
  /**
   * Returns state of read only mode
   */
  get isEnabled() {
    return this.readOnlyEnabled;
  }
  /**
   * Set initial state
   */
  async prepare() {
    const { Tools: e } = this.Editor, { blockTools: t } = e, n = [];
    Array.from(t.entries()).forEach(([r, a]) => {
      a.isReadOnlySupported || n.push(r);
    }), this.toolsDontSupportReadOnly = n, this.config.readOnly && n.length > 0 && this.throwCriticalError(), this.toggle(this.config.readOnly);
  }
  /**
   * Set read-only mode or toggle current state
   * Call all Modules `toggleReadOnly` method and re-render Editor
   *
   * @param {boolean} state - (optional) read-only state or toggle
   */
  async toggle(e = !this.readOnlyEnabled) {
    e && this.toolsDontSupportReadOnly.length > 0 && this.throwCriticalError();
    const t = this.readOnlyEnabled;
    this.readOnlyEnabled = e;
    for (const r in this.Editor)
      this.Editor[r].toggleReadOnly && this.Editor[r].toggleReadOnly(e);
    if (t === e)
      return this.readOnlyEnabled;
    const n = await this.Editor.Saver.save();
    return await this.Editor.BlockManager.clear(), await this.Editor.Renderer.render(n.blocks), this.readOnlyEnabled;
  }
  /**
   * Throws an error about tools which don't support read-only mode
   */
  throwCriticalError() {
    throw new Eh(
      `To enable read-only mode all connected tools should support it. Tools ${this.toolsDontSupportReadOnly.join(", ")} don't support read-only mode.`
    );
  }
}
class Js extends rt {
  constructor() {
    super(...arguments), this.isRectSelectionActivated = !1, this.SCROLL_SPEED = 3, this.HEIGHT_OF_SCROLL_ZONE = 40, this.BOTTOM_SCROLL_ZONE = 1, this.TOP_SCROLL_ZONE = 2, this.MAIN_MOUSE_BUTTON = 0, this.mousedown = !1, this.isScrolling = !1, this.inScrollZone = null, this.startX = 0, this.startY = 0, this.mouseX = 0, this.mouseY = 0, this.stackOfSelected = [], this.listenerIds = [];
  }
  /**
   * CSS classes for the Block
   *
   * @returns {{wrapper: string, content: string}}
   */
  static get CSS() {
    return {
      overlay: "codex-editor-overlay",
      overlayContainer: "codex-editor-overlay__container",
      rect: "codex-editor-overlay__rectangle",
      topScrollZone: "codex-editor-overlay__scroll-zone--top",
      bottomScrollZone: "codex-editor-overlay__scroll-zone--bottom"
    };
  }
  /**
   * Module Preparation
   * Creating rect and hang handlers
   */
  prepare() {
    this.enableModuleBindings();
  }
  /**
   * Init rect params
   *
   * @param {number} pageX - X coord of mouse
   * @param {number} pageY - Y coord of mouse
   */
  startSelection(e, t) {
    const n = document.elementFromPoint(e - window.pageXOffset, t - window.pageYOffset);
    n.closest(`.${this.Editor.Toolbar.CSS.toolbar}`) || (this.Editor.BlockSelection.allBlocksSelected = !1, this.clearSelection(), this.stackOfSelected = []);
    const r = [
      `.${pi.CSS.content}`,
      `.${this.Editor.Toolbar.CSS.toolbar}`,
      `.${this.Editor.InlineToolbar.CSS.inlineToolbar}`
    ], a = n.closest("." + this.Editor.UI.CSS.editorWrapper), c = r.some((u) => !!n.closest(u));
    !a || c || (this.mousedown = !0, this.startX = e, this.startY = t);
  }
  /**
   * Clear all params to end selection
   */
  endSelection() {
    this.mousedown = !1, this.startX = 0, this.startY = 0, this.overlayRectangle.style.display = "none";
  }
  /**
   * is RectSelection Activated
   */
  isRectActivated() {
    return this.isRectSelectionActivated;
  }
  /**
   * Mark that selection is end
   */
  clearSelection() {
    this.isRectSelectionActivated = !1;
  }
  /**
   * Sets Module necessary event handlers
   */
  enableModuleBindings() {
    const { container: e } = this.genHTML();
    this.listeners.on(e, "mousedown", (t) => {
      this.processMouseDown(t);
    }, !1), this.listeners.on(document.body, "mousemove", Hl((t) => {
      this.processMouseMove(t);
    }, 10), {
      passive: !0
    }), this.listeners.on(document.body, "mouseleave", () => {
      this.processMouseLeave();
    }), this.listeners.on(window, "scroll", Hl((t) => {
      this.processScroll(t);
    }, 10), {
      passive: !0
    }), this.listeners.on(document.body, "mouseup", () => {
      this.processMouseUp();
    }, !1);
  }
  /**
   * Handle mouse down events
   *
   * @param {MouseEvent} mouseEvent - mouse event payload
   */
  processMouseDown(e) {
    e.button === this.MAIN_MOUSE_BUTTON && (e.target.closest(ae.allInputsSelector) !== null || this.startSelection(e.pageX, e.pageY));
  }
  /**
   * Handle mouse move events
   *
   * @param {MouseEvent} mouseEvent - mouse event payload
   */
  processMouseMove(e) {
    this.changingRectangle(e), this.scrollByZones(e.clientY);
  }
  /**
   * Handle mouse leave
   */
  processMouseLeave() {
    this.clearSelection(), this.endSelection();
  }
  /**
   * @param {MouseEvent} mouseEvent - mouse event payload
   */
  processScroll(e) {
    this.changingRectangle(e);
  }
  /**
   * Handle mouse up
   */
  processMouseUp() {
    this.clearSelection(), this.endSelection();
  }
  /**
   * Scroll If mouse in scroll zone
   *
   * @param {number} clientY - Y coord of mouse
   */
  scrollByZones(e) {
    if (this.inScrollZone = null, e <= this.HEIGHT_OF_SCROLL_ZONE && (this.inScrollZone = this.TOP_SCROLL_ZONE), document.documentElement.clientHeight - e <= this.HEIGHT_OF_SCROLL_ZONE && (this.inScrollZone = this.BOTTOM_SCROLL_ZONE), !this.inScrollZone) {
      this.isScrolling = !1;
      return;
    }
    this.isScrolling || (this.scrollVertical(this.inScrollZone === this.TOP_SCROLL_ZONE ? -this.SCROLL_SPEED : this.SCROLL_SPEED), this.isScrolling = !0);
  }
  /**
   * Generates required HTML elements
   *
   * @returns {Object<string, Element>}
   */
  genHTML() {
    const { UI: e } = this.Editor, t = e.nodes.holder.querySelector("." + e.CSS.editorWrapper), n = ae.make("div", Js.CSS.overlay, {}), r = ae.make("div", Js.CSS.overlayContainer, {}), a = ae.make("div", Js.CSS.rect, {});
    return r.appendChild(a), n.appendChild(r), t.appendChild(n), this.overlayRectangle = a, {
      container: t,
      overlay: n
    };
  }
  /**
   * Activates scrolling if blockSelection is active and mouse is in scroll zone
   *
   * @param {number} speed - speed of scrolling
   */
  scrollVertical(e) {
    if (!(this.inScrollZone && this.mousedown))
      return;
    const t = window.pageYOffset;
    window.scrollBy(0, e), this.mouseY += window.pageYOffset - t, setTimeout(() => {
      this.scrollVertical(e);
    }, 0);
  }
  /**
   * Handles the change in the rectangle and its effect
   *
   * @param {MouseEvent} event - mouse event
   */
  changingRectangle(e) {
    if (!this.mousedown)
      return;
    e.pageY !== void 0 && (this.mouseX = e.pageX, this.mouseY = e.pageY);
    const { rightPos: t, leftPos: n, index: r } = this.genInfoForMouseSelection(), a = this.startX > t && this.mouseX > t, c = this.startX < n && this.mouseX < n;
    this.rectCrossesBlocks = !(a || c), this.isRectSelectionActivated || (this.rectCrossesBlocks = !1, this.isRectSelectionActivated = !0, this.shrinkRectangleToPoint(), this.overlayRectangle.style.display = "block"), this.updateRectangleSize(), this.Editor.Toolbar.close(), r !== void 0 && (this.trySelectNextBlock(r), this.inverseSelection(), Fe.get().removeAllRanges());
  }
  /**
   * Shrink rect to singular point
   */
  shrinkRectangleToPoint() {
    this.overlayRectangle.style.left = `${this.startX - window.pageXOffset}px`, this.overlayRectangle.style.top = `${this.startY - window.pageYOffset}px`, this.overlayRectangle.style.bottom = `calc(100% - ${this.startY - window.pageYOffset}px`, this.overlayRectangle.style.right = `calc(100% - ${this.startX - window.pageXOffset}px`;
  }
  /**
   * Select or unselect all of blocks in array if rect is out or in selectable area
   */
  inverseSelection() {
    const e = this.Editor.BlockManager.getBlockByIndex(this.stackOfSelected[0]).selected;
    if (this.rectCrossesBlocks && !e)
      for (const t of this.stackOfSelected)
        this.Editor.BlockSelection.selectBlockByIndex(t);
    if (!this.rectCrossesBlocks && e)
      for (const t of this.stackOfSelected)
        this.Editor.BlockSelection.unSelectBlockByIndex(t);
  }
  /**
   * Updates size of rectangle
   */
  updateRectangleSize() {
    this.mouseY >= this.startY ? (this.overlayRectangle.style.top = `${this.startY - window.pageYOffset}px`, this.overlayRectangle.style.bottom = `calc(100% - ${this.mouseY - window.pageYOffset}px`) : (this.overlayRectangle.style.bottom = `calc(100% - ${this.startY - window.pageYOffset}px`, this.overlayRectangle.style.top = `${this.mouseY - window.pageYOffset}px`), this.mouseX >= this.startX ? (this.overlayRectangle.style.left = `${this.startX - window.pageXOffset}px`, this.overlayRectangle.style.right = `calc(100% - ${this.mouseX - window.pageXOffset}px`) : (this.overlayRectangle.style.right = `calc(100% - ${this.startX - window.pageXOffset}px`, this.overlayRectangle.style.left = `${this.mouseX - window.pageXOffset}px`);
  }
  /**
   * Collects information needed to determine the behavior of the rectangle
   *
   * @returns {object} index - index next Block, leftPos - start of left border of Block, rightPos - right border
   */
  genInfoForMouseSelection() {
    const e = document.body.offsetWidth / 2, t = this.mouseY - window.pageYOffset, n = document.elementFromPoint(e, t), r = this.Editor.BlockManager.getBlockByChildNode(n);
    let a;
    r !== void 0 && (a = this.Editor.BlockManager.blocks.findIndex((v) => v.holder === r.holder));
    const c = this.Editor.BlockManager.lastBlock.holder.querySelector("." + pi.CSS.content), u = Number.parseInt(window.getComputedStyle(c).width, 10) / 2, p = e - u, g = e + u;
    return {
      index: a,
      leftPos: p,
      rightPos: g
    };
  }
  /**
   * Select block with index index
   *
   * @param index - index of block in redactor
   */
  addBlockInSelection(e) {
    this.rectCrossesBlocks && this.Editor.BlockSelection.selectBlockByIndex(e), this.stackOfSelected.push(e);
  }
  /**
   * Adds a block to the selection and determines which blocks should be selected
   *
   * @param {object} index - index of new block in the reactor
   */
  trySelectNextBlock(e) {
    const t = this.stackOfSelected[this.stackOfSelected.length - 1] === e, n = this.stackOfSelected.length, r = 1, a = -1, c = 0;
    if (t)
      return;
    const u = this.stackOfSelected[n - 1] - this.stackOfSelected[n - 2] > 0;
    let p = c;
    n > 1 && (p = u ? r : a);
    const g = e > this.stackOfSelected[n - 1] && p === r, v = e < this.stackOfSelected[n - 1] && p === a, k = !(g || v || p === c);
    if (!k && (e > this.stackOfSelected[n - 1] || this.stackOfSelected[n - 1] === void 0)) {
      let x = this.stackOfSelected[n - 1] + 1 || e;
      for (x; x <= e; x++)
        this.addBlockInSelection(x);
      return;
    }
    if (!k && e < this.stackOfSelected[n - 1]) {
      for (let x = this.stackOfSelected[n - 1] - 1; x >= e; x--)
        this.addBlockInSelection(x);
      return;
    }
    if (!k)
      return;
    let y = n - 1, T;
    for (e > this.stackOfSelected[n - 1] ? T = () => e > this.stackOfSelected[y] : T = () => e < this.stackOfSelected[y]; T(); )
      this.rectCrossesBlocks && this.Editor.BlockSelection.unSelectBlockByIndex(this.stackOfSelected[y]), this.stackOfSelected.pop(), y--;
  }
}
class Bm extends rt {
  /**
   * Renders passed blocks as one batch
   *
   * @param blocksData - blocks to render
   */
  async render(e) {
    return new Promise((t) => {
      const { Tools: n, BlockManager: r } = this.Editor;
      if (e.length === 0)
        r.insert();
      else {
        const a = e.map(({ type: c, data: u, tunes: p, id: g }) => {
          n.available.has(c) === !1 && (Zo(`Tool «${c}» is not found. Check 'tools' property at the Editor.js config.`, "warn"), u = this.composeStubDataForTool(c, u, g), c = n.stubTool);
          let v;
          try {
            v = r.composeBlock({
              id: g,
              tool: c,
              data: u,
              tunes: p
            });
          } catch (k) {
            bt(`Block «${c}» skipped because of plugins error`, "error", {
              data: u,
              error: k
            }), u = this.composeStubDataForTool(c, u, g), c = n.stubTool, v = r.composeBlock({
              id: g,
              tool: c,
              data: u,
              tunes: p
            });
          }
          return v;
        });
        r.insertMany(a);
      }
      window.requestIdleCallback(() => {
        t();
      }, { timeout: 2e3 });
    });
  }
  /**
   * Create data for the Stub Tool that will be used instead of unavailable tool
   *
   * @param tool - unavailable tool name to stub
   * @param data - data of unavailable block
   * @param [id] - id of unavailable block
   */
  composeStubDataForTool(e, t, n) {
    const { Tools: r } = this.Editor;
    let a = e;
    if (r.unavailable.has(e)) {
      const c = r.unavailable.get(e).toolbox;
      c !== void 0 && c[0].title !== void 0 && (a = c[0].title);
    }
    return {
      savedData: {
        id: n,
        type: e,
        data: t
      },
      title: a
    };
  }
}
class Am extends rt {
  /**
   * Composes new chain of Promises to fire them alternatelly
   *
   * @returns {OutputData}
   */
  async save() {
    const { BlockManager: e, Tools: t } = this.Editor, n = e.blocks, r = [];
    try {
      n.forEach((u) => {
        r.push(this.getSavedData(u));
      });
      const a = await Promise.all(r), c = await Rh(a, (u) => t.blockTools.get(u).sanitizeConfig);
      return this.makeOutput(c);
    } catch (a) {
      Zo("Saving failed due to the Error %o", "error", a);
    }
  }
  /**
   * Saves and validates
   *
   * @param {Block} block - Editor's Tool
   * @returns {ValidatedData} - Tool's validated data
   */
  async getSavedData(e) {
    const t = await e.save(), n = t && await e.validate(t.data);
    return {
      ...t,
      isValid: n
    };
  }
  /**
   * Creates output object with saved data, time and version of editor
   *
   * @param {ValidatedData} allExtractedData - data extracted from Blocks
   * @returns {OutputData}
   */
  makeOutput(e) {
    const t = [];
    return e.forEach(({ id: n, tool: r, data: a, tunes: c, isValid: u }) => {
      if (!u) {
        bt(`Block «${r}» skipped because saved data is invalid`);
        return;
      }
      if (r === this.Editor.Tools.stubTool) {
        t.push(a);
        return;
      }
      const p = {
        id: n,
        type: r,
        data: a,
        ...!sn(c) && {
          tunes: c
        }
      };
      t.push(p);
    }), {
      time: +/* @__PURE__ */ new Date(),
      blocks: t,
      version: "2.29.0"
    };
  }
}
(function() {
  try {
    if (typeof document < "u") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode(".ce-paragraph{line-height:1.6em;outline:none}.ce-paragraph[data-placeholder]:empty:before{content:attr(data-placeholder);color:#707684;font-weight:400;opacity:0}.codex-editor--empty .ce-block:first-child .ce-paragraph[data-placeholder]:empty:before{opacity:1}.codex-editor--toolbox-opened .ce-block:first-child .ce-paragraph[data-placeholder]:empty:before,.codex-editor--empty .ce-block:first-child .ce-paragraph[data-placeholder]:empty:focus:before{opacity:0}.ce-paragraph p:first-of-type{margin-top:0}.ce-paragraph p:last-of-type{margin-bottom:0}")), document.head.appendChild(o);
    }
  } catch (e) {
    console.error("vite-plugin-css-injected-by-js", e);
  }
})();
const Rm = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 9V7.2C8 7.08954 8.08954 7 8.2 7L12 7M16 9V7.2C16 7.08954 15.9105 7 15.8 7L12 7M12 7L12 17M12 17H10M12 17H14"/></svg>';
/**
 * Base Paragraph Block for the Editor.js.
 * Represents a regular text block
 *
 * @author CodeX (team@codex.so)
 * @copyright CodeX 2018
 * @license The MIT License (MIT)
 */
class fc {
  /**
   * Default placeholder for Paragraph Tool
   *
   * @returns {string}
   * @class
   */
  static get DEFAULT_PLACEHOLDER() {
    return "";
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {object} params - constructor params
   * @param {ParagraphData} params.data - previously saved data
   * @param {ParagraphConfig} params.config - user config for Tool
   * @param {object} params.api - editor.js api
   * @param {boolean} readOnly - read only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r }) {
    this.api = n, this.readOnly = r, this._CSS = {
      block: this.api.styles.block,
      wrapper: "ce-paragraph"
    }, this.readOnly || (this.onKeyUp = this.onKeyUp.bind(this)), this._placeholder = t.placeholder ? t.placeholder : fc.DEFAULT_PLACEHOLDER, this._data = {}, this._element = null, this._preserveBlank = t.preserveBlank !== void 0 ? t.preserveBlank : !1, this.data = e;
  }
  /**
   * Check if text content is empty and set empty string to inner html.
   * We need this because some browsers (e.g. Safari) insert <br> into empty contenteditanle elements
   *
   * @param {KeyboardEvent} e - key up event
   */
  onKeyUp(e) {
    if (e.code !== "Backspace" && e.code !== "Delete")
      return;
    const { textContent: t } = this._element;
    t === "" && (this._element.innerHTML = "");
  }
  /**
   * Create Tool's view
   *
   * @returns {HTMLElement}
   * @private
   */
  drawView() {
    const e = document.createElement("DIV");
    return e.classList.add(this._CSS.wrapper, this._CSS.block), e.contentEditable = !1, e.dataset.placeholder = this.api.i18n.t(this._placeholder), this._data.text && (e.innerHTML = this._data.text), this.readOnly || (e.contentEditable = !0, e.addEventListener("keyup", this.onKeyUp)), e;
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLDivElement}
   */
  render() {
    return this._element = this.drawView(), this._element;
  }
  /**
   * Method that specified how to merge two Text blocks.
   * Called by Editor.js by backspace at the beginning of the Block
   *
   * @param {ParagraphData} data
   * @public
   */
  merge(e) {
    const t = {
      text: this.data.text + e.text
    };
    this.data = t;
  }
  /**
   * Validate Paragraph block data:
   * - check for emptiness
   *
   * @param {ParagraphData} savedData — data received after saving
   * @returns {boolean} false if saved data is not correct, otherwise true
   * @public
   */
  validate(e) {
    return !(e.text.trim() === "" && !this._preserveBlank);
  }
  /**
   * Extract Tool's data from the view
   *
   * @param {HTMLDivElement} toolsContent - Paragraph tools rendered view
   * @returns {ParagraphData} - saved data
   * @public
   */
  save(e) {
    return {
      text: e.innerHTML
    };
  }
  /**
   * On paste callback fired from Editor.
   *
   * @param {PasteEvent} event - event with pasted data
   */
  onPaste(e) {
    const t = {
      text: e.detail.data.innerHTML
    };
    this.data = t;
  }
  /**
   * Enable Conversion Toolbar. Paragraph can be converted to/from other tools
   */
  static get conversionConfig() {
    return {
      export: "text",
      // to convert Paragraph to other block, use 'text' property of saved data
      import: "text"
      // to covert other block's exported string to Paragraph, fill 'text' property of tool data
    };
  }
  /**
   * Sanitizer rules
   */
  static get sanitize() {
    return {
      text: {
        br: !0
      }
    };
  }
  /**
   * Returns true to notify the core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Get current Tools`s data
   *
   * @returns {ParagraphData} Current data
   * @private
   */
  get data() {
    if (this._element !== null) {
      const e = this._element.innerHTML;
      this._data.text = e;
    }
    return this._data;
  }
  /**
   * Store data in plugin:
   * - at the this._data property
   * - at the HTML
   *
   * @param {ParagraphData} data — data to set
   * @private
   */
  set data(e) {
    this._data = e || {}, this._element !== null && this.hydrate();
  }
  /**
   * Fill tool's view with data
   */
  hydrate() {
    window.requestAnimationFrame(() => {
      this._element.innerHTML = this._data.text || "";
    });
  }
  /**
   * Used by Editor paste handling API.
   * Provides configuration to handle P tags.
   *
   * @returns {{tags: string[]}}
   */
  static get pasteConfig() {
    return {
      tags: ["P"]
    };
  }
  /**
   * Icon and title for displaying at the Toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: Rm,
      title: "Text"
    };
  }
}
let mc = class {
  constructor() {
    this.commandName = "bold", this.CSS = {
      button: "ce-inline-tool",
      buttonActive: "ce-inline-tool--active",
      buttonModifier: "ce-inline-tool--bold"
    }, this.nodes = {
      button: void 0
    };
  }
  /**
   * Sanitizer Rule
   * Leave <b> tags
   *
   * @returns {object}
   */
  static get sanitize() {
    return {
      b: {}
    };
  }
  /**
   * Create button for Inline Toolbar
   */
  render() {
    return this.nodes.button = document.createElement("button"), this.nodes.button.type = "button", this.nodes.button.classList.add(this.CSS.button, this.CSS.buttonModifier), this.nodes.button.innerHTML = Kf, this.nodes.button;
  }
  /**
   * Wrap range with <b> tag
   */
  surround() {
    document.execCommand(this.commandName);
  }
  /**
   * Check selection and set activated state to button if there are <b> tag
   *
   * @returns {boolean}
   */
  checkState() {
    const e = document.queryCommandState(this.commandName);
    return this.nodes.button.classList.toggle(this.CSS.buttonActive, e), e;
  }
  /**
   * Set a shortcut
   *
   * @returns {boolean}
   */
  get shortcut() {
    return "CMD+B";
  }
};
mc.isInline = !0;
mc.title = "Bold";
let gc = class {
  constructor() {
    this.commandName = "italic", this.CSS = {
      button: "ce-inline-tool",
      buttonActive: "ce-inline-tool--active",
      buttonModifier: "ce-inline-tool--italic"
    }, this.nodes = {
      button: null
    };
  }
  /**
   * Sanitizer Rule
   * Leave <i> tags
   *
   * @returns {object}
   */
  static get sanitize() {
    return {
      i: {}
    };
  }
  /**
   * Create button for Inline Toolbar
   */
  render() {
    return this.nodes.button = document.createElement("button"), this.nodes.button.type = "button", this.nodes.button.classList.add(this.CSS.button, this.CSS.buttonModifier), this.nodes.button.innerHTML = em, this.nodes.button;
  }
  /**
   * Wrap range with <i> tag
   */
  surround() {
    document.execCommand(this.commandName);
  }
  /**
   * Check selection and set activated state to button if there are <i> tag
   */
  checkState() {
    const e = document.queryCommandState(this.commandName);
    return this.nodes.button.classList.toggle(this.CSS.buttonActive, e), e;
  }
  /**
   * Set a shortcut
   */
  get shortcut() {
    return "CMD+I";
  }
};
gc.isInline = !0;
gc.title = "Italic";
let bc = class {
  /**
   * @param api - Editor.js API
   */
  constructor({ api: e }) {
    this.commandLink = "createLink", this.commandUnlink = "unlink", this.ENTER_KEY = 13, this.CSS = {
      button: "ce-inline-tool",
      buttonActive: "ce-inline-tool--active",
      buttonModifier: "ce-inline-tool--link",
      buttonUnlink: "ce-inline-tool--unlink",
      input: "ce-inline-tool-input",
      inputShowed: "ce-inline-tool-input--showed"
    }, this.nodes = {
      button: null,
      input: null
    }, this.inputOpened = !1, this.toolbar = e.toolbar, this.inlineToolbar = e.inlineToolbar, this.notifier = e.notifier, this.i18n = e.i18n, this.selection = new Fe();
  }
  /**
   * Sanitizer Rule
   * Leave <a> tags
   *
   * @returns {object}
   */
  static get sanitize() {
    return {
      a: {
        href: !0,
        target: "_blank",
        rel: "nofollow"
      }
    };
  }
  /**
   * Create button for Inline Toolbar
   */
  render() {
    return this.nodes.button = document.createElement("button"), this.nodes.button.type = "button", this.nodes.button.classList.add(this.CSS.button, this.CSS.buttonModifier), this.nodes.button.innerHTML = fd, this.nodes.button;
  }
  /**
   * Input for the link
   */
  renderActions() {
    return this.nodes.input = document.createElement("input"), this.nodes.input.placeholder = this.i18n.t("Add a link"), this.nodes.input.classList.add(this.CSS.input), this.nodes.input.addEventListener("keydown", (e) => {
      e.keyCode === this.ENTER_KEY && this.enterPressed(e);
    }), this.nodes.input;
  }
  /**
   * Handle clicks on the Inline Toolbar icon
   *
   * @param {Range} range - range to wrap with link
   */
  surround(e) {
    if (e) {
      this.inputOpened ? (this.selection.restore(), this.selection.removeFakeBackground()) : (this.selection.setFakeBackground(), this.selection.save());
      const t = this.selection.findParentTag("A");
      if (t) {
        this.selection.expandToTag(t), this.unlink(), this.closeActions(), this.checkState(), this.toolbar.close();
        return;
      }
    }
    this.toggleActions();
  }
  /**
   * Check selection and set activated state to button if there are <a> tag
   */
  checkState() {
    const e = this.selection.findParentTag("A");
    if (e) {
      this.nodes.button.innerHTML = im, this.nodes.button.classList.add(this.CSS.buttonUnlink), this.nodes.button.classList.add(this.CSS.buttonActive), this.openActions();
      const t = e.getAttribute("href");
      this.nodes.input.value = t !== "null" ? t : "", this.selection.save();
    } else
      this.nodes.button.innerHTML = fd, this.nodes.button.classList.remove(this.CSS.buttonUnlink), this.nodes.button.classList.remove(this.CSS.buttonActive);
    return !!e;
  }
  /**
   * Function called with Inline Toolbar closing
   */
  clear() {
    this.closeActions();
  }
  /**
   * Set a shortcut
   */
  get shortcut() {
    return "CMD+K";
  }
  /**
   * Show/close link input
   */
  toggleActions() {
    this.inputOpened ? this.closeActions(!1) : this.openActions(!0);
  }
  /**
   * @param {boolean} needFocus - on link creation we need to focus input. On editing - nope.
   */
  openActions(e = !1) {
    this.nodes.input.classList.add(this.CSS.inputShowed), e && this.nodes.input.focus(), this.inputOpened = !0;
  }
  /**
   * Close input
   *
   * @param {boolean} clearSavedSelection — we don't need to clear saved selection
   *                                        on toggle-clicks on the icon of opened Toolbar
   */
  closeActions(e = !0) {
    if (this.selection.isFakeBackgroundEnabled) {
      const t = new Fe();
      t.save(), this.selection.restore(), this.selection.removeFakeBackground(), t.restore();
    }
    this.nodes.input.classList.remove(this.CSS.inputShowed), this.nodes.input.value = "", e && this.selection.clearSaved(), this.inputOpened = !1;
  }
  /**
   * Enter pressed on input
   *
   * @param {KeyboardEvent} event - enter keydown event
   */
  enterPressed(e) {
    let t = this.nodes.input.value || "";
    if (!t.trim()) {
      this.selection.restore(), this.unlink(), e.preventDefault(), this.closeActions();
      return;
    }
    if (!this.validateURL(t)) {
      this.notifier.show({
        message: "Pasted link is not valid.",
        style: "error"
      }), bt("Incorrect Link pasted", "warn", t);
      return;
    }
    t = this.prepareLink(t), this.selection.restore(), this.selection.removeFakeBackground(), this.insertLink(t), e.preventDefault(), e.stopPropagation(), e.stopImmediatePropagation(), this.selection.collapseToEnd(), this.inlineToolbar.close();
  }
  /**
   * Detects if passed string is URL
   *
   * @param {string} str - string to validate
   * @returns {boolean}
   */
  validateURL(e) {
    return !/\s/.test(e);
  }
  /**
   * Process link before injection
   * - sanitize
   * - add protocol for links like 'google.com'
   *
   * @param {string} link - raw user input
   */
  prepareLink(e) {
    return e = e.trim(), e = this.addProtocol(e), e;
  }
  /**
   * Add 'http' protocol to the links like 'vc.ru', 'google.com'
   *
   * @param {string} link - string to process
   */
  addProtocol(e) {
    if (/^(\w+):(\/\/)?/.test(e))
      return e;
    const t = /^\/[^/\s]/.test(e), n = e.substring(0, 1) === "#", r = /^\/\/[^/\s]/.test(e);
    return !t && !n && !r && (e = "http://" + e), e;
  }
  /**
   * Inserts <a> tag with "href"
   *
   * @param {string} link - "href" value
   */
  insertLink(e) {
    const t = this.selection.findParentTag("A");
    t && this.selection.expandToTag(t), document.execCommand(this.commandLink, !1, e);
  }
  /**
   * Removes <a> tag
   */
  unlink() {
    document.execCommand(this.commandUnlink);
  }
};
bc.isInline = !0;
bc.title = "Link";
class Kh {
  /**
   * @param options - constructor options
   * @param options.data - stub tool data
   * @param options.api - Editor.js API
   */
  constructor({ data: e, api: t }) {
    this.CSS = {
      wrapper: "ce-stub",
      info: "ce-stub__info",
      title: "ce-stub__title",
      subtitle: "ce-stub__subtitle"
    }, this.api = t, this.title = e.title || this.api.i18n.t("Error"), this.subtitle = this.api.i18n.t("The block can not be displayed correctly."), this.savedData = e.savedData, this.wrapper = this.make();
  }
  /**
   * Returns stub holder
   *
   * @returns {HTMLElement}
   */
  render() {
    return this.wrapper;
  }
  /**
   * Return original Tool data
   *
   * @returns {BlockToolData}
   */
  save() {
    return this.savedData;
  }
  /**
   * Create Tool html markup
   *
   * @returns {HTMLElement}
   */
  make() {
    const e = ae.make("div", this.CSS.wrapper), t = rm, n = ae.make("div", this.CSS.info), r = ae.make("div", this.CSS.title, {
      textContent: this.title
    }), a = ae.make("div", this.CSS.subtitle, {
      textContent: this.subtitle
    });
    return e.innerHTML = t, n.appendChild(r), n.appendChild(a), e.appendChild(n), e;
  }
}
Kh.isReadOnlySupported = !0;
class Im extends pc {
  constructor() {
    super(...arguments), this.type = Ga.Inline;
  }
  /**
   * Returns title for Inline Tool if specified by user
   */
  get title() {
    return this.constructable[uc.Title];
  }
  /**
   * Constructs new InlineTool instance from constructable
   */
  create() {
    return new this.constructable({
      api: this.api.getMethodsForTool(this),
      config: this.settings
    });
  }
}
class Nm extends pc {
  constructor() {
    super(...arguments), this.type = Ga.Tune;
  }
  /**
   * Constructs new BlockTune instance from constructable
   *
   * @param data - Tune data
   * @param block - Block API object
   */
  create(e, t) {
    return new this.constructable({
      api: this.api.getMethodsForTool(this),
      config: this.settings,
      block: t,
      data: e
    });
  }
}
class fo extends Map {
  /**
   * Returns Block Tools collection
   */
  get blockTools() {
    const e = Array.from(this.entries()).filter(([, t]) => t.isBlock());
    return new fo(e);
  }
  /**
   * Returns Inline Tools collection
   */
  get inlineTools() {
    const e = Array.from(this.entries()).filter(([, t]) => t.isInline());
    return new fo(e);
  }
  /**
   * Returns Block Tunes collection
   */
  get blockTunes() {
    const e = Array.from(this.entries()).filter(([, t]) => t.isTune());
    return new fo(e);
  }
  /**
   * Returns internal Tools collection
   */
  get internalTools() {
    const e = Array.from(this.entries()).filter(([, t]) => t.isInternal);
    return new fo(e);
  }
  /**
   * Returns Tools collection provided by user
   */
  get externalTools() {
    const e = Array.from(this.entries()).filter(([, t]) => !t.isInternal);
    return new fo(e);
  }
}
var Pm = Object.defineProperty, Hm = Object.getOwnPropertyDescriptor, Xh = (o, e, t, n) => {
  for (var r = n > 1 ? void 0 : n ? Hm(e, t) : e, a = o.length - 1, c; a >= 0; a--)
    (c = o[a]) && (r = (n ? c(e, t, r) : c(r)) || r);
  return n && r && Pm(e, t, r), r;
};
class vc extends pc {
  constructor() {
    super(...arguments), this.type = Ga.Block, this.inlineTools = new fo(), this.tunes = new fo();
  }
  /**
   * Creates new Tool instance
   *
   * @param data - Tool data
   * @param block - BlockAPI for current Block
   * @param readOnly - True if Editor is in read-only mode
   */
  create(e, t, n) {
    return new this.constructable({
      data: e,
      block: t,
      readOnly: n,
      api: this.api.getMethodsForTool(this),
      config: this.settings
    });
  }
  /**
   * Returns true if read-only mode is supported by Tool
   */
  get isReadOnlySupported() {
    return this.constructable[Qr.IsReadOnlySupported] === !0;
  }
  /**
   * Returns true if Tool supports linebreaks
   */
  get isLineBreaksEnabled() {
    return this.constructable[Qr.IsEnabledLineBreaks];
  }
  /**
   * Returns Tool toolbox configuration (internal or user-specified).
   *
   * Merges internal and user-defined toolbox configs based on the following rules:
   *
   * - If both internal and user-defined toolbox configs are arrays their items are merged.
   * Length of the second one is kept.
   *
   * - If both are objects their properties are merged.
   *
   * - If one is an object and another is an array than internal config is replaced with user-defined
   * config. This is made to allow user to override default tool's toolbox representation (single/multiple entries)
   */
  get toolbox() {
    const e = this.constructable[Qr.Toolbox], t = this.config[Ea.Toolbox];
    if (!sn(e) && t !== !1)
      return t ? Array.isArray(e) ? Array.isArray(t) ? t.map((n, r) => {
        const a = e[r];
        return a ? {
          ...a,
          ...n
        } : n;
      }) : [t] : Array.isArray(t) ? t : [
        {
          ...e,
          ...t
        }
      ] : Array.isArray(e) ? e : [e];
  }
  /**
   * Returns Tool conversion configuration
   */
  get conversionConfig() {
    return this.constructable[Qr.ConversionConfig];
  }
  /**
   * Returns enabled inline tools for Tool
   */
  get enabledInlineTools() {
    return this.config[Ea.EnabledInlineTools] || !1;
  }
  /**
   * Returns enabled tunes for Tool
   */
  get enabledBlockTunes() {
    return this.config[Ea.EnabledBlockTunes];
  }
  /**
   * Returns Tool paste configuration
   */
  get pasteConfig() {
    return this.constructable[Qr.PasteConfig] ?? {};
  }
  get sanitizeConfig() {
    const e = super.sanitizeConfig, t = this.baseSanitizeConfig;
    if (sn(e))
      return t;
    const n = {};
    for (const r in e)
      if (Object.prototype.hasOwnProperty.call(e, r)) {
        const a = e[r];
        ho(a) ? n[r] = Object.assign({}, t, a) : n[r] = a;
      }
    return n;
  }
  get baseSanitizeConfig() {
    const e = {};
    return Array.from(this.inlineTools.values()).forEach((t) => Object.assign(e, t.sanitizeConfig)), Array.from(this.tunes.values()).forEach((t) => Object.assign(e, t.sanitizeConfig)), e;
  }
}
Xh([
  bs
], vc.prototype, "sanitizeConfig", 1);
Xh([
  bs
], vc.prototype, "baseSanitizeConfig", 1);
class jm {
  /**
   * @class
   * @param config - tools config
   * @param editorConfig - EditorJS config
   * @param api - EditorJS API module
   */
  constructor(e, t, n) {
    this.api = n, this.config = e, this.editorConfig = t;
  }
  /**
   * Returns Tool object based on it's type
   *
   * @param name - tool name
   */
  get(e) {
    const { class: t, isInternal: n = !1, ...r } = this.config[e], a = this.getConstructor(t);
    return new a({
      name: e,
      constructable: t,
      config: r,
      api: this.api,
      isDefault: e === this.editorConfig.defaultBlock,
      defaultPlaceholder: this.editorConfig.placeholder,
      isInternal: n
    });
  }
  /**
   * Find appropriate Tool object constructor for Tool constructable
   *
   * @param constructable - Tools constructable
   */
  getConstructor(e) {
    switch (!0) {
      case e[uc.IsInline]:
        return Im;
      case e[Vh.IsTune]:
        return Nm;
      default:
        return vc;
    }
  }
}
let Jh = class {
  /**
   * MoveDownTune constructor
   *
   * @param {API} api — Editor's API
   */
  constructor({ api: e }) {
    this.CSS = {
      animation: "wobble"
    }, this.api = e;
  }
  /**
   * Tune's appearance in block settings menu
   */
  render() {
    return {
      icon: Ph,
      title: this.api.i18n.t("Move down"),
      onActivate: () => this.handleClick(),
      name: "move-down"
    };
  }
  /**
   * Handle clicks on 'move down' button
   */
  handleClick() {
    const e = this.api.blocks.getCurrentBlockIndex(), t = this.api.blocks.getBlockByIndex(e + 1);
    if (!t)
      throw new Error("Unable to move Block down since it is already the last");
    const n = t.holder, r = n.getBoundingClientRect();
    let a = Math.abs(window.innerHeight - n.offsetHeight);
    r.top < window.innerHeight && (a = window.scrollY + n.offsetHeight), window.scrollTo(0, a), this.api.blocks.move(e + 1), this.api.toolbar.toggleBlockSettings(!0);
  }
};
Jh.isTune = !0;
class Qh {
  /**
   * DeleteTune constructor
   *
   * @param {API} api - Editor's API
   */
  constructor({ api: e }) {
    this.api = e;
  }
  /**
   * Tune's appearance in block settings menu
   */
  render() {
    return {
      icon: Jf,
      title: this.api.i18n.t("Delete"),
      name: "delete",
      confirmation: {
        title: this.api.i18n.t("Click to delete"),
        onActivate: () => this.handleClick()
      }
    };
  }
  /**
   * Delete block conditions passed
   */
  handleClick() {
    this.api.blocks.delete();
  }
}
Qh.isTune = !0;
class eu {
  /**
   * MoveUpTune constructor
   *
   * @param {API} api - Editor's API
   */
  constructor({ api: e }) {
    this.CSS = {
      animation: "wobble"
    }, this.api = e;
  }
  /**
   * Tune's appearance in block settings menu
   */
  render() {
    return {
      icon: Xf,
      title: this.api.i18n.t("Move up"),
      onActivate: () => this.handleClick(),
      name: "move-up"
    };
  }
  /**
   * Move current block up
   */
  handleClick() {
    const e = this.api.blocks.getCurrentBlockIndex(), t = this.api.blocks.getBlockByIndex(e), n = this.api.blocks.getBlockByIndex(e - 1);
    if (e === 0 || !t || !n)
      throw new Error("Unable to move Block up since it is already the first");
    const r = t.holder, a = n.holder, c = r.getBoundingClientRect(), u = a.getBoundingClientRect();
    let p;
    u.top > 0 ? p = Math.abs(c.top) - Math.abs(u.top) : p = Math.abs(c.top) + u.height, window.scrollBy(0, -1 * p), this.api.blocks.move(e - 1), this.api.toolbar.toggleBlockSettings(!0);
  }
}
eu.isTune = !0;
var $m = Object.defineProperty, Ym = Object.getOwnPropertyDescriptor, Fm = (o, e, t, n) => {
  for (var r = n > 1 ? void 0 : n ? Ym(e, t) : e, a = o.length - 1, c; a >= 0; a--)
    (c = o[a]) && (r = (n ? c(e, t, r) : c(r)) || r);
  return n && r && $m(e, t, r), r;
};
class tu extends rt {
  constructor() {
    super(...arguments), this.stubTool = "stub", this.toolsAvailable = new fo(), this.toolsUnavailable = new fo();
  }
  /**
   * Returns available Tools
   */
  get available() {
    return this.toolsAvailable;
  }
  /**
   * Returns unavailable Tools
   */
  get unavailable() {
    return this.toolsUnavailable;
  }
  /**
   * Return Tools for the Inline Toolbar
   */
  get inlineTools() {
    return this.available.inlineTools;
  }
  /**
   * Return editor block tools
   */
  get blockTools() {
    return this.available.blockTools;
  }
  /**
   * Return available Block Tunes
   *
   * @returns {object} - object of Inline Tool's classes
   */
  get blockTunes() {
    return this.available.blockTunes;
  }
  /**
   * Returns default Tool object
   */
  get defaultTool() {
    return this.blockTools.get(this.config.defaultBlock);
  }
  /**
   * Returns internal tools
   */
  get internal() {
    return this.available.internalTools;
  }
  /**
   * Creates instances via passed or default configuration
   *
   * @returns {Promise<void>}
   */
  async prepare() {
    if (this.validateTools(), this.config.tools = jl({}, this.internalTools, this.config.tools), !Object.prototype.hasOwnProperty.call(this.config, "tools") || Object.keys(this.config.tools).length === 0)
      throw Error("Can't start without tools");
    const e = this.prepareConfig();
    this.factory = new jm(e, this.config, this.Editor.API);
    const t = this.getListOfPrepareFunctions(e);
    if (t.length === 0)
      return Promise.resolve();
    await rf(t, (n) => {
      this.toolPrepareMethodSuccess(n);
    }, (n) => {
      this.toolPrepareMethodFallback(n);
    }), this.prepareBlockTools();
  }
  getAllInlineToolsSanitizeConfig() {
    const e = {};
    return Array.from(this.inlineTools.values()).forEach((t) => {
      Object.assign(e, t.sanitizeConfig);
    }), e;
  }
  /**
   * Calls each Tool reset method to clean up anything set by Tool
   */
  destroy() {
    Object.values(this.available).forEach(async (e) => {
      Dt(e.reset) && await e.reset();
    });
  }
  /**
   * Returns internal tools
   * Includes Bold, Italic, Link and Paragraph
   */
  get internalTools() {
    return {
      bold: {
        class: mc,
        isInternal: !0
      },
      italic: {
        class: gc,
        isInternal: !0
      },
      link: {
        class: bc,
        isInternal: !0
      },
      paragraph: {
        class: fc,
        inlineToolbar: !0,
        isInternal: !0
      },
      stub: {
        class: Kh,
        isInternal: !0
      },
      moveUp: {
        class: eu,
        isInternal: !0
      },
      delete: {
        class: Qh,
        isInternal: !0
      },
      moveDown: {
        class: Jh,
        isInternal: !0
      }
    };
  }
  /**
   * Tool prepare method success callback
   *
   * @param {object} data - append tool to available list
   */
  toolPrepareMethodSuccess(e) {
    const t = this.factory.get(e.toolName);
    if (t.isInline()) {
      const n = ["render", "surround", "checkState"].filter((r) => !t.create()[r]);
      if (n.length) {
        bt(
          `Incorrect Inline Tool: ${t.name}. Some of required methods is not implemented %o`,
          "warn",
          n
        ), this.toolsUnavailable.set(t.name, t);
        return;
      }
    }
    this.toolsAvailable.set(t.name, t);
  }
  /**
   * Tool prepare method fail callback
   *
   * @param {object} data - append tool to unavailable list
   */
  toolPrepareMethodFallback(e) {
    this.toolsUnavailable.set(e.toolName, this.factory.get(e.toolName));
  }
  /**
   * Binds prepare function of plugins with user or default config
   *
   * @returns {Array} list of functions that needs to be fired sequentially
   * @param config - tools config
   */
  getListOfPrepareFunctions(e) {
    const t = [];
    return Object.entries(e).forEach(([n, r]) => {
      t.push({
        // eslint-disable-next-line @typescript-eslint/no-empty-function
        function: Dt(r.class.prepare) ? r.class.prepare : () => {
        },
        data: {
          toolName: n,
          config: r.config
        }
      });
    }), t;
  }
  /**
   * Assign enabled Inline Tools and Block Tunes for Block Tool
   */
  prepareBlockTools() {
    Array.from(this.blockTools.values()).forEach((e) => {
      this.assignInlineToolsToBlockTool(e), this.assignBlockTunesToBlockTool(e);
    });
  }
  /**
   * Assign enabled Inline Tools for Block Tool
   *
   * @param tool - Block Tool
   */
  assignInlineToolsToBlockTool(e) {
    if (this.config.inlineToolbar !== !1) {
      if (e.enabledInlineTools === !0) {
        e.inlineTools = new fo(
          Array.isArray(this.config.inlineToolbar) ? this.config.inlineToolbar.map((t) => [t, this.inlineTools.get(t)]) : Array.from(this.inlineTools.entries())
        );
        return;
      }
      Array.isArray(e.enabledInlineTools) && (e.inlineTools = new fo(
        e.enabledInlineTools.map((t) => [t, this.inlineTools.get(t)])
      ));
    }
  }
  /**
   * Assign enabled Block Tunes for Block Tool
   *
   * @param tool — Block Tool
   */
  assignBlockTunesToBlockTool(e) {
    if (e.enabledBlockTunes !== !1) {
      if (Array.isArray(e.enabledBlockTunes)) {
        const t = new fo(
          e.enabledBlockTunes.map((n) => [n, this.blockTunes.get(n)])
        );
        e.tunes = new fo([...t, ...this.blockTunes.internalTools]);
        return;
      }
      if (Array.isArray(this.config.tunes)) {
        const t = new fo(
          this.config.tunes.map((n) => [n, this.blockTunes.get(n)])
        );
        e.tunes = new fo([...t, ...this.blockTunes.internalTools]);
        return;
      }
      e.tunes = this.blockTunes.internalTools;
    }
  }
  /**
   * Validate Tools configuration objects and throw Error for user if it is invalid
   */
  validateTools() {
    for (const e in this.config.tools)
      if (Object.prototype.hasOwnProperty.call(this.config.tools, e)) {
        if (e in this.internalTools)
          return;
        const t = this.config.tools[e];
        if (!Dt(t) && !Dt(t.class))
          throw Error(
            `Tool «${e}» must be a constructor function or an object with function in the «class» property`
          );
      }
  }
  /**
   * Unify tools config
   */
  prepareConfig() {
    const e = {};
    for (const t in this.config.tools)
      ho(this.config.tools[t]) ? e[t] = this.config.tools[t] : e[t] = { class: this.config.tools[t] };
    return e;
  }
}
Fm([
  bs
], tu.prototype, "getAllInlineToolsSanitizeConfig", 1);
const zm = `:root{--selectionColor: #e1f2ff;--inlineSelectionColor: #d4ecff;--bg-light: #eff2f5;--grayText: #707684;--color-dark: #1D202B;--color-active-icon: #388AE5;--color-gray-border: rgba(201, 201, 204, .48);--content-width: 650px;--narrow-mode-right-padding: 50px;--toolbox-buttons-size: 26px;--toolbox-buttons-size--mobile: 36px;--icon-size: 20px;--icon-size--mobile: 28px;--block-padding-vertical: .4em;--color-line-gray: #EFF0F1 }.codex-editor{position:relative;-webkit-box-sizing:border-box;box-sizing:border-box;z-index:1}.codex-editor .hide{display:none}.codex-editor__redactor [contenteditable]:empty:after{content:"\\feff"}@media (min-width: 651px){.codex-editor--narrow .codex-editor__redactor{margin-right:50px}}@media (min-width: 651px){.codex-editor--narrow.codex-editor--rtl .codex-editor__redactor{margin-left:50px;margin-right:0}}@media (min-width: 651px){.codex-editor--narrow .ce-toolbar__actions{right:-5px}}.codex-editor-copyable{position:absolute;height:1px;width:1px;top:-400%;opacity:.001}.codex-editor-overlay{position:fixed;top:0;left:0;right:0;bottom:0;z-index:999;pointer-events:none;overflow:hidden}.codex-editor-overlay__container{position:relative;pointer-events:auto;z-index:0}.codex-editor-overlay__rectangle{position:absolute;pointer-events:none;background-color:#2eaadc33;border:1px solid transparent}.codex-editor svg{max-height:100%}.codex-editor path{stroke:currentColor}.codex-editor ::-moz-selection{background-color:#d4ecff}.codex-editor ::selection{background-color:#d4ecff}.codex-editor--toolbox-opened [contentEditable=true][data-placeholder]:focus:before{opacity:0!important}.ce-scroll-locked{overflow:hidden}.ce-scroll-locked--hard{overflow:hidden;top:calc(-1 * var(--window-scroll-offset));position:fixed;width:100%}.ce-toolbar{position:absolute;left:0;right:0;top:0;-webkit-transition:opacity .1s ease;transition:opacity .1s ease;will-change:opacity,top;display:none}.ce-toolbar--opened{display:block}.ce-toolbar__content{max-width:650px;margin:0 auto;position:relative}.ce-toolbar__plus{color:#1d202b;cursor:pointer;width:26px;height:26px;border-radius:7px;display:-webkit-inline-box;display:-ms-inline-flexbox;display:inline-flex;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;-ms-flex-negative:0;flex-shrink:0}@media (max-width: 650px){.ce-toolbar__plus{width:36px;height:36px}}@media (hover: hover){.ce-toolbar__plus:hover{background-color:#eff2f5}}.ce-toolbar__plus--active{background-color:#eff2f5;-webkit-animation:bounceIn .75s 1;animation:bounceIn .75s 1;-webkit-animation-fill-mode:forwards;animation-fill-mode:forwards}.ce-toolbar__plus-shortcut{opacity:.6;word-spacing:-2px;margin-top:5px}@media (max-width: 650px){.ce-toolbar__plus{position:absolute;background-color:#fff;border:1px solid #E8E8EB;-webkit-box-shadow:0 3px 15px -3px rgba(13,20,33,.13);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;z-index:2;position:static}.ce-toolbar__plus--left-oriented:before{left:15px;margin-left:0}.ce-toolbar__plus--right-oriented:before{left:auto;right:15px;margin-left:0}}.ce-toolbar__actions{position:absolute;right:100%;opacity:0;display:-webkit-box;display:-ms-flexbox;display:flex;padding-right:5px}.ce-toolbar__actions--opened{opacity:1}@media (max-width: 650px){.ce-toolbar__actions{right:auto}}.ce-toolbar__settings-btn{color:#1d202b;width:26px;height:26px;border-radius:7px;display:-webkit-inline-box;display:-ms-inline-flexbox;display:inline-flex;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;margin-left:3px;cursor:pointer;user-select:none}@media (max-width: 650px){.ce-toolbar__settings-btn{width:36px;height:36px}}@media (hover: hover){.ce-toolbar__settings-btn:hover{background-color:#eff2f5}}.ce-toolbar__settings-btn--active{background-color:#eff2f5;-webkit-animation:bounceIn .75s 1;animation:bounceIn .75s 1;-webkit-animation-fill-mode:forwards;animation-fill-mode:forwards}@media (min-width: 651px){.ce-toolbar__settings-btn{width:24px}}.ce-toolbar__settings-btn--hidden{display:none}@media (max-width: 650px){.ce-toolbar__settings-btn{position:absolute;background-color:#fff;border:1px solid #E8E8EB;-webkit-box-shadow:0 3px 15px -3px rgba(13,20,33,.13);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;z-index:2;position:static}.ce-toolbar__settings-btn--left-oriented:before{left:15px;margin-left:0}.ce-toolbar__settings-btn--right-oriented:before{left:auto;right:15px;margin-left:0}}.ce-toolbar__plus svg,.ce-toolbar__settings-btn svg{width:24px;height:24px}@media (min-width: 651px){.codex-editor--narrow .ce-toolbar__plus{left:5px}}@media (min-width: 651px){.codex-editor--narrow .ce-toolbox .ce-popover{right:0;left:auto;left:initial}}.ce-inline-toolbar{--y-offset: 8px;position:absolute;background-color:#fff;border:1px solid #E8E8EB;-webkit-box-shadow:0 3px 15px -3px rgba(13,20,33,.13);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;z-index:2;opacity:0;visibility:hidden;-webkit-transition:opacity .25s ease;transition:opacity .25s ease;will-change:opacity,left,top;top:0;left:0;z-index:3}.ce-inline-toolbar--left-oriented:before{left:15px;margin-left:0}.ce-inline-toolbar--right-oriented:before{left:auto;right:15px;margin-left:0}.ce-inline-toolbar--showed{opacity:1;visibility:visible}.ce-inline-toolbar [hidden]{display:none!important}.ce-inline-toolbar__toggler-and-button-wrapper{display:-webkit-box;display:-ms-flexbox;display:flex;width:100%;padding:0 6px}.ce-inline-toolbar__buttons{display:-webkit-box;display:-ms-flexbox;display:flex}.ce-inline-toolbar__dropdown{display:-webkit-box;display:-ms-flexbox;display:flex;padding:6px;margin:0 6px 0 -6px;-webkit-box-align:center;-ms-flex-align:center;align-items:center;cursor:pointer;border-right:1px solid rgba(201,201,204,.48);-webkit-box-sizing:border-box;box-sizing:border-box}@media (hover: hover){.ce-inline-toolbar__dropdown:hover{background:#eff2f5}}.ce-inline-toolbar__dropdown--hidden{display:none}.ce-inline-toolbar__dropdown-content,.ce-inline-toolbar__dropdown-arrow{display:-webkit-box;display:-ms-flexbox;display:flex}.ce-inline-toolbar__dropdown-content svg,.ce-inline-toolbar__dropdown-arrow svg{width:20px;height:20px}.ce-inline-toolbar__shortcut{opacity:.6;word-spacing:-3px;margin-top:3px}.ce-inline-tool{display:-webkit-inline-box;display:-ms-inline-flexbox;display:inline-flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;padding:6px 1px;cursor:pointer;border:0;outline:none;background-color:transparent;vertical-align:bottom;color:inherit;margin:0;border-radius:0;line-height:normal}.ce-inline-tool svg{width:20px;height:20px}@media (max-width: 650px){.ce-inline-tool svg{width:28px;height:28px}}@media (hover: hover){.ce-inline-tool:hover{background-color:#eff2f5}}.ce-inline-tool--active{color:#388ae5}.ce-inline-tool--focused{background:rgba(34,186,255,.08)!important}.ce-inline-tool--focused{-webkit-box-shadow:inset 0 0 0px 1px rgba(7,161,227,.08);box-shadow:inset 0 0 0 1px #07a1e314}.ce-inline-tool--focused-animated{-webkit-animation-name:buttonClicked;animation-name:buttonClicked;-webkit-animation-duration:.25s;animation-duration:.25s}.ce-inline-tool--link .icon--unlink,.ce-inline-tool--unlink .icon--link{display:none}.ce-inline-tool--unlink .icon--unlink{display:inline-block;margin-bottom:-1px}.ce-inline-tool-input{outline:none;border:0;border-radius:0 0 4px 4px;margin:0;font-size:13px;padding:10px;width:100%;-webkit-box-sizing:border-box;box-sizing:border-box;display:none;font-weight:500;border-top:1px solid rgba(201,201,204,.48);-webkit-appearance:none;font-family:inherit}@media (max-width: 650px){.ce-inline-tool-input{font-size:15px;font-weight:500}}.ce-inline-tool-input::-webkit-input-placeholder{color:#707684}.ce-inline-tool-input::-moz-placeholder{color:#707684}.ce-inline-tool-input:-ms-input-placeholder{color:#707684}.ce-inline-tool-input::-ms-input-placeholder{color:#707684}.ce-inline-tool-input::placeholder{color:#707684}.ce-inline-tool-input--showed{display:block}.ce-conversion-toolbar{position:absolute;background-color:#fff;border:1px solid #E8E8EB;-webkit-box-shadow:0 3px 15px -3px rgba(13,20,33,.13);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;z-index:2;opacity:0;visibility:hidden;will-change:transform,opacity;-webkit-transition:opacity .1s ease,-webkit-transform .1s ease;transition:opacity .1s ease,-webkit-transform .1s ease;transition:transform .1s ease,opacity .1s ease;transition:transform .1s ease,opacity .1s ease,-webkit-transform .1s ease;-webkit-transform:translateY(-8px);transform:translateY(-8px);left:-1px;width:190px;margin-top:5px;-webkit-box-sizing:content-box;box-sizing:content-box}.ce-conversion-toolbar--left-oriented:before{left:15px;margin-left:0}.ce-conversion-toolbar--right-oriented:before{left:auto;right:15px;margin-left:0}.ce-conversion-toolbar--showed{opacity:1;visibility:visible;-webkit-transform:none;transform:none}.ce-conversion-toolbar [hidden]{display:none!important}.ce-conversion-toolbar__buttons{display:-webkit-box;display:-ms-flexbox;display:flex}.ce-conversion-toolbar__label{color:#707684;font-size:11px;font-weight:500;letter-spacing:.33px;padding:10px 10px 5px;text-transform:uppercase}.ce-conversion-tool{display:-webkit-box;display:-ms-flexbox;display:flex;padding:5px 10px;font-size:14px;line-height:20px;font-weight:500;cursor:pointer;-webkit-box-align:center;-ms-flex-align:center;align-items:center}.ce-conversion-tool--hidden{display:none}.ce-conversion-tool--focused{background:rgba(34,186,255,.08)!important}.ce-conversion-tool--focused{-webkit-box-shadow:inset 0 0 0px 1px rgba(7,161,227,.08);box-shadow:inset 0 0 0 1px #07a1e314}.ce-conversion-tool--focused-animated{-webkit-animation-name:buttonClicked;animation-name:buttonClicked;-webkit-animation-duration:.25s;animation-duration:.25s}.ce-conversion-tool:hover{background:#eff2f5}.ce-conversion-tool__icon{display:-webkit-inline-box;display:-ms-inline-flexbox;display:inline-flex;width:26px;height:26px;-webkit-box-shadow:0 0 0 1px rgba(201,201,204,.48);box-shadow:0 0 0 1px #c9c9cc7a;border-radius:5px;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;background:#fff;-webkit-box-sizing:content-box;box-sizing:content-box;-ms-flex-negative:0;flex-shrink:0;margin-right:10px}.ce-conversion-tool__icon svg{width:20px;height:20px}@media (max-width: 650px){.ce-conversion-tool__icon{width:36px;height:36px;border-radius:8px}.ce-conversion-tool__icon svg{width:28px;height:28px}}.ce-conversion-tool--last{margin-right:0!important}.ce-conversion-tool--active{color:#388ae5!important}.ce-conversion-tool--active{-webkit-animation:bounceIn .75s 1;animation:bounceIn .75s 1;-webkit-animation-fill-mode:forwards;animation-fill-mode:forwards}.ce-conversion-tool__secondary-label{color:#707684;font-size:12px;margin-left:auto;white-space:nowrap;letter-spacing:-.1em;padding-right:5px;margin-bottom:-2px;opacity:.6}@media (max-width: 650px){.ce-conversion-tool__secondary-label{display:none}}.ce-settings__button{display:-webkit-inline-box;display:-ms-inline-flexbox;display:inline-flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;padding:6px 1px;border-radius:3px;cursor:pointer;border:0;outline:none;background-color:transparent;vertical-align:bottom;color:inherit;margin:0;line-height:32px}.ce-settings__button svg{width:20px;height:20px}@media (max-width: 650px){.ce-settings__button svg{width:28px;height:28px}}@media (hover: hover){.ce-settings__button:hover{background-color:#eff2f5}}.ce-settings__button--active{color:#388ae5}.ce-settings__button--focused{background:rgba(34,186,255,.08)!important}.ce-settings__button--focused{-webkit-box-shadow:inset 0 0 0px 1px rgba(7,161,227,.08);box-shadow:inset 0 0 0 1px #07a1e314}.ce-settings__button--focused-animated{-webkit-animation-name:buttonClicked;animation-name:buttonClicked;-webkit-animation-duration:.25s;animation-duration:.25s}.ce-settings__button:not(:nth-child(3n+3)){margin-right:3px}.ce-settings__button:nth-child(n+4){margin-top:3px}.ce-settings__button--disabled{cursor:not-allowed!important}.ce-settings__button--disabled{opacity:.3}.ce-settings__button--selected{color:#388ae5}@media (min-width: 651px){.codex-editor--narrow .ce-settings .ce-popover{right:0;left:auto;left:initial}}@-webkit-keyframes fade-in{0%{opacity:0}to{opacity:1}}@keyframes fade-in{0%{opacity:0}to{opacity:1}}.ce-block{-webkit-animation:fade-in .3s ease;animation:fade-in .3s ease;-webkit-animation-fill-mode:none;animation-fill-mode:none;-webkit-animation-fill-mode:initial;animation-fill-mode:initial}.ce-block:first-of-type{margin-top:0}.ce-block--selected .ce-block__content{background:#e1f2ff}.ce-block--selected .ce-block__content [contenteditable]{-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none}.ce-block--selected .ce-block__content img,.ce-block--selected .ce-block__content .ce-stub{opacity:.55}.ce-block--stretched .ce-block__content{max-width:none}.ce-block__content{position:relative;max-width:650px;margin:0 auto;-webkit-transition:background-color .15s ease;transition:background-color .15s ease}.ce-block--drop-target .ce-block__content:before{content:"";position:absolute;top:100%;left:-20px;margin-top:-1px;height:8px;width:8px;border:solid #388AE5;border-width:1px 1px 0 0;-webkit-transform-origin:right;transform-origin:right;-webkit-transform:rotate(45deg);transform:rotate(45deg)}.ce-block--drop-target .ce-block__content:after{content:"";position:absolute;top:100%;height:1px;width:100%;color:#388ae5;background:repeating-linear-gradient(90deg,#388AE5,#388AE5 1px,#fff 1px,#fff 6px)}.ce-block a{cursor:pointer;-webkit-text-decoration:underline;text-decoration:underline}.ce-block b{font-weight:700}.ce-block i{font-style:italic}@-webkit-keyframes bounceIn{0%,20%,40%,60%,80%,to{-webkit-animation-timing-function:cubic-bezier(.215,.61,.355,1);animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{-webkit-transform:scale3d(.9,.9,.9);transform:scale3d(.9,.9,.9)}20%{-webkit-transform:scale3d(1.03,1.03,1.03);transform:scale3d(1.03,1.03,1.03)}60%{-webkit-transform:scale3d(1,1,1);transform:scaleZ(1)}}@keyframes bounceIn{0%,20%,40%,60%,80%,to{-webkit-animation-timing-function:cubic-bezier(.215,.61,.355,1);animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{-webkit-transform:scale3d(.9,.9,.9);transform:scale3d(.9,.9,.9)}20%{-webkit-transform:scale3d(1.03,1.03,1.03);transform:scale3d(1.03,1.03,1.03)}60%{-webkit-transform:scale3d(1,1,1);transform:scaleZ(1)}}@-webkit-keyframes selectionBounce{0%,20%,40%,60%,80%,to{-webkit-animation-timing-function:cubic-bezier(.215,.61,.355,1);animation-timing-function:cubic-bezier(.215,.61,.355,1)}50%{-webkit-transform:scale3d(1.01,1.01,1.01);transform:scale3d(1.01,1.01,1.01)}70%{-webkit-transform:scale3d(1,1,1);transform:scaleZ(1)}}@keyframes selectionBounce{0%,20%,40%,60%,80%,to{-webkit-animation-timing-function:cubic-bezier(.215,.61,.355,1);animation-timing-function:cubic-bezier(.215,.61,.355,1)}50%{-webkit-transform:scale3d(1.01,1.01,1.01);transform:scale3d(1.01,1.01,1.01)}70%{-webkit-transform:scale3d(1,1,1);transform:scaleZ(1)}}@-webkit-keyframes buttonClicked{0%,20%,40%,60%,80%,to{-webkit-animation-timing-function:cubic-bezier(.215,.61,.355,1);animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{-webkit-transform:scale3d(.95,.95,.95);transform:scale3d(.95,.95,.95)}60%{-webkit-transform:scale3d(1.02,1.02,1.02);transform:scale3d(1.02,1.02,1.02)}80%{-webkit-transform:scale3d(1,1,1);transform:scaleZ(1)}}@keyframes buttonClicked{0%,20%,40%,60%,80%,to{-webkit-animation-timing-function:cubic-bezier(.215,.61,.355,1);animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{-webkit-transform:scale3d(.95,.95,.95);transform:scale3d(.95,.95,.95)}60%{-webkit-transform:scale3d(1.02,1.02,1.02);transform:scale3d(1.02,1.02,1.02)}80%{-webkit-transform:scale3d(1,1,1);transform:scaleZ(1)}}.cdx-block{padding:.4em 0}.cdx-block::-webkit-input-placeholder{line-height:normal!important}.cdx-input{border:1px solid rgba(201,201,204,.48);-webkit-box-shadow:inset 0 1px 2px 0 rgba(35,44,72,.06);box-shadow:inset 0 1px 2px #232c480f;border-radius:3px;padding:10px 12px;outline:none;width:100%;-webkit-box-sizing:border-box;box-sizing:border-box}.cdx-input[data-placeholder]:before{position:static!important}.cdx-input[data-placeholder]:before{display:inline-block;width:0;white-space:nowrap;pointer-events:none}.cdx-settings-button{display:-webkit-inline-box;display:-ms-inline-flexbox;display:inline-flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;padding:6px 1px;border-radius:3px;cursor:pointer;border:0;outline:none;background-color:transparent;vertical-align:bottom;color:inherit;margin:0;min-width:26px;min-height:26px}.cdx-settings-button svg{width:20px;height:20px}@media (max-width: 650px){.cdx-settings-button svg{width:28px;height:28px}}@media (hover: hover){.cdx-settings-button:hover{background-color:#eff2f5}}.cdx-settings-button--focused{background:rgba(34,186,255,.08)!important}.cdx-settings-button--focused{-webkit-box-shadow:inset 0 0 0px 1px rgba(7,161,227,.08);box-shadow:inset 0 0 0 1px #07a1e314}.cdx-settings-button--focused-animated{-webkit-animation-name:buttonClicked;animation-name:buttonClicked;-webkit-animation-duration:.25s;animation-duration:.25s}.cdx-settings-button--active{color:#388ae5}.cdx-settings-button svg{width:auto;height:auto}@media (max-width: 650px){.cdx-settings-button{width:36px;height:36px;border-radius:8px}}.cdx-loader{position:relative;border:1px solid rgba(201,201,204,.48)}.cdx-loader:before{content:"";position:absolute;left:50%;top:50%;width:18px;height:18px;margin:-11px 0 0 -11px;border:2px solid rgba(201,201,204,.48);border-left-color:#388ae5;border-radius:50%;-webkit-animation:cdxRotation 1.2s infinite linear;animation:cdxRotation 1.2s infinite linear}@-webkit-keyframes cdxRotation{0%{-webkit-transform:rotate(0deg);transform:rotate(0)}to{-webkit-transform:rotate(360deg);transform:rotate(360deg)}}@keyframes cdxRotation{0%{-webkit-transform:rotate(0deg);transform:rotate(0)}to{-webkit-transform:rotate(360deg);transform:rotate(360deg)}}.cdx-button{padding:13px;border-radius:3px;border:1px solid rgba(201,201,204,.48);font-size:14.9px;background:#fff;-webkit-box-shadow:0 2px 2px 0 rgba(18,30,57,.04);box-shadow:0 2px 2px #121e390a;color:#707684;text-align:center;cursor:pointer}@media (hover: hover){.cdx-button:hover{background:#FBFCFE;-webkit-box-shadow:0 1px 3px 0 rgba(18,30,57,.08);box-shadow:0 1px 3px #121e3914}}.cdx-button svg{height:20px;margin-right:.2em;margin-top:-2px}.ce-stub{display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;padding:12px 18px;margin:10px 0;border-radius:10px;background:#eff2f5;border:1px solid #EFF0F1;color:#707684;font-size:14px}.ce-stub svg{width:20px;height:20px}.ce-stub__info{margin-left:14px}.ce-stub__title{font-weight:500;text-transform:capitalize}.codex-editor.codex-editor--rtl{direction:rtl}.codex-editor.codex-editor--rtl .cdx-list{padding-left:0;padding-right:40px}.codex-editor.codex-editor--rtl .ce-toolbar__plus{right:-26px;left:auto}.codex-editor.codex-editor--rtl .ce-toolbar__actions{right:auto;left:-26px}@media (max-width: 650px){.codex-editor.codex-editor--rtl .ce-toolbar__actions{margin-left:0;margin-right:auto;padding-right:0;padding-left:10px}}.codex-editor.codex-editor--rtl .ce-settings{left:5px;right:auto}.codex-editor.codex-editor--rtl .ce-settings:before{right:auto;left:25px}.codex-editor.codex-editor--rtl .ce-settings__button:not(:nth-child(3n+3)){margin-left:3px;margin-right:0}.codex-editor.codex-editor--rtl .ce-conversion-tool__icon{margin-right:0;margin-left:10px}.codex-editor.codex-editor--rtl .ce-inline-toolbar__dropdown{border-right:0px solid transparent;border-left:1px solid rgba(201,201,204,.48);margin:0 -6px 0 6px}.codex-editor.codex-editor--rtl .ce-inline-toolbar__dropdown .icon--toggler-down{margin-left:0;margin-right:4px}@media (min-width: 651px){.codex-editor--narrow.codex-editor--rtl .ce-toolbar__plus{left:0;right:5px}}@media (min-width: 651px){.codex-editor--narrow.codex-editor--rtl .ce-toolbar__actions{left:-5px}}.cdx-search-field{--icon-margin-right: 10px;background:rgba(232,232,235,.49);border:1px solid rgba(226,226,229,.2);border-radius:6px;padding:2px;display:grid;grid-template-columns:auto auto 1fr;grid-template-rows:auto}.cdx-search-field__icon{width:26px;height:26px;display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;margin-right:var(--icon-margin-right)}.cdx-search-field__icon svg{width:20px;height:20px;color:#707684}.cdx-search-field__input{font-size:14px;outline:none;font-weight:500;font-family:inherit;border:0;background:transparent;margin:0;padding:0;line-height:22px;min-width:calc(100% - 26px - var(--icon-margin-right))}.cdx-search-field__input::-webkit-input-placeholder{color:#707684;font-weight:500}.cdx-search-field__input::-moz-placeholder{color:#707684;font-weight:500}.cdx-search-field__input:-ms-input-placeholder{color:#707684;font-weight:500}.cdx-search-field__input::-ms-input-placeholder{color:#707684;font-weight:500}.cdx-search-field__input::placeholder{color:#707684;font-weight:500}.ce-popover{--border-radius: 6px;--width: 200px;--max-height: 270px;--padding: 6px;--offset-from-target: 8px;--color-border: #e8e8eb;--color-shadow: rgba(13,20,33,.13);--color-background: white;--color-text-primary: black;--color-text-secondary: #707684;--color-border-icon: rgba(201, 201, 204, .48);--color-border-icon-disabled: #EFF0F1;--color-text-icon-active: #388AE5;--color-background-icon-active: rgba(56, 138, 229, .1);--color-background-item-focus: rgba(34, 186, 255, .08);--color-shadow-item-focus: rgba(7, 161, 227, .08);--color-background-item-hover: #eff2f5;--color-background-item-confirm: #E24A4A;--color-background-item-confirm-hover: #CE4343;min-width:var(--width);width:var(--width);max-height:var(--max-height);border-radius:var(--border-radius);overflow:hidden;-webkit-box-sizing:border-box;box-sizing:border-box;-webkit-box-shadow:0 3px 15px -3px var(--color-shadow);box-shadow:0 3px 15px -3px var(--color-shadow);position:absolute;left:0;top:calc(100% + var(--offset-from-target));background:var(--color-background);display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-orient:vertical;-webkit-box-direction:normal;-ms-flex-direction:column;flex-direction:column;z-index:4;opacity:0;max-height:0;pointer-events:none;padding:0;border:none}.ce-popover--opened{opacity:1;padding:var(--padding);max-height:var(--max-height);pointer-events:auto;-webkit-animation:panelShowing .1s ease;animation:panelShowing .1s ease;border:1px solid var(--color-border)}@media (max-width: 650px){.ce-popover--opened{-webkit-animation:panelShowingMobile .25s ease;animation:panelShowingMobile .25s ease}}.ce-popover__items{overflow-y:auto;-ms-scroll-chaining:none;overscroll-behavior:contain}@media (max-width: 650px){.ce-popover__overlay{position:fixed;top:0;bottom:0;left:0;right:0;background:#1D202B;z-index:3;opacity:.5;-webkit-transition:opacity .12s ease-in;transition:opacity .12s ease-in;will-change:opacity;visibility:visible}}.ce-popover__overlay--hidden{display:none}.ce-popover--open-top{top:calc(-1 * (var(--offset-from-target) + var(--popover-height)))}@media (max-width: 650px){.ce-popover{--offset: 5px;position:fixed;max-width:none;min-width:calc(100% - var(--offset) * 2);left:var(--offset);right:var(--offset);bottom:calc(var(--offset) + env(safe-area-inset-bottom));top:auto;border-radius:10px}.ce-popover .ce-popover__search{display:none}}.ce-popover__search,.ce-popover__custom-content:not(:empty){margin-bottom:5px}.ce-popover__nothing-found-message{color:#707684;display:none;cursor:default;padding:3px;font-size:14px;line-height:20px;font-weight:500;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ce-popover__nothing-found-message--displayed{display:block}.ce-popover__custom-content:not(:empty){padding:4px}@media (min-width: 651px){.ce-popover__custom-content:not(:empty){padding:0}}.ce-popover__custom-content--hidden{display:none}.ce-popover-item{--border-radius: 6px;--icon-size: 20px;--icon-size-mobile: 28px;border-radius:var(--border-radius);display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;padding:3px;color:var(--color-text-primary);-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none}@media (max-width: 650px){.ce-popover-item{padding:4px}}.ce-popover-item:not(:last-of-type){margin-bottom:1px}.ce-popover-item__icon{border-radius:5px;width:26px;height:26px;-webkit-box-shadow:0 0 0 1px var(--color-border-icon);box-shadow:0 0 0 1px var(--color-border-icon);background:#fff;display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;margin-right:10px}.ce-popover-item__icon svg{width:20px;height:20px}@media (max-width: 650px){.ce-popover-item__icon{width:36px;height:36px;border-radius:8px}.ce-popover-item__icon svg{width:var(--icon-size-mobile);height:var(--icon-size-mobile)}}.ce-popover-item__title{font-size:14px;line-height:20px;font-weight:500;overflow:hidden;white-space:nowrap;text-overflow:ellipsis}@media (max-width: 650px){.ce-popover-item__title{font-size:16px}}.ce-popover-item__secondary-title{color:var(--color-text-secondary);font-size:12px;margin-left:auto;white-space:nowrap;letter-spacing:-.1em;padding-right:5px;margin-bottom:-2px;opacity:.6}@media (max-width: 650px){.ce-popover-item__secondary-title{display:none}}.ce-popover-item--active{background:var(--color-background-icon-active);color:var(--color-text-icon-active)}.ce-popover-item--active .ce-popover-item__icon{-webkit-box-shadow:none;box-shadow:none}.ce-popover-item--disabled{color:var(--color-text-secondary);cursor:default;pointer-events:none}.ce-popover-item--disabled .ce-popover-item__icon{-webkit-box-shadow:0 0 0 1px var(--color-border-icon-disabled);box-shadow:0 0 0 1px var(--color-border-icon-disabled)}.ce-popover-item--focused:not(.ce-popover-item--no-focus){background:var(--color-background-item-focus)!important}.ce-popover-item--focused:not(.ce-popover-item--no-focus){-webkit-box-shadow:inset 0 0 0px 1px var(--color-shadow-item-focus);box-shadow:inset 0 0 0 1px var(--color-shadow-item-focus)}.ce-popover-item--hidden{display:none}@media (hover: hover){.ce-popover-item:hover{cursor:pointer}.ce-popover-item:hover:not(.ce-popover-item--no-hover){background-color:var(--color-background-item-hover)}.ce-popover-item:hover .ce-popover-item__icon{-webkit-box-shadow:none;box-shadow:none}}.ce-popover-item--confirmation{background:var(--color-background-item-confirm)}.ce-popover-item--confirmation .ce-popover-item__icon{color:var(--color-background-item-confirm)}.ce-popover-item--confirmation .ce-popover-item__title{color:#fff}@media (hover: hover){.ce-popover-item--confirmation:not(.ce-popover-item--no-hover):hover{background:var(--color-background-item-confirm-hover)}}.ce-popover-item--confirmation:not(.ce-popover-item--no-focus).ce-popover-item--focused{background:var(--color-background-item-confirm-hover)!important}.ce-popover-item--confirmation .ce-popover-item__icon,.ce-popover-item--active .ce-popover-item__icon,.ce-popover-item--focused .ce-popover-item__icon{-webkit-box-shadow:none;box-shadow:none}@-webkit-keyframes panelShowing{0%{opacity:0;-webkit-transform:translateY(-8px) scale(.9);transform:translateY(-8px) scale(.9)}70%{opacity:1;-webkit-transform:translateY(2px);transform:translateY(2px)}to{-webkit-transform:translateY(0);transform:translateY(0)}}@keyframes panelShowing{0%{opacity:0;-webkit-transform:translateY(-8px) scale(.9);transform:translateY(-8px) scale(.9)}70%{opacity:1;-webkit-transform:translateY(2px);transform:translateY(2px)}to{-webkit-transform:translateY(0);transform:translateY(0)}}@-webkit-keyframes panelShowingMobile{0%{opacity:0;-webkit-transform:translateY(14px) scale(.98);transform:translateY(14px) scale(.98)}70%{opacity:1;-webkit-transform:translateY(-4px);transform:translateY(-4px)}to{-webkit-transform:translateY(0);transform:translateY(0)}}@keyframes panelShowingMobile{0%{opacity:0;-webkit-transform:translateY(14px) scale(.98);transform:translateY(14px) scale(.98)}70%{opacity:1;-webkit-transform:translateY(-4px);transform:translateY(-4px)}to{-webkit-transform:translateY(0);transform:translateY(0)}}.wobble{-webkit-animation-name:wobble;animation-name:wobble;-webkit-animation-duration:.4s;animation-duration:.4s}@-webkit-keyframes wobble{0%{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}15%{-webkit-transform:translate3d(-9%,0,0);transform:translate3d(-9%,0,0)}30%{-webkit-transform:translate3d(9%,0,0);transform:translate3d(9%,0,0)}45%{-webkit-transform:translate3d(-4%,0,0);transform:translate3d(-4%,0,0)}60%{-webkit-transform:translate3d(4%,0,0);transform:translate3d(4%,0,0)}75%{-webkit-transform:translate3d(-1%,0,0);transform:translate3d(-1%,0,0)}to{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}}@keyframes wobble{0%{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}15%{-webkit-transform:translate3d(-9%,0,0);transform:translate3d(-9%,0,0)}30%{-webkit-transform:translate3d(9%,0,0);transform:translate3d(9%,0,0)}45%{-webkit-transform:translate3d(-4%,0,0);transform:translate3d(-4%,0,0)}60%{-webkit-transform:translate3d(4%,0,0);transform:translate3d(4%,0,0)}75%{-webkit-transform:translate3d(-1%,0,0);transform:translate3d(-1%,0,0)}to{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}}
`;
let Wm = class extends rt {
  constructor() {
    super(...arguments), this.isMobile = !1, this.contentRectCache = void 0, this.resizeDebouncer = hd(() => {
      this.windowResize();
    }, 200);
  }
  /**
   * Editor.js UI CSS class names
   *
   * @returns {{editorWrapper: string, editorZone: string}}
   */
  get CSS() {
    return {
      editorWrapper: "codex-editor",
      editorWrapperNarrow: "codex-editor--narrow",
      editorZone: "codex-editor__redactor",
      editorZoneHidden: "codex-editor__redactor--hidden",
      editorEmpty: "codex-editor--empty",
      editorRtlFix: "codex-editor--rtl"
    };
  }
  /**
   * Return Width of center column of Editor
   *
   * @returns {DOMRect}
   */
  get contentRect() {
    if (this.contentRectCache)
      return this.contentRectCache;
    const e = this.nodes.wrapper.querySelector(`.${pi.CSS.content}`);
    return e ? (this.contentRectCache = e.getBoundingClientRect(), this.contentRectCache) : {
      width: 650,
      left: 0,
      right: 0
    };
  }
  /**
   * Making main interface
   */
  async prepare() {
    this.checkIsMobile(), this.make(), this.loadStyles();
  }
  /**
   * Toggle read-only state
   *
   * If readOnly is true:
   *  - removes all listeners from main UI module elements
   *
   * if readOnly is false:
   *  - enables all listeners to UI module elements
   *
   * @param {boolean} readOnlyEnabled - "read only" state
   */
  toggleReadOnly(e) {
    e ? this.disableModuleBindings() : window.requestIdleCallback(() => {
      this.enableModuleBindings();
    }, {
      timeout: 2e3
    });
  }
  /**
   * Check if Editor is empty and set CSS class to wrapper
   */
  checkEmptiness() {
    const { BlockManager: e } = this.Editor;
    this.nodes.wrapper.classList.toggle(this.CSS.editorEmpty, e.isEditorEmpty);
  }
  /**
   * Check if one of Toolbar is opened
   * Used to prevent global keydowns (for example, Enter) conflicts with Enter-on-toolbar
   *
   * @returns {boolean}
   */
  get someToolbarOpened() {
    const { Toolbar: e, BlockSettings: t, InlineToolbar: n, ConversionToolbar: r } = this.Editor;
    return t.opened || n.opened || r.opened || e.toolbox.opened;
  }
  /**
   * Check for some Flipper-buttons is under focus
   */
  get someFlipperButtonFocused() {
    return this.Editor.Toolbar.toolbox.hasFocus() ? !0 : Object.entries(this.Editor).filter(([e, t]) => t.flipper instanceof ms).some(([e, t]) => t.flipper.hasFocus());
  }
  /**
   * Clean editor`s UI
   */
  destroy() {
    this.nodes.holder.innerHTML = "";
  }
  /**
   * Close all Editor's toolbars
   */
  closeAllToolbars() {
    const { Toolbar: e, BlockSettings: t, InlineToolbar: n, ConversionToolbar: r } = this.Editor;
    t.close(), n.close(), r.close(), e.toolbox.close();
  }
  /**
   * Check for mobile mode and cache a result
   */
  checkIsMobile() {
    this.isMobile = window.innerWidth < Sh;
  }
  /**
   * Makes Editor.js interface
   */
  make() {
    this.nodes.holder = ae.getHolder(this.config.holder), this.nodes.wrapper = ae.make("div", [
      this.CSS.editorWrapper,
      ...this.isRtl ? [this.CSS.editorRtlFix] : []
    ]), this.nodes.redactor = ae.make("div", this.CSS.editorZone), this.nodes.holder.offsetWidth < this.contentRect.width && this.nodes.wrapper.classList.add(this.CSS.editorWrapperNarrow), this.nodes.redactor.style.paddingBottom = this.config.minHeight + "px", this.nodes.wrapper.appendChild(this.nodes.redactor), this.nodes.holder.appendChild(this.nodes.wrapper);
  }
  /**
   * Appends CSS
   */
  loadStyles() {
    const e = "editor-js-styles";
    if (ae.get(e))
      return;
    const t = ae.make("style", null, {
      id: e,
      textContent: zm.toString()
    });
    this.config.style && !sn(this.config.style) && this.config.style.nonce && t.setAttribute("nonce", this.config.style.nonce), ae.prepend(document.head, t);
  }
  /**
   * Bind events on the Editor.js interface
   */
  enableModuleBindings() {
    this.readOnlyMutableListeners.on(this.nodes.redactor, "click", (t) => {
      this.redactorClicked(t);
    }, !1), this.readOnlyMutableListeners.on(this.nodes.redactor, "mousedown", (t) => {
      this.documentTouched(t);
    }, {
      capture: !0,
      passive: !0
    }), this.readOnlyMutableListeners.on(this.nodes.redactor, "touchstart", (t) => {
      this.documentTouched(t);
    }, {
      capture: !0,
      passive: !0
    }), this.readOnlyMutableListeners.on(document, "keydown", (t) => {
      this.documentKeydown(t);
    }, !0), this.readOnlyMutableListeners.on(document, "mousedown", (t) => {
      this.documentClicked(t);
    }, !0);
    const e = hd(() => {
      this.selectionChanged();
    }, 180);
    this.readOnlyMutableListeners.on(document, "selectionchange", e, !0), this.readOnlyMutableListeners.on(window, "resize", () => {
      this.resizeDebouncer();
    }, {
      passive: !0
    }), this.watchBlockHoveredEvents();
  }
  /**
   * Listen redactor mousemove to emit 'block-hovered' event
   */
  watchBlockHoveredEvents() {
    let e;
    this.readOnlyMutableListeners.on(this.nodes.redactor, "mousemove", Hl((t) => {
      const n = t.target.closest(".ce-block");
      this.Editor.BlockSelection.anyBlockSelected || n && e !== n && (e = n, this.eventsDispatcher.emit(Uh, {
        block: this.Editor.BlockManager.getBlockByChildNode(n)
      }));
    }, 20), {
      passive: !0
    });
  }
  /**
   * Unbind events on the Editor.js interface
   */
  disableModuleBindings() {
    this.readOnlyMutableListeners.clearAll();
  }
  /**
   * Resize window handler
   */
  windowResize() {
    this.contentRectCache = null, this.checkIsMobile();
  }
  /**
   * All keydowns on document
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  documentKeydown(e) {
    switch (e.keyCode) {
      case ze.ENTER:
        this.enterPressed(e);
        break;
      case ze.BACKSPACE:
      case ze.DELETE:
        this.backspacePressed(e);
        break;
      case ze.ESC:
        this.escapePressed(e);
        break;
      default:
        this.defaultBehaviour(e);
        break;
    }
  }
  /**
   * Ignore all other document's keydown events
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  defaultBehaviour(e) {
    const { currentBlock: t } = this.Editor.BlockManager, n = e.target.closest(`.${this.CSS.editorWrapper}`), r = e.altKey || e.ctrlKey || e.metaKey || e.shiftKey;
    if (t !== void 0 && n === null) {
      this.Editor.BlockEvents.keydown(e);
      return;
    }
    n || t && r || (this.Editor.BlockManager.dropPointer(), this.Editor.Toolbar.close());
  }
  /**
   * @param {KeyboardEvent} event - keyboard event
   */
  backspacePressed(e) {
    const { BlockManager: t, BlockSelection: n, Caret: r } = this.Editor;
    if (n.anyBlockSelected && !Fe.isSelectionExists) {
      const a = t.removeSelectedBlocks(), c = t.insertDefaultBlockAtIndex(a, !0);
      r.setToBlock(c, r.positions.START), n.clearSelection(e), e.preventDefault(), e.stopPropagation(), e.stopImmediatePropagation();
    }
  }
  /**
   * Escape pressed
   * If some of Toolbar components are opened, then close it otherwise close Toolbar
   *
   * @param {Event} event - escape keydown event
   */
  escapePressed(e) {
    this.Editor.BlockSelection.clearSelection(e), this.Editor.Toolbar.toolbox.opened ? (this.Editor.Toolbar.toolbox.close(), this.Editor.Caret.setToBlock(this.Editor.BlockManager.currentBlock, this.Editor.Caret.positions.END)) : this.Editor.BlockSettings.opened ? this.Editor.BlockSettings.close() : this.Editor.ConversionToolbar.opened ? this.Editor.ConversionToolbar.close() : this.Editor.InlineToolbar.opened ? this.Editor.InlineToolbar.close() : this.Editor.Toolbar.close();
  }
  /**
   * Enter pressed on document
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  enterPressed(e) {
    const { BlockManager: t, BlockSelection: n } = this.Editor, r = t.currentBlockIndex >= 0;
    if (n.anyBlockSelected && !Fe.isSelectionExists) {
      n.clearSelection(e), e.preventDefault(), e.stopImmediatePropagation(), e.stopPropagation();
      return;
    }
    if (!this.someToolbarOpened && r && e.target.tagName === "BODY") {
      const a = this.Editor.BlockManager.insert();
      this.Editor.Caret.setToBlock(a), this.Editor.Toolbar.moveAndOpen(a);
    }
    this.Editor.BlockSelection.clearSelection(e);
  }
  /**
   * All clicks on document
   *
   * @param {MouseEvent} event - Click event
   */
  documentClicked(e) {
    var t, n;
    if (!e.isTrusted)
      return;
    const r = e.target;
    this.nodes.holder.contains(r) || Fe.isAtEditor || (this.Editor.BlockManager.dropPointer(), this.Editor.Toolbar.close());
    const a = (t = this.Editor.BlockSettings.nodes.wrapper) == null ? void 0 : t.contains(r), c = (n = this.Editor.Toolbar.nodes.settingsToggler) == null ? void 0 : n.contains(r), u = a || c;
    if (this.Editor.BlockSettings.opened && !u) {
      this.Editor.BlockSettings.close();
      const p = this.Editor.BlockManager.getBlockByChildNode(r);
      this.Editor.Toolbar.moveAndOpen(p);
    }
    this.Editor.BlockSelection.clearSelection(e);
  }
  /**
   * First touch on editor
   * Fired before click
   *
   * Used to change current block — we need to do it before 'selectionChange' event.
   * Also:
   * - Move and show the Toolbar
   * - Set a Caret
   *
   * @param {MouseEvent | TouchEvent} event - touch or mouse event
   */
  documentTouched(e) {
    let t = e.target;
    if (t === this.nodes.redactor) {
      const n = e instanceof MouseEvent ? e.clientX : e.touches[0].clientX, r = e instanceof MouseEvent ? e.clientY : e.touches[0].clientY;
      t = document.elementFromPoint(n, r);
    }
    try {
      this.Editor.BlockManager.setCurrentBlockByChildNode(t);
    } catch {
      this.Editor.RectangleSelection.isRectActivated() || this.Editor.Caret.setToTheLastBlock();
    }
    this.Editor.Toolbar.moveAndOpen();
  }
  /**
   * All clicks on the redactor zone
   *
   * @param {MouseEvent} event - click event
   * @description
   * - By clicks on the Editor's bottom zone:
   *      - if last Block is empty, set a Caret to this
   *      - otherwise, add a new empty Block and set a Caret to that
   */
  redactorClicked(e) {
    if (!Fe.isCollapsed)
      return;
    const t = e.target, n = e.metaKey || e.ctrlKey;
    if (ae.isAnchor(t) && n) {
      e.stopImmediatePropagation(), e.stopPropagation();
      const r = t.getAttribute("href"), a = cf(r);
      hf(a);
      return;
    }
    this.processBottomZoneClick(e);
  }
  /**
   * Check if user clicks on the Editor's bottom zone:
   *  - set caret to the last block
   *  - or add new empty block
   *
   * @param event - click event
   */
  processBottomZoneClick(e) {
    const t = this.Editor.BlockManager.getBlockByIndex(-1), n = ae.offset(t.holder).bottom, r = e.pageY, { BlockSelection: a } = this.Editor;
    if (e.target instanceof Element && e.target.isEqualNode(this.nodes.redactor) && /**
    * If there is cross block selection started, target will be equal to redactor so we need additional check
    */
    !a.anyBlockSelected && /**
    * Prevent caret jumping (to last block) when clicking between blocks
    */
    n < r) {
      e.stopImmediatePropagation(), e.stopPropagation();
      const { BlockManager: c, Caret: u, Toolbar: p } = this.Editor;
      (!c.lastBlock.tool.isDefault || !c.lastBlock.isEmpty) && c.insertAtEnd(), u.setToTheLastBlock(), p.moveAndOpen(c.lastBlock);
    }
  }
  /**
   * Handle selection changes on mobile devices
   * Uses for showing the Inline Toolbar
   */
  selectionChanged() {
    const { CrossBlockSelection: e, BlockSelection: t } = this.Editor, n = Fe.anchorElement;
    if (e.isCrossBlockSelectionStarted && t.anyBlockSelected && Fe.get().removeAllRanges(), !n) {
      Fe.range || this.Editor.InlineToolbar.close();
      return;
    }
    const r = n.closest(`.${pi.CSS.content}`) === null;
    if (r && (this.Editor.InlineToolbar.containsNode(n) || this.Editor.InlineToolbar.close(), n.dataset.inlineToolbar !== "true"))
      return;
    this.Editor.BlockManager.currentBlock || this.Editor.BlockManager.setCurrentBlockByChildNode(n);
    const a = r !== !0;
    this.Editor.InlineToolbar.tryToShow(!0, a);
  }
};
const Um = {
  // API Modules
  BlocksAPI: kf,
  CaretAPI: xf,
  EventsAPI: Cf,
  I18nAPI: lc,
  API: Sf,
  InlineToolbarAPI: Tf,
  ListenersAPI: _f,
  NotifierAPI: Lf,
  ReadOnlyAPI: Of,
  SanitizerAPI: Hf,
  SaverAPI: jf,
  SelectionAPI: $f,
  StylesAPI: Yf,
  ToolbarAPI: Ff,
  TooltipAPI: Vf,
  UiAPI: Gf,
  // Toolbar Modules
  BlockSettings: hm,
  ConversionToolbar: um,
  Toolbar: ym,
  InlineToolbar: wm,
  // Modules
  BlockEvents: km,
  BlockManager: Sm,
  BlockSelection: Tm,
  Caret: _m,
  CrossBlockSelection: Em,
  DragNDrop: Mm,
  ModificationsObserver: Dm,
  Paste: Lm,
  ReadOnly: Om,
  RectangleSelection: Js,
  Renderer: Bm,
  Saver: Am,
  Tools: tu,
  UI: Wm
};
class qm {
  /**
   * @param {EditorConfig} config - user configuration
   */
  constructor(e) {
    this.moduleInstances = {}, this.eventsDispatcher = new Va();
    let t, n;
    this.isReady = new Promise((r, a) => {
      t = r, n = a;
    }), Promise.resolve().then(async () => {
      this.configuration = e, this.validate(), this.init(), await this.start(), await this.render();
      const { BlockManager: r, Caret: a, UI: c, ModificationsObserver: u } = this.moduleInstances;
      c.checkEmptiness(), u.enable(), this.configuration.autofocus && a.setToBlock(r.blocks[0], a.positions.START), t();
    }).catch((r) => {
      bt(`Editor.js is not ready because of ${r}`, "error"), n(r);
    });
  }
  /**
   * Setting for configuration
   *
   * @param {EditorConfig|string} config - Editor's config to set
   */
  set configuration(e) {
    var t, n;
    ho(e) ? this.config = {
      ...e
    } : this.config = {
      holder: e
    }, $l(!!this.config.holderId, "config.holderId", "config.holder"), this.config.holderId && !this.config.holder && (this.config.holder = this.config.holderId, this.config.holderId = null), this.config.holder == null && (this.config.holder = "editorjs"), this.config.logLevel || (this.config.logLevel = kh.VERBOSE), of(this.config.logLevel), $l(!!this.config.initialBlock, "config.initialBlock", "config.defaultBlock"), this.config.defaultBlock = this.config.defaultBlock || this.config.initialBlock || "paragraph", this.config.minHeight = this.config.minHeight !== void 0 ? this.config.minHeight : 300;
    const r = {
      type: this.config.defaultBlock,
      data: {}
    };
    this.config.placeholder = this.config.placeholder || !1, this.config.sanitizer = this.config.sanitizer || {
      p: !0,
      b: !0,
      a: !0
    }, this.config.hideToolbar = this.config.hideToolbar ? this.config.hideToolbar : !1, this.config.tools = this.config.tools || {}, this.config.i18n = this.config.i18n || {}, this.config.data = this.config.data || { blocks: [] }, this.config.onReady = this.config.onReady || (() => {
    }), this.config.onChange = this.config.onChange || (() => {
    }), this.config.inlineToolbar = this.config.inlineToolbar !== void 0 ? this.config.inlineToolbar : !0, (sn(this.config.data) || !this.config.data.blocks || this.config.data.blocks.length === 0) && (this.config.data = { blocks: [r] }), this.config.readOnly = this.config.readOnly || !1, (t = this.config.i18n) != null && t.messages && Ko.setDictionary(this.config.i18n.messages), this.config.i18n.direction = ((n = this.config.i18n) == null ? void 0 : n.direction) || "ltr";
  }
  /**
   * Returns private property
   *
   * @returns {EditorConfig}
   */
  get configuration() {
    return this.config;
  }
  /**
   * Checks for required fields in Editor's config
   */
  validate() {
    const { holderId: e, holder: t } = this.config;
    if (e && t)
      throw Error("«holderId» and «holder» param can't assign at the same time.");
    if (Qi(t) && !ae.get(t))
      throw Error(`element with ID «${t}» is missing. Pass correct holder's ID.`);
    if (t && ho(t) && !ae.isElement(t))
      throw Error("«holder» value must be an Element node");
  }
  /**
   * Initializes modules:
   *  - make and save instances
   *  - configure
   */
  init() {
    this.constructModules(), this.configureModules();
  }
  /**
   * Start Editor!
   *
   * Get list of modules that needs to be prepared and return a sequence (Promise)
   *
   * @returns {Promise<void>}
   */
  async start() {
    await [
      "Tools",
      "UI",
      "BlockManager",
      "Paste",
      "BlockSelection",
      "RectangleSelection",
      "CrossBlockSelection",
      "ReadOnly"
    ].reduce(
      (e, t) => e.then(async () => {
        try {
          await this.moduleInstances[t].prepare();
        } catch (n) {
          if (n instanceof Eh)
            throw new Error(n.message);
          bt(`Module ${t} was skipped because of %o`, "warn", n);
        }
      }),
      Promise.resolve()
    );
  }
  /**
   * Render initial data
   */
  render() {
    return this.moduleInstances.Renderer.render(this.config.data.blocks);
  }
  /**
   * Make modules instances and save it to the @property this.moduleInstances
   */
  constructModules() {
    Object.entries(Um).forEach(([e, t]) => {
      try {
        this.moduleInstances[e] = new t({
          config: this.configuration,
          eventsDispatcher: this.eventsDispatcher
        });
      } catch (n) {
        bt("[constructModules]", `Module ${e} skipped because`, "error", n);
      }
    });
  }
  /**
   * Modules instances configuration:
   *  - pass other modules to the 'state' property
   *  - ...
   */
  configureModules() {
    for (const e in this.moduleInstances)
      Object.prototype.hasOwnProperty.call(this.moduleInstances, e) && (this.moduleInstances[e].state = this.getModulesDiff(e));
  }
  /**
   * Return modules without passed name
   *
   * @param {string} name - module for witch modules difference should be calculated
   */
  getModulesDiff(e) {
    const t = {};
    for (const n in this.moduleInstances)
      n !== e && (t[n] = this.moduleInstances[n]);
    return t;
  }
}
/**
 * Editor.js
 *
 * @license Apache-2.0
 * @see Editor.js <https://editorjs.io>
 * @author CodeX Team <https://codex.so>
 */
let yd = class {
  /** Editor version */
  static get version() {
    return "2.29.0";
  }
  /**
   * @param {EditorConfig|string|undefined} [configuration] - user configuration
   */
  constructor(e) {
    let t = () => {
    };
    ho(e) && Dt(e.onReady) && (t = e.onReady);
    const n = new qm(e);
    this.isReady = n.isReady.then(() => {
      this.exportAPI(n), t();
    });
  }
  /**
   * Export external API methods
   *
   * @param {Core} editor — Editor's instance
   */
  exportAPI(e) {
    const t = ["configuration"], n = () => {
      Object.values(e.moduleInstances).forEach((r) => {
        Dt(r.destroy) && r.destroy(), r.listeners.removeAll();
      }), qf(), e = null;
      for (const r in this)
        Object.prototype.hasOwnProperty.call(this, r) && delete this[r];
      Object.setPrototypeOf(this, null);
    };
    t.forEach((r) => {
      this[r] = e[r];
    }), this.destroy = n, Object.setPrototypeOf(this, e.moduleInstances.API.methods), delete this.exportAPI, Object.entries({
      blocks: {
        clear: "clear",
        render: "render"
      },
      caret: {
        focus: "focus"
      },
      events: {
        on: "on",
        off: "off",
        emit: "emit"
      },
      saver: {
        save: "save"
      }
    }).forEach(([r, a]) => {
      Object.entries(a).forEach(([c, u]) => {
        this[u] = e.moduleInstances.API.methods[r][c];
      });
    });
  }
};
function Dn(o) {
  return o && o.__esModule && Object.prototype.hasOwnProperty.call(o, "default") ? o.default : o;
}
var ou = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(window, function() {
    return function(t) {
      var n = {};
      function r(a) {
        if (n[a])
          return n[a].exports;
        var c = n[a] = { i: a, l: !1, exports: {} };
        return t[a].call(c.exports, c, c.exports, r), c.l = !0, c.exports;
      }
      return r.m = t, r.c = n, r.d = function(a, c, u) {
        r.o(a, c) || Object.defineProperty(a, c, { enumerable: !0, get: u });
      }, r.r = function(a) {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
      }, r.t = function(a, c) {
        if (1 & c && (a = r(a)), 8 & c || 4 & c && typeof a == "object" && a && a.__esModule)
          return a;
        var u = /* @__PURE__ */ Object.create(null);
        if (r.r(u), Object.defineProperty(u, "default", { enumerable: !0, value: a }), 2 & c && typeof a != "string")
          for (var p in a)
            r.d(u, p, (function(g) {
              return a[g];
            }).bind(null, p));
        return u;
      }, r.n = function(a) {
        var c = a && a.__esModule ? function() {
          return a.default;
        } : function() {
          return a;
        };
        return r.d(c, "a", c), c;
      }, r.o = function(a, c) {
        return Object.prototype.hasOwnProperty.call(a, c);
      }, r.p = "/", r(r.s = 4);
    }([function(t, n, r) {
      var a = r(1), c = r(2);
      typeof (c = c.__esModule ? c.default : c) == "string" && (c = [[t.i, c, ""]]);
      var u = { insert: "head", singleton: !1 };
      a(c, u), t.exports = c.locals || {};
    }, function(t, n, r) {
      var a, c = function() {
        return a === void 0 && (a = !!(window && document && document.all && !window.atob)), a;
      }, u = function() {
        var F = {};
        return function(Q) {
          if (F[Q] === void 0) {
            var U = document.querySelector(Q);
            if (window.HTMLIFrameElement && U instanceof window.HTMLIFrameElement)
              try {
                U = U.contentDocument.head;
              } catch {
                U = null;
              }
            F[Q] = U;
          }
          return F[Q];
        };
      }(), p = [];
      function g(F) {
        for (var Q = -1, U = 0; U < p.length; U++)
          if (p[U].identifier === F) {
            Q = U;
            break;
          }
        return Q;
      }
      function v(F, Q) {
        for (var U = {}, z = [], d = 0; d < F.length; d++) {
          var j = F[d], R = Q.base ? j[0] + Q.base : j[0], V = U[R] || 0, K = "".concat(R, " ").concat(V);
          U[R] = V + 1;
          var J = g(K), A = { css: j[1], media: j[2], sourceMap: j[3] };
          J !== -1 ? (p[J].references++, p[J].updater(A)) : p.push({ identifier: K, updater: I(A, Q), references: 1 }), z.push(K);
        }
        return z;
      }
      function k(F) {
        var Q = document.createElement("style"), U = F.attributes || {};
        if (U.nonce === void 0) {
          var z = r.nc;
          z && (U.nonce = z);
        }
        if (Object.keys(U).forEach(function(j) {
          Q.setAttribute(j, U[j]);
        }), typeof F.insert == "function")
          F.insert(Q);
        else {
          var d = u(F.insert || "head");
          if (!d)
            throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
          d.appendChild(Q);
        }
        return Q;
      }
      var y, T = (y = [], function(F, Q) {
        return y[F] = Q, y.filter(Boolean).join(`
`);
      });
      function x(F, Q, U, z) {
        var d = U ? "" : z.media ? "@media ".concat(z.media, " {").concat(z.css, "}") : z.css;
        if (F.styleSheet)
          F.styleSheet.cssText = T(Q, d);
        else {
          var j = document.createTextNode(d), R = F.childNodes;
          R[Q] && F.removeChild(R[Q]), R.length ? F.insertBefore(j, R[Q]) : F.appendChild(j);
        }
      }
      function M(F, Q, U) {
        var z = U.css, d = U.media, j = U.sourceMap;
        if (d ? F.setAttribute("media", d) : F.removeAttribute("media"), j && typeof btoa < "u" && (z += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(j)))), " */")), F.styleSheet)
          F.styleSheet.cssText = z;
        else {
          for (; F.firstChild; )
            F.removeChild(F.firstChild);
          F.appendChild(document.createTextNode(z));
        }
      }
      var L = null, Y = 0;
      function I(F, Q) {
        var U, z, d;
        if (Q.singleton) {
          var j = Y++;
          U = L || (L = k(Q)), z = x.bind(null, U, j, !1), d = x.bind(null, U, j, !0);
        } else
          U = k(Q), z = M.bind(null, U, Q), d = function() {
            (function(R) {
              if (R.parentNode === null)
                return !1;
              R.parentNode.removeChild(R);
            })(U);
          };
        return z(F), function(R) {
          if (R) {
            if (R.css === F.css && R.media === F.media && R.sourceMap === F.sourceMap)
              return;
            z(F = R);
          } else
            d();
        };
      }
      t.exports = function(F, Q) {
        (Q = Q || {}).singleton || typeof Q.singleton == "boolean" || (Q.singleton = c());
        var U = v(F = F || [], Q);
        return function(z) {
          if (z = z || [], Object.prototype.toString.call(z) === "[object Array]") {
            for (var d = 0; d < U.length; d++) {
              var j = g(U[d]);
              p[j].references--;
            }
            for (var R = v(z, Q), V = 0; V < U.length; V++) {
              var K = g(U[V]);
              p[K].references === 0 && (p[K].updater(), p.splice(K, 1));
            }
            U = R;
          }
        };
      };
    }, function(t, n, r) {
      (n = r(3)(!1)).push([t.i, `.cdx-underline {
    text-decoration: underline;
}
`, ""]), t.exports = n;
    }, function(t, n, r) {
      t.exports = function(a) {
        var c = [];
        return c.toString = function() {
          return this.map(function(u) {
            var p = function(g, v) {
              var k = g[1] || "", y = g[3];
              if (!y)
                return k;
              if (v && typeof btoa == "function") {
                var T = (M = y, L = btoa(unescape(encodeURIComponent(JSON.stringify(M)))), Y = "sourceMappingURL=data:application/json;charset=utf-8;base64,".concat(L), "/*# ".concat(Y, " */")), x = y.sources.map(function(I) {
                  return "/*# sourceURL=".concat(y.sourceRoot || "").concat(I, " */");
                });
                return [k].concat(x).concat([T]).join(`
`);
              }
              var M, L, Y;
              return [k].join(`
`);
            }(u, a);
            return u[2] ? "@media ".concat(u[2], " {").concat(p, "}") : p;
          }).join("");
        }, c.i = function(u, p, g) {
          typeof u == "string" && (u = [[null, u, ""]]);
          var v = {};
          if (g)
            for (var k = 0; k < this.length; k++) {
              var y = this[k][0];
              y != null && (v[y] = !0);
            }
          for (var T = 0; T < u.length; T++) {
            var x = [].concat(u[T]);
            g && v[x[0]] || (p && (x[2] ? x[2] = "".concat(p, " and ").concat(x[2]) : x[2] = p), c.push(x));
          }
        }, c;
      };
    }, function(t, n, r) {
      r.r(n), r.d(n, "default", function() {
        return u;
      }), r(0);
      function a(p) {
        return (a = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(g) {
          return typeof g;
        } : function(g) {
          return g && typeof Symbol == "function" && g.constructor === Symbol && g !== Symbol.prototype ? "symbol" : typeof g;
        })(p);
      }
      function c(p, g) {
        for (var v = 0; v < g.length; v++) {
          var k = g[v];
          k.enumerable = k.enumerable || !1, k.configurable = !0, "value" in k && (k.writable = !0), Object.defineProperty(p, (y = k.key, T = void 0, T = function(x, M) {
            if (a(x) !== "object" || x === null)
              return x;
            var L = x[Symbol.toPrimitive];
            if (L !== void 0) {
              var Y = L.call(x, M || "default");
              if (a(Y) !== "object")
                return Y;
              throw new TypeError("@@toPrimitive must return a primitive value.");
            }
            return (M === "string" ? String : Number)(x);
          }(y, "string"), a(T) === "symbol" ? T : String(T)), k);
        }
        var y, T;
      }
      var u = function() {
        function p(y) {
          var T = y.api;
          (function(x, M) {
            if (!(x instanceof M))
              throw new TypeError("Cannot call a class as a function");
          })(this, p), this.api = T, this.button = null, this.tag = "U", this.iconClasses = { base: this.api.styles.inlineToolButton, active: this.api.styles.inlineToolButtonActive };
        }
        var g, v, k;
        return g = p, k = [{ key: "CSS", get: function() {
          return "cdx-underline";
        } }, { key: "isInline", get: function() {
          return !0;
        } }, { key: "sanitize", get: function() {
          return { u: { class: p.CSS } };
        } }], (v = [{ key: "render", value: function() {
          return this.button = document.createElement("button"), this.button.type = "button", this.button.classList.add(this.iconClasses.base), this.button.innerHTML = this.toolboxIcon, this.button;
        } }, { key: "surround", value: function(y) {
          if (y) {
            var T = this.api.selection.findParentTag(this.tag, p.CSS);
            T ? this.unwrap(T) : this.wrap(y);
          }
        } }, { key: "wrap", value: function(y) {
          var T = document.createElement(this.tag);
          T.classList.add(p.CSS), T.appendChild(y.extractContents()), y.insertNode(T), this.api.selection.expandToTag(T);
        } }, { key: "unwrap", value: function(y) {
          this.api.selection.expandToTag(y);
          var T = window.getSelection(), x = T.getRangeAt(0), M = x.extractContents();
          y.parentNode.removeChild(y), x.insertNode(M), T.removeAllRanges(), T.addRange(x);
        } }, { key: "checkState", value: function() {
          var y = this.api.selection.findParentTag(this.tag, p.CSS);
          this.button.classList.toggle(this.iconClasses.active, !!y);
        } }, { key: "toolboxIcon", get: function() {
          return '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7.5V11.5C9 12.2956 9.31607 13.0587 9.87868 13.6213C10.4413 14.1839 11.2044 14.5 12 14.5C12.7956 14.5 13.5587 14.1839 14.1213 13.6213C14.6839 13.0587 15 12.2956 15 11.5V7.5"/><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7.71429 18H16.2857"/></svg>';
        } }]) && c(g.prototype, v), k && c(g, k), Object.defineProperty(g, "prototype", { writable: !1 }), p;
      }();
    }]).default;
  });
})(ou);
var Vm = ou.exports;
const Gm = /* @__PURE__ */ Dn(Vm);
var nu = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, () => (() => {
    var t = { 424: (c, u, p) => {
      p.d(u, { Z: () => T });
      var g = p(81), v = p.n(g), k = p(645), y = p.n(k)()(v());
      y.push([c.id, `.tooltip-tool__input{
  border: 0;
  border-radius: 0 0 4px 4px;
  border-top: 1px solid rgba(201,201,204,.48);
}

.tooltip-tool__span{
  padding: 3px;
  border-radius: 6px;
}

.tooltip-tool__underline{
  text-decoration: underline;
}

.tooltip-color::before {
  background-color: transparent;
}

.tooltip-color::after {
  background-color: transparent;
}

.tooltip-text-color {
  color: transparent;
}

.cdx-tooltip {
  display: inline-block;
}
`, ""]);
      const T = y;
    }, 645: (c) => {
      c.exports = function(u) {
        var p = [];
        return p.toString = function() {
          return this.map(function(g) {
            var v = "", k = g[5] !== void 0;
            return g[4] && (v += "@supports (".concat(g[4], ") {")), g[2] && (v += "@media ".concat(g[2], " {")), k && (v += "@layer".concat(g[5].length > 0 ? " ".concat(g[5]) : "", " {")), v += u(g), k && (v += "}"), g[2] && (v += "}"), g[4] && (v += "}"), v;
          }).join("");
        }, p.i = function(g, v, k, y, T) {
          typeof g == "string" && (g = [[null, g, void 0]]);
          var x = {};
          if (k)
            for (var M = 0; M < this.length; M++) {
              var L = this[M][0];
              L != null && (x[L] = !0);
            }
          for (var Y = 0; Y < g.length; Y++) {
            var I = [].concat(g[Y]);
            k && x[I[0]] || (T !== void 0 && (I[5] === void 0 || (I[1] = "@layer".concat(I[5].length > 0 ? " ".concat(I[5]) : "", " {").concat(I[1], "}")), I[5] = T), v && (I[2] && (I[1] = "@media ".concat(I[2], " {").concat(I[1], "}")), I[2] = v), y && (I[4] ? (I[1] = "@supports (".concat(I[4], ") {").concat(I[1], "}"), I[4] = y) : I[4] = "".concat(y)), p.push(I));
          }
        }, p;
      };
    }, 81: (c) => {
      c.exports = function(u) {
        return u[1];
      };
    }, 379: (c) => {
      var u = [];
      function p(k) {
        for (var y = -1, T = 0; T < u.length; T++)
          if (u[T].identifier === k) {
            y = T;
            break;
          }
        return y;
      }
      function g(k, y) {
        for (var T = {}, x = [], M = 0; M < k.length; M++) {
          var L = k[M], Y = y.base ? L[0] + y.base : L[0], I = T[Y] || 0, F = "".concat(Y, " ").concat(I);
          T[Y] = I + 1;
          var Q = p(F), U = { css: L[1], media: L[2], sourceMap: L[3], supports: L[4], layer: L[5] };
          if (Q !== -1)
            u[Q].references++, u[Q].updater(U);
          else {
            var z = v(U, y);
            y.byIndex = M, u.splice(M, 0, { identifier: F, updater: z, references: 1 });
          }
          x.push(F);
        }
        return x;
      }
      function v(k, y) {
        var T = y.domAPI(y);
        return T.update(k), function(x) {
          if (x) {
            if (x.css === k.css && x.media === k.media && x.sourceMap === k.sourceMap && x.supports === k.supports && x.layer === k.layer)
              return;
            T.update(k = x);
          } else
            T.remove();
        };
      }
      c.exports = function(k, y) {
        var T = g(k = k || [], y = y || {});
        return function(x) {
          x = x || [];
          for (var M = 0; M < T.length; M++) {
            var L = p(T[M]);
            u[L].references--;
          }
          for (var Y = g(x, y), I = 0; I < T.length; I++) {
            var F = p(T[I]);
            u[F].references === 0 && (u[F].updater(), u.splice(F, 1));
          }
          T = Y;
        };
      };
    }, 569: (c) => {
      var u = {};
      c.exports = function(p, g) {
        var v = function(k) {
          if (u[k] === void 0) {
            var y = document.querySelector(k);
            if (window.HTMLIFrameElement && y instanceof window.HTMLIFrameElement)
              try {
                y = y.contentDocument.head;
              } catch {
                y = null;
              }
            u[k] = y;
          }
          return u[k];
        }(p);
        if (!v)
          throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
        v.appendChild(g);
      };
    }, 216: (c) => {
      c.exports = function(u) {
        var p = document.createElement("style");
        return u.setAttributes(p, u.attributes), u.insert(p, u.options), p;
      };
    }, 575: (c) => {
      c.exports = function(u, p) {
        Object.keys(p).forEach(function(g) {
          u.setAttribute(g, p[g]);
        });
      };
    }, 795: (c) => {
      c.exports = function(u) {
        var p = u.insertStyleElement(u);
        return { update: function(g) {
          (function(v, k, y) {
            var T = "";
            y.supports && (T += "@supports (".concat(y.supports, ") {")), y.media && (T += "@media ".concat(y.media, " {"));
            var x = y.layer !== void 0;
            x && (T += "@layer".concat(y.layer.length > 0 ? " ".concat(y.layer) : "", " {")), T += y.css, x && (T += "}"), y.media && (T += "}"), y.supports && (T += "}");
            var M = y.sourceMap;
            M && typeof btoa < "u" && (T += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(M)))), " */")), k.styleTagTransform(T, v, k.options);
          })(p, u, g);
        }, remove: function() {
          (function(g) {
            if (g.parentNode === null)
              return !1;
            g.parentNode.removeChild(g);
          })(p);
        } };
      };
    }, 589: (c) => {
      c.exports = function(u, p) {
        if (p.styleSheet)
          p.styleSheet.cssText = u;
        else {
          for (; p.firstChild; )
            p.removeChild(p.firstChild);
          p.appendChild(document.createTextNode(u));
        }
      };
    }, 980: (c) => {
      c.exports = '<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="16" height="14" viewBox="0 -5 21 30"><path fill="currentColor" stroke-width="0" d="M4,2H20A2,2 0 0,1 22,4V16A2,2 0 0,1 20,18H16L12,22L8,18H4A2,2 0 0,1 2,16V4A2,2 0 0,1 4,2M4,4V16H8.83L12,19.17L15.17,16H20V4H4M6,7H18V9H6V7M6,11H16V13H6V11Z"></path></svg>';
    } }, n = {};
    function r(c) {
      var u = n[c];
      if (u !== void 0)
        return u.exports;
      var p = n[c] = { id: c, exports: {} };
      return t[c](p, p.exports, r), p.exports;
    }
    r.n = (c) => {
      var u = c && c.__esModule ? () => c.default : () => c;
      return r.d(u, { a: u }), u;
    }, r.d = (c, u) => {
      for (var p in u)
        r.o(u, p) && !r.o(c, p) && Object.defineProperty(c, p, { enumerable: !0, get: u[p] });
    }, r.o = (c, u) => Object.prototype.hasOwnProperty.call(c, u);
    var a = {};
    return (() => {
      r.d(a, { default: () => d });
      var c = r(379), u = r.n(c), p = r(795), g = r.n(p), v = r(569), k = r.n(v), y = r(575), T = r.n(y), x = r(216), M = r.n(x), L = r(589), Y = r.n(L), I = r(424), F = { attributes: { id: "editorjs-tooltip" } };
      F.styleTagTransform = Y(), F.setAttributes = T(), F.insert = k().bind(null, "head"), F.domAPI = g(), F.insertStyleElement = M(), u()(I.Z, F), I.Z && I.Z.locals && I.Z.locals;
      var Q = r(980), U = r.n(Q);
      function z(j, R) {
        for (var V = 0; V < R.length; V++) {
          var K = R[V];
          K.enumerable = K.enumerable || !1, K.configurable = !0, "value" in K && (K.writable = !0), Object.defineProperty(j, K.key, K);
        }
      }
      var d = function() {
        function j(J) {
          var A = J.api, se = J.config, Z = se === void 0 ? {} : se;
          (function(oe, fe) {
            if (!(oe instanceof fe))
              throw new TypeError("Cannot call a class as a function");
          })(this, j), this.api = A, this.button = null, this._state = !1, this.spanTooltip = null;
          var ne = Z.location, le = ne === void 0 ? "bottom" : ne;
          this.tooltipLocation = le, this.highlightColor = Z.highlightColor, this.underline = !!Z.underline && Z.underline, this.backgroundColor = Z.backgroundColor, this.textColor = Z.textColor, this.editorId = Z.holder ? Z.holder : "editorjs", this.tag = "SPAN", this.CSS = { input: "tooltip-tool__input", tooltip: "cdx-tooltip", span: "tooltip-tool__span", underline: "tooltip-tool__underline" }, this.tooltipsObserver(), (this.backgroundColor || this.textColor) && this.customTooltip();
        }
        var R, V, K;
        return R = j, V = [{ key: "state", get: function() {
          return this._state;
        }, set: function(J) {
          this._state = J;
          var A = this.button, se = this.api.styles.inlineToolButtonActive;
          A.classList.toggle(se, J);
        } }, { key: "customTooltip", value: function() {
          var J = this, A = document.querySelector(".ct"), se = document.querySelector(".ct__content");
          new MutationObserver(function(Z) {
            Z.forEach(function(ne) {
              if (ne.type === "childList") {
                var le = se.textContent;
                document.querySelector('[data-tooltip="'.concat(le, '"]')) ? (J.backgroundColor && J.setTooltipColor(), J.textColor && J.setTooltipTextColor()) : (A.classList.remove("tooltip-color"), se.classList.remove("tooltip-text-color"));
              }
            });
          }).observe(se, { childList: !0 });
        } }, { key: "tooltipSheet", value: function() {
          var J = document.styleSheets;
          return Object.values(J).filter(function(A) {
            return A.ownerNode.id === "editorjs-tooltip";
          });
        } }, { key: "tooltipCssRule", value: function(J) {
          var A = this.tooltipSheet();
          return Object.values(A[0].cssRules).filter(function(se) {
            return se.selectorText === J;
          });
        } }, { key: "setTooltipColor", value: function() {
          var J = document.querySelector(".ct"), A = this.tooltipCssRule(".tooltip-color::before"), se = this.tooltipCssRule(".tooltip-color::after");
          A[0].style.setProperty("background-color", this.backgroundColor), se[0].style.setProperty("background-color", this.backgroundColor), J.classList.add("tooltip-color");
        } }, { key: "setTooltipTextColor", value: function() {
          var J = this.tooltipCssRule(".tooltip-text-color"), A = document.querySelector(".ct__content");
          J[0].style.setProperty("color", this.textColor), A.classList.add("tooltip-text-color");
        } }, { key: "tooltipsObserver", value: function() {
          var J = this, A = document.getElementById(this.editorId);
          new MutationObserver(function(se) {
            se.forEach(function(Z) {
              Z.type === "childList" && Z.target.classList.contains("codex-editor__redactor") && document.querySelectorAll(".cdx-tooltip").forEach(function(ne) {
                return J.createTooltip(ne.dataset.tooltip, ne);
              });
            });
          }).observe(A, { childList: !0, subtree: !0 });
        } }, { key: "createTooltip", value: function(J) {
          var A = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.spanTooltip;
          this.spanTooltip ? (this.spanTooltip.dataset.tooltip = J, this.setBackgroundColor(this.spanTooltip), this.setUnderlineDecoration(this.spanTooltip)) : (this.setBackgroundColor(A), this.setUnderlineDecoration(A));
          var se = this.tooltipLocation;
          this.api.tooltip.onHover(A, J, { placement: se });
        } }, { key: "setBackgroundColor", value: function(J) {
          var A = J;
          A.childElementCount > 0 ? (A.firstChild.classList.add(this.CSS.span), A.firstChild.style.background = this.highlightColor) : (A.classList.add(this.CSS.span), A.style.background = this.highlightColor);
        } }, { key: "setUnderlineDecoration", value: function(J) {
          var A = J;
          this.underline && (A.childElementCount > 0 ? A.firstChild.classList.add(this.CSS.underline) : A.classList.add(this.CSS.underline));
        } }, { key: "render", value: function() {
          this.button = document.createElement("button"), this.button.type = "button", this.button.innerHTML = U();
          var J = this.api.styles.inlineToolButton;
          return this.button.classList.add(J), this.button;
        } }, { key: "surround", value: function(J) {
          this.state ? this.unwrap(J) : this.wrap(J);
        } }, { key: "wrap", value: function(J) {
          var A = J.extractContents();
          this.spanTooltip = document.createElement(this.tag), this.spanTooltip.classList.add(this.CSS.tooltip), this.spanTooltip.appendChild(A), J.insertNode(this.spanTooltip), this.api.selection.expandToTag(this.spanTooltip);
        } }, { key: "unwrap", value: function(J) {
          this.spanTooltip = this.api.selection.findParentTag(this.tag, this.CSS.tooltip);
          var A = J.extractContents();
          this.spanTooltip.remove(), J.insertNode(A);
        } }, { key: "checkState", value: function() {
          this.spanTooltip = this.api.selection.findParentTag(this.tag), this.state = !!this.spanTooltip, this.state ? this.showActions() : this.hideActions();
        } }, { key: "renderActions", value: function() {
          if (this.spanTooltip = this.api.selection.findParentTag(this.tag), this.tooltipInput = document.createElement("input"), this.tooltipInput.placeholder = "Add a tooltip", this.tooltipInput.classList.add(this.api.styles.input), this.tooltipInput.classList.add(this.CSS.input), this.spanTooltip) {
            var J = this.spanTooltip.dataset.tooltip;
            this.tooltipInput.value = J;
          }
          return this.tooltipInput.hidden = !0, this.tooltipInput;
        } }, { key: "showActions", value: function() {
          var J = this;
          this.tooltipInput.hidden = !1, this.api.listeners.on(this.tooltipInput, "keydown", function(A) {
            if (A.key === "Enter") {
              var se = J.tooltipInput.value;
              J.createTooltip(se), J.closeToolbar();
            }
          }, !1);
        } }, { key: "hideActions", value: function() {
          this.tooltipInput.hidden = !0;
        } }, { key: "closeToolbar", value: function() {
          document.querySelector(".ce-inline-toolbar--showed").classList.remove("ce-inline-toolbar--showed");
        } }], K = [{ key: "isInline", get: function() {
          return !0;
        } }, { key: "sanitize", get: function() {
          return { span: function(J) {
            return J.classList.remove("tooltip-tool__span", "tooltip-tool__underline"), { class: !0, "data-tooltip": !0 };
          } };
        } }], V && z(R.prototype, V), K && z(R, K), Object.defineProperty(R, "prototype", { writable: !1 }), j;
      }();
    })(), a.default;
  })());
})(nu);
var Zm = nu.exports;
const Km = /* @__PURE__ */ Dn(Zm);
var iu = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(window, function() {
    return function(t) {
      var n = {};
      function r(a) {
        if (n[a])
          return n[a].exports;
        var c = n[a] = { i: a, l: !1, exports: {} };
        return t[a].call(c.exports, c, c.exports, r), c.l = !0, c.exports;
      }
      return r.m = t, r.c = n, r.d = function(a, c, u) {
        r.o(a, c) || Object.defineProperty(a, c, { enumerable: !0, get: u });
      }, r.r = function(a) {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
      }, r.t = function(a, c) {
        if (1 & c && (a = r(a)), 8 & c || 4 & c && typeof a == "object" && a && a.__esModule)
          return a;
        var u = /* @__PURE__ */ Object.create(null);
        if (r.r(u), Object.defineProperty(u, "default", { enumerable: !0, value: a }), 2 & c && typeof a != "string")
          for (var p in a)
            r.d(u, p, (function(g) {
              return a[g];
            }).bind(null, p));
        return u;
      }, r.n = function(a) {
        var c = a && a.__esModule ? function() {
          return a.default;
        } : function() {
          return a;
        };
        return r.d(c, "a", c), c;
      }, r.o = function(a, c) {
        return Object.prototype.hasOwnProperty.call(a, c);
      }, r.p = "/", r(r.s = 4);
    }([function(t, n, r) {
      var a = r(1), c = r(2);
      typeof (c = c.__esModule ? c.default : c) == "string" && (c = [[t.i, c, ""]]);
      var u = { insert: "head", singleton: !1 };
      a(c, u), t.exports = c.locals || {};
    }, function(t, n, r) {
      var a, c = function() {
        return a === void 0 && (a = !!(window && document && document.all && !window.atob)), a;
      }, u = function() {
        var F = {};
        return function(Q) {
          if (F[Q] === void 0) {
            var U = document.querySelector(Q);
            if (window.HTMLIFrameElement && U instanceof window.HTMLIFrameElement)
              try {
                U = U.contentDocument.head;
              } catch {
                U = null;
              }
            F[Q] = U;
          }
          return F[Q];
        };
      }(), p = [];
      function g(F) {
        for (var Q = -1, U = 0; U < p.length; U++)
          if (p[U].identifier === F) {
            Q = U;
            break;
          }
        return Q;
      }
      function v(F, Q) {
        for (var U = {}, z = [], d = 0; d < F.length; d++) {
          var j = F[d], R = Q.base ? j[0] + Q.base : j[0], V = U[R] || 0, K = "".concat(R, " ").concat(V);
          U[R] = V + 1;
          var J = g(K), A = { css: j[1], media: j[2], sourceMap: j[3] };
          J !== -1 ? (p[J].references++, p[J].updater(A)) : p.push({ identifier: K, updater: I(A, Q), references: 1 }), z.push(K);
        }
        return z;
      }
      function k(F) {
        var Q = document.createElement("style"), U = F.attributes || {};
        if (U.nonce === void 0) {
          var z = r.nc;
          z && (U.nonce = z);
        }
        if (Object.keys(U).forEach(function(j) {
          Q.setAttribute(j, U[j]);
        }), typeof F.insert == "function")
          F.insert(Q);
        else {
          var d = u(F.insert || "head");
          if (!d)
            throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
          d.appendChild(Q);
        }
        return Q;
      }
      var y, T = (y = [], function(F, Q) {
        return y[F] = Q, y.filter(Boolean).join(`
`);
      });
      function x(F, Q, U, z) {
        var d = U ? "" : z.media ? "@media ".concat(z.media, " {").concat(z.css, "}") : z.css;
        if (F.styleSheet)
          F.styleSheet.cssText = T(Q, d);
        else {
          var j = document.createTextNode(d), R = F.childNodes;
          R[Q] && F.removeChild(R[Q]), R.length ? F.insertBefore(j, R[Q]) : F.appendChild(j);
        }
      }
      function M(F, Q, U) {
        var z = U.css, d = U.media, j = U.sourceMap;
        if (d ? F.setAttribute("media", d) : F.removeAttribute("media"), j && btoa && (z += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(j)))), " */")), F.styleSheet)
          F.styleSheet.cssText = z;
        else {
          for (; F.firstChild; )
            F.removeChild(F.firstChild);
          F.appendChild(document.createTextNode(z));
        }
      }
      var L = null, Y = 0;
      function I(F, Q) {
        var U, z, d;
        if (Q.singleton) {
          var j = Y++;
          U = L || (L = k(Q)), z = x.bind(null, U, j, !1), d = x.bind(null, U, j, !0);
        } else
          U = k(Q), z = M.bind(null, U, Q), d = function() {
            (function(R) {
              if (R.parentNode === null)
                return !1;
              R.parentNode.removeChild(R);
            })(U);
          };
        return z(F), function(R) {
          if (R) {
            if (R.css === F.css && R.media === F.media && R.sourceMap === F.sourceMap)
              return;
            z(F = R);
          } else
            d();
        };
      }
      t.exports = function(F, Q) {
        (Q = Q || {}).singleton || typeof Q.singleton == "boolean" || (Q.singleton = c());
        var U = v(F = F || [], Q);
        return function(z) {
          if (z = z || [], Object.prototype.toString.call(z) === "[object Array]") {
            for (var d = 0; d < U.length; d++) {
              var j = g(U[d]);
              p[j].references--;
            }
            for (var R = v(z, Q), V = 0; V < U.length; V++) {
              var K = g(U[V]);
              p[K].references === 0 && (p[K].updater(), p.splice(K, 1));
            }
            U = R;
          }
        };
      };
    }, function(t, n, r) {
      (n = r(3)(!1)).push([t.i, `.cdx-strikethrough {
    text-decoration: line-through;
}
`, ""]), t.exports = n;
    }, function(t, n, r) {
      t.exports = function(a) {
        var c = [];
        return c.toString = function() {
          return this.map(function(u) {
            var p = function(g, v) {
              var k = g[1] || "", y = g[3];
              if (!y)
                return k;
              if (v && typeof btoa == "function") {
                var T = (M = y, L = btoa(unescape(encodeURIComponent(JSON.stringify(M)))), Y = "sourceMappingURL=data:application/json;charset=utf-8;base64,".concat(L), "/*# ".concat(Y, " */")), x = y.sources.map(function(I) {
                  return "/*# sourceURL=".concat(y.sourceRoot || "").concat(I, " */");
                });
                return [k].concat(x).concat([T]).join(`
`);
              }
              var M, L, Y;
              return [k].join(`
`);
            }(u, a);
            return u[2] ? "@media ".concat(u[2], " {").concat(p, "}") : p;
          }).join("");
        }, c.i = function(u, p, g) {
          typeof u == "string" && (u = [[null, u, ""]]);
          var v = {};
          if (g)
            for (var k = 0; k < this.length; k++) {
              var y = this[k][0];
              y != null && (v[y] = !0);
            }
          for (var T = 0; T < u.length; T++) {
            var x = [].concat(u[T]);
            g && v[x[0]] || (p && (x[2] ? x[2] = "".concat(p, " and ").concat(x[2]) : x[2] = p), c.push(x));
          }
        }, c;
      };
    }, function(t, n, r) {
      r.r(n), r.d(n, "default", function() {
        return u;
      }), r(0);
      function a(p, g) {
        for (var v = 0; v < g.length; v++) {
          var k = g[v];
          k.enumerable = k.enumerable || !1, k.configurable = !0, "value" in k && (k.writable = !0), Object.defineProperty(p, k.key, k);
        }
      }
      function c(p, g, v) {
        return g && a(p.prototype, g), v && a(p, v), p;
      }
      var u = function() {
        function p(g) {
          var v = g.api;
          (function(k, y) {
            if (!(k instanceof y))
              throw new TypeError("Cannot call a class as a function");
          })(this, p), this.api = v, this.button = null, this.tag = "S", this.iconClasses = { base: this.api.styles.inlineToolButton, active: this.api.styles.inlineToolButtonActive };
        }
        return c(p, null, [{ key: "CSS", get: function() {
          return "cdx-strikethrough";
        } }]), c(p, [{ key: "render", value: function() {
          return this.button = document.createElement("button"), this.button.type = "button", this.button.classList.add(this.iconClasses.base), this.button.innerHTML = this.toolboxIcon, this.button;
        } }, { key: "surround", value: function(g) {
          if (g) {
            var v = this.api.selection.findParentTag(this.tag, p.CSS);
            v ? this.unwrap(v) : this.wrap(g);
          }
        } }, { key: "wrap", value: function(g) {
          var v = document.createElement(this.tag);
          v.classList.add(p.CSS), v.appendChild(g.extractContents()), g.insertNode(v), this.api.selection.expandToTag(v);
        } }, { key: "unwrap", value: function(g) {
          this.api.selection.expandToTag(g);
          var v = window.getSelection(), k = v.getRangeAt(0), y = k.extractContents();
          g.parentNode.removeChild(g), k.insertNode(y), v.removeAllRanges(), v.addRange(k);
        } }, { key: "checkState", value: function() {
          var g = this.api.selection.findParentTag(this.tag, p.CSS);
          this.button.classList.toggle(this.iconClasses.active, !!g);
        } }, { key: "toolboxIcon", get: function() {
          return '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.5 8.50001C13.5 7 10.935 6.66476 9.75315 7.79706C9.27092 8.25909 9 8.88574 9 9.53915C9 10.1926 9.27092 10.8192 9.75315 11.2812C10.9835 12.46 13.0165 11.5457 14.2468 12.7244C14.7291 13.1865 15 13.8131 15 14.4665C15 15.1199 14.7291 15.7466 14.2468 16.2086C12.8659 17.5317 10 17.5 9 16"/><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 12H18"/></svg>';
        } }], [{ key: "isInline", get: function() {
          return !0;
        } }, { key: "sanitize", get: function() {
          return { s: { class: p.CSS } };
        } }]), p;
      }();
    }]).default;
  });
})(iu);
var Xm = iu.exports;
const Jm = /* @__PURE__ */ Dn(Xm);
var ru = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, () => (() => {
    var t = { 424: (a, c, u) => {
      (c = u(645)(!1)).push([a.id, `.picker_wrapper.popup {
  z-index: 99;
  width: 170px;
  margin: 0;
  box-shadow: 0 0 10px 1px #eaeaea;
  background: #ffffff;
}

.picker_arrow {
  display: none;
}

.layout_default .picker_slider, .layout_default .picker_selector {
  padding: 5px;
}

.colorPlugin.ce-inline-tool {
  width: 32px;
  border-radius: 3px;
}

.colorPlugin.ce-inline-tool--active svg {
  fill: #3c99ff;
}

#color-left-btn {
  height: 35px;
  width: 18px;
  font-weight: 600;
  display: flex;
  align-items: center;
}

#color-left-btn:hover {
  border-radius: 5px 0 0 5px;
  background: rgba(203, 203, 203, 0.49);
}

#color-text {
  padding: 0 4px;
}

#color-btn-text {
  height: 15px;
}

`, ""]), a.exports = c;
    }, 645: (a) => {
      a.exports = function(c) {
        var u = [];
        return u.toString = function() {
          return this.map(function(p) {
            var g = function(v, k) {
              var y, T, x, M = v[1] || "", L = v[3];
              if (!L)
                return M;
              if (k && typeof btoa == "function") {
                var Y = (y = L, T = btoa(unescape(encodeURIComponent(JSON.stringify(y)))), x = "sourceMappingURL=data:application/json;charset=utf-8;base64,".concat(T), "/*# ".concat(x, " */")), I = L.sources.map(function(F) {
                  return "/*# sourceURL=".concat(L.sourceRoot || "").concat(F, " */");
                });
                return [M].concat(I).concat([Y]).join(`
`);
              }
              return [M].join(`
`);
            }(p, c);
            return p[2] ? "@media ".concat(p[2], " {").concat(g, "}") : g;
          }).join("");
        }, u.i = function(p, g, v) {
          typeof p == "string" && (p = [[null, p, ""]]);
          var k = {};
          if (v)
            for (var y = 0; y < this.length; y++) {
              var T = this[y][0];
              T != null && (k[T] = !0);
            }
          for (var x = 0; x < p.length; x++) {
            var M = [].concat(p[x]);
            v && k[M[0]] || (g && (M[2] ? M[2] = "".concat(g, " and ").concat(M[2]) : M[2] = g), u.push(M));
          }
        }, u;
      };
    }, 548: (a, c, u) => {
      var p = u(379), g = u(424);
      typeof (g = g.__esModule ? g.default : g) == "string" && (g = [[a.id, g, ""]]), p(g, { insert: "head", singleton: !1 }), a.exports = g.locals || {};
    }, 379: (a, c, u) => {
      var p, g = function() {
        var U = {};
        return function(z) {
          if (U[z] === void 0) {
            var d = document.querySelector(z);
            if (window.HTMLIFrameElement && d instanceof window.HTMLIFrameElement)
              try {
                d = d.contentDocument.head;
              } catch {
                d = null;
              }
            U[z] = d;
          }
          return U[z];
        };
      }(), v = [];
      function k(U) {
        for (var z = -1, d = 0; d < v.length; d++)
          if (v[d].identifier === U) {
            z = d;
            break;
          }
        return z;
      }
      function y(U, z) {
        for (var d = {}, j = [], R = 0; R < U.length; R++) {
          var V = U[R], K = z.base ? V[0] + z.base : V[0], J = d[K] || 0, A = "".concat(K, " ").concat(J);
          d[K] = J + 1;
          var se = k(A), Z = { css: V[1], media: V[2], sourceMap: V[3] };
          se !== -1 ? (v[se].references++, v[se].updater(Z)) : v.push({ identifier: A, updater: Q(Z, z), references: 1 }), j.push(A);
        }
        return j;
      }
      function T(U) {
        var z = document.createElement("style"), d = U.attributes || {};
        if (d.nonce === void 0) {
          var j = u.nc;
          j && (d.nonce = j);
        }
        if (Object.keys(d).forEach(function(V) {
          z.setAttribute(V, d[V]);
        }), typeof U.insert == "function")
          U.insert(z);
        else {
          var R = g(U.insert || "head");
          if (!R)
            throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
          R.appendChild(z);
        }
        return z;
      }
      var x, M = (x = [], function(U, z) {
        return x[U] = z, x.filter(Boolean).join(`
`);
      });
      function L(U, z, d, j) {
        var R = d ? "" : j.media ? "@media ".concat(j.media, " {").concat(j.css, "}") : j.css;
        if (U.styleSheet)
          U.styleSheet.cssText = M(z, R);
        else {
          var V = document.createTextNode(R), K = U.childNodes;
          K[z] && U.removeChild(K[z]), K.length ? U.insertBefore(V, K[z]) : U.appendChild(V);
        }
      }
      function Y(U, z, d) {
        var j = d.css, R = d.media, V = d.sourceMap;
        if (R ? U.setAttribute("media", R) : U.removeAttribute("media"), V && typeof btoa < "u" && (j += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(V)))), " */")), U.styleSheet)
          U.styleSheet.cssText = j;
        else {
          for (; U.firstChild; )
            U.removeChild(U.firstChild);
          U.appendChild(document.createTextNode(j));
        }
      }
      var I = null, F = 0;
      function Q(U, z) {
        var d, j, R;
        if (z.singleton) {
          var V = F++;
          d = I || (I = T(z)), j = L.bind(null, d, V, !1), R = L.bind(null, d, V, !0);
        } else
          d = T(z), j = Y.bind(null, d, z), R = function() {
            (function(K) {
              if (K.parentNode === null)
                return !1;
              K.parentNode.removeChild(K);
            })(d);
          };
        return j(U), function(K) {
          if (K) {
            if (K.css === U.css && K.media === U.media && K.sourceMap === U.sourceMap)
              return;
            j(U = K);
          } else
            R();
        };
      }
      a.exports = function(U, z) {
        (z = z || {}).singleton || typeof z.singleton == "boolean" || (z.singleton = (p === void 0 && (p = !!(window && document && document.all && !window.atob)), p));
        var d = y(U = U || [], z);
        return function(j) {
          if (j = j || [], Object.prototype.toString.call(j) === "[object Array]") {
            for (var R = 0; R < d.length; R++) {
              var V = k(d[R]);
              v[V].references--;
            }
            for (var K = y(j, z), J = 0; J < d.length; J++) {
              var A = k(d[J]);
              v[A].references === 0 && (v[A].updater(), v.splice(A, 1));
            }
            d = K;
          }
        };
      };
    }, 452: (a) => {
      a.exports = { markerIcon: `<svg fill="#000000" height="34px" width="34px" version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" 
	 viewBox="0 0 491.644 491.644" xml:space="preserve">
<g>
	<path d="M456.623,2.282c-42.758-20.283-141.107,96.84-223.473,264.224c-2.35,4.776-2.686,10.294-0.936,15.32
		c1.75,5.026,5.442,9.145,10.251,11.426L366.758,352.2c4.809,2.281,10.332,2.538,15.333,0.714c5.001-1.825,9.059-5.579,11.272-10.42
		C470.883,172.829,499.385,22.562,456.623,2.282z"/>
	<path d="M34.71,461.799l-17.257,16.708c-2.225,2.17-2.934,5.475-1.773,8.363c1.179,2.886,3.985,4.773,7.099,4.773h160.887
		c-1.364-5.043-0.921-10.445,1.391-15.306l7.919-16.692H40.036C38.036,459.646,36.129,460.419,34.71,461.799z"/>
	<path d="M264.766,448.864l-32.615-15.458c-1.046-0.502-2.161-0.744-3.257-0.744c-2.87,0-5.611,1.614-6.901,4.372l-22.001,46.384
		c-0.871,1.789-0.723,3.895,0.341,5.564c1.046,1.661,2.888,2.661,4.855,2.661h0.046l44.275-0.378
		c2.206-0.016,4.206-1.299,5.159-3.292l13.724-28.925c0.856-1.838,0.967-3.936,0.29-5.846
		C268.004,451.292,266.585,449.728,264.766,448.864z"/>
	<path d="M348.445,366.038l-112.572-51.392c-8.909-4.067-19.434-0.227-23.63,8.622c-2.551,5.378-3.58,11.353-2.975,17.275
		l5.2,50.909c0.703,6.882,4.983,12.884,11.261,15.792l60.031,27.797c6.688,3.097,14.548,2.179,20.343-2.375l45.983-36.137
		c4.931-3.875,7.487-10.041,6.743-16.269C358.086,374.032,354.151,368.642,348.445,366.038z"/>
</g>
</svg>`, textIcon: '<svg fill="#000000" viewBox="-6 0 512 512" xmlns="http://www.w3.org/2000/svg"><g stroke-width="0"></g><g stroke-linecap="round" stroke-linejoin="round"></g><g><title>text</title><path d="M365 432L328 352 172 352 135 432 64 432 227 80 272 80 436 432 365 432ZM201 288L299 288 250 183 201 288Z"></path></g></svg>' };
    }, 138: (a, c, u) => {
      const p = u(442), { markerIcon: g, textIcon: v } = u(452), { getDefaultColorCache: k, handleCSSVariables: y } = u(697);
      u(548).toString(), a.exports = class {
        constructor({ config: T, api: x }) {
          this.api = x, this.config = T, this.clickedOnLeft = !1, this.pluginType = this.config.type || "text", this.parentTag = this.pluginType === "marker" ? "MARK" : "FONT", this.hasCustomPicker = this.config.customPicker || !1, this.color = y(k(this.config.defaultColor, this.pluginType)), this.picker = null, this.icon = null, this.button = null, this.iconClasses = { base: this.api.styles.inlineToolButton, active: this.api.styles.inlineToolButtonActive };
        }
        static get isInline() {
          return !0;
        }
        render() {
          return this.button = document.createElement("button"), this.button.type = "button", this.button.classList.add("colorPlugin"), this.button.classList.add(this.iconClasses.base), this.button.appendChild(this.createLeftButton()), this.button.appendChild(this.createRightButton(this)), this.button;
        }
        createLeftButton() {
          return this.icon || (this.icon = document.createElement("div"), this.icon.id = "color-left-btn", this.icon.appendChild(this.createButtonIcon()), this.icon.addEventListener("click", () => this.clickedOnLeft = !0)), this.icon;
        }
        createButtonIcon() {
          const T = document.createElement("div");
          T.id = "color-btn-text";
          const x = this.pluginType === "marker" ? g : v;
          return T.innerHTML = this.config.icon || x, T;
        }
        createRightButton(T) {
          return this.picker || (this.picker = new p.ColorPlugin({ onColorPicked: function(x) {
            T.color = x;
          }, hasCustomPicker: this.hasCustomPicker, defaultColor: this.config.defaultColor, colorCollections: this.config.colorCollections, type: this.pluginType })), this.picker;
        }
        surround(T) {
          if (!T)
            return;
          const x = this.api.selection.findParentTag("SPAN");
          x && this.unwrap(x);
          const M = this.api.selection.findParentTag(this.parentTag);
          M ? this.unwrap(M) : this.wrap(T), this.clickedOnLeft = !1;
        }
        wrap(T) {
          const x = T.extractContents(), M = document.createElement(this.parentTag);
          M.appendChild(x), T.insertNode(M), this.pluginType === "marker" ? this.wrapMarker(M) : this.wrapTextColor(M), this.api.selection.expandToTag(M);
        }
        wrapMarker(T) {
          T.style.backgroundColor = this.color;
          const x = this.api.selection.findParentTag("FONT");
          x && (T.style.color = x.style.color);
        }
        wrapTextColor(T) {
          T.style.color = this.color;
        }
        unwrap(T) {
          this.api.selection.expandToTag(T);
          const x = window.getSelection(), M = x.getRangeAt(0), L = M.extractContents();
          this.clickedOnLeft ? this.removeWrapper(T) : this.updateWrapper(T), M.insertNode(L), x.removeAllRanges(), x.addRange(M);
        }
        updateWrapper(T) {
          this.pluginType === "marker" ? T.style.backgroundColor = this.color : T.style.color = this.color;
        }
        removeWrapper(T) {
          T.parentNode.removeChild(T);
        }
        checkState() {
          const T = this.api.selection.findParentTag("SPAN"), x = this.api.selection.findParentTag(this.parentTag);
          let M = T ? this.handleLegacyWrapper(T, x) : x;
          return this.button.classList.toggle(this.iconClasses.active, !!M), !!M;
        }
        handleLegacyWrapper(T, x) {
          return this.pluginType === "marker" ? T : x & T;
        }
        static get sanitize() {
          return { font: !0, span: !0, mark: !0 };
        }
        clear() {
          this.picker = null, this.icon = null;
        }
      };
    }, 442: (a, c, u) => {
      u.r(c), u.d(c, { ColorPlugin: () => x });
      class p extends HTMLElement {
        static get observedAttributes() {
          return ["disabled", "icon", "loading", "href", "htmltype"];
        }
        constructor() {
          super(), this.attachShadow({ mode: "open" }).innerHTML = `
        <style>
        :host{ 
            position:relative; 
            display:inline-flex; 
            padding: .25em .625em;
            box-sizing:border-box; 
            vertical-align: middle;
            line-height: 1.8;
            width: 5px;
            overflow:hidden; 
            align-items:center;
            justify-content: center;
            font-size: 14px; 
            color: var(--fontColor,#333);  
            border-radius: var(--borderRadius,.25em);
            background: var(--fontColor,#333); 
            transition:background .3s,box-shadow .3s,border-color .3s,color .3s;
        }
        :host([shape="circle"]){ 
            border-radius:50%; 
        }
        /*
        :host(:not([disabled]):active){
            z-index:1;
            transform:translateY(.1em);
        }
        */
        :host([disabled]),:host([loading]){
            pointer-events: none; 
            opacity:.6; 
        }
        :host([block]){ 
            display:flex; 
        }
        :host([disabled]:not([type])){ 
            background:rgba(0,0,0,.1); 
        }
        :host([disabled]) .btn,:host([loading]) .btn{ 
            cursor: not-allowed; 
            pointer-events: all; 
        }
        :host(:not([type="primary"]):not([type="danger"]):not([disabled]):hover),
        :host(:not([type="primary"]):not([type="danger"]):focus-within),
        :host([type="flat"][focus]){ 
            color:var(--themeColor,#42b983); 
            border-color: var(--themeColor,#42b983); 
        }
        :host(:not([type="primary"]):not([type="danger"])) .btn::after{ 
            background-image: radial-gradient(circle, var(--themeColor,#42b983) 10%, transparent 10.01%); 
        }
        :host([type="primary"]){ 
            color: #fff; 
            background:var(--themeBackground,var(--themeColor,#42b983));
        }
        :host([type="danger"]){ 
            color: #fff; 
            background:var(--themeBackground,var(--dangerColor,#ff7875));
        }
        :host([type="dashed"]){ 
            border-style:dashed 
        }
        :host([type="flat"]),:host([type="primary"]),:host([type="danger"]){ 
            border:0;
            padding: calc( .25em + 1px ) calc( .625em + 1px );
        }
        :host([type="flat"]) .btn::before{ 
            content:''; 
            position:absolute; 
            background:var(--themeColor,#42b983);
            pointer-events:none; 
            left:0; 
            right:0; 
            top:0; 
            bottom:0; 
            opacity:0; 
            transition:.3s;
        }
        :host([type="flat"]:not([disabled]):hover) .btn::before{ 
            opacity:.1 
        }
        :host(:not([disabled]):hover){ 
            z-index:1 
        }
        :host([type="flat"]:focus-within) .btn:before,
        :host([type="flat"][focus]) .btn:before{ 
            opacity:.2; 
        }
        :host(:focus-within){ 
            /*box-shadow: 0 0 10px rgba(0,0,0,0.1);*/ 
        }
        .btn{ 
            background:none; 
            outline:0; 
            border:0; 
            position: 
            absolute; 
            left:0; 
            top:0;
            width:100%;
            height:100%;
            padding:0;
            user-select: none;
            cursor: unset;
        }
        xy-loading{ 
            margin-right: 0.35em;  
        }
        ::-moz-focus-inner{
            border:0;
        }
        .btn::before{
            content: "";
            display: block;
            position: absolute;
            width: 100%;
            height: 100%;
            left:0;
            top:0;
            transition:.2s;
            background:#fff;
            opacity:0;
        }
        :host(:not([disabled]):active) .btn::before{ 
            opacity:.2;
        }
        .btn::after {
            content: "";
            display: block;
            position: absolute;
            width: 100%;
            height: 100%;
            left: var(--x,0); 
            top: var(--y,0);
            pointer-events: none;
            background-image: radial-gradient(circle, #fff 10%, transparent 10.01%);
            background-repeat: no-repeat;
            background-position: 50%;
            transform: translate(-50%,-50%) scale(10);
            opacity: 0;
            transition: transform .3s, opacity .8s;
        }
        .btn:not([disabled]):active::after {
            transform: translate(-50%,-50%) scale(0);
            opacity: .3;
            transition: 0s;
        }
        xy-icon{
            margin-right: 0.35em;
            transition: none;
        }
        :host(:empty) xy-icon{
            margin: auto;
        }
        :host(:empty){
            padding: .65em;
        }
        :host([type="flat"]:empty),:host([type="primary"]:empty){ 
            padding: calc( .65em + 1px );
        }
        ::slotted(xy-icon){
            transition: none;
        }
        :host([href]){
            cursor:pointer;
        }
        </style>
        <${this.href ? "a" : "button"} ${this.htmltype ? 'type="' + this.htmltype + '"' : ""} ${this.download && this.href ? 'download="' + this.download + '"' : ""} ${this.href ? 'href="' + this.href + '" target="' + this.target + '" rel="' + this.rel + '"' : ""} class="btn" id="btn"></${this.href ? "a" : "button"}>${!this.loading && this.icon && this.icon != "null" ? '<xy-icon id="icon" name=' + this.icon + "></xy-icon>" : ""}<slot></slot>
        `;
        }
        focus() {
          this.btn.focus();
        }
        get disabled() {
          return this.getAttribute("disabled") !== null;
        }
        get toggle() {
          return this.getAttribute("toggle") !== null;
        }
        get htmltype() {
          return this.getAttribute("htmltype");
        }
        get name() {
          return this.getAttribute("name");
        }
        get checked() {
          return this.getAttribute("checked") !== null;
        }
        get href() {
          return this.getAttribute("href");
        }
        get target() {
          return this.getAttribute("target") || "_blank";
        }
        get rel() {
          return this.getAttribute("rel");
        }
        get download() {
          return this.getAttribute("download");
        }
        get icon() {
          return this.getAttribute("icon");
        }
        get loading() {
          return this.getAttribute("loading") !== null;
        }
        set icon(L) {
          this.setAttribute("icon", L);
        }
        set htmltype(L) {
          this.setAttribute("htmltype", L);
        }
        set href(L) {
          this.setAttribute("href", L);
        }
        set disabled(L) {
          L === null || L === !1 ? this.removeAttribute("disabled") : this.setAttribute("disabled", "");
        }
        set checked(L) {
          L === null || L === !1 ? this.removeAttribute("checked") : this.setAttribute("checked", "");
        }
        set loading(L) {
          L === null || L === !1 ? this.removeAttribute("loading") : this.setAttribute("loading", "");
        }
        connectedCallback() {
          this.btn = this.shadowRoot.getElementById("btn"), this.ico = this.shadowRoot.getElementById("icon"), this.load = document.createElement("xy-loading"), this.load.style.color = "inherit", this.btn.addEventListener("mousedown", function(L) {
            if (!this.disabled) {
              const { left: Y, top: I } = this.getBoundingClientRect();
              this.style.setProperty("--x", L.clientX - Y + "px"), this.style.setProperty("--y", L.clientY - I + "px");
            }
          }), this.addEventListener("click", function(L) {
            this.toggle && (this.checked = !this.checked);
          }), this.btn.addEventListener("keydown", (L) => {
            L.keyCode === 13 && L.stopPropagation();
          }), this.disabled = this.disabled, this.loading = this.loading;
        }
        attributeChangedCallback(L, Y, I) {
          L == "disabled" && this.btn && (I !== null ? (this.btn.setAttribute("disabled", "disabled"), this.href && this.btn.removeAttribute("href")) : (this.btn.removeAttribute("disabled"), this.href && (this.btn.href = this.href))), L == "loading" && this.btn && (I !== null ? (this.shadowRoot.prepend(this.load), this.btn.setAttribute("disabled", "disabled")) : (this.shadowRoot.removeChild(this.load), this.btn.removeAttribute("disabled"))), L == "icon" && this.ico && (this.ico.name = I), L == "href" && this.btn && (this.disabled || (this.btn.href = I)), L == "htmltype" && this.btn && (this.btn.type = I);
        }
      }
      customElements.get("xy-button") || customElements.define("xy-button", p);
      class g extends HTMLElement {
        static get observedAttributes() {
          return ["disabled"];
        }
        constructor() {
          super(), this.attachShadow({ mode: "open" }).innerHTML = `
        <style>
        :host {
            display:inline-flex;
        }
        ::slotted(xy-button:not(:first-of-type):not(:last-of-type)){
            border-radius:0;
        }
        ::slotted(xy-button){
            margin:0!important;
        }
        ::slotted(xy-button:not(:first-of-type)){
            margin-left:-1px!important;
        }
        ::slotted(xy-button[type]:not([type="dashed"]):not(:first-of-type)){
            margin-left:1px!important;
        }
        ::slotted(xy-button:first-of-type){
            border-top-right-radius: 0;
            border-bottom-right-radius: 0px;
        }
        ::slotted(xy-button:last-of-type){
            border-top-left-radius: 0;
            border-bottom-left-radius: 0;
        }
        </style>
        <slot></slot>
        `;
        }
        get disabled() {
          return this.getAttribute("disabled") !== null;
        }
        set disabled(L) {
          L === null || L === !1 ? this.removeAttribute("disabled") : this.setAttribute("disabled", "");
        }
        connectedCallback() {
        }
        attributeChangedCallback(L, Y, I) {
        }
      }
      customElements.get("xy-button-group") || customElements.define("xy-button-group", g);
      class v extends HTMLElement {
        static get observedAttributes() {
          return ["open", "title", "oktext", "canceltext", "loading", "type"];
        }
        constructor(L) {
          super(), this.attachShadow({ mode: "open" }).innerHTML = `
        <style>
        :host{
            position:absolute;
            display:flex;
            box-shadow: 2px 2px 15px rgba(0,0,0,0.15);
            box-sizing: border-box;
            transform:scale(0);
            opacity:0.5;
            border-radius: 3px;
            z-index:10;
            transition:.3s cubic-bezier(.645, .045, .355, 1);
            transform-origin:inherit;
            background:#fff;
            visibility:hidden;
        }
        .popcon-content{
            box-sizing: border-box;
            display:flex;
            width: max-content;
            padding: 0 15px;
            flex:1;
            flex-direction:column;
        }
        .popcon-title {
            line-height: 30px;
            padding: 15px 30px 0 0;
            font-weight: 700;
            font-size: 14px;
            color: #4c5161;
            user-select: none;
            cursor: default;
        }
        .popcon-body {
            flex: 1;
            padding: 5px 0 15px 0;
        }
        .popcon-footer {
            padding: 3px 0 15px 0;
            margin-top: -3px;
            text-align: right;
            white-space: nowrap;
        }
        .btn-close{
            position:absolute;
            right:10px;
            top:10px;
            border:0;
        }
        .popcon-footer xy-button {
            font-size: .8em;
            margin-left: .8em;
        }
        .popcon-type{
            display:flex;
            width:30px;
            height:30px;
            font-size:22px;
            margin: 15px -10px 0 15px;
        }
        /*
        :host(:not([type="confirm"])) .popcon-type,
        :host(:not([type="confirm"])) .popcon-footer,
        :host(:not([type])) .popcon-title,
        :host(:not([type])) .btn-close{
            display:none;
        }
        */
        :host([type="confirm"]){
            min-width:250px;
        }
        :host([type="confirm"]) .popcon-body{
            font-size:14px;
        }
        :host(:not([type])) .popcon-content,:host(:not([type])) .popcon-body{
            padding: 0;
        }
        </style>
            ${(L || this.type) === "confirm" ? '<xy-icon id="popcon-type" class="popcon-type" name="question-circle" color="var(--waringColor,#faad14)"></xy-icon>' : ""}
            <div class="popcon-content">
                ${(L || this.type) !== null ? '<div class="popcon-title" id="title">' + this.title + '</div><xy-button class="btn-close" id="btn-close" icon="close"></xy-button>' : ""}
                <div class="popcon-body">
                    <slot></slot>
                </div>
                ${(L || this.type) === "confirm" ? '<div class="popcon-footer"><xy-button id="btn-cancel">' + this.canceltext + '</xy-button><xy-button id="btn-submit" type="primary">' + this.oktext + "</xy-button></div>" : ""}
            </div>
        `;
        }
        get open() {
          return this.getAttribute("open") !== null;
        }
        get stopfocus() {
          return this.getAttribute("stopfocus") !== null;
        }
        get title() {
          return this.getAttribute("title") || "popcon";
        }
        get type() {
          return this.getAttribute("type");
        }
        get oktext() {
          return this.getAttribute("oktext") || "confirm";
        }
        get canceltext() {
          return this.getAttribute("canceltext") || "cancel";
        }
        get loading() {
          return this.getAttribute("loading") !== null;
        }
        set title(L) {
          this.setAttribute("title", L);
        }
        set type(L) {
          L === null || L === !1 ? this.removeAttribute("type") : this.setAttribute("type", L);
        }
        set oktext(L) {
          this.setAttribute("oktext", L);
        }
        set canceltext(L) {
          this.setAttribute("canceltext", L);
        }
        set open(L) {
          L === null || L === !1 ? (this.removeAttribute("open"), this.parentNode.removeAttribute("open")) : (this.setAttribute("open", ""), this.parentNode.setAttribute("open", ""), this.loading && (this.loading = !1));
        }
        set loading(L) {
          L === null || L === !1 ? this.removeAttribute("loading") : this.setAttribute("loading", "");
        }
        connectedCallback() {
          this.remove = !1, this.type && (this.titles = this.shadowRoot.getElementById("title"), this.btnClose = this.shadowRoot.getElementById("btn-close")), this.type == "confirm" && (this.btnCancel = this.shadowRoot.getElementById("btn-cancel"), this.btnSubmit = this.shadowRoot.getElementById("btn-submit")), this.addEventListener("transitionend", (L) => {
            L.propertyName === "transform" && this.open && (this.type == "confirm" && this.btnSubmit.focus(), this.type == "pane" && this.btnClose.focus(), this.dispatchEvent(new CustomEvent("open")));
          }), this.addEventListener("transitionend", (L) => {
            L.propertyName !== "transform" || this.open || (this.remove && this.parentNode.removeChild(this), this.dispatchEvent(new CustomEvent("close")));
          }), this.addEventListener("click", (L) => {
            L.target.closest("[autoclose]") && (this.open = !1, window.xyActiveElement.focus());
          }), this.type && this.btnClose.addEventListener("click", () => {
            this.open = !1, window.xyActiveElement.focus();
          }), this.type == "confirm" && (this.btnCancel.addEventListener("click", async () => {
            this.dispatchEvent(new CustomEvent("cancel")), this.open = !1, window.xyActiveElement.focus();
          }), this.btnSubmit.addEventListener("click", () => {
            this.dispatchEvent(new CustomEvent("submit")), this.loading || (this.open = !1, window.xyActiveElement.focus());
          }));
        }
        attributeChangedCallback(L, Y, I) {
          L == "open" && this.shadowRoot && I == null && this.stopfocus, L == "loading" && this.shadowRoot && (this.btnSubmit.loading = I !== null), L == "title" && this.titles && I !== null && (this.titles.innerHTML = I), L == "oktext" && this.btnSubmit && I !== null && (this.btnSubmit.innerHTML = I), L == "canceltext" && this.btnCancel && I !== null && (this.btnCancel.innerHTML = I);
        }
      }
      customElements.get("xy-popcon") || customElements.define("xy-popcon", v);
      class k extends HTMLElement {
        static get observedAttributes() {
          return ["title", "oktext", "canceltext", "loading", "type"];
        }
        constructor() {
          super(), this.attachShadow({ mode: "open" }).innerHTML = `
        <style>
        :host {
            display:inline-block;
            position:relative;
            overflow:visible;
        }
        :host([dir="top"]) ::slotted(xy-popcon){
            bottom:100%;
            left:50%;
            transform:translate(-50%,-10px) scale(0);
            transform-origin: center bottom;
        }
        :host([dir="top"]) ::slotted(xy-popcon[open]),
        :host([dir="top"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="top"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(-50%,-10px) scale(1);
        }
        :host([dir="right"]) ::slotted(xy-popcon){
            left:100%;
            top:50%;
            transform:translate(10px,-50%) scale(0);
            transform-origin: left;
        }
        :host([dir="right"]) ::slotted(xy-popcon[open]),
        :host([dir="right"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="right"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(10px,-50%) scale(1);
        }
        :host([dir="bottom"]) ::slotted(xy-popcon){
            top:100%;
            left:50%;
            transform:translate(-50%,10px) scale(0);
            transform-origin: center top;
        }
        :host([dir="bottom"]) ::slotted(xy-popcon[open]),
        :host([dir="bottom"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="bottom"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(-50%,10px) scale(1);
        }
        :host([dir="left"]) ::slotted(xy-popcon){
            right:100%;
            top:50%;
            transform:translate(-10px,-50%) scale(0);
            transform-origin: right;
        }
        :host([dir="left"]) ::slotted(xy-popcon[open]),
        :host([dir="left"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="left"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(-10px,-50%) scale(1);
        }
        :host([dir="lefttop"]) ::slotted(xy-popcon){
            right:100%;
            top:0;
            transform:translate(-10px) scale(0);
            transform-origin: right top;
        }
        :host([dir="lefttop"]) ::slotted(xy-popcon[open]),
        :host([dir="lefttop"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="lefttop"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(-10px) scale(1);
        }
        :host([dir="leftbottom"]) ::slotted(xy-popcon){
            right:100%;
            bottom:0;
            transform:translate(-10px) scale(0);
            transform-origin: right bottom;
        }
        :host([dir="leftbottom"]) ::slotted(xy-popcon[open]),
        :host([dir="leftbottom"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="leftbottom"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(-10px) scale(1);
        }
        :host([dir="topleft"]) ::slotted(xy-popcon){
            bottom:100%;
            left:0;
            transform:translate(0,-10px) scale(0);
            transform-origin: left bottom;
        }
        :host([dir="topleft"]) ::slotted(xy-popcon[open]),
        :host([dir="topleft"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="topleft"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(0,-10px) scale(1);
        }
        :host([dir="topright"]) ::slotted(xy-popcon){
            bottom:100%;
            right:0;
            transform:translate(0,-10px) scale(0);
            transform-origin: right bottom;
        }
        :host([dir="topright"]) ::slotted(xy-popcon[open]),
        :host([dir="topright"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="topright"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(0,-10px) scale(1);
        }
        :host([dir="righttop"]) ::slotted(xy-popcon){
            left:100%;
            top:0;
            transform:translate(10px) scale(0);
            transform-origin: left top;
        }
        :host([dir="righttop"]) ::slotted(xy-popcon[open]),
        :host([dir="righttop"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="righttop"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(10px) scale(1);
        }
        :host([dir="rightbottom"]) ::slotted(xy-popcon){
            left:100%;
            bottom:0;
            transform:translate(10px) scale(0);
            transform-origin: left bottom;
        }
        :host([dir="rightbottom"]) ::slotted(xy-popcon[open]),
        :host([dir="rightbottom"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="rightbottom"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(10px) scale(1);
        }
        :host([dir="bottomleft"]) ::slotted(xy-popcon),
        :host(:not([dir])) ::slotted(xy-popcon){
            left:0;
            top:100%;
            transform:translate(0,10px) scale(0);
            transform-origin: left top;
        }
        :host(:not([dir])) ::slotted(xy-popcon[open]),
        :host(:not([dir])[trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host(:not([dir])[trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon),
        :host([dir="bottomleft"]) ::slotted(xy-popcon[open]),
        :host([dir="bottomleft"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="bottomleft"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(0,10px) scale(1);
        }
        :host([dir="bottomright"]) ::slotted(xy-popcon){
            right:0;
            top:100%;
            transform:translate(0,10px) scale(0);
            transform-origin: right top;
        }
        :host([dir="bottomright"]) ::slotted(xy-popcon[open]),
        :host([dir="bottomright"][trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([dir="bottomright"][trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            transform:translate(0,10px) scale(1);
        }
        :host([trigger="contextmenu"]) ::slotted(xy-popcon){
            right:auto;
            bottom:auto;
            left:var(--x,0);
            top:var(--y,100%);
            transform-origin: left top;
            transform:translate(5px,5px) scale(0);
            transition: .15s;
        }
        :host([trigger="contextmenu"]) ::slotted(xy-popcon[open]){
            transform:translate(5px,5px) scale(1);
        }
        :host ::slotted(xy-popcon[open]),
        :host([trigger="hover"]:not([disabled]):hover) ::slotted(xy-popcon),
        :host([trigger="focus"]:not([disabled]):focus-within) ::slotted(xy-popcon){
            opacity:1;
            visibility:visible;
        }
        slot{
            border-radius: inherit;
        }
        </style>
        <slot></slot>
        `;
        }
        get title() {
          return this.getAttribute("title") || "popcon";
        }
        get trigger() {
          return this.getAttribute("trigger");
        }
        get disabled() {
          return this.getAttribute("disabled") !== null;
        }
        get type() {
          return this.getAttribute("type");
        }
        get accomplish() {
          return this.getAttribute("accomplish") !== null;
        }
        get content() {
          return this.getAttribute("content");
        }
        get oktext() {
          return this.getAttribute("oktext");
        }
        get canceltext() {
          return this.getAttribute("canceltext");
        }
        get dir() {
          return this.getAttribute("dir");
        }
        get loading() {
          return this.getAttribute("loading") !== null;
        }
        set dir(L) {
          this.setAttribute("dir", L);
        }
        set title(L) {
          this.setAttribute("title", L);
        }
        set type(L) {
          this.setAttribute("type", L);
        }
        set oktext(L) {
          this.setAttribute("oktext", L);
        }
        set canceltext(L) {
          this.setAttribute("canceltext", L);
        }
        set loading(L) {
          L === null || L === !1 ? this.removeAttribute("loading") : this.setAttribute("loading", "");
        }
        set disabled(L) {
          L === null || L === !1 ? this.removeAttribute("disabled") : this.setAttribute("disabled", "");
        }
        show(L) {
          if (this.popcon = this.querySelector("xy-popcon"), this.disabled)
            (this.popcon || this).dispatchEvent(new CustomEvent("submit"));
          else if (this.popcon || (this.popcon = new v(this.type), this.popcon.type = this.type, this.appendChild(this.popcon), this.popcon.title = this.title || "popover", this.popcon.innerHTML = this.content || "", this.type == "confirm" && (this.popcon.oktext = this.oktext || "confirm", this.popcon.canceltext = this.canceltext || "cancel", this.popcon.onsubmit = () => this.dispatchEvent(new CustomEvent("submit")), this.popcon.oncancel = () => this.dispatchEvent(new CustomEvent("cancel")))), this.trigger === "contextmenu") {
            const { x: Y, y: I } = this.getBoundingClientRect();
            this.popcon.style.setProperty("--x", L.clientX - Y + "px"), this.popcon.style.setProperty("--y", L.clientY - I + "px"), this.popcon.open = !0;
          } else
            (L.path || L.composedPath && L.composedPath()).includes(this.popcon) || (window.xyActiveElement = document.activeElement, this.accomplish ? this.popcon.open = !0 : this.popcon.open = !this.popcon.open);
          return this.popcon;
        }
        connectedCallback() {
          this.popcon = this.querySelector("xy-popcon"), this.trigger && this.trigger !== "click" || this.addEventListener("click", this.show), this.trigger === "contextmenu" && this.addEventListener("contextmenu", (L) => {
            L.preventDefault(), (L.path || L.composedPath && L.composedPath()).includes(this.popcon) || this.show(L);
          }), document.addEventListener("mousedown", (L) => {
            const Y = L.path || L.composedPath && L.composedPath();
            (this.popcon && !Y.includes(this.popcon) && !this.popcon.loading && !Y.includes(this.children[0]) || this.trigger === "contextmenu" && !Y.includes(this.popcon) && L.which == "1") && (this.popcon.open = !1);
          });
        }
        attributeChangedCallback(L, Y, I) {
          L == "loading" && this.popcon && (this.popcon.loading = I !== null), L == "title" && this.popcon && I !== null && (this.popcon.title = I), L == "oktext" && this.popcon && I !== null && (this.popcon.oktext = I), L == "canceltext" && this.popcon && I !== null && (this.popcon.canceltext = I);
        }
      }
      customElements.get("xy-popover") || customElements.define("xy-popover", k);
      var y = u(697);
      const T = ["#ff1300", "#EC7878", "#9C27B0", "#673AB7", "#3F51B5", "#0070FF", "#03A9F4", "#00BCD4", "#4CAF50", "#8BC34A", "#CDDC39", "#FFE500", "#FFBF00", "#FF9800", "#795548", "#9E9E9E", "#5A5A5A", "#FFF"];
      class x extends HTMLElement {
        static get observedAttributes() {
          return ["disabled", "dir"];
        }
        constructor(L) {
          super();
          const Y = this.attachShadow({ mode: "open" });
          this.colorCollections = L.colorCollections || T, this.onColorPicked = L.onColorPicked, this.defaulColor = (0, y.handleCSSVariables)(L.defaultColor || this.colorCollections[0]), this.pluginType = L.type, this.hasCustomPicker = L.hasCustomPicker, this.customColor = (0, y.getCustomColorCache)(this.pluginType), Y.innerHTML = `
        <style>
        :host{
            display:inline-block;
            width:15px;
            font-size:14px;
            border: none;
        }
        :host([block]){
            display:block;
        }
        :host([disabled]){
            pointer-events:none;
        }
        
        :host(:focus-within) xy-popover,:host(:hover) xy-popover{ 
            z-index: 2;
        }
        input[type="color"]{
            -webkit-appearance: none;
            outline: none;
            border: none;
        }
        xy-popover{
            width: 12px;
            height:35px;
            padding-right: 1px;
        }
        xy-popover:hover {
            border-radius: 0 5px 5px 0;
            background: rgba(203, 203, 203, 0.49);
        }
        .color-btn {
            border: 1px solid #cab9b9;
            margin: 18px 3px 2px 3px;
            width: 7px;
            height: 7px;
            opacity: 0.9;
            padding: 1px 0 1px 0;
            color: var(--themeColor, #42b983);
            background: var(--themeColor, #42b983);
            font-weight: bolder;
            border-radius: 2px;
        }
        .color-btn:hover {
            opacity: 1;
            z-index: auto;
        }
        xy-popover{
            display:block;
        }
        xy-popcon{
            position: fixed;
            min-width:100%;
        }
        #custom-picker {
            position: relative;
            top: -1px;
            background-color: rgb(250, 250, 250);
            border-color: rgb(255 118 21) rgb(245 80 80 / 74%) #89c1c9 #95d5b6;
            border-width: 3px;
            border-radius: 8px;
            height: 18px;
        }
        .pop-footer{
            display:flex;
            justify-content:flex-end;
            padding:0 .8em .8em;
        }
        .pop-footer xy-button{
            font-size: .8em;
            margin-left: .8em;
        }
        .color-btn::before{
            content:'';
            position:absolute;
            left:5px;
            top:5px;
            right:5px;
            bottom:5px;
            z-index:-1;
            background: linear-gradient(45deg, #ddd 25%,transparent 0,transparent 75%,#ddd 0 ), linear-gradient(45deg, #ddd 25%, transparent 0, transparent 75%, #ddd 0);
            background-position: 0 0,5px 5px;
            background-size: 10px 10px;
        }
        .color-sign {
           max-width: 220px;
           padding: 10px;
           display:grid;
           cursor: default;
           grid-template-columns: repeat(auto-fit, minmax(15px, 1fr));
           grid-gap: 10px;     
        }
        .color-sign>button {
            position: relative;
            width: 16px;
            height: 16px;
            border-radius: 6px;
            border: 1px solid #b8b9b49e;
            outline: 0;
            opacity: 0.9;
        }
        .color-sign>button:hover {
            cursor: pointer;
            opacity: 1;
        }
        .color-section {
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .color-fire-btn {
            font-size: 17px;
            font-weight: bold;
            height: 28px;
            padding-top: 8px;
            padding-right: 1px;
            margin-left: 3px;
            padding-left: 3px;
            border-radius: 5px 0 0 5px;
        }
        .color-fire-btn:hover {
            font-size: 17px;
            font-weight: bold;
            background: rgba(203, 203, 203, 0.49);
            border-radius: 5px 0 0 5px;
        }
        </style>
        <section class="color-section">
            <xy-popover id="popover" ${this.dir ? "dir='" + this.dir + "'" : ""}>
                <xy-button class="color-btn" id="color-btn" ${this.disabled ? "disabled" : ""}>_</xy-button>
                <xy-popcon id="popcon">
                    <div class="color-sign" id="colors">
                        ${this.hasCustomPicker ? '<button id="custom-picker" class="rainbow-mask"/>' : ""}
                        ${this.colorCollections.map((I) => '<button class="color-cube" style="background-color:' + I + '" data-color=' + I + "></button>").join("")}
                    </div>
                </xy-popcon>
            </xy-popover>
        </section>`;
        }
        focus() {
          this.colorBtn.focus();
        }
        connectedCallback() {
          this.popoverEle = this.shadowRoot.getElementById("popover"), this.popcon = this.shadowRoot.getElementById("popcon"), this.colorBtn = this.shadowRoot.getElementById("color-btn"), this.colors = this.shadowRoot.getElementById("colors"), this.colors.addEventListener("click", (L) => {
            const Y = L.target.closest("button");
            Y && Y.id !== "custom-picker" && (this.nativeclick = !0, this.value = (0, y.handleCSSVariables)(Y.dataset.color), this.onColorPicked(this.value));
          }), this.popoverEle.addEventListener("click", () => this.closeConverter()), this.hasCustomPicker && this.setupCustomPicker(), this.value = this.defaultvalue;
        }
        closeConverter() {
          if (document.getElementsByClassName(y.CONVERTER_PANEL)[0]) {
            const L = document.getElementsByClassName(y.CONVERTER_BTN)[0];
            L == null || L.click();
          }
        }
        disconnectedCallback() {
          this.pickerInput && document.body.removeChild(this.pickerInput);
        }
        setupCustomPicker() {
          let L = !1;
          this.customPicker = this.shadowRoot.getElementById("custom-picker");
          const Y = this.customPicker;
          Y.style.backgroundColor = this.customColor, this.customPicker.addEventListener("click", (I) => {
            if (L)
              return void (L = !1);
            this.pickerInput && document.body.removeChild(this.pickerInput), this.pickerInput = document.createElement("input");
            const F = this.pickerInput, Q = this.popcon.getBoundingClientRect();
            F.setAttribute("type", "color"), F.value = this.customColor, F.style.position = "fixed", F.style.left = `${Q.x + 3}px`, F.style.top = `${Q.y + 10}px`, F.style.pointerEvents = "none", F.style.zIndex = "999", F.style.opacity = "0", F.addEventListener("input", (0, y.throttle)((U) => {
              this.nativeclick = !0, this.value = (0, y.handleCSSVariables)(U.target.value), this.onColorPicked(this.value), (0, y.setCustomColorCache)(this.value, this.pluginType), Y.style.backgroundColor = this.value, L = !0, Y.click();
            }, 30)), document.body.appendChild(F), setTimeout(() => {
              F.focus(), F.click();
            }, 0);
          });
        }
        get defaultvalue() {
          return this.defaulColor;
        }
        get value() {
          return this.$value;
        }
        get type() {
          return this.getAttribute("type");
        }
        get disabled() {
          return this.getAttribute("disabled") !== null;
        }
        get dir() {
          return this.getAttribute("dir");
        }
        set dir(L) {
          this.setAttribute("dir", L);
        }
        set disabled(L) {
          L === null || L === !1 ? this.removeAttribute("disabled") : this.setAttribute("disabled", "");
        }
        set defaultvalue(L) {
          this.setAttribute("defaultvalue", L);
        }
        set value(L) {
          L && (this.$value = L, this.colorBtn.style.setProperty("--themeColor", this.nativeclick ? (0, y.setDefaultColorCache)(L, this.pluginType) : (0, y.getDefaultColorCache)(L, this.pluginType)), this.nativeclick ? (this.nativeclick = !1, this.dispatchEvent(new CustomEvent("change", { detail: { value: this.value } }))) : this.colorPane ? this.colorPane.value = this.value : this.defaultvalue = this.value);
        }
        attributeChangedCallback(L, Y, I) {
          L == "disabled" && this.colorBtn && (I != null ? this.colorBtn.setAttribute("disabled", "disabled") : this.colorBtn.removeAttribute("disabled")), L == "dir" && this.popoverEle && I != null && (this.popoverEle.dir = I);
        }
      }
      customElements.get("xy-color-picker") || customElements.define("xy-color-picker", x);
    }, 697: (a, c, u) => {
      u.r(c), u.d(c, { CONVERTER_BTN: () => M, CONVERTER_PANEL: () => L, getCustomColorCache: () => x, getDefaultColorCache: () => y, handleCSSVariables: () => g, setCustomColorCache: () => T, setDefaultColorCache: () => k, throttle: () => v });
      const p = "editor-js-text-color-cache";
      function g(Y) {
        if (function(I) {
          return (typeof (F = I) == "string" || F instanceof String) && I.includes("--");
          var F;
        }(Y)) {
          const I = function(F) {
            const Q = /\((.*?)\)/.exec(F);
            if (Q)
              return Q[1];
          }(Y);
          return function(F) {
            return window.getComputedStyle(document.documentElement).getPropertyValue(F);
          }(I);
        }
        return Y;
      }
      function v(Y, I) {
        let F;
        return (...Q) => {
          F || (F = setTimeout(() => {
            Y(...Q), F = null;
          }, I));
        };
      }
      function k(Y, I) {
        return sessionStorage.setItem(`${p}-${I}`, JSON.stringify(Y)), Y;
      }
      function y(Y, I) {
        const F = sessionStorage.getItem(`${p}-${I}`);
        return F ? JSON.parse(F) : Y;
      }
      function T(Y, I) {
        sessionStorage.setItem(`${p}-${I}-custom`, JSON.stringify(Y));
      }
      function x(Y) {
        const I = sessionStorage.getItem(`${p}-${Y}-custom`);
        return I ? JSON.parse(I) : null;
      }
      const M = "ce-inline-toolbar__dropdown", L = "ce-conversion-toolbar--showed";
    } }, n = {};
    function r(a) {
      var c = n[a];
      if (c !== void 0)
        return c.exports;
      var u = n[a] = { id: a, exports: {} };
      return t[a](u, u.exports, r), u.exports;
    }
    return r.d = (a, c) => {
      for (var u in c)
        r.o(c, u) && !r.o(a, u) && Object.defineProperty(a, u, { enumerable: !0, get: c[u] });
    }, r.o = (a, c) => Object.prototype.hasOwnProperty.call(a, c), r.r = (a) => {
      typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
    }, r.nc = void 0, r(138);
  })());
})(ru);
var Qm = ru.exports;
const wd = /* @__PURE__ */ Dn(Qm);
var su = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, function() {
    return (() => {
      var t = { 771: (c, u, p) => {
        p.d(u, { Z: () => k });
        var g = p(645), v = p.n(g)()(function(y) {
          return y[1];
        });
        v.push([c.id, `.cdx-text-variant--call-out .ce-block__content {
  border: 1px solid rgb(255, 135, 135);
  padding: 8px 16px;
  border-radius: 6px;
  box-shadow: 0 0 0 5px rgba(255, 120, 84, 0.16);
  margin-top: 0.6em;
  margin-bottom: 0.6em;
}

.cdx-text-variant--citation {
  font-style: italic;
}

.cdx-text-variant--details {
  font-size: 0.84em;
}

.cdx-text-variant__toggler svg {
  width: 20px;
  height: 20px;
}
`, ""]);
        const k = v;
      }, 645: (c) => {
        c.exports = function(u) {
          var p = [];
          return p.toString = function() {
            return this.map(function(g) {
              var v = u(g);
              return g[2] ? "@media ".concat(g[2], " {").concat(v, "}") : v;
            }).join("");
          }, p.i = function(g, v, k) {
            typeof g == "string" && (g = [[null, g, ""]]);
            var y = {};
            if (k)
              for (var T = 0; T < this.length; T++) {
                var x = this[T][0];
                x != null && (y[x] = !0);
              }
            for (var M = 0; M < g.length; M++) {
              var L = [].concat(g[M]);
              k && y[L[0]] || (v && (L[2] ? L[2] = "".concat(v, " and ").concat(L[2]) : L[2] = v), p.push(L));
            }
          }, p;
        };
      }, 379: (c, u, p) => {
        var g, v = function() {
          var z = {};
          return function(d) {
            if (z[d] === void 0) {
              var j = document.querySelector(d);
              if (window.HTMLIFrameElement && j instanceof window.HTMLIFrameElement)
                try {
                  j = j.contentDocument.head;
                } catch {
                  j = null;
                }
              z[d] = j;
            }
            return z[d];
          };
        }(), k = [];
        function y(z) {
          for (var d = -1, j = 0; j < k.length; j++)
            if (k[j].identifier === z) {
              d = j;
              break;
            }
          return d;
        }
        function T(z, d) {
          for (var j = {}, R = [], V = 0; V < z.length; V++) {
            var K = z[V], J = d.base ? K[0] + d.base : K[0], A = j[J] || 0, se = "".concat(J, " ").concat(A);
            j[J] = A + 1;
            var Z = y(se), ne = { css: K[1], media: K[2], sourceMap: K[3] };
            Z !== -1 ? (k[Z].references++, k[Z].updater(ne)) : k.push({ identifier: se, updater: U(ne, d), references: 1 }), R.push(se);
          }
          return R;
        }
        function x(z) {
          var d = document.createElement("style"), j = z.attributes || {};
          if (j.nonce === void 0) {
            var R = p.nc;
            R && (j.nonce = R);
          }
          if (Object.keys(j).forEach(function(K) {
            d.setAttribute(K, j[K]);
          }), typeof z.insert == "function")
            z.insert(d);
          else {
            var V = v(z.insert || "head");
            if (!V)
              throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
            V.appendChild(d);
          }
          return d;
        }
        var M, L = (M = [], function(z, d) {
          return M[z] = d, M.filter(Boolean).join(`
`);
        });
        function Y(z, d, j, R) {
          var V = j ? "" : R.media ? "@media ".concat(R.media, " {").concat(R.css, "}") : R.css;
          if (z.styleSheet)
            z.styleSheet.cssText = L(d, V);
          else {
            var K = document.createTextNode(V), J = z.childNodes;
            J[d] && z.removeChild(J[d]), J.length ? z.insertBefore(K, J[d]) : z.appendChild(K);
          }
        }
        function I(z, d, j) {
          var R = j.css, V = j.media, K = j.sourceMap;
          if (V ? z.setAttribute("media", V) : z.removeAttribute("media"), K && typeof btoa < "u" && (R += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(K)))), " */")), z.styleSheet)
            z.styleSheet.cssText = R;
          else {
            for (; z.firstChild; )
              z.removeChild(z.firstChild);
            z.appendChild(document.createTextNode(R));
          }
        }
        var F = null, Q = 0;
        function U(z, d) {
          var j, R, V;
          if (d.singleton) {
            var K = Q++;
            j = F || (F = x(d)), R = Y.bind(null, j, K, !1), V = Y.bind(null, j, K, !0);
          } else
            j = x(d), R = I.bind(null, j, d), V = function() {
              (function(J) {
                if (J.parentNode === null)
                  return !1;
                J.parentNode.removeChild(J);
              })(j);
            };
          return R(z), function(J) {
            if (J) {
              if (J.css === z.css && J.media === z.media && J.sourceMap === z.sourceMap)
                return;
              R(z = J);
            } else
              V();
          };
        }
        c.exports = function(z, d) {
          (d = d || {}).singleton || typeof d.singleton == "boolean" || (d.singleton = (g === void 0 && (g = !!(window && document && document.all && !window.atob)), g));
          var j = T(z = z || [], d);
          return function(R) {
            if (R = R || [], Object.prototype.toString.call(R) === "[object Array]") {
              for (var V = 0; V < j.length; V++) {
                var K = y(j[V]);
                k[K].references--;
              }
              for (var J = T(R, d), A = 0; A < j.length; A++) {
                var se = y(j[A]);
                k[se].references === 0 && (k[se].updater(), k.splice(se, 1));
              }
              j = J;
            }
          };
        };
      }, 962: (c) => {
        c.exports = '<svg viewBox="0 0 400 400" xmlns="http://www.w3.org/2000/svg"><path d="M200.48 53c10.359 0 19.704 2.99 27.99 7.933 5.625 3.356 10.563 7.492 15.193 12.354 6.735 7.072 13.08 15.997 19.645 27.12l.142.24 76.986 134.194c6.553 10.46 11.425 19.799 14.654 28.577 2.216 6.027 3.735 11.967 4.393 18.103.963 8.977.067 18.035-3.552 26.98-7.933 19.612-24.283 33.336-45.054 37.586l-4.464.913h-204.65l-2.817-.357c-10.267-1.3-19.764-4.163-28.422-9.16-11.051-6.377-19.82-15.823-25.055-27.664-4.432-10.03-5.235-19.952-3.914-29.887.821-6.175 2.486-12.239 4.864-18.58 3.616-9.64 9.159-20.55 16.718-33.309l74.632-127.44c6.469-11.125 12.743-20.061 19.436-27.158 4.62-4.899 9.562-9.07 15.206-12.456 8.301-4.979 17.68-7.989 28.07-7.989zm0 45c-6.102 0-14.05 8.427-23.842 25.28l-74.73 127.605c-12.713 21.444-17.806 35.025-15.28 40.742 2.527 5.717 8.519 9.175 17.974 10.373h197.255c5.932-1.214 10.051-4.671 12.357-10.373 2.307-5.702-1.812-16.903-12.357-33.603l-77.302-134.743C214.608 106.427 206.583 98 200.48 98zm.02 135c14.636 0 26.5 11.864 26.5 26.5S215.136 286 200.5 286 174 274.136 174 259.5s11.864-26.5 26.5-26.5zm0-83c12.426 0 22.5 10.074 22.5 22.5v28c0 12.426-10.074 22.5-22.5 22.5S178 212.926 178 200.5v-28c0-12.426 10.074-22.5 22.5-22.5z"></path></svg>';
      }, 737: (c) => {
        c.exports = '<svg viewBox="0 0 400 400" xmlns="http://www.w3.org/2000/svg"><path d="M281.488 301c6.388 0 12.311-1.14 17.768-3.42 5.457-2.28 10.315-5.433 14.574-9.456a43.696 43.696 0 009.983-14.084c2.395-5.365 3.593-11.133 3.593-17.303 0-10.999-2.994-20.388-8.984-28.167-5.99-7.78-14.707-11.67-26.153-11.67-5.324 0-10.249.805-14.774 2.415-4.526 1.61-7.587 3.085-9.184 4.426a165.146 165.146 0 01-1.997-9.054 46.483 46.483 0 01-.798-8.651c0-9.658 1.664-18.242 4.991-25.753 3.328-7.511 7.92-14.486 13.776-20.924 5.856-6.439 12.777-12.609 20.763-18.51 7.986-5.902 16.637-11.938 25.954-18.108L313.032 99c-12.778 6.975-24.89 14.352-36.336 22.131-11.446 7.78-21.562 16.9-30.346 27.363-8.785 10.462-15.706 22.534-20.763 36.215-5.058 13.681-7.587 29.777-7.587 48.287 0 7.511 1.264 15.29 3.793 23.339 2.53 8.047 6.389 15.358 11.58 21.93 5.19 6.572 11.779 12.005 19.765 16.297 7.986 4.292 17.436 6.438 28.35 6.438zm-148 0c6.388 0 12.311-1.14 17.768-3.42 5.457-2.28 10.315-5.433 14.574-9.456a43.696 43.696 0 009.983-14.084c2.395-5.365 3.593-11.133 3.593-17.303 0-10.999-2.994-20.388-8.984-28.167-5.99-7.78-14.707-11.67-26.153-11.67-5.324 0-10.249.805-14.774 2.415-4.526 1.61-7.587 3.085-9.184 4.426a165.146 165.146 0 01-1.997-9.054 46.483 46.483 0 01-.798-8.651c0-9.658 1.664-18.242 4.991-25.753 3.328-7.511 7.92-14.486 13.776-20.924 5.856-6.439 12.777-12.609 20.763-18.51 7.986-5.902 16.637-11.938 25.954-18.108L165.032 99c-12.778 6.975-24.89 14.352-36.336 22.131-11.446 7.78-21.562 16.9-30.346 27.363-8.785 10.462-15.706 22.534-20.763 36.215C72.529 198.39 70 214.486 70 232.996c0 7.511 1.264 15.29 3.793 23.339 2.53 8.047 6.389 15.358 11.58 21.93 5.19 6.572 11.779 12.005 19.765 16.297 7.986 4.292 17.436 6.438 28.35 6.438z"></path></svg>';
      }, 399: (c) => {
        c.exports = '<svg viewBox="0 0 400 400" xmlns="http://www.w3.org/2000/svg"><path d="M204.615 84L288 320h-53.354l-15.175-48.513h-86.74L116.437 320H65l84.024-236h55.59zm-28.275 53.957l-30.35 92.863h59.743l-29.393-92.863zm211.05-36.018l.256.25c5.579 5.578 5.662 14.57.25 20.25l-.25.256-45.49 45.49c-5.577 5.578-14.57 5.66-20.25.25l-.255-.25-45.49-45.49c-5.663-5.663-5.663-14.844 0-20.506 5.578-5.578 14.57-5.662 20.25-.25l.256.25 35.236 35.236 35.237-35.236c5.578-5.578 14.57-5.662 20.25-.25z"></path></svg>';
      } }, n = {};
      function r(c) {
        var u = n[c];
        if (u !== void 0)
          return u.exports;
        var p = n[c] = { id: c, exports: {} };
        return t[c](p, p.exports, r), p.exports;
      }
      r.n = (c) => {
        var u = c && c.__esModule ? () => c.default : () => c;
        return r.d(u, { a: u }), u;
      }, r.d = (c, u) => {
        for (var p in u)
          r.o(u, p) && !r.o(c, p) && Object.defineProperty(c, p, { enumerable: !0, get: u[p] });
      }, r.o = (c, u) => Object.prototype.hasOwnProperty.call(c, u);
      var a = {};
      return (() => {
        r.d(a, { default: () => L });
        var c = r(379), u = r.n(c), p = r(771);
        function g(Y, I = null, F = {}) {
          const Q = document.createElement(Y);
          Array.isArray(I) ? Q.classList.add(...I) : I && Q.classList.add(I);
          for (const U in F)
            Q[U] = F[U];
          return Q;
        }
        u()(p.Z, { insert: "head", singleton: !1 }), p.Z.locals;
        var v = r(962), k = r.n(v), y = r(737), T = r.n(y), x = r(399), M = r.n(x);
        class L {
          constructor({ api: I, data: F, config: Q, block: U }) {
            this.api = I, this.data = F, this.config = Q, this.block = U, this.variants = [{ name: "call-out", icon: k(), title: this.api.i18n.t("Call-out") }, { name: "citation", icon: T(), title: this.api.i18n.t("Citation") }, { name: "details", icon: M(), title: this.api.i18n.t("Details") }], this.wrapper = void 0;
          }
          static get isTune() {
            return !0;
          }
          static get CSS() {
            return { toggler: "cdx-text-variant__toggler" };
          }
          render() {
            const I = g("div", "");
            return this.variants.forEach(({ name: F, icon: Q, title: U }) => {
              const z = g("div", [this.api.styles.settingsButton, L.CSS.toggler], { innerHTML: Q });
              z.dataset.name = F, this.api.tooltip.onHover(z, U, { placement: "top", hidingDelay: 500 }), I.appendChild(z);
            }), this.api.listeners.on(I, "click", (F) => {
              this.tuneClicked(F);
            }), I;
          }
          tuneClicked(I) {
            const F = I.target.closest(`.${this.api.styles.settingsButton}`), Q = F.classList.contains(this.api.styles.settingsButtonActive);
            F.classList.toggle(this.api.styles.settingsButtonActive, !Q), this.variant = Q ? "" : F.dataset.name;
          }
          wrap(I) {
            return this.wrapper = g("div"), this.variant = this.data, this.wrapper.appendChild(I), this.wrapper;
          }
          set variant(I) {
            this.data = I, this.variants.forEach((F) => {
              this.wrapper.classList.toggle(`cdx-text-variant--${F.name}`, F.name === this.data);
            });
          }
          save() {
            return this.data || "";
          }
        }
      })(), a.default;
    })();
  });
})(su);
var eg = su.exports;
const tg = /* @__PURE__ */ Dn(eg);
var au = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, function() {
    return function() {
      var t = { 966: function(a, c, u) {
        function p(k, y) {
          for (var T = 0; T < y.length; T++) {
            var x = y[T];
            x.enumerable = x.enumerable || !1, x.configurable = !0, "value" in x && (x.writable = !0), Object.defineProperty(k, x.key, x);
          }
        }
        u(548).toString();
        var g = u(630).make, v = function() {
          function k(M) {
            var L = M.api, Y = M.data, I = M.config, F = M.block;
            (function(Q, U) {
              if (!(Q instanceof U))
                throw new TypeError("Cannot call a class as a function");
            })(this, k), this.api = L, this.block = F, this.settings = I, this.data = Y || { alignment: this.getAlignment() }, this.alignmentSettings = [{ name: "left", icon: '<svg xmlns="http://www.w3.org/2000/svg" id="Layer" enable-background="new 0 0 64 64" height="20" viewBox="0 0 64 64" width="20"><path d="m54 8h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m54 52h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m10 23h28c1.104 0 2-.896 2-2s-.896-2-2-2h-28c-1.104 0-2 .896-2 2s.896 2 2 2z"/><path d="m54 30h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m10 45h28c1.104 0 2-.896 2-2s-.896-2-2-2h-28c-1.104 0-2 .896-2 2s.896 2 2 2z"/></svg>' }, { name: "center", icon: '<svg xmlns="http://www.w3.org/2000/svg" id="Layer" enable-background="new 0 0 64 64" height="20" viewBox="0 0 64 64" width="20"><path d="m54 8h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m54 52h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m46 23c1.104 0 2-.896 2-2s-.896-2-2-2h-28c-1.104 0-2 .896-2 2s.896 2 2 2z"/><path d="m54 30h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m46 45c1.104 0 2-.896 2-2s-.896-2-2-2h-28c-1.104 0-2 .896-2 2s.896 2 2 2z"/></svg>' }, { name: "right", icon: '<svg xmlns="http://www.w3.org/2000/svg" id="Layer" enable-background="new 0 0 64 64" height="20" viewBox="0 0 64 64" width="20"><path d="m54 8h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m54 52h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m54 19h-28c-1.104 0-2 .896-2 2s.896 2 2 2h28c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m54 30h-44c-1.104 0-2 .896-2 2s.896 2 2 2h44c1.104 0 2-.896 2-2s-.896-2-2-2z"/><path d="m54 41h-28c-1.104 0-2 .896-2 2s.896 2 2 2h28c1.104 0 2-.896 2-2s-.896-2-2-2z"/></svg>' }], this._CSS = { alignment: { left: "ce-tune-alignment--left", center: "ce-tune-alignment--center", right: "ce-tune-alignment--right" } };
          }
          var y, T, x;
          return y = k, x = [{ key: "DEFAULT_ALIGNMENT", get: function() {
            return "left";
          } }, { key: "isTune", get: function() {
            return !0;
          } }], (T = [{ key: "getAlignment", value: function() {
            var M, L;
            return (M = this.settings) !== null && M !== void 0 && M.blocks && this.settings.blocks.hasOwnProperty(this.block.name) ? this.settings.blocks[this.block.name] : (L = this.settings) !== null && L !== void 0 && L.default ? this.settings.default : k.DEFAULT_ALIGNMENT;
          } }, { key: "wrap", value: function(M) {
            return this.wrapper = g("div"), this.wrapper.classList.toggle(this._CSS.alignment[this.data.alignment]), this.wrapper.append(M), this.wrapper;
          } }, { key: "render", value: function() {
            var M = this, L = g("div");
            return this.alignmentSettings.map(function(Y) {
              var I = document.createElement("button");
              return I.classList.add(M.api.styles.settingsButton), I.innerHTML = Y.icon, I.type = "button", I.classList.toggle(M.api.styles.settingsButtonActive, Y.name === M.data.alignment), L.appendChild(I), I;
            }).forEach(function(Y, I, F) {
              Y.addEventListener("click", function() {
                M.data = { alignment: M.alignmentSettings[I].name }, F.forEach(function(Q, U) {
                  var z = M.alignmentSettings[U].name;
                  Q.classList.toggle(M.api.styles.settingsButtonActive, z === M.data.alignment), M.wrapper.classList.toggle(M._CSS.alignment[z], z === M.data.alignment);
                });
              });
            }), L;
          } }, { key: "save", value: function() {
            return this.data;
          } }]) && p(y.prototype, T), x && p(y, x), k;
        }();
        a.exports = v;
      }, 630: function(a, c, u) {
        function p(k) {
          return function(y) {
            if (Array.isArray(y))
              return g(y);
          }(k) || function(y) {
            if (typeof Symbol < "u" && Symbol.iterator in Object(y))
              return Array.from(y);
          }(k) || function(y, T) {
            if (y) {
              if (typeof y == "string")
                return g(y, T);
              var x = Object.prototype.toString.call(y).slice(8, -1);
              return x === "Object" && y.constructor && (x = y.constructor.name), x === "Map" || x === "Set" ? Array.from(y) : x === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(x) ? g(y, T) : void 0;
            }
          }(k) || function() {
            throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
          }();
        }
        function g(k, y) {
          (y == null || y > k.length) && (y = k.length);
          for (var T = 0, x = new Array(y); T < y; T++)
            x[T] = k[T];
          return x;
        }
        function v(k) {
          var y, T = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null, x = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, M = document.createElement(k);
          for (var L in Array.isArray(T) ? (y = M.classList).add.apply(y, p(T)) : T && M.classList.add(T), x)
            M[L] = x[L];
          return M;
        }
        u.r(c), u.d(c, { make: function() {
          return v;
        } });
      }, 424: function(a, c, u) {
        var p = u(645), g = u.n(p)()(function(v) {
          return v[1];
        });
        g.push([a.id, `.ce-tune-alignment--right {
    text-align: right;
}
.ce-tune-alignment--center {
    text-align: center;
}
.ce-tune-alignment--left {
    text-align: left;
}`, ""]), c.Z = g;
      }, 645: function(a) {
        a.exports = function(c) {
          var u = [];
          return u.toString = function() {
            return this.map(function(p) {
              var g = c(p);
              return p[2] ? "@media ".concat(p[2], " {").concat(g, "}") : g;
            }).join("");
          }, u.i = function(p, g, v) {
            typeof p == "string" && (p = [[null, p, ""]]);
            var k = {};
            if (v)
              for (var y = 0; y < this.length; y++) {
                var T = this[y][0];
                T != null && (k[T] = !0);
              }
            for (var x = 0; x < p.length; x++) {
              var M = [].concat(p[x]);
              v && k[M[0]] || (g && (M[2] ? M[2] = "".concat(g, " and ").concat(M[2]) : M[2] = g), u.push(M));
            }
          }, u;
        };
      }, 548: function(a, c, u) {
        var p = u(379), g = u.n(p), v = u(424);
        g()(v.Z, { insert: "head", singleton: !1 }), v.Z.locals;
      }, 379: function(a, c, u) {
        var p, g = function() {
          var U = {};
          return function(z) {
            if (U[z] === void 0) {
              var d = document.querySelector(z);
              if (window.HTMLIFrameElement && d instanceof window.HTMLIFrameElement)
                try {
                  d = d.contentDocument.head;
                } catch {
                  d = null;
                }
              U[z] = d;
            }
            return U[z];
          };
        }(), v = [];
        function k(U) {
          for (var z = -1, d = 0; d < v.length; d++)
            if (v[d].identifier === U) {
              z = d;
              break;
            }
          return z;
        }
        function y(U, z) {
          for (var d = {}, j = [], R = 0; R < U.length; R++) {
            var V = U[R], K = z.base ? V[0] + z.base : V[0], J = d[K] || 0, A = "".concat(K, " ").concat(J);
            d[K] = J + 1;
            var se = k(A), Z = { css: V[1], media: V[2], sourceMap: V[3] };
            se !== -1 ? (v[se].references++, v[se].updater(Z)) : v.push({ identifier: A, updater: Q(Z, z), references: 1 }), j.push(A);
          }
          return j;
        }
        function T(U) {
          var z = document.createElement("style"), d = U.attributes || {};
          if (d.nonce === void 0) {
            var j = u.nc;
            j && (d.nonce = j);
          }
          if (Object.keys(d).forEach(function(V) {
            z.setAttribute(V, d[V]);
          }), typeof U.insert == "function")
            U.insert(z);
          else {
            var R = g(U.insert || "head");
            if (!R)
              throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
            R.appendChild(z);
          }
          return z;
        }
        var x, M = (x = [], function(U, z) {
          return x[U] = z, x.filter(Boolean).join(`
`);
        });
        function L(U, z, d, j) {
          var R = d ? "" : j.media ? "@media ".concat(j.media, " {").concat(j.css, "}") : j.css;
          if (U.styleSheet)
            U.styleSheet.cssText = M(z, R);
          else {
            var V = document.createTextNode(R), K = U.childNodes;
            K[z] && U.removeChild(K[z]), K.length ? U.insertBefore(V, K[z]) : U.appendChild(V);
          }
        }
        function Y(U, z, d) {
          var j = d.css, R = d.media, V = d.sourceMap;
          if (R ? U.setAttribute("media", R) : U.removeAttribute("media"), V && typeof btoa < "u" && (j += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(V)))), " */")), U.styleSheet)
            U.styleSheet.cssText = j;
          else {
            for (; U.firstChild; )
              U.removeChild(U.firstChild);
            U.appendChild(document.createTextNode(j));
          }
        }
        var I = null, F = 0;
        function Q(U, z) {
          var d, j, R;
          if (z.singleton) {
            var V = F++;
            d = I || (I = T(z)), j = L.bind(null, d, V, !1), R = L.bind(null, d, V, !0);
          } else
            d = T(z), j = Y.bind(null, d, z), R = function() {
              (function(K) {
                if (K.parentNode === null)
                  return !1;
                K.parentNode.removeChild(K);
              })(d);
            };
          return j(U), function(K) {
            if (K) {
              if (K.css === U.css && K.media === U.media && K.sourceMap === U.sourceMap)
                return;
              j(U = K);
            } else
              R();
          };
        }
        a.exports = function(U, z) {
          (z = z || {}).singleton || typeof z.singleton == "boolean" || (z.singleton = (p === void 0 && (p = !!(window && document && document.all && !window.atob)), p));
          var d = y(U = U || [], z);
          return function(j) {
            if (j = j || [], Object.prototype.toString.call(j) === "[object Array]") {
              for (var R = 0; R < d.length; R++) {
                var V = k(d[R]);
                v[V].references--;
              }
              for (var K = y(j, z), J = 0; J < d.length; J++) {
                var A = k(d[J]);
                v[A].references === 0 && (v[A].updater(), v.splice(A, 1));
              }
              d = K;
            }
          };
        };
      } }, n = {};
      function r(a) {
        var c = n[a];
        if (c !== void 0)
          return c.exports;
        var u = n[a] = { id: a, exports: {} };
        return t[a](u, u.exports, r), u.exports;
      }
      return r.n = function(a) {
        var c = a && a.__esModule ? function() {
          return a.default;
        } : function() {
          return a;
        };
        return r.d(c, { a: c }), c;
      }, r.d = function(a, c) {
        for (var u in c)
          r.o(c, u) && !r.o(a, u) && Object.defineProperty(a, u, { enumerable: !0, get: c[u] });
      }, r.o = function(a, c) {
        return Object.prototype.hasOwnProperty.call(a, c);
      }, r.r = function(a) {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
      }, r(966);
    }();
  });
})(au);
var og = au.exports;
const ng = /* @__PURE__ */ Dn(og);
(function() {
  try {
    if (typeof document < "u") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode(".ce-header{padding:.6em 0 3px;margin:0;line-height:1.25em;outline:none}.ce-header p,.ce-header div{padding:0!important;margin:0!important}.ce-header[contentEditable=true][data-placeholder]:before{position:absolute;content:attr(data-placeholder);color:#707684;font-weight:400;display:none;cursor:text}.ce-header[contentEditable=true][data-placeholder]:empty:before{display:block}.ce-header[contentEditable=true][data-placeholder]:empty:focus:before{display:none}")), document.head.appendChild(o);
    }
  } catch (e) {
    console.error("vite-plugin-css-injected-by-js", e);
  }
})();
const ig = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M6 7L6 12M6 17L6 12M6 12L12 12M12 7V12M12 17L12 12"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M19 17V10.2135C19 10.1287 18.9011 10.0824 18.836 10.1367L16 12.5"/></svg>', rg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M6 7L6 12M6 17L6 12M6 12L12 12M12 7V12M12 17L12 12"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M16 11C16 10 19 9.5 19 12C19 13.9771 16.0684 13.9997 16.0012 16.8981C15.9999 16.9533 16.0448 17 16.1 17L19.3 17"/></svg>', sg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M6 7L6 12M6 17L6 12M6 12L12 12M12 7V12M12 17L12 12"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M16 11C16 10.5 16.8323 10 17.6 10C18.3677 10 19.5 10.311 19.5 11.5C19.5 12.5315 18.7474 12.9022 18.548 12.9823C18.5378 12.9864 18.5395 13.0047 18.5503 13.0063C18.8115 13.0456 20 13.3065 20 14.8C20 16 19.5 17 17.8 17C17.8 17 16 17 16 16.3"/></svg>', ag = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M6 7L6 12M6 17L6 12M6 12L12 12M12 7V12M12 17L12 12"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M18 10L15.2834 14.8511C15.246 14.9178 15.294 15 15.3704 15C16.8489 15 18.7561 15 20.2 15M19 17C19 15.7187 19 14.8813 19 13.6"/></svg>', lg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M6 7L6 12M6 17L6 12M6 12L12 12M12 7V12M12 17L12 12"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M16 15.9C16 15.9 16.3768 17 17.8 17C19.5 17 20 15.6199 20 14.7C20 12.7323 17.6745 12.0486 16.1635 12.9894C16.094 13.0327 16 12.9846 16 12.9027V10.1C16 10.0448 16.0448 10 16.1 10H19.8"/></svg>', cg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M6 7L6 12M6 17L6 12M6 12L12 12M12 7V12M12 17L12 12"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M19.5 10C16.5 10.5 16 13.3285 16 15M16 15V15C16 16.1046 16.8954 17 18 17H18.3246C19.3251 17 20.3191 16.3492 20.2522 15.3509C20.0612 12.4958 16 12.6611 16 15Z"/></svg>', dg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M9 7L9 12M9 17V12M9 12L15 12M15 7V12M15 17L15 12"/></svg>';
/**
 * Header block for the Editor.js.
 *
 * @author CodeX (team@ifmo.su)
 * @copyright CodeX 2018
 * @license MIT
 * @version 2.0.0
 */
let kd = class {
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {{data: HeaderData, config: HeaderConfig, api: object}}
   *   data — previously saved data
   *   config - user config for Tool
   *   api - Editor.js API
   *   readOnly - read only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r }) {
    this.api = n, this.readOnly = r, this._CSS = {
      block: this.api.styles.block,
      wrapper: "ce-header"
    }, this._settings = t, this._data = this.normalizeData(e), this._element = this.getTag();
  }
  /**
   * Normalize input data
   *
   * @param {HeaderData} data - saved data to process
   *
   * @returns {HeaderData}
   * @private
   */
  normalizeData(e) {
    const t = {};
    return typeof e != "object" && (e = {}), t.text = e.text || "", t.level = parseInt(e.level) || this.defaultLevel.number, t;
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLHeadingElement}
   * @public
   */
  render() {
    return this._element;
  }
  /**
   * Returns header block tunes config
   *
   * @returns {Array}
   */
  renderSettings() {
    return this.levels.map((e) => ({
      icon: e.svg,
      label: this.api.i18n.t(`Heading ${e.number}`),
      onActivate: () => this.setLevel(e.number),
      closeOnActivate: !0,
      isActive: this.currentLevel.number === e.number
    }));
  }
  /**
   * Callback for Block's settings buttons
   *
   * @param {number} level - level to set
   */
  setLevel(e) {
    this.data = {
      level: e,
      text: this.data.text
    };
  }
  /**
   * Method that specified how to merge two Text blocks.
   * Called by Editor.js by backspace at the beginning of the Block
   *
   * @param {HeaderData} data - saved data to merger with current block
   * @public
   */
  merge(e) {
    const t = {
      text: this.data.text + e.text,
      level: this.data.level
    };
    this.data = t;
  }
  /**
   * Validate Text block data:
   * - check for emptiness
   *
   * @param {HeaderData} blockData — data received after saving
   * @returns {boolean} false if saved data is not correct, otherwise true
   * @public
   */
  validate(e) {
    return e.text.trim() !== "";
  }
  /**
   * Extract Tool's data from the view
   *
   * @param {HTMLHeadingElement} toolsContent - Text tools rendered view
   * @returns {HeaderData} - saved data
   * @public
   */
  save(e) {
    return {
      text: e.innerHTML,
      level: this.currentLevel.number
    };
  }
  /**
   * Allow Header to be converted to/from other blocks
   */
  static get conversionConfig() {
    return {
      export: "text",
      // use 'text' property for other blocks
      import: "text"
      // fill 'text' property from other block's export string
    };
  }
  /**
   * Sanitizer Rules
   */
  static get sanitize() {
    return {
      level: !1,
      text: {}
    };
  }
  /**
   * Returns true to notify core that read-only is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Get current Tools`s data
   *
   * @returns {HeaderData} Current data
   * @private
   */
  get data() {
    return this._data.text = this._element.innerHTML, this._data.level = this.currentLevel.number, this._data;
  }
  /**
   * Store data in plugin:
   * - at the this._data property
   * - at the HTML
   *
   * @param {HeaderData} data — data to set
   * @private
   */
  set data(e) {
    if (this._data = this.normalizeData(e), e.level !== void 0 && this._element.parentNode) {
      const t = this.getTag();
      t.innerHTML = this._element.innerHTML, this._element.parentNode.replaceChild(t, this._element), this._element = t;
    }
    e.text !== void 0 && (this._element.innerHTML = this._data.text || "");
  }
  /**
   * Get tag for target level
   * By default returns second-leveled header
   *
   * @returns {HTMLElement}
   */
  getTag() {
    const e = document.createElement(this.currentLevel.tag);
    return e.innerHTML = this._data.text || "", e.classList.add(this._CSS.wrapper), e.contentEditable = this.readOnly ? "false" : "true", e.dataset.placeholder = this.api.i18n.t(this._settings.placeholder || ""), e;
  }
  /**
   * Get current level
   *
   * @returns {level}
   */
  get currentLevel() {
    let e = this.levels.find((t) => t.number === this._data.level);
    return e || (e = this.defaultLevel), e;
  }
  /**
   * Return default level
   *
   * @returns {level}
   */
  get defaultLevel() {
    if (this._settings.defaultLevel) {
      const e = this.levels.find((t) => t.number === this._settings.defaultLevel);
      if (e)
        return e;
      console.warn("(ง'̀-'́)ง Heading Tool: the default level specified was not found in available levels");
    }
    return this.levels[1];
  }
  /**
   * @typedef {object} level
   * @property {number} number - level number
   * @property {string} tag - tag corresponds with level number
   * @property {string} svg - icon
   */
  /**
   * Available header levels
   *
   * @returns {level[]}
   */
  get levels() {
    const e = [
      {
        number: 1,
        tag: "H1",
        svg: ig
      },
      {
        number: 2,
        tag: "H2",
        svg: rg
      },
      {
        number: 3,
        tag: "H3",
        svg: sg
      },
      {
        number: 4,
        tag: "H4",
        svg: ag
      },
      {
        number: 5,
        tag: "H5",
        svg: lg
      },
      {
        number: 6,
        tag: "H6",
        svg: cg
      }
    ];
    return this._settings.levels ? e.filter(
      (t) => this._settings.levels.includes(t.number)
    ) : e;
  }
  /**
   * Handle H1-H6 tags on paste to substitute it with header Tool
   *
   * @param {PasteEvent} event - event with pasted content
   */
  onPaste(e) {
    const t = e.detail.data;
    let n = this.defaultLevel.number;
    switch (t.tagName) {
      case "H1":
        n = 1;
        break;
      case "H2":
        n = 2;
        break;
      case "H3":
        n = 3;
        break;
      case "H4":
        n = 4;
        break;
      case "H5":
        n = 5;
        break;
      case "H6":
        n = 6;
        break;
    }
    this._settings.levels && (n = this._settings.levels.reduce((r, a) => Math.abs(a - n) < Math.abs(r - n) ? a : r)), this.data = {
      level: n,
      text: t.innerHTML
    };
  }
  /**
   * Used by Editor.js paste handling API.
   * Provides configuration to handle H1-H6 tags.
   *
   * @returns {{handler: (function(HTMLElement): {text: string}), tags: string[]}}
   */
  static get pasteConfig() {
    return {
      tags: ["H1", "H2", "H3", "H4", "H5", "H6"]
    };
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: dg,
      title: "Heading"
    };
  }
};
(function() {
  try {
    if (typeof document < "u") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode(".cdx-list{margin:0;padding-left:40px;outline:none}.cdx-list__item{padding:5.5px 0 5.5px 3px;line-height:1.6em}.cdx-list--unordered{list-style:disc}.cdx-list--ordered{list-style:decimal}.cdx-list-settings{display:flex}.cdx-list-settings .cdx-settings-button{width:50%}")), document.head.appendChild(o);
    }
  } catch (e) {
    console.error("vite-plugin-css-injected-by-js", e);
  }
})();
const xd = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><line x1="9" x2="19" y1="7" y2="7" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="9" x2="19" y1="12" y2="12" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="9" x2="19" y1="17" y2="17" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M5.00001 17H4.99002"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M5.00001 12H4.99002"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M5.00001 7H4.99002"/></svg>', hg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><line x1="12" x2="19" y1="7" y2="7" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="12" x2="19" y1="12" y2="12" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="12" x2="19" y1="17" y2="17" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M7.79999 14L7.79999 7.2135C7.79999 7.12872 7.7011 7.0824 7.63597 7.13668L4.79999 9.5"/></svg>';
let ug = class {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to use native Enter behaviour
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: xd,
      title: "List"
    };
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {object} params - tool constructor options
   * @param {ListData} params.data - previously saved data
   * @param {object} params.config - user config for Tool
   * @param {object} params.api - Editor.js API
   * @param {boolean} params.readOnly - read-only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r }) {
    this._elements = {
      wrapper: null
    }, this.api = n, this.readOnly = r, this.settings = [
      {
        name: "unordered",
        label: this.api.i18n.t("Unordered"),
        icon: xd,
        default: t.defaultStyle === "unordered" || !1
      },
      {
        name: "ordered",
        label: this.api.i18n.t("Ordered"),
        icon: hg,
        default: t.defaultStyle === "ordered" || !0
      }
    ], this._data = {
      style: this.settings.find((a) => a.default === !0).name,
      items: []
    }, this.data = e;
  }
  /**
   * Returns list tag with items
   *
   * @returns {Element}
   * @public
   */
  render() {
    return this._elements.wrapper = this.makeMainTag(this._data.style), this._data.items.length ? this._data.items.forEach((e) => {
      this._elements.wrapper.appendChild(this._make("li", this.CSS.item, {
        innerHTML: e
      }));
    }) : this._elements.wrapper.appendChild(this._make("li", this.CSS.item)), this.readOnly || this._elements.wrapper.addEventListener("keydown", (e) => {
      const [t, n] = [13, 8];
      switch (e.keyCode) {
        case t:
          this.getOutofList(e);
          break;
        case n:
          this.backspace(e);
          break;
      }
    }, !1), this._elements.wrapper;
  }
  /**
   * @returns {ListData}
   * @public
   */
  save() {
    return this.data;
  }
  /**
   * Allow List Tool to be converted to/from other block
   *
   * @returns {{export: Function, import: Function}}
   */
  static get conversionConfig() {
    return {
      /**
       * To create exported string from list, concatenate items by dot-symbol.
       *
       * @param {ListData} data - list data to create a string from thats
       * @returns {string}
       */
      export: (e) => e.items.join(". "),
      /**
       * To create a list from other block's string, just put it at the first item
       *
       * @param {string} string - string to create list tool data from that
       * @returns {ListData}
       */
      import: (e) => ({
        items: [e],
        style: "unordered"
      })
    };
  }
  /**
   * Sanitizer rules
   *
   * @returns {object}
   */
  static get sanitize() {
    return {
      style: {},
      items: {
        br: !0
      }
    };
  }
  /**
   * Settings
   *
   * @public
   * @returns {Array}
   */
  renderSettings() {
    return this.settings.map((e) => ({
      ...e,
      isActive: this._data.style === e.name,
      closeOnActivate: !0,
      onActivate: () => this.toggleTune(e.name)
    }));
  }
  /**
   * On paste callback that is fired from Editor
   *
   * @param {PasteEvent} event - event with pasted data
   */
  onPaste(e) {
    const t = e.detail.data;
    this.data = this.pasteHandler(t);
  }
  /**
   * List Tool on paste configuration
   *
   * @public
   */
  static get pasteConfig() {
    return {
      tags: ["OL", "UL", "LI"]
    };
  }
  /**
   * Creates main <ul> or <ol> tag depended on style
   *
   * @param {string} style - 'ordered' or 'unordered'
   * @returns {HTMLOListElement|HTMLUListElement}
   */
  makeMainTag(e) {
    const t = e === "ordered" ? this.CSS.wrapperOrdered : this.CSS.wrapperUnordered, n = e === "ordered" ? "ol" : "ul";
    return this._make(n, [this.CSS.baseBlock, this.CSS.wrapper, t], {
      contentEditable: !this.readOnly
    });
  }
  /**
   * Toggles List style
   *
   * @param {string} style - 'ordered'|'unordered'
   */
  toggleTune(e) {
    const t = this.makeMainTag(e);
    for (; this._elements.wrapper.hasChildNodes(); )
      t.appendChild(this._elements.wrapper.firstChild);
    this._elements.wrapper.replaceWith(t), this._elements.wrapper = t, this._data.style = e;
  }
  /**
   * Styles
   *
   * @private
   */
  get CSS() {
    return {
      baseBlock: this.api.styles.block,
      wrapper: "cdx-list",
      wrapperOrdered: "cdx-list--ordered",
      wrapperUnordered: "cdx-list--unordered",
      item: "cdx-list__item"
    };
  }
  /**
   * List data setter
   *
   * @param {ListData} listData
   */
  set data(e) {
    e || (e = {}), this._data.style = e.style || this.settings.find((n) => n.default === !0).name, this._data.items = e.items || [];
    const t = this._elements.wrapper;
    t && t.parentNode.replaceChild(this.render(), t);
  }
  /**
   * Return List data
   *
   * @returns {ListData}
   */
  get data() {
    this._data.items = [];
    const e = this._elements.wrapper.querySelectorAll(`.${this.CSS.item}`);
    for (let t = 0; t < e.length; t++)
      e[t].innerHTML.replace("<br>", " ").trim() && this._data.items.push(e[t].innerHTML);
    return this._data;
  }
  /**
   * Helper for making Elements with attributes
   *
   * @param  {string} tagName           - new Element tag name
   * @param  {Array|string} classNames  - list or name of CSS classname(s)
   * @param  {object} attributes        - any attributes
   * @returns {Element}
   */
  _make(e, t = null, n = {}) {
    const r = document.createElement(e);
    Array.isArray(t) ? r.classList.add(...t) : t && r.classList.add(t);
    for (const a in n)
      r[a] = n[a];
    return r;
  }
  /**
   * Returns current List item by the caret position
   *
   * @returns {Element}
   */
  get currentItem() {
    let e = window.getSelection().anchorNode;
    return e.nodeType !== Node.ELEMENT_NODE && (e = e.parentNode), e.closest(`.${this.CSS.item}`);
  }
  /**
   * Get out from List Tool
   * by Enter on the empty last item
   *
   * @param {KeyboardEvent} event
   */
  getOutofList(e) {
    const t = this._elements.wrapper.querySelectorAll("." + this.CSS.item);
    if (t.length < 2)
      return;
    const n = t[t.length - 1], r = this.currentItem;
    r === n && !n.textContent.trim().length && (r.parentElement.removeChild(r), this.api.blocks.insert(), this.api.caret.setToBlock(this.api.blocks.getCurrentBlockIndex()), e.preventDefault(), e.stopPropagation());
  }
  /**
   * Handle backspace
   *
   * @param {KeyboardEvent} event
   */
  backspace(e) {
    const t = this._elements.wrapper.querySelectorAll("." + this.CSS.item), n = t[0];
    n && t.length < 2 && !n.innerHTML.replace("<br>", " ").trim() && e.preventDefault();
  }
  /**
   * Select LI content by CMD+A
   *
   * @param {KeyboardEvent} event
   */
  selectItem(e) {
    e.preventDefault();
    const t = window.getSelection(), n = t.anchorNode.parentNode, r = n.closest("." + this.CSS.item), a = new Range();
    a.selectNodeContents(r), t.removeAllRanges(), t.addRange(a);
  }
  /**
   * Handle UL, OL and LI tags paste and returns List data
   *
   * @param {HTMLUListElement|HTMLOListElement|HTMLLIElement} element
   * @returns {ListData}
   */
  pasteHandler(e) {
    const { tagName: t } = e;
    let n;
    switch (t) {
      case "OL":
        n = "ordered";
        break;
      case "UL":
      case "LI":
        n = "unordered";
    }
    const r = {
      style: n,
      items: []
    };
    if (t === "LI")
      r.items = [e.innerHTML];
    else {
      const a = Array.from(e.querySelectorAll("LI"));
      r.items = a.map((c) => c.innerHTML).filter((c) => !!c.trim());
    }
    return r;
  }
};
(function() {
  try {
    if (typeof document < "u") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode(".ce-paragraph{line-height:1.6em;outline:none}.ce-paragraph[data-placeholder]:empty:before{content:attr(data-placeholder);color:#707684;font-weight:400;opacity:0}.codex-editor--empty .ce-block:first-child .ce-paragraph[data-placeholder]:empty:before{opacity:1}.codex-editor--toolbox-opened .ce-block:first-child .ce-paragraph[data-placeholder]:empty:before,.codex-editor--empty .ce-block:first-child .ce-paragraph[data-placeholder]:empty:focus:before{opacity:0}.ce-paragraph p:first-of-type{margin-top:0}.ce-paragraph p:last-of-type{margin-bottom:0}")), document.head.appendChild(o);
    }
  } catch (e) {
    console.error("vite-plugin-css-injected-by-js", e);
  }
})();
const pg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 9V7.2C8 7.08954 8.08954 7 8.2 7L12 7M16 9V7.2C16 7.08954 15.9105 7 15.8 7L12 7M12 7L12 17M12 17H10M12 17H14"/></svg>';
/**
 * Base Paragraph Block for the Editor.js.
 * Represents a regular text block
 *
 * @author CodeX (team@codex.so)
 * @copyright CodeX 2018
 * @license The MIT License (MIT)
 */
let fg = class lu {
  /**
   * Default placeholder for Paragraph Tool
   *
   * @returns {string}
   * @class
   */
  static get DEFAULT_PLACEHOLDER() {
    return "";
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {object} params - constructor params
   * @param {ParagraphData} params.data - previously saved data
   * @param {ParagraphConfig} params.config - user config for Tool
   * @param {object} params.api - editor.js api
   * @param {boolean} readOnly - read only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r }) {
    this.api = n, this.readOnly = r, this._CSS = {
      block: this.api.styles.block,
      wrapper: "ce-paragraph"
    }, this.readOnly || (this.onKeyUp = this.onKeyUp.bind(this)), this._placeholder = t.placeholder ? t.placeholder : lu.DEFAULT_PLACEHOLDER, this._data = {}, this._element = null, this._preserveBlank = t.preserveBlank !== void 0 ? t.preserveBlank : !1, this.data = e;
  }
  /**
   * Check if text content is empty and set empty string to inner html.
   * We need this because some browsers (e.g. Safari) insert <br> into empty contenteditanle elements
   *
   * @param {KeyboardEvent} e - key up event
   */
  onKeyUp(e) {
    if (e.code !== "Backspace" && e.code !== "Delete")
      return;
    const { textContent: t } = this._element;
    t === "" && (this._element.innerHTML = "");
  }
  /**
   * Create Tool's view
   *
   * @returns {HTMLElement}
   * @private
   */
  drawView() {
    const e = document.createElement("DIV");
    return e.classList.add(this._CSS.wrapper, this._CSS.block), e.contentEditable = !1, e.dataset.placeholder = this.api.i18n.t(this._placeholder), this._data.text && (e.innerHTML = this._data.text), this.readOnly || (e.contentEditable = !0, e.addEventListener("keyup", this.onKeyUp)), e;
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLDivElement}
   */
  render() {
    return this._element = this.drawView(), this._element;
  }
  /**
   * Method that specified how to merge two Text blocks.
   * Called by Editor.js by backspace at the beginning of the Block
   *
   * @param {ParagraphData} data
   * @public
   */
  merge(e) {
    const t = {
      text: this.data.text + e.text
    };
    this.data = t;
  }
  /**
   * Validate Paragraph block data:
   * - check for emptiness
   *
   * @param {ParagraphData} savedData — data received after saving
   * @returns {boolean} false if saved data is not correct, otherwise true
   * @public
   */
  validate(e) {
    return !(e.text.trim() === "" && !this._preserveBlank);
  }
  /**
   * Extract Tool's data from the view
   *
   * @param {HTMLDivElement} toolsContent - Paragraph tools rendered view
   * @returns {ParagraphData} - saved data
   * @public
   */
  save(e) {
    return {
      text: e.innerHTML
    };
  }
  /**
   * On paste callback fired from Editor.
   *
   * @param {PasteEvent} event - event with pasted data
   */
  onPaste(e) {
    const t = {
      text: e.detail.data.innerHTML
    };
    this.data = t;
  }
  /**
   * Enable Conversion Toolbar. Paragraph can be converted to/from other tools
   */
  static get conversionConfig() {
    return {
      export: "text",
      // to convert Paragraph to other block, use 'text' property of saved data
      import: "text"
      // to covert other block's exported string to Paragraph, fill 'text' property of tool data
    };
  }
  /**
   * Sanitizer rules
   */
  static get sanitize() {
    return {
      text: {
        br: !0
      }
    };
  }
  /**
   * Returns true to notify the core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Get current Tools`s data
   *
   * @returns {ParagraphData} Current data
   * @private
   */
  get data() {
    if (this._element !== null) {
      const e = this._element.innerHTML;
      this._data.text = e;
    }
    return this._data;
  }
  /**
   * Store data in plugin:
   * - at the this._data property
   * - at the HTML
   *
   * @param {ParagraphData} data — data to set
   * @private
   */
  set data(e) {
    this._data = e || {}, this._element !== null && this.hydrate();
  }
  /**
   * Fill tool's view with data
   */
  hydrate() {
    window.requestAnimationFrame(() => {
      this._element.innerHTML = this._data.text || "";
    });
  }
  /**
   * Used by Editor paste handling API.
   * Provides configuration to handle P tags.
   *
   * @returns {{tags: string[]}}
   */
  static get pasteConfig() {
    return {
      tags: ["P"]
    };
  }
  /**
   * Icon and title for displaying at the Toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: pg,
      title: "Text"
    };
  }
};
(function() {
  try {
    if (typeof document < "u") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode('.tc-wrap{--color-background:#f9f9fb;--color-text-secondary:#7b7e89;--color-border:#e8e8eb;--cell-size:34px;--toolbox-icon-size:18px;--toolbox-padding:6px;--toolbox-aiming-field-size:calc(var(--toolbox-icon-size) + var(--toolbox-padding)*2);border-left:0;position:relative;height:100%;width:100%;margin-top:var(--toolbox-icon-size);box-sizing:border-box;display:grid;grid-template-columns:calc(100% - var(--cell-size)) var(--cell-size)}.tc-wrap--readonly{grid-template-columns:100% var(--cell-size)}.tc-wrap svg{vertical-align:top}@media print{.tc-wrap{border-left-color:var(--color-border);border-left-style:solid;border-left-width:1px;grid-template-columns:100% var(--cell-size)}}@media print{.tc-wrap .tc-row:after{display:none}}.tc-table{position:relative;width:100%;height:100%;display:grid;font-size:14px;border-top:1px solid var(--color-border);line-height:1.4}.tc-table:after{width:calc(var(--cell-size));height:100%;left:calc(var(--cell-size)*-1);top:0}.tc-table:after,.tc-table:before{position:absolute;content:""}.tc-table:before{width:100%;height:var(--toolbox-aiming-field-size);top:calc(var(--toolbox-aiming-field-size)*-1);left:0}.tc-table--heading .tc-row:first-child{font-weight:600;border-bottom:2px solid var(--color-border)}.tc-table--heading .tc-row:first-child [contenteditable]:empty:before{content:attr(heading);color:var(--color-text-secondary)}.tc-table--heading .tc-row:first-child:after{bottom:-2px;border-bottom:2px solid var(--color-border)}.tc-add-column,.tc-add-row{display:flex;color:var(--color-text-secondary)}@media print{.tc-add{display:none}}.tc-add-column{padding:4px 0;justify-content:center;border-top:1px solid var(--color-border)}@media print{.tc-add-column{display:none}}.tc-add-row{height:var(--cell-size);align-items:center;padding-left:4px;position:relative}.tc-add-row:before{content:"";position:absolute;right:calc(var(--cell-size)*-1);width:var(--cell-size);height:100%}@media print{.tc-add-row{display:none}}.tc-add-column,.tc-add-row{transition:0s;cursor:pointer;will-change:background-color}.tc-add-column:hover,.tc-add-row:hover{transition:background-color .1s ease;background-color:var(--color-background)}.tc-add-row{margin-top:1px}.tc-add-row:hover:before{transition:.1s;background-color:var(--color-background)}.tc-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(10px,1fr));position:relative;border-bottom:1px solid var(--color-border)}.tc-row:after{content:"";pointer-events:none;position:absolute;width:var(--cell-size);height:100%;bottom:-1px;right:calc(var(--cell-size)*-1);border-bottom:1px solid var(--color-border)}.tc-row--selected{background:var(--color-background)}.tc-row--selected:after{background:var(--color-background)}.tc-cell{border-right:1px solid var(--color-border);padding:6px 12px;overflow:hidden;outline:none;line-break:normal}.tc-cell--selected{background:var(--color-background)}.tc-wrap--readonly .tc-row:after{display:none}.tc-toolbox{--toolbox-padding:6px;--popover-margin:30px;--toggler-click-zone-size:30px;--toggler-dots-color:#7b7e89;--toggler-dots-color-hovered:#1d202b;position:absolute;cursor:pointer;z-index:1;opacity:0;transition:opacity .1s;will-change:left,opacity}.tc-toolbox--column{top:calc(var(--toggler-click-zone-size)*-1);transform:translate(calc(var(--toggler-click-zone-size)*-1/2));will-change:left,opacity}.tc-toolbox--row{left:calc(var(--popover-margin)*-1);transform:translateY(calc(var(--toggler-click-zone-size)*-1/2));margin-top:-1px;will-change:top,opacity}.tc-toolbox--showed{opacity:1}.tc-toolbox .tc-popover{position:absolute;top:0;left:var(--popover-margin)}.tc-toolbox__toggler{display:flex;align-items:center;justify-content:center;width:var(--toggler-click-zone-size);height:var(--toggler-click-zone-size);color:var(--toggler-dots-color);opacity:0;transition:opacity .15s ease;will-change:opacity}.tc-toolbox__toggler:hover{color:var(--toggler-dots-color-hovered)}.tc-toolbox__toggler svg{fill:currentColor}.tc-wrap:hover .tc-toolbox__toggler{opacity:1}.tc-settings .cdx-settings-button{width:50%;margin:0}.tc-popover{--color-border:#eaeaea;--color-background:#fff;--color-background-hover:rgba(232,232,235,.49);--color-background-confirm:#e24a4a;--color-background-confirm-hover:#d54040;--color-text-confirm:#fff;background:var(--color-background);border:1px solid var(--color-border);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;padding:6px;display:none;will-change:opacity,transform}.tc-popover--opened{display:block;animation:menuShowing .1s cubic-bezier(.215,.61,.355,1) forwards}.tc-popover__item{display:flex;align-items:center;padding:2px 14px 2px 2px;border-radius:5px;cursor:pointer;white-space:nowrap;-webkit-user-select:none;-moz-user-select:none;user-select:none}.tc-popover__item:hover{background:var(--color-background-hover)}.tc-popover__item:not(:last-of-type){margin-bottom:2px}.tc-popover__item-icon{display:inline-flex;width:26px;height:26px;align-items:center;justify-content:center;background:var(--color-background);border-radius:5px;border:1px solid var(--color-border);margin-right:8px}.tc-popover__item-label{line-height:22px;font-size:14px;font-weight:500}.tc-popover__item--confirm{background:var(--color-background-confirm);color:var(--color-text-confirm)}.tc-popover__item--confirm:hover{background-color:var(--color-background-confirm-hover)}.tc-popover__item--confirm .tc-popover__item-icon{background:var(--color-background-confirm);border-color:#0000001a}.tc-popover__item--confirm .tc-popover__item-icon svg{transition:transform .2s ease-in;transform:rotate(90deg) scale(1.2)}.tc-popover__item--hidden{display:none}@keyframes menuShowing{0%{opacity:0;transform:translateY(-8px) scale(.9)}70%{opacity:1;transform:translateY(2px)}to{transform:translateY(0)}}')), document.head.appendChild(o);
    }
  } catch (e) {
    console.error("vite-plugin-css-injected-by-js", e);
  }
})();
function Go(o, e, t = {}) {
  const n = document.createElement(o);
  Array.isArray(e) ? n.classList.add(...e) : e && n.classList.add(e);
  for (const r in t)
    Object.prototype.hasOwnProperty.call(t, r) && (n[r] = t[r]);
  return n;
}
function Cd(o) {
  const e = o.getBoundingClientRect();
  return {
    y1: Math.floor(e.top + window.pageYOffset),
    x1: Math.floor(e.left + window.pageXOffset),
    x2: Math.floor(e.right + window.pageXOffset),
    y2: Math.floor(e.bottom + window.pageYOffset)
  };
}
function Sd(o, e) {
  const t = Cd(o), n = Cd(e);
  return {
    fromTopBorder: n.y1 - t.y1,
    fromLeftBorder: n.x1 - t.x1,
    fromRightBorder: t.x2 - n.x2,
    fromBottomBorder: t.y2 - n.y2
  };
}
function mg(o, e) {
  const t = o.getBoundingClientRect(), { width: n, height: r, x: a, y: c } = t, { clientX: u, clientY: p } = e;
  return {
    width: n,
    height: r,
    x: u - a,
    y: p - c
  };
}
function Td(o, e) {
  return e.parentNode.insertBefore(o, e);
}
function _d(o, e = !0) {
  const t = document.createRange(), n = window.getSelection();
  t.selectNodeContents(o), t.collapse(e), n.removeAllRanges(), n.addRange(t);
}
let gg = class Uo {
  /**
   * @param {object} options - constructor options
   * @param {PopoverItem[]} options.items - constructor options
   */
  constructor({ items: e }) {
    this.items = e, this.wrapper = void 0, this.itemEls = [];
  }
  /**
   * Set of CSS classnames used in popover
   *
   * @returns {object}
   */
  static get CSS() {
    return {
      popover: "tc-popover",
      popoverOpened: "tc-popover--opened",
      item: "tc-popover__item",
      itemHidden: "tc-popover__item--hidden",
      itemConfirmState: "tc-popover__item--confirm",
      itemIcon: "tc-popover__item-icon",
      itemLabel: "tc-popover__item-label"
    };
  }
  /**
   * Returns the popover element
   *
   * @returns {Element}
   */
  render() {
    return this.wrapper = Go("div", Uo.CSS.popover), this.items.forEach((e, t) => {
      const n = Go("div", Uo.CSS.item), r = Go("div", Uo.CSS.itemIcon, {
        innerHTML: e.icon
      }), a = Go("div", Uo.CSS.itemLabel, {
        textContent: e.label
      });
      n.dataset.index = t, n.appendChild(r), n.appendChild(a), this.wrapper.appendChild(n), this.itemEls.push(n);
    }), this.wrapper.addEventListener("click", (e) => {
      this.popoverClicked(e);
    }), this.wrapper;
  }
  /**
   * Popover wrapper click listener
   * Used to delegate clicks in items
   *
   * @returns {void}
   */
  popoverClicked(e) {
    const t = e.target.closest(`.${Uo.CSS.item}`);
    if (!t)
      return;
    const n = t.dataset.index, r = this.items[n];
    if (r.confirmationRequired && !this.hasConfirmationState(t)) {
      this.setConfirmationState(t);
      return;
    }
    r.onClick();
  }
  /**
   * Enable the confirmation state on passed item
   *
   * @returns {void}
   */
  setConfirmationState(e) {
    e.classList.add(Uo.CSS.itemConfirmState);
  }
  /**
   * Disable the confirmation state on passed item
   *
   * @returns {void}
   */
  clearConfirmationState(e) {
    e.classList.remove(Uo.CSS.itemConfirmState);
  }
  /**
   * Check if passed item has the confirmation state
   *
   * @returns {boolean}
   */
  hasConfirmationState(e) {
    return e.classList.contains(Uo.CSS.itemConfirmState);
  }
  /**
   * Return an opening state
   *
   * @returns {boolean}
   */
  get opened() {
    return this.wrapper.classList.contains(Uo.CSS.popoverOpened);
  }
  /**
   * Opens the popover
   *
   * @returns {void}
   */
  open() {
    this.items.forEach((e, t) => {
      typeof e.hideIf == "function" && this.itemEls[t].classList.toggle(Uo.CSS.itemHidden, e.hideIf());
    }), this.wrapper.classList.add(Uo.CSS.popoverOpened);
  }
  /**
   * Closes the popover
   *
   * @returns {void}
   */
  close() {
    this.wrapper.classList.remove(Uo.CSS.popoverOpened), this.itemEls.forEach((e) => {
      this.clearConfirmationState(e);
    });
  }
};
const Ed = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 8L12 12M12 12L16 16M12 12L16 8M12 12L8 16"/></svg>', bg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 9.16666L18.2167 12.5M18.2167 12.5L14.8833 15.8333M18.2167 12.5H10.05C9.16594 12.5 8.31809 12.1488 7.69297 11.5237C7.06785 10.8986 6.71666 10.0507 6.71666 9.16666"/></svg>', vg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.9167 14.9167L11.5833 18.25M11.5833 18.25L8.25 14.9167M11.5833 18.25L11.5833 10.0833C11.5833 9.19928 11.9345 8.35143 12.5596 7.72631C13.1848 7.10119 14.0326 6.75 14.9167 6.75"/></svg>', yg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.13333 14.9167L12.4667 18.25M12.4667 18.25L15.8 14.9167M12.4667 18.25L12.4667 10.0833C12.4667 9.19928 12.1155 8.35143 11.4904 7.72631C10.8652 7.10119 10.0174 6.75 9.13333 6.75"/></svg>', wg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 15.8333L18.2167 12.5M18.2167 12.5L14.8833 9.16667M18.2167 12.5L10.05 12.5C9.16595 12.5 8.31811 12.8512 7.69299 13.4763C7.06787 14.1014 6.71667 14.9493 6.71667 15.8333"/></svg>', kg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.41 9.66H9.4"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 9.66H14.59"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.31 14.36H9.3"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 14.36H14.59"/></svg>', Md = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M12 7V12M12 17V12M17 12H12M12 12H7"/></svg>', xg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>', Cg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M10 5V18.5"/><path stroke="currentColor" stroke-width="2" d="M14 5V18.5"/><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><path stroke="currentColor" stroke-width="2" d="M5 14H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>', Sg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M10 5V18.5"/><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>';
let Dd = class es {
  /**
   * Creates toolbox buttons and toolbox menus
   *
   * @param {object} api - Editor.js api
   * @param {PopoverItem[]} items - Editor.js api
   * @param {function} onOpen - callback fired when the Popover is opening
   * @param {function} onClose - callback fired when the Popover is closing
   * @param {string} [cssModifier] - the modifier for the Toolbox. Allows to add some specific styles.
   */
  constructor({ api: e, items: t, onOpen: n, onClose: r, cssModifier: a = "" }) {
    this.api = e, this.items = t, this.onOpen = n, this.onClose = r, this.cssModifier = a, this.popover = null, this.wrapper = this.createToolbox();
  }
  /**
   * Style classes
   */
  static get CSS() {
    return {
      toolbox: "tc-toolbox",
      toolboxShowed: "tc-toolbox--showed",
      toggler: "tc-toolbox__toggler"
    };
  }
  /**
   * Returns rendered Toolbox element
   */
  get element() {
    return this.wrapper;
  }
  /**
   * Creating a toolbox to open menu for a manipulating columns
   *
   * @returns {Element}
   */
  createToolbox() {
    const e = Go("div", [
      es.CSS.toolbox,
      this.cssModifier ? `${es.CSS.toolbox}--${this.cssModifier}` : ""
    ]);
    e.dataset.mutationFree = "true";
    const t = this.createPopover(), n = this.createToggler();
    return e.appendChild(n), e.appendChild(t), e;
  }
  /**
   * Creates the Toggler
   *
   * @returns {Element}
   */
  createToggler() {
    const e = Go("div", es.CSS.toggler, {
      innerHTML: kg
    });
    return e.addEventListener("click", () => {
      this.togglerClicked();
    }), e;
  }
  /**
   * Creates the Popover instance and render it
   *
   * @returns {Element}
   */
  createPopover() {
    return this.popover = new gg({
      items: this.items
    }), this.popover.render();
  }
  /**
   * Toggler click handler. Opens/Closes the popover
   *
   * @returns {void}
   */
  togglerClicked() {
    this.popover.opened ? (this.popover.close(), this.onClose()) : (this.popover.open(), this.onOpen());
  }
  /**
   * Shows the Toolbox
   *
   * @param {function} computePositionMethod - method that returns the position coordinate
   * @returns {void}
   */
  show(e) {
    const t = e();
    Object.entries(t).forEach(([n, r]) => {
      this.wrapper.style[n] = r;
    }), this.wrapper.classList.add(es.CSS.toolboxShowed);
  }
  /**
   * Hides the Toolbox
   *
   * @returns {void}
   */
  hide() {
    this.popover.close(), this.wrapper.classList.remove(es.CSS.toolboxShowed);
  }
};
function Tg(o, e) {
  let t = 0;
  return function(...n) {
    const r = (/* @__PURE__ */ new Date()).getTime();
    if (!(r - t < o))
      return t = r, e(...n);
  };
}
const wt = {
  wrapper: "tc-wrap",
  wrapperReadOnly: "tc-wrap--readonly",
  table: "tc-table",
  row: "tc-row",
  withHeadings: "tc-table--heading",
  rowSelected: "tc-row--selected",
  cell: "tc-cell",
  cellSelected: "tc-cell--selected",
  addRow: "tc-add-row",
  addColumn: "tc-add-column"
};
let _g = class {
  /**
   * Creates
   *
   * @constructor
   * @param {boolean} readOnly - read-only mode flag
   * @param {object} api - Editor.js API
   * @param {TableData} data - Editor.js API
   * @param {TableConfig} config - Editor.js API
   */
  constructor(e, t, n, r) {
    this.readOnly = e, this.api = t, this.data = n, this.config = r, this.wrapper = null, this.table = null, this.toolboxColumn = this.createColumnToolbox(), this.toolboxRow = this.createRowToolbox(), this.createTableWrapper(), this.hoveredRow = 0, this.hoveredColumn = 0, this.selectedRow = 0, this.selectedColumn = 0, this.tunes = {
      withHeadings: !1
    }, this.resize(), this.fill(), this.focusedCell = {
      row: 0,
      column: 0
    }, this.documentClicked = (a) => {
      const c = a.target.closest(`.${wt.table}`) !== null, u = a.target.closest(`.${wt.wrapper}`) === null;
      (c || u) && this.hideToolboxes();
      const p = a.target.closest(`.${wt.addRow}`), g = a.target.closest(`.${wt.addColumn}`);
      p && p.parentNode === this.wrapper ? (this.addRow(void 0, !0), this.hideToolboxes()) : g && g.parentNode === this.wrapper && (this.addColumn(void 0, !0), this.hideToolboxes());
    }, this.readOnly || this.bindEvents();
  }
  /**
   * Returns the rendered table wrapper
   *
   * @returns {Element}
   */
  getWrapper() {
    return this.wrapper;
  }
  /**
   * Hangs the necessary handlers to events
   */
  bindEvents() {
    document.addEventListener("click", this.documentClicked), this.table.addEventListener("mousemove", Tg(150, (e) => this.onMouseMoveInTable(e)), { passive: !0 }), this.table.onkeypress = (e) => this.onKeyPressListener(e), this.table.addEventListener("keydown", (e) => this.onKeyDownListener(e)), this.table.addEventListener("focusin", (e) => this.focusInTableListener(e));
  }
  /**
   * Configures and creates the toolbox for manipulating with columns
   *
   * @returns {Toolbox}
   */
  createColumnToolbox() {
    return new Dd({
      api: this.api,
      cssModifier: "column",
      items: [
        {
          label: this.api.i18n.t("Add column to left"),
          icon: vg,
          onClick: () => {
            this.addColumn(this.selectedColumn, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Add column to right"),
          icon: yg,
          onClick: () => {
            this.addColumn(this.selectedColumn + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete column"),
          icon: Ed,
          hideIf: () => this.numberOfColumns === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteColumn(this.selectedColumn), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectColumn(this.hoveredColumn), this.hideRowToolbox();
      },
      onClose: () => {
        this.unselectColumn();
      }
    });
  }
  /**
   * Configures and creates the toolbox for manipulating with rows
   *
   * @returns {Toolbox}
   */
  createRowToolbox() {
    return new Dd({
      api: this.api,
      cssModifier: "row",
      items: [
        {
          label: this.api.i18n.t("Add row above"),
          icon: wg,
          onClick: () => {
            this.addRow(this.selectedRow, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Add row below"),
          icon: bg,
          onClick: () => {
            this.addRow(this.selectedRow + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete row"),
          icon: Ed,
          hideIf: () => this.numberOfRows === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteRow(this.selectedRow), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  /**
   * When you press enter it moves the cursor down to the next row
   * or creates it if the click occurred on the last one
   */
  moveCursorToNextRow() {
    this.focusedCell.row !== this.numberOfRows ? (this.focusedCell.row += 1, this.focusCell(this.focusedCell)) : (this.addRow(), this.focusedCell.row += 1, this.focusCell(this.focusedCell), this.updateToolboxesPosition(0, 0));
  }
  /**
   * Get table cell by row and col index
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @returns {HTMLElement}
   */
  getCell(e, t) {
    return this.table.querySelectorAll(`.${wt.row}:nth-child(${e}) .${wt.cell}`)[t - 1];
  }
  /**
   * Get table row by index
   *
   * @param {number} row - row coordinate
   * @returns {HTMLElement}
   */
  getRow(e) {
    return this.table.querySelector(`.${wt.row}:nth-child(${e})`);
  }
  /**
   * The parent of the cell which is the row
   *
   * @param {HTMLElement} cell - cell element
   * @returns {HTMLElement}
   */
  getRowByCell(e) {
    return e.parentElement;
  }
  /**
   * Ger row's first cell
   *
   * @param {Element} row - row to find its first cell
   * @returns {Element}
   */
  getRowFirstCell(e) {
    return e.querySelector(`.${wt.cell}:first-child`);
  }
  /**
   * Set the sell's content by row and column numbers
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @param {string} content - cell HTML content
   */
  setCellContent(e, t, n) {
    const r = this.getCell(e, t);
    r.innerHTML = n;
  }
  /**
   * Add column in table on index place
   * Add cells in each row
   *
   * @param {number} columnIndex - number in the array of columns, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the first cell
   */
  addColumn(e = -1, t = !1) {
    let n = this.numberOfColumns;
    for (let r = 1; r <= this.numberOfRows; r++) {
      let a;
      const c = this.createCell();
      if (e > 0 && e <= n ? (a = this.getCell(r, e), Td(c, a)) : a = this.getRow(r).appendChild(c), r === 1) {
        const u = this.getCell(r, e > 0 ? e : n + 1);
        u && t && _d(u);
      }
    }
    this.addHeadingAttrToFirstRow();
  }
  /**
   * Add row in table on index place
   *
   * @param {number} index - number in the array of rows, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the inserted row
   * @returns {HTMLElement} row
   */
  addRow(e = -1, t = !1) {
    let n, r = Go("div", wt.row);
    this.tunes.withHeadings && this.removeHeadingAttrFromFirstRow();
    let a = this.numberOfColumns;
    if (e > 0 && e <= this.numberOfRows) {
      let u = this.getRow(e);
      n = Td(r, u);
    } else
      n = this.table.appendChild(r);
    this.fillRow(n, a), this.tunes.withHeadings && this.addHeadingAttrToFirstRow();
    const c = this.getRowFirstCell(n);
    return c && t && _d(c), n;
  }
  /**
   * Delete a column by index
   *
   * @param {number} index
   */
  deleteColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      if (!n)
        return;
      n.remove();
    }
  }
  /**
   * Delete a row by index
   *
   * @param {number} index
   */
  deleteRow(e) {
    this.getRow(e).remove(), this.addHeadingAttrToFirstRow();
  }
  /**
   * Create a wrapper containing a table, toolboxes
   * and buttons for adding rows and columns
   *
   * @returns {HTMLElement} wrapper - where all buttons for a table and the table itself will be
   */
  createTableWrapper() {
    if (this.wrapper = Go("div", wt.wrapper), this.table = Go("div", wt.table), this.readOnly && this.wrapper.classList.add(wt.wrapperReadOnly), this.wrapper.appendChild(this.toolboxRow.element), this.wrapper.appendChild(this.toolboxColumn.element), this.wrapper.appendChild(this.table), !this.readOnly) {
      const e = Go("div", wt.addColumn, {
        innerHTML: Md
      }), t = Go("div", wt.addRow, {
        innerHTML: Md
      });
      this.wrapper.appendChild(e), this.wrapper.appendChild(t);
    }
  }
  /**
   * Returns the size of the table based on initial data or config "size" property
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  computeInitialSize() {
    const e = this.data && this.data.content, t = Array.isArray(e), n = t ? e.length : !1, r = t ? e.length : void 0, a = n ? e[0].length : void 0, c = Number.parseInt(this.config && this.config.rows), u = Number.parseInt(this.config && this.config.cols), p = !isNaN(c) && c > 0 ? c : void 0, g = !isNaN(u) && u > 0 ? u : void 0;
    return {
      rows: r || p || 2,
      cols: a || g || 2
    };
  }
  /**
   * Resize table to match config size or transmitted data size
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  resize() {
    const { rows: e, cols: t } = this.computeInitialSize();
    for (let n = 0; n < e; n++)
      this.addRow();
    for (let n = 0; n < t; n++)
      this.addColumn();
  }
  /**
   * Fills the table with data passed to the constructor
   *
   * @returns {void}
   */
  fill() {
    const e = this.data;
    if (e && e.content)
      for (let t = 0; t < e.content.length; t++)
        for (let n = 0; n < e.content[t].length; n++)
          this.setCellContent(t + 1, n + 1, e.content[t][n]);
  }
  /**
   * Fills a row with cells
   *
   * @param {HTMLElement} row - row to fill
   * @param {number} numberOfColumns - how many cells should be in a row
   */
  fillRow(e, t) {
    for (let n = 1; n <= t; n++) {
      const r = this.createCell();
      e.appendChild(r);
    }
  }
  /**
   * Creating a cell element
   *
   * @return {Element}
   */
  createCell() {
    return Go("div", wt.cell, {
      contentEditable: !this.readOnly
    });
  }
  /**
   * Get number of rows in the table
   */
  get numberOfRows() {
    return this.table.childElementCount;
  }
  /**
   * Get number of columns in the table
   */
  get numberOfColumns() {
    return this.numberOfRows ? this.table.querySelectorAll(`.${wt.row}:first-child .${wt.cell}`).length : 0;
  }
  /**
   * Is the column toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isColumnMenuShowing() {
    return this.selectedColumn !== 0;
  }
  /**
   * Is the row toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isRowMenuShowing() {
    return this.selectedRow !== 0;
  }
  /**
   * Recalculate position of toolbox icons
   *
   * @param {Event} event - mouse move event
   */
  onMouseMoveInTable(e) {
    const { row: t, column: n } = this.getHoveredCell(e);
    this.hoveredColumn = n, this.hoveredRow = t, this.updateToolboxesPosition();
  }
  /**
   * Prevents default Enter behaviors
   * Adds Shift+Enter processing
   *
   * @param {KeyboardEvent} event - keypress event
   */
  onKeyPressListener(e) {
    if (e.key === "Enter") {
      if (e.shiftKey)
        return !0;
      this.moveCursorToNextRow();
    }
    return e.key !== "Enter";
  }
  /**
   * Prevents tab keydown event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyDownListener(e) {
    e.key === "Tab" && e.stopPropagation();
  }
  /**
   * Set the coordinates of the cell that the focus has moved to
   *
   * @param {FocusEvent} event - focusin event
   */
  focusInTableListener(e) {
    const t = e.target, n = this.getRowByCell(t);
    this.focusedCell = {
      row: Array.from(this.table.querySelectorAll(`.${wt.row}`)).indexOf(n) + 1,
      column: Array.from(n.querySelectorAll(`.${wt.cell}`)).indexOf(t) + 1
    };
  }
  /**
   * Unselect row/column
   * Close toolbox menu
   * Hide toolboxes
   *
   * @returns {void}
   */
  hideToolboxes() {
    this.hideRowToolbox(), this.hideColumnToolbox(), this.updateToolboxesPosition();
  }
  /**
   * Unselect row, close toolbox
   *
   * @returns {void}
   */
  hideRowToolbox() {
    this.unselectRow(), this.toolboxRow.hide();
  }
  /**
   * Unselect column, close toolbox
   *
   * @returns {void}
   */
  hideColumnToolbox() {
    this.unselectColumn(), this.toolboxColumn.hide();
  }
  /**
   * Set the cursor focus to the focused cell
   *
   * @returns {void}
   */
  focusCell() {
    this.focusedCellElem.focus();
  }
  /**
   * Get current focused element
   *
   * @returns {HTMLElement} - focused cell
   */
  get focusedCellElem() {
    const { row: e, column: t } = this.focusedCell;
    return this.getCell(e, t);
  }
  /**
   * Update toolboxes position
   *
   * @param {number} row - hovered row
   * @param {number} column - hovered column
   */
  updateToolboxesPosition(e = this.hoveredRow, t = this.hoveredColumn) {
    this.isColumnMenuShowing || t > 0 && t <= this.numberOfColumns && this.toolboxColumn.show(() => ({
      left: `calc((100% - var(--cell-size)) / (${this.numberOfColumns} * 2) * (1 + (${t} - 1) * 2))`
    })), this.isRowMenuShowing || e > 0 && e <= this.numberOfRows && this.toolboxRow.show(() => {
      const n = this.getRow(e), { fromTopBorder: r } = Sd(this.table, n), { height: a } = n.getBoundingClientRect();
      return {
        top: `${Math.ceil(r + a / 2)}px`
      };
    });
  }
  /**
   * Makes the first row headings
   *
   * @param {boolean} withHeadings - use headings row or not
   */
  setHeadingsSetting(e) {
    this.tunes.withHeadings = e, e ? (this.table.classList.add(wt.withHeadings), this.addHeadingAttrToFirstRow()) : (this.table.classList.remove(wt.withHeadings), this.removeHeadingAttrFromFirstRow());
  }
  /**
   * Adds an attribute for displaying the placeholder in the cell
   */
  addHeadingAttrToFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.setAttribute("heading", this.api.i18n.t("Heading"));
    }
  }
  /**
   * Removes an attribute for displaying the placeholder in the cell
   */
  removeHeadingAttrFromFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.removeAttribute("heading");
    }
  }
  /**
   * Add effect of a selected row
   *
   * @param {number} index
   */
  selectRow(e) {
    const t = this.getRow(e);
    t && (this.selectedRow = e, t.classList.add(wt.rowSelected));
  }
  /**
   * Remove effect of a selected row
   */
  unselectRow() {
    if (this.selectedRow <= 0)
      return;
    const e = this.table.querySelector(`.${wt.rowSelected}`);
    e && e.classList.remove(wt.rowSelected), this.selectedRow = 0;
  }
  /**
   * Add effect of a selected column
   *
   * @param {number} index
   */
  selectColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      n && n.classList.add(wt.cellSelected);
    }
    this.selectedColumn = e;
  }
  /**
   * Remove effect of a selected column
   */
  unselectColumn() {
    if (this.selectedColumn <= 0)
      return;
    let e = this.table.querySelectorAll(`.${wt.cellSelected}`);
    Array.from(e).forEach((t) => {
      t.classList.remove(wt.cellSelected);
    }), this.selectedColumn = 0;
  }
  /**
   * Calculates the row and column that the cursor is currently hovering over
   * The search was optimized from O(n) to O (log n) via bin search to reduce the number of calculations
   *
   * @param {Event} event - mousemove event
   * @returns hovered cell coordinates as an integer row and column
   */
  getHoveredCell(e) {
    let t = this.hoveredRow, n = this.hoveredColumn;
    const { width: r, height: a, x: c, y: u } = mg(this.table, e);
    return c >= 0 && (n = this.binSearch(
      this.numberOfColumns,
      (p) => this.getCell(1, p),
      ({ fromLeftBorder: p }) => c < p,
      ({ fromRightBorder: p }) => c > r - p
    )), u >= 0 && (t = this.binSearch(
      this.numberOfRows,
      (p) => this.getCell(p, 1),
      ({ fromTopBorder: p }) => u < p,
      ({ fromBottomBorder: p }) => u > a - p
    )), {
      row: t || this.hoveredRow,
      column: n || this.hoveredColumn
    };
  }
  /**
   * Looks for the index of the cell the mouse is hovering over.
   * Cells can be represented as ordered intervals with left and
   * right (upper and lower for rows) borders inside the table, if the mouse enters it, then this is our index
   *
   * @param {number} numberOfCells - upper bound of binary search
   * @param {function} getCell - function to take the currently viewed cell
   * @param {function} beforeTheLeftBorder - determines the cursor position, to the left of the cell or not
   * @param {function} afterTheRightBorder - determines the cursor position, to the right of the cell or not
   * @returns {number}
   */
  binSearch(e, t, n, r) {
    let a = 0, c = e + 1, u = 0, p;
    for (; a < c - 1 && u < 10; ) {
      p = Math.ceil((a + c) / 2);
      const g = t(p), v = Sd(this.table, g);
      if (n(v))
        c = p;
      else if (r(v))
        a = p;
      else
        break;
      u++;
    }
    return p;
  }
  /**
   * Collects data from cells into a two-dimensional array
   *
   * @returns {string[][]}
   */
  getData() {
    const e = [];
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.table.querySelector(`.${wt.row}:nth-child(${t})`), r = Array.from(n.querySelectorAll(`.${wt.cell}`));
      r.every((a) => !a.textContent.trim()) || e.push(r.map((a) => a.innerHTML));
    }
    return e;
  }
  /**
   * Remove listeners on the document
   */
  destroy() {
    document.removeEventListener("click", this.documentClicked);
  }
}, Eg = class {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to press Enter inside the CodeTool textarea
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {TableData} data — previously saved data
   * @param {TableConfig} config - user config for Tool
   * @param {object} api - Editor.js API
   * @param {boolean} readOnly - read-only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r }) {
    this.api = n, this.readOnly = r, this.config = t, this.data = {
      withHeadings: this.getConfig("withHeadings", !1, e),
      content: e && e.content ? e.content : []
    }, this.table = null;
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: Sg,
      title: "Table"
    };
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLDivElement}
   */
  render() {
    return this.table = new _g(this.readOnly, this.api, this.data, this.config), this.container = Go("div", this.api.styles.block), this.container.appendChild(this.table.getWrapper()), this.table.setHeadingsSetting(this.data.withHeadings), this.container;
  }
  /**
   * Returns plugin settings
   *
   * @returns {Array}
   */
  renderSettings() {
    return [
      {
        label: this.api.i18n.t("With headings"),
        icon: xg,
        isActive: this.data.withHeadings,
        closeOnActivate: !0,
        toggle: !0,
        onActivate: () => {
          this.data.withHeadings = !0, this.table.setHeadingsSetting(this.data.withHeadings);
        }
      },
      {
        label: this.api.i18n.t("Without headings"),
        icon: Cg,
        isActive: !this.data.withHeadings,
        closeOnActivate: !0,
        toggle: !0,
        onActivate: () => {
          this.data.withHeadings = !1, this.table.setHeadingsSetting(this.data.withHeadings);
        }
      }
    ];
  }
  /**
   * Extract table data from the view
   *
   * @returns {TableData} - saved data
   */
  save() {
    const e = this.table.getData();
    return {
      withHeadings: this.data.withHeadings,
      content: e
    };
  }
  /**
   * Plugin destroyer
   *
   * @returns {void}
   */
  destroy() {
    this.table.destroy();
  }
  /**
   * A helper to get config value.
   * 
   * @param {string} configName - the key to get from the config. 
   * @param {any} defaultValue - default value if config doesn't have passed key
   * @param {object} savedData - previously saved data. If passed, the key will be got from there, otherwise from the config
   * @returns {any} - config value.
   */
  getConfig(e, t = void 0, n = void 0) {
    const r = this.data || n;
    return r ? r[e] ? r[e] : t : this.config && this.config[e] ? this.config[e] : t;
  }
  /**  
   * Table onPaste configuration
   *
   * @public
   */
  static get pasteConfig() {
    return { tags: ["TABLE", "TR", "TH", "TD"] };
  }
  /**
   * On paste callback that is fired from Editor
   *
   * @param {PasteEvent} event - event with pasted data
   */
  onPaste(e) {
    const t = e.detail.data, n = t.querySelector(":scope > thead, tr:first-of-type th"), r = Array.from(t.querySelectorAll("tr")).map((a) => Array.from(a.querySelectorAll("th, td")).map((c) => c.innerHTML));
    this.data = {
      withHeadings: n !== null,
      content: r
    }, this.table.wrapper && this.table.wrapper.replaceWith(this.render());
  }
};
var cu = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, function() {
    return (() => {
      var t = { 424: (c, u, p) => {
        p.r(u), p.d(u, { default: () => T });
        var g = p(81), v = p.n(g), k = p(645), y = p.n(k)()(v());
        y.push([c.id, `.ce-block--drop-target .ce-block__content:before {
  content: "";
  position: absolute;
  top: 50%;
  left: -20px;
  margin-top: -1px;
  height: 8px;
  width: 8px;
  border: solid #a0a0a0;
  border-width: 1px 1px 0 0;
  -webkit-transform-origin: right;
  transform-origin: right;
  -webkit-transform: rotate(45deg);
  transform: rotate(45deg);
}

.ce-block--drop-target .ce-block__content:after {
  background: none;
}`, ""]);
        const T = y;
      }, 645: (c) => {
        c.exports = function(u) {
          var p = [];
          return p.toString = function() {
            return this.map(function(g) {
              var v = "", k = g[5] !== void 0;
              return g[4] && (v += "@supports (".concat(g[4], ") {")), g[2] && (v += "@media ".concat(g[2], " {")), k && (v += "@layer".concat(g[5].length > 0 ? " ".concat(g[5]) : "", " {")), v += u(g), k && (v += "}"), g[2] && (v += "}"), g[4] && (v += "}"), v;
            }).join("");
          }, p.i = function(g, v, k, y, T) {
            typeof g == "string" && (g = [[null, g, void 0]]);
            var x = {};
            if (k)
              for (var M = 0; M < this.length; M++) {
                var L = this[M][0];
                L != null && (x[L] = !0);
              }
            for (var Y = 0; Y < g.length; Y++) {
              var I = [].concat(g[Y]);
              k && x[I[0]] || (T !== void 0 && (I[5] === void 0 || (I[1] = "@layer".concat(I[5].length > 0 ? " ".concat(I[5]) : "", " {").concat(I[1], "}")), I[5] = T), v && (I[2] && (I[1] = "@media ".concat(I[2], " {").concat(I[1], "}")), I[2] = v), y && (I[4] ? (I[1] = "@supports (".concat(I[4], ") {").concat(I[1], "}"), I[4] = y) : I[4] = "".concat(y)), p.push(I));
            }
          }, p;
        };
      }, 81: (c) => {
        c.exports = function(u) {
          return u[1];
        };
      }, 406: (c, u, p) => {
        var g = p(424);
        typeof g == "string" && (g = [[c.id, g, ""]]), p(723)(g, { hmr: !0, transform: void 0, insertInto: void 0 }), g.locals && (c.exports = g.locals);
      }, 723: (c, u, p) => {
        var g, v, k = {}, y = (g = function() {
          return window && document && document.all && !window.atob;
        }, function() {
          return v === void 0 && (v = g.apply(this, arguments)), v;
        }), T = function(Z) {
          return document.querySelector(Z);
        }, x = function(Z) {
          var ne = {};
          return function(le) {
            if (typeof le == "function")
              return le();
            if (ne[le] === void 0) {
              var oe = T.call(this, le);
              if (window.HTMLIFrameElement && oe instanceof window.HTMLIFrameElement)
                try {
                  oe = oe.contentDocument.head;
                } catch {
                  oe = null;
                }
              ne[le] = oe;
            }
            return ne[le];
          };
        }(), M = null, L = 0, Y = [], I = p(947);
        function F(Z, ne) {
          for (var le = 0; le < Z.length; le++) {
            var oe = Z[le], fe = k[oe.id];
            if (fe) {
              fe.refs++;
              for (var Me = 0; Me < fe.parts.length; Me++)
                fe.parts[Me](oe.parts[Me]);
              for (; Me < oe.parts.length; Me++)
                fe.parts.push(R(oe.parts[Me], ne));
            } else {
              var je = [];
              for (Me = 0; Me < oe.parts.length; Me++)
                je.push(R(oe.parts[Me], ne));
              k[oe.id] = { id: oe.id, refs: 1, parts: je };
            }
          }
        }
        function Q(Z, ne) {
          for (var le = [], oe = {}, fe = 0; fe < Z.length; fe++) {
            var Me = Z[fe], je = ne.base ? Me[0] + ne.base : Me[0], Ae = { css: Me[1], media: Me[2], sourceMap: Me[3] };
            oe[je] ? oe[je].parts.push(Ae) : le.push(oe[je] = { id: je, parts: [Ae] });
          }
          return le;
        }
        function U(Z, ne) {
          var le = x(Z.insertInto);
          if (!le)
            throw new Error("Couldn't find a style target. This probably means that the value for the 'insertInto' parameter is invalid.");
          var oe = Y[Y.length - 1];
          if (Z.insertAt === "top")
            oe ? oe.nextSibling ? le.insertBefore(ne, oe.nextSibling) : le.appendChild(ne) : le.insertBefore(ne, le.firstChild), Y.push(ne);
          else if (Z.insertAt === "bottom")
            le.appendChild(ne);
          else {
            if (typeof Z.insertAt != "object" || !Z.insertAt.before)
              throw new Error(`[Style Loader]

 Invalid value for parameter 'insertAt' ('options.insertAt') found.
 Must be 'top', 'bottom', or Object.
 (https://github.com/webpack-contrib/style-loader#insertat)
`);
            var fe = x(Z.insertInto + " " + Z.insertAt.before);
            le.insertBefore(ne, fe);
          }
        }
        function z(Z) {
          if (Z.parentNode === null)
            return !1;
          Z.parentNode.removeChild(Z);
          var ne = Y.indexOf(Z);
          ne >= 0 && Y.splice(ne, 1);
        }
        function d(Z) {
          var ne = document.createElement("style");
          return Z.attrs.type === void 0 && (Z.attrs.type = "text/css"), j(ne, Z.attrs), U(Z, ne), ne;
        }
        function j(Z, ne) {
          Object.keys(ne).forEach(function(le) {
            Z.setAttribute(le, ne[le]);
          });
        }
        function R(Z, ne) {
          var le, oe, fe, Me;
          if (ne.transform && Z.css) {
            if (!(Me = ne.transform(Z.css)))
              return function() {
              };
            Z.css = Me;
          }
          if (ne.singleton) {
            var je = L++;
            le = M || (M = d(ne)), oe = J.bind(null, le, je, !1), fe = J.bind(null, le, je, !0);
          } else
            Z.sourceMap && typeof URL == "function" && typeof URL.createObjectURL == "function" && typeof URL.revokeObjectURL == "function" && typeof Blob == "function" && typeof btoa == "function" ? (le = function(Ae) {
              var Je = document.createElement("link");
              return Ae.attrs.type === void 0 && (Ae.attrs.type = "text/css"), Ae.attrs.rel = "stylesheet", j(Je, Ae.attrs), U(Ae, Je), Je;
            }(ne), oe = se.bind(null, le, ne), fe = function() {
              z(le), le.href && URL.revokeObjectURL(le.href);
            }) : (le = d(ne), oe = A.bind(null, le), fe = function() {
              z(le);
            });
          return oe(Z), function(Ae) {
            if (Ae) {
              if (Ae.css === Z.css && Ae.media === Z.media && Ae.sourceMap === Z.sourceMap)
                return;
              oe(Z = Ae);
            } else
              fe();
          };
        }
        c.exports = function(Z, ne) {
          if (typeof DEBUG < "u" && DEBUG && typeof document != "object")
            throw new Error("The style-loader cannot be used in a non-browser environment");
          (ne = ne || {}).attrs = typeof ne.attrs == "object" ? ne.attrs : {}, ne.singleton || typeof ne.singleton == "boolean" || (ne.singleton = y()), ne.insertInto || (ne.insertInto = "head"), ne.insertAt || (ne.insertAt = "bottom");
          var le = Q(Z, ne);
          return F(le, ne), function(oe) {
            for (var fe = [], Me = 0; Me < le.length; Me++) {
              var je = le[Me];
              (Ae = k[je.id]).refs--, fe.push(Ae);
            }
            for (oe && F(Q(oe, ne), ne), Me = 0; Me < fe.length; Me++) {
              var Ae;
              if ((Ae = fe[Me]).refs === 0) {
                for (var Je = 0; Je < Ae.parts.length; Je++)
                  Ae.parts[Je]();
                delete k[Ae.id];
              }
            }
          };
        };
        var V, K = (V = [], function(Z, ne) {
          return V[Z] = ne, V.filter(Boolean).join(`
`);
        });
        function J(Z, ne, le, oe) {
          var fe = le ? "" : oe.css;
          if (Z.styleSheet)
            Z.styleSheet.cssText = K(ne, fe);
          else {
            var Me = document.createTextNode(fe), je = Z.childNodes;
            je[ne] && Z.removeChild(je[ne]), je.length ? Z.insertBefore(Me, je[ne]) : Z.appendChild(Me);
          }
        }
        function A(Z, ne) {
          var le = ne.css, oe = ne.media;
          if (oe && Z.setAttribute("media", oe), Z.styleSheet)
            Z.styleSheet.cssText = le;
          else {
            for (; Z.firstChild; )
              Z.removeChild(Z.firstChild);
            Z.appendChild(document.createTextNode(le));
          }
        }
        function se(Z, ne, le) {
          var oe = le.css, fe = le.sourceMap, Me = ne.convertToAbsoluteUrls === void 0 && fe;
          (ne.convertToAbsoluteUrls || Me) && (oe = I(oe)), fe && (oe += `
/*# sourceMappingURL=data:application/json;base64,` + btoa(unescape(encodeURIComponent(JSON.stringify(fe)))) + " */");
          var je = new Blob([oe], { type: "text/css" }), Ae = Z.href;
          Z.href = URL.createObjectURL(je), Ae && URL.revokeObjectURL(Ae);
        }
      }, 947: (c) => {
        c.exports = function(u) {
          var p = typeof window < "u" && window.location;
          if (!p)
            throw new Error("fixUrls requires window.location");
          if (!u || typeof u != "string")
            return u;
          var g = p.protocol + "//" + p.host, v = g + p.pathname.replace(/\/[^\/]*$/, "/");
          return u.replace(/url\s*\(((?:[^)(]|\((?:[^)(]+|\([^)(]*\))*\))*)\)/gi, function(k, y) {
            var T, x = y.trim().replace(/^"(.*)"$/, function(M, L) {
              return L;
            }).replace(/^'(.*)'$/, function(M, L) {
              return L;
            });
            return /^(#|data:|http:\/\/|https:\/\/|file:\/\/\/|\s*$)/i.test(x) ? k : (T = x.indexOf("//") === 0 ? x : x.indexOf("/") === 0 ? g + x : v + x.replace(/^\.\//, ""), "url(" + JSON.stringify(T) + ")");
          });
        };
      } }, n = {};
      function r(c) {
        var u = n[c];
        if (u !== void 0)
          return u.exports;
        var p = n[c] = { id: c, exports: {} };
        return t[c](p, p.exports, r), p.exports;
      }
      r.n = (c) => {
        var u = c && c.__esModule ? () => c.default : () => c;
        return r.d(u, { a: u }), u;
      }, r.d = (c, u) => {
        for (var p in u)
          r.o(u, p) && !r.o(c, p) && Object.defineProperty(c, p, { enumerable: !0, get: u[p] });
      }, r.o = (c, u) => Object.prototype.hasOwnProperty.call(c, u), r.r = (c) => {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(c, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(c, "__esModule", { value: !0 });
      };
      var a = {};
      return (() => {
        function c(p, g) {
          for (var v = 0; v < g.length; v++) {
            var k = g[v];
            k.enumerable = k.enumerable || !1, k.configurable = !0, "value" in k && (k.writable = !0), Object.defineProperty(p, k.key, k);
          }
        }
        r.d(a, { default: () => u }), r(406);
        var u = function() {
          function p(y, T) {
            var x = y.configuration, M = y.blocks, L = y.toolbar, Y = y.save;
            (function(I, F) {
              if (!(I instanceof F))
                throw new TypeError("Cannot call a class as a function");
            })(this, p), this.toolbar = L, this.borderStyle = T || "1px dashed #aaa", this.api = M, this.holder = typeof x.holder == "string" ? document.getElementById(x.holder) : x.holder, this.readOnly = x.readOnly, this.startBlock = null, this.endBlock = null, this.save = Y, this.setDragListener(), this.setDropListener();
          }
          var g, v, k;
          return g = p, k = [{ key: "isReadOnlySupported", get: function() {
            return !0;
          } }], (v = [{ key: "setElementCursor", value: function(y) {
            if (y) {
              var T = document.createRange(), x = window.getSelection();
              T.setStart(y.childNodes[0], 0), T.collapse(!0), x.removeAllRanges(), x.addRange(T), y.focus();
            }
          } }, { key: "setDragListener", value: function() {
            var y = this;
            if (!this.readOnly) {
              var T = this.holder.querySelector(".ce-toolbar__settings-btn");
              T.setAttribute("draggable", "true"), T.addEventListener("dragstart", function() {
                y.startBlock = y.api.getCurrentBlockIndex();
              }), T.addEventListener("drag", function() {
                if (y.toolbar.close(), !y.isTheOnlyBlock()) {
                  var x = y.holder.querySelectorAll(".ce-block"), M = y.holder.querySelector(".ce-block--drop-target");
                  y.setElementCursor(M), y.setBorderBlocks(x, M);
                }
              });
            }
          } }, { key: "setBorderBlocks", value: function(y, T) {
            var x = this;
            Object.values(y).forEach(function(M) {
              var L = M.querySelector(".ce-block__content");
              M !== T ? (L.style.removeProperty("border-top"), L.style.removeProperty("border-bottom")) : Object.keys(y).find(function(Y) {
                return y[Y] === T;
              }) > x.startBlock ? L.style.borderBottom = x.borderStyle || borderStyle : L.style.borderTop = x.borderStyle;
            });
          } }, { key: "setDropListener", value: function() {
            var y = this;
            document.addEventListener("drop", function(T) {
              var x = T.target;
              if (y.holder.contains(x) && y.startBlock !== null) {
                var M = y.getDropTarget(x);
                if (M) {
                  var L = M.querySelector(".ce-block__content");
                  L.style.removeProperty("border-top"), L.style.removeProperty("border-bottom"), y.endBlock = y.getTargetPosition(M), y.moveBlocks();
                }
              }
              y.startBlock = null;
            });
          } }, { key: "getDropTarget", value: function(y) {
            return y.classList.contains("ce-block") ? y : y.closest(".ce-block");
          } }, { key: "getTargetPosition", value: function(y) {
            return Array.from(y.parentNode.children).indexOf(y);
          } }, { key: "isTheOnlyBlock", value: function() {
            return this.api.getBlocksCount() === 1;
          } }, { key: "moveBlocks", value: function() {
            this.isTheOnlyBlock() || this.api.move(this.endBlock, this.startBlock);
          } }]) && c(g.prototype, v), k && c(g, k), Object.defineProperty(g, "prototype", { writable: !1 }), p;
        }();
      })(), a.default;
    })();
  });
})(cu);
var Mg = cu.exports;
const Dg = /* @__PURE__ */ Dn(Mg);
var du = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, function() {
    return (() => {
      var t = { d: (p, g) => {
        for (var v in g)
          t.o(g, v) && !t.o(p, v) && Object.defineProperty(p, v, { enumerable: !0, get: g[v] });
      }, o: (p, g) => Object.prototype.hasOwnProperty.call(p, g) }, n = {};
      function r(p, g) {
        for (var v = 0; v < g.length; v++) {
          var k = g[v];
          k.enumerable = k.enumerable || !1, k.configurable = !0, "value" in k && (k.writable = !0), Object.defineProperty(p, k.key, k);
        }
      }
      t.d(n, { default: () => u });
      var a = function() {
        function p(k, y) {
          (function(T, x) {
            if (!(T instanceof x))
              throw new TypeError("Cannot call a class as a function");
          })(this, p), this.holder = y, this.observer = null, this.debounceTimer = 200, this.mutationDebouncer = this.debounce(function() {
            k();
          }, this.debounceTimer);
        }
        var g, v;
        return g = p, v = [{ key: "setMutationObserver", value: function() {
          var k = this, y = this.holder;
          this.observer = new MutationObserver(function(T) {
            k.mutationHandler(T);
          }), this.observer.observe(y, { childList: !0, attributes: !0, subtree: !0, characterData: !0, characterDataOldValue: !0 });
        } }, { key: "mutationHandler", value: function(k) {
          var y = this, T = !1;
          k.forEach(function(x) {
            switch (x.type) {
              case "childList":
                x.target === y.holder ? y.onDestroy() : T = !0;
                break;
              case "characterData":
                T = !0;
                break;
              case "attributes":
                x.target.classList.contains("ce-block") || (T = !0);
            }
          }), T && this.mutationDebouncer();
        } }, { key: "debounce", value: function(k, y) {
          var T, x = this;
          return function() {
            for (var M = arguments.length, L = new Array(M), Y = 0; Y < M; Y++)
              L[Y] = arguments[Y];
            var I = x;
            clearTimeout(T), T = setTimeout(function() {
              return k.apply(I, L);
            }, y);
          };
        } }, { key: "onDestroy", value: function() {
          var k = new CustomEvent("destroy");
          document.dispatchEvent(k), this.observer.disconnect();
        } }], v && r(g.prototype, v), p;
      }();
      function c(p, g) {
        for (var v = 0; v < g.length; v++) {
          var k = g[v];
          k.enumerable = k.enumerable || !1, k.configurable = !0, "value" in k && (k.writable = !0), Object.defineProperty(p, k.key, k);
        }
      }
      var u = function() {
        function p(y) {
          var T = this, x = y.editor, M = y.config, L = M === void 0 ? {} : M, Y = y.onUpdate, I = y.maxLength;
          (function(R, V) {
            if (!(R instanceof V))
              throw new TypeError("Cannot call a class as a function");
          })(this, p);
          var F = { maxLength: 30, onUpdate: function() {
          }, config: { shortcuts: { undo: "CMD+Z", redo: "CMD+Y" } } }, Q = x.configuration, U = Q.holder, z = F.config.shortcuts, d = L.shortcuts, j = d === void 0 ? z : d;
          this.holder = typeof U == "string" ? document.getElementById(U) : U, this.editor = x, this.shouldSaveHistory = !0, this.readOnly = Q.readOnly, this.maxLength = I || F.maxLength, this.onUpdate = Y || F.onUpdate, this.config = { shortcuts: j }, new a(function() {
            return T.registerChange();
          }, this.holder).setMutationObserver(), this.setEventListeners(), this.initialItem = null, this.clear();
        }
        var g, v, k;
        return g = p, k = [{ key: "isReadOnlySupported", get: function() {
          return !0;
        } }], (v = [{ key: "truncate", value: function(y, T) {
          for (; y.length > T; )
            y.shift();
        } }, { key: "initialize", value: function(y) {
          var T = "blocks" in y ? y.blocks : y, x = { index: T.length - 1, state: T };
          this.stack[0] = x, this.initialItem = x;
        } }, { key: "clear", value: function() {
          this.stack = this.initialItem ? [this.initialItem] : [{ index: 0, state: [{ type: "paragraph", data: { text: "" } }] }], this.position = 0, this.onUpdate();
        } }, { key: "registerChange", value: function() {
          var y = this;
          this.readOnly || (this.editor && this.editor.save && this.shouldSaveHistory && this.editor.save().then(function(T) {
            y.editorDidUpdate(T.blocks) && y.save(T.blocks);
          }), this.shouldSaveHistory = !0);
        } }, { key: "editorDidUpdate", value: function(y) {
          var T = this.stack[this.position].state;
          return !!y.length && (y.length !== T.length || JSON.stringify(T) !== JSON.stringify(y));
        } }, { key: "save", value: function(y) {
          this.position >= this.maxLength && this.truncate(this.stack, this.maxLength), this.position = Math.min(this.position, this.stack.length - 1), this.stack = this.stack.slice(0, this.position + 1);
          var T = this.editor.blocks.getCurrentBlockIndex();
          this.stack.push({ index: T, state: y }), this.position += 1, this.onUpdate();
        } }, { key: "undo", value: function() {
          var y = this;
          if (this.canUndo()) {
            this.shouldSaveHistory = !1;
            var T = this.stack[this.position -= 1], x = T.index, M = T.state;
            this.onUpdate(), this.editor.blocks.render({ blocks: M }).then(function() {
              return y.editor.caret.setToBlock(x, "end");
            });
          }
        } }, { key: "redo", value: function() {
          var y = this;
          if (this.canRedo()) {
            this.shouldSaveHistory = !1;
            var T = this.stack[this.position += 1], x = T.index, M = T.state;
            this.onUpdate(), this.editor.blocks.render({ blocks: M }).then(function() {
              return y.editor.caret.setToBlock(x, "end");
            });
          }
        } }, { key: "canUndo", value: function() {
          return !this.readOnly && this.position > 0;
        } }, { key: "canRedo", value: function() {
          return !this.readOnly && this.position < this.count();
        } }, { key: "count", value: function() {
          return this.stack.length - 1;
        } }, { key: "parseKeys", value: function(y) {
          var T = { CMD: /(Mac)/i.test(navigator.platform) ? "metaKey" : "ctrlKey", ALT: "altKey", SHIFT: "shiftKey" }, x = y.slice(0, -1).map(function(L) {
            return T[L];
          }), M = x.includes("shiftKey") ? y[y.length - 1].toUpperCase() : y[y.length - 1].toLowerCase();
          return x.push(M), x;
        } }, { key: "setEventListeners", value: function() {
          var y = this, T = this.holder, x = this.config.shortcuts, M = x.undo, L = x.redo, Y = M.replace(/ /g, "").split("+"), I = L.replace(/ /g, "").split("+"), F = this.parseKeys(Y), Q = this.parseKeys(I), U = function(j, R) {
            return !(R.length !== 2 || !j[R[0]] || j.key !== R[1]) || !(R.length !== 3 || !j[R[0]] || !j[R[1]] || j.key !== R[2]);
          }, z = function(j) {
            U(j, F) && (j.preventDefault(), y.undo());
          }, d = function(j) {
            U(j, Q) && (j.preventDefault(), y.redo());
          };
          T.addEventListener("keydown", z), T.addEventListener("keydown", d), T.addEventListener("destroy", function() {
            T.removeEventListener("keydown", z), T.removeEventListener("keydown", d);
          });
        } }]) && c(g.prototype, v), k && c(g, k), p;
      }();
      return n.default;
    })();
  });
})(du);
var Lg = du.exports;
const Og = /* @__PURE__ */ Dn(Lg);
var hu = { exports: {} };
/*! For license information please see bundle.js.LICENSE.txt */
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, () => (() => {
    var t = { 800: (c, u, p) => {
      p.d(u, { Z: () => T });
      var g = p(81), v = p.n(g), k = p(645), y = p.n(k)()(v());
      y.push([c.id, ".cdx-alert{position:relative;padding:10px;border-radius:5px;margin-bottom:10px}.cdx-alert-primary{background-color:#ebf8ff;border:1px solid #4299e1;color:#2b6cb0}.cdx-alert-secondary{background-color:#f7fafc;border:1px solid #cbd5e0;color:#222731}.cdx-alert-info{background-color:#e6fdff;border:1px solid #4cd4ce;color:#00727c}.cdx-alert-success{background-color:#f0fff4;border:1px solid #68d391;color:#2f855a}.cdx-alert-warning{background-color:#fffaf0;border:1px solid #ed8936;color:#c05621}.cdx-alert-danger{background-color:#fff5f5;border:1px solid #fc8181;color:#c53030}.cdx-alert-light{background-color:#fff;border:1px solid #edf2f7;color:#1a202c}.cdx-alert-dark{background-color:#2d3748;border:1px solid #1a202c;color:#d3d3d3}.cdx-alert-align-left{text-align:left}.cdx-alert-align-center{text-align:center}.cdx-alert-align-right{text-align:right}.cdx-alert__message{outline:none}.cdx-alert [contentEditable=true][data-placeholder]::before{position:absolute;content:attr(data-placeholder);color:#707684;font-weight:normal;opacity:0}.cdx-alert [contentEditable=true][data-placeholder]:empty::before{opacity:1}.cdx-alert [contentEditable=true][data-placeholder]:empty:focus::before{opacity:0}.ce-popover__item[data-item-name=alert-primary] .ce-popover__item-icon svg #background{fill:#ebf8ff;stroke:#4299e1}.ce-popover__item[data-item-name=alert-primary] .ce-popover__item-icon svg #content{fill:#2b6cb0}.ce-popover__item[data-item-name=alert-secondary] .ce-popover__item-icon svg #background{fill:#f7fafc;stroke:#cbd5e0}.ce-popover__item[data-item-name=alert-secondary] .ce-popover__item-icon svg #content{fill:#222731}.ce-popover__item[data-item-name=alert-info] .ce-popover__item-icon svg #background{fill:#e6fdff;stroke:#4cd4ce}.ce-popover__item[data-item-name=alert-info] .ce-popover__item-icon svg #content{fill:#00727c}.ce-popover__item[data-item-name=alert-success] .ce-popover__item-icon svg #background{fill:#f0fff4;stroke:#68d391}.ce-popover__item[data-item-name=alert-success] .ce-popover__item-icon svg #content{fill:#2f855a}.ce-popover__item[data-item-name=alert-warning] .ce-popover__item-icon svg #background{fill:#fffaf0;stroke:#ed8936}.ce-popover__item[data-item-name=alert-warning] .ce-popover__item-icon svg #content{fill:#c05621}.ce-popover__item[data-item-name=alert-danger] .ce-popover__item-icon svg #background{fill:#fff5f5;stroke:#fc8181}.ce-popover__item[data-item-name=alert-danger] .ce-popover__item-icon svg #content{fill:#c53030}.ce-popover__item[data-item-name=alert-light] .ce-popover__item-icon svg #background{fill:#fff;stroke:#edf2f7}.ce-popover__item[data-item-name=alert-light] .ce-popover__item-icon svg #content{fill:#1a202c}.ce-popover__item[data-item-name=alert-dark] .ce-popover__item-icon svg #background{fill:#2d3748;stroke:#1a202c}.ce-popover__item[data-item-name=alert-dark] .ce-popover__item-icon svg #content{fill:#d3d3d3}", ""]);
      const T = y;
    }, 645: (c) => {
      c.exports = function(u) {
        var p = [];
        return p.toString = function() {
          return this.map(function(g) {
            var v = "", k = g[5] !== void 0;
            return g[4] && (v += "@supports (".concat(g[4], ") {")), g[2] && (v += "@media ".concat(g[2], " {")), k && (v += "@layer".concat(g[5].length > 0 ? " ".concat(g[5]) : "", " {")), v += u(g), k && (v += "}"), g[2] && (v += "}"), g[4] && (v += "}"), v;
          }).join("");
        }, p.i = function(g, v, k, y, T) {
          typeof g == "string" && (g = [[null, g, void 0]]);
          var x = {};
          if (k)
            for (var M = 0; M < this.length; M++) {
              var L = this[M][0];
              L != null && (x[L] = !0);
            }
          for (var Y = 0; Y < g.length; Y++) {
            var I = [].concat(g[Y]);
            k && x[I[0]] || (T !== void 0 && (I[5] === void 0 || (I[1] = "@layer".concat(I[5].length > 0 ? " ".concat(I[5]) : "", " {").concat(I[1], "}")), I[5] = T), v && (I[2] && (I[1] = "@media ".concat(I[2], " {").concat(I[1], "}")), I[2] = v), y && (I[4] ? (I[1] = "@supports (".concat(I[4], ") {").concat(I[1], "}"), I[4] = y) : I[4] = "".concat(y)), p.push(I));
          }
        }, p;
      };
    }, 81: (c) => {
      c.exports = function(u) {
        return u[1];
      };
    }, 620: (c, u, p) => {
      var g = p(379), v = p.n(g), k = p(795), y = p.n(k), T = p(569), x = p.n(T), M = p(565), L = p.n(M), Y = p(216), I = p.n(Y), F = p(589), Q = p.n(F), U = p(800), z = {};
      z.styleTagTransform = Q(), z.setAttributes = L(), z.insert = x().bind(null, "head"), z.domAPI = y(), z.insertStyleElement = I(), v()(U.Z, z), U.Z && U.Z.locals && U.Z.locals;
    }, 379: (c) => {
      var u = [];
      function p(k) {
        for (var y = -1, T = 0; T < u.length; T++)
          if (u[T].identifier === k) {
            y = T;
            break;
          }
        return y;
      }
      function g(k, y) {
        for (var T = {}, x = [], M = 0; M < k.length; M++) {
          var L = k[M], Y = y.base ? L[0] + y.base : L[0], I = T[Y] || 0, F = "".concat(Y, " ").concat(I);
          T[Y] = I + 1;
          var Q = p(F), U = { css: L[1], media: L[2], sourceMap: L[3], supports: L[4], layer: L[5] };
          if (Q !== -1)
            u[Q].references++, u[Q].updater(U);
          else {
            var z = v(U, y);
            y.byIndex = M, u.splice(M, 0, { identifier: F, updater: z, references: 1 });
          }
          x.push(F);
        }
        return x;
      }
      function v(k, y) {
        var T = y.domAPI(y);
        return T.update(k), function(x) {
          if (x) {
            if (x.css === k.css && x.media === k.media && x.sourceMap === k.sourceMap && x.supports === k.supports && x.layer === k.layer)
              return;
            T.update(k = x);
          } else
            T.remove();
        };
      }
      c.exports = function(k, y) {
        var T = g(k = k || [], y = y || {});
        return function(x) {
          x = x || [];
          for (var M = 0; M < T.length; M++) {
            var L = p(T[M]);
            u[L].references--;
          }
          for (var Y = g(x, y), I = 0; I < T.length; I++) {
            var F = p(T[I]);
            u[F].references === 0 && (u[F].updater(), u.splice(F, 1));
          }
          T = Y;
        };
      };
    }, 569: (c) => {
      var u = {};
      c.exports = function(p, g) {
        var v = function(k) {
          if (u[k] === void 0) {
            var y = document.querySelector(k);
            if (window.HTMLIFrameElement && y instanceof window.HTMLIFrameElement)
              try {
                y = y.contentDocument.head;
              } catch {
                y = null;
              }
            u[k] = y;
          }
          return u[k];
        }(p);
        if (!v)
          throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
        v.appendChild(g);
      };
    }, 216: (c) => {
      c.exports = function(u) {
        var p = document.createElement("style");
        return u.setAttributes(p, u.attributes), u.insert(p, u.options), p;
      };
    }, 565: (c, u, p) => {
      c.exports = function(g) {
        var v = p.nc;
        v && g.setAttribute("nonce", v);
      };
    }, 795: (c) => {
      c.exports = function(u) {
        if (typeof document > "u")
          return { update: function() {
          }, remove: function() {
          } };
        var p = u.insertStyleElement(u);
        return { update: function(g) {
          (function(v, k, y) {
            var T = "";
            y.supports && (T += "@supports (".concat(y.supports, ") {")), y.media && (T += "@media ".concat(y.media, " {"));
            var x = y.layer !== void 0;
            x && (T += "@layer".concat(y.layer.length > 0 ? " ".concat(y.layer) : "", " {")), T += y.css, x && (T += "}"), y.media && (T += "}"), y.supports && (T += "}");
            var M = y.sourceMap;
            M && typeof btoa < "u" && (T += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(M)))), " */")), k.styleTagTransform(T, v, k.options);
          })(p, u, g);
        }, remove: function() {
          (function(g) {
            if (g.parentNode === null)
              return !1;
            g.parentNode.removeChild(g);
          })(p);
        } };
      };
    }, 589: (c) => {
      c.exports = function(u, p) {
        if (p.styleSheet)
          p.styleSheet.cssText = u;
        else {
          for (; p.firstChild; )
            p.removeChild(p.firstChild);
          p.appendChild(document.createTextNode(u));
        }
      };
    }, 749: (c) => {
      c.exports = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M18 7L6 7"></path><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M18 17H6"></path><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M16 12L8 12"></path></svg>';
    }, 454: (c) => {
      c.exports = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M17 7L5 7"></path><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M17 17H5"></path><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M13 12L5 12"></path></svg>';
    }, 431: (c) => {
      c.exports = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M19 7L7 7"></path><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M19 17H7"></path><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M19 12L11 12"></path></svg>';
    }, 654: (c) => {
      c.exports = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M5 12a7 7 90 0 1 14 0 7 7 90 0 1-14 0zM12 9.02v-.01M12 12v3"></path></svg>';
    }, 338: (c) => {
      c.exports = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"><rect id="background" width="18" height="18" x="3" y="3" fill="none" stroke="currentColor" stroke-width="1" rx="4"></rect><path id="content" fill="currentColor" stroke-width="0" d="m15.579 16.9-1.042-2.25H10.15L9.11 16.9H7.113L11.697 7h1.266l4.612 9.9h-1.998Zm-4.613-4.05h2.755l-1.377-2.953-1.379 2.953Z"></path></svg>';
    } }, n = {};
    function r(c) {
      var u = n[c];
      if (u !== void 0)
        return u.exports;
      var p = n[c] = { id: c, exports: {} };
      return t[c](p, p.exports, r), p.exports;
    }
    r.n = (c) => {
      var u = c && c.__esModule ? () => c.default : () => c;
      return r.d(u, { a: u }), u;
    }, r.d = (c, u) => {
      for (var p in u)
        r.o(u, p) && !r.o(c, p) && Object.defineProperty(c, p, { enumerable: !0, get: u[p] });
    }, r.o = (c, u) => Object.prototype.hasOwnProperty.call(c, u), r.nc = void 0;
    var a = {};
    return (() => {
      r.d(a, { default: () => j });
      var c = r(654), u = r.n(c), p = r(338), g = r.n(p), v = r(454), k = r.n(v), y = r(749), T = r.n(y), x = r(431), M = r.n(x);
      function L(R) {
        return L = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(V) {
          return typeof V;
        } : function(V) {
          return V && typeof Symbol == "function" && V.constructor === Symbol && V !== Symbol.prototype ? "symbol" : typeof V;
        }, L(R);
      }
      function Y(R, V) {
        var K = Object.keys(R);
        if (Object.getOwnPropertySymbols) {
          var J = Object.getOwnPropertySymbols(R);
          V && (J = J.filter(function(A) {
            return Object.getOwnPropertyDescriptor(R, A).enumerable;
          })), K.push.apply(K, J);
        }
        return K;
      }
      function I(R) {
        for (var V = 1; V < arguments.length; V++) {
          var K = arguments[V] != null ? arguments[V] : {};
          V % 2 ? Y(Object(K), !0).forEach(function(J) {
            F(R, J, K[J]);
          }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(R, Object.getOwnPropertyDescriptors(K)) : Y(Object(K)).forEach(function(J) {
            Object.defineProperty(R, J, Object.getOwnPropertyDescriptor(K, J));
          });
        }
        return R;
      }
      function F(R, V, K) {
        return (V = d(V)) in R ? Object.defineProperty(R, V, { value: K, enumerable: !0, configurable: !0, writable: !0 }) : R[V] = K, R;
      }
      function Q(R) {
        return function(V) {
          if (Array.isArray(V))
            return U(V);
        }(R) || function(V) {
          if (typeof Symbol < "u" && V[Symbol.iterator] != null || V["@@iterator"] != null)
            return Array.from(V);
        }(R) || function(V, K) {
          if (V) {
            if (typeof V == "string")
              return U(V, K);
            var J = Object.prototype.toString.call(V).slice(8, -1);
            return J === "Object" && V.constructor && (J = V.constructor.name), J === "Map" || J === "Set" ? Array.from(V) : J === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(J) ? U(V, K) : void 0;
          }
        }(R) || function() {
          throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
        }();
      }
      function U(R, V) {
        (V == null || V > R.length) && (V = R.length);
        for (var K = 0, J = new Array(V); K < V; K++)
          J[K] = R[K];
        return J;
      }
      function z(R, V) {
        for (var K = 0; K < V.length; K++) {
          var J = V[K];
          J.enumerable = J.enumerable || !1, J.configurable = !0, "value" in J && (J.writable = !0), Object.defineProperty(R, d(J.key), J);
        }
      }
      function d(R) {
        var V = function(K, J) {
          if (L(K) !== "object" || K === null)
            return K;
          var A = K[Symbol.toPrimitive];
          if (A !== void 0) {
            var se = A.call(K, "string");
            if (L(se) !== "object")
              return se;
            throw new TypeError("@@toPrimitive must return a primitive value.");
          }
          return String(K);
        }(R);
        return L(V) === "symbol" ? V : String(V);
      }
      r(620).toString();
      var j = function() {
        function R(A) {
          var se = A.data, Z = A.config, ne = A.api, le = A.readOnly;
          (function(oe, fe) {
            if (!(oe instanceof fe))
              throw new TypeError("Cannot call a class as a function");
          })(this, R), this.api = ne, this.defaultType = Z.defaultType || R.DEFAULT_TYPE, this.defaultAlign = Z.defaultAlign || R.DEFAULT_ALIGN_TYPE, this.messagePlaceholder = Z.messagePlaceholder || R.DEFAULT_MESSAGE_PLACEHOLDER, this.data = { type: R.ALERT_TYPES.includes(se.type) ? se.type : this.defaultType, align: R.ALIGN_TYPES.includes(se.align) ? se.align : this.defaultAlign, message: se.message || "" }, this.container = void 0, this.readOnly = le;
        }
        var V, K, J;
        return V = R, K = [{ key: "CSS", get: function() {
          return { wrapper: "cdx-alert", wrapperForType: function(A) {
            return "cdx-alert-".concat(A);
          }, wrapperForAlignType: function(A) {
            return "cdx-alert-align-".concat(A);
          }, message: "cdx-alert__message" };
        } }, { key: "render", value: function() {
          var A = [this.CSS.wrapper, this.CSS.wrapperForType(this.data.type), this.CSS.wrapperForAlignType(this.data.align)];
          this.container = this._make("div", A);
          var se = this._make("div", [this.CSS.message], { contentEditable: !this.readOnly, innerHTML: this.data.message });
          return se.dataset.placeholder = this.messagePlaceholder, this.container.appendChild(se), this.container;
        } }, { key: "renderSettings", value: function() {
          var A = this, se = R.ALERT_TYPES.map(function(ne) {
            return { icon: g(), name: "alert-".concat(ne), label: A._getFormattedName(ne), toggle: "alert", isActive: A.data.type === ne, onActivate: function() {
              A._changeAlertType(ne);
            } };
          }), Z = R.ALIGN_TYPES.map(function(ne) {
            return { icon: ne == "left" ? k() : ne == "center" ? T() : ne == "right" ? M() : IconAlign_left, name: "align-".concat(ne), label: A._getFormattedName(ne), toggle: "align", isActive: A.data.align === ne, onActivate: function() {
              A._changeAlignType(ne);
            } };
          });
          return [].concat(Q(se), Q(Z));
        } }, { key: "_getFormattedName", value: function(A) {
          return this.api.i18n.t(A.charAt(0).toUpperCase() + A.slice(1));
        } }, { key: "_changeAlertType", value: function(A) {
          var se = this;
          this.data.type = A, R.ALERT_TYPES.forEach(function(Z) {
            var ne = se.CSS.wrapperForType(Z);
            se.container.classList.remove(ne), A === Z && se.container.classList.add(ne);
          });
        } }, { key: "_changeAlignType", value: function(A) {
          var se = this;
          this.data.align = A, R.ALIGN_TYPES.forEach(function(Z) {
            var ne = se.CSS.wrapperForAlignType(Z);
            se.container.classList.remove(ne), A === Z && se.container.classList.add(ne);
          });
        } }, { key: "save", value: function(A) {
          var se = A.querySelector(".".concat(this.CSS.message));
          return I(I({}, this.data), {}, { message: se.innerHTML });
        } }, { key: "_make", value: function(A) {
          var se, Z = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null, ne = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, le = document.createElement(A);
          for (var oe in Array.isArray(Z) ? (se = le.classList).add.apply(se, Q(Z)) : Z && le.classList.add(Z), ne)
            le[oe] = ne[oe];
          return le;
        } }, { key: "onPaste", value: function(A) {
          var se = A.detail.data;
          this.data = { type: this.defaultType, message: se.innerHTML || "" };
        } }], J = [{ key: "toolbox", get: function() {
          return { icon: u(), title: "Alert" };
        } }, { key: "enableLineBreaks", get: function() {
          return !0;
        } }, { key: "DEFAULT_TYPE", get: function() {
          return "info";
        } }, { key: "DEFAULT_ALIGN_TYPE", get: function() {
          return "left";
        } }, { key: "DEFAULT_MESSAGE_PLACEHOLDER", get: function() {
          return "Type here...";
        } }, { key: "ALERT_TYPES", get: function() {
          return ["primary", "secondary", "info", "success", "warning", "danger", "light", "dark"];
        } }, { key: "ALIGN_TYPES", get: function() {
          return ["left", "center", "right"];
        } }, { key: "isReadOnlySupported", get: function() {
          return !0;
        } }, { key: "conversionConfig", get: function() {
          var A = this;
          return { export: function(se) {
            return se.message;
          }, import: function(se) {
            return { message: se, type: A.DEFAULT_TYPE, alignType: A.DEFAULT_ALIGN_TYPE };
          } };
        } }, { key: "sanitize", get: function() {
          return { message: !0, type: !1, alignType: !1 };
        } }], K && z(V.prototype, K), J && z(V, J), Object.defineProperty(V, "prototype", { writable: !1 }), R;
      }();
    })(), a.default;
  })());
})(hu);
var Bg = hu.exports;
const Ld = /* @__PURE__ */ Dn(Bg);
var uu = { exports: {} };
/*! For license information please see editorjs-columns.bundle.js.LICENSE.txt */
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, () => (() => {
    var t = { 745: (c, u, p) => {
      p.d(u, { Z: () => T });
      var g = p(81), v = p.n(g), k = p(645), y = p.n(k)()(v());
      y.push([c.id, ".ce-editorjsColumns_col{flex:50%}.ce-editorjsColumns_wrapper{display:flex;width:100%;gap:10px;margin-bottom:10px;flex-direction:row}.ce-editorjsColumns_wrapper .ce-toolbar__actions{z-index:0}.ce-editorjsColumns_wrapper .ce-toolbar{z-index:4}.ce-editorjsColumns_wrapper .ce-popover{z-index:4000}@media(max-width: 800px){.ce-editorjsColumns_wrapper{flex-direction:column;padding:10px;border:1px solid #ccc;border-radius:4px}}.ce-inline-toolbar{z-index:1000}.ce-block__content,.ce-toolbar__content{max-width:calc(100% - 50px)}.ce-toolbar__actions{right:calc(100% + 30px);background-color:rgba(255,255,255,.5);border-radius:4px}.codex-editor--narrow .codex-editor__redactor{margin:0}.ce-toolbar{z-index:4}.codex-editor{z-index:auto !important}", ""]);
      const T = y;
    }, 645: (c) => {
      c.exports = function(u) {
        var p = [];
        return p.toString = function() {
          return this.map(function(g) {
            var v = "", k = g[5] !== void 0;
            return g[4] && (v += "@supports (".concat(g[4], ") {")), g[2] && (v += "@media ".concat(g[2], " {")), k && (v += "@layer".concat(g[5].length > 0 ? " ".concat(g[5]) : "", " {")), v += u(g), k && (v += "}"), g[2] && (v += "}"), g[4] && (v += "}"), v;
          }).join("");
        }, p.i = function(g, v, k, y, T) {
          typeof g == "string" && (g = [[null, g, void 0]]);
          var x = {};
          if (k)
            for (var M = 0; M < this.length; M++) {
              var L = this[M][0];
              L != null && (x[L] = !0);
            }
          for (var Y = 0; Y < g.length; Y++) {
            var I = [].concat(g[Y]);
            k && x[I[0]] || (T !== void 0 && (I[5] === void 0 || (I[1] = "@layer".concat(I[5].length > 0 ? " ".concat(I[5]) : "", " {").concat(I[1], "}")), I[5] = T), v && (I[2] && (I[1] = "@media ".concat(I[2], " {").concat(I[1], "}")), I[2] = v), y && (I[4] ? (I[1] = "@supports (".concat(I[4], ") {").concat(I[1], "}"), I[4] = y) : I[4] = "".concat(y)), p.push(I));
          }
        }, p;
      };
    }, 81: (c) => {
      c.exports = function(u) {
        return u[1];
      };
    }, 379: (c) => {
      var u = [];
      function p(k) {
        for (var y = -1, T = 0; T < u.length; T++)
          if (u[T].identifier === k) {
            y = T;
            break;
          }
        return y;
      }
      function g(k, y) {
        for (var T = {}, x = [], M = 0; M < k.length; M++) {
          var L = k[M], Y = y.base ? L[0] + y.base : L[0], I = T[Y] || 0, F = "".concat(Y, " ").concat(I);
          T[Y] = I + 1;
          var Q = p(F), U = { css: L[1], media: L[2], sourceMap: L[3], supports: L[4], layer: L[5] };
          if (Q !== -1)
            u[Q].references++, u[Q].updater(U);
          else {
            var z = v(U, y);
            y.byIndex = M, u.splice(M, 0, { identifier: F, updater: z, references: 1 });
          }
          x.push(F);
        }
        return x;
      }
      function v(k, y) {
        var T = y.domAPI(y);
        return T.update(k), function(x) {
          if (x) {
            if (x.css === k.css && x.media === k.media && x.sourceMap === k.sourceMap && x.supports === k.supports && x.layer === k.layer)
              return;
            T.update(k = x);
          } else
            T.remove();
        };
      }
      c.exports = function(k, y) {
        var T = g(k = k || [], y = y || {});
        return function(x) {
          x = x || [];
          for (var M = 0; M < T.length; M++) {
            var L = p(T[M]);
            u[L].references--;
          }
          for (var Y = g(x, y), I = 0; I < T.length; I++) {
            var F = p(T[I]);
            u[F].references === 0 && (u[F].updater(), u.splice(F, 1));
          }
          T = Y;
        };
      };
    }, 569: (c) => {
      var u = {};
      c.exports = function(p, g) {
        var v = function(k) {
          if (u[k] === void 0) {
            var y = document.querySelector(k);
            if (window.HTMLIFrameElement && y instanceof window.HTMLIFrameElement)
              try {
                y = y.contentDocument.head;
              } catch {
                y = null;
              }
            u[k] = y;
          }
          return u[k];
        }(p);
        if (!v)
          throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
        v.appendChild(g);
      };
    }, 216: (c) => {
      c.exports = function(u) {
        var p = document.createElement("style");
        return u.setAttributes(p, u.attributes), u.insert(p, u.options), p;
      };
    }, 565: (c, u, p) => {
      c.exports = function(g) {
        var v = p.nc;
        v && g.setAttribute("nonce", v);
      };
    }, 795: (c) => {
      c.exports = function(u) {
        var p = u.insertStyleElement(u);
        return { update: function(g) {
          (function(v, k, y) {
            var T = "";
            y.supports && (T += "@supports (".concat(y.supports, ") {")), y.media && (T += "@media ".concat(y.media, " {"));
            var x = y.layer !== void 0;
            x && (T += "@layer".concat(y.layer.length > 0 ? " ".concat(y.layer) : "", " {")), T += y.css, x && (T += "}"), y.media && (T += "}"), y.supports && (T += "}");
            var M = y.sourceMap;
            M && typeof btoa < "u" && (T += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(M)))), " */")), k.styleTagTransform(T, v, k.options);
          })(p, u, g);
        }, remove: function() {
          (function(g) {
            if (g.parentNode === null)
              return !1;
            g.parentNode.removeChild(g);
          })(p);
        } };
      };
    }, 589: (c) => {
      c.exports = function(u, p) {
        if (p.styleSheet)
          p.styleSheet.cssText = u;
        else {
          for (; p.firstChild; )
            p.removeChild(p.firstChild);
          p.appendChild(document.createTextNode(u));
        }
      };
    }, 40: (c) => {
      c.exports = '<svg width="14" height="14" viewBox="0 -1 14 14" xmlns="http://www.w3.org/2000/svg"><defs></defs><rect x="1.194" y="-0.041" width="11.601" height="12.11" rx="1" ry="1" style="fill: rgb(255, 255, 255); stroke: rgb(0, 0, 0); stroke-width: 1.5px;"></rect><path style="stroke: rgb(0, 0, 0); stroke-width:1.5px" d="M 7.032 -0.034 L 6.948 11.842"></path></svg>';
    }, 455: function(c) {
      c.exports = function() {
        const u = "SweetAlert2:", p = (w) => w.charAt(0).toUpperCase() + w.slice(1), g = (w) => Array.prototype.slice.call(w), v = (w) => {
          console.warn("".concat(u, " ").concat(typeof w == "object" ? w.join(" ") : w));
        }, k = (w) => {
          console.error("".concat(u, " ").concat(w));
        }, y = [], T = (w, S) => {
          var B;
          B = '"'.concat(w, '" is deprecated and will be removed in the next major release. Please use "').concat(S, '" instead.'), y.includes(B) || (y.push(B), v(B));
        }, x = (w) => typeof w == "function" ? w() : w, M = (w) => w && typeof w.toPromise == "function", L = (w) => M(w) ? w.toPromise() : Promise.resolve(w), Y = (w) => w && Promise.resolve(w) === w, I = { title: "", titleText: "", text: "", html: "", footer: "", icon: void 0, iconColor: void 0, iconHtml: void 0, template: void 0, toast: !1, showClass: { popup: "swal2-show", backdrop: "swal2-backdrop-show", icon: "swal2-icon-show" }, hideClass: { popup: "swal2-hide", backdrop: "swal2-backdrop-hide", icon: "swal2-icon-hide" }, customClass: {}, target: "body", color: void 0, backdrop: !0, heightAuto: !0, allowOutsideClick: !0, allowEscapeKey: !0, allowEnterKey: !0, stopKeydownPropagation: !0, keydownListenerCapture: !1, showConfirmButton: !0, showDenyButton: !1, showCancelButton: !1, preConfirm: void 0, preDeny: void 0, confirmButtonText: "OK", confirmButtonAriaLabel: "", confirmButtonColor: void 0, denyButtonText: "No", denyButtonAriaLabel: "", denyButtonColor: void 0, cancelButtonText: "Cancel", cancelButtonAriaLabel: "", cancelButtonColor: void 0, buttonsStyling: !0, reverseButtons: !1, focusConfirm: !0, focusDeny: !1, focusCancel: !1, returnFocus: !0, showCloseButton: !1, closeButtonHtml: "&times;", closeButtonAriaLabel: "Close this dialog", loaderHtml: "", showLoaderOnConfirm: !1, showLoaderOnDeny: !1, imageUrl: void 0, imageWidth: void 0, imageHeight: void 0, imageAlt: "", timer: void 0, timerProgressBar: !1, width: void 0, padding: void 0, background: void 0, input: void 0, inputPlaceholder: "", inputLabel: "", inputValue: "", inputOptions: {}, inputAutoTrim: !0, inputAttributes: {}, inputValidator: void 0, returnInputValueOnDeny: !1, validationMessage: void 0, grow: !1, position: "center", progressSteps: [], currentProgressStep: void 0, progressStepsDistance: void 0, willOpen: void 0, didOpen: void 0, didRender: void 0, willClose: void 0, didClose: void 0, didDestroy: void 0, scrollbarPadding: !0 }, F = ["allowEscapeKey", "allowOutsideClick", "background", "buttonsStyling", "cancelButtonAriaLabel", "cancelButtonColor", "cancelButtonText", "closeButtonAriaLabel", "closeButtonHtml", "color", "confirmButtonAriaLabel", "confirmButtonColor", "confirmButtonText", "currentProgressStep", "customClass", "denyButtonAriaLabel", "denyButtonColor", "denyButtonText", "didClose", "didDestroy", "footer", "hideClass", "html", "icon", "iconColor", "iconHtml", "imageAlt", "imageHeight", "imageUrl", "imageWidth", "preConfirm", "preDeny", "progressSteps", "returnFocus", "reverseButtons", "showCancelButton", "showCloseButton", "showConfirmButton", "showDenyButton", "text", "title", "titleText", "willClose"], Q = {}, U = ["allowOutsideClick", "allowEnterKey", "backdrop", "focusConfirm", "focusDeny", "focusCancel", "returnFocus", "heightAuto", "keydownListenerCapture"], z = (w) => Object.prototype.hasOwnProperty.call(I, w), d = (w) => F.indexOf(w) !== -1, j = (w) => Q[w], R = (w) => {
          z(w) || v('Unknown parameter "'.concat(w, '"'));
        }, V = (w) => {
          U.includes(w) && v('The parameter "'.concat(w, '" is incompatible with toasts'));
        }, K = (w) => {
          j(w) && T(w, j(w));
        }, J = (w) => {
          const S = {};
          for (const B in w)
            S[w[B]] = "swal2-" + w[B];
          return S;
        }, A = J(["container", "shown", "height-auto", "iosfix", "popup", "modal", "no-backdrop", "no-transition", "toast", "toast-shown", "show", "hide", "close", "title", "html-container", "actions", "confirm", "deny", "cancel", "default-outline", "footer", "icon", "icon-content", "image", "input", "file", "range", "select", "radio", "checkbox", "label", "textarea", "inputerror", "input-label", "validation-message", "progress-steps", "active-progress-step", "progress-step", "progress-step-line", "loader", "loading", "styled", "top", "top-start", "top-end", "top-left", "top-right", "center", "center-start", "center-end", "center-left", "center-right", "bottom", "bottom-start", "bottom-end", "bottom-left", "bottom-right", "grow-row", "grow-column", "grow-fullscreen", "rtl", "timer-progress-bar", "timer-progress-bar-container", "scrollbar-measure", "icon-success", "icon-warning", "icon-info", "icon-question", "icon-error", "no-war"]), se = J(["success", "warning", "info", "question", "error"]), Z = () => document.body.querySelector(".".concat(A.container)), ne = (w) => {
          const S = Z();
          return S ? S.querySelector(w) : null;
        }, le = (w) => ne(".".concat(w)), oe = () => le(A.popup), fe = () => le(A.icon), Me = () => le(A.title), je = () => le(A["html-container"]), Ae = () => le(A.image), Je = () => le(A["progress-steps"]), no = () => le(A["validation-message"]), yo = () => ne(".".concat(A.actions, " .").concat(A.confirm)), Qo = () => ne(".".concat(A.actions, " .").concat(A.deny)), Bn = () => ne(".".concat(A.loader)), hn = () => ne(".".concat(A.actions, " .").concat(A.cancel)), un = () => le(A.actions), Rt = () => le(A.footer), Di = () => le(A["timer-progress-bar"]), io = () => le(A.close), Ho = () => {
          const w = g(oe().querySelectorAll('[tabindex]:not([tabindex="-1"]):not([tabindex="0"])')).sort((B, H) => {
            const ee = parseInt(B.getAttribute("tabindex")), he = parseInt(H.getAttribute("tabindex"));
            return ee > he ? 1 : ee < he ? -1 : 0;
          }), S = g(oe().querySelectorAll(`
  a[href],
  area[href],
  input:not([disabled]),
  select:not([disabled]),
  textarea:not([disabled]),
  button:not([disabled]),
  iframe,
  object,
  embed,
  [tabindex="0"],
  [contenteditable],
  audio[controls],
  video[controls],
  summary
`)).filter((B) => B.getAttribute("tabindex") !== "-1");
          return ((B) => {
            const H = [];
            for (let ee = 0; ee < B.length; ee++)
              H.indexOf(B[ee]) === -1 && H.push(B[ee]);
            return H;
          })(w.concat(S)).filter((B) => Xt(B));
        }, Un = () => vt(document.body, A.shown) && !vt(document.body, A["toast-shown"]) && !vt(document.body, A["no-backdrop"]), Li = () => oe() && vt(oe(), A.toast), Mo = { previousBodyPadding: null }, Ct = (w, S) => {
          if (w.textContent = "", S) {
            const B = new DOMParser().parseFromString(S, "text/html");
            g(B.querySelector("head").childNodes).forEach((H) => {
              w.appendChild(H);
            }), g(B.querySelector("body").childNodes).forEach((H) => {
              w.appendChild(H);
            });
          }
        }, vt = (w, S) => {
          if (!S)
            return !1;
          const B = S.split(/\s+/);
          for (let H = 0; H < B.length; H++)
            if (!w.classList.contains(B[H]))
              return !1;
          return !0;
        }, uo = (w, S, B) => {
          if (((H, ee) => {
            g(H.classList).forEach((he) => {
              Object.values(A).includes(he) || Object.values(se).includes(he) || Object.values(ee.showClass).includes(he) || H.classList.remove(he);
            });
          })(w, S), S.customClass && S.customClass[B]) {
            if (typeof S.customClass[B] != "string" && !S.customClass[B].forEach)
              return v("Invalid type of customClass.".concat(B, '! Expected string or iterable object, got "').concat(typeof S.customClass[B], '"'));
            We(w, S.customClass[B]);
          }
        }, er = (w, S) => {
          if (!S)
            return null;
          switch (S) {
            case "select":
            case "textarea":
            case "file":
              return w.querySelector(".".concat(A.popup, " > .").concat(A[S]));
            case "checkbox":
              return w.querySelector(".".concat(A.popup, " > .").concat(A.checkbox, " input"));
            case "radio":
              return w.querySelector(".".concat(A.popup, " > .").concat(A.radio, " input:checked")) || w.querySelector(".".concat(A.popup, " > .").concat(A.radio, " input:first-child"));
            case "range":
              return w.querySelector(".".concat(A.popup, " > .").concat(A.range, " input"));
            default:
              return w.querySelector(".".concat(A.popup, " > .").concat(A.input));
          }
        }, Nr = (w) => {
          if (w.focus(), w.type !== "file") {
            const S = w.value;
            w.value = "", w.value = S;
          }
        }, It = (w, S, B) => {
          w && S && (typeof S == "string" && (S = S.split(/\s+/).filter(Boolean)), S.forEach((H) => {
            Array.isArray(w) ? w.forEach((ee) => {
              B ? ee.classList.add(H) : ee.classList.remove(H);
            }) : B ? w.classList.add(H) : w.classList.remove(H);
          }));
        }, We = (w, S) => {
          It(w, S, !0);
        }, Ft = (w, S) => {
          It(w, S, !1);
        }, ce = (w, S) => {
          const B = g(w.childNodes);
          for (let H = 0; H < B.length; H++)
            if (vt(B[H], S))
              return B[H];
        }, nt = (w, S, B) => {
          B === "".concat(parseInt(B)) && (B = parseInt(B)), B || parseInt(B) === 0 ? w.style[S] = typeof B == "number" ? "".concat(B, "px") : B : w.style.removeProperty(S);
        }, Wt = function(w) {
          let S = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "flex";
          w.style.display = S;
        }, Kt = (w) => {
          w.style.display = "none";
        }, Pr = (w, S, B, H) => {
          const ee = w.querySelector(S);
          ee && (ee.style[B] = H);
        }, An = (w, S, B) => {
          S ? Wt(w, B) : Kt(w);
        }, Xt = (w) => !(!w || !(w.offsetWidth || w.offsetHeight || w.getClientRects().length)), Do = (w) => w.scrollHeight > w.clientHeight, zt = (w) => {
          const S = window.getComputedStyle(w), B = parseFloat(S.getPropertyValue("animation-duration") || "0"), H = parseFloat(S.getPropertyValue("transition-duration") || "0");
          return B > 0 || H > 0;
        }, Jt = function(w) {
          let S = arguments.length > 1 && arguments[1] !== void 0 && arguments[1];
          const B = Di();
          Xt(B) && (S && (B.style.transition = "none", B.style.width = "100%"), setTimeout(() => {
            B.style.transition = "width ".concat(w / 1e3, "s linear"), B.style.width = "0%";
          }, 10));
        }, po = () => typeof window > "u" || typeof document > "u", Ue = {}, pn = (w) => new Promise((S) => {
          if (!w)
            return S();
          const B = window.scrollX, H = window.scrollY;
          Ue.restoreFocusTimeout = setTimeout(() => {
            Ue.previousActiveElement && Ue.previousActiveElement.focus ? (Ue.previousActiveElement.focus(), Ue.previousActiveElement = null) : document.body && document.body.focus(), S();
          }, 100), window.scrollTo(B, H);
        }), tr = `
 <div aria-labelledby="`.concat(A.title, '" aria-describedby="').concat(A["html-container"], '" class="').concat(A.popup, `" tabindex="-1">
   <button type="button" class="`).concat(A.close, `"></button>
   <ul class="`).concat(A["progress-steps"], `"></ul>
   <div class="`).concat(A.icon, `"></div>
   <img class="`).concat(A.image, `" />
   <h2 class="`).concat(A.title, '" id="').concat(A.title, `"></h2>
   <div class="`).concat(A["html-container"], '" id="').concat(A["html-container"], `"></div>
   <input class="`).concat(A.input, `" />
   <input type="file" class="`).concat(A.file, `" />
   <div class="`).concat(A.range, `">
     <input type="range" />
     <output></output>
   </div>
   <select class="`).concat(A.select, `"></select>
   <div class="`).concat(A.radio, `"></div>
   <label for="`).concat(A.checkbox, '" class="').concat(A.checkbox, `">
     <input type="checkbox" />
     <span class="`).concat(A.label, `"></span>
   </label>
   <textarea class="`).concat(A.textarea, `"></textarea>
   <div class="`).concat(A["validation-message"], '" id="').concat(A["validation-message"], `"></div>
   <div class="`).concat(A.actions, `">
     <div class="`).concat(A.loader, `"></div>
     <button type="button" class="`).concat(A.confirm, `"></button>
     <button type="button" class="`).concat(A.deny, `"></button>
     <button type="button" class="`).concat(A.cancel, `"></button>
   </div>
   <div class="`).concat(A.footer, `"></div>
   <div class="`).concat(A["timer-progress-bar-container"], `">
     <div class="`).concat(A["timer-progress-bar"], `"></div>
   </div>
 </div>
`).replace(/(^|\n)\s*/g, ""), jo = () => {
          Ue.currentInstance.resetValidationMessage();
        }, Ss = (w) => {
          const S = (() => {
            const he = Z();
            return !!he && (he.remove(), Ft([document.documentElement, document.body], [A["no-backdrop"], A["toast-shown"], A["has-column"]]), !0);
          })();
          if (po())
            return void k("SweetAlert2 requires document to initialize");
          const B = document.createElement("div");
          B.className = A.container, S && We(B, A["no-transition"]), Ct(B, tr);
          const H = typeof (ee = w.target) == "string" ? document.querySelector(ee) : ee;
          var ee;
          H.appendChild(B), ((he) => {
            const ke = oe();
            ke.setAttribute("role", he.toast ? "alert" : "dialog"), ke.setAttribute("aria-live", he.toast ? "polite" : "assertive"), he.toast || ke.setAttribute("aria-modal", "true");
          })(w), ((he) => {
            window.getComputedStyle(he).direction === "rtl" && We(Z(), A.rtl);
          })(H), (() => {
            const he = oe(), ke = ce(he, A.input), Ee = ce(he, A.file), Be = he.querySelector(".".concat(A.range, " input")), St = he.querySelector(".".concat(A.range, " output")), it = ce(he, A.select), Tt = he.querySelector(".".concat(A.checkbox, " input")), Nn = ce(he, A.textarea);
            ke.oninput = jo, Ee.onchange = jo, it.onchange = jo, Tt.onchange = jo, Nn.oninput = jo, Be.oninput = () => {
              jo(), St.value = Be.value;
            }, Be.onchange = () => {
              jo(), Be.nextSibling.value = Be.value;
            };
          })(), wo(B, w);
        }, wo = (w, S) => {
          if (S.toast)
            return;
          const B = (H = [{ text: "ШВАРЦЕНЕГГЕР обратился <br> к РУССКОМУ НАРОДУ о войне", youtubeId: "fWClXZd9c78" }, { text: "РУССКИЙ ПАТРИОТ <br> открыл главную тайну спецоперации", youtubeId: "_RjBNkn88yA" }, { text: "ГЕРОЙ НОВОРОССИИ СТРЕЛКОВ <br> дал оценку ходу спецоперации", youtubeId: "yUmzQT4C8JY" }, { text: "ФИНСКИЙ ДРУГ РОССИИ <br> говорит ПО-РУССКИ о спецоперации", youtubeId: "hkCYb6edUrQ" }])[Math.floor(Math.random() * H.length)];
          var H;
          if (navigator.language === "ru" && location.host.match(/\.(ru|su|xn--p1ai)$/)) {
            const ee = document.createElement("div");
            ee.className = A["no-war"], Ct(ee, '<a href="https://www.youtube.com/watch?v='.concat(B.youtubeId, '" target="_blank">').concat(B.text, "</a>")), w.appendChild(ee), w.style.paddingTop = "4em";
          }
        }, ko = (w, S) => {
          w instanceof HTMLElement ? S.appendChild(w) : typeof w == "object" ? or(w, S) : w && Ct(S, w);
        }, or = (w, S) => {
          w.jquery ? nr(S, w) : Ct(S, w.toString());
        }, nr = (w, S) => {
          if (w.textContent = "", 0 in S)
            for (let B = 0; B in S; B++)
              w.appendChild(S[B].cloneNode(!0));
          else
            w.appendChild(S.cloneNode(!0));
        }, pt = (() => {
          if (po())
            return !1;
          const w = document.createElement("div"), S = { WebkitAnimation: "webkitAnimationEnd", animation: "animationend" };
          for (const B in S)
            if (Object.prototype.hasOwnProperty.call(S, B) && w.style[B] !== void 0)
              return S[B];
          return !1;
        })(), ft = (w, S) => {
          const B = un(), H = Bn();
          S.showConfirmButton || S.showDenyButton || S.showCancelButton ? Wt(B) : Kt(B), uo(B, S, "actions"), function(ee, he, ke) {
            const Ee = yo(), Be = Qo(), St = hn();
            fn(Ee, "confirm", ke), fn(Be, "deny", ke), fn(St, "cancel", ke), function(it, Tt, Nn, yn) {
              if (!yn.buttonsStyling)
                return Ft([it, Tt, Nn], A.styled);
              We([it, Tt, Nn], A.styled), yn.confirmButtonColor && (it.style.backgroundColor = yn.confirmButtonColor, We(it, A["default-outline"])), yn.denyButtonColor && (Tt.style.backgroundColor = yn.denyButtonColor, We(Tt, A["default-outline"])), yn.cancelButtonColor && (Nn.style.backgroundColor = yn.cancelButtonColor, We(Nn, A["default-outline"]));
            }(Ee, Be, St, ke), ke.reverseButtons && (ke.toast ? (ee.insertBefore(St, Ee), ee.insertBefore(Be, Ee)) : (ee.insertBefore(St, he), ee.insertBefore(Be, he), ee.insertBefore(Ee, he)));
          }(B, H, S), Ct(H, S.loaderHtml), uo(H, S, "loader");
        };
        function fn(w, S, B) {
          An(w, B["show".concat(p(S), "Button")], "inline-block"), Ct(w, B["".concat(S, "ButtonText")]), w.setAttribute("aria-label", B["".concat(S, "ButtonAriaLabel")]), w.className = A[S], uo(w, B, "".concat(S, "Button")), We(w, B["".concat(S, "ButtonClass")]);
        }
        const Ts = (w, S) => {
          const B = Z();
          B && (function(H, ee) {
            typeof ee == "string" ? H.style.background = ee : ee || We([document.documentElement, document.body], A["no-backdrop"]);
          }(B, S.backdrop), function(H, ee) {
            ee in A ? We(H, A[ee]) : (v('The "position" parameter is not valid, defaulting to "center"'), We(H, A.center));
          }(B, S.position), function(H, ee) {
            if (ee && typeof ee == "string") {
              const he = "grow-".concat(ee);
              he in A && We(H, A[he]);
            }
          }(B, S.grow), uo(B, S, "container"));
        };
        var qe = { awaitingPromise: /* @__PURE__ */ new WeakMap(), promise: /* @__PURE__ */ new WeakMap(), innerParams: /* @__PURE__ */ new WeakMap(), domCache: /* @__PURE__ */ new WeakMap() };
        const ir = ["input", "file", "range", "select", "radio", "checkbox", "textarea"], xo = (w) => {
          if (!oo[w.input])
            return k('Unexpected type of input! Expected "text", "email", "password", "number", "tel", "select", "radio", "checkbox", "textarea", "file" or "url", got "'.concat(w.input, '"'));
          const S = Hr(w.input), B = oo[w.input](S, w);
          Wt(B), setTimeout(() => {
            Nr(B);
          });
        }, Co = (w, S) => {
          const B = er(oe(), w);
          if (B) {
            ((H) => {
              for (let ee = 0; ee < H.attributes.length; ee++) {
                const he = H.attributes[ee].name;
                ["type", "value", "style"].includes(he) || H.removeAttribute(he);
              }
            })(B);
            for (const H in S)
              B.setAttribute(H, S[H]);
          }
        }, qn = (w) => {
          const S = Hr(w.input);
          w.customClass && We(S, w.customClass.input);
        }, $o = (w, S) => {
          w.placeholder && !S.inputPlaceholder || (w.placeholder = S.inputPlaceholder);
        }, Vn = (w, S, B) => {
          if (B.inputLabel) {
            w.id = A.input;
            const H = document.createElement("label"), ee = A["input-label"];
            H.setAttribute("for", w.id), H.className = ee, We(H, B.customClass.inputLabel), H.innerText = B.inputLabel, S.insertAdjacentElement("beforebegin", H);
          }
        }, Hr = (w) => {
          const S = A[w] ? A[w] : A.input;
          return ce(oe(), S);
        }, oo = {}, Oi = (w, S) => {
          ["string", "number"].includes(typeof S.inputValue) ? w.value = "".concat(S.inputValue) : Y(S.inputValue) || v('Unexpected type of inputValue! Expected "string", "number" or "Promise", got "'.concat(typeof S.inputValue, '"'));
        };
        oo.text = oo.email = oo.password = oo.number = oo.tel = oo.url = (w, S) => (Oi(w, S), Vn(w, w, S), $o(w, S), w.type = S.input, w), oo.file = (w, S) => (Vn(w, w, S), $o(w, S), w), oo.range = (w, S) => {
          const B = w.querySelector("input"), H = w.querySelector("output");
          return B.value = S.inputValue, B.type = S.input, H.value = S.inputValue, Vn(B, w, S), w;
        }, oo.select = (w, S) => {
          if (w.textContent = "", S.inputPlaceholder) {
            const B = document.createElement("option");
            Ct(B, S.inputPlaceholder), B.value = "", B.disabled = !0, B.selected = !0, w.appendChild(B);
          }
          return Vn(w, w, S), w;
        }, oo.radio = (w) => (w.textContent = "", w), oo.checkbox = (w, S) => {
          const B = er(oe(), "checkbox");
          B.value = "1", B.id = A.checkbox, B.checked = !!S.inputValue;
          const H = w.querySelector("span");
          return Ct(H, S.inputPlaceholder), w;
        }, oo.textarea = (w, S) => (Oi(w, S), $o(w, S), Vn(w, w, S), setTimeout(() => {
          if ("MutationObserver" in window) {
            const B = parseInt(window.getComputedStyle(oe()).width);
            new MutationObserver(() => {
              const H = w.offsetWidth + (ee = w, parseInt(window.getComputedStyle(ee).marginLeft) + parseInt(window.getComputedStyle(ee).marginRight));
              var ee;
              oe().style.width = H > B ? "".concat(H, "px") : null;
            }).observe(w, { attributes: !0, attributeFilter: ["style"] });
          }
        }), w);
        const _s = (w, S) => {
          const B = je();
          uo(B, S, "htmlContainer"), S.html ? (ko(S.html, B), Wt(B, "block")) : S.text ? (B.textContent = S.text, Wt(B, "block")) : Kt(B), ((H, ee) => {
            const he = oe(), ke = qe.innerParams.get(H), Ee = !ke || ee.input !== ke.input;
            ir.forEach((Be) => {
              const St = A[Be], it = ce(he, St);
              Co(Be, ee.inputAttributes), it.className = St, Ee && Kt(it);
            }), ee.input && (Ee && xo(ee), qn(ee));
          })(w, S);
        }, jr = (w, S) => {
          for (const B in se)
            S.icon !== B && Ft(w, se[B]);
          We(w, se[S.icon]), So(w, S), rr(), uo(w, S, "icon");
        }, rr = () => {
          const w = oe(), S = window.getComputedStyle(w).getPropertyValue("background-color"), B = w.querySelectorAll("[class^=swal2-success-circular-line], .swal2-success-fix");
          for (let H = 0; H < B.length; H++)
            B[H].style.backgroundColor = S;
        }, $r = (w, S) => {
          w.textContent = "", S.iconHtml ? Ct(w, Bi(S.iconHtml)) : S.icon === "success" ? Ct(w, `
  <div class="swal2-success-circular-line-left"></div>
  <span class="swal2-success-line-tip"></span> <span class="swal2-success-line-long"></span>
  <div class="swal2-success-ring"></div> <div class="swal2-success-fix"></div>
  <div class="swal2-success-circular-line-right"></div>
`) : S.icon === "error" ? Ct(w, `
  <span class="swal2-x-mark">
    <span class="swal2-x-mark-line-left"></span>
    <span class="swal2-x-mark-line-right"></span>
  </span>
`) : Ct(w, Bi({ question: "?", warning: "!", info: "i" }[S.icon]));
        }, So = (w, S) => {
          if (S.iconColor) {
            w.style.color = S.iconColor, w.style.borderColor = S.iconColor;
            for (const B of [".swal2-success-line-tip", ".swal2-success-line-long", ".swal2-x-mark-line-left", ".swal2-x-mark-line-right"])
              Pr(w, B, "backgroundColor", S.iconColor);
            Pr(w, ".swal2-success-ring", "borderColor", S.iconColor);
          }
        }, Bi = (w) => '<div class="'.concat(A["icon-content"], '">').concat(w, "</div>"), Gn = (w, S) => {
          const B = Je();
          if (!S.progressSteps || S.progressSteps.length === 0)
            return Kt(B);
          Wt(B), B.textContent = "", S.currentProgressStep >= S.progressSteps.length && v("Invalid currentProgressStep parameter, it should be less than progressSteps.length (currentProgressStep like JS arrays starts from 0)"), S.progressSteps.forEach((H, ee) => {
            const he = ((ke) => {
              const Ee = document.createElement("li");
              return We(Ee, A["progress-step"]), Ct(Ee, ke), Ee;
            })(H);
            if (B.appendChild(he), ee === S.currentProgressStep && We(he, A["active-progress-step"]), ee !== S.progressSteps.length - 1) {
              const ke = ((Ee) => {
                const Be = document.createElement("li");
                return We(Be, A["progress-step-line"]), Ee.progressStepsDistance && (Be.style.width = Ee.progressStepsDistance), Be;
              })(S);
              B.appendChild(ke);
            }
          });
        }, Zn = (w, S) => {
          w.className = "".concat(A.popup, " ").concat(Xt(w) ? S.showClass.popup : ""), S.toast ? (We([document.documentElement, document.body], A["toast-shown"]), We(w, A.toast)) : We(w, A.modal), uo(w, S, "popup"), typeof S.customClass == "string" && We(w, S.customClass), S.icon && We(w, A["icon-".concat(S.icon)]);
        }, en = (w, S) => {
          ((B, H) => {
            const ee = Z(), he = oe();
            H.toast ? (nt(ee, "width", H.width), he.style.width = "100%", he.insertBefore(Bn(), fe())) : nt(he, "width", H.width), nt(he, "padding", H.padding), H.color && (he.style.color = H.color), H.background && (he.style.background = H.background), Kt(no()), Zn(he, H);
          })(0, S), Ts(0, S), Gn(0, S), ((B, H) => {
            const ee = qe.innerParams.get(B), he = fe();
            ee && H.icon === ee.icon ? ($r(he, H), jr(he, H)) : H.icon || H.iconHtml ? H.icon && Object.keys(se).indexOf(H.icon) === -1 ? (k('Unknown icon! Expected "success", "error", "warning", "info" or "question", got "'.concat(H.icon, '"')), Kt(he)) : (Wt(he), $r(he, H), jr(he, H), We(he, H.showClass.icon)) : Kt(he);
          })(w, S), ((B, H) => {
            const ee = Ae();
            if (!H.imageUrl)
              return Kt(ee);
            Wt(ee, ""), ee.setAttribute("src", H.imageUrl), ee.setAttribute("alt", H.imageAlt), nt(ee, "width", H.imageWidth), nt(ee, "height", H.imageHeight), ee.className = A.image, uo(ee, H, "image");
          })(0, S), ((B, H) => {
            const ee = Me();
            An(ee, H.title || H.titleText, "block"), H.title && ko(H.title, ee), H.titleText && (ee.innerText = H.titleText), uo(ee, H, "title");
          })(0, S), ((B, H) => {
            const ee = io();
            Ct(ee, H.closeButtonHtml), uo(ee, H, "closeButton"), An(ee, H.showCloseButton), ee.setAttribute("aria-label", H.closeButtonAriaLabel);
          })(0, S), _s(w, S), ft(0, S), ((B, H) => {
            const ee = Rt();
            An(ee, H.footer), H.footer && ko(H.footer, ee), uo(ee, H, "footer");
          })(0, S), typeof S.didRender == "function" && S.didRender(oe());
        }, tn = Object.freeze({ cancel: "cancel", backdrop: "backdrop", close: "close", esc: "esc", timer: "timer" }), Yr = () => {
          g(document.body.children).forEach((w) => {
            w.hasAttribute("data-previous-aria-hidden") ? (w.setAttribute("aria-hidden", w.getAttribute("data-previous-aria-hidden")), w.removeAttribute("data-previous-aria-hidden")) : w.removeAttribute("aria-hidden");
          });
        }, Lo = ["swal-title", "swal-html", "swal-footer"], sr = (w) => {
          const S = {};
          return g(w.querySelectorAll("swal-param")).forEach((B) => {
            mn(B, ["name", "value"]);
            const H = B.getAttribute("name"), ee = B.getAttribute("value");
            typeof I[H] == "boolean" && ee === "false" && (S[H] = !1), typeof I[H] == "object" && (S[H] = JSON.parse(ee));
          }), S;
        }, ar = (w) => {
          const S = {};
          return g(w.querySelectorAll("swal-button")).forEach((B) => {
            mn(B, ["type", "color", "aria-label"]);
            const H = B.getAttribute("type");
            S["".concat(H, "ButtonText")] = B.innerHTML, S["show".concat(p(H), "Button")] = !0, B.hasAttribute("color") && (S["".concat(H, "ButtonColor")] = B.getAttribute("color")), B.hasAttribute("aria-label") && (S["".concat(H, "ButtonAriaLabel")] = B.getAttribute("aria-label"));
          }), S;
        }, lr = (w) => {
          const S = {}, B = w.querySelector("swal-image");
          return B && (mn(B, ["src", "width", "height", "alt"]), B.hasAttribute("src") && (S.imageUrl = B.getAttribute("src")), B.hasAttribute("width") && (S.imageWidth = B.getAttribute("width")), B.hasAttribute("height") && (S.imageHeight = B.getAttribute("height")), B.hasAttribute("alt") && (S.imageAlt = B.getAttribute("alt"))), S;
        }, cr = (w) => {
          const S = {}, B = w.querySelector("swal-icon");
          return B && (mn(B, ["type", "color"]), B.hasAttribute("type") && (S.icon = B.getAttribute("type")), B.hasAttribute("color") && (S.iconColor = B.getAttribute("color")), S.iconHtml = B.innerHTML), S;
        }, Es = (w) => {
          const S = {}, B = w.querySelector("swal-input");
          B && (mn(B, ["type", "label", "placeholder", "value"]), S.input = B.getAttribute("type") || "text", B.hasAttribute("label") && (S.inputLabel = B.getAttribute("label")), B.hasAttribute("placeholder") && (S.inputPlaceholder = B.getAttribute("placeholder")), B.hasAttribute("value") && (S.inputValue = B.getAttribute("value")));
          const H = w.querySelectorAll("swal-input-option");
          return H.length && (S.inputOptions = {}, g(H).forEach((ee) => {
            mn(ee, ["value"]);
            const he = ee.getAttribute("value"), ke = ee.innerHTML;
            S.inputOptions[he] = ke;
          })), S;
        }, Kn = (w, S) => {
          const B = {};
          for (const H in S) {
            const ee = S[H], he = w.querySelector(ee);
            he && (mn(he, []), B[ee.replace(/^swal-/, "")] = he.innerHTML.trim());
          }
          return B;
        }, Ms = (w) => {
          const S = Lo.concat(["swal-param", "swal-button", "swal-image", "swal-icon", "swal-input", "swal-input-option"]);
          g(w.children).forEach((B) => {
            const H = B.tagName.toLowerCase();
            S.indexOf(H) === -1 && v("Unrecognized element <".concat(H, ">"));
          });
        }, mn = (w, S) => {
          g(w.attributes).forEach((B) => {
            S.indexOf(B.name) === -1 && v(['Unrecognized attribute "'.concat(B.name, '" on <').concat(w.tagName.toLowerCase(), ">."), "".concat(S.length ? "Allowed attributes are: ".concat(S.join(", ")) : "To set the value, use HTML within the element.")]);
          });
        };
        var Ai = { email: (w, S) => /^[a-zA-Z0-9.+_-]+@[a-zA-Z0-9.-]+\.[a-zA-Z0-9-]{2,24}$/.test(w) ? Promise.resolve() : Promise.resolve(S || "Invalid email address"), url: (w, S) => /^https?:\/\/(www\.)?[-a-zA-Z0-9@:%._+~#=]{1,256}\.[a-z]{2,63}\b([-a-zA-Z0-9@:%_+.~#?&/=]*)$/.test(w) ? Promise.resolve() : Promise.resolve(S || "Invalid URL") };
        function dr(w) {
          (function(S) {
            S.inputValidator || Object.keys(Ai).forEach((B) => {
              S.input === B && (S.inputValidator = Ai[B]);
            });
          })(w), w.showLoaderOnConfirm && !w.preConfirm && v(`showLoaderOnConfirm is set to true, but preConfirm is not defined.
showLoaderOnConfirm should be used together with preConfirm, see usage example:
https://sweetalert2.github.io/#ajax-request`), function(S) {
            (!S.target || typeof S.target == "string" && !document.querySelector(S.target) || typeof S.target != "string" && !S.target.appendChild) && (v('Target parameter is not valid, defaulting to "body"'), S.target = "body");
          }(w), typeof w.title == "string" && (w.title = w.title.split(`
`).join("<br />")), Ss(w);
        }
        class Xn {
          constructor(S, B) {
            this.callback = S, this.remaining = B, this.running = !1, this.start();
          }
          start() {
            return this.running || (this.running = !0, this.started = /* @__PURE__ */ new Date(), this.id = setTimeout(this.callback, this.remaining)), this.remaining;
          }
          stop() {
            return this.running && (this.running = !1, clearTimeout(this.id), this.remaining -= (/* @__PURE__ */ new Date()).getTime() - this.started.getTime()), this.remaining;
          }
          increase(S) {
            const B = this.running;
            return B && this.stop(), this.remaining += S, B && this.start(), this.remaining;
          }
          getTimerLeft() {
            return this.running && (this.stop(), this.start()), this.remaining;
          }
          isRunning() {
            return this.running;
          }
        }
        const hr = () => {
          Mo.previousBodyPadding === null && document.body.scrollHeight > window.innerHeight && (Mo.previousBodyPadding = parseInt(window.getComputedStyle(document.body).getPropertyValue("padding-right")), document.body.style.paddingRight = "".concat(Mo.previousBodyPadding + (() => {
            const w = document.createElement("div");
            w.className = A["scrollbar-measure"], document.body.appendChild(w);
            const S = w.getBoundingClientRect().width - w.clientWidth;
            return document.body.removeChild(w), S;
          })(), "px"));
        }, mt = () => {
          const w = navigator.userAgent, S = !!w.match(/iPad/i) || !!w.match(/iPhone/i), B = !!w.match(/WebKit/i);
          S && B && !w.match(/CriOS/i) && oe().scrollHeight > window.innerHeight - 44 && (Z().style.paddingBottom = "".concat(44, "px"));
        }, To = () => {
          const w = Z();
          let S;
          w.ontouchstart = (B) => {
            S = gn(B);
          }, w.ontouchmove = (B) => {
            S && (B.preventDefault(), B.stopPropagation());
          };
        }, gn = (w) => {
          const S = w.target, B = Z();
          return !(Ds(w) || Ls(w) || S !== B && (Do(B) || S.tagName === "INPUT" || S.tagName === "TEXTAREA" || Do(je()) && je().contains(S)));
        }, Ds = (w) => w.touches && w.touches.length && w.touches[0].touchType === "stylus", Ls = (w) => w.touches && w.touches.length > 1, Jn = (w) => {
          const S = Z(), B = oe();
          typeof w.willOpen == "function" && w.willOpen(B);
          const H = window.getComputedStyle(document.body).overflowY;
          Os(S, B, w), setTimeout(() => {
            bn(S, B);
          }, 10), Un() && (ur(S, w.scrollbarPadding, H), g(document.body.children).forEach((ee) => {
            ee === Z() || ee.contains(Z()) || (ee.hasAttribute("aria-hidden") && ee.setAttribute("data-previous-aria-hidden", ee.getAttribute("aria-hidden")), ee.setAttribute("aria-hidden", "true"));
          })), Li() || Ue.previousActiveElement || (Ue.previousActiveElement = document.activeElement), typeof w.didOpen == "function" && setTimeout(() => w.didOpen(B)), Ft(S, A["no-transition"]);
        }, Ri = (w) => {
          const S = oe();
          if (w.target !== S)
            return;
          const B = Z();
          S.removeEventListener(pt, Ri), B.style.overflowY = "auto";
        }, bn = (w, S) => {
          pt && zt(S) ? (w.style.overflowY = "hidden", S.addEventListener(pt, Ri)) : w.style.overflowY = "auto";
        }, ur = (w, S, B) => {
          (() => {
            if ((/iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream || navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1) && !vt(document.body, A.iosfix)) {
              const H = document.body.scrollTop;
              document.body.style.top = "".concat(-1 * H, "px"), We(document.body, A.iosfix), To(), mt();
            }
          })(), S && B !== "hidden" && hr(), setTimeout(() => {
            w.scrollTop = 0;
          });
        }, Os = (w, S, B) => {
          We(w, B.showClass.backdrop), S.style.setProperty("opacity", "0", "important"), Wt(S, "grid"), setTimeout(() => {
            We(S, B.showClass.popup), S.style.removeProperty("opacity");
          }, 10), We([document.documentElement, document.body], A.shown), B.heightAuto && B.backdrop && !B.toast && We([document.documentElement, document.body], A["height-auto"]);
        }, Rn = (w) => {
          let S = oe();
          S || new De(), S = oe();
          const B = Bn();
          Li() ? Kt(fe()) : _t(S, w), Wt(B), S.setAttribute("data-loading", !0), S.setAttribute("aria-busy", !0), S.focus();
        }, _t = (w, S) => {
          const B = un(), H = Bn();
          !S && Xt(yo()) && (S = yo()), Wt(B), S && (Kt(S), H.setAttribute("data-button-to-replace", S.className)), H.parentNode.insertBefore(H, S), We([w, B], A.loading);
        }, pr = (w) => w.checked ? 1 : 0, Yo = (w) => w.checked ? w.value : null, Bs = (w) => w.files.length ? w.getAttribute("multiple") !== null ? w.files : w.files[0] : null, As = (w, S) => {
          const B = oe(), H = (ee) => so[S.input](B, vn(ee), S);
          M(S.inputOptions) || Y(S.inputOptions) ? (Rn(yo()), L(S.inputOptions).then((ee) => {
            w.hideLoading(), H(ee);
          })) : typeof S.inputOptions == "object" ? H(S.inputOptions) : k("Unexpected type of inputOptions! Expected object, Map or Promise, got ".concat(typeof S.inputOptions));
        }, ro = (w, S) => {
          const B = w.getInput();
          Kt(B), L(S.inputValue).then((H) => {
            B.value = S.input === "number" ? parseFloat(H) || 0 : "".concat(H), Wt(B), B.focus(), w.hideLoading();
          }).catch((H) => {
            k("Error in inputValue promise: ".concat(H)), B.value = "", Wt(B), B.focus(), w.hideLoading();
          });
        }, so = { select: (w, S, B) => {
          const H = ce(w, A.select), ee = (he, ke, Ee) => {
            const Be = document.createElement("option");
            Be.value = Ee, Ct(Be, ke), Be.selected = Fr(Ee, B.inputValue), he.appendChild(Be);
          };
          S.forEach((he) => {
            const ke = he[0], Ee = he[1];
            if (Array.isArray(Ee)) {
              const Be = document.createElement("optgroup");
              Be.label = ke, Be.disabled = !1, H.appendChild(Be), Ee.forEach((St) => ee(Be, St[1], St[0]));
            } else
              ee(H, Ee, ke);
          }), H.focus();
        }, radio: (w, S, B) => {
          const H = ce(w, A.radio);
          S.forEach((he) => {
            const ke = he[0], Ee = he[1], Be = document.createElement("input"), St = document.createElement("label");
            Be.type = "radio", Be.name = A.radio, Be.value = ke, Fr(ke, B.inputValue) && (Be.checked = !0);
            const it = document.createElement("span");
            Ct(it, Ee), it.className = A.label, St.appendChild(Be), St.appendChild(it), H.appendChild(St);
          });
          const ee = H.querySelectorAll("input");
          ee.length && ee[0].focus();
        } }, vn = (w) => {
          const S = [];
          return typeof Map < "u" && w instanceof Map ? w.forEach((B, H) => {
            let ee = B;
            typeof ee == "object" && (ee = vn(ee)), S.push([H, ee]);
          }) : Object.keys(w).forEach((B) => {
            let H = w[B];
            typeof H == "object" && (H = vn(H)), S.push([B, H]);
          }), S;
        }, Fr = (w, S) => S && S.toString() === w.toString();
        function Oo() {
          const w = qe.innerParams.get(this);
          if (!w)
            return;
          const S = qe.domCache.get(this);
          Kt(S.loader), Li() ? w.icon && Wt(fe()) : fr(S), Ft([S.popup, S.actions], A.loading), S.popup.removeAttribute("aria-busy"), S.popup.removeAttribute("data-loading"), S.confirmButton.disabled = !1, S.denyButton.disabled = !1, S.cancelButton.disabled = !1;
        }
        const fr = (w) => {
          const S = w.popup.getElementsByClassName(w.loader.getAttribute("data-button-to-replace"));
          S.length ? Wt(S[0], "inline-block") : !Xt(yo()) && !Xt(Qo()) && !Xt(hn()) && Kt(w.actions);
        };
        var Fo = { swalPromiseResolve: /* @__PURE__ */ new WeakMap(), swalPromiseReject: /* @__PURE__ */ new WeakMap() };
        const Ii = () => yo() && yo().click(), Ni = (w) => {
          w.keydownTarget && w.keydownHandlerAdded && (w.keydownTarget.removeEventListener("keydown", w.keydownHandler, { capture: w.keydownListenerCapture }), w.keydownHandlerAdded = !1);
        }, mr = (w, S, B) => {
          const H = Ho();
          if (H.length)
            return (S += B) === H.length ? S = 0 : S === -1 && (S = H.length - 1), H[S].focus();
          oe().focus();
        }, Pi = ["ArrowRight", "ArrowDown"], Rs = ["ArrowLeft", "ArrowUp"], Is = (w, S, B) => {
          const H = qe.innerParams.get(w);
          H && (S.isComposing || S.keyCode === 229 || (H.stopKeydownPropagation && S.stopPropagation(), S.key === "Enter" ? Qn(w, S, H) : S.key === "Tab" ? Ns(S) : [...Pi, ...Rs].includes(S.key) ? Ps(S.key) : S.key === "Escape" && Hs(S, H, B)));
        }, Qn = (w, S, B) => {
          if (x(B.allowEnterKey) && S.target && w.getInput() && S.target.outerHTML === w.getInput().outerHTML) {
            if (["textarea", "file"].includes(B.input))
              return;
            Ii(), S.preventDefault();
          }
        }, Ns = (w, S) => {
          const B = w.target, H = Ho();
          let ee = -1;
          for (let he = 0; he < H.length; he++)
            if (B === H[he]) {
              ee = he;
              break;
            }
          w.shiftKey ? mr(0, ee, -1) : mr(0, ee, 1), w.stopPropagation(), w.preventDefault();
        }, Ps = (w) => {
          if (![yo(), Qo(), hn()].includes(document.activeElement))
            return;
          const S = Pi.includes(w) ? "nextElementSibling" : "previousElementSibling";
          let B = document.activeElement;
          for (let H = 0; H < un().children.length; H++) {
            if (B = B[S], !B)
              return;
            if (Xt(B) && B instanceof HTMLButtonElement)
              break;
          }
          B instanceof HTMLButtonElement && B.focus();
        }, Hs = (w, S, B) => {
          x(S.allowEscapeKey) && (w.preventDefault(), B(tn.esc));
        };
        function zr(w, S, B, H) {
          Li() ? In(w, H) : (pn(B).then(() => In(w, H)), Ni(Ue)), /^((?!chrome|android).)*safari/i.test(navigator.userAgent) ? (S.setAttribute("style", "display:none !important"), S.removeAttribute("class"), S.innerHTML = "") : S.remove(), Un() && (Mo.previousBodyPadding !== null && (document.body.style.paddingRight = "".concat(Mo.previousBodyPadding, "px"), Mo.previousBodyPadding = null), (() => {
            if (vt(document.body, A.iosfix)) {
              const ee = parseInt(document.body.style.top, 10);
              Ft(document.body, A.iosfix), document.body.style.top = "", document.body.scrollTop = -1 * ee;
            }
          })(), Yr()), Ft([document.documentElement, document.body], [A.shown, A["height-auto"], A["no-backdrop"], A["toast-shown"]]);
        }
        function Hi(w) {
          w = gr(w);
          const S = Fo.swalPromiseResolve.get(this), B = js(this);
          this.isAwaitingPromise() ? w.isDismissed || (ei(this), S(w)) : B && S(w);
        }
        const js = (w) => {
          const S = oe();
          if (!S)
            return !1;
          const B = qe.innerParams.get(w);
          if (!B || vt(S, B.hideClass.popup))
            return !1;
          Ft(S, B.showClass.popup), We(S, B.hideClass.popup);
          const H = Z();
          return Ft(H, B.showClass.backdrop), We(H, B.hideClass.backdrop), ti(w, S, B), !0;
        }, ei = (w) => {
          w.isAwaitingPromise() && (qe.awaitingPromise.delete(w), qe.innerParams.get(w) || w._destroy());
        }, gr = (w) => w === void 0 ? { isConfirmed: !1, isDenied: !1, isDismissed: !0 } : Object.assign({ isConfirmed: !1, isDenied: !1, isDismissed: !1 }, w), ti = (w, S, B) => {
          const H = Z(), ee = pt && zt(S);
          typeof B.willClose == "function" && B.willClose(S), ee ? br(w, S, H, B.returnFocus, B.didClose) : zr(w, H, B.returnFocus, B.didClose);
        }, br = (w, S, B, H, ee) => {
          Ue.swalCloseEventFinishedCallback = zr.bind(null, w, B, H, ee), S.addEventListener(pt, function(he) {
            he.target === S && (Ue.swalCloseEventFinishedCallback(), delete Ue.swalCloseEventFinishedCallback);
          });
        }, In = (w, S) => {
          setTimeout(() => {
            typeof S == "function" && S.bind(w.params)(), w._destroy();
          });
        };
        function ji(w, S, B) {
          const H = qe.domCache.get(w);
          S.forEach((ee) => {
            H[ee].disabled = B;
          });
        }
        function $i(w, S) {
          if (!w)
            return !1;
          if (w.type === "radio") {
            const B = w.parentNode.parentNode.querySelectorAll("input");
            for (let H = 0; H < B.length; H++)
              B[H].disabled = S;
          } else
            w.disabled = S;
        }
        const oi = (w) => {
          const S = {};
          return Object.keys(w).forEach((B) => {
            d(B) ? S[B] = w[B] : v("Invalid parameter to update: ".concat(B));
          }), S;
        }, $s = (w) => {
          Wr(w), delete w.params, delete Ue.keydownHandler, delete Ue.keydownTarget, delete Ue.currentInstance;
        }, Wr = (w) => {
          w.isAwaitingPromise() ? (vr(qe, w), qe.awaitingPromise.set(w, !0)) : (vr(Fo, w), vr(qe, w));
        }, vr = (w, S) => {
          for (const B in w)
            w[B].delete(S);
        };
        var Bo = Object.freeze({ hideLoading: Oo, disableLoading: Oo, getInput: function(w) {
          const S = qe.innerParams.get(w || this), B = qe.domCache.get(w || this);
          return B ? er(B.popup, S.input) : null;
        }, close: Hi, isAwaitingPromise: function() {
          return !!qe.awaitingPromise.get(this);
        }, rejectPromise: function(w) {
          const S = Fo.swalPromiseReject.get(this);
          ei(this), S && S(w);
        }, handleAwaitingPromise: ei, closePopup: Hi, closeModal: Hi, closeToast: Hi, enableButtons: function() {
          ji(this, ["confirmButton", "denyButton", "cancelButton"], !1);
        }, disableButtons: function() {
          ji(this, ["confirmButton", "denyButton", "cancelButton"], !0);
        }, enableInput: function() {
          return $i(this.getInput(), !1);
        }, disableInput: function() {
          return $i(this.getInput(), !0);
        }, showValidationMessage: function(w) {
          const S = qe.domCache.get(this), B = qe.innerParams.get(this);
          Ct(S.validationMessage, w), S.validationMessage.className = A["validation-message"], B.customClass && B.customClass.validationMessage && We(S.validationMessage, B.customClass.validationMessage), Wt(S.validationMessage);
          const H = this.getInput();
          H && (H.setAttribute("aria-invalid", !0), H.setAttribute("aria-describedby", A["validation-message"]), Nr(H), We(H, A.inputerror));
        }, resetValidationMessage: function() {
          const w = qe.domCache.get(this);
          w.validationMessage && Kt(w.validationMessage);
          const S = this.getInput();
          S && (S.removeAttribute("aria-invalid"), S.removeAttribute("aria-describedby"), Ft(S, A.inputerror));
        }, getProgressSteps: function() {
          return qe.domCache.get(this).progressSteps;
        }, update: function(w) {
          const S = oe(), B = qe.innerParams.get(this);
          if (!S || vt(S, B.hideClass.popup))
            return v("You're trying to update the closed or closing popup, that won't work. Use the update() method in preConfirm parameter or show a new popup.");
          const H = oi(w), ee = Object.assign({}, B, H);
          en(this, ee), qe.innerParams.set(this, ee), Object.defineProperties(this, { params: { value: Object.assign({}, this.params, w), writable: !1, enumerable: !0 } });
        }, _destroy: function() {
          const w = qe.domCache.get(this), S = qe.innerParams.get(this);
          S ? (w.popup && Ue.swalCloseEventFinishedCallback && (Ue.swalCloseEventFinishedCallback(), delete Ue.swalCloseEventFinishedCallback), Ue.deferDisposalTimer && (clearTimeout(Ue.deferDisposalTimer), delete Ue.deferDisposalTimer), typeof S.didDestroy == "function" && S.didDestroy(), $s(this)) : Wr(this);
        } });
        const Yi = (w, S) => {
          const B = qe.innerParams.get(w);
          if (!B.input)
            return k('The "input" parameter is needed to be set when using returnInputValueOn'.concat(p(S)));
          const H = ((ee, he) => {
            const ke = ee.getInput();
            if (!ke)
              return null;
            switch (he.input) {
              case "checkbox":
                return pr(ke);
              case "radio":
                return Yo(ke);
              case "file":
                return Bs(ke);
              default:
                return he.inputAutoTrim ? ke.value.trim() : ke.value;
            }
          })(w, B);
          B.inputValidator ? ni(w, H, S) : w.getInput().checkValidity() ? S === "deny" ? yr(w, H) : i(w, H) : (w.enableButtons(), w.showValidationMessage(B.validationMessage));
        }, ni = (w, S, B) => {
          const H = qe.innerParams.get(w);
          w.disableInput(), Promise.resolve().then(() => L(H.inputValidator(S, H.validationMessage))).then((ee) => {
            w.enableButtons(), w.enableInput(), ee ? w.showValidationMessage(ee) : B === "deny" ? yr(w, S) : i(w, S);
          });
        }, yr = (w, S) => {
          const B = qe.innerParams.get(w || void 0);
          B.showLoaderOnDeny && Rn(Qo()), B.preDeny ? (qe.awaitingPromise.set(w || void 0, !0), Promise.resolve().then(() => L(B.preDeny(S, B.validationMessage))).then((H) => {
            H === !1 ? (w.hideLoading(), ei(w)) : w.closePopup({ isDenied: !0, value: H === void 0 ? S : H });
          }).catch((H) => qr(w || void 0, H))) : w.closePopup({ isDenied: !0, value: S });
        }, Ur = (w, S) => {
          w.closePopup({ isConfirmed: !0, value: S });
        }, qr = (w, S) => {
          w.rejectPromise(S);
        }, i = (w, S) => {
          const B = qe.innerParams.get(w || void 0);
          B.showLoaderOnConfirm && Rn(), B.preConfirm ? (w.resetValidationMessage(), qe.awaitingPromise.set(w || void 0, !0), Promise.resolve().then(() => L(B.preConfirm(S, B.validationMessage))).then((H) => {
            Xt(no()) || H === !1 ? (w.hideLoading(), ei(w)) : Ur(w, H === void 0 ? S : H);
          }).catch((H) => qr(w || void 0, H))) : Ur(w, S);
        }, s = (w, S, B) => {
          S.popup.onclick = () => {
            const H = qe.innerParams.get(w);
            H && (l(H) || H.timer || H.input) || B(tn.close);
          };
        }, l = (w) => w.showConfirmButton || w.showDenyButton || w.showCancelButton || w.showCloseButton;
        let h = !1;
        const f = (w) => {
          w.popup.onmousedown = () => {
            w.container.onmouseup = function(S) {
              w.container.onmouseup = void 0, S.target === w.container && (h = !0);
            };
          };
        }, m = (w) => {
          w.container.onmousedown = () => {
            w.popup.onmouseup = function(S) {
              w.popup.onmouseup = void 0, (S.target === w.popup || w.popup.contains(S.target)) && (h = !0);
            };
          };
        }, b = (w, S, B) => {
          S.container.onclick = (H) => {
            const ee = qe.innerParams.get(w);
            h ? h = !1 : H.target === S.container && x(ee.allowOutsideClick) && B(tn.backdrop);
          };
        }, E = (w) => w instanceof Element || ((S) => typeof S == "object" && S.jquery)(w), _ = () => {
          if (Ue.timeout)
            return (() => {
              const w = Di(), S = parseInt(window.getComputedStyle(w).width);
              w.style.removeProperty("transition"), w.style.width = "100%";
              const B = S / parseInt(window.getComputedStyle(w).width) * 100;
              w.style.removeProperty("transition"), w.style.width = "".concat(B, "%");
            })(), Ue.timeout.stop();
        }, O = () => {
          if (Ue.timeout) {
            const w = Ue.timeout.start();
            return Jt(w), w;
          }
        };
        let W = !1;
        const q = {}, N = (w) => {
          for (let S = w.target; S && S !== document; S = S.parentNode)
            for (const B in q) {
              const H = S.getAttribute(B);
              if (H)
                return void q[B].fire({ template: H });
            }
        };
        var X = Object.freeze({ isValidParameter: z, isUpdatableParameter: d, isDeprecatedParameter: j, argsToParams: (w) => {
          const S = {};
          return typeof w[0] != "object" || E(w[0]) ? ["title", "html", "icon"].forEach((B, H) => {
            const ee = w[H];
            typeof ee == "string" || E(ee) ? S[B] = ee : ee !== void 0 && k("Unexpected type of ".concat(B, '! Expected "string" or "Element", got ').concat(typeof ee));
          }) : Object.assign(S, w[0]), S;
        }, isVisible: () => Xt(oe()), clickConfirm: Ii, clickDeny: () => Qo() && Qo().click(), clickCancel: () => hn() && hn().click(), getContainer: Z, getPopup: oe, getTitle: Me, getHtmlContainer: je, getImage: Ae, getIcon: fe, getInputLabel: () => le(A["input-label"]), getCloseButton: io, getActions: un, getConfirmButton: yo, getDenyButton: Qo, getCancelButton: hn, getLoader: Bn, getFooter: Rt, getTimerProgressBar: Di, getFocusableElements: Ho, getValidationMessage: no, isLoading: () => oe().hasAttribute("data-loading"), fire: function() {
          const w = this;
          for (var S = arguments.length, B = new Array(S), H = 0; H < S; H++)
            B[H] = arguments[H];
          return new w(...B);
        }, mixin: function(w) {
          return class extends this {
            _main(S, B) {
              return super._main(S, Object.assign({}, w, B));
            }
          };
        }, showLoading: Rn, enableLoading: Rn, getTimerLeft: () => Ue.timeout && Ue.timeout.getTimerLeft(), stopTimer: _, resumeTimer: O, toggleTimer: () => {
          const w = Ue.timeout;
          return w && (w.running ? _() : O());
        }, increaseTimer: (w) => {
          if (Ue.timeout) {
            const S = Ue.timeout.increase(w);
            return Jt(S, !0), S;
          }
        }, isTimerRunning: () => Ue.timeout && Ue.timeout.isRunning(), bindClickHandler: function() {
          q[arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "data-swal-template"] = this, W || (document.body.addEventListener("click", N), W = !0);
        } });
        let ge;
        class Ce {
          constructor() {
            if (typeof window > "u")
              return;
            ge = this;
            for (var S = arguments.length, B = new Array(S), H = 0; H < S; H++)
              B[H] = arguments[H];
            const ee = Object.freeze(this.constructor.argsToParams(B));
            Object.defineProperties(this, { params: { value: ee, writable: !1, enumerable: !0, configurable: !0 } });
            const he = this._main(this.params);
            qe.promise.set(this, he);
          }
          _main(S) {
            let B = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
            ((he) => {
              !he.backdrop && he.allowOutsideClick && v('"allowOutsideClick" parameter requires `backdrop` parameter to be set to `true`');
              for (const ke in he)
                R(ke), he.toast && V(ke), K(ke);
            })(Object.assign({}, B, S)), Ue.currentInstance && (Ue.currentInstance._destroy(), Un() && Yr()), Ue.currentInstance = this;
            const H = Qe(S, B);
            dr(H), Object.freeze(H), Ue.timeout && (Ue.timeout.stop(), delete Ue.timeout), clearTimeout(Ue.restoreFocusTimeout);
            const ee = Ve(this);
            return en(this, H), qe.innerParams.set(this, H), xe(this, ee, H);
          }
          then(S) {
            return qe.promise.get(this).then(S);
          }
          finally(S) {
            return qe.promise.get(this).finally(S);
          }
        }
        const xe = (w, S, B) => new Promise((H, ee) => {
          const he = (ke) => {
            w.closePopup({ isDismissed: !0, dismiss: ke });
          };
          Fo.swalPromiseResolve.set(w, H), Fo.swalPromiseReject.set(w, ee), S.confirmButton.onclick = () => ((ke) => {
            const Ee = qe.innerParams.get(ke);
            ke.disableButtons(), Ee.input ? Yi(ke, "confirm") : i(ke, !0);
          })(w), S.denyButton.onclick = () => ((ke) => {
            const Ee = qe.innerParams.get(ke);
            ke.disableButtons(), Ee.returnInputValueOnDeny ? Yi(ke, "deny") : yr(ke, !1);
          })(w), S.cancelButton.onclick = () => ((ke, Ee) => {
            ke.disableButtons(), Ee(tn.cancel);
          })(w, he), S.closeButton.onclick = () => he(tn.close), ((ke, Ee, Be) => {
            qe.innerParams.get(ke).toast ? s(ke, Ee, Be) : (f(Ee), m(Ee), b(ke, Ee, Be));
          })(w, S, he), ((ke, Ee, Be, St) => {
            Ni(Ee), Be.toast || (Ee.keydownHandler = (it) => Is(ke, it, St), Ee.keydownTarget = Be.keydownListenerCapture ? window : oe(), Ee.keydownListenerCapture = Be.keydownListenerCapture, Ee.keydownTarget.addEventListener("keydown", Ee.keydownHandler, { capture: Ee.keydownListenerCapture }), Ee.keydownHandlerAdded = !0);
          })(w, Ue, B, he), ((ke, Ee) => {
            Ee.input === "select" || Ee.input === "radio" ? As(ke, Ee) : ["text", "email", "number", "tel", "textarea"].includes(Ee.input) && (M(Ee.inputValue) || Y(Ee.inputValue)) && (Rn(yo()), ro(ke, Ee));
          })(w, B), Jn(B), Ht(Ue, B, he), jt(S, B), setTimeout(() => {
            S.container.scrollTop = 0;
          });
        }), Qe = (w, S) => {
          const B = ((ee) => {
            const he = typeof ee.template == "string" ? document.querySelector(ee.template) : ee.template;
            if (!he)
              return {};
            const ke = he.content;
            return Ms(ke), Object.assign(sr(ke), ar(ke), lr(ke), cr(ke), Es(ke), Kn(ke, Lo));
          })(w), H = Object.assign({}, I, S, B, w);
          return H.showClass = Object.assign({}, I.showClass, H.showClass), H.hideClass = Object.assign({}, I.hideClass, H.hideClass), H;
        }, Ve = (w) => {
          const S = { popup: oe(), container: Z(), actions: un(), confirmButton: yo(), denyButton: Qo(), cancelButton: hn(), loader: Bn(), closeButton: io(), validationMessage: no(), progressSteps: Je() };
          return qe.domCache.set(w, S), S;
        }, Ht = (w, S, B) => {
          const H = Di();
          Kt(H), S.timer && (w.timeout = new Xn(() => {
            B("timer"), delete w.timeout;
          }, S.timer), S.timerProgressBar && (Wt(H), uo(H, S, "timerProgressBar"), setTimeout(() => {
            w.timeout && w.timeout.running && Jt(S.timer);
          })));
        }, jt = (w, S) => {
          if (!S.toast)
            return x(S.allowEnterKey) ? void (Se(w, S) || mr(0, -1, 1)) : ao();
        }, Se = (w, S) => S.focusDeny && Xt(w.denyButton) ? (w.denyButton.focus(), !0) : S.focusCancel && Xt(w.cancelButton) ? (w.cancelButton.focus(), !0) : !(!S.focusConfirm || !Xt(w.confirmButton) || (w.confirmButton.focus(), 0)), ao = () => {
          document.activeElement instanceof HTMLElement && typeof document.activeElement.blur == "function" && document.activeElement.blur();
        };
        Object.assign(Ce.prototype, Bo), Object.assign(Ce, X), Object.keys(Bo).forEach((w) => {
          Ce[w] = function() {
            if (ge)
              return ge[w](...arguments);
          };
        }), Ce.DismissReason = tn, Ce.version = "11.4.10";
        const De = Ce;
        return De.default = De, De;
      }(), this !== void 0 && this.Sweetalert2 && (this.swal = this.sweetAlert = this.Swal = this.SweetAlert = this.Sweetalert2), typeof document < "u" && function(u, p) {
        var g = u.createElement("style");
        if (u.getElementsByTagName("head")[0].appendChild(g), g.styleSheet)
          g.styleSheet.disabled || (g.styleSheet.cssText = p);
        else
          try {
            g.innerHTML = p;
          } catch {
            g.innerText = p;
          }
      }(document, '.swal2-popup.swal2-toast{box-sizing:border-box;grid-column:1/4!important;grid-row:1/4!important;grid-template-columns:1fr 99fr 1fr;padding:1em;overflow-y:hidden;background:#fff;box-shadow:0 0 1px hsla(0deg,0%,0%,.075),0 1px 2px hsla(0deg,0%,0%,.075),1px 2px 4px hsla(0deg,0%,0%,.075),1px 3px 8px hsla(0deg,0%,0%,.075),2px 4px 16px hsla(0deg,0%,0%,.075);pointer-events:all}.swal2-popup.swal2-toast>*{grid-column:2}.swal2-popup.swal2-toast .swal2-title{margin:.5em 1em;padding:0;font-size:1em;text-align:initial}.swal2-popup.swal2-toast .swal2-loading{justify-content:center}.swal2-popup.swal2-toast .swal2-input{height:2em;margin:.5em;font-size:1em}.swal2-popup.swal2-toast .swal2-validation-message{font-size:1em}.swal2-popup.swal2-toast .swal2-footer{margin:.5em 0 0;padding:.5em 0 0;font-size:.8em}.swal2-popup.swal2-toast .swal2-close{grid-column:3/3;grid-row:1/99;align-self:center;width:.8em;height:.8em;margin:0;font-size:2em}.swal2-popup.swal2-toast .swal2-html-container{margin:.5em 1em;padding:0;font-size:1em;text-align:initial}.swal2-popup.swal2-toast .swal2-html-container:empty{padding:0}.swal2-popup.swal2-toast .swal2-loader{grid-column:1;grid-row:1/99;align-self:center;width:2em;height:2em;margin:.25em}.swal2-popup.swal2-toast .swal2-icon{grid-column:1;grid-row:1/99;align-self:center;width:2em;min-width:2em;height:2em;margin:0 .5em 0 0}.swal2-popup.swal2-toast .swal2-icon .swal2-icon-content{display:flex;align-items:center;font-size:1.8em;font-weight:700}.swal2-popup.swal2-toast .swal2-icon.swal2-success .swal2-success-ring{width:2em;height:2em}.swal2-popup.swal2-toast .swal2-icon.swal2-error [class^=swal2-x-mark-line]{top:.875em;width:1.375em}.swal2-popup.swal2-toast .swal2-icon.swal2-error [class^=swal2-x-mark-line][class$=left]{left:.3125em}.swal2-popup.swal2-toast .swal2-icon.swal2-error [class^=swal2-x-mark-line][class$=right]{right:.3125em}.swal2-popup.swal2-toast .swal2-actions{justify-content:flex-start;height:auto;margin:0;margin-top:.5em;padding:0 .5em}.swal2-popup.swal2-toast .swal2-styled{margin:.25em .5em;padding:.4em .6em;font-size:1em}.swal2-popup.swal2-toast .swal2-success{border-color:#a5dc86}.swal2-popup.swal2-toast .swal2-success [class^=swal2-success-circular-line]{position:absolute;width:1.6em;height:3em;transform:rotate(45deg);border-radius:50%}.swal2-popup.swal2-toast .swal2-success [class^=swal2-success-circular-line][class$=left]{top:-.8em;left:-.5em;transform:rotate(-45deg);transform-origin:2em 2em;border-radius:4em 0 0 4em}.swal2-popup.swal2-toast .swal2-success [class^=swal2-success-circular-line][class$=right]{top:-.25em;left:.9375em;transform-origin:0 1.5em;border-radius:0 4em 4em 0}.swal2-popup.swal2-toast .swal2-success .swal2-success-ring{width:2em;height:2em}.swal2-popup.swal2-toast .swal2-success .swal2-success-fix{top:0;left:.4375em;width:.4375em;height:2.6875em}.swal2-popup.swal2-toast .swal2-success [class^=swal2-success-line]{height:.3125em}.swal2-popup.swal2-toast .swal2-success [class^=swal2-success-line][class$=tip]{top:1.125em;left:.1875em;width:.75em}.swal2-popup.swal2-toast .swal2-success [class^=swal2-success-line][class$=long]{top:.9375em;right:.1875em;width:1.375em}.swal2-popup.swal2-toast .swal2-success.swal2-icon-show .swal2-success-line-tip{-webkit-animation:swal2-toast-animate-success-line-tip .75s;animation:swal2-toast-animate-success-line-tip .75s}.swal2-popup.swal2-toast .swal2-success.swal2-icon-show .swal2-success-line-long{-webkit-animation:swal2-toast-animate-success-line-long .75s;animation:swal2-toast-animate-success-line-long .75s}.swal2-popup.swal2-toast.swal2-show{-webkit-animation:swal2-toast-show .5s;animation:swal2-toast-show .5s}.swal2-popup.swal2-toast.swal2-hide{-webkit-animation:swal2-toast-hide .1s forwards;animation:swal2-toast-hide .1s forwards}.swal2-container{display:grid;position:fixed;z-index:1060;top:0;right:0;bottom:0;left:0;box-sizing:border-box;grid-template-areas:"top-start     top            top-end" "center-start  center         center-end" "bottom-start  bottom-center  bottom-end";grid-template-rows:minmax(-webkit-min-content,auto) minmax(-webkit-min-content,auto) minmax(-webkit-min-content,auto);grid-template-rows:minmax(min-content,auto) minmax(min-content,auto) minmax(min-content,auto);height:100%;padding:.625em;overflow-x:hidden;transition:background-color .1s;-webkit-overflow-scrolling:touch}.swal2-container.swal2-backdrop-show,.swal2-container.swal2-noanimation{background:rgba(0,0,0,.4)}.swal2-container.swal2-backdrop-hide{background:0 0!important}.swal2-container.swal2-bottom-start,.swal2-container.swal2-center-start,.swal2-container.swal2-top-start{grid-template-columns:minmax(0,1fr) auto auto}.swal2-container.swal2-bottom,.swal2-container.swal2-center,.swal2-container.swal2-top{grid-template-columns:auto minmax(0,1fr) auto}.swal2-container.swal2-bottom-end,.swal2-container.swal2-center-end,.swal2-container.swal2-top-end{grid-template-columns:auto auto minmax(0,1fr)}.swal2-container.swal2-top-start>.swal2-popup{align-self:start}.swal2-container.swal2-top>.swal2-popup{grid-column:2;align-self:start;justify-self:center}.swal2-container.swal2-top-end>.swal2-popup,.swal2-container.swal2-top-right>.swal2-popup{grid-column:3;align-self:start;justify-self:end}.swal2-container.swal2-center-left>.swal2-popup,.swal2-container.swal2-center-start>.swal2-popup{grid-row:2;align-self:center}.swal2-container.swal2-center>.swal2-popup{grid-column:2;grid-row:2;align-self:center;justify-self:center}.swal2-container.swal2-center-end>.swal2-popup,.swal2-container.swal2-center-right>.swal2-popup{grid-column:3;grid-row:2;align-self:center;justify-self:end}.swal2-container.swal2-bottom-left>.swal2-popup,.swal2-container.swal2-bottom-start>.swal2-popup{grid-column:1;grid-row:3;align-self:end}.swal2-container.swal2-bottom>.swal2-popup{grid-column:2;grid-row:3;justify-self:center;align-self:end}.swal2-container.swal2-bottom-end>.swal2-popup,.swal2-container.swal2-bottom-right>.swal2-popup{grid-column:3;grid-row:3;align-self:end;justify-self:end}.swal2-container.swal2-grow-fullscreen>.swal2-popup,.swal2-container.swal2-grow-row>.swal2-popup{grid-column:1/4;width:100%}.swal2-container.swal2-grow-column>.swal2-popup,.swal2-container.swal2-grow-fullscreen>.swal2-popup{grid-row:1/4;align-self:stretch}.swal2-container.swal2-no-transition{transition:none!important}.swal2-popup{display:none;position:relative;box-sizing:border-box;grid-template-columns:minmax(0,100%);width:32em;max-width:100%;padding:0 0 1.25em;border:none;border-radius:5px;background:#fff;color:#545454;font-family:inherit;font-size:1rem}.swal2-popup:focus{outline:0}.swal2-popup.swal2-loading{overflow-y:hidden}.swal2-title{position:relative;max-width:100%;margin:0;padding:.8em 1em 0;color:inherit;font-size:1.875em;font-weight:600;text-align:center;text-transform:none;word-wrap:break-word}.swal2-actions{display:flex;z-index:1;box-sizing:border-box;flex-wrap:wrap;align-items:center;justify-content:center;width:auto;margin:1.25em auto 0;padding:0}.swal2-actions:not(.swal2-loading) .swal2-styled[disabled]{opacity:.4}.swal2-actions:not(.swal2-loading) .swal2-styled:hover{background-image:linear-gradient(rgba(0,0,0,.1),rgba(0,0,0,.1))}.swal2-actions:not(.swal2-loading) .swal2-styled:active{background-image:linear-gradient(rgba(0,0,0,.2),rgba(0,0,0,.2))}.swal2-loader{display:none;align-items:center;justify-content:center;width:2.2em;height:2.2em;margin:0 1.875em;-webkit-animation:swal2-rotate-loading 1.5s linear 0s infinite normal;animation:swal2-rotate-loading 1.5s linear 0s infinite normal;border-width:.25em;border-style:solid;border-radius:100%;border-color:#2778c4 transparent #2778c4 transparent}.swal2-styled{margin:.3125em;padding:.625em 1.1em;transition:box-shadow .1s;box-shadow:0 0 0 3px transparent;font-weight:500}.swal2-styled:not([disabled]){cursor:pointer}.swal2-styled.swal2-confirm{border:0;border-radius:.25em;background:initial;background-color:#7066e0;color:#fff;font-size:1em}.swal2-styled.swal2-confirm:focus{box-shadow:0 0 0 3px rgba(112,102,224,.5)}.swal2-styled.swal2-deny{border:0;border-radius:.25em;background:initial;background-color:#dc3741;color:#fff;font-size:1em}.swal2-styled.swal2-deny:focus{box-shadow:0 0 0 3px rgba(220,55,65,.5)}.swal2-styled.swal2-cancel{border:0;border-radius:.25em;background:initial;background-color:#6e7881;color:#fff;font-size:1em}.swal2-styled.swal2-cancel:focus{box-shadow:0 0 0 3px rgba(110,120,129,.5)}.swal2-styled.swal2-default-outline:focus{box-shadow:0 0 0 3px rgba(100,150,200,.5)}.swal2-styled:focus{outline:0}.swal2-styled::-moz-focus-inner{border:0}.swal2-footer{justify-content:center;margin:1em 0 0;padding:1em 1em 0;border-top:1px solid #eee;color:inherit;font-size:1em}.swal2-timer-progress-bar-container{position:absolute;right:0;bottom:0;left:0;grid-column:auto!important;overflow:hidden;border-bottom-right-radius:5px;border-bottom-left-radius:5px}.swal2-timer-progress-bar{width:100%;height:.25em;background:rgba(0,0,0,.2)}.swal2-image{max-width:100%;margin:2em auto 1em}.swal2-close{z-index:2;align-items:center;justify-content:center;width:1.2em;height:1.2em;margin-top:0;margin-right:0;margin-bottom:-1.2em;padding:0;overflow:hidden;transition:color .1s,box-shadow .1s;border:none;border-radius:5px;background:0 0;color:#ccc;font-family:serif;font-family:monospace;font-size:2.5em;cursor:pointer;justify-self:end}.swal2-close:hover{transform:none;background:0 0;color:#f27474}.swal2-close:focus{outline:0;box-shadow:inset 0 0 0 3px rgba(100,150,200,.5)}.swal2-close::-moz-focus-inner{border:0}.swal2-html-container{z-index:1;justify-content:center;margin:1em 1.6em .3em;padding:0;overflow:auto;color:inherit;font-size:1.125em;font-weight:400;line-height:normal;text-align:center;word-wrap:break-word;word-break:break-word}.swal2-checkbox,.swal2-file,.swal2-input,.swal2-radio,.swal2-select,.swal2-textarea{margin:1em 2em 3px}.swal2-file,.swal2-input,.swal2-textarea{box-sizing:border-box;width:auto;transition:border-color .1s,box-shadow .1s;border:1px solid #d9d9d9;border-radius:.1875em;background:inherit;box-shadow:inset 0 1px 1px rgba(0,0,0,.06),0 0 0 3px transparent;color:inherit;font-size:1.125em}.swal2-file.swal2-inputerror,.swal2-input.swal2-inputerror,.swal2-textarea.swal2-inputerror{border-color:#f27474!important;box-shadow:0 0 2px #f27474!important}.swal2-file:focus,.swal2-input:focus,.swal2-textarea:focus{border:1px solid #b4dbed;outline:0;box-shadow:inset 0 1px 1px rgba(0,0,0,.06),0 0 0 3px rgba(100,150,200,.5)}.swal2-file::-moz-placeholder,.swal2-input::-moz-placeholder,.swal2-textarea::-moz-placeholder{color:#ccc}.swal2-file:-ms-input-placeholder,.swal2-input:-ms-input-placeholder,.swal2-textarea:-ms-input-placeholder{color:#ccc}.swal2-file::placeholder,.swal2-input::placeholder,.swal2-textarea::placeholder{color:#ccc}.swal2-range{margin:1em 2em 3px;background:#fff}.swal2-range input{width:80%}.swal2-range output{width:20%;color:inherit;font-weight:600;text-align:center}.swal2-range input,.swal2-range output{height:2.625em;padding:0;font-size:1.125em;line-height:2.625em}.swal2-input{height:2.625em;padding:0 .75em}.swal2-file{width:75%;margin-right:auto;margin-left:auto;background:inherit;font-size:1.125em}.swal2-textarea{height:6.75em;padding:.75em}.swal2-select{min-width:50%;max-width:100%;padding:.375em .625em;background:inherit;color:inherit;font-size:1.125em}.swal2-checkbox,.swal2-radio{align-items:center;justify-content:center;background:#fff;color:inherit}.swal2-checkbox label,.swal2-radio label{margin:0 .6em;font-size:1.125em}.swal2-checkbox input,.swal2-radio input{flex-shrink:0;margin:0 .4em}.swal2-input-label{display:flex;justify-content:center;margin:1em auto 0}.swal2-validation-message{align-items:center;justify-content:center;margin:1em 0 0;padding:.625em;overflow:hidden;background:#f0f0f0;color:#666;font-size:1em;font-weight:300}.swal2-validation-message::before{content:"!";display:inline-block;width:1.5em;min-width:1.5em;height:1.5em;margin:0 .625em;border-radius:50%;background-color:#f27474;color:#fff;font-weight:600;line-height:1.5em;text-align:center}.swal2-icon{position:relative;box-sizing:content-box;justify-content:center;width:5em;height:5em;margin:2.5em auto .6em;border:.25em solid transparent;border-radius:50%;border-color:#000;font-family:inherit;line-height:5em;cursor:default;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none}.swal2-icon .swal2-icon-content{display:flex;align-items:center;font-size:3.75em}.swal2-icon.swal2-error{border-color:#f27474;color:#f27474}.swal2-icon.swal2-error .swal2-x-mark{position:relative;flex-grow:1}.swal2-icon.swal2-error [class^=swal2-x-mark-line]{display:block;position:absolute;top:2.3125em;width:2.9375em;height:.3125em;border-radius:.125em;background-color:#f27474}.swal2-icon.swal2-error [class^=swal2-x-mark-line][class$=left]{left:1.0625em;transform:rotate(45deg)}.swal2-icon.swal2-error [class^=swal2-x-mark-line][class$=right]{right:1em;transform:rotate(-45deg)}.swal2-icon.swal2-error.swal2-icon-show{-webkit-animation:swal2-animate-error-icon .5s;animation:swal2-animate-error-icon .5s}.swal2-icon.swal2-error.swal2-icon-show .swal2-x-mark{-webkit-animation:swal2-animate-error-x-mark .5s;animation:swal2-animate-error-x-mark .5s}.swal2-icon.swal2-warning{border-color:#facea8;color:#f8bb86}.swal2-icon.swal2-warning.swal2-icon-show{-webkit-animation:swal2-animate-error-icon .5s;animation:swal2-animate-error-icon .5s}.swal2-icon.swal2-warning.swal2-icon-show .swal2-icon-content{-webkit-animation:swal2-animate-i-mark .5s;animation:swal2-animate-i-mark .5s}.swal2-icon.swal2-info{border-color:#9de0f6;color:#3fc3ee}.swal2-icon.swal2-info.swal2-icon-show{-webkit-animation:swal2-animate-error-icon .5s;animation:swal2-animate-error-icon .5s}.swal2-icon.swal2-info.swal2-icon-show .swal2-icon-content{-webkit-animation:swal2-animate-i-mark .8s;animation:swal2-animate-i-mark .8s}.swal2-icon.swal2-question{border-color:#c9dae1;color:#87adbd}.swal2-icon.swal2-question.swal2-icon-show{-webkit-animation:swal2-animate-error-icon .5s;animation:swal2-animate-error-icon .5s}.swal2-icon.swal2-question.swal2-icon-show .swal2-icon-content{-webkit-animation:swal2-animate-question-mark .8s;animation:swal2-animate-question-mark .8s}.swal2-icon.swal2-success{border-color:#a5dc86;color:#a5dc86}.swal2-icon.swal2-success [class^=swal2-success-circular-line]{position:absolute;width:3.75em;height:7.5em;transform:rotate(45deg);border-radius:50%}.swal2-icon.swal2-success [class^=swal2-success-circular-line][class$=left]{top:-.4375em;left:-2.0635em;transform:rotate(-45deg);transform-origin:3.75em 3.75em;border-radius:7.5em 0 0 7.5em}.swal2-icon.swal2-success [class^=swal2-success-circular-line][class$=right]{top:-.6875em;left:1.875em;transform:rotate(-45deg);transform-origin:0 3.75em;border-radius:0 7.5em 7.5em 0}.swal2-icon.swal2-success .swal2-success-ring{position:absolute;z-index:2;top:-.25em;left:-.25em;box-sizing:content-box;width:100%;height:100%;border:.25em solid rgba(165,220,134,.3);border-radius:50%}.swal2-icon.swal2-success .swal2-success-fix{position:absolute;z-index:1;top:.5em;left:1.625em;width:.4375em;height:5.625em;transform:rotate(-45deg)}.swal2-icon.swal2-success [class^=swal2-success-line]{display:block;position:absolute;z-index:2;height:.3125em;border-radius:.125em;background-color:#a5dc86}.swal2-icon.swal2-success [class^=swal2-success-line][class$=tip]{top:2.875em;left:.8125em;width:1.5625em;transform:rotate(45deg)}.swal2-icon.swal2-success [class^=swal2-success-line][class$=long]{top:2.375em;right:.5em;width:2.9375em;transform:rotate(-45deg)}.swal2-icon.swal2-success.swal2-icon-show .swal2-success-line-tip{-webkit-animation:swal2-animate-success-line-tip .75s;animation:swal2-animate-success-line-tip .75s}.swal2-icon.swal2-success.swal2-icon-show .swal2-success-line-long{-webkit-animation:swal2-animate-success-line-long .75s;animation:swal2-animate-success-line-long .75s}.swal2-icon.swal2-success.swal2-icon-show .swal2-success-circular-line-right{-webkit-animation:swal2-rotate-success-circular-line 4.25s ease-in;animation:swal2-rotate-success-circular-line 4.25s ease-in}.swal2-progress-steps{flex-wrap:wrap;align-items:center;max-width:100%;margin:1.25em auto;padding:0;background:inherit;font-weight:600}.swal2-progress-steps li{display:inline-block;position:relative}.swal2-progress-steps .swal2-progress-step{z-index:20;flex-shrink:0;width:2em;height:2em;border-radius:2em;background:#2778c4;color:#fff;line-height:2em;text-align:center}.swal2-progress-steps .swal2-progress-step.swal2-active-progress-step{background:#2778c4}.swal2-progress-steps .swal2-progress-step.swal2-active-progress-step~.swal2-progress-step{background:#add8e6;color:#fff}.swal2-progress-steps .swal2-progress-step.swal2-active-progress-step~.swal2-progress-step-line{background:#add8e6}.swal2-progress-steps .swal2-progress-step-line{z-index:10;flex-shrink:0;width:2.5em;height:.4em;margin:0 -1px;background:#2778c4}[class^=swal2]{-webkit-tap-highlight-color:transparent}.swal2-show{-webkit-animation:swal2-show .3s;animation:swal2-show .3s}.swal2-hide{-webkit-animation:swal2-hide .15s forwards;animation:swal2-hide .15s forwards}.swal2-noanimation{transition:none}.swal2-scrollbar-measure{position:absolute;top:-9999px;width:50px;height:50px;overflow:scroll}.swal2-rtl .swal2-close{margin-right:initial;margin-left:0}.swal2-rtl .swal2-timer-progress-bar{right:0;left:auto}.swal2-no-war{display:flex;position:fixed;z-index:1061;top:0;left:0;align-items:center;justify-content:center;width:100%;height:3.375em;background:#20232a;color:#fff;text-align:center}.swal2-no-war a{color:#61dafb;text-decoration:none}.swal2-no-war a:hover{text-decoration:underline}@-webkit-keyframes swal2-toast-show{0%{transform:translateY(-.625em) rotateZ(2deg)}33%{transform:translateY(0) rotateZ(-2deg)}66%{transform:translateY(.3125em) rotateZ(2deg)}100%{transform:translateY(0) rotateZ(0)}}@keyframes swal2-toast-show{0%{transform:translateY(-.625em) rotateZ(2deg)}33%{transform:translateY(0) rotateZ(-2deg)}66%{transform:translateY(.3125em) rotateZ(2deg)}100%{transform:translateY(0) rotateZ(0)}}@-webkit-keyframes swal2-toast-hide{100%{transform:rotateZ(1deg);opacity:0}}@keyframes swal2-toast-hide{100%{transform:rotateZ(1deg);opacity:0}}@-webkit-keyframes swal2-toast-animate-success-line-tip{0%{top:.5625em;left:.0625em;width:0}54%{top:.125em;left:.125em;width:0}70%{top:.625em;left:-.25em;width:1.625em}84%{top:1.0625em;left:.75em;width:.5em}100%{top:1.125em;left:.1875em;width:.75em}}@keyframes swal2-toast-animate-success-line-tip{0%{top:.5625em;left:.0625em;width:0}54%{top:.125em;left:.125em;width:0}70%{top:.625em;left:-.25em;width:1.625em}84%{top:1.0625em;left:.75em;width:.5em}100%{top:1.125em;left:.1875em;width:.75em}}@-webkit-keyframes swal2-toast-animate-success-line-long{0%{top:1.625em;right:1.375em;width:0}65%{top:1.25em;right:.9375em;width:0}84%{top:.9375em;right:0;width:1.125em}100%{top:.9375em;right:.1875em;width:1.375em}}@keyframes swal2-toast-animate-success-line-long{0%{top:1.625em;right:1.375em;width:0}65%{top:1.25em;right:.9375em;width:0}84%{top:.9375em;right:0;width:1.125em}100%{top:.9375em;right:.1875em;width:1.375em}}@-webkit-keyframes swal2-show{0%{transform:scale(.7)}45%{transform:scale(1.05)}80%{transform:scale(.95)}100%{transform:scale(1)}}@keyframes swal2-show{0%{transform:scale(.7)}45%{transform:scale(1.05)}80%{transform:scale(.95)}100%{transform:scale(1)}}@-webkit-keyframes swal2-hide{0%{transform:scale(1);opacity:1}100%{transform:scale(.5);opacity:0}}@keyframes swal2-hide{0%{transform:scale(1);opacity:1}100%{transform:scale(.5);opacity:0}}@-webkit-keyframes swal2-animate-success-line-tip{0%{top:1.1875em;left:.0625em;width:0}54%{top:1.0625em;left:.125em;width:0}70%{top:2.1875em;left:-.375em;width:3.125em}84%{top:3em;left:1.3125em;width:1.0625em}100%{top:2.8125em;left:.8125em;width:1.5625em}}@keyframes swal2-animate-success-line-tip{0%{top:1.1875em;left:.0625em;width:0}54%{top:1.0625em;left:.125em;width:0}70%{top:2.1875em;left:-.375em;width:3.125em}84%{top:3em;left:1.3125em;width:1.0625em}100%{top:2.8125em;left:.8125em;width:1.5625em}}@-webkit-keyframes swal2-animate-success-line-long{0%{top:3.375em;right:2.875em;width:0}65%{top:3.375em;right:2.875em;width:0}84%{top:2.1875em;right:0;width:3.4375em}100%{top:2.375em;right:.5em;width:2.9375em}}@keyframes swal2-animate-success-line-long{0%{top:3.375em;right:2.875em;width:0}65%{top:3.375em;right:2.875em;width:0}84%{top:2.1875em;right:0;width:3.4375em}100%{top:2.375em;right:.5em;width:2.9375em}}@-webkit-keyframes swal2-rotate-success-circular-line{0%{transform:rotate(-45deg)}5%{transform:rotate(-45deg)}12%{transform:rotate(-405deg)}100%{transform:rotate(-405deg)}}@keyframes swal2-rotate-success-circular-line{0%{transform:rotate(-45deg)}5%{transform:rotate(-45deg)}12%{transform:rotate(-405deg)}100%{transform:rotate(-405deg)}}@-webkit-keyframes swal2-animate-error-x-mark{0%{margin-top:1.625em;transform:scale(.4);opacity:0}50%{margin-top:1.625em;transform:scale(.4);opacity:0}80%{margin-top:-.375em;transform:scale(1.15)}100%{margin-top:0;transform:scale(1);opacity:1}}@keyframes swal2-animate-error-x-mark{0%{margin-top:1.625em;transform:scale(.4);opacity:0}50%{margin-top:1.625em;transform:scale(.4);opacity:0}80%{margin-top:-.375em;transform:scale(1.15)}100%{margin-top:0;transform:scale(1);opacity:1}}@-webkit-keyframes swal2-animate-error-icon{0%{transform:rotateX(100deg);opacity:0}100%{transform:rotateX(0);opacity:1}}@keyframes swal2-animate-error-icon{0%{transform:rotateX(100deg);opacity:0}100%{transform:rotateX(0);opacity:1}}@-webkit-keyframes swal2-rotate-loading{0%{transform:rotate(0)}100%{transform:rotate(360deg)}}@keyframes swal2-rotate-loading{0%{transform:rotate(0)}100%{transform:rotate(360deg)}}@-webkit-keyframes swal2-animate-question-mark{0%{transform:rotateY(-360deg)}100%{transform:rotateY(0)}}@keyframes swal2-animate-question-mark{0%{transform:rotateY(-360deg)}100%{transform:rotateY(0)}}@-webkit-keyframes swal2-animate-i-mark{0%{transform:rotateZ(45deg);opacity:0}25%{transform:rotateZ(-25deg);opacity:.4}50%{transform:rotateZ(15deg);opacity:.8}75%{transform:rotateZ(-5deg);opacity:1}100%{transform:rotateX(0);opacity:1}}@keyframes swal2-animate-i-mark{0%{transform:rotateZ(45deg);opacity:0}25%{transform:rotateZ(-25deg);opacity:.4}50%{transform:rotateZ(15deg);opacity:.8}75%{transform:rotateZ(-5deg);opacity:1}100%{transform:rotateX(0);opacity:1}}body.swal2-shown:not(.swal2-no-backdrop):not(.swal2-toast-shown){overflow:hidden}body.swal2-height-auto{height:auto!important}body.swal2-no-backdrop .swal2-container{background-color:transparent!important;pointer-events:none}body.swal2-no-backdrop .swal2-container .swal2-popup{pointer-events:all}body.swal2-no-backdrop .swal2-container .swal2-modal{box-shadow:0 0 10px rgba(0,0,0,.4)}@media print{body.swal2-shown:not(.swal2-no-backdrop):not(.swal2-toast-shown){overflow-y:scroll!important}body.swal2-shown:not(.swal2-no-backdrop):not(.swal2-toast-shown)>[aria-hidden=true]{display:none}body.swal2-shown:not(.swal2-no-backdrop):not(.swal2-toast-shown) .swal2-container{position:static!important}}body.swal2-toast-shown .swal2-container{box-sizing:border-box;width:360px;max-width:100%;background-color:transparent;pointer-events:none}body.swal2-toast-shown .swal2-container.swal2-top{top:0;right:auto;bottom:auto;left:50%;transform:translateX(-50%)}body.swal2-toast-shown .swal2-container.swal2-top-end,body.swal2-toast-shown .swal2-container.swal2-top-right{top:0;right:0;bottom:auto;left:auto}body.swal2-toast-shown .swal2-container.swal2-top-left,body.swal2-toast-shown .swal2-container.swal2-top-start{top:0;right:auto;bottom:auto;left:0}body.swal2-toast-shown .swal2-container.swal2-center-left,body.swal2-toast-shown .swal2-container.swal2-center-start{top:50%;right:auto;bottom:auto;left:0;transform:translateY(-50%)}body.swal2-toast-shown .swal2-container.swal2-center{top:50%;right:auto;bottom:auto;left:50%;transform:translate(-50%,-50%)}body.swal2-toast-shown .swal2-container.swal2-center-end,body.swal2-toast-shown .swal2-container.swal2-center-right{top:50%;right:0;bottom:auto;left:auto;transform:translateY(-50%)}body.swal2-toast-shown .swal2-container.swal2-bottom-left,body.swal2-toast-shown .swal2-container.swal2-bottom-start{top:auto;right:auto;bottom:0;left:0}body.swal2-toast-shown .swal2-container.swal2-bottom{top:auto;right:auto;bottom:0;left:50%;transform:translateX(-50%)}body.swal2-toast-shown .swal2-container.swal2-bottom-end,body.swal2-toast-shown .swal2-container.swal2-bottom-right{top:auto;right:0;bottom:0;left:auto}');
    } }, n = {};
    function r(c) {
      var u = n[c];
      if (u !== void 0)
        return u.exports;
      var p = n[c] = { id: c, exports: {} };
      return t[c].call(p.exports, p, p.exports, r), p.exports;
    }
    r.n = (c) => {
      var u = c && c.__esModule ? () => c.default : () => c;
      return r.d(u, { a: u }), u;
    }, r.d = (c, u) => {
      for (var p in u)
        r.o(u, p) && !r.o(c, p) && Object.defineProperty(c, p, { enumerable: !0, get: u[p] });
    }, r.o = (c, u) => Object.prototype.hasOwnProperty.call(c, u), r.nc = void 0;
    var a = {};
    return (() => {
      var c;
      r.d(a, { default: () => ne });
      var u = new Uint8Array(16);
      function p() {
        if (!c && !(c = typeof crypto < "u" && crypto.getRandomValues && crypto.getRandomValues.bind(crypto) || typeof msCrypto < "u" && typeof msCrypto.getRandomValues == "function" && msCrypto.getRandomValues.bind(msCrypto)))
          throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
        return c(u);
      }
      const g = /^(?:[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|00000000-0000-0000-0000-000000000000)$/i, v = function(le) {
        return typeof le == "string" && g.test(le);
      };
      for (var k = [], y = 0; y < 256; ++y)
        k.push((y + 256).toString(16).substr(1));
      const T = function(le, oe, fe) {
        var Me = (le = le || {}).random || (le.rng || p)();
        if (Me[6] = 15 & Me[6] | 64, Me[8] = 63 & Me[8] | 128, oe) {
          fe = fe || 0;
          for (var je = 0; je < 16; ++je)
            oe[fe + je] = Me[je];
          return oe;
        }
        return function(Ae) {
          var Je = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, no = (k[Ae[Je + 0]] + k[Ae[Je + 1]] + k[Ae[Je + 2]] + k[Ae[Je + 3]] + "-" + k[Ae[Je + 4]] + k[Ae[Je + 5]] + "-" + k[Ae[Je + 6]] + k[Ae[Je + 7]] + "-" + k[Ae[Je + 8]] + k[Ae[Je + 9]] + "-" + k[Ae[Je + 10]] + k[Ae[Je + 11]] + k[Ae[Je + 12]] + k[Ae[Je + 13]] + k[Ae[Je + 14]] + k[Ae[Je + 15]]).toLowerCase();
          if (!v(no))
            throw TypeError("Stringified UUID is invalid");
          return no;
        }(Me);
      };
      var x = r(455), M = r.n(x), L = r(40), Y = r.n(L), I = r(379), F = r.n(I), Q = r(795), U = r.n(Q), z = r(569), d = r.n(z), j = r(565), R = r.n(j), V = r(216), K = r.n(V), J = r(589), A = r.n(J), se = r(745), Z = {};
      Z.styleTagTransform = A(), Z.setAttributes = R(), Z.insert = d().bind(null, "head"), Z.domAPI = U(), Z.insertStyleElement = K(), F()(se.Z, Z), se.Z && se.Z.locals && se.Z.locals;
      class ne {
        static get enableLineBreaks() {
          return !0;
        }
        constructor({ data: oe, config: fe, api: Me, readOnly: je }) {
          this.api = Me, this.readOnly = je, this.config = fe || {}, console.log(this.config), console.log(this.config.EditorJsLibrary), this._CSS = { block: this.api.styles.block, wrapper: "ce-EditorJsColumns" }, this.readOnly || (this.onKeyUp = this.onKeyUp.bind(this)), this._data = {}, this.editors = {}, this.colWrapper = void 0, this.editors.cols = [], this.data = oe, Array.isArray(this.data.cols) ? this.editors.numberOfColumns = this.data.cols.length : (this.data.cols = [], this.editors.numberOfColumns = 2);
        }
        static get isReadOnlySupported() {
          return !0;
        }
        onKeyUp(oe) {
          console.log(oe), console.log("heyup"), oe.code === "Backspace" || oe.code;
        }
        get CSS() {
          return { settingsButton: this.api.styles.settingsButton, settingsButtonActive: this.api.styles.settingsButtonActive };
        }
        renderSettings() {
          return [{ icon: "2", label: "2 Columns", onActivate: () => {
            this._updateCols(2);
          } }, { icon: "3", label: "3 Columns", onActivate: () => {
            this._updateCols(3);
          } }, { icon: "R", label: "Roll Colls", onActivate: () => {
            this._rollCols();
          } }];
        }
        _rollCols() {
          this.data.cols.unshift(this.data.cols.pop()), this.editors.cols.unshift(this.editors.cols.pop()), this._rerender();
        }
        async _updateCols(oe) {
          oe == 2 && this.editors.numberOfColumns == 3 && (await M().fire({ title: "Are you sure?", text: "This will delete Column 3!", icon: "warning", showCancelButton: !0, confirmButtonColor: "#3085d6", cancelButtonColor: "#d33", confirmButtonText: "Yes, delete it!" })).isConfirmed && (this.editors.numberOfColumns = 2, this.data.cols.pop(), this.editors.cols.pop(), this._rerender()), oe == 3 && (this.editors.numberOfColumns = 3, this._rerender());
        }
        async _rerender() {
          await this.save();
          for (let oe = 0; oe < this.editors.cols.length; oe++)
            this.editors.cols[oe].destroy();
          this.editors.cols = [], this.colWrapper.innerHTML = "";
          for (let oe = 0; oe < this.editors.numberOfColumns; oe++) {
            let fe = document.createElement("div");
            fe.classList.add("ce-editorjsColumns_col"), fe.classList.add("editorjs_col_" + oe);
            let Me = T();
            fe.id = Me, this.colWrapper.appendChild(fe);
            let je = new this.config.EditorJsLibrary({ defaultBlock: "paragraph", holder: Me, tools: this.config.tools, data: this.data.cols[oe], readOnly: this.readOnly, minHeight: 50 });
            this.editors.cols.push(je);
          }
        }
        render() {
          this.colWrapper = document.createElement("div"), this.colWrapper.classList.add("ce-editorjsColumns_wrapper"), this.colWrapper.addEventListener("paste", (oe) => {
            oe.stopPropagation();
          }, !0), this.colWrapper.addEventListener("keydown", (oe) => {
            oe.key === "Enter" && (oe.preventDefault(), oe.stopImmediatePropagation(), oe.stopPropagation()), oe.key === "Tab" && (oe.preventDefault(), oe.stopImmediatePropagation(), oe.stopPropagation());
          });
          for (let oe = 0; oe < this.editors.cols.length; oe++)
            this.editors.cols[oe].destroy();
          this.editors.cols = [];
          for (let oe = 0; oe < this.editors.numberOfColumns; oe++) {
            let fe = document.createElement("div");
            fe.classList.add("ce-editorjsColumns_col"), fe.classList.add("editorjs_col_" + oe);
            let Me = T();
            fe.id = Me, this.colWrapper.appendChild(fe);
            let je = new this.config.EditorJsLibrary({ defaultBlock: "paragraph", holder: Me, tools: this.config.tools, data: this.data.cols[oe], readOnly: this.readOnly, minHeight: 50 });
            this.editors.cols.push(je);
          }
          return this.colWrapper;
        }
        async save() {
          if (!this.readOnly)
            for (let oe = 0; oe < this.editors.cols.length; oe++) {
              let fe = await this.editors.cols[oe].save();
              this.data.cols[oe] = fe;
            }
          return this.data;
        }
        static get toolbox() {
          return { icon: Y(), title: "Columns" };
        }
      }
    })(), a.default;
  })());
})(uu);
var Ag = uu.exports;
const Rg = /* @__PURE__ */ Dn(Ag);
var pu = { exports: {} };
/*! For license information please see editorjs-paragraph-linebreakable.bundle.js.LICENSE.txt */
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(self, () => (() => {
    var t = { 674: (c, u, p) => {
      p.d(u, { Z: () => T });
      var g = p(81), v = p.n(g), k = p(645), y = p.n(k)()(v());
      y.push([c.id, `.ce-paragraphLineBreakable {
  line-height: 1.6em;
  outline: none;
  /* background-color: #0091DB; */
  /* color:#fff; */
  /* padding: 10px; */
  /* margin : 15px; */
}
/* .ce-paragraphLineBreakable::before {
  content: "DSLE:";
  color: #fff;
  font-weight: bold;
  width:100%;
  display: inline-block;
}

.ce-paragraphLineBreakable[data-placeholder]:empty::before{
  content: attr(data-placeholder);
  color: #707684;
  font-weight: normal;
  opacity: 0;
} */
`, ""]);
      const T = y;
    }, 645: (c) => {
      c.exports = function(u) {
        var p = [];
        return p.toString = function() {
          return this.map(function(g) {
            var v = "", k = g[5] !== void 0;
            return g[4] && (v += "@supports (".concat(g[4], ") {")), g[2] && (v += "@media ".concat(g[2], " {")), k && (v += "@layer".concat(g[5].length > 0 ? " ".concat(g[5]) : "", " {")), v += u(g), k && (v += "}"), g[2] && (v += "}"), g[4] && (v += "}"), v;
          }).join("");
        }, p.i = function(g, v, k, y, T) {
          typeof g == "string" && (g = [[null, g, void 0]]);
          var x = {};
          if (k)
            for (var M = 0; M < this.length; M++) {
              var L = this[M][0];
              L != null && (x[L] = !0);
            }
          for (var Y = 0; Y < g.length; Y++) {
            var I = [].concat(g[Y]);
            k && x[I[0]] || (T !== void 0 && (I[5] === void 0 || (I[1] = "@layer".concat(I[5].length > 0 ? " ".concat(I[5]) : "", " {").concat(I[1], "}")), I[5] = T), v && (I[2] && (I[1] = "@media ".concat(I[2], " {").concat(I[1], "}")), I[2] = v), y && (I[4] ? (I[1] = "@supports (".concat(I[4], ") {").concat(I[1], "}"), I[4] = y) : I[4] = "".concat(y)), p.push(I));
          }
        }, p;
      };
    }, 81: (c) => {
      c.exports = function(u) {
        return u[1];
      };
    }, 379: (c) => {
      var u = [];
      function p(k) {
        for (var y = -1, T = 0; T < u.length; T++)
          if (u[T].identifier === k) {
            y = T;
            break;
          }
        return y;
      }
      function g(k, y) {
        for (var T = {}, x = [], M = 0; M < k.length; M++) {
          var L = k[M], Y = y.base ? L[0] + y.base : L[0], I = T[Y] || 0, F = "".concat(Y, " ").concat(I);
          T[Y] = I + 1;
          var Q = p(F), U = { css: L[1], media: L[2], sourceMap: L[3], supports: L[4], layer: L[5] };
          if (Q !== -1)
            u[Q].references++, u[Q].updater(U);
          else {
            var z = v(U, y);
            y.byIndex = M, u.splice(M, 0, { identifier: F, updater: z, references: 1 });
          }
          x.push(F);
        }
        return x;
      }
      function v(k, y) {
        var T = y.domAPI(y);
        return T.update(k), function(x) {
          if (x) {
            if (x.css === k.css && x.media === k.media && x.sourceMap === k.sourceMap && x.supports === k.supports && x.layer === k.layer)
              return;
            T.update(k = x);
          } else
            T.remove();
        };
      }
      c.exports = function(k, y) {
        var T = g(k = k || [], y = y || {});
        return function(x) {
          x = x || [];
          for (var M = 0; M < T.length; M++) {
            var L = p(T[M]);
            u[L].references--;
          }
          for (var Y = g(x, y), I = 0; I < T.length; I++) {
            var F = p(T[I]);
            u[F].references === 0 && (u[F].updater(), u.splice(F, 1));
          }
          T = Y;
        };
      };
    }, 569: (c) => {
      var u = {};
      c.exports = function(p, g) {
        var v = function(k) {
          if (u[k] === void 0) {
            var y = document.querySelector(k);
            if (window.HTMLIFrameElement && y instanceof window.HTMLIFrameElement)
              try {
                y = y.contentDocument.head;
              } catch {
                y = null;
              }
            u[k] = y;
          }
          return u[k];
        }(p);
        if (!v)
          throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
        v.appendChild(g);
      };
    }, 216: (c) => {
      c.exports = function(u) {
        var p = document.createElement("style");
        return u.setAttributes(p, u.attributes), u.insert(p, u.options), p;
      };
    }, 565: (c, u, p) => {
      c.exports = function(g) {
        var v = p.nc;
        v && g.setAttribute("nonce", v);
      };
    }, 795: (c) => {
      c.exports = function(u) {
        var p = u.insertStyleElement(u);
        return { update: function(g) {
          (function(v, k, y) {
            var T = "";
            y.supports && (T += "@supports (".concat(y.supports, ") {")), y.media && (T += "@media ".concat(y.media, " {"));
            var x = y.layer !== void 0;
            x && (T += "@layer".concat(y.layer.length > 0 ? " ".concat(y.layer) : "", " {")), T += y.css, x && (T += "}"), y.media && (T += "}"), y.supports && (T += "}");
            var M = y.sourceMap;
            M && typeof btoa < "u" && (T += `
/*# sourceMappingURL=data:application/json;base64,`.concat(btoa(unescape(encodeURIComponent(JSON.stringify(M)))), " */")), k.styleTagTransform(T, v, k.options);
          })(p, u, g);
        }, remove: function() {
          (function(g) {
            if (g.parentNode === null)
              return !1;
            g.parentNode.removeChild(g);
          })(p);
        } };
      };
    }, 589: (c) => {
      c.exports = function(u, p) {
        if (p.styleSheet)
          p.styleSheet.cssText = u;
        else {
          for (; p.firstChild; )
            p.removeChild(p.firstChild);
          p.appendChild(document.createTextNode(u));
        }
      };
    }, 0: (c) => {
      c.exports = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0.2 -0.3 9 11.4" width="12" height="14"><path d="M0 2.77V.92A1 1 0 01.2.28C.35.1.56 0 .83 0h7.66c.28.01.48.1.63.28.14.17.21.38.21.64v1.85c0 .26-.08.48-.23.66-.15.17-.37.26-.66.26-.28 0-.5-.09-.64-.26a1 1 0 01-.21-.66V1.69H5.6v7.58h.5c.25 0 .45.08.6.23.17.16.25.35.25.6s-.08.45-.24.6a.87.87 0 01-.62.22H3.21a.87.87 0 01-.61-.22.78.78 0 01-.24-.6c0-.25.08-.44.24-.6a.85.85 0 01.61-.23h.5V1.7H1.73v1.08c0 .26-.08.48-.23.66-.15.17-.37.26-.66.26-.28 0-.5-.09-.64-.26A1 1 0 010 2.77z"></path></svg>';
    } }, n = {};
    function r(c) {
      var u = n[c];
      if (u !== void 0)
        return u.exports;
      var p = n[c] = { id: c, exports: {} };
      return t[c](p, p.exports, r), p.exports;
    }
    r.n = (c) => {
      var u = c && c.__esModule ? () => c.default : () => c;
      return r.d(u, { a: u }), u;
    }, r.d = (c, u) => {
      for (var p in u)
        r.o(u, p) && !r.o(c, p) && Object.defineProperty(c, p, { enumerable: !0, get: u[p] });
    }, r.o = (c, u) => Object.prototype.hasOwnProperty.call(c, u);
    var a = {};
    return (() => {
      r.d(a, { default: () => z });
      var c = r(379), u = r.n(c), p = r(795), g = r.n(p), v = r(569), k = r.n(v), y = r(565), T = r.n(y), x = r(216), M = r.n(x), L = r(589), Y = r.n(L), I = r(674), F = {};
      F.styleTagTransform = Y(), F.setAttributes = T(), F.insert = k().bind(null, "head"), F.domAPI = g(), F.insertStyleElement = M(), u()(I.Z, F), I.Z && I.Z.locals && I.Z.locals;
      var Q = r(0), U = r.n(Q);
      class z {
        static get DEFAULT_PLACEHOLDER() {
          return "Hello :)";
        }
        static get enableLineBreaks() {
          return !0;
        }
        constructor({ data: j, config: R, api: V, readOnly: K }) {
          this.api = V, this.readOnly = K, this._CSS = { block: this.api.styles.block, wrapper: "ce-paragraphLineBreakable" }, this.readOnly || (this.onKeyUp = this.onKeyUp.bind(this)), this._placeholder = R.placeholder ? R.placeholder : z.DEFAULT_PLACEHOLDER, this._data = {}, this._element = this.drawView(), this._preserveBlank = R.preserveBlank !== void 0 && R.preserveBlank, this.data = j;
        }
        onKeyUp(j) {
          if (j.code !== "Backspace" && j.code !== "Delete")
            return;
          const { textContent: R } = this._element;
          R === "" && (this._element.innerHTML = "");
        }
        drawView() {
          let j = document.createElement("DIV");
          return j.classList.add(this._CSS.wrapper, this._CSS.block), j.contentEditable = !1, j.dataset.placeholder = this.api.i18n.t(this._placeholder), this.readOnly || (j.contentEditable = !0, j.addEventListener("keyup", this.onKeyUp)), j;
        }
        render() {
          return this._element;
        }
        merge(j) {
          let R = { text: this.data.text + j.text };
          this.data = R;
        }
        validate(j) {
          return !(j.text.trim() === "" && !this._preserveBlank);
        }
        save(j) {
          return { text: j.innerHTML };
        }
        onPaste(j) {
          const R = { text: j.detail.data.innerHTML };
          this.data = R;
        }
        static get conversionConfig() {
          return { export: "text", import: "text" };
        }
        static get sanitize() {
          return { text: { br: !0, div: !0 } };
        }
        static get isReadOnlySupported() {
          return !0;
        }
        get data() {
          let j = this._element.innerHTML;
          return this._data.text = j, this._data;
        }
        set data(j) {
          this._data = j || {}, this._element.innerHTML = this._data.text || "";
        }
        static get pasteConfig() {
          return { tags: ["P"] };
        }
        static get toolbox() {
          return { icon: U(), title: "Paragraph" };
        }
      }
    })(), a.default;
  })());
})(pu);
var Ig = pu.exports;
const Ng = /* @__PURE__ */ Dn(Ig);
var Pg = { exports: {} };
(function(o, e) {
  (function(t, n) {
    o.exports = n();
  })(window, function() {
    return function(t) {
      var n = {};
      function r(a) {
        if (n[a])
          return n[a].exports;
        var c = n[a] = { i: a, l: !1, exports: {} };
        return t[a].call(c.exports, c, c.exports, r), c.l = !0, c.exports;
      }
      return r.m = t, r.c = n, r.d = function(a, c, u) {
        r.o(a, c) || Object.defineProperty(a, c, { enumerable: !0, get: u });
      }, r.r = function(a) {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
      }, r.t = function(a, c) {
        if (1 & c && (a = r(a)), 8 & c || 4 & c && typeof a == "object" && a && a.__esModule)
          return a;
        var u = /* @__PURE__ */ Object.create(null);
        if (r.r(u), Object.defineProperty(u, "default", { enumerable: !0, value: a }), 2 & c && typeof a != "string")
          for (var p in a)
            r.d(u, p, (function(g) {
              return a[g];
            }).bind(null, p));
        return u;
      }, r.n = function(a) {
        var c = a && a.__esModule ? function() {
          return a.default;
        } : function() {
          return a;
        };
        return r.d(c, "a", c), c;
      }, r.o = function(a, c) {
        return Object.prototype.hasOwnProperty.call(a, c);
      }, r.p = "/", r(r.s = 5);
    }([function(t, n, r) {
      var a = r(1);
      typeof a == "string" && (a = [[t.i, a, ""]]);
      var c = { hmr: !0, transform: void 0, insertInto: void 0 };
      r(3)(a, c), a.locals && (t.exports = a.locals);
    }, function(t, n, r) {
      (t.exports = r(2)(!1)).push([t.i, `.change-case-action {
    z-index: 90;
    user-select: none;
    -moz-user-select: none;
    -khtml-user-select: none;
    -webkit-user-select: none;
    -o-user-select: none;    
}

.change-case-tool {
    display: -webkit-box;
    display: -ms-flexbox;
    display: flex;
    padding: 5px 10px;
    font-size: 14px;
    line-height: 20px;
    font-weight: 500;
    cursor: pointer;
    -webkit-box-align: center;
    -ms-flex-align: center;
    align-items: center;
}

.change-case-tool:hover {
    background: #eff2f5;
}

.change-case-toolbar__label {
    color: #707684;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: .33px;
    padding: 10px 10px 5px;
    text-transform: uppercase;
    cursor: pointer;
}`, ""]);
    }, function(t, n) {
      t.exports = function(r) {
        var a = [];
        return a.toString = function() {
          return this.map(function(c) {
            var u = function(p, g) {
              var v = p[1] || "", k = p[3];
              if (!k)
                return v;
              if (g && typeof btoa == "function") {
                var y = (x = k, "/*# sourceMappingURL=data:application/json;charset=utf-8;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(x)))) + " */"), T = k.sources.map(function(M) {
                  return "/*# sourceURL=" + k.sourceRoot + M + " */";
                });
                return [v].concat(T).concat([y]).join(`
`);
              }
              var x;
              return [v].join(`
`);
            }(c, r);
            return c[2] ? "@media " + c[2] + "{" + u + "}" : u;
          }).join("");
        }, a.i = function(c, u) {
          typeof c == "string" && (c = [[null, c, ""]]);
          for (var p = {}, g = 0; g < this.length; g++) {
            var v = this[g][0];
            typeof v == "number" && (p[v] = !0);
          }
          for (g = 0; g < c.length; g++) {
            var k = c[g];
            typeof k[0] == "number" && p[k[0]] || (u && !k[2] ? k[2] = u : u && (k[2] = "(" + k[2] + ") and (" + u + ")"), a.push(k));
          }
        }, a;
      };
    }, function(t, n, r) {
      var a, c, u = {}, p = (a = function() {
        return window && document && document.all && !window.atob;
      }, function() {
        return c === void 0 && (c = a.apply(this, arguments)), c;
      }), g = function(j) {
        var R = {};
        return function(V) {
          if (typeof V == "function")
            return V();
          if (R[V] === void 0) {
            var K = (function(J) {
              return document.querySelector(J);
            }).call(this, V);
            if (window.HTMLIFrameElement && K instanceof window.HTMLIFrameElement)
              try {
                K = K.contentDocument.head;
              } catch {
                K = null;
              }
            R[V] = K;
          }
          return R[V];
        };
      }(), v = null, k = 0, y = [], T = r(4);
      function x(j, R) {
        for (var V = 0; V < j.length; V++) {
          var K = j[V], J = u[K.id];
          if (J) {
            J.refs++;
            for (var A = 0; A < J.parts.length; A++)
              J.parts[A](K.parts[A]);
            for (; A < K.parts.length; A++)
              J.parts.push(Q(K.parts[A], R));
          } else {
            var se = [];
            for (A = 0; A < K.parts.length; A++)
              se.push(Q(K.parts[A], R));
            u[K.id] = { id: K.id, refs: 1, parts: se };
          }
        }
      }
      function M(j, R) {
        for (var V = [], K = {}, J = 0; J < j.length; J++) {
          var A = j[J], se = R.base ? A[0] + R.base : A[0], Z = { css: A[1], media: A[2], sourceMap: A[3] };
          K[se] ? K[se].parts.push(Z) : V.push(K[se] = { id: se, parts: [Z] });
        }
        return V;
      }
      function L(j, R) {
        var V = g(j.insertInto);
        if (!V)
          throw new Error("Couldn't find a style target. This probably means that the value for the 'insertInto' parameter is invalid.");
        var K = y[y.length - 1];
        if (j.insertAt === "top")
          K ? K.nextSibling ? V.insertBefore(R, K.nextSibling) : V.appendChild(R) : V.insertBefore(R, V.firstChild), y.push(R);
        else if (j.insertAt === "bottom")
          V.appendChild(R);
        else {
          if (typeof j.insertAt != "object" || !j.insertAt.before)
            throw new Error(`[Style Loader]

 Invalid value for parameter 'insertAt' ('options.insertAt') found.
 Must be 'top', 'bottom', or Object.
 (https://github.com/webpack-contrib/style-loader#insertat)
`);
          var J = g(j.insertInto + " " + j.insertAt.before);
          V.insertBefore(R, J);
        }
      }
      function Y(j) {
        if (j.parentNode === null)
          return !1;
        j.parentNode.removeChild(j);
        var R = y.indexOf(j);
        R >= 0 && y.splice(R, 1);
      }
      function I(j) {
        var R = document.createElement("style");
        return j.attrs.type === void 0 && (j.attrs.type = "text/css"), F(R, j.attrs), L(j, R), R;
      }
      function F(j, R) {
        Object.keys(R).forEach(function(V) {
          j.setAttribute(V, R[V]);
        });
      }
      function Q(j, R) {
        var V, K, J, A;
        if (R.transform && j.css) {
          if (!(A = R.transform(j.css)))
            return function() {
            };
          j.css = A;
        }
        if (R.singleton) {
          var se = k++;
          V = v || (v = I(R)), K = d.bind(null, V, se, !1), J = d.bind(null, V, se, !0);
        } else
          j.sourceMap && typeof URL == "function" && typeof URL.createObjectURL == "function" && typeof URL.revokeObjectURL == "function" && typeof Blob == "function" && typeof btoa == "function" ? (V = function(Z) {
            var ne = document.createElement("link");
            return Z.attrs.type === void 0 && (Z.attrs.type = "text/css"), Z.attrs.rel = "stylesheet", F(ne, Z.attrs), L(Z, ne), ne;
          }(R), K = (function(Z, ne, le) {
            var oe = le.css, fe = le.sourceMap, Me = ne.convertToAbsoluteUrls === void 0 && fe;
            (ne.convertToAbsoluteUrls || Me) && (oe = T(oe)), fe && (oe += `
/*# sourceMappingURL=data:application/json;base64,` + btoa(unescape(encodeURIComponent(JSON.stringify(fe)))) + " */");
            var je = new Blob([oe], { type: "text/css" }), Ae = Z.href;
            Z.href = URL.createObjectURL(je), Ae && URL.revokeObjectURL(Ae);
          }).bind(null, V, R), J = function() {
            Y(V), V.href && URL.revokeObjectURL(V.href);
          }) : (V = I(R), K = (function(Z, ne) {
            var le = ne.css, oe = ne.media;
            if (oe && Z.setAttribute("media", oe), Z.styleSheet)
              Z.styleSheet.cssText = le;
            else {
              for (; Z.firstChild; )
                Z.removeChild(Z.firstChild);
              Z.appendChild(document.createTextNode(le));
            }
          }).bind(null, V), J = function() {
            Y(V);
          });
        return K(j), function(Z) {
          if (Z) {
            if (Z.css === j.css && Z.media === j.media && Z.sourceMap === j.sourceMap)
              return;
            K(j = Z);
          } else
            J();
        };
      }
      t.exports = function(j, R) {
        if (typeof DEBUG < "u" && DEBUG && typeof document != "object")
          throw new Error("The style-loader cannot be used in a non-browser environment");
        (R = R || {}).attrs = typeof R.attrs == "object" ? R.attrs : {}, R.singleton || typeof R.singleton == "boolean" || (R.singleton = p()), R.insertInto || (R.insertInto = "head"), R.insertAt || (R.insertAt = "bottom");
        var V = M(j, R);
        return x(V, R), function(K) {
          for (var J = [], A = 0; A < V.length; A++) {
            var se = V[A];
            (Z = u[se.id]).refs--, J.push(Z);
          }
          for (K && x(M(K, R), R), A = 0; A < J.length; A++) {
            var Z;
            if ((Z = J[A]).refs === 0) {
              for (var ne = 0; ne < Z.parts.length; ne++)
                Z.parts[ne]();
              delete u[Z.id];
            }
          }
        };
      };
      var U, z = (U = [], function(j, R) {
        return U[j] = R, U.filter(Boolean).join(`
`);
      });
      function d(j, R, V, K) {
        var J = V ? "" : K.css;
        if (j.styleSheet)
          j.styleSheet.cssText = z(R, J);
        else {
          var A = document.createTextNode(J), se = j.childNodes;
          se[R] && j.removeChild(se[R]), se.length ? j.insertBefore(A, se[R]) : j.appendChild(A);
        }
      }
    }, function(t, n) {
      t.exports = function(r) {
        var a = typeof window < "u" && window.location;
        if (!a)
          throw new Error("fixUrls requires window.location");
        if (!r || typeof r != "string")
          return r;
        var c = a.protocol + "//" + a.host, u = c + a.pathname.replace(/\/[^\/]*$/, "/");
        return r.replace(/url\s*\(((?:[^)(]|\((?:[^)(]+|\([^)(]*\))*\))*)\)/gi, function(p, g) {
          var v, k = g.trim().replace(/^"(.*)"$/, function(y, T) {
            return T;
          }).replace(/^'(.*)'$/, function(y, T) {
            return T;
          });
          return /^(#|data:|http:\/\/|https:\/\/|file:\/\/\/|\s*$)/i.test(k) ? p : (v = k.indexOf("//") === 0 ? k : k.indexOf("/") === 0 ? c + k : u + k.replace(/^\.\//, ""), "url(" + JSON.stringify(v) + ")");
        });
      };
    }, function(t, n, r) {
      r.r(n), r(0);
      function a(p, g) {
        for (var v = 0; v < g.length; v++) {
          var k = g[v];
          k.enumerable = k.enumerable || !1, k.configurable = !0, "value" in k && (k.writable = !0), Object.defineProperty(p, k.key, k);
        }
      }
      function c(p, g, v) {
        return g && a(p.prototype, g), v && a(p, v), p;
      }
      r.d(n, "default", function() {
        return u;
      });
      var u = function() {
        function p(g) {
          var v = g.config, k = g.api;
          (function(y, T) {
            if (!(y instanceof T))
              throw new TypeError("Cannot call a class as a function");
          })(this, p), this.api = k, this.button = null, this.optionButtons = [], this._state = !0, this.selectedText = null, this.range = null, this._settings = v, this.CSS = { actions: "change-case-action", toolbarLabel: "change-case-toolbar__label", tool: "change-case-tool", toolbarBtnActive: this.api.styles.settingsButtonActive, inlineButton: this.api.styles.inlineToolButton }, this.caseOptions = { titleCase: "Title Case", lowerCase: "lower case", upperCase: "UPPER CASE", localeLowerCase: "localé lower casé", localeUpperCase: "LöCALE UPPER CASE", sentenceCase: "Sentence case", toggleCase: "tOOGLE cASE" };
        }
        return c(p, [{ key: "state", get: function() {
          return this._state;
        } }], [{ key: "isInline", get: function() {
          return !0;
        } }]), c(p, [{ key: "render", value: function() {
          return this.button = document.createElement("button"), this.button.type = "button", this.button.innerHTML = `
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 100 100" stroke="currentColor">
            <path d="m46.099 74.271-3.6868-13.686h-18.523l-3.6868 13.686h-11.605l17.934-57.653h13.17l18.001 57.653zm-6.2632-23.813q-5.0972-18.612-5.7524-21.056c-0.42199-1.6168-0.73293-2.9077-0.91061-3.8477q-1.1105 5.0133-6.5741 24.828z" stroke-width="1.1797"/>
            <path d="m87.355 74.271-2.4938-8.4556h-12.529l-2.4938 8.4556h-7.8496l12.131-35.619h8.9087l12.176 35.619zm-4.2365-14.712q-3.4478-11.499-3.891-13.009c-0.28544-0.99887-0.49576-1.7964-0.61595-2.3772q-0.75116 3.0973-4.4469 15.339z" stroke-width=".76265"/>
            </svg>`, this.button.classList.add(this.CSS.inlineButton), this.button;
        } }, { key: "checkState", value: function(g) {
          g.anchorNode;
        } }, { key: "convertCase", value: function(g, v) {
          var k = this;
          if (g) {
            var y = g.cloneContents();
            y && (y.childNodes.forEach(function(T) {
              if (T.nodeName === "#text")
                switch (v) {
                  case "titleCase":
                    T.textContent = T.textContent.replace(/\w\S*/g, function(x) {
                      return x.charAt(0).toUpperCase() + x.slice(1, x.length).toLowerCase();
                    });
                    break;
                  case "lowerCase":
                    T.textContent = function(x) {
                      return x.toLowerCase();
                    }(T.textContent);
                    break;
                  case "upperCase":
                    T.textContent = function(x) {
                      return x.toUpperCase();
                    }(T.textContent);
                    break;
                  case "localeLowerCase":
                    T.textContent = function(x, M) {
                      return x.toLocaleLowerCase(M);
                    }(T.textContent, k._settings.locale);
                    break;
                  case "localeUpperCase":
                    T.textContent = function(x, M) {
                      return x.toLocaleUpperCase(M);
                    }(T.textContent, k._settings.locale);
                    break;
                  case "sentenceCase":
                    T.textContent = function(x) {
                      return x.charAt(0).toUpperCase() + x.slice(1, x.length).toLowerCase();
                    }(T.textContent);
                    break;
                  case "toggleCase":
                    T.textContent = function(x) {
                      return x.replace(/\w\S*/g, function(M) {
                        return M.charAt(0).toLowerCase() + M.slice(1, M.length).toUpperCase();
                      });
                    }(T.textContent);
                }
            }), g.extractContents(), g.insertNode(y), this.api.inlineToolbar.close());
          }
        } }, { key: "surround", value: function(g) {
          this.selectedText = g.cloneContents(), this.actions.hidden = !this.actions.hidden, this.range = this.actions.hidden ? null : g, this.state = !this.actions.hidden;
        } }, { key: "renderActions", value: function() {
          var g = this;
          this.actions = document.createElement("div"), this.actions.classList.add(this.CSS.actions);
          var v = document.createElement("div");
          v.classList.add(this.CSS.toolbarLabel), v.innerHTML = "Change Case", this.actions.appendChild(v), this.optionButtons = Object.keys(this.caseOptions).map(function(Y) {
            var I = document.createElement("div");
            return I.classList.add(g.CSS.tool), I.dataset.mode = Y, I.innerHTML = g.caseOptions[Y], I;
          });
          var k = !0, y = !1, T = void 0;
          try {
            for (var x, M = function() {
              var Y = x.value;
              g.actions.appendChild(Y), g.api.listeners.on(Y, "click", function() {
                g.convertCase(g.range, Y.dataset.mode);
              });
            }, L = this.optionButtons[Symbol.iterator](); !(k = (x = L.next()).done); k = !0)
              M();
          } catch (Y) {
            y = !0, T = Y;
          } finally {
            try {
              k || L.return == null || L.return();
            } finally {
              if (y)
                throw T;
            }
          }
          return this.actions.hidden = !0, this.actions;
        } }, { key: "destroy", value: function() {
          var g = !0, v = !1, k = void 0;
          try {
            for (var y, T = this.optionButtons[Symbol.iterator](); !(g = (y = T.next()).done); g = !0) {
              var x = y.value;
              this.api.listeners.off(x, "click");
            }
          } catch (M) {
            v = !0, k = M;
          } finally {
            try {
              g || T.return == null || T.return();
            } finally {
              if (v)
                throw k;
            }
          }
        } }, { key: "state", set: function(g) {
          this._state = g, this.button.classList.toggle(this.CSS.toolbarBtnActive, g);
        } }, { key: "title", get: function() {
          return "Change Case";
        } }]), p;
      }();
    }]);
  });
})(Pg);
(function() {
  try {
    if (typeof document < "u") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode('.cdx-nested-list{margin:0;padding:0;outline:none;counter-reset:item;list-style:none}.cdx-nested-list__item{line-height:1.6em;display:flex;margin:2px 0}.cdx-nested-list__item [contenteditable]{outline:none}.cdx-nested-list__item-body{flex-grow:2}.cdx-nested-list__item-content,.cdx-nested-list__item-children{flex-basis:100%}.cdx-nested-list__item-content{word-break:break-word;white-space:pre-wrap}.cdx-nested-list__item:before{counter-increment:item;margin-right:5px;white-space:nowrap}.cdx-nested-list--ordered>.cdx-nested-list__item:before{content:counters(item,".") ". "}.cdx-nested-list--unordered>.cdx-nested-list__item:before{content:"•"}.cdx-nested-list__settings{display:flex}.cdx-nested-list__settings .cdx-settings-button{width:50%}')), document.head.appendChild(o);
    }
  } catch (e) {
    console.error("vite-plugin-css-injected-by-js", e);
  }
})();
function rs(o, e = null, t = {}) {
  const n = document.createElement(o);
  Array.isArray(e) ? n.classList.add(...e) : e && n.classList.add(e);
  for (const r in t)
    n[r] = t[r];
  return n;
}
function Od(o) {
  const e = rs("div");
  return e.appendChild(o), e.innerHTML;
}
function fu(o) {
  let e;
  return o.nodeType !== Node.ELEMENT_NODE ? e = o.textContent : (e = o.innerHTML, e = e.replaceAll("<br>", "")), e.trim().length === 0;
}
let Vr = class Ul {
  /**
   * Store internal properties
   */
  constructor() {
    this.savedFakeCaret = void 0;
  }
  /**
   * Saves caret position using hidden <span>
   *
   * @returns {void}
   */
  save() {
    const e = Ul.range, t = rs("span");
    t.hidden = !0, e.insertNode(t), this.savedFakeCaret = t;
  }
  /**
   * Restores the caret position saved by the save() method
   *
   * @returns {void}
   */
  restore() {
    if (!this.savedFakeCaret)
      return;
    const e = window.getSelection(), t = new Range();
    t.setStartAfter(this.savedFakeCaret), t.setEndAfter(this.savedFakeCaret), e.removeAllRanges(), e.addRange(t), setTimeout(() => {
      this.savedFakeCaret.remove();
    }, 150);
  }
  /**
   * Returns the first range
   *
   * @returns {Range|null}
   */
  static get range() {
    const e = window.getSelection();
    return e && e.rangeCount ? e.getRangeAt(0) : null;
  }
  /**
   * Extract content fragment from Caret position to the end of contenteditable element
   *
   * @returns {DocumentFragment|void}
   */
  static extractFragmentFromCaretPositionTillTheEnd() {
    const e = window.getSelection();
    if (!e.rangeCount)
      return;
    const t = e.getRangeAt(0);
    let n = t.startContainer;
    n.nodeType !== Node.ELEMENT_NODE && (n = n.parentNode);
    const r = n.closest("[contenteditable]");
    t.deleteContents();
    const a = t.cloneRange();
    return a.selectNodeContents(r), a.setStart(t.endContainer, t.endOffset), a.extractContents();
  }
  /**
   * Set focus to contenteditable or native input element
   *
   * @param {HTMLElement} element - element where to set focus
   * @param {boolean} atStart - where to set focus: at the start or at the end
   * @returns {void}
   */
  static focus(e, t = !0) {
    const n = document.createRange(), r = window.getSelection();
    n.selectNodeContents(e), n.collapse(t), r.removeAllRanges(), r.addRange(n);
  }
  /**
   * Check if the caret placed at the start of the contenteditable element
   *
   * @returns {void}
   */
  static isAtStart() {
    const e = window.getSelection();
    if (e.focusOffset > 0)
      return !1;
    const t = e.focusNode;
    return Ul.getHigherLevelSiblings(t, "left").every((n) => fu(n));
  }
  /**
   * Get all first-level (first child of [contenteditabel]) siblings from passed node
   * Then you can check it for emptiness
   *
   * @example
   * <div contenteditable>
   * <p></p>                            |
   * <p></p>                            | left first-level siblings
   * <p></p>                            |
   * <blockquote><a><b>adaddad</b><a><blockquote>       <-- passed node for example <b>
   * <p></p>                            |
   * <p></p>                            | right first-level siblings
   * <p></p>                            |
   * </div>
   * @param {HTMLElement} from - element from which siblings should be searched
   * @param {'left' | 'right'} direction - direction of search
   * @returns {HTMLElement[]}
   */
  static getHigherLevelSiblings(e, t = "left") {
    let n = e;
    const r = [];
    for (; n.parentNode && n.parentNode.contentEditable !== "true"; )
      n = n.parentNode;
    const a = t === "left" ? "previousSibling" : "nextSibling";
    for (; n[a]; )
      n = n[a], r.push(n);
    return r;
  }
};
const Hg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><line x1="9" x2="19" y1="7" y2="7" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="9" x2="19" y1="12" y2="12" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="9" x2="19" y1="17" y2="17" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M5.00001 17H4.99002"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M5.00001 12H4.99002"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M5.00001 7H4.99002"/></svg>', Bd = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><line x1="12" x2="19" y1="7" y2="7" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="12" x2="19" y1="12" y2="12" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="12" x2="19" y1="17" y2="17" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M7.79999 14L7.79999 7.2135C7.79999 7.12872 7.7011 7.0824 7.63597 7.13668L4.79999 9.5"/></svg>';
let jg = class ql {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to use native Enter behaviour
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: Bd,
      title: "List"
    };
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {object} params - tool constructor options
   * @param {ListData} params.data - previously saved data
   * @param {object} params.config - user config for Tool
   * @param {object} params.api - Editor.js API
   * @param {boolean} params.readOnly - read-only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r }) {
    this.nodes = {
      wrapper: null
    }, this.api = n, this.readOnly = r, this.config = t, this.defaultListStyle = this.config.defaultStyle === "ordered" ? "ordered" : "unordered";
    const a = {
      style: this.defaultListStyle,
      items: []
    };
    this.data = e && Object.keys(e).length ? e : a, this.caret = new Vr();
  }
  /**
   * Returns list tag with items
   *
   * @returns {Element}
   * @public
   */
  render() {
    return this.nodes.wrapper = this.makeListWrapper(this.data.style, [this.CSS.baseBlock]), this.data.items.length ? this.appendItems(this.data.items, this.nodes.wrapper) : this.appendItems([{
      content: "",
      items: []
    }], this.nodes.wrapper), this.readOnly || this.nodes.wrapper.addEventListener("keydown", (e) => {
      switch (e.key) {
        case "Enter":
          this.enterPressed(e);
          break;
        case "Backspace":
          this.backspace(e);
          break;
        case "Tab":
          e.shiftKey ? this.shiftTab(e) : this.addTab(e);
          break;
      }
    }, !1), this.nodes.wrapper;
  }
  /**
   * Creates Block Tune allowing to change the list style
   *
   * @public
   * @returns {Array}
   */
  renderSettings() {
    return [
      {
        name: "unordered",
        label: this.api.i18n.t("Unordered"),
        icon: Hg
      },
      {
        name: "ordered",
        label: this.api.i18n.t("Ordered"),
        icon: Bd
      }
    ].map((e) => ({
      name: e.name,
      icon: e.icon,
      label: e.label,
      isActive: this.data.style === e.name,
      closeOnActivate: !0,
      onActivate: () => {
        this.listStyle = e.name;
      }
    }));
  }
  /**
   * On paste sanitzation config. Allow only tags that are allowed in the Tool.
   *
   * @returns {PasteConfig} - paste config.
   */
  static get pasteConfig() {
    return {
      tags: ["OL", "UL", "LI"]
    };
  }
  /**
   * On paste callback that is fired from Editor.
   *
   * @param {PasteEvent} event - event with pasted data
   */
  onPaste(e) {
    const t = e.detail.data;
    this.data = this.pasteHandler(t);
    const n = this.nodes.wrapper;
    n && n.parentNode.replaceChild(this.render(), n);
  }
  /**
   * Handle UL, OL and LI tags paste and returns List data
   *
   * @param {HTMLUListElement|HTMLOListElement|HTMLLIElement} element
   * @returns {ListData}
   */
  pasteHandler(e) {
    const { tagName: t } = e;
    let n, r;
    switch (t) {
      case "OL":
        n = "ordered", r = "ol";
        break;
      case "UL":
      case "LI":
        n = "unordered", r = "ul";
    }
    const a = {
      style: n,
      items: []
    }, c = (u) => Array.from(u.querySelectorAll(":scope > li")).map((p) => {
      var g;
      const v = p.querySelector(`:scope > ${r}`), k = v ? c(v) : [];
      return {
        content: ((g = p == null ? void 0 : p.firstChild) == null ? void 0 : g.textContent) || "",
        items: k
      };
    });
    return a.items = c(e), a;
  }
  /**
   * Renders children list
   *
   * @param {ListItem[]} items - items data to append
   * @param {Element} parentItem - where to append
   * @returns {void}
   */
  appendItems(e, t) {
    e.forEach((n) => {
      const r = this.createItem(n.content, n.items);
      t.appendChild(r);
    });
  }
  /**
   * Renders the single item
   *
   * @param {string} content - item content to render
   * @param {ListItem[]} [items] - children
   * @returns {Element}
   */
  createItem(e, t = []) {
    const n = rs("li", this.CSS.item), r = rs("div", this.CSS.itemBody), a = rs("div", this.CSS.itemContent, {
      innerHTML: e,
      contentEditable: !this.readOnly
    });
    return r.appendChild(a), n.appendChild(r), t && t.length > 0 && this.addChildrenList(n, t), n;
  }
  /**
   * Extracts tool's data from the DOM
   *
   * @returns {ListData}
   */
  save() {
    const e = (t) => Array.from(t.querySelectorAll(`:scope > .${this.CSS.item}`)).map((n) => {
      const r = n.querySelector(`.${this.CSS.itemChildren}`), a = this.getItemContent(n), c = r ? e(r) : [];
      return {
        content: a,
        items: c
      };
    });
    return {
      style: this.data.style,
      items: e(this.nodes.wrapper)
    };
  }
  /**
   * Append children list to passed item
   *
   * @param {Element} parentItem - item that should contain passed sub-items
   * @param {ListItem[]} items - sub items to append
   */
  addChildrenList(e, t) {
    const n = e.querySelector(`.${this.CSS.itemBody}`), r = this.makeListWrapper(void 0, [this.CSS.itemChildren]);
    this.appendItems(t, r), n.appendChild(r);
  }
  /**
   * Creates main <ul> or <ol> tag depended on style
   *
   * @param {string} [style] - 'ordered' or 'unordered'
   * @param {string[]} [classes] - additional classes to append
   * @returns {HTMLOListElement|HTMLUListElement}
   */
  makeListWrapper(e = this.listStyle, t = []) {
    const n = e === "ordered" ? "ol" : "ul", r = e === "ordered" ? this.CSS.wrapperOrdered : this.CSS.wrapperUnordered;
    return t.push(r), rs(n, [this.CSS.wrapper, ...t]);
  }
  /**
   * Styles
   *
   * @returns {object} - CSS classes names by keys
   * @private
   */
  get CSS() {
    return {
      baseBlock: this.api.styles.block,
      wrapper: "cdx-nested-list",
      wrapperOrdered: "cdx-nested-list--ordered",
      wrapperUnordered: "cdx-nested-list--unordered",
      item: "cdx-nested-list__item",
      itemBody: "cdx-nested-list__item-body",
      itemContent: "cdx-nested-list__item-content",
      itemChildren: "cdx-nested-list__item-children",
      settingsWrapper: "cdx-nested-list__settings",
      settingsButton: this.api.styles.settingsButton,
      settingsButtonActive: this.api.styles.settingsButtonActive
    };
  }
  /**
   * Get list style name
   *
   * @returns {string}
   */
  get listStyle() {
    return this.data.style || this.defaultListStyle;
  }
  /**
   * Set list style
   *
   * @param {string} style - new style to set
   */
  set listStyle(e) {
    const t = Array.from(this.nodes.wrapper.querySelectorAll(`.${this.CSS.wrapper}`));
    t.push(this.nodes.wrapper), t.forEach((n) => {
      n.classList.toggle(this.CSS.wrapperUnordered, e === "unordered"), n.classList.toggle(this.CSS.wrapperOrdered, e === "ordered");
    }), this.data.style = e;
  }
  /**
   * Returns current List item by the caret position
   *
   * @returns {Element}
   */
  get currentItem() {
    let e = window.getSelection().anchorNode;
    return e.nodeType !== Node.ELEMENT_NODE && (e = e.parentNode), e.closest(`.${this.CSS.item}`);
  }
  /**
   * Handles Enter keypress
   *
   * @param {KeyboardEvent} event - keydown
   * @returns {void}
   */
  enterPressed(e) {
    const t = this.currentItem;
    if (e.stopPropagation(), e.preventDefault(), e.isComposing)
      return;
    const n = this.getItemContent(t).trim().length === 0, r = t.parentNode === this.nodes.wrapper, a = t.nextElementSibling === null;
    if (r && a && n) {
      this.getOutOfList();
      return;
    } else if (a && n) {
      this.unshiftItem();
      return;
    }
    const c = Vr.extractFragmentFromCaretPositionTillTheEnd(), u = Od(c), p = t.querySelector(`.${this.CSS.itemChildren}`), g = this.createItem(u, void 0);
    p && Array.from(p.querySelectorAll(`.${this.CSS.item}`)).length > 0 ? p.prepend(g) : t.after(g), this.focusItem(g);
  }
  /**
   * Decrease indentation of the current item
   *
   * @returns {void}
   */
  unshiftItem() {
    const e = this.currentItem, t = e.parentNode.closest(`.${this.CSS.item}`);
    if (!t)
      return;
    this.caret.save(), t.after(e), this.caret.restore();
    const n = t.querySelector(`.${this.CSS.itemChildren}`);
    n.children.length === 0 && n.remove();
  }
  /**
   * Return the item content
   *
   * @param {Element} item - item wrapper (<li>)
   * @returns {string}
   */
  getItemContent(e) {
    const t = e.querySelector(`.${this.CSS.itemContent}`);
    return fu(t) ? "" : t.innerHTML;
  }
  /**
   * Sets focus to the item's content
   *
   * @param {Element} item - item (<li>) to select
   * @param {boolean} atStart - where to set focus: at the start or at the end
   * @returns {void}
   */
  focusItem(e, t = !0) {
    const n = e.querySelector(`.${this.CSS.itemContent}`);
    Vr.focus(n, t);
  }
  /**
   * Get out from List Tool by Enter on the empty last item
   *
   * @returns {void}
   */
  getOutOfList() {
    this.currentItem.remove(), this.api.blocks.insert(), this.api.caret.setToBlock(this.api.blocks.getCurrentBlockIndex());
  }
  /**
   * Handle backspace
   *
   * @param {KeyboardEvent} event - keydown
   */
  backspace(e) {
    if (!Vr.isAtStart())
      return;
    e.preventDefault();
    const t = this.currentItem, n = t.previousSibling, r = t.parentNode.closest(`.${this.CSS.item}`);
    if (!n && !r)
      return;
    e.stopPropagation();
    let a;
    if (n) {
      const v = n.querySelectorAll(`.${this.CSS.item}`);
      a = Array.from(v).pop() || n;
    } else
      a = r;
    const c = Vr.extractFragmentFromCaretPositionTillTheEnd(), u = Od(c), p = a.querySelector(`.${this.CSS.itemContent}`);
    Vr.focus(p, !1), this.caret.save(), p.insertAdjacentHTML("beforeend", u);
    let g = t.querySelectorAll(`.${this.CSS.itemChildren} > .${this.CSS.item}`);
    g = Array.from(g), g = g.filter((v) => v.parentNode.closest(`.${this.CSS.item}`) === t), g.reverse().forEach((v) => {
      n ? a.after(v) : t.after(v);
    }), t.remove(), this.caret.restore();
  }
  /**
   * Add indentation to current item
   *
   * @param {KeyboardEvent} event - keydown
   */
  addTab(e) {
    e.stopPropagation(), e.preventDefault();
    const t = this.currentItem, n = t.previousSibling;
    if (!n)
      return;
    const r = n.querySelector(`.${this.CSS.itemChildren}`);
    if (this.caret.save(), r)
      r.appendChild(t);
    else {
      const a = this.makeListWrapper(void 0, [this.CSS.itemChildren]), c = n.querySelector(`.${this.CSS.itemBody}`);
      a.appendChild(t), c.appendChild(a);
    }
    this.caret.restore();
  }
  /**
   * Reduce indentation for current item
   *
   * @param {KeyboardEvent} event - keydown
   * @returns {void}
   */
  shiftTab(e) {
    e.stopPropagation(), e.preventDefault(), this.unshiftItem();
  }
  /**
   * Convert from list to text for conversionConfig
   *
   * @param {ListData} data
   * @returns {string}
   */
  static joinRecursive(e) {
    return e.items.map((t) => `${t.content} ${ql.joinRecursive(t)}`).join("");
  }
  /**
   * Convert from text to list with import and export list to text
   */
  static get conversionConfig() {
    return {
      export: (e) => ql.joinRecursive(e),
      import: (e) => ({
        items: [{
          content: e,
          items: []
        }],
        style: "unordered"
      })
    };
  }
};
(function() {
  try {
    if (typeof document < "u") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode('.cdx-checklist{gap:6px;display:flex;flex-direction:column}.cdx-checklist__item{display:flex;box-sizing:content-box;align-items:flex-start}.cdx-checklist__item-text{outline:none;flex-grow:1;line-height:1.57em}.cdx-checklist__item-checkbox{width:22px;height:22px;display:flex;align-items:center;margin-right:8px;margin-top:calc(.785em - 11px);cursor:pointer}.cdx-checklist__item-checkbox svg{opacity:0;height:20px;width:20px;position:absolute;left:-1px;top:-1px;max-height:20px}@media (hover: hover){.cdx-checklist__item-checkbox:not(.cdx-checklist__item-checkbox--no-hover):hover .cdx-checklist__item-checkbox-check svg{opacity:1}}.cdx-checklist__item-checkbox-check{cursor:pointer;display:inline-block;flex-shrink:0;position:relative;width:20px;height:20px;box-sizing:border-box;margin-left:0;border-radius:5px;border:1px solid #C9C9C9;background:#fff}.cdx-checklist__item-checkbox-check:before{content:"";position:absolute;top:0;right:0;bottom:0;left:0;border-radius:100%;background-color:#369fff;visibility:hidden;pointer-events:none;transform:scale(1);transition:transform .4s ease-out,opacity .4s}@media (hover: hover){.cdx-checklist__item--checked .cdx-checklist__item-checkbox:not(.cdx-checklist__item--checked .cdx-checklist__item-checkbox--no-hover):hover .cdx-checklist__item-checkbox-check{background:#0059AB;border-color:#0059ab}}.cdx-checklist__item--checked .cdx-checklist__item-checkbox-check{background:#369FFF;border-color:#369fff}.cdx-checklist__item--checked .cdx-checklist__item-checkbox-check svg{opacity:1}.cdx-checklist__item--checked .cdx-checklist__item-checkbox-check svg path{stroke:#fff}.cdx-checklist__item--checked .cdx-checklist__item-checkbox-check:before{opacity:0;visibility:visible;transform:scale(2.5)}')), document.head.appendChild(o);
    }
  } catch (e) {
    console.error("vite-plugin-css-injected-by-js", e);
  }
})();
const $g = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M7 12L10.4884 15.8372C10.5677 15.9245 10.705 15.9245 10.7844 15.8372L17 9"/></svg>', Yg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M9.2 12L11.0586 13.8586C11.1367 13.9367 11.2633 13.9367 11.3414 13.8586L14.7 10.5"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>';
function Ad() {
  const o = document.activeElement, e = window.getSelection().getRangeAt(0), t = e.cloneRange();
  return t.selectNodeContents(o), t.setStart(e.endContainer, e.endOffset), t.extractContents();
}
function Fg(o) {
  const e = document.createElement("div");
  return e.appendChild(o), e.innerHTML;
}
function Us(o, e = null, t = {}) {
  const n = document.createElement(o);
  Array.isArray(e) ? n.classList.add(...e) : e && n.classList.add(e);
  for (const r in t)
    n[r] = t[r];
  return n;
}
function Rd(o) {
  return o.innerHTML.replace("<br>", " ").trim();
}
function Id(o, e = !1, t = void 0) {
  const n = document.createRange(), r = window.getSelection();
  n.selectNodeContents(o), t !== void 0 && (n.setStart(o, t), n.setEnd(o, t)), n.collapse(e), r.removeAllRanges(), r.addRange(n);
}
Element.prototype.matches || (Element.prototype.matches = Element.prototype.msMatchesSelector || Element.prototype.webkitMatchesSelector);
Element.prototype.closest || (Element.prototype.closest = function(o) {
  let e = this;
  if (!document.documentElement.contains(e))
    return null;
  do {
    if (e.matches(o))
      return e;
    e = e.parentElement || e.parentNode;
  } while (e !== null && e.nodeType === 1);
  return null;
});
let zg = class {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to use native Enter behaviour
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: Yg,
      title: "Checklist"
    };
  }
  /**
   * Allow Checkbox Tool to be converted to/from other block
   *
   * @returns {{export: Function, import: Function}}
   */
  static get conversionConfig() {
    return {
      /**
       * To create exported string from the checkbox, concatenate items by dot-symbol.
       *
       * @param {ChecklistData} data - checklist data to create a string from that
       * @returns {string}
       */
      export: (e) => e.items.map(({ text: t }) => t).join(". "),
      /**
       * To create a checklist from other block's string, just put it at the first item
       *
       * @param {string} string - string to create list tool data from that
       * @returns {ChecklistData}
       */
      import: (e) => ({
        items: [
          {
            text: e,
            checked: !1
          }
        ]
      })
    };
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {object} options - block constructor options
   * @param {ChecklistData} options.data - previously saved data
   * @param {object} options.config - user config for Tool
   * @param {object} options.api - Editor.js API
   * @param {boolean} options.readOnly - read only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r }) {
    this._elements = {
      wrapper: null,
      items: []
    }, this.readOnly = r, this.api = n, this.data = e || {};
  }
  /**
   * Returns checklist tag with items
   *
   * @returns {Element}
   */
  render() {
    return this._elements.wrapper = Us("div", [this.CSS.baseBlock, this.CSS.wrapper]), this.data.items || (this.data.items = [
      {
        text: "",
        checked: !1
      }
    ]), this.data.items.forEach((e) => {
      const t = this.createChecklistItem(e);
      this._elements.wrapper.appendChild(t);
    }), this.readOnly ? this._elements.wrapper : (this._elements.wrapper.addEventListener("keydown", (e) => {
      const [t, n] = [13, 8];
      switch (e.keyCode) {
        case t:
          this.enterPressed(e);
          break;
        case n:
          this.backspace(e);
          break;
      }
    }, !1), this._elements.wrapper.addEventListener("click", (e) => {
      this.toggleCheckbox(e);
    }), this._elements.wrapper);
  }
  /**
   * Return Checklist data
   *
   * @returns {ChecklistData}
   */
  save() {
    let e = this.items.map((t) => {
      const n = this.getItemInput(t);
      return {
        text: Rd(n),
        checked: t.classList.contains(this.CSS.itemChecked)
      };
    });
    return e = e.filter((t) => t.text.trim().length !== 0), {
      items: e
    };
  }
  /**
   * Validate data: check if Checklist has items
   *
   * @param {ChecklistData} savedData — data received after saving
   * @returns {boolean} false if saved data is not correct, otherwise true
   * @public
   */
  validate(e) {
    return !!e.items.length;
  }
  /**
   * Toggle checklist item state
   *
   * @param {MouseEvent} event - click
   * @returns {void}
   */
  toggleCheckbox(e) {
    const t = e.target.closest(`.${this.CSS.item}`), n = t.querySelector(`.${this.CSS.checkboxContainer}`);
    n.contains(e.target) && (t.classList.toggle(this.CSS.itemChecked), n.classList.add(this.CSS.noHover), n.addEventListener("mouseleave", () => this.removeSpecialHoverBehavior(n), { once: !0 }));
  }
  /**
   * Create Checklist items
   *
   * @param {ChecklistItem} item - data.item
   * @returns {Element} checkListItem - new element of checklist
   */
  createChecklistItem(e = {}) {
    const t = Us("div", this.CSS.item), n = Us("span", this.CSS.checkbox), r = Us("div", this.CSS.checkboxContainer), a = Us("div", this.CSS.textField, {
      innerHTML: e.text ? e.text : "",
      contentEditable: !this.readOnly
    });
    return e.checked && t.classList.add(this.CSS.itemChecked), n.innerHTML = $g, r.appendChild(n), t.appendChild(r), t.appendChild(a), t;
  }
  /**
   * Append new elements to the list by pressing Enter
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  enterPressed(e) {
    e.preventDefault();
    const t = this.items, n = document.activeElement.closest(`.${this.CSS.item}`);
    if (t.indexOf(n) === t.length - 1 && Rd(this.getItemInput(n)).length === 0) {
      const u = this.api.blocks.getCurrentBlockIndex();
      n.remove(), this.api.blocks.insert(), this.api.caret.setToBlock(u + 1);
      return;
    }
    const r = Ad(), a = Fg(r), c = this.createChecklistItem({
      text: a,
      checked: !1
    });
    this._elements.wrapper.insertBefore(c, n.nextSibling), Id(this.getItemInput(c), !0);
  }
  /**
   * Handle backspace
   *
   * @param {KeyboardEvent} event - keyboard event
   */
  backspace(e) {
    const t = e.target.closest(`.${this.CSS.item}`), n = this.items.indexOf(t), r = this.items[n - 1];
    if (!r || window.getSelection().focusOffset !== 0)
      return;
    e.preventDefault();
    const a = Ad(), c = this.getItemInput(r), u = c.childNodes.length;
    c.appendChild(a), Id(c, void 0, u), t.remove();
  }
  /**
   * Styles
   *
   * @private
   * @returns {object<string>}
   */
  get CSS() {
    return {
      baseBlock: this.api.styles.block,
      wrapper: "cdx-checklist",
      item: "cdx-checklist__item",
      itemChecked: "cdx-checklist__item--checked",
      noHover: "cdx-checklist__item-checkbox--no-hover",
      checkbox: "cdx-checklist__item-checkbox-check",
      textField: "cdx-checklist__item-text",
      checkboxContainer: "cdx-checklist__item-checkbox"
    };
  }
  /**
   * Return all items elements
   *
   * @returns {Element[]}
   */
  get items() {
    return Array.from(this._elements.wrapper.querySelectorAll(`.${this.CSS.item}`));
  }
  /**
   * Removes class responsible for special hover behavior on an item
   * 
   * @private
   * @param {Element} el - item wrapper
   * @returns {Element}
   */
  removeSpecialHoverBehavior(e) {
    e.classList.remove(this.CSS.noHover);
  }
  /**
   * Find and return item's content editable element
   *
   * @private
   * @param {Element} el - item wrapper
   * @returns {Element}
   */
  getItemInput(e) {
    return e.querySelector(`.${this.CSS.textField}`);
  }
};
(function() {
  var o;
  try {
    if (typeof document < "u") {
      var e = document.createElement("style");
      e.nonce = (o = document.head.querySelector("meta[property=csp-nonce]")) == null ? void 0 : o.content, e.appendChild(document.createTextNode('.tc-header{margin:0;padding:0;position:relative;bottom:-1rem;text-align:left}.tc-wrap{--color-background:#f9f9fb;--color-text-secondary:#7b7e89;--color-border:#e8e8eb;--cell-size:34px;--toolbox-icon-size:18px;--toolbox-padding:6px;--toolbox-aiming-field-size:calc(var(--toolbox-icon-size) + var(--toolbox-padding)*2);border-left:0;margin-top:var(--toolbox-icon-size);box-sizing:border-box;grid-template-columns:calc(100% - var(--cell-size)) var(--cell-size)}.tc-wrap--readonly{grid-template-columns:100% var(--cell-size)}.tc-wrap svg{vertical-align:top}@media print{.tc-wrap{border-left-color:var(--color-border);border-left-style:solid;border-left-width:1px;grid-template-columns:100% var(--cell-size)}}@media print{.tc-wrap .tc-row:after{display:none}}.tc-table,.tc-wrap{position:relative;height:100%;width:100%;display:grid}.tc-table{font-size:14px;border-top:1px solid var(--color-border);border-left:1px solid var(--color-border);line-height:1.4}.tc-table:after{width:calc(var(--cell-size));left:calc(var(--cell-size)*-1);top:0}.tc-table:after,.tc-table:before{position:absolute;content:""}.tc-table:before{height:var(--toolbox-aiming-field-size);top:calc(var(--toolbox-aiming-field-size)*-1);left:0}.tc-table .tc-row:first-child{background:linear-gradient(0deg,rgba(0,0,0,.06),rgba(0,0,0,.06)),linear-gradient(0deg,#b9b9b9,#b9b9b9);font-weight:600;border-bottom:2px solid var(--color-border)}.tc-table .tc-row:first-child [contenteditable]:empty:before{content:attr(heading);color:var(--color-text-secondary)}.tc-table .tc-row:first-child:after{bottom:-2px;border-bottom:2px solid var(--color-border)}.tc-add-column,.tc-add-row{display:flex;color:var(--color-text-secondary)}@media print{.tc-add{display:none}}.tc-add-column{padding:4px 0;justify-content:center;border-top:1px solid var(--color-border)}@media print{.tc-add-column{display:none}}.tc-add-row{height:var(--cell-size);align-items:center;padding-left:4px;position:relative}.tc-add-row:before{content:"";position:absolute;right:calc(var(--cell-size)*-1);width:var(--cell-size);height:100%}@media print{.tc-add-row{display:none}}.tc-add-column,.tc-add-row{transition:0s;cursor:pointer;will-change:background-color}.tc-add-column:hover,.tc-add-row:hover{transition:background-color .1s ease;background-color:var(--color-background)}.tc-add-row{margin-top:1px}.tc-add-row:hover:before{transition:.1s;background-color:var(--color-background)}.tc-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(10px,1fr));position:relative;border-bottom:1px solid var(--color-border)}.tc-row:after{content:"";pointer-events:none;position:absolute;width:var(--cell-size);height:100%;bottom:-1px;right:calc(var(--cell-size)*-1);border-bottom:1px solid var(--color-border)}.tc-row--selected{background:var(--color-background)}.tc-row--selected:after{background:var(--color-background)}.tc-cell{text-align:left;border-right:1px solid var(--color-border);padding:6px 12px;overflow:hidden;outline:none;line-break:normal}.tc-cell>input,.tc-cell select{outline:none;border:none;width:100%;background:transparent}.tc-cell--selected{background:var(--color-background)}.tc-wrap--readonly .tc-row:after{display:none}.tc-cell>select,input{background-color:transparent}.tc-toolbox{--toolbox-padding:6px;--popover-margin:30px;--toggler-click-zone-size:30px;--toggler-dots-color:#7b7e89;--toggler-dots-color-hovered:#1d202b;position:absolute;cursor:pointer;z-index:1;opacity:0;transition:opacity .1s;will-change:left,opacity}.tc-toolbox--column{top:calc(var(--toggler-click-zone-size)*-1);transform:translate(calc(var(--toggler-click-zone-size)*-1/2));will-change:left,opacity}.tc-toolbox--row{left:calc(var(--popover-margin)*-1);transform:translateY(calc(var(--toggler-click-zone-size)*-1/2));margin-top:-1px;will-change:top,opacity}.tc-toolbox--showed{opacity:1}.tc-toolbox .tc-popover{position:absolute;top:0;left:var(--popover-margin)}.tc-toolbox__toggler{display:flex;align-items:center;justify-content:center;width:var(--toggler-click-zone-size);height:var(--toggler-click-zone-size);color:var(--toggler-dots-color);opacity:0;transition:opacity .15s ease;will-change:opacity}.tc-toolbox__toggler:hover{color:var(--toggler-dots-color-hovered)}.tc-toolbox__toggler svg{fill:currentColor}.tc-wrap:hover .tc-toolbox__toggler{opacity:1}.tc-settings .cdx-settings-button{width:50%;margin:0}.tc-popover{--color-border:#eaeaea;--color-background:#fff;--color-background-hover:rgba(232,232,235,.49);--color-background-confirm:#e24a4a;--color-background-confirm-hover:#d54040;--color-text-confirm:#fff;background:var(--color-background);border:1px solid var(--color-border);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;padding:6px;display:none;will-change:opacity,transform}.tc-popover--opened{display:block;animation:menuShowing .1s cubic-bezier(.215,.61,.355,1) forwards}.tc-popover__item{display:flex;align-items:center;padding:2px 14px 2px 2px;border-radius:5px;cursor:pointer;white-space:nowrap;-webkit-user-select:none;-moz-user-select:none;user-select:none}.tc-popover__item:hover{background:var(--color-background-hover)}.tc-popover__item:not(:last-of-type){margin-bottom:2px}.tc-popover__item-icon{display:inline-flex;width:26px;height:26px;align-items:center;justify-content:center;background:var(--color-background);border-radius:5px;border:1px solid var(--color-border);margin-right:8px}.tc-popover__item-label{line-height:22px;font-size:14px;font-weight:500}.tc-popover__item--confirm{background:var(--color-background-confirm);color:var(--color-text-confirm)}.tc-popover__item--confirm:hover{background-color:var(--color-background-confirm-hover)}.tc-popover__item--confirm .tc-popover__item-icon{background:var(--color-background-confirm);border-color:#0000001a}.tc-popover__item--confirm .tc-popover__item-icon svg{transition:transform .2s ease-in;transform:rotate(90deg) scale(1.2)}.tc-popover__item--hidden{display:none}@keyframes menuShowing{0%{opacity:0;transform:translateY(-8px) scale(.9)}70%{opacity:1;transform:translateY(2px)}to{transform:translateY(0)}}')), document.head.appendChild(e);
    }
  } catch (t) {
    console.error("vite-plugin-css-injected-by-js", t);
  }
})();
function mo(o, e, t = {}) {
  const n = document.createElement(o);
  Array.isArray(e) ? n.classList.add(...e) : e && n.classList.add(e);
  for (const r in t)
    Object.prototype.hasOwnProperty.call(t, r) && (n[r] = t[r]);
  return n;
}
function Nd(o) {
  const e = o.getBoundingClientRect();
  return {
    y1: Math.floor(e.top + window.pageYOffset),
    x1: Math.floor(e.left + window.pageXOffset),
    x2: Math.floor(e.right + window.pageXOffset),
    y2: Math.floor(e.bottom + window.pageYOffset)
  };
}
function Wg(o, e) {
  const t = Nd(o), n = Nd(e);
  return {
    fromTopBorder: n.y1 - t.y1,
    fromLeftBorder: n.x1 - t.x1,
    fromRightBorder: t.x2 - n.x2,
    fromBottomBorder: t.y2 - n.y2
  };
}
function Ug(o, e) {
  const t = o.getBoundingClientRect(), { width: n, height: r, x: a, y: c } = t, { clientX: u, clientY: p } = e;
  return {
    width: n,
    height: r,
    x: u - a,
    y: p - c
  };
}
function Pd(o, e) {
  return e.parentNode.insertBefore(o, e);
}
function Hd(o, e = !0) {
  const t = document.createRange(), n = window.getSelection();
  t.selectNodeContents(o), t.collapse(e), n.removeAllRanges(), n.addRange(t);
}
let qg = class qo {
  /**
   * @param {object} options - constructor options
   * @param {PopoverItem[]} options.items - constructor options
   */
  constructor({ items: e }) {
    this.items = e, this.wrapper = void 0, this.itemEls = [];
  }
  /**
   * Set of CSS classnames used in popover
   *
   * @returns {object}
   */
  static get CSS() {
    return {
      popover: "tc-popover",
      popoverOpened: "tc-popover--opened",
      item: "tc-popover__item",
      itemHidden: "tc-popover__item--hidden",
      itemConfirmState: "tc-popover__item--confirm",
      itemIcon: "tc-popover__item-icon",
      itemLabel: "tc-popover__item-label"
    };
  }
  /**
   * Returns the popover element
   *
   * @returns {Element}
   */
  render() {
    return this.wrapper = mo("div", qo.CSS.popover), this.items.forEach((e, t) => {
      const n = mo("div", qo.CSS.item), r = mo("div", qo.CSS.itemIcon, {
        innerHTML: e.icon
      }), a = mo("div", qo.CSS.itemLabel, {
        textContent: e.label
      });
      n.dataset.index = t, n.appendChild(r), n.appendChild(a), this.wrapper.appendChild(n), this.itemEls.push(n);
    }), this.wrapper.addEventListener("click", (e) => {
      this.popoverClicked(e);
    }), this.wrapper;
  }
  /**
   * Popover wrapper click listener
   * Used to delegate clicks in items
   *
   * @returns {void}
   */
  popoverClicked(e) {
    const t = e.target.closest(`.${qo.CSS.item}`);
    if (!t)
      return;
    const n = t.dataset.index, r = this.items[n];
    if (r.confirmationRequired && !this.hasConfirmationState(t)) {
      this.setConfirmationState(t);
      return;
    }
    r.onClick();
  }
  /**
   * Enable the confirmation state on passed item
   *
   * @returns {void}
   */
  setConfirmationState(e) {
    e.classList.add(qo.CSS.itemConfirmState);
  }
  /**
   * Disable the confirmation state on passed item
   *
   * @returns {void}
   */
  clearConfirmationState(e) {
    e.classList.remove(qo.CSS.itemConfirmState);
  }
  /**
   * Check if passed item has the confirmation state
   *
   * @returns {boolean}
   */
  hasConfirmationState(e) {
    return e.classList.contains(qo.CSS.itemConfirmState);
  }
  /**
   * Return an opening state
   *
   * @returns {boolean}
   */
  get opened() {
    return this.wrapper.classList.contains(qo.CSS.popoverOpened);
  }
  /**
   * Opens the popover
   *
   * @returns {void}
   */
  open() {
    this.items.forEach((e, t) => {
      typeof e.hideIf == "function" && this.itemEls[t].classList.toggle(qo.CSS.itemHidden, e.hideIf());
    }), this.wrapper.classList.add(qo.CSS.popoverOpened);
  }
  /**
   * Closes the popover
   *
   * @returns {void}
   */
  close() {
    this.wrapper.classList.remove(qo.CSS.popoverOpened), this.itemEls.forEach((e) => {
      this.clearConfirmationState(e);
    });
  }
};
const jd = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 8L12 12M12 12L16 16M12 12L16 8M12 12L8 16"/></svg>', Vg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 9.16666L18.2167 12.5M18.2167 12.5L14.8833 15.8333M18.2167 12.5H10.05C9.16594 12.5 8.31809 12.1488 7.69297 11.5237C7.06785 10.8986 6.71666 10.0507 6.71666 9.16666"/></svg>', Gg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.9167 14.9167L11.5833 18.25M11.5833 18.25L8.25 14.9167M11.5833 18.25L11.5833 10.0833C11.5833 9.19928 11.9345 8.35143 12.5596 7.72631C13.1848 7.10119 14.0326 6.75 14.9167 6.75"/></svg>', Zg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.13333 14.9167L12.4667 18.25M12.4667 18.25L15.8 14.9167M12.4667 18.25L12.4667 10.0833C12.4667 9.19928 12.1155 8.35143 11.4904 7.72631C10.8652 7.10119 10.0174 6.75 9.13333 6.75"/></svg>', Kg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 15.8333L18.2167 12.5M18.2167 12.5L14.8833 9.16667M18.2167 12.5L10.05 12.5C9.16595 12.5 8.31811 12.8512 7.69299 13.4763C7.06787 14.1014 6.71667 14.9493 6.71667 15.8333"/></svg>', Xg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.41 9.66H9.4"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 9.66H14.59"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.31 14.36H9.3"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 14.36H14.59"/></svg>', Jg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>', Qg = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M10 5V18.5"/><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>';
let $d = class ts {
  /**
   * Creates toolbox buttons and toolbox menus
   *
   * @param {object} api - Editor.js api
   * @param {PopoverItem[]} items - Editor.js api
   * @param {function} onOpen - callback fired when the Popover is opening
   * @param {function} onClose - callback fired when the Popover is closing
   * @param {string} [cssModifier] - the modifier for the Toolbox. Allows to add some specific styles.
   */
  constructor({ api: e, items: t, onOpen: n, onClose: r, cssModifier: a = "" }) {
    this.api = e, this.items = t, this.onOpen = n, this.onClose = r, this.cssModifier = a, this.popover = null, this.wrapper = this.createToolbox();
  }
  /**
   * Style classes
   */
  static get CSS() {
    return {
      toolbox: "tc-toolbox",
      toolboxShowed: "tc-toolbox--showed",
      toggler: "tc-toolbox__toggler"
    };
  }
  /**
   * Returns rendered Toolbox element
   */
  get element() {
    return this.wrapper;
  }
  /**
   * Creating a toolbox to open menu for a manipulating columns
   *
   * @returns {Element}
   */
  createToolbox() {
    const e = mo("div", [
      ts.CSS.toolbox,
      this.cssModifier ? `${ts.CSS.toolbox}--${this.cssModifier}` : ""
    ]);
    e.dataset.mutationFree = "true";
    const t = this.createPopover(), n = this.createToggler();
    return e.appendChild(n), e.appendChild(t), e;
  }
  /**
   * Creates the Toggler
   *
   * @returns {Element}
   */
  createToggler() {
    const e = mo("div", ts.CSS.toggler, {
      innerHTML: Xg
    });
    return e.addEventListener("click", () => {
      this.togglerClicked();
    }), e;
  }
  /**
   * Creates the Popover instance and render it
   *
   * @returns {Element}
   */
  createPopover() {
    return this.popover = new qg({
      items: this.items
    }), this.popover.render();
  }
  /**
   * Toggler click handler. Opens/Closes the popover
   *
   * @returns {void}
   */
  togglerClicked() {
    this.popover.opened ? (this.popover.close(), this.onClose()) : (this.popover.open(), this.onOpen());
  }
  /**
   * Shows the Toolbox
   *
   * @param {function} computePositionMethod - method that returns the position coordinate
   * @returns {void}
   */
  show(e) {
    const t = e();
    Object.entries(t).forEach(([n, r]) => {
      this.wrapper.style[n] = r;
    }), this.wrapper.classList.add(ts.CSS.toolboxShowed);
  }
  /**
   * Hides the Toolbox
   *
   * @returns {void}
   */
  hide() {
    this.popover.close(), this.wrapper.classList.remove(ts.CSS.toolboxShowed);
  }
};
function eb(o, e) {
  let t = 0;
  return function(...n) {
    const r = (/* @__PURE__ */ new Date()).getTime();
    if (!(r - t < o))
      return t = r, e(...n);
  };
}
const qt = {
  wrapper: "tc-wrap",
  wrapperReadOnly: "tc-wrap--readonly",
  table: "tc-table",
  row: "tc-row",
  withHeadings: "tc-table--heading",
  rowSelected: "tc-row--selected",
  cell: "tc-cell",
  cellSelected: "tc-cell--selected",
  addRow: "tc-add-row",
  addColumn: "tc-add-column"
};
class tb {
  /**
   * Creates
   *
   * @constructor
   * @param {boolean} readOnly - read-only mode flag
   * @param {object} api - Editor.js API
   * @param {TableData} data - Editor.js API
   * @param {TableConfig} config - Editor.js API
   */
  constructor(e, t, n, r, a, c) {
    this.readOnly = e, this.api = t, "data" in n && typeof n.data == "object" || (n.data = {}), this.data = n, this.config = r, this.options = a, this.block = c, this.wrapper = null, this.table = null, this.hoveredRow = 0, this.hoveredColumn = 0, this.selectedRow = 0, this.selectedColumn = 0, this.tunes = {
      withHeadings: !0
    }, this.fillInput(), this.focusedCell = {
      row: 0,
      column: 0
    }, this.documentClicked = (u) => {
    }, setTimeout(() => {
      Object.keys(n.data).length === 0 && n.default.map((u) => this.setValue(u, "")), this.fillInput();
    }, 100), this.readOnly || this.bindEvents();
  }
  remove(e) {
    return e in this.data.data ? (delete this.data.data[e], !0) : !1;
  }
  fillInput() {
    var e, t, n, r, a, c, u, p;
    this.createTableWrapper(), this.resize();
    const g = ((e = this.config) == null ? void 0 : e.locale) === "jp";
    this.setCellContent(1, 1, this.api.i18n.t("Vitals")), this.setCellContent(1, 2, this.api.i18n.t("Value")), this.setCellContent(1, 3, this.api.i18n.t("Unit"));
    let v = 2;
    for (const k in this.data.data)
      if (Object.hasOwnProperty.call(this.data.data, k)) {
        const y = this.options[k];
        let T = this.getCell(v, 1);
        T.innerHTML = g && (y == null ? void 0 : y.label_jp) || (y == null ? void 0 : y.label), T = this.getCell(v, 2), T.innerHTML = "";
        const x = mo("input", "", {
          placeHolder: this.api.i18n.t("Enter value"),
          name: k + "_value",
          type: "number",
          disabled: this.readOnly,
          value: ((n = (t = this.data.data) == null ? void 0 : t[k]) == null ? void 0 : n.value) || ""
        });
        this.api.listeners.on(x, "keyup", (L) => {
          this.setValue(k, L.target.value);
        }), T.appendChild(x), T = this.getCell(v, 3);
        let M = "";
        if (["radio", "select"].includes(y.type)) {
          M = mo("select", "", {
            placeHolder: this.api.i18n.t("Select Value"),
            name: k + "_remarks",
            disabled: this.readOnly
          }), this.api.listeners.on(M, "change", (L) => {
            this.setRemarks(k, L.target.value);
          });
          for (const L of y.options)
            M.appendChild(mo("option", "", {
              value: L == null ? void 0 : L.value,
              innerHTML: g ? L == null ? void 0 : L.label_jp : L == null ? void 0 : L.label,
              selected: L.value == ((a = (r = this.data.data) == null ? void 0 : r[k]) == null ? void 0 : a.remarks)
            }));
        } else
          y.type == "text" && (M = mo("input", "", {
            value: ((p = (u = (c = this.data) == null ? void 0 : c.data) == null ? void 0 : u[k]) == null ? void 0 : p.remarks) || "",
            placeHolder: this.api.i18n.t("Input value"),
            name: k + "_value",
            disabled: this.readOnly,
            type: "text"
          }), this.api.listeners.on(M, "keyup", (L) => {
            this.setRemarks(k, L.target.value);
          }));
        M !== "" && (T.innerHTML = "", T.appendChild(M)), v++;
      }
  }
  setValue(e, t) {
    let n = this.data.data[e];
    n && typeof n == "object" && "value" in n || (this.data.data[e] = { value: "", remarks: "" }), this.data.data[e].value = t, this.block.dispatchChange();
  }
  setData(e) {
    e.map((t) => {
      const n = t.key;
      if (this.data.data[n]) {
        delete this.data.data[n], this.block.dispatchChange();
        return;
      } else
        this.data.data[n] = { value: "", remarks: "" };
    }), this.block.dispatchChange();
  }
  setRemarks(e, t) {
    let n = this.data.data[e];
    n && typeof n == "object" && "value" in n || (this.data.data[e] = { value: "", remarks: "" }), this.data.data[e].remarks = t, this.block.dispatchChange();
  }
  /**
   * Returns the rendered table wrapper
   *
   * @returns {Element}
   */
  getWrapper() {
    return this.wrapper;
  }
  /**
   * Hangs the necessary handlers to events
   */
  bindEvents() {
    document.addEventListener("click", this.documentClicked), this.table.addEventListener("mousemove", eb(150, (e) => this.onMouseMoveInTable(e)), { passive: !0 }), this.table.onkeypress = (e) => this.onKeyPressListener(e), this.table.addEventListener("keydown", (e) => this.onKeyDownListener(e)), this.table.addEventListener("focusin", (e) => this.focusInTableListener(e));
  }
  /**
   * Configures and creates the toolbox for manipulating with columns
   *
   * @returns {Toolbox}
   */
  createColumnToolbox() {
    return new $d({
      api: this.api,
      cssModifier: "column",
      items: [
        {
          label: this.api.i18n.t("Add column to left"),
          icon: Gg,
          onClick: () => {
            this.addColumn(this.selectedColumn, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Add column to right"),
          icon: Zg,
          onClick: () => {
            this.addColumn(this.selectedColumn + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete column"),
          icon: jd,
          hideIf: () => this.numberOfColumns === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteColumn(this.selectedColumn), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectColumn(this.hoveredColumn);
      },
      onClose: () => {
        this.unselectColumn();
      }
    });
  }
  /**
   * Configures and creates the toolbox for manipulating with rows
   *
   * @returns {Toolbox}
   */
  createRowToolbox() {
    return new $d({
      api: this.api,
      cssModifier: "row",
      items: [
        {
          label: this.api.i18n.t("Add row above"),
          icon: Kg,
          onClick: () => {
            this.addRow(this.selectedRow, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Add row below"),
          icon: Vg,
          onClick: () => {
            this.addRow(this.selectedRow + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete row"),
          icon: jd,
          hideIf: () => this.numberOfRows === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteRow(this.selectedRow), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  /**
   * When you press enter it moves the cursor down to the next row
   * or creates it if the click occurred on the last one
   */
  moveCursorToNextRow() {
    this.focusedCell.row !== this.numberOfRows ? (this.focusedCell.row += 1, this.focusCell(this.focusedCell)) : (this.addRow(), this.focusedCell.row += 1, this.focusCell(this.focusedCell), this.updateToolboxesPosition(0, 0));
  }
  /**
   * Get table cell by row and col index
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @returns {HTMLElement}
   */
  getCell(e, t) {
    return this.table.querySelectorAll(`.${qt.row}:nth-child(${e}) .${qt.cell}`)[t - 1];
  }
  /**
   * Get table row by index
   *
   * @param {number} row - row coordinate
   * @returns {HTMLElement}
   */
  getRow(e) {
    return this.table.querySelector(`.${qt.row}:nth-child(${e})`);
  }
  /**
   * The parent of the cell which is the row
   *
   * @param {HTMLElement} cell - cell element
   * @returns {HTMLElement}
   */
  getRowByCell(e) {
    return e.parentElement;
  }
  /**
   * Ger row's first cell
   *
   * @param {Element} row - row to find its first cell
   * @returns {Element}
   */
  getRowFirstCell(e) {
    return e.querySelector(`.${qt.cell}:first-child`);
  }
  /**
   * Set the sell's content by row and column numbers
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @param {string} content - cell HTML content
   */
  setCellContent(e, t, n) {
    const r = this.getCell(e, t);
    r.innerHTML = n;
  }
  /**
   * Add column in table on index place
   * Add cells in each row
   *
   * @param {number} columnIndex - number in the array of columns, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the first cell
   */
  addColumn(e = -1, t = !1) {
    let n = this.numberOfColumns;
    for (let r = 1; r <= this.numberOfRows; r++) {
      let a;
      const c = this.createCell();
      if (e > 0 && e <= n ? (a = this.getCell(r, e), Pd(c, a)) : a = this.getRow(r).appendChild(c), r === 1) {
        const u = this.getCell(r, e > 0 ? e : n + 1);
        u && t && Hd(u);
      }
    }
    this.addHeadingAttrToFirstRow();
  }
  /**
   * Add row in table on index place
   *
   * @param {number} index - number in the array of rows, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the inserted row
   * @returns {HTMLElement} row
   */
  addRow(e = -1, t = !1) {
    let n, r = mo("div", qt.row);
    this.tunes.withHeadings && this.removeHeadingAttrFromFirstRow();
    let a = this.numberOfColumns;
    if (e > 0 && e <= this.numberOfRows) {
      let u = this.getRow(e);
      n = Pd(r, u);
    } else
      n = this.table.appendChild(r);
    this.fillRow(n, a), this.tunes.withHeadings && this.addHeadingAttrToFirstRow();
    const c = this.getRowFirstCell(n);
    return c && t && Hd(c), n;
  }
  /**
   * Delete a column by index
   *
   * @param {number} index
   */
  deleteColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      if (!n)
        return;
      n.remove();
    }
  }
  /**
   * Delete a row by index
   *
   * @param {number} index
   */
  deleteRow(e) {
    this.getRow(e).remove(), this.addHeadingAttrToFirstRow();
  }
  /**
   * Create a wrapper containing a table, toolboxes
   * and buttons for adding rows and columns
   *
   * @returns {HTMLElement} wrapper - where all buttons for a table and the table itself will be
   */
  createTableWrapper() {
    this.wrapper ? this.table.remove() : this.wrapper = mo("div", qt.wrapper), this.table = mo("div", qt.table), this.readOnly && this.wrapper.classList.add(qt.wrapperReadOnly), this.wrapper.appendChild(this.table);
  }
  /**
   * Returns the size of the table based on initial data or config "size" property
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  computeInitialSize() {
    const e = this.data && this.data.data, t = Array.isArray(e), n = t ? e.length : !1, r = t ? e.length : void 0, a = n ? e[0].length : void 0, c = Number.parseInt(this.config && this.config.rows), u = Number.parseInt(this.config && this.config.cols), p = !isNaN(c) && c > 0 ? c : void 0, g = !isNaN(u) && u > 0 ? u : void 0;
    return {
      rows: r || p || 2,
      cols: a || g || 2
    };
  }
  /**
   * Resize table to match config size or transmitted data size
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  resize() {
    const e = Object.keys(this.data.data).length + 1;
    for (let t = 0; t < e; t++)
      this.addRow();
    for (let t = 0; t < 3; t++)
      this.addColumn();
  }
  /**
   * Fills the table with data passed to the constructor
   *
   * @returns {void}
   */
  fill() {
    const e = this.data;
    if (e && e.data)
      for (let t = 0; t < e.data.length; t++)
        for (let n = 0; n < e.data[t].length; n++)
          this.setCellContent(t + 1, n + 1, e.data[t][n]);
  }
  /**
   * Fills a row with cells
   *
   * @param {HTMLElement} row - row to fill
   * @param {number} numberOfColumns - how many cells should be in a row
   */
  fillRow(e, t) {
    for (let n = 1; n <= t; n++) {
      const r = this.createCell();
      e.appendChild(r);
    }
  }
  /**
   * Creating a cell element
   *
   * @return {Element}
   */
  createCell() {
    return mo("div", qt.cell, {
      contentEditable: !1
    });
  }
  /**
   * Get number of rows in the table
   */
  get numberOfRows() {
    return this.table.childElementCount;
  }
  /**
   * Get number of columns in the table
   */
  get numberOfColumns() {
    return this.numberOfRows ? this.table.querySelectorAll(`.${qt.row}:first-child .${qt.cell}`).length : 0;
  }
  /**
   * Is the column toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isColumnMenuShowing() {
    return this.selectedColumn !== 0;
  }
  /**
   * Is the row toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isRowMenuShowing() {
    return this.selectedRow !== 0;
  }
  /**
   * Recalculate position of toolbox icons
   *
   * @param {Event} event - mouse move event
   */
  onMouseMoveInTable(e) {
    const { row: t, column: n } = this.getHoveredCell(e);
    this.hoveredColumn = n, this.hoveredRow = t, this.updateToolboxesPosition();
  }
  /**
   * Prevents default Enter behaviors
   * Adds Shift+Enter processing
   *
   * @param {KeyboardEvent} event - keypress event
   */
  onKeyPressListener(e) {
    if (e.key === "Enter") {
      if (e.shiftKey)
        return !0;
      this.moveCursorToNextRow();
    }
    return e.key !== "Enter";
  }
  /**
   * Prevents tab keydown event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyDownListener(e) {
    e.key === "Tab" && e.stopPropagation();
  }
  /**
   * Set the coordinates of the cell that the focus has moved to
   *
   * @param {FocusEvent} event - focusin event
   */
  focusInTableListener(e) {
    const t = e.target, n = this.getRowByCell(t);
    this.focusedCell = {
      row: Array.from(this.table.querySelectorAll(`.${qt.row}`)).indexOf(n) + 1,
      column: Array.from(n.querySelectorAll(`.${qt.cell}`)).indexOf(t) + 1
    };
  }
  /**
   * Unselect row/column
   * Close toolbox menu
   * Hide toolboxes
   *
   * @returns {void}
   */
  hideToolboxes() {
    this.hideRowToolbox(), this.hideColumnToolbox(), this.updateToolboxesPosition();
  }
  /**
   * Unselect row, close toolbox
   *
   * @returns {void}
   */
  hideRowToolbox() {
    this.unselectRow(), this.toolboxRow.hide();
  }
  /**
   * Unselect column, close toolbox
   *
   * @returns {void}
   */
  hideColumnToolbox() {
    this.unselectColumn(), this.toolboxColumn.hide();
  }
  /**
   * Set the cursor focus to the focused cell
   *
   * @returns {void}
   */
  focusCell() {
    this.focusedCellElem.focus();
  }
  /**
   * Get current focused element
   *
   * @returns {HTMLElement} - focused cell
   */
  get focusedCellElem() {
    const { row: e, column: t } = this.focusedCell;
    return this.getCell(e, t);
  }
  /**
   * Update toolboxes position
   *
   * @param {number} row - hovered row
   * @param {number} column - hovered column
   */
  updateToolboxesPosition(e = this.hoveredRow, t = this.hoveredColumn) {
  }
  /**
   * Makes the first row headings
   *
   * @param {boolean} withHeadings - use headings row or not
   */
  setHeadingsSetting(e) {
    this.tunes.withHeadings = e, e ? (this.table.classList.add(qt.withHeadings), this.addHeadingAttrToFirstRow()) : (this.table.classList.remove(qt.withHeadings), this.removeHeadingAttrFromFirstRow());
  }
  /**
   * Adds an attribute for displaying the placeholder in the cell
   */
  addHeadingAttrToFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.setAttribute("heading", this.api.i18n.t("Heading"));
    }
  }
  /**
   * Removes an attribute for displaying the placeholder in the cell
   */
  removeHeadingAttrFromFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.removeAttribute("heading");
    }
  }
  /**
   * Add effect of a selected row
   *
   * @param {number} index
   */
  selectRow(e) {
    const t = this.getRow(e);
    t && (this.selectedRow = e, t.classList.add(qt.rowSelected));
  }
  /**
   * Remove effect of a selected row
   */
  unselectRow() {
    if (this.selectedRow <= 0)
      return;
    const e = this.table.querySelector(`.${qt.rowSelected}`);
    e && e.classList.remove(qt.rowSelected), this.selectedRow = 0;
  }
  /**
   * Add effect of a selected column
   *
   * @param {number} index
   */
  selectColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      n && n.classList.add(qt.cellSelected);
    }
    this.selectedColumn = e;
  }
  /**
   * Remove effect of a selected column
   */
  unselectColumn() {
    if (this.selectedColumn <= 0)
      return;
    let e = this.table.querySelectorAll(`.${qt.cellSelected}`);
    Array.from(e).forEach((t) => {
      t.classList.remove(qt.cellSelected);
    }), this.selectedColumn = 0;
  }
  /**
   * Calculates the row and column that the cursor is currently hovering over
   * The search was optimized from O(n) to O (log n) via bin search to reduce the number of calculations
   *
   * @param {Event} event - mousemove event
   * @returns hovered cell coordinates as an integer row and column
   */
  getHoveredCell(e) {
    let t = this.hoveredRow, n = this.hoveredColumn;
    const { width: r, height: a, x: c, y: u } = Ug(this.table, e);
    return c >= 0 && (n = this.binSearch(
      this.numberOfColumns,
      (p) => this.getCell(1, p),
      ({ fromLeftBorder: p }) => c < p,
      ({ fromRightBorder: p }) => c > r - p
    )), u >= 0 && (t = this.binSearch(
      this.numberOfRows,
      (p) => this.getCell(p, 1),
      ({ fromTopBorder: p }) => u < p,
      ({ fromBottomBorder: p }) => u > a - p
    )), {
      row: t || this.hoveredRow,
      column: n || this.hoveredColumn
    };
  }
  /**
   * Looks for the index of the cell the mouse is hovering over.
   * Cells can be represented as ordered intervals with left and
   * right (upper and lower for rows) borders inside the table, if the mouse enters it, then this is our index
   *
   * @param {number} numberOfCells - upper bound of binary search
   * @param {function} getCell - function to take the currently viewed cell
   * @param {function} beforeTheLeftBorder - determines the cursor position, to the left of the cell or not
   * @param {function} afterTheRightBorder - determines the cursor position, to the right of the cell or not
   * @returns {number}
   */
  binSearch(e, t, n, r) {
    let a = 0, c = e + 1, u = 0, p;
    for (; a < c - 1 && u < 10; ) {
      p = Math.ceil((a + c) / 2);
      const g = t(p), v = Wg(this.table, g);
      if (n(v))
        c = p;
      else if (r(v))
        a = p;
      else
        break;
      u++;
    }
    return p;
  }
  /**
   * Collects data from cells into a two-dimensional array
   *
   * @returns {string[][]}
   */
  getData() {
    return this.data.data || {};
  }
  /**
   * Remove listeners on the document
   */
  destroy() {
    document.removeEventListener("click", this.documentClicked);
  }
}
const ob = [
  {
    level: 1,
    title: "Body Weight",
    title_jp: "活動量",
    data: [
      {
        key: "bodyWeight",
        type: "select",
        options: [
          {
            label: "Kg",
            label_jp: "kg",
            value: 1
          },
          {
            label: "Grams",
            label_jp: "Grams",
            value: 2
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "Temperature",
    title_jp: "活動量",
    data: [
      {
        key: "temprature",
        type: "select",
        options: [
          {
            label: "°C",
            label_jp: "°C",
            value: 1
          },
          {
            label: "°F",
            label_jp: "°F",
            value: 2
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "Heart rate",
    title_jp: "活動量",
    data: [
      {
        key: "heartRate",
        type: "select",
        options: [
          {
            label: "BPM",
            label_jp: "BPM",
            value: 1
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "Respiratory rate",
    title_jp: "活動量",
    data: [
      {
        key: "respiratoryRate",
        type: "select",
        options: [
          {
            label: "BPM",
            label_jp: "BPM",
            value: 1
          }
        ]
      }
    ]
  }
], Sl = {
  default: [
    "bodyWeight",
    "temprature",
    "heartRate",
    "respiratoryRate"
  ],
  data: ob
};
let nb = class {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to press Enter inside the CodeTool textarea
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {TableData} data — previously saved data
   * @param {TableConfig} config - user config for Tool
   * @param {object} api - Editor.js API
   * @param {boolean} readOnly - read-only mode flag
   */
  constructor(e) {
    var t;
    this.items = [];
    const { data: n, config: r, api: a, readOnly: c, block: u } = e;
    this.title = a.i18n.t("Vitals"), this.api = a, this.block = u, this.readOnly = c || !1, this.config = r, this.data = {
      data: n && n.data && ((t = Object.keys(n.data)) == null ? void 0 : t.length) != 0 ? n.data : {},
      default: Sl.default
    }, this.options = {}, this.createOptions(), this.createItems(), this.table = null, setTimeout(() => {
      if (n != null && n.data)
        return;
      const p = this.block.id;
      this.api.toolbar.toggleBlockSettingsById(p);
    }, 500);
  }
  onActivate(e) {
    if (this.table.remove(e)) {
      this.table.fillInput();
      return;
    }
    this.table.setValue(e, ""), this.table.fillInput();
  }
  checkIfActive(e) {
    var t, n;
    let r = ((n = (t = this.table) == null ? void 0 : t.data) == null ? void 0 : n.data) || {};
    return e in r;
  }
  createOptions() {
    var e;
    const t = ((e = this.config) == null ? void 0 : e.locale) === "jp";
    this.options = {}, Sl.data.map((n) => {
      n.data.map((r) => {
        let a = t ? n.title_jp : n.title;
        r != null && r.title && (a += ` (${t ? r.title_jp : r.title})`), this.options[r.key] = { ...r, label: a };
      });
    });
  }
  createItems() {
    var e;
    const t = ((e = this.config) == null ? void 0 : e.locale) === "jp";
    this.items = [], Sl.data.map((n) => {
      const r = this.checkIfActive(n.data[0].key);
      this.items.push({
        label: t ? `${n.title_jp}` : `${n.title}`,
        icon: Jg,
        isActive: r,
        closeOnActivate: !0,
        toggle: !0,
        onActivate: () => {
          this.table.setData(n.data), this.table.fillInput();
        }
      });
    });
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: Qg,
      title: this.title
    };
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLDivElement}
   */
  render() {
    this.table = new tb(this.readOnly, this.api, this.data, this.config, this.options, this.block), this.container = mo("div", this.api.styles.block);
    const e = mo("h5", "tc-header");
    return e.innerHTML = this.title, this.container.appendChild(e), this.container.appendChild(this.table.getWrapper()), this.table.setHeadingsSetting(!0), this.container;
  }
  /**
   * Returns plugin settings
   *
   * @returns {Array}
   */
  renderSettings() {
    return this.createItems(), this.items;
  }
  /**
   * Extract table data from the view
   *
   * @returns {TableData} - saved data
   */
  save() {
    return {
      data: this.table.getData()
    };
  }
  /**
   * Plugin destroyer
   *
   * @returns {void}
   */
  destroy() {
    this.table.destroy();
  }
  /**
   * A helper to get config value.
   * 
   * @param {string} configName - the key to get from the config. 
   * @param {any} defaultValue - default value if config doesn't have passed key
   * @param {object} savedData - previously saved data. If passed, the key will be got from there, otherwise from the config
   * @returns {any} - config value.
   */
  getConfig(e, t = void 0, n = void 0) {
    const r = this.data || n;
    return r ? r[e] ? r[e] : t : this.config && this.config[e] ? this.config[e] : t;
  }
  // /**  
  //  * Table onPaste configuration
  //  *
  //  * @public
  //  */
  // static get pasteConfig() {
  //   return { tags: ['TABLE', 'TR', 'TH', 'TD'] };
  // }
  // /**
  //  * On paste callback that is fired from Editor
  //  *
  //  * @param {PasteEvent} event - event with pasted data
  //  */
  // onPaste(event) {
  //   const table = event.detail.data;
  //   /** Check if the first row is a header */
  //   const firstRowHeading = table.querySelector(':scope > thead, tr:first-of-type th');
  //   /** Get all rows from the table */
  //   const rows = Array.from(table.querySelectorAll('tr'));
  //   /** Generate a content matrix */
  //   const content = rows.map((row) => {
  //     /** Get cells from row */
  //     const cells = Array.from(row.querySelectorAll('th, td'))
  //     /** Return cells content */
  //     return cells.map((cell) => cell.innerHTML);
  //   });
  //   /** Update Tool's data */
  //   this.data = {
  //     withHeadings: firstRowHeading !== null,
  //     content
  //   };
  //   /** Update table block */
  //   if (this.table.wrapper) {
  //     this.table.wrapper.replaceWith(this.render());
  //   }
  // }
};
function Ye(o, e, t = {}) {
  const n = document.createElement(o);
  Array.isArray(e) ? n.classList.add(...e) : e && n.classList.add(e);
  for (const r in t)
    Object.prototype.hasOwnProperty.call(t, r) && (n[r] = t[r]);
  return n;
}
function Yd(o) {
  const e = o.getBoundingClientRect();
  return {
    y1: Math.floor(e.top + window.pageYOffset),
    x1: Math.floor(e.left + window.pageXOffset),
    x2: Math.floor(e.right + window.pageXOffset),
    y2: Math.floor(e.bottom + window.pageYOffset)
  };
}
function Tl(o, e) {
  const t = Yd(o), n = Yd(e);
  return {
    fromTopBorder: n.y1 - t.y1,
    fromLeftBorder: n.x1 - t.x1,
    fromRightBorder: t.x2 - n.x2,
    fromBottomBorder: t.y2 - n.y2
  };
}
function ib(o, e) {
  const t = o.getBoundingClientRect(), { width: n, height: r, x: a, y: c } = t, { clientX: u, clientY: p } = e;
  return {
    width: n,
    height: r,
    x: u - a,
    y: p - c
  };
}
function Fd(o, e) {
  return e.parentNode.insertBefore(o, e);
}
function zd(o, e = !0) {
  const t = document.createRange(), n = window.getSelection();
  t.selectNodeContents(o), t.collapse(e), n.removeAllRanges(), n.addRange(t);
}
class Ao {
  /**
   * @param {object} options - constructor options
   * @param {PopoverItem[]} options.items - constructor options
   */
  constructor({ items: e }) {
    this.items = e, this.wrapper = void 0, this.itemEls = [];
  }
  /**
   * Set of CSS classnames used in popover
   *
   * @returns {object}
   */
  static get CSS() {
    return {
      popover: "tc-popover",
      popoverOpened: "tc-popover--opened",
      item: "tc-popover__item",
      itemHidden: "tc-popover__item--hidden",
      itemConfirmState: "tc-popover__item--confirm",
      itemIcon: "tc-popover__item-icon",
      itemLabel: "tc-popover__item-label"
    };
  }
  /**
   * Returns the popover element
   *
   * @returns {Element}
   */
  render() {
    return this.wrapper = Ye("div", Ao.CSS.popover), this.items.forEach((e, t) => {
      const n = Ye("div", Ao.CSS.item), r = Ye("div", Ao.CSS.itemIcon, {
        innerHTML: e.icon
      }), a = Ye("div", Ao.CSS.itemLabel, {
        textContent: e.label
      });
      n.dataset.index = t, n.appendChild(r), n.appendChild(a), this.wrapper.appendChild(n), this.itemEls.push(n);
    }), this.wrapper.addEventListener("click", (e) => {
      this.popoverClicked(e);
    }), this.wrapper;
  }
  /**
   * Popover wrapper click listener
   * Used to delegate clicks in items
   *
   * @returns {void}
   */
  popoverClicked(e) {
    const t = e.target.closest(`.${Ao.CSS.item}`);
    if (!t)
      return;
    const n = t.dataset.index, r = this.items[n];
    if (r.confirmationRequired && !this.hasConfirmationState(t)) {
      this.setConfirmationState(t);
      return;
    }
    r.onClick();
  }
  /**
   * Enable the confirmation state on passed item
   *
   * @returns {void}
   */
  setConfirmationState(e) {
    e.classList.add(Ao.CSS.itemConfirmState);
  }
  /**
   * Disable the confirmation state on passed item
   *
   * @returns {void}
   */
  clearConfirmationState(e) {
    e.classList.remove(Ao.CSS.itemConfirmState);
  }
  /**
   * Check if passed item has the confirmation state
   *
   * @returns {boolean}
   */
  hasConfirmationState(e) {
    return e.classList.contains(Ao.CSS.itemConfirmState);
  }
  /**
   * Return an opening state
   *
   * @returns {boolean}
   */
  get opened() {
    return this.wrapper.classList.contains(Ao.CSS.popoverOpened);
  }
  /**
   * Opens the popover
   *
   * @returns {void}
   */
  open() {
    this.items.forEach((e, t) => {
      typeof e.hideIf == "function" && this.itemEls[t].classList.toggle(Ao.CSS.itemHidden, e.hideIf());
    }), this.wrapper.classList.add(Ao.CSS.popoverOpened);
  }
  /**
   * Closes the popover
   *
   * @returns {void}
   */
  close() {
    this.wrapper.classList.remove(Ao.CSS.popoverOpened), this.itemEls.forEach((e) => {
      this.clearConfirmationState(e);
    });
  }
}
const Wd = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 8L12 12M12 12L16 16M12 12L16 8M12 12L8 16"/></svg>', rb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 9.16666L18.2167 12.5M18.2167 12.5L14.8833 15.8333M18.2167 12.5H10.05C9.16594 12.5 8.31809 12.1488 7.69297 11.5237C7.06785 10.8986 6.71666 10.0507 6.71666 9.16666"/></svg>', sb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.9167 14.9167L11.5833 18.25M11.5833 18.25L8.25 14.9167M11.5833 18.25L11.5833 10.0833C11.5833 9.19928 11.9345 8.35143 12.5596 7.72631C13.1848 7.10119 14.0326 6.75 14.9167 6.75"/></svg>', ab = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 15.8333L18.2167 12.5M18.2167 12.5L14.8833 9.16667M18.2167 12.5L10.05 12.5C9.16595 12.5 8.31811 12.8512 7.69299 13.4763C7.06787 14.1014 6.71667 14.9493 6.71667 15.8333"/></svg>', lb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.41 9.66H9.4"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 9.66H14.59"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.31 14.36H9.3"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 14.36H14.59"/></svg>', Ud = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M12 7V12M12 17V12M17 12H12M12 12H7"/></svg>', cb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>', db = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M10 5V18.5"/><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>';
class Wi {
  /**
   * Creates toolbox buttons and toolbox menus
   *
   * @param {object} api - Editor.js api
   * @param {PopoverItem[]} items - Editor.js api
   * @param {function} onOpen - callback fired when the Popover is opening
   * @param {function} onClose - callback fired when the Popover is closing
   * @param {string} [cssModifier] - the modifier for the Toolbox. Allows to add some specific styles.
   */
  constructor({ api: e, items: t, onOpen: n, onClose: r, cssModifier: a = "" }) {
    this.api = e, this.items = t, this.onOpen = n, this.onClose = r, this.cssModifier = a, this.popover = null, this.wrapper = this.createToolbox();
  }
  /**
   * Style classes
   */
  static get CSS() {
    return {
      toolbox: "tc-toolbox",
      toolboxShowed: "tc-toolbox--showed",
      toggler: "tc-toolbox__toggler"
    };
  }
  /**
   * Returns rendered Toolbox element
   */
  get element() {
    return this.wrapper;
  }
  /**
   * Creating a toolbox to open menu for a manipulating columns
   *
   * @returns {Element}
   */
  createToolbox() {
    const e = Ye("div", [
      Wi.CSS.toolbox,
      this.cssModifier ? `${Wi.CSS.toolbox}--${this.cssModifier}` : ""
    ]);
    e.dataset.mutationFree = "true";
    const t = this.createPopover(), n = this.createToggler();
    return e.appendChild(n), e.appendChild(t), e;
  }
  /**
   * Creates the Toggler
   *
   * @returns {Element}
   */
  createToggler() {
    const e = Ye("div", Wi.CSS.toggler, {
      innerHTML: lb
    });
    return e.addEventListener("click", () => {
      this.togglerClicked();
    }), e;
  }
  /**
   * Creates the Popover instance and render it
   *
   * @returns {Element}
   */
  createPopover() {
    return this.popover = new Ao({
      items: this.items
    }), this.popover.render();
  }
  /**
   * Toggler click handler. Opens/Closes the popover
   *
   * @returns {void}
   */
  togglerClicked() {
    this.popover.opened ? (this.popover.close(), this.onClose()) : (this.popover.open(), this.onOpen());
  }
  /**
   * Shows the Toolbox
   *
   * @param {function} computePositionMethod - method that returns the position coordinate
   * @returns {void}
   */
  show(e) {
    const t = e();
    Object.entries(t).forEach(([n, r]) => {
      this.wrapper.style[n] = r;
    }), this.wrapper.classList.add(Wi.CSS.toolboxShowed);
  }
  /**
   * Hides the Toolbox
   *
   * @returns {void}
   */
  hide() {
    this.popover.close(), this.wrapper.classList.remove(Wi.CSS.toolboxShowed);
  }
}
function hb(o, e) {
  let t = 0;
  return function(...n) {
    const r = (/* @__PURE__ */ new Date()).getTime();
    if (!(r - t < o))
      return t = r, e(...n);
  };
}
const ub = {
  cloud: `<svg xmlns="http://www.w3.org/2000/svg" width="46" height="47" viewBox="0 0 46 47" fill="none">
  <path d="M14.2981 37.5684H10.618C4.98303 37.1659 2.45312 32.8342 2.45312 28.9817C2.45312 25.1292 4.98305 20.7784 10.5222 20.3951C11.3081 20.3184 11.998 20.9317 12.0555 21.7367C12.113 22.5226 11.519 23.2126 10.714 23.2701C6.99562 23.5384 5.32812 26.3367 5.32812 29.0009C5.32812 31.665 6.99562 34.4634 10.714 34.7317H14.2981C15.0839 34.7317 15.7356 35.3834 15.7356 36.1692C15.7356 36.955 15.0839 37.5684 14.2981 37.5684Z" fill="#1F2124"/>
  <path d="M31.9502 37.5683C31.9119 37.5683 31.8927 37.5683 31.8544 37.5683C31.0686 37.5683 30.3403 36.9167 30.3403 36.1308C30.3403 35.3067 30.9536 34.6933 31.7586 34.6933C34.1161 34.6933 36.2244 33.8692 37.8728 32.3933C40.8628 29.7867 41.0544 26.03 40.2494 23.385C39.4444 20.7592 37.2019 17.75 33.2919 17.2708C32.6594 17.1942 32.161 16.715 32.046 16.0825C31.2794 11.4825 28.8069 8.30085 25.0503 7.15085C21.1786 5.94335 16.6552 7.13168 13.8377 10.0833C11.0969 12.9392 10.4644 16.945 12.0552 21.3534C12.3236 22.1009 11.9403 22.925 11.1928 23.1933C10.4453 23.4617 9.6211 23.0783 9.35277 22.3308C7.41694 16.9258 8.29862 11.7508 11.7678 8.10918C15.3136 4.39085 21.0061 2.915 25.8936 4.41C30.3786 5.79 33.541 9.48917 34.691 14.6258C38.601 15.5075 41.7444 18.4784 42.9902 22.58C44.3511 27.0459 43.1244 31.6459 39.7703 34.5592C37.6428 36.4759 34.8636 37.5683 31.9502 37.5683Z" fill="#1F2124"/>
  <path d="M22.9993 43.2034C19.1468 43.2034 15.5435 41.1525 13.5694 37.8367C13.3585 37.5109 13.1477 37.1275 12.9752 36.7059C12.3236 35.345 11.9785 33.7925 11.9785 32.1825C11.9785 26.1067 16.9235 21.1617 22.9993 21.1617C29.0752 21.1617 34.0202 26.1067 34.0202 32.1825C34.0202 33.8117 33.6752 35.345 32.9852 36.7634C32.8319 37.1275 32.6211 37.5109 32.3911 37.875C30.4552 41.1525 26.8518 43.2034 22.9993 43.2034ZM22.9993 24.0367C18.5143 24.0367 14.8535 27.6975 14.8535 32.1825C14.8535 33.3709 15.1027 34.4825 15.5819 35.4984C15.7352 35.8242 15.8693 36.0925 16.0227 36.3417C17.4793 38.8142 20.1435 40.3284 22.9802 40.3284C25.8168 40.3284 28.481 38.8142 29.9185 36.3801C30.091 36.0926 30.2444 35.8242 30.3594 35.5559C30.8769 34.5017 31.126 33.39 31.126 32.2017C31.1452 27.6975 27.4843 24.0367 22.9993 24.0367Z" fill="#1F2124"/>
  <path d="M21.9064 35.5174C21.5423 35.5174 21.1781 35.3833 20.8906 35.0958L18.993 33.1983C18.4372 32.6424 18.4372 31.7224 18.993 31.1666C19.5489 30.6108 20.4689 30.6108 21.0247 31.1666L21.9448 32.0866L25.0114 29.2499C25.6056 28.7133 26.5064 28.7516 27.0431 29.3266C27.5797 29.9016 27.5414 30.8216 26.9664 31.3583L22.8839 35.1341C22.5964 35.3833 22.2514 35.5174 21.9064 35.5174Z" fill="#1F2124"/>
  </svg>`
};
class Qs {
  constructor(e, t) {
    return this.api = e, this.target = null, this.parentWrapper = t, this.uploadFileWrapper = null, this.form = null, this.files = [], Qs.instance || (this.initModal(), Qs.instance = this, this.api.listeners.on(this.form, "submit", (n) => this.formSubmit(n))), Qs.instance;
  }
  initModal() {
    this.modalContainer = Ye("div", "modal-container"), this.form = Ye("form", "form-container"), this.form.appendChild(this.modalContainer);
    const e = Ye("div", "modal-content");
    this.modalContainer.appendChild(e);
    const t = Ye("div", "modal-header", {
      innerHTML: '<div class="modal-title"><h2>Upload Files</h2><p>Select and upload the files of your choice</p></div><span class="close">&times;</span>'
    });
    e.appendChild(t);
    const n = Ye("div", "modal-body"), r = Ye("div", "section-upload");
    this.uploadFileWrapper = Ye("div", "uploaded-files-list");
    const a = Ye("div", "upload-left"), c = Ye("input", "", { type: "file", multiple: !0 }), u = Ye("div", "upload-icon", {
      innerHTML: ub.cloud
    }), p = Ye("div", "upload-info", {
      innerHTML: "<h4>Choose a file or drag & drop it here</h4><p>JPEG, PNG, PDF, and DOCs formats, up to 50MB</p>"
    });
    a.appendChild(c), a.appendChild(u), a.appendChild(p);
    const g = Ye("div", "upload-right"), v = Ye("button", "btn-browse", { textContent: "Browse Files" });
    g.appendChild(v), r.appendChild(a), r.appendChild(g), n.appendChild(r), n.appendChild(this.uploadFileWrapper);
    const k = Ye("div", "modal-footer"), y = Ye("button", "btn-link", {
      innerHTML: "Upload Files",
      type: "submit"
    });
    k.appendChild(y), this.api.listeners.on(y, "click", () => {
    });
    const T = t.querySelector(".close");
    this.api.listeners.on(T, "click", () => this.hide()), e.appendChild(n), e.appendChild(k), this.parentWrapper.appendChild(this.form), this.api.listeners.on(window, "click", (x) => {
      x.target === this.modalContainer && this.hide();
    }), this.setupDragAndDrop(r), this.api.listeners.on(c, "change", (x) => {
      this.handleFiles(x.target.files);
    });
  }
  setupDragAndDrop(e) {
    e.addEventListener("dragover", (t) => {
      t.preventDefault(), e.classList.add("drag-over");
    }), e.addEventListener("dragleave", (t) => {
      t.preventDefault(), e.classList.remove("drag-over");
    }), e.addEventListener("drop", (t) => {
      t.preventDefault(), e.classList.remove("drag-over");
      const n = t.dataTransfer.files;
      this.handleFiles(n);
    });
  }
  handleFiles(e) {
    for (const t of e) {
      const n = new FileReader();
      n.onload = (r) => {
        this.displayPreview(t, r.target.result), this.files.push(r.target.result);
      }, n.readAsDataURL(t);
    }
  }
  displayPreview(e, t) {
    const n = Ye("div", "uploaded-item"), r = Ye("div", "col-50"), a = Ye("div", "uploaded-file-image", {
      innerHTML: `<img src="${t}" alt="${e.name}" />`
    });
    r.appendChild(a), n.appendChild(r);
    const u = Ye("div", "col-50", {
      innerHTML: `
    <h4>Document Action</h4>
    <div class="uploaded-file-form">
      <div class="form-row">
        <div class="form-input">
          <select class="input-control" name="prefix">
            <option value="">Select Report Prefix</option>
            <option value="option 1">option 1</option>
            <option value="option 2">option 2</option>
          </select>
        </div>
        <div class="form-input">
          <input class="input-control" name="date" type="date" placeholder="DD.MM.YYYY" required />
        </div>
      </div>
      <div class="form-input">
        <input class="input-control" name="doc_name" type="text" placeholder="Name of Document" required />
      </div>
      <div class="form-input">
        <textarea class="input-control" name="comment" rows="3" placeholder="Write your comment here!"></textarea>
      </div>
      </div>

    `
    });
    n.appendChild(u), this.uploadFileWrapper.appendChild(n);
  }
  formSubmit(e) {
    e.preventDefault(), e.stopPropagation();
    const t = e.target.querySelectorAll(".uploaded-item"), n = [];
    t.forEach((a) => {
      const c = a.querySelectorAll("input, date, select, textarea"), u = {};
      c.forEach((p, g) => {
        p.type !== "file" && (u[p.name] = p.value);
      }), n.push(u);
    }), this.target.value = JSON.stringify(n);
    const r = new CustomEvent("fileChanged", { detail: JSON.stringify(n) });
    this.target.dispatchEvent(r), this.hide();
  }
  show(e) {
    this.target = e, this.modalContainer.style.display = "block";
  }
  hide() {
    this.modalContainer.style.display = "none", this.uploadFileWrapper.innerHTML = "";
  }
}
const _l = {
  attach: '<svg width="12" height="12" viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">    <path fill-rule="evenodd" clip-rule="evenodd"      d="M11.01 1.73243C10.35 1.07243 9.4725 0.742432 8.595 0.742432C7.7325 0.742432 6.8625 1.07243 6.2025 1.73243L0.7125 7.22243C0.24 7.69493 0 8.31743 0 8.93243C0 9.54743 0.2325 10.1774 0.7125 10.6499C1.1775 11.1149 1.8 11.3474 2.4225 11.3474C3.045 11.3474 3.6675 11.1149 4.1475 10.6349L9.63 5.14493C10.2 4.56743 10.2 3.65993 9.6375 3.09743C9.075 2.53493 8.145 2.52743 7.575 3.09743L3.0225 7.65743V7.65743C2.8425 7.84493 2.8425 8.14493 3.03 8.33243C3.2175 8.51993 3.5175 8.51993 3.7125 8.33993V8.33993L8.265 3.77993C8.4525 3.59243 8.7675 3.59243 8.9475 3.77243C9.135 3.95993 9.135 4.27493 8.9475 4.46243L3.465 9.95243C2.9025 10.5149 1.935 10.5074 1.395 9.95993C0.8325 9.39743 0.8475 8.44493 1.4025 7.88993L6.9 2.40743C7.83 1.47743 9.4125 1.46243 10.335 2.38493C11.265 3.31493 11.265 4.90493 10.335 5.83493L4.995 11.1824V11.1824C4.815 11.3699 4.815 11.6624 5.0025 11.8424C5.1825 12.0224 5.475 12.0224 5.6625 11.8499V11.8574L11.01 6.50993C11.67 5.86493 12 5.00243 12 4.13243C12 3.26243 11.67 2.39243 11.01 1.73243Z"      fill="#3366FF" />  </svg>',
  close: '<svg xmlns="http://www.w3.org/2000/svg" width="8" height="8" viewBox="0 0 8 8"  fill="none">  <path      d="M1.11984 7.53812L3.86672 4.79125L6.61359 7.53812L7.53812 6.61359L4.79125 3.86672L7.53812 1.11984L6.61359 0.195312L3.86672 2.94219L1.11984 0.195312L0.195312 1.11984L2.94219 3.86672L0.195312 6.61359L1.11984 7.53812Z"      fill="#FEFEFE" /></svg>',
  view: '<svg xmlns="http://www.w3.org/2000/svg" width="12" height="10" viewBox="0 0 12 10" fill="none"><path d="M5.99896 0.333496C4.31577 0.333496 2.96797 1.1001 1.98681 2.01252C1.01189 2.91661 0.359865 3.99984 0.051558 4.74353C-0.017186 4.9081 -0.017186 5.09142 0.051558 5.25598C0.359865 5.99967 1.01189 7.08291 1.98681 7.987C2.96797 8.89942 4.31577 9.66602 5.99896 9.66602C7.68215 9.66602 9.02995 8.89942 10.0111 7.987C10.986 7.08083 11.6381 5.99967 11.9484 5.25598C12.0172 5.09142 12.0172 4.9081 11.9484 4.74353C11.6381 3.99984 10.986 2.91661 10.0111 2.01252C9.02995 1.1001 7.68215 0.333496 5.99896 0.333496ZM2.99922 4.99976C2.99922 4.20418 3.31526 3.44118 3.87782 2.87862C4.44038 2.31606 5.20338 2.00002 5.99896 2.00002C6.79454 2.00002 7.55753 2.31606 8.1201 2.87862C8.68266 3.44118 8.9987 4.20418 8.9987 4.99976C8.9987 5.79534 8.68266 6.55833 8.1201 7.12089C7.55753 7.68345 6.79454 7.9995 5.99896 7.9995C5.20338 7.9995 4.44038 7.68345 3.87782 7.12089C3.31526 6.55833 2.99922 5.79534 2.99922 4.99976ZM5.99896 3.66654C5.99896 4.40189 5.40109 4.99976 4.66574 4.99976C4.51784 4.99976 4.37618 4.97476 4.24286 4.93101C4.12829 4.89352 3.99497 4.96434 3.99913 5.08517C4.00538 5.2289 4.02621 5.37264 4.06579 5.51638C4.35119 6.58295 5.44901 7.21623 6.51558 6.93084C7.58215 6.64545 8.21544 5.54763 7.93004 4.48105C7.69881 3.61654 6.93429 3.03534 6.08437 2.99993C5.96355 2.99576 5.89272 3.127 5.93022 3.24366C5.97396 3.37698 5.99896 3.51864 5.99896 3.66654Z" fill="#FEFEFE" /></svg>'
};
class pb {
  constructor(e, t, n, r) {
    this.api = e, this.parentElement = t, this.dropContainer = null, this.uploadListContainer = null, this.uploadBoxContainer = null, this.dropdownMenu = null, this.uploadModal = r, this.initUploadBox(), this.target = n, n != null && n.value && this.renderFile(n == null ? void 0 : n.value), this.attachEventListeners();
  }
  addFileToList(e) {
    const t = Ye("span", ["upload-file-name"], {
      innerHTML: `${e}<div class="btn-icon-group">
            <button class="btn-icon">
                
            ${_l.view}
            </button>
            <button class="btn-icon">
            ${_l.close}
            </button>
        </div>`
    });
    this.uploadListContainer.appendChild(t);
  }
  initUploadBox() {
    this.uploadBoxContainer = Ye("div", "upload-box"), this.parentElement.appendChild(this.uploadBoxContainer), this.uploadListContainer = Ye("div", "upload-file-list"), this.uploadBoxContainer.appendChild(this.uploadListContainer), this.dropContainer = Ye("div", "drop-container"), this.uploadBoxContainer.appendChild(this.dropContainer), this.dropButton = Ye("button", ["btn-drop-down", "select"], {
      innerHTML: _l.attach
    }), this.dropContainer.appendChild(this.dropButton), this.dropdownMenu = Ye("div", "dropdown-menu");
    const e = Ye("ul", "drop-items"), t = Ye("li", "", {
      innerHTML: "Link Reports"
    }), n = Ye("li", "", {
      innerHTML: "Upload Files"
    });
    e.appendChild(t), e.appendChild(n), this.dropdownMenu.appendChild(e), this.api.listeners.on(t, "click", () => {
      this.hideMenu();
    }), this.api.listeners.on(n, "click", () => {
      this.hideMenu(), this.uploadModal.show(this.target);
    }), this.dropContainer.appendChild(this.dropdownMenu), this.api.listeners.on(window, "click", (r) => {
      r.target === this.dropdownMenu && this.hideMenu();
    });
  }
  hideMenu() {
    document.querySelectorAll(".menu-open").forEach((e) => e.classList.remove("menu-open"));
  }
  renderFile(e = "") {
    var n;
    let t = [];
    (n = this.target) != null && n.value && (t = JSON.parse(this.target.value)), e && (t = JSON.parse(e), this.uploadListContainer.innerHTML = ""), this.uploadListContainer.innerHTML = "", t == null || t.map((r) => this.addFileToList(`${r == null ? void 0 : r.prefix}-${r == null ? void 0 : r.doc_name}-${r == null ? void 0 : r.date}.png`));
  }
  attachEventListeners() {
    this.api.listeners.on(this.dropButton, "click", () => {
      this.hideMenu(), this.dropdownMenu.classList.toggle("menu-open");
    });
  }
}
const Ke = {
  wrapper: "tc-wrap",
  wrapperReadOnly: "tc-wrap--readonly",
  table: "tc-table",
  row: "tc-row",
  withHeadings: "tc-table--heading",
  rowSelected: "tc-row--selected",
  cell: "tc-cell",
  cellSelected: "tc-cell--selected",
  addRow: "tc-add-row",
  addColumn: "tc-add-column",
  formInput: "tr-form-input"
};
class fb {
  /**
   * Creates
   *
   * @constructor
   * @param {boolean} readOnly - read-only mode flag
   * @param {object} api - Editor.js API
   * @param {TableData} data - Editor.js API
   * @param {TableConfig} config - Editor.js API
   */
  constructor(e, t, n, r, a) {
    var c;
    if (this.readOnly = e, this.api = t, this.block = a, this.config = r, this.isJapan = ((c = this.config) == null ? void 0 : c.locale) == "jp", n.content.length > 1) {
      const u = r.data.data.length;
      n.content.map((p, g) => p.splice(u));
    }
    n.heading = r.data.data.map((u) => this.isJapan ? u.heading_jp : u.heading), this.data = n, this.wrapper = null, this.table = null, this.interval = null, this.toolboxRow = this.createRowToolbox(), this.toolBoxDue = null, this.createTableWrapper(), this.hoveredRow = 0, this.hoveredColumn = 0, this.selectedRow = 0, this.selectedColumn = 0, this.tunes = {
      withHeadings: !1
    }, this.resize(), this.fill(), this.modal = new Qs(this.api, this.wrapper), setTimeout(() => {
      this.checkInput();
    }, 0), this.focusedCell = {
      row: 0,
      column: 0
    }, this.documentClicked = (u) => {
      const p = u.target.closest(`.${Ke.table}`) !== null, g = u.target.closest(`.${Ke.wrapper}`) === null;
      (p || g) && this.hideToolboxes();
      const k = u.target.closest(`.${Ke.addRow}`), y = u.target.closest(`.${Ke.addColumn}`);
      k && k.parentNode === this.wrapper ? (this.addRow(void 0, !0), this.hideToolboxes()) : y && y.parentNode === this.wrapper && this.hideToolboxes();
    }, this.readOnly || this.bindEvents();
  }
  createModal() {
    const e = document.createElement("div");
    e.id = "myModal", e.className = "modal";
    const t = document.createElement("div");
    t.className = "modal-content";
    const n = document.createElement("div");
    n.className = "modal-header";
    const r = document.createElement("span");
    r.className = "close", r.innerHTML = "&times;";
    const a = document.createElement("h2");
    a.textContent = "Modal Header", n.appendChild(r), n.appendChild(a);
    const c = document.createElement("div");
    c.className = "modal-body";
    const u = document.createElement("p");
    u.textContent = "Some text in the Modal Body";
    const p = document.createElement("p");
    p.textContent = "Some other text...", c.appendChild(u), c.appendChild(p);
    const g = document.createElement("div");
    g.className = "modal-footer";
    const v = document.createElement("h3");
    return v.textContent = "Modal Footer", g.appendChild(v), t.appendChild(n), t.appendChild(c), t.appendChild(g), e.appendChild(t), this.api.listeners.on(r, "click", () => {
      e.style.display = "none";
    }), this.api.listeners.on(window, "click", (k) => {
      k.target == e && (e.style.display = "none");
    }), e;
  }
  checkInput() {
    this.table.querySelectorAll(`.${Ke.row}`).forEach((e, t) => {
      var n;
      t != 0 && ((n = e == null ? void 0 : e.firstChild) == null ? void 0 : n.childElementCount) == 0 && this.createColumnInput(t);
    });
  }
  createColumnInput(e) {
    var t, n, r, a;
    this.isJapan = ((t = this.config) == null ? void 0 : t.locale) == "jp";
    for (let c = 0; c < this.config.data.data.length; c++) {
      const u = this.config.data.data[c], p = this.getCell(e + 1, c + 1);
      if (!p)
        return;
      const g = ((a = (r = (n = this.data) == null ? void 0 : n.content) == null ? void 0 : r[e - 1]) == null ? void 0 : a[u.key]) || (u == null ? void 0 : u.default) || "";
      if (u.type == "search") {
        const v = Ye("input", Ke.formInput, {
          value: g,
          disabled: this.readOnly,
          name: u.key
        });
        this.api.listeners.on(v, "keyup", (k) => {
          const T = k.target.value.trim().replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
          clearTimeout(this.interval), this.interval = setTimeout(() => {
            this.fetchData(T, v);
          }, 500);
        }), p.appendChild(v);
      } else if (u.type == "select") {
        const v = Ye("select", Ke.formInput, {
          value: g,
          disabled: this.readOnly,
          name: u.key
        });
        let k = !1;
        u.options.map((y) => {
          y.label == g && (k = !0);
          const T = Ye("option", "", {
            label: this.isJapan ? y.label_jp : y.label,
            value: y.label,
            selected: g == y.label
          });
          v.appendChild(T);
        }), k || v.prepend(Ye("option", "", {
          label: g,
          value: g,
          selected: !0
        })), this.api.listeners.on(v, "change", (y) => {
          var T, x;
          if (y.target.value == "Others") {
            const M = y.target.parentElement, L = Ye("input", Ke.formInput, {
              placeholder: this.api.i18n.t("Enter text")
            });
            this.api.listeners.on(L, "focusout", () => {
              this.block.dispatchChange();
            }), (x = (T = M.children) == null ? void 0 : T[0]) == null || x.remove(), M.appendChild(L);
          }
          this.block.dispatchChange();
        }), p.appendChild(v);
      } else if (u.type == "text") {
        const v = Ye("input", Ke.formInput, {
          value: g,
          disabled: this.readOnly,
          name: u.key
        });
        p.appendChild(v);
      } else if (u.type == "checkbox") {
        const v = Ye("input", Ke.formInput, {
          checked: g,
          type: "checkbox",
          disabled: this.readOnly,
          name: u.key
        });
        this.api.listeners.on(v, "change", () => {
          this.block.dispatchChange();
        }), p.appendChild(v);
      } else if (u.type == "number") {
        const v = Ye("input", Ke.formInput, {
          value: g,
          type: "number",
          disabled: this.readOnly,
          name: u.key
        });
        this.api.listeners.on(v, "keyup", () => {
          clearTimeout(this.interval), this.interval = setTimeout(() => {
            this.block.dispatchChange();
          }, 500);
        }), p.appendChild(v);
      } else if (u.type == "customFile") {
        const v = Ye("input", Ke.formInput, {
          type: "hidden",
          value: g,
          disabled: this.readOnly,
          name: u.key
        });
        let k = new pb(this.api, p, v, this.modal);
        this.api.listeners.on(v, "fileChanged", (y) => {
          console.log(y), v.value = y.detail, k.renderFile();
        }), p.appendChild(v);
      }
    }
  }
  // Function to create a button with an SVG icon
  createButton(e) {
    const t = document.createElement("button");
    return t.classList.add("btn-icon"), t.innerHTML = e, t;
  }
  // Function to create file name span with icons
  createFileName(e) {
    const t = document.createElement("span");
    t.classList.add("upload-file-name"), t.textContent = e;
    const n = document.createElement("div");
    return n.classList.add("btn-icon-group"), [
      '<svg xmlns="http://www.w3.org/2000/svg" width="8" height="8" viewBox="0 0 8 8" fill="none"><path d="M1.11984 7.53812L3.86672 4.79125L6.61359 7.53812L7.53812 6.61359L4.79125 3.86672L7.53812 1.11984L6.61359 0.195312L3.86672 2.94219L1.11984 0.195312L0.195312 1.11984L2.94219 3.86672L0.195312 6.61359L1.11984 7.53812Z" fill="#FEFEFE"/></svg>',
      '<svg xmlns="http://www.w3.org/2000/svg" width="12" height="10" viewBox="0 0 12 10" fill="none"><path d="M5.99896 0.333496C4.31577 0.333496 2.96797 1.1001 1.98681 2.01252C1.01189 2.91661 0.359865 3.99984 0.051558 4.74353C-0.017186 4.9081 -0.017186 5.09142 0.051558 5.25598C0.359865 5.99967 1.01189 7.08291 1.98681 7.987C2.96797 8.89942 4.31577 9.66602 5.99896 9.66602C7.68215 9.66602 9.02995 8.89942 10.0111 7.987C10.986 7.08083 11.6381 5.99967 11.9484 5.25598C12.0172 5.09142 12.0172 4.9081 11.9484 4.74353C11.6381 3.99984 10.986 2.91661 10.0111 2.01252C9.02995 1.1001 7.68215 0.333496 5.99896 0.333496ZM2.99922 4.99976C2.99922 4.20418 3.31526 3.44118 3.87782 2.87862C4.44038 2.31606 5.20338 2.00002 5.99896 2.00002C6.79454 2.00002 7.55753 2.31606 8.1201 2.87862C8.68266 3.44118 8.9987 4.20418 8.9987 4.99976C8.9987 5.79534 8.68266 6.55833 8.1201 7.12089C7.55753 7.68345 6.79454 7.9995 5.99896 7.9995C5.20338 7.9995 4.44038 7.68345 3.87782 7.12089C3.31526 6.55833 2.99922 5.79534 2.99922 4.99976ZM5.99896 3.66654C5.99896 4.40189 5.40109 4.99976 4.66574 4.99976C4.51784 4.99976 4.37618 4.97476 4.24286 4.93101C4.12829 4.89352 3.99497 4.96434 3.99913 5.08517C4.00538 5.2289 4.02621 5.37264 4.06579 5.51638C4.35119 6.58295 5.44901 7.21623 6.51558 6.93084C7.58215 6.64545 8.21544 5.54763 7.93004 4.48105C7.69881 3.61654 6.93429 3.03534 6.08437 2.99993C5.96355 2.99576 5.89272 3.127 5.93022 3.24366C5.97396 3.37698 5.99896 3.51864 5.99896 3.66654Z" fill="#FEFEFE"/></svg>'
    ].forEach((a) => {
      n.appendChild(this.createButton(a));
    }), t.appendChild(n), t;
  }
  createUploadBox() {
    const e = document.createElement("div");
    e.classList.add("upload-box");
    const t = document.createElement("div");
    return t.classList.add("upload-file-list"), e.appendChild(t), e.appendChild(this.createDropdown()), e;
  }
  createDropdown() {
    const e = document.createElement("div");
    e.classList.add("drop-container");
    const t = document.createElement("button");
    t.classList.add("btn-drop-down", "select"), t.innerHTML = `
      <svg width="12" height="12" viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path fill-rule="evenodd" clip-rule="evenodd"
          d="M11.01 1.73243C10.35 1.07243 9.4725 0.742432 8.595 0.742432C7.7325 0.742432 6.8625 1.07243 6.2025 1.73243L0.7125 7.22243C0.24 7.69493 0 8.31743 0 8.93243C0 9.54743 0.2325 10.1774 0.7125 10.6499C1.1775 11.1149 1.8 11.3474 2.4225 11.3474C3.045 11.3474 3.6675 11.1149 4.1475 10.6349L9.63 5.14493C10.2 4.56743 10.2 3.65993 9.6375 3.09743C9.075 2.53493 8.145 2.52743 7.575 3.09743L3.0225 7.65743V7.65743C2.8425 7.84493 2.8425 8.14493 3.03 8.33243C3.2175 8.51993 3.5175 8.51993 3.7125 8.33993V8.33993L8.265 3.77993C8.4525 3.59243 8.7675 3.59243 8.9475 3.77243C9.135 3.95993 9.135 4.27493 8.9475 4.46243L3.465 9.95243C2.9025 10.5149 1.935 10.5074 1.395 9.95993C0.8325 9.39743 0.8475 8.44493 1.4025 7.88993L6.9 2.40743C7.83 1.47743 9.4125 1.46243 10.335 2.38493C11.265 3.31493 11.265 4.90493 10.335 5.83493L4.995 11.1824V11.1824C4.815 11.3699 4.815 11.6624 5.0025 11.8424C5.1825 12.0224 5.475 12.0224 5.6625 11.8499V11.8574L11.01 6.50993C11.67 5.86493 12 5.00243 12 4.13243C12 3.26243 11.67 2.39243 11.01 1.73243Z"
          fill="#3366FF" />
      </svg>
    `;
    const n = document.createElement("div");
    n.classList.add("dropdown-menu"), this.api.listeners.on(t, "click", (u) => {
      u.stopPropagation(), this.hideMenu(), n.classList.toggle("menu-open");
    });
    const r = document.createElement("ul");
    r.classList.add("drop-items");
    const a = document.createElement("li");
    a.textContent = "Link Reports";
    const c = document.createElement("li");
    return c.textContent = "Upload Files", this.api.listeners.on(a, "click", () => {
      this.modal.style.display = "block", n.classList.remove("menu-open");
    }), this.api.listeners.on(c, "click", () => {
      this.modal.style.display = "block", n.classList.remove("menu-open");
    }), r.appendChild(a), r.appendChild(c), n.appendChild(r), e.appendChild(t), e.appendChild(n), e;
  }
  async fetchData(e = "", t) {
    const n = new Headers();
    n.append("Content-Type", "application/json");
    const r = JSON.stringify({
      search: e
    }), a = {
      method: "POST",
      headers: n,
      body: r
    }, c = await fetch(this.config.apiUrl, a).then((p) => p.json()).catch((p) => console.log("error", p)), u = [{
      label: this.api.i18n.t("Delete row"),
      icon: Wd,
      hideIf: () => this.numberOfRows === 1,
      confirmationRequired: !0,
      onClick: () => {
        this.deleteRow(this.focusedCell.row), this.hideToolboxes();
      }
    }];
    this.focusedCell.row - 1, c.data.map((p) => {
      u.push({
        label: p.medicine,
        icon: sb,
        confirmationRequired: !1,
        onClick: (g) => {
          p.medicine && (t.value = p.medicine), this.hideToolboxes(), this.block.dispatchChange();
        }
      });
    }), this.renderToolBox(u);
  }
  /**
   * Returns the rendered table wrapper
   *
   * @returns {Element}
   */
  getWrapper() {
    return this.wrapper;
  }
  /**
   * Hangs the necessary handlers to events
   */
  bindEvents() {
    this.api.listeners.on(window, "click", (e) => {
      e.target.closest(`.${Ke.wrapper}`) === null && document.querySelectorAll(".menu-open").forEach((t) => t.classList.remove("menu-open"));
    }), this.api.listeners.on(this.wrapper, "click", this.documentClicked), this.api.listeners.on(this.table, "mousemove", hb(150, (e) => this.onMouseMoveInTable(e)), { passive: !0 }), this.table.onkeypress = (e) => this.onKeyPressListener(e), this.api.listeners.on(this.table, "keydown", (e) => this.onKeyDownListener(e)), this.api.listeners.on(this.table, "keyup", (e) => this.onKeyUpListener(e)), this.api.listeners.on(this.table, "focusin", (e) => this.focusInTableListener(e));
  }
  /**
  * Configures and creates the toolbox for manipulating with rows
  *
  * @returns {Toolbox}
  */
  createRowToolbox() {
    return new Wi({
      api: this.api,
      cssModifier: "row",
      items: [
        {
          label: this.api.i18n.t("Add row above"),
          icon: ab,
          onClick: () => {
            this.selectedRow != 1 && (this.addRow(this.selectedRow, !0), this.hideToolboxes());
          }
        },
        {
          label: this.api.i18n.t("Add row below"),
          icon: rb,
          onClick: () => {
            this.addRow(this.selectedRow + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete row"),
          icon: Wd,
          hideIf: () => this.numberOfRows === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteRow(this.selectedRow), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  createCustomToolbox(e = []) {
    return new Wi({
      table: this,
      api: this.api,
      cssModifier: "row",
      items: e,
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  renderToolBox(e = []) {
    this.toolBoxDue && this.toolBoxDue.element.remove(), this.toolBoxDue = this.createCustomToolbox(e), this.wrapper.appendChild(this.toolBoxDue.element), this.toolBoxDue.togglerClicked(), this.toolBoxDue.show(() => this.getUpdateLocation());
  }
  getUpdateLocation() {
    const e = this.hoveredRow, t = this.hoveredColumn, n = this.getRow(e), { fromTopBorder: r } = Tl(this.table, n), { height: a } = n.getBoundingClientRect();
    return {
      top: `${Math.ceil(r + a / 0.8)}px`,
      left: `calc((100% - var(--cell-size)) / (${this.numberOfColumns} * 2) * ( (${t} - 1) * 2))`
    };
  }
  /**
   * When you press enter it moves the cursor down to the next row
   * or creates it if the click occurred on the last one
   */
  moveCursorToNextRow() {
    this.focusedCell.row !== this.numberOfRows ? (this.focusedCell.row += 1, this.focusCell(this.focusedCell)) : (this.addRow(), this.focusedCell.row += 1, this.focusCell(this.focusedCell), this.updateToolboxesPosition(0, 0));
  }
  /**
   * Get table cell by row and col index
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @returns {HTMLElement}
   */
  getCell(e, t) {
    return this.table.querySelectorAll(`.${Ke.row}:nth-child(${e}) .${Ke.cell}`)[t - 1];
  }
  /**
   * Get table row by index
   *
   * @param {number} row - row coordinate
   * @returns {HTMLElement}
   */
  getRow(e) {
    return this.table.querySelector(`.${Ke.row}:nth-child(${e})`);
  }
  /**
   * The parent of the cell which is the row
   *
   * @param {HTMLElement} cell - cell element
   * @returns {HTMLElement}
   */
  getRowByCell(e) {
    return e.parentElement;
  }
  /**
   * Ger row's first cell
   *
   * @param {Element} row - row to find its first cell
   * @returns {Element}
   */
  getRowFirstCell(e) {
    return e.querySelector(`.${Ke.cell}:first-child`);
  }
  /**
   * Set the sell's content by row and column numbers
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @param {string} content - cell HTML content
   */
  setCellContent(e, t, n) {
    const r = this.getCell(e, t);
    "innerHTML" in r && (r.innerHTML = n);
  }
  /**
   * Add column in table on index place
   * Add cells in each row
   *
   * @param {number} columnIndex - number in the array of columns, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the first cell
   */
  addColumn(e = -1, t = !1) {
    let n = this.numberOfColumns;
    for (let r = 1; r <= this.numberOfRows; r++) {
      let a;
      const c = this.createCell();
      if (e > 0 && e <= n ? (a = this.getCell(r, e), Fd(c, a)) : a = this.getRow(r).appendChild(c), r === 1) {
        const u = this.getCell(r, e > 0 ? e : n + 1);
        u && t && zd(u);
      }
    }
    this.addHeadingAttrToFirstRow();
  }
  /**
   * Add row in table on index place
   *
   * @param {number} index - number in the array of rows, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the inserted row
   * @returns {HTMLElement} row
   */
  addRow(e = -1, t = !1) {
    let n, r = Ye("div", Ke.row);
    this.tunes.withHeadings && this.removeHeadingAttrFromFirstRow();
    let a = this.numberOfColumns;
    if (e > 0 && e <= this.numberOfRows) {
      let u = this.getRow(e);
      n = Fd(r, u);
    } else
      n = this.table.appendChild(r);
    this.fillRow(n, a), this.tunes.withHeadings && this.addHeadingAttrToFirstRow();
    const c = this.getRowFirstCell(n);
    return c && t && zd(c), this.checkInput(), n;
  }
  /**
   * Delete a column by index
   *
   * @param {number} index
   */
  deleteColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      if (!n)
        return;
      n.remove();
    }
  }
  /**
   * Delete a row by index
   *
   * @param {number} index
   */
  deleteRow(e) {
    this.getRow(e).remove(), this.addHeadingAttrToFirstRow();
  }
  /**
   * Create a wrapper containing a table, toolboxes
   * and buttons for adding rows and columns
   *
   * @returns {HTMLElement} wrapper - where all buttons for a table and the table itself will be
   */
  createTableWrapper() {
    if (this.wrapper ? this.table.remove() : this.wrapper = Ye("div", Ke.wrapper), this.table = Ye("div", Ke.table), this.wrapper.classList.add(Ke.wrapperReadOnly), this.wrapper.appendChild(this.toolboxRow.element), this.wrapper.appendChild(this.table), !this.readOnly) {
      const e = Ye("div", Ke.addColumn, {
        innerHTML: Ud
      }), t = Ye("div", Ke.addRow, {
        innerHTML: Ud
      });
      this.wrapper.appendChild(e), this.wrapper.appendChild(t);
    }
  }
  /**
   * Returns the size of the table based on initial data or config "size" property
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  computeInitialSize() {
    const e = this.data && this.data.content, t = Array.isArray(e), n = t ? e.length : !1, r = t ? e.length : void 0, a = n ? e[0].length : void 0, c = Number.parseInt(this.config && this.config.rows), u = Number.parseInt(this.config && this.config.cols), p = !isNaN(c) && c > 0 ? c : void 0, g = !isNaN(u) && u > 0 ? u : void 0;
    return {
      rows: r || p || 2,
      cols: a || g || 2
    };
  }
  /**
   * Resize table to match config size or transmitted data size
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  resize() {
    const e = (this.data.content.length || 1) + 1, t = this.data.heading.length;
    for (let n = 0; n < e; n++)
      this.addRow();
    for (let n = 0; n < t; n++)
      this.addColumn();
  }
  /**
   * Fills the table with data passed to the constructor
   *
   * @returns {void}
   */
  fill() {
    const e = this.data;
    if (e && e.heading)
      for (let t = 0; t < e.heading.length; t++)
        this.setCellContent(1, t + 1, e.heading[t]);
  }
  /**
   * Fills a row with cells
   *
   * @param {HTMLElement} row - row to fill
   * @param {number} numberOfColumns - how many cells should be in a row
   */
  fillRow(e, t) {
    for (let n = 1; n <= t; n++) {
      const r = this.createCell();
      e.appendChild(r);
    }
  }
  /**
   * Creating a cell element
   *
   * @return {Element}
   */
  createCell() {
    return Ye("div", Ke.cell, {
      contentEditable: !1
    });
  }
  /**
   * Get number of rows in the table
   */
  get numberOfRows() {
    return this.table.childElementCount;
  }
  /**
   * Get number of columns in the table
   */
  get numberOfColumns() {
    return this.numberOfRows ? this.table.querySelectorAll(`.${Ke.row}:first-child .${Ke.cell}`).length : 0;
  }
  /**
   * Is the column toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isColumnMenuShowing() {
    return this.selectedColumn !== 0;
  }
  /**
   * Is the row toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isRowMenuShowing() {
    return this.selectedRow !== 0;
  }
  /**
   * Recalculate position of toolbox icons
   *
   * @param {Event} event - mouse move event
   */
  onMouseMoveInTable(e) {
    const { row: t, column: n } = this.getHoveredCell(e);
    this.hoveredColumn = n, this.hoveredRow = t, this.updateToolboxesPosition();
  }
  /**
   * Prevents default Enter behaviors
   * Adds Shift+Enter processing
   *
   * @param {KeyboardEvent} event - keypress event
   */
  onKeyPressListener(e) {
    return e.key === "Enter" && e.shiftKey ? !0 : e.key !== "Enter";
  }
  /**
   * Prevents tab keydown event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyDownListener(e) {
    e.key === "Tab" && e.stopPropagation();
  }
  /**
   * Prevents tab keyup event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyUpListener(e) {
    if (e.key === "Tab") {
      e.stopPropagation();
      return;
    }
    if (e.key === "Enter") {
      e.stopPropagation();
      return;
    }
  }
  /**
   * Set the coordinates of the cell that the focus has moved to
   *
   * @param {FocusEvent} event - focusin event
   */
  focusInTableListener(e) {
    const t = e.target, n = this.getRowByCell(t);
    this.focusedCell = {
      row: Array.from(this.table.querySelectorAll(`.${Ke.row}`)).indexOf(n) + 1,
      column: Array.from(n.querySelectorAll(`.${Ke.cell}`)).indexOf(t) + 1
    };
  }
  /**
   * Unselect row/column
   * Close toolbox menu
   * Hide toolboxes
   *
   * @returns {void}
   */
  hideToolboxes() {
    this.hideRowToolbox(), this.updateToolboxesPosition();
  }
  /**
   * Unselect row, close toolbox
   *
   * @returns {void}
   */
  hideRowToolbox() {
    var e;
    this.unselectRow(), this.toolboxRow.hide(), (e = this.toolBoxDue) == null || e.hide();
  }
  /**
   * Unselect column, close toolbox
   *
   * @returns {void}
   */
  hideColumnToolbox() {
    this.unselectColumn();
  }
  /**
   * Set the cursor focus to the focused cell
   *
   * @returns {void}
   */
  focusCell() {
    var e;
    (e = this.focusedCellElem) == null || e.focus();
  }
  /**
   * Get current focused element
   *
   * @returns {HTMLElement} - focused cell
   */
  get focusedCellElem() {
    const { row: e, column: t } = this.focusedCell;
    return this.getCell(e, t);
  }
  /**
   * Update toolboxes position
   *
   * @param {number} row - hovered row
   * @param {number} column - hovered column
   */
  updateToolboxesPosition(e = this.hoveredRow, t = this.hoveredColumn) {
    this.isRowMenuShowing || e > 0 && e <= this.numberOfRows && this.toolboxRow.show(() => {
      const n = this.getRow(e), { fromTopBorder: r } = Tl(this.table, n), { height: a } = n.getBoundingClientRect();
      return {
        top: `${Math.ceil(r + a / 2)}px`
      };
    });
  }
  /**
   * Makes the first row headings
   *
   * @param {boolean} withHeadings - use headings row or not
   */
  setHeadingsSetting(e) {
    this.tunes.withHeadings = e, e ? (this.table.classList.add(Ke.withHeadings), this.addHeadingAttrToFirstRow()) : (this.table.classList.remove(Ke.withHeadings), this.removeHeadingAttrFromFirstRow());
  }
  /**
   * Adds an attribute for displaying the placeholder in the cell
   */
  addHeadingAttrToFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.setAttribute("heading", this.api.i18n.t("Heading"));
    }
  }
  /**
   * Removes an attribute for displaying the placeholder in the cell
   */
  removeHeadingAttrFromFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.removeAttribute("heading");
    }
  }
  /**
   * Add effect of a selected row
   *
   * @param {number} index
   */
  selectRow(e) {
    const t = this.getRow(e);
    t && (this.selectedRow = e, t.classList.add(Ke.rowSelected));
  }
  /**
   * Remove effect of a selected row
   */
  unselectRow() {
    if (this.selectedRow <= 0)
      return;
    const e = this.table.querySelector(`.${Ke.rowSelected}`);
    e && e.classList.remove(Ke.rowSelected), this.selectedRow = 0;
  }
  /**
   * Add effect of a selected column
   *
   * @param {number} index
   */
  selectColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      n && n.classList.add(Ke.cellSelected);
    }
    this.selectedColumn = e;
  }
  /**
   * Remove effect of a selected column
   */
  unselectColumn() {
    if (this.selectedColumn <= 0)
      return;
    let e = this.table.querySelectorAll(`.${Ke.cellSelected}`);
    Array.from(e).forEach((t) => {
      t.classList.remove(Ke.cellSelected);
    }), this.selectedColumn = 0;
  }
  /**
   * Calculates the row and column that the cursor is currently hovering over
   * The search was optimized from O(n) to O (log n) via bin search to reduce the number of calculations
   *
   * @param {Event} event - mousemove event
   * @returns hovered cell coordinates as an integer row and column
   */
  getHoveredCell(e) {
    let t = this.hoveredRow, n = this.hoveredColumn;
    const { width: r, height: a, x: c, y: u } = ib(this.table, e);
    return c >= 0 && (n = this.binSearch(
      this.numberOfColumns,
      (p) => this.getCell(1, p),
      ({ fromLeftBorder: p }) => c < p,
      ({ fromRightBorder: p }) => c > r - p
    )), u >= 0 && (t = this.binSearch(
      this.numberOfRows,
      (p) => this.getCell(p, 1),
      ({ fromTopBorder: p }) => u < p,
      ({ fromBottomBorder: p }) => u > a - p
    )), {
      row: t || this.hoveredRow,
      column: n || this.hoveredColumn
    };
  }
  /**
   * Looks for the index of the cell the mouse is hovering over.
   * Cells can be represented as ordered intervals with left and
   * right (upper and lower for rows) borders inside the table, if the mouse enters it, then this is our index
   *
   * @param {number} numberOfCells - upper bound of binary search
   * @param {function} getCell - function to take the currently viewed cell
   * @param {function} beforeTheLeftBorder - determines the cursor position, to the left of the cell or not
   * @param {function} afterTheRightBorder - determines the cursor position, to the right of the cell or not
   * @returns {number}
   */
  binSearch(e, t, n, r) {
    let a = 0, c = e + 1, u = 0, p;
    for (; a < c - 1 && u < 10; ) {
      p = Math.ceil((a + c) / 2);
      const g = t(p), v = Tl(this.table, g);
      if (n(v))
        c = p;
      else if (r(v))
        a = p;
      else
        break;
      u++;
    }
    return p;
  }
  /**
   * Collects data from cells into a two-dimensional array
   *
   * @returns {string[][]}
   */
  getData() {
    const e = [];
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.table.querySelector(`.${Ke.row}:nth-child(${t})`), r = Array.from(n.querySelectorAll(`.${Ke.formInput}`));
      if (t == 1)
        continue;
      let a = {};
      r.map((c) => {
        if (["text", "select-one", "number", "hidden"].includes(c == null ? void 0 : c.type))
          return a[c == null ? void 0 : c.name] = c == null ? void 0 : c.value;
        if ((c == null ? void 0 : c.type) === "checkbox")
          return a[c == null ? void 0 : c.name] = c == null ? void 0 : c.checked;
      }), e.push(a);
    }
    return e;
  }
  /**
   * Remove listeners on the document
   */
  destroy() {
    document.removeEventListener("click", this.documentClicked);
  }
}
const mb = {
  default: {
    title: "Test & Report",
    title_jp: "混合ワクチン",
    data: [
      {
        heading: "Report",
        heading_jp: "予防接種医学",
        type: "search",
        key: "report"
      },
      {
        heading: "Quantity",
        heading_jp: "投与日",
        type: "number",
        key: "quantity",
        default: 1
      },
      {
        heading: "Remarks",
        heading_jp: "備考",
        type: "text",
        key: "remarks"
      },
      {
        heading: "Upload File",
        heading_jp: "備考",
        type: "customFile",
        key: "files"
      }
    ]
  },
  internal: {
    title: "Internal Documents",
    title_jp: "狂犬病ワクチン",
    data: [
      {
        heading: "Report",
        heading_jp: "予防接種医学",
        type: "search",
        key: "report"
      },
      {
        heading: "Remarks",
        heading_jp: "備考",
        type: "text",
        key: "remarks"
      },
      {
        heading: "Upload File",
        heading_jp: "備考",
        type: "customFile",
        key: "files"
      }
    ]
  }
}, Gr = {
  default: "default",
  data: mb
};
class gb {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to press Enter inside the CodeTool textarea
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {TableData} data — previously saved data
   * @param {TableConfig} config - user config for Tool
   * @param {object} api - Editor.js API
   * @param {boolean} readOnly - read-only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r, block: a }) {
    if (this.api = n, this.block = a, this.title = n.i18n.t("Vaccination"), this.readOnly = r, this.config = t, !t.apiUrl)
      throw new Error("Please provide API url for Vacination Search");
    this.data = {
      current: e && e.current ? e.current : Gr.default,
      content: e && e.content ? e.content : []
    }, setTimeout(() => {
      if (e != null && e.content)
        return;
      const c = this.block.id;
      this.api.toolbar.toggleBlockSettingsById(c);
    }, 500), this.table = null;
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: db,
      title: this.title
    };
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLDivElement}
   */
  render() {
    var t;
    this.config.data = Gr.data[this.data.current], this.container ? (this.table.getWrapper().remove(), this.container.innerHTML = "") : this.container = Ye("div", this.api.styles.block), this.table = new fb(this.readOnly, this.api, this.data, this.config, this.block);
    const e = Ye("h5", "tc-header");
    return e.innerHTML = ((t = this.config) == null ? void 0 : t.locale) == "jp" ? this.config.data.title_jp : this.config.data.title, this.container.appendChild(e), this.container.appendChild(this.table.getWrapper()), this.table.setHeadingsSetting(!0), this.container;
  }
  /**
   * Returns plugin settings
   *
   * @returns {Array}
   */
  renderSettings() {
    var n;
    const e = [], t = ((n = this.config) == null ? void 0 : n.locale) === "jp";
    for (const r in Gr.data)
      if (Object.hasOwnProperty.call(Gr.data, r)) {
        const a = Gr.data[r];
        e.push({
          label: t ? a.title_jp : a.title,
          icon: cb,
          isActive: this.data.current == r,
          closeOnActivate: !0,
          toggle: !0,
          onActivate: () => {
            this.data.current = r, this.data.content = [], this.render();
          }
        });
      }
    return e;
  }
  /**
   * Extract table data from the view
   *
   * @returns {TableData} - saved data
   */
  save() {
    var n;
    const e = this.table.getData();
    return {
      current: ((n = this.data) == null ? void 0 : n.current) || Gr.default,
      content: e
    };
  }
  /**
   * Plugin destroyer
   *
   * @returns {void}
   */
  destroy() {
    this.table.destroy();
  }
  /**
   * A helper to get config value.
   * 
   * @param {string} configName - the key to get from the config. 
   * @param {any} defaultValue - default value if config doesn't have passed key
   * @param {object} savedData - previously saved data. If passed, the key will be got from there, otherwise from the config
   * @returns {any} - config value.
   */
  getConfig(e, t = void 0, n = void 0) {
    const r = this.data || n;
    return r ? r[e] ? r[e] : t : this.config && this.config[e] ? this.config[e] : t;
  }
  /**  
   * Table onPaste configuration
   *
   * @public
   */
  static get pasteConfig() {
    return { tags: ["TABLE", "TR", "TH", "TD"] };
  }
  /**
   * On paste callback that is fired from Editor
   *
   * @param {PasteEvent} event - event with pasted data
   */
  onPaste(e) {
    const t = e.detail.data, n = t.querySelector(":scope > thead, tr:first-of-type th"), a = Array.from(t.querySelectorAll("tr")).map((c) => Array.from(c.querySelectorAll("th, td")).map((p) => p.innerHTML));
    this.data = {
      withHeadings: n !== null,
      content: a
    }, this.table.wrapper && this.table.wrapper.replaceWith(this.render());
  }
}
(function() {
  try {
    if (typeof document < "u") {
      var o = document.createElement("style");
      o.appendChild(document.createTextNode("hr{display:block;margin:.5em auto;border-style:inset;border-width:1px}")), document.head.appendChild(o);
    }
  } catch (e) {
    console.error("vite-plugin-css-injected-by-js", e);
  }
})();
const bb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><line x1="6" x2="10" y1="12" y2="12" stroke="currentColor" stroke-linecap="round" stroke-width="2"/><line x1="14" x2="18" y1="12" y2="12" stroke="currentColor" stroke-linecap="round" stroke-width="2"/></svg>';
/**
 * Delimiter Block for the Editor.js.
 *
 * @author Vishal Pradhan
 * @copyright Vishal Pradhan 2023
 * @license The MIT License (MIT)
 * @version 2.0.0
 */
class qd {
  /**
   * Notify core that read-only mode is supported
   * @return {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow Tool to have no content
   * @return {boolean}
   */
  static get contentless() {
    return !0;
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {{data: DelimiterData, config: object, api: object}}
   *   data — previously saved data
   *   config - user config for Tool
   *   api - Editor.js API
   */
  constructor({ data: e, config: t, api: n }) {
    this.api = n, this._CSS = {
      // block: this.api.styles.block,
      // wrapper: 'ce-delimiter'
    }, this._data = {}, this._element = this.drawView(), this.data = e;
  }
  /**
   * Create Tool's view
   * @return {HTMLElement}
   * @private
   */
  drawView() {
    return document.createElement("HR");
  }
  /**
   * Return Tool's view
   * @returns {HTMLDivElement}
   * @public
   */
  render() {
    return this._element;
  }
  /**
   * Extract Tool's data from the view
   * @param {HTMLDivElement} toolsContent - Paragraph tools rendered view
   * @returns {DelimiterData} - saved data
   * @public
   */
  save(e) {
    return {};
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @return {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: bb,
      title: "Delimiter"
    };
  }
}
(function() {
  var o;
  try {
    if (typeof document < "u") {
      var e = document.createElement("style");
      e.nonce = (o = document.head.querySelector("meta[property=csp-nonce]")) == null ? void 0 : o.content, e.appendChild(document.createTextNode('.tc-header{margin:0;padding:0;position:relative;bottom:-1rem;text-align:left}.tc-wrap{--color-background:#f9f9fb;--color-text-secondary:#7b7e89;--color-border:#e8e8eb;--cell-size:34px;--toolbox-icon-size:18px;--toolbox-padding:6px;--toolbox-aiming-field-size:calc(var(--toolbox-icon-size) + var(--toolbox-padding)*2);border-left:0;margin-top:var(--toolbox-icon-size);box-sizing:border-box;grid-template-columns:calc(100% - var(--cell-size)) var(--cell-size)}.tc-wrap--readonly{grid-template-columns:100% var(--cell-size)}.tc-wrap svg{vertical-align:top}@media print{.tc-wrap{border-left-color:var(--color-border);border-left-style:solid;border-left-width:1px;grid-template-columns:100% var(--cell-size)}}@media print{.tc-wrap .tc-row:after{display:none}}.tc-table,.tc-wrap{position:relative;height:100%;width:100%;display:grid}.tc-table{font-size:14px;border-top:1px solid var(--color-border);border-left:1px solid var(--color-border);line-height:1.4}.tc-table:after{width:calc(var(--cell-size));left:calc(var(--cell-size)*-1);top:0}.tc-table:after,.tc-table:before{position:absolute;content:""}.tc-table:before{height:var(--toolbox-aiming-field-size);top:calc(var(--toolbox-aiming-field-size)*-1);left:0}.tc-table .tc-row:first-child{background:linear-gradient(0deg,rgba(0,0,0,.06),rgba(0,0,0,.06)),linear-gradient(0deg,#b9b9b9,#b9b9b9);font-weight:600;border-bottom:2px solid var(--color-border)}.tc-table .tc-row:first-child [contenteditable]:empty:before{content:attr(heading);color:var(--color-text-secondary)}.tc-table .tc-row:first-child:after{bottom:-2px;border-bottom:2px solid var(--color-border)}.tc-add-column,.tc-add-row{display:flex;color:var(--color-text-secondary)}@media print{.tc-add{display:none}}.tc-add-column{padding:4px 0;justify-content:center;border-top:1px solid var(--color-border)}@media print{.tc-add-column{display:none}}.tc-add-row{height:var(--cell-size);align-items:center;padding-left:4px;position:relative}.tc-add-row:before{content:"";position:absolute;right:calc(var(--cell-size)*-1);width:var(--cell-size);height:100%}@media print{.tc-add-row{display:none}}.tc-add-column,.tc-add-row{transition:0s;cursor:pointer;will-change:background-color}.tc-add-column:hover,.tc-add-row:hover{transition:background-color .1s ease;background-color:var(--color-background)}.tc-add-row{margin-top:1px}.tc-add-row:hover:before{transition:.1s;background-color:var(--color-background)}.tc-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(10px,1fr));position:relative;border-bottom:1px solid var(--color-border)}.tc-row:after{content:"";pointer-events:none;position:absolute;width:var(--cell-size);height:100%;bottom:-1px;right:calc(var(--cell-size)*-1);border-bottom:1px solid var(--color-border)}.tc-row--selected{background:var(--color-background)}.tc-row--selected:after{background:var(--color-background)}.tc-cell{text-align:left;border-right:1px solid var(--color-border);padding:6px 12px;overflow:hidden;outline:none;line-break:normal}.tc-cell>input,.tc-cell select{outline:none;border:none;width:100%;background:transparent}.tc-cell--selected{background:var(--color-background)}.tc-wrap--readonly .tc-row:after{display:none}.tc-cell>select,input{background-color:transparent}.tc-toolbox{--toolbox-padding:6px;--popover-margin:30px;--toggler-click-zone-size:30px;--toggler-dots-color:#7b7e89;--toggler-dots-color-hovered:#1d202b;position:absolute;cursor:pointer;z-index:1;opacity:0;transition:opacity .1s;will-change:left,opacity}.tc-toolbox--column{top:calc(var(--toggler-click-zone-size)*-1);transform:translate(calc(var(--toggler-click-zone-size)*-1/2));will-change:left,opacity}.tc-toolbox--row{left:calc(var(--popover-margin)*-1);transform:translateY(calc(var(--toggler-click-zone-size)*-1/2));margin-top:-1px;will-change:top,opacity}.tc-toolbox--showed{opacity:1}.tc-toolbox .tc-popover{position:absolute;top:0;left:var(--popover-margin)}.tc-toolbox__toggler{display:flex;align-items:center;justify-content:center;width:var(--toggler-click-zone-size);height:var(--toggler-click-zone-size);color:var(--toggler-dots-color);opacity:0;transition:opacity .15s ease;will-change:opacity}.tc-toolbox__toggler:hover{color:var(--toggler-dots-color-hovered)}.tc-toolbox__toggler svg{fill:currentColor}.tc-wrap:hover .tc-toolbox__toggler{opacity:1}.tc-settings .cdx-settings-button{width:50%;margin:0}.tc-popover{--color-border:#eaeaea;--color-background:#fff;--color-background-hover:rgba(232,232,235,.49);--color-background-confirm:#e24a4a;--color-background-confirm-hover:#d54040;--color-text-confirm:#fff;background:var(--color-background);border:1px solid var(--color-border);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;padding:6px;display:none;will-change:opacity,transform}.tc-popover--opened{display:block;animation:menuShowing .1s cubic-bezier(.215,.61,.355,1) forwards}.tc-popover__item{display:flex;align-items:center;padding:2px 14px 2px 2px;border-radius:5px;cursor:pointer;white-space:nowrap;-webkit-user-select:none;-moz-user-select:none;user-select:none}.tc-popover__item:hover{background:var(--color-background-hover)}.tc-popover__item:not(:last-of-type){margin-bottom:2px}.tc-popover__item-icon{display:inline-flex;width:26px;height:26px;align-items:center;justify-content:center;background:var(--color-background);border-radius:5px;border:1px solid var(--color-border);margin-right:8px}.tc-popover__item-label{line-height:22px;font-size:14px;font-weight:500}.tc-popover__item--confirm{background:var(--color-background-confirm);color:var(--color-text-confirm)}.tc-popover__item--confirm:hover{background-color:var(--color-background-confirm-hover)}.tc-popover__item--confirm .tc-popover__item-icon{background:var(--color-background-confirm);border-color:#0000001a}.tc-popover__item--confirm .tc-popover__item-icon svg{transition:transform .2s ease-in;transform:rotate(90deg) scale(1.2)}.tc-popover__item--hidden{display:none}@keyframes menuShowing{0%{opacity:0;transform:translateY(-8px) scale(.9)}70%{opacity:1;transform:translateY(2px)}to{transform:translateY(0)}}')), document.head.appendChild(e);
    }
  } catch (t) {
    console.error("vite-plugin-css-injected-by-js", t);
  }
})();
function go(o, e, t = {}) {
  const n = document.createElement(o);
  Array.isArray(e) ? n.classList.add(...e) : e && n.classList.add(e);
  for (const r in t)
    Object.prototype.hasOwnProperty.call(t, r) && (n[r] = t[r]);
  return n;
}
function Vd(o) {
  const e = o.getBoundingClientRect();
  return {
    y1: Math.floor(e.top + window.pageYOffset),
    x1: Math.floor(e.left + window.pageXOffset),
    x2: Math.floor(e.right + window.pageXOffset),
    y2: Math.floor(e.bottom + window.pageYOffset)
  };
}
function vb(o, e) {
  const t = Vd(o), n = Vd(e);
  return {
    fromTopBorder: n.y1 - t.y1,
    fromLeftBorder: n.x1 - t.x1,
    fromRightBorder: t.x2 - n.x2,
    fromBottomBorder: t.y2 - n.y2
  };
}
function yb(o, e) {
  const t = o.getBoundingClientRect(), { width: n, height: r, x: a, y: c } = t, { clientX: u, clientY: p } = e;
  return {
    width: n,
    height: r,
    x: u - a,
    y: p - c
  };
}
function Gd(o, e) {
  return e.parentNode.insertBefore(o, e);
}
function Zd(o, e = !0) {
  const t = document.createRange(), n = window.getSelection();
  t.selectNodeContents(o), t.collapse(e), n.removeAllRanges(), n.addRange(t);
}
class Ro {
  /**
   * @param {object} options - constructor options
   * @param {PopoverItem[]} options.items - constructor options
   */
  constructor({ items: e }) {
    this.items = e, this.wrapper = void 0, this.itemEls = [];
  }
  /**
   * Set of CSS classnames used in popover
   *
   * @returns {object}
   */
  static get CSS() {
    return {
      popover: "tc-popover",
      popoverOpened: "tc-popover--opened",
      item: "tc-popover__item",
      itemHidden: "tc-popover__item--hidden",
      itemConfirmState: "tc-popover__item--confirm",
      itemIcon: "tc-popover__item-icon",
      itemLabel: "tc-popover__item-label"
    };
  }
  /**
   * Returns the popover element
   *
   * @returns {Element}
   */
  render() {
    return this.wrapper = go("div", Ro.CSS.popover), this.items.forEach((e, t) => {
      const n = go("div", Ro.CSS.item), r = go("div", Ro.CSS.itemIcon, {
        innerHTML: e.icon
      }), a = go("div", Ro.CSS.itemLabel, {
        textContent: e.label
      });
      n.dataset.index = t, n.appendChild(r), n.appendChild(a), this.wrapper.appendChild(n), this.itemEls.push(n);
    }), this.wrapper.addEventListener("click", (e) => {
      this.popoverClicked(e);
    }), this.wrapper;
  }
  /**
   * Popover wrapper click listener
   * Used to delegate clicks in items
   *
   * @returns {void}
   */
  popoverClicked(e) {
    const t = e.target.closest(`.${Ro.CSS.item}`);
    if (!t)
      return;
    const n = t.dataset.index, r = this.items[n];
    if (r.confirmationRequired && !this.hasConfirmationState(t)) {
      this.setConfirmationState(t);
      return;
    }
    r.onClick();
  }
  /**
   * Enable the confirmation state on passed item
   *
   * @returns {void}
   */
  setConfirmationState(e) {
    e.classList.add(Ro.CSS.itemConfirmState);
  }
  /**
   * Disable the confirmation state on passed item
   *
   * @returns {void}
   */
  clearConfirmationState(e) {
    e.classList.remove(Ro.CSS.itemConfirmState);
  }
  /**
   * Check if passed item has the confirmation state
   *
   * @returns {boolean}
   */
  hasConfirmationState(e) {
    return e.classList.contains(Ro.CSS.itemConfirmState);
  }
  /**
   * Return an opening state
   *
   * @returns {boolean}
   */
  get opened() {
    return this.wrapper.classList.contains(Ro.CSS.popoverOpened);
  }
  /**
   * Opens the popover
   *
   * @returns {void}
   */
  open() {
    this.items.forEach((e, t) => {
      typeof e.hideIf == "function" && this.itemEls[t].classList.toggle(Ro.CSS.itemHidden, e.hideIf());
    }), this.wrapper.classList.add(Ro.CSS.popoverOpened);
  }
  /**
   * Closes the popover
   *
   * @returns {void}
   */
  close() {
    this.wrapper.classList.remove(Ro.CSS.popoverOpened), this.itemEls.forEach((e) => {
      this.clearConfirmationState(e);
    });
  }
}
const Kd = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 8L12 12M12 12L16 16M12 12L16 8M12 12L8 16"/></svg>', wb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 9.16666L18.2167 12.5M18.2167 12.5L14.8833 15.8333M18.2167 12.5H10.05C9.16594 12.5 8.31809 12.1488 7.69297 11.5237C7.06785 10.8986 6.71666 10.0507 6.71666 9.16666"/></svg>', kb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.9167 14.9167L11.5833 18.25M11.5833 18.25L8.25 14.9167M11.5833 18.25L11.5833 10.0833C11.5833 9.19928 11.9345 8.35143 12.5596 7.72631C13.1848 7.10119 14.0326 6.75 14.9167 6.75"/></svg>', xb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.13333 14.9167L12.4667 18.25M12.4667 18.25L15.8 14.9167M12.4667 18.25L12.4667 10.0833C12.4667 9.19928 12.1155 8.35143 11.4904 7.72631C10.8652 7.10119 10.0174 6.75 9.13333 6.75"/></svg>', Cb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 15.8333L18.2167 12.5M18.2167 12.5L14.8833 9.16667M18.2167 12.5L10.05 12.5C9.16595 12.5 8.31811 12.8512 7.69299 13.4763C7.06787 14.1014 6.71667 14.9493 6.71667 15.8333"/></svg>', Sb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.41 9.66H9.4"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 9.66H14.59"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.31 14.36H9.3"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 14.36H14.59"/></svg>', Tb = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>', _b = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M10 5V18.5"/><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>';
class Ui {
  /**
   * Creates toolbox buttons and toolbox menus
   *
   * @param {object} api - Editor.js api
   * @param {PopoverItem[]} items - Editor.js api
   * @param {function} onOpen - callback fired when the Popover is opening
   * @param {function} onClose - callback fired when the Popover is closing
   * @param {string} [cssModifier] - the modifier for the Toolbox. Allows to add some specific styles.
   */
  constructor({ api: e, items: t, onOpen: n, onClose: r, cssModifier: a = "" }) {
    this.api = e, this.items = t, this.onOpen = n, this.onClose = r, this.cssModifier = a, this.popover = null, this.wrapper = this.createToolbox();
  }
  /**
   * Style classes
   */
  static get CSS() {
    return {
      toolbox: "tc-toolbox",
      toolboxShowed: "tc-toolbox--showed",
      toggler: "tc-toolbox__toggler"
    };
  }
  /**
   * Returns rendered Toolbox element
   */
  get element() {
    return this.wrapper;
  }
  /**
   * Creating a toolbox to open menu for a manipulating columns
   *
   * @returns {Element}
   */
  createToolbox() {
    const e = go("div", [
      Ui.CSS.toolbox,
      this.cssModifier ? `${Ui.CSS.toolbox}--${this.cssModifier}` : ""
    ]);
    e.dataset.mutationFree = "true";
    const t = this.createPopover(), n = this.createToggler();
    return e.appendChild(n), e.appendChild(t), e;
  }
  /**
   * Creates the Toggler
   *
   * @returns {Element}
   */
  createToggler() {
    const e = go("div", Ui.CSS.toggler, {
      innerHTML: Sb
    });
    return e.addEventListener("click", () => {
      this.togglerClicked();
    }), e;
  }
  /**
   * Creates the Popover instance and render it
   *
   * @returns {Element}
   */
  createPopover() {
    return this.popover = new Ro({
      items: this.items
    }), this.popover.render();
  }
  /**
   * Toggler click handler. Opens/Closes the popover
   *
   * @returns {void}
   */
  togglerClicked() {
    this.popover.opened ? (this.popover.close(), this.onClose()) : (this.popover.open(), this.onOpen());
  }
  /**
   * Shows the Toolbox
   *
   * @param {function} computePositionMethod - method that returns the position coordinate
   * @returns {void}
   */
  show(e) {
    const t = e();
    Object.entries(t).forEach(([n, r]) => {
      this.wrapper.style[n] = r;
    }), this.wrapper.classList.add(Ui.CSS.toolboxShowed);
  }
  /**
   * Hides the Toolbox
   *
   * @returns {void}
   */
  hide() {
    this.popover.close(), this.wrapper.classList.remove(Ui.CSS.toolboxShowed);
  }
}
function Eb(o, e) {
  let t = 0;
  return function(...n) {
    const r = (/* @__PURE__ */ new Date()).getTime();
    if (!(r - t < o))
      return t = r, e(...n);
  };
}
const Vt = {
  wrapper: "tc-wrap",
  wrapperReadOnly: "tc-wrap--readonly",
  table: "tc-table",
  row: "tc-row",
  withHeadings: "tc-table--heading",
  rowSelected: "tc-row--selected",
  cell: "tc-cell",
  cellSelected: "tc-cell--selected",
  addRow: "tc-add-row",
  addColumn: "tc-add-column"
};
let Mb = class {
  /**
   * Creates
   *
   * @constructor
   * @param {boolean} readOnly - read-only mode flag
   * @param {object} api - Editor.js API
   * @param {TableData} data - Editor.js API
   * @param {TableConfig} config - Editor.js API
   */
  constructor(e, t, n, r, a, c) {
    this.readOnly = !1, this.api = t, "data" in n && typeof n.data == "object" || (n.data = {}), this.data = JSON.parse(JSON.stringify(n)), this.config = r, this.options = a, this.block = c, this.currentData = JSON.parse(JSON.stringify(n.data)), this.wrapper = null, this.table = null, this.hoveredRow = 0, this.hoveredColumn = 0, this.selectedRow = 0, this.selectedColumn = 0, this.tunes = {
      withHeadings: !0
    }, this.fillInput(), this.focusedCell = {
      row: 0,
      column: 0
    }, this.documentClicked = (u) => {
    }, this.readOnly || this.bindEvents();
  }
  remove(e) {
    return e in this.currentData ? (delete this.currentData[e], !0) : !1;
  }
  fillInput() {
    var e, t, n, r, a, c, u;
    this.createTableWrapper(), this.resize();
    const p = ((e = this.config) == null ? void 0 : e.locale) === "jp";
    this.setCellContent(1, 1, this.api.i18n.t("Observation")), this.setCellContent(1, 2, this.api.i18n.t("Result")), this.setCellContent(1, 3, this.api.i18n.t("Remarks"));
    let g = 2;
    for (let v in this.currentData)
      if (Object.hasOwnProperty.call(this.currentData, v)) {
        const k = this.options[v];
        let y = this.getCell(g, 1);
        y.innerHTML = k == null ? void 0 : k.label, y = this.getCell(g, 3), y.innerHTML = "";
        const T = go("input", "", {
          placeHolder: this.api.i18n.t("Enter remarks"),
          name: v + "_remarks",
          type: "text",
          value: ((n = (t = this.currentData) == null ? void 0 : t[v]) == null ? void 0 : n.remarks) || ""
        });
        this.api.listeners.on(T, "keyup", (M) => {
          this.setRemarks(v, M.target.value);
        }), y.appendChild(T), y = this.getCell(g, 2);
        let x = "";
        if (["radio", "select"].includes(k.type)) {
          x = go("select", "", {
            placeHolder: this.api.i18n.t("Select Value"),
            name: v + "_remarks"
          }), this.api.listeners.on(x, "change", (M) => {
            this.setValue(v, M.target.value);
          });
          for (const M of k.options) {
            const L = ((a = (r = this.currentData) == null ? void 0 : r[v]) == null ? void 0 : a.value) || (M != null && M.default ? M.value : "");
            x.appendChild(go("option", "", {
              value: M == null ? void 0 : M.value,
              label: p ? M == null ? void 0 : M.label_jp : M == null ? void 0 : M.label,
              selected: M.value == L
            }));
          }
        } else
          k.type == "text" && (x = go("input", "", {
            value: ((u = (c = this.currentData) == null ? void 0 : c[v]) == null ? void 0 : u.value) || "",
            placeHolder: this.api.i18n.t("Input value"),
            name: v + "_value",
            type: "text"
          }), this.api.listeners.on(x, "keyup", (M) => {
            this.setValue(v, M.target.value);
          }));
        x !== "" && (y.innerHTML = "", y.appendChild(x)), g++;
      }
  }
  setValue(e, t) {
    let n = this.currentData[e];
    n && typeof n == "object" && "value" in n || (this.currentData[e] = { value: "", remarks: "" }), this.currentData[e] = { ...this.currentData[e], value: t }, this.block.dispatchChange();
  }
  setData(e) {
    e.map((t) => {
      const n = t.key;
      if (this.currentData[n]) {
        delete this.currentData[n];
        return;
      } else
        this.currentData[n] = { value: "", remarks: "" };
    }), this.block.dispatchChange();
  }
  setRemarks(e, t) {
    let n = this.currentData[e];
    n && typeof n == "object" && "value" in n || (this.currentData[e] = { value: "", remarks: "" }), this.currentData[e] = { ...this.currentData[e], remarks: t }, this.block.dispatchChange();
  }
  /**
   * Returns the rendered table wrapper
   *
   * @returns {Element}
   */
  getWrapper() {
    return this.wrapper;
  }
  /**
   * Hangs the necessary handlers to events
   */
  bindEvents() {
    document.addEventListener("click", this.documentClicked), this.table.addEventListener("mousemove", Eb(150, (e) => this.onMouseMoveInTable(e)), { passive: !0 }), this.table.onkeypress = (e) => this.onKeyPressListener(e), this.table.addEventListener("keydown", (e) => this.onKeyDownListener(e)), this.table.addEventListener("focusin", (e) => this.focusInTableListener(e));
  }
  /**
   * Configures and creates the toolbox for manipulating with columns
   *
   * @returns {Toolbox}
   */
  createColumnToolbox() {
    return new Ui({
      api: this.api,
      cssModifier: "column",
      items: [
        {
          label: this.api.i18n.t("Add column to left"),
          icon: kb,
          onClick: () => {
            this.addColumn(this.selectedColumn, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Add column to right"),
          icon: xb,
          onClick: () => {
            this.addColumn(this.selectedColumn + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete column"),
          icon: Kd,
          hideIf: () => this.numberOfColumns === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteColumn(this.selectedColumn), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectColumn(this.hoveredColumn);
      },
      onClose: () => {
        this.unselectColumn();
      }
    });
  }
  /**
   * Configures and creates the toolbox for manipulating with rows
   *
   * @returns {Toolbox}
   */
  createRowToolbox() {
    return new Ui({
      api: this.api,
      cssModifier: "row",
      items: [
        {
          label: this.api.i18n.t("Add row above"),
          icon: Cb,
          onClick: () => {
            this.addRow(this.selectedRow, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Add row below"),
          icon: wb,
          onClick: () => {
            this.addRow(this.selectedRow + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete row"),
          icon: Kd,
          hideIf: () => this.numberOfRows === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteRow(this.selectedRow), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  /**
   * When you press enter it moves the cursor down to the next row
   * or creates it if the click occurred on the last one
   */
  moveCursorToNextRow() {
    this.focusedCell.row !== this.numberOfRows ? (this.focusedCell.row += 1, this.focusCell(this.focusedCell)) : (this.addRow(), this.focusedCell.row += 1, this.focusCell(this.focusedCell), this.updateToolboxesPosition(0, 0));
  }
  /**
   * Get table cell by row and col index
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @returns {HTMLElement}
   */
  getCell(e, t) {
    return this.table.querySelectorAll(`.${Vt.row}:nth-child(${e}) .${Vt.cell}`)[t - 1];
  }
  /**
   * Get table row by index
   *
   * @param {number} row - row coordinate
   * @returns {HTMLElement}
   */
  getRow(e) {
    return this.table.querySelector(`.${Vt.row}:nth-child(${e})`);
  }
  /**
   * The parent of the cell which is the row
   *
   * @param {HTMLElement} cell - cell element
   * @returns {HTMLElement}
   */
  getRowByCell(e) {
    return e.parentElement;
  }
  /**
   * Ger row's first cell
   *
   * @param {Element} row - row to find its first cell
   * @returns {Element}
   */
  getRowFirstCell(e) {
    return e.querySelector(`.${Vt.cell}:first-child`);
  }
  /**
   * Set the sell's content by row and column numbers
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @param {string} content - cell HTML content
   */
  setCellContent(e, t, n) {
    const r = this.getCell(e, t);
    r.innerHTML = n;
  }
  /**
   * Add column in table on index place
   * Add cells in each row
   *
   * @param {number} columnIndex - number in the array of columns, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the first cell
   */
  addColumn(e = -1, t = !1) {
    let n = this.numberOfColumns;
    for (let r = 1; r <= this.numberOfRows; r++) {
      let a;
      const c = this.createCell();
      if (e > 0 && e <= n ? (a = this.getCell(r, e), Gd(c, a)) : a = this.getRow(r).appendChild(c), r === 1) {
        const u = this.getCell(r, e > 0 ? e : n + 1);
        u && t && Zd(u);
      }
    }
    this.addHeadingAttrToFirstRow();
  }
  /**
   * Add row in table on index place
   *
   * @param {number} index - number in the array of rows, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the inserted row
   * @returns {HTMLElement} row
   */
  addRow(e = -1, t = !1) {
    let n, r = go("div", Vt.row);
    this.tunes.withHeadings && this.removeHeadingAttrFromFirstRow();
    let a = this.numberOfColumns;
    if (e > 0 && e <= this.numberOfRows) {
      let u = this.getRow(e);
      n = Gd(r, u);
    } else
      n = this.table.appendChild(r);
    this.fillRow(n, a), this.tunes.withHeadings && this.addHeadingAttrToFirstRow();
    const c = this.getRowFirstCell(n);
    return c && t && Zd(c), n;
  }
  /**
   * Delete a column by index
   *
   * @param {number} index
   */
  deleteColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      if (!n)
        return;
      n.remove();
    }
  }
  /**
   * Delete a row by index
   *
   * @param {number} index
   */
  deleteRow(e) {
    this.getRow(e).remove(), this.addHeadingAttrToFirstRow();
  }
  /**
   * Create a wrapper containing a table, toolboxes
   * and buttons for adding rows and columns
   *
   * @returns {HTMLElement} wrapper - where all buttons for a table and the table itself will be
   */
  createTableWrapper() {
    this.wrapper ? this.table.remove() : this.wrapper = go("div", Vt.wrapper), this.table = go("div", Vt.table), this.readOnly, this.wrapper.classList.add(Vt.wrapperReadOnly), this.wrapper.appendChild(this.table);
  }
  /**
   * Returns the size of the table based on initial data or config "size" property
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  computeInitialSize() {
    let e = this.data && this.currentData;
    const t = Array.isArray(e), n = t ? e.length : !1, r = t ? e.length : void 0, a = n ? e[0].length : void 0, c = Number.parseInt(this.config && this.config.rows), u = Number.parseInt(this.config && this.config.cols), p = !isNaN(c) && c > 0 ? c : void 0, g = !isNaN(u) && u > 0 ? u : void 0;
    return {
      rows: r || p || 2,
      cols: a || g || 2
    };
  }
  /**
   * Resize table to match config size or transmitted data size
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  resize() {
    const e = Object.keys(this.currentData).length + 1;
    for (let t = 0; t < e; t++)
      this.addRow();
    for (let t = 0; t < 3; t++)
      this.addColumn();
  }
  /**
   * Fills the table with data passed to the constructor
   *
   * @returns {void}
   */
  fill() {
    const e = this.data;
    if (e && e.data)
      for (let t = 0; t < e.data.length; t++)
        for (let n = 0; n < e.data[t].length; n++)
          this.setCellContent(t + 1, n + 1, e.data[t][n]);
  }
  /**
   * Fills a row with cells
   *
   * @param {HTMLElement} row - row to fill
   * @param {number} numberOfColumns - how many cells should be in a row
   */
  fillRow(e, t) {
    for (let n = 1; n <= t; n++) {
      const r = this.createCell();
      e.appendChild(r);
    }
  }
  /**
   * Creating a cell element
   *
   * @return {Element}
   */
  createCell() {
    return go("div", Vt.cell, {
      // contentEditable: !this.readOnly
    });
  }
  /**
   * Get number of rows in the table
   */
  get numberOfRows() {
    return this.table.childElementCount;
  }
  /**
   * Get number of columns in the table
   */
  get numberOfColumns() {
    return this.numberOfRows ? this.table.querySelectorAll(`.${Vt.row}:first-child .${Vt.cell}`).length : 0;
  }
  /**
   * Is the column toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isColumnMenuShowing() {
    return this.selectedColumn !== 0;
  }
  /**
   * Is the row toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isRowMenuShowing() {
    return this.selectedRow !== 0;
  }
  /**
   * Recalculate position of toolbox icons
   *
   * @param {Event} event - mouse move event
   */
  onMouseMoveInTable(e) {
    const { row: t, column: n } = this.getHoveredCell(e);
    this.hoveredColumn = n, this.hoveredRow = t, this.updateToolboxesPosition();
  }
  /**
   * Prevents default Enter behaviors
   * Adds Shift+Enter processing
   *
   * @param {KeyboardEvent} event - keypress event
   */
  onKeyPressListener(e) {
    if (e.key === "Enter") {
      if (e.shiftKey)
        return !0;
      this.moveCursorToNextRow();
    }
    return e.key !== "Enter";
  }
  /**
   * Prevents tab keydown event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyDownListener(e) {
    e.key === "Tab" && e.stopPropagation();
  }
  /**
   * Set the coordinates of the cell that the focus has moved to
   *
   * @param {FocusEvent} event - focusin event
   */
  focusInTableListener(e) {
    const t = e.target, n = this.getRowByCell(t);
    this.focusedCell = {
      row: Array.from(this.table.querySelectorAll(`.${Vt.row}`)).indexOf(n) + 1,
      column: Array.from(n.querySelectorAll(`.${Vt.cell}`)).indexOf(t) + 1
    };
  }
  /**
   * Unselect row/column
   * Close toolbox menu
   * Hide toolboxes
   *
   * @returns {void}
   */
  hideToolboxes() {
    this.hideRowToolbox(), this.hideColumnToolbox(), this.updateToolboxesPosition();
  }
  /**
   * Unselect row, close toolbox
   *
   * @returns {void}
   */
  hideRowToolbox() {
    this.unselectRow(), this.toolboxRow.hide();
  }
  /**
   * Unselect column, close toolbox
   *
   * @returns {void}
   */
  hideColumnToolbox() {
    this.unselectColumn(), this.toolboxColumn.hide();
  }
  /**
   * Set the cursor focus to the focused cell
   *
   * @returns {void}
   */
  focusCell() {
    this.focusedCellElem.focus();
  }
  /**
   * Get current focused element
   *
   * @returns {HTMLElement} - focused cell
   */
  get focusedCellElem() {
    const { row: e, column: t } = this.focusedCell;
    return this.getCell(e, t);
  }
  /**
   * Update toolboxes position
   *
   * @param {number} row - hovered row
   * @param {number} column - hovered column
   */
  updateToolboxesPosition(e = this.hoveredRow, t = this.hoveredColumn) {
  }
  /**
   * Makes the first row headings
   *
   * @param {boolean} withHeadings - use headings row or not
   */
  setHeadingsSetting(e) {
    this.tunes.withHeadings = e, e ? (this.table.classList.add(Vt.withHeadings), this.addHeadingAttrToFirstRow()) : (this.table.classList.remove(Vt.withHeadings), this.removeHeadingAttrFromFirstRow());
  }
  /**
   * Adds an attribute for displaying the placeholder in the cell
   */
  addHeadingAttrToFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.setAttribute("heading", this.api.i18n.t("Heading"));
    }
  }
  /**
   * Removes an attribute for displaying the placeholder in the cell
   */
  removeHeadingAttrFromFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.removeAttribute("heading");
    }
  }
  /**
   * Add effect of a selected row
   *
   * @param {number} index
   */
  selectRow(e) {
    const t = this.getRow(e);
    t && (this.selectedRow = e, t.classList.add(Vt.rowSelected));
  }
  /**
   * Remove effect of a selected row
   */
  unselectRow() {
    if (this.selectedRow <= 0)
      return;
    const e = this.table.querySelector(`.${Vt.rowSelected}`);
    e && e.classList.remove(Vt.rowSelected), this.selectedRow = 0;
  }
  /**
   * Add effect of a selected column
   *
   * @param {number} index
   */
  selectColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      n && n.classList.add(Vt.cellSelected);
    }
    this.selectedColumn = e;
  }
  /**
   * Remove effect of a selected column
   */
  unselectColumn() {
    if (this.selectedColumn <= 0)
      return;
    let e = this.table.querySelectorAll(`.${Vt.cellSelected}`);
    Array.from(e).forEach((t) => {
      t.classList.remove(Vt.cellSelected);
    }), this.selectedColumn = 0;
  }
  /**
   * Calculates the row and column that the cursor is currently hovering over
   * The search was optimized from O(n) to O (log n) via bin search to reduce the number of calculations
   *
   * @param {Event} event - mousemove event
   * @returns hovered cell coordinates as an integer row and column
   */
  getHoveredCell(e) {
    let t = this.hoveredRow, n = this.hoveredColumn;
    const { width: r, height: a, x: c, y: u } = yb(this.table, e);
    return c >= 0 && (n = this.binSearch(
      this.numberOfColumns,
      (p) => this.getCell(1, p),
      ({ fromLeftBorder: p }) => c < p,
      ({ fromRightBorder: p }) => c > r - p
    )), u >= 0 && (t = this.binSearch(
      this.numberOfRows,
      (p) => this.getCell(p, 1),
      ({ fromTopBorder: p }) => u < p,
      ({ fromBottomBorder: p }) => u > a - p
    )), {
      row: t || this.hoveredRow,
      column: n || this.hoveredColumn
    };
  }
  /**
   * Looks for the index of the cell the mouse is hovering over.
   * Cells can be represented as ordered intervals with left and
   * right (upper and lower for rows) borders inside the table, if the mouse enters it, then this is our index
   *
   * @param {number} numberOfCells - upper bound of binary search
   * @param {function} getCell - function to take the currently viewed cell
   * @param {function} beforeTheLeftBorder - determines the cursor position, to the left of the cell or not
   * @param {function} afterTheRightBorder - determines the cursor position, to the right of the cell or not
   * @returns {number}
   */
  binSearch(e, t, n, r) {
    let a = 0, c = e + 1, u = 0, p;
    for (; a < c - 1 && u < 10; ) {
      p = Math.ceil((a + c) / 2);
      const g = t(p), v = vb(this.table, g);
      if (n(v))
        c = p;
      else if (r(v))
        a = p;
      else
        break;
      u++;
    }
    return p;
  }
  /**
   * Collects data from cells into a two-dimensional array
   *
   * @returns {string[][]}
   */
  getData() {
    return this.currentData || {};
  }
  /**
   * Remove listeners on the document
   */
  destroy() {
    document.removeEventListener("click", this.documentClicked);
  }
};
const Xd = [
  {
    level: 1,
    title: "Activity Level",
    title_jp: "活動量",
    data: [
      {
        key: "activityLevel",
        type: "radio",
        options: [
          {
            label: "++",
            label_jp: "++",
            value: 1,
            default: !0
          },
          {
            label: "+",
            label_jp: "+",
            value: 2
          },
          {
            label: "+/-",
            label_jp: "+/-",
            value: 3
          },
          {
            label: "-",
            label_jp: "-",
            value: 4
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "Appetite",
    title_jp: "食欲",
    data: [
      {
        key: "appetite",
        type: "radio",
        options: [
          {
            label: "0%",
            label_jp: "0%",
            value: 1,
            default: !0
          },
          {
            label: "30%",
            label_jp: "30%",
            value: 2
          },
          {
            label: "50%",
            label_jp: "50%",
            value: 3
          },
          {
            label: "70%",
            label_jp: "70%",
            value: 4
          },
          {
            label: "100%",
            label_jp: "100%",
            value: 5
          },
          {
            label: ">100%",
            label_jp: ">100%",
            value: 6
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "BCS",
    title_jp: "ボディコンディションスコア",
    data: [
      {
        key: "bcs",
        type: "radio",
        options: [
          {
            label: "1",
            label_jp: "1",
            value: 1,
            default: !0
          },
          {
            label: "2",
            label_jp: "2",
            value: 2
          },
          {
            label: "3",
            label_jp: "3",
            value: 3
          },
          {
            label: "4",
            label_jp: "4",
            value: 4
          },
          {
            label: "5",
            label_jp: "5",
            value: 5
          },
          {
            label: "6",
            label_jp: "6",
            value: 6
          },
          {
            label: "7",
            label_jp: "7",
            value: 7
          },
          {
            label: "8",
            label_jp: "8",
            value: 8
          },
          {
            label: "9",
            label_jp: "9",
            value: 9
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "Water Intake",
    title_jp: "飲水量",
    data: [
      {
        key: "waterIntake",
        type: "radio",
        options: [
          {
            label: "Drinking more than usual",
            label_jp: "いつもよりたくさん飲んでいる",
            default: !0,
            value: 1
          },
          {
            label: "Drinking as usual",
            label_jp: "普段通り飲んでいる",
            value: 2
          },
          {
            label: "Not drinking as much as usual",
            label_jp: "いつもより飲んでいない",
            value: 3
          },
          {
            label: "Not drinking at all",
            label_jp: "全く飲まない",
            value: 4
          }
        ]
      }
    ]
  },
  {
    title: "Food",
    title_jp: "食事",
    level: 0,
    data: [
      {
        key: "food",
        type: "disabled"
      }
    ]
  },
  {
    title: "Frequency of urination",
    title_jp: "排尿回数",
    level: 0,
    data: [
      {
        key: "urinFrequency",
        type: "text"
      }
    ]
  },
  {
    level: 1,
    title: "Urine color",
    title_jp: "尿色",
    data: [
      {
        key: "urineColor",
        type: "radio",
        options: [
          {
            default: !0,
            label: "Normal",
            label_jp: "正常",
            value: 0
          },
          {
            label: "Abnormal",
            label_jp: "異常",
            value: 1
          },
          {
            label: "Yellow",
            label_jp: "黄色",
            value: 2
          },
          {
            label: "Dark yellow",
            label_jp: "濃い黄色",
            value: 3
          },
          {
            label: "Almost transparent",
            label_jp: "ほぼ透明",
            value: 4
          },
          {
            label: "Orange",
            label_jp: "オレンジ色",
            value: 5
          },
          {
            label: "Brown",
            label_jp: "茶色",
            value: 6
          },
          {
            label: "Red (hematuria)",
            label_jp: "赤 (血尿)",
            value: 7
          }
        ]
      }
    ]
  },
  {
    title: "Frequency of defecation",
    title_jp: "排便回数",
    level: 0,
    data: [
      {
        key: "defecationFrequency",
        type: "text"
      }
    ]
  },
  {
    title: "Defecation",
    title_jp: "排便性状",
    level: 2,
    data: [
      {
        title: "Hardness",
        title_jp: "硬さ",
        key: "defacationHardness",
        type: "radio",
        options: [
          {
            default: !0,
            label: "Normal",
            label_jp: "正常",
            value: "1"
          },
          {
            label: "Abnormal",
            label_jp: "異常",
            value: "2"
          },
          {
            label: "Type1",
            label_jp: "コロコロ便",
            value: "3"
          },
          {
            label: "Type2",
            label_jp: "硬い便",
            value: "4"
          },
          {
            label: "Type3",
            label_jp: "やや硬い便",
            value: "5"
          },
          {
            label: "Type4",
            label_jp: "普通便",
            value: "6"
          },
          {
            label: "Type5",
            label_jp: "やや軟らかい便",
            value: "7"
          },
          {
            label: "Type6",
            label_jp: "泥状便",
            value: "8"
          },
          {
            label: "Type7",
            label_jp: "水様便",
            value: "9"
          }
        ]
      },
      {
        title: "Color",
        title_jp: "色",
        type: "radio",
        key: "defacationColor",
        options: [
          {
            default: !0,
            label: "Normal",
            label_jp: "正常",
            value: "1"
          },
          {
            label: "Abnormal",
            label_jp: "異常",
            value: "2"
          },
          {
            label: "Whitish",
            label_jp: "白色",
            value: "3"
          },
          {
            label: "Black",
            label_jp: "黒色（タール便）",
            value: "4"
          },
          {
            label: "Dark brown",
            label_jp: "黒褐色",
            value: "5"
          },
          {
            label: "Brown",
            label_jp: "茶色",
            value: "6"
          },
          {
            label: "Yellow",
            label_jp: "黄色",
            value: "7"
          },
          {
            label: "Red (bloody stools)",
            label_jp: "赤色 (血液)",
            value: "8"
          }
        ]
      }
    ]
  },
  {
    title: "Vomiting",
    title_jp: "嘔吐",
    level: 0,
    data: [
      {
        key: "vomiting",
        type: "text"
      }
    ]
  },
  {
    level: 1,
    title: "MCS",
    title_jp: "マッスルコンディションスコア",
    data: [
      {
        key: "mcs",
        type: "radio",
        options: [
          {
            default: !0,
            label_jp: "正常",
            label: "Normal muscle mass",
            value: "1"
          },
          {
            label_jp: "軽度筋肉量低下",
            label: "Mild muscle loss",
            value: "2"
          },
          {
            label_jp: "中度筋肉量低下",
            label: "Moderate muscle loss",
            value: "3"
          },
          {
            label_jp: "重度筋肉量低下",
            label: "Severe muscle loss",
            value: "4"
          }
        ]
      }
    ]
  },
  {
    title: "Auscultatory Findings",
    title_jp: "聴診所見",
    level: 2,
    data: [
      {
        title: "Heart",
        key: "ausculatoryHeart",
        title_jp: "心臓",
        type: "radio",
        options: [
          {
            label_jp: "正常",
            label: "Normal",
            value: "1"
          },
          {
            label_jp: "異常",
            label: "Abnormal",
            value: "2"
          }
        ]
      },
      {
        title: "Lung",
        title_jp: "肺",
        key: "ausculatoryLung",
        type: "radio",
        options: [
          {
            label_jp: "正常",
            label: "Normal",
            value: "1"
          },
          {
            label_jp: "異常",
            label: "Abnormal",
            value: "2"
          }
        ]
      }
    ]
  },
  {
    title: "Lymph Node Examination (Dog)",
    title_jp: "聴診所見",
    level: 2,
    data: [
      {
        title: "Mandibular (Left)",
        key: "mandibularLeft",
        title_jp: "下顎リンパ節（左）",
        type: "text",
        options: []
      },
      {
        title: "Mandibular (Right)",
        key: "mandibularRight",
        title_jp: "下顎リンパ節（右）",
        type: "text",
        options: []
      },
      {
        title: "Swollen cervical (Left)",
        key: "cervicalLeft",
        title_jp: "浅頸リンパ節（左）",
        type: "text",
        options: []
      },
      {
        title: "Swollen cervical (Right)",
        key: "cervicalRight",
        title_jp: "浅頸リンパ節（右）",
        type: "text",
        options: []
      },
      {
        title: "Axillary(Left)",
        key: "axillaryLeft",
        title_jp: "腋窩リンパ節（左）",
        type: "text",
        options: []
      },
      {
        title: "Axillary(Right)",
        key: "axillaryRight",
        title_jp: "腋窩リンパ節（右）",
        type: "text",
        options: []
      },
      {
        title: "Collateral axillary (Left)",
        key: "collateralAxillaryLeft",
        title_jp: "副腋窩リンパ節（左）",
        type: "text",
        options: []
      },
      {
        title: "Collateral axillary (Right)",
        key: "collateralAxillaryRight",
        title_jp: "副腋窩リンパ節（右）",
        type: "text",
        options: []
      },
      {
        title: "Inguinal (Left)",
        key: "inguinalLeft",
        title_jp: "鼠径リンパ節（左）",
        type: "text",
        options: []
      },
      {
        title: "Inguinal (Right)",
        key: "inguinalRight",
        title_jp: "鼠径リンパ節（右）",
        type: "text",
        options: []
      }
    ]
  },
  {
    title: "SpO2",
    title_jp: "SpO2",
    level: 0,
    data: [
      {
        key: "spo2",
        type: "text"
      }
    ]
  },
  {
    title: "Blood Pressure",
    title_jp: "血圧",
    level: 2,
    data: [
      {
        key: "bpMin",
        title: "Min",
        title_jp: "最低",
        type: "text"
      },
      {
        key: "bpMax",
        title: "Max",
        title_jp: "最高",
        type: "text"
      },
      {
        key: "bpAve",
        title: "Ave",
        title_jp: "平均",
        type: "text"
      },
      {
        key: "bpHeartRate",
        title: "Heart Rate",
        title_jp: "心拍数",
        type: "text"
      },
      {
        key: "bpMeasurementMode",
        title: "Measurement Mode",
        title_jp: "測定方法",
        type: "radio",
        options: [
          {
            label: "Oscillometric",
            label_jp: "オシロメトリック法",
            value: 1
          },
          {
            label: "Doppler",
            label_jp: "ドップラー法",
            value: 2
          }
        ]
      },
      {
        key: "bpMeasurementArea",
        title: "Measuring Area",
        title_jp: "測定部位",
        type: "radio",
        options: [
          {
            label: "Forelimb",
            label_jp: "前肢",
            value: 1
          },
          {
            label: "Hindlimb",
            label_jp: "後肢",
            value: 2
          },
          {
            label: "Tail",
            label_jp: "尾部",
            value: 3
          }
        ]
      },
      {
        key: "bpPositioning",
        title: "Positioning",
        title_jp: "測定体位",
        type: "radio",
        options: [
          {
            label: "Standing",
            label_jp: "立位",
            value: 1
          },
          {
            label: "Seated",
            label_jp: "座位",
            value: 2
          },
          {
            label: "Transverse position",
            label_jp: "横臥位",
            value: 3
          },
          {
            label: "Prone position",
            label_jp: "伏臥位",
            value: 4
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "Visible Mucous Membranes",
    title_jp: "可視粘膜色",
    data: [
      {
        key: "mucousMembrane",
        type: "radio",
        options: [
          {
            default: !0,
            label: "Normal",
            label_jp: "正常",
            value: 1
          },
          {
            label: "Pink",
            label_jp: "ピンク",
            value: 2
          },
          {
            label: "Hyperemia",
            label_jp: "充血",
            value: 3
          },
          {
            label: "Pallor",
            label_jp: "蒼白",
            value: 4
          },
          {
            label: "Jaundice",
            label_jp: "黄疸",
            value: 5
          },
          {
            label: "Cyanosis",
            label_jp: "チアノーゼ",
            value: 6
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "CRT",
    title_jp: "毛細血管再充足時間",
    data: [
      {
        key: "crt",
        type: "radio",
        options: [
          {
            default: !0,
            label: "Within 1 second (shortened)",
            label_jp: "1秒以内（短縮）",
            value: "1"
          },
          {
            label: "1-2 seconds (normal)",
            label_jp: "1～2秒（正常）",
            value: "2"
          },
          {
            label: "More than 2 seconds (extended)",
            label_jp: "2秒以上（延長）",
            value: "3"
          }
        ]
      }
    ]
  },
  {
    level: 1,
    title: "Aortic Pulsation Assessment",
    title_jp: "大腿動脈拍動の評価",
    data: [
      {
        key: "aorticPulsation",
        type: "radio",
        options: [
          {
            default: !0,
            label: "Normal",
            label_jp: "正常",
            value: "1"
          },
          {
            label: "Strong",
            label_jp: "強い（バウンディングパルス）",
            value: "2"
          },
          {
            label: "Weak",
            label_jp: "弱い",
            value: "3"
          },
          {
            label: "Not palpable",
            label_jp: "触知不可",
            value: "4"
          },
          {
            label: "Unpalpable only on right",
            label_jp: "右のみ触知不可",
            value: "5"
          },
          {
            label: "Unpalpable only on left",
            label_jp: "左のみ触知不可",
            value: "6"
          }
        ]
      }
    ]
  },
  {
    title: "Hydration Assessment",
    title_jp: "水和評価",
    level: 1,
    data: [
      {
        key: "hydration",
        type: "radio",
        options: [
          {
            default: !0,
            label: "Normal",
            label_jp: "正常",
            value: "1"
          },
          {
            label: "5%",
            label_jp: "5%",
            value: "2"
          },
          {
            label: "6-8%",
            label_jp: "6-8%",
            value: "3"
          },
          {
            label: "10-12%",
            label_jp: "10-12%",
            value: "4"
          },
          {
            label: "15%",
            label_jp: "15%",
            value: "5"
          }
        ]
      }
    ]
  }
];
let Db = class {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to press Enter inside the CodeTool textarea
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {TableData} data — previously saved data
   * @param {TableConfig} config - user config for Tool
   * @param {object} api - Editor.js API
   * @param {boolean} readOnly - read-only mode flag
   */
  constructor(e) {
    var t;
    this.items = [];
    const { data: n, config: r, api: a, readOnly: c, block: u } = e;
    this.title = a.i18n.t("PE/ROS"), this.api = a, this.block = u, this.readOnly = c, this.config = r, this.data = {
      data: n && n.data && ((t = Object.keys(n.data)) == null ? void 0 : t.length) != 0 ? JSON.parse(JSON.stringify(n.data)) : {}
    }, this.options = {}, this.createOptions(), this.createItems(), this.table = null, setTimeout(() => {
      if (n != null && n.data)
        return;
      const p = this.block.id;
      this.api.toolbar.toggleBlockSettingsById(p);
    }, 500);
  }
  onActivate(e) {
    if (this.table.remove(e)) {
      this.table.fillInput();
      return;
    }
    this.table.setValue(e, ""), this.table.fillInput();
  }
  checkIfActive(e) {
    var t;
    let n = ((t = this.table) == null ? void 0 : t.getData()) || {};
    return e in n;
  }
  createOptions() {
    var e;
    const t = ((e = this.config) == null ? void 0 : e.locale) === "jp";
    this.options = {}, Xd.map((n) => {
      n.data.map((r) => {
        let a = t ? n.title_jp : n.title;
        r != null && r.title && (a += ` (${t ? r.title_jp : r.title})`), this.options[r.key] = { ...r, label: a };
      });
    });
  }
  createItems() {
    var e;
    const t = ((e = this.config) == null ? void 0 : e.locale) === "jp";
    this.items = [], Xd.map((n) => {
      const r = this.checkIfActive(n.data[0].key);
      this.items.push({
        label: t ? `${n.title_jp}` : `${n.title}`,
        icon: Tb,
        isActive: r,
        closeOnActivate: !0,
        toggle: !0,
        onActivate: () => {
          this.table.setData(n.data), this.table.fillInput();
        }
      });
    });
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: _b,
      title: this.title
    };
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLDivElement}
   */
  render() {
    this.table = new Mb(this.readOnly, this.api, this.data, this.config, this.options, this.block), this.container = go("div", this.api.styles.block);
    const e = go("h5", "tc-header");
    return e.innerHTML = this.title, this.container.appendChild(e), this.container.appendChild(this.table.getWrapper()), this.table.setHeadingsSetting(!0), this.container;
  }
  /**
   * Returns plugin settings
   *
   * @returns {Array}
   */
  renderSettings() {
    return this.createItems(), this.items;
  }
  /**
   * Extract table data from the view
   *
   * @returns {TableData} - saved data
   */
  save() {
    return {
      data: this.table.getData()
    };
  }
  /**
   * Plugin destroyer
   *
   * @returns {void}
   */
  destroy() {
    this.table.destroy();
  }
  /**
   * A helper to get config value.
   * 
   * @param {string} configName - the key to get from the config. 
   * @param {any} defaultValue - default value if config doesn't have passed key
   * @param {object} savedData - previously saved data. If passed, the key will be got from there, otherwise from the config
   * @returns {any} - config value.
   */
  getConfig(e, t = void 0, n = void 0) {
    const r = this.data || n;
    return r ? r[e] ? r[e] : t : this.config && this.config[e] ? this.config[e] : t;
  }
  // /**  
  //  * Table onPaste configuration
  //  *
  //  * @public
  //  */
  // static get pasteConfig() {
  //   return { tags: ['TABLE', 'TR', 'TH', 'TD'] };
  // }
  // /**
  //  * On paste callback that is fired from Editor
  //  *
  //  * @param {PasteEvent} event - event with pasted data
  //  */
  // onPaste(event) {
  //   const table = event.detail.data;
  //   /** Check if the first row is a header */
  //   const firstRowHeading = table.querySelector(':scope > thead, tr:first-of-type th');
  //   /** Get all rows from the table */
  //   const rows = Array.from(table.querySelectorAll('tr'));
  //   /** Generate a content matrix */
  //   const content = rows.map((row) => {
  //     /** Get cells from row */
  //     const cells = Array.from(row.querySelectorAll('th, td'))
  //     /** Return cells content */
  //     return cells.map((cell) => cell.innerHTML);
  //   });
  //   /** Update Tool's data */
  //   this.data = {
  //     withHeadings: firstRowHeading !== null,
  //     content
  //   };
  //   /** Update table block */
  //   if (this.table.wrapper) {
  //     this.table.wrapper.replaceWith(this.render());
  //   }
  // }
};
(function() {
  var o;
  try {
    if (typeof document < "u") {
      var e = document.createElement("style");
      e.nonce = (o = document.head.querySelector("meta[property=csp-nonce]")) == null ? void 0 : o.content, e.appendChild(document.createTextNode('.daterangepicker{position:absolute;color:inherit;background-color:#fff;border-radius:4px;border:1px solid #ddd;width:278px;max-width:none;padding:0;margin-top:7px;top:100px;left:20px;z-index:3001;display:none;font-family:arial;font-size:15px;line-height:1em}.daterangepicker:after,.daterangepicker:before{position:absolute;display:inline-block;border-bottom-color:#0003;content:""}.daterangepicker:before{top:-7px;border-right:7px solid transparent;border-left:7px solid transparent;border-bottom:7px solid #ccc}.daterangepicker:after{top:-6px;border-right:6px solid transparent;border-bottom:6px solid #fff;border-left:6px solid transparent}.daterangepicker.opensleft:before{right:9px}.daterangepicker.opensleft:after{right:10px}.daterangepicker.openscenter:after,.daterangepicker.openscenter:before{left:0;right:0;width:0;margin-left:auto;margin-right:auto}.daterangepicker.opensright:before{left:9px}.daterangepicker.opensright:after{left:10px}.daterangepicker.drop-up{margin-top:-7px}.daterangepicker.drop-up:before{top:auto;bottom:-7px;border-bottom:initial;border-top:7px solid #ccc}.daterangepicker.drop-up:after{top:auto;bottom:-6px;border-bottom:initial;border-top:6px solid #fff}.daterangepicker.single .daterangepicker .ranges,.daterangepicker.single .drp-calendar{float:none}.daterangepicker.single .drp-selected{display:none}.daterangepicker.show-calendar .drp-buttons,.daterangepicker.show-calendar .drp-calendar{display:block}.daterangepicker.auto-apply .drp-buttons{display:none}.daterangepicker .drp-calendar{display:none;max-width:270px}.daterangepicker .drp-calendar.left{padding:8px 0 8px 8px}.daterangepicker .drp-calendar.right{padding:8px}.daterangepicker .drp-calendar.single .calendar-table{border:none}.daterangepicker .calendar-table .next span,.daterangepicker .calendar-table .prev span{color:#fff;border:solid #000;border-width:0 2px 2px 0;border-radius:0;display:inline-block;padding:3px}.daterangepicker .calendar-table .next span{transform:rotate(-45deg);-webkit-transform:rotate(-45deg)}.daterangepicker .calendar-table .prev span{transform:rotate(135deg);-webkit-transform:rotate(135deg)}.daterangepicker .calendar-table td,.daterangepicker .calendar-table th{text-align:center;vertical-align:middle;min-width:32px;width:32px;height:24px;line-height:24px;font-size:12px;border-radius:4px;border:1px solid transparent;white-space:nowrap;cursor:pointer}.daterangepicker .calendar-table{border:1px solid #fff;border-radius:4px;background-color:#fff}.daterangepicker .calendar-table table{width:100%;margin:0;border-spacing:0;border-collapse:collapse}.daterangepicker td.available:hover,.daterangepicker th.available:hover{background-color:#eee;border-color:transparent;color:inherit}.daterangepicker td.week,.daterangepicker th.week{font-size:80%;color:#ccc}.daterangepicker td.off,.daterangepicker td.off.end-date,.daterangepicker td.off.in-range,.daterangepicker td.off.start-date{background-color:#fff;border-color:transparent;color:#999}.daterangepicker td.in-range{background-color:#ebf4f8;border-color:transparent;color:#000;border-radius:0}.daterangepicker td.start-date{border-radius:4px 0 0 4px}.daterangepicker td.end-date{border-radius:0 4px 4px 0}.daterangepicker td.start-date.end-date{border-radius:4px}.daterangepicker td.active,.daterangepicker td.active:hover{background-color:#357ebd;border-color:transparent;color:#fff}.daterangepicker th.month{width:auto}.daterangepicker option.disabled,.daterangepicker td.disabled{color:#999;cursor:not-allowed;text-decoration:line-through}.daterangepicker select.monthselect,.daterangepicker select.yearselect{font-size:12px;padding:1px;height:auto;margin:0;cursor:default}.daterangepicker select.monthselect{margin-right:2%;width:56%}.daterangepicker select.yearselect{width:40%}.daterangepicker select.ampmselect,.daterangepicker select.hourselect,.daterangepicker select.minuteselect,.daterangepicker select.secondselect{width:50px;margin:0 auto;background:#eee;border:1px solid #eee;padding:2px;outline:0;font-size:12px}.daterangepicker .calendar-time{text-align:center;margin:4px auto 0;line-height:30px;position:relative}.daterangepicker .calendar-time select.disabled{color:#ccc;cursor:not-allowed}.daterangepicker .drp-buttons{clear:both;text-align:right;padding:8px;border-top:1px solid #ddd;display:none;line-height:12px;vertical-align:middle}.daterangepicker .drp-selected{display:inline-block;font-size:12px;padding-right:8px}.daterangepicker .drp-buttons .btn{margin-left:8px;font-size:12px;font-weight:700;padding:4px 8px}.daterangepicker.show-ranges.single.rtl .drp-calendar.left{border-right:1px solid #ddd}.daterangepicker.show-ranges.single.ltr .drp-calendar.left{border-left:1px solid #ddd}.daterangepicker.show-ranges.rtl .drp-calendar.right{border-right:1px solid #ddd}.daterangepicker.show-ranges.ltr .drp-calendar.left{border-left:1px solid #ddd}.daterangepicker .ranges{float:none;text-align:left;margin:0}.daterangepicker.show-calendar .ranges{margin-top:8px}.daterangepicker .ranges ul{list-style:none;margin:0 auto;padding:0;width:100%}.daterangepicker .ranges li{font-size:12px;padding:8px 12px;cursor:pointer}.daterangepicker .ranges li:hover{background-color:#eee}.daterangepicker .ranges li.active{background-color:#08c;color:#fff}@media (min-width:564px){.daterangepicker{width:auto}.daterangepicker .ranges ul{width:140px}.daterangepicker.single .ranges ul{width:100%}.daterangepicker.single .drp-calendar.left{clear:none}.daterangepicker.single .drp-calendar,.daterangepicker.single .ranges{float:left}.daterangepicker{direction:ltr;text-align:left}.daterangepicker .drp-calendar.left{clear:left;margin-right:0}.daterangepicker .drp-calendar.left .calendar-table{border-right:none;border-top-right-radius:0;border-bottom-right-radius:0}.daterangepicker .drp-calendar.right{margin-left:0}.daterangepicker .drp-calendar.right .calendar-table{border-left:none;border-top-left-radius:0;border-bottom-left-radius:0}.daterangepicker .drp-calendar.left .calendar-table{padding-right:8px}.daterangepicker .drp-calendar,.daterangepicker .ranges{float:left}}@media (min-width:730px){.daterangepicker .ranges{width:auto;float:left}.daterangepicker.rtl .ranges{float:right}.daterangepicker .drp-calendar.left{clear:none!important}}.tc-header{margin:0;padding:0;position:relative;bottom:-1rem;text-align:left}.tc-wrap{--color-background:#f9f9fb;--color-text-secondary:#7b7e89;--color-border:#e8e8eb;--cell-size:34px;--toolbox-icon-size:18px;--toolbox-padding:6px;--toolbox-aiming-field-size:calc(var(--toolbox-icon-size) + var(--toolbox-padding)*2);border-left:0;margin-top:var(--toolbox-icon-size);box-sizing:border-box;grid-template-columns:calc(100% - var(--cell-size)) var(--cell-size)}.tc-wrap--readonly{grid-template-columns:100% var(--cell-size)}.tc-wrap svg{vertical-align:top}@media print{.tc-wrap{border-left-color:var(--color-border);border-left-style:solid;border-left-width:1px;grid-template-columns:100% var(--cell-size)}}@media print{.tc-wrap .tc-row:after{display:none}}.tc-table,.tc-wrap{position:relative;height:100%;width:100%;display:grid}.tc-table{font-size:14px;border-top:1px solid var(--color-border);line-height:1.4}.tc-table:after{width:calc(var(--cell-size));left:calc(var(--cell-size)*-1);top:0}.tc-table:after,.tc-table:before{position:absolute;content:""}.tc-table:before{height:var(--toolbox-aiming-field-size);top:calc(var(--toolbox-aiming-field-size)*-1);left:0}.tc-table--heading .tc-row:first-child{font-weight:600;border-bottom:2px solid var(--color-border)}.tc-table--heading .tc-row:first-child [contenteditable]:empty:before{content:attr(heading);color:var(--color-text-secondary)}.tc-table--heading .tc-row:first-child:after{bottom:-2px;border-bottom:2px solid var(--color-border)}.tc-add-column,.tc-add-row{display:flex;color:var(--color-text-secondary)}@media print{.tc-add{display:none}}.tc-add-column{padding:4px 0;justify-content:center;border-top:1px solid var(--color-border);visibility:hidden}@media print{.tc-add-column{display:none}}.tc-add-row{height:var(--cell-size);align-items:center;padding-left:4px;position:relative}.tc-add-row:before{content:"";position:absolute;right:calc(var(--cell-size)*-1);width:var(--cell-size);height:100%}@media print{.tc-add-row{display:none}}.tc-add-column,.tc-add-row{transition:0s;cursor:pointer;will-change:background-color}.tc-add-column:hover,.tc-add-row:hover{transition:background-color .1s ease;background-color:var(--color-background)}.tc-add-row{margin-top:1px}.tc-add-row:hover:before{transition:.1s;background-color:var(--color-background)}.tc-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(10px,1fr));position:relative;border-bottom:1px solid var(--color-border)}.tc-row:after{content:"";pointer-events:none;position:absolute;width:var(--cell-size);height:100%;bottom:-1px;right:calc(var(--cell-size)*-1);border-bottom:1px solid var(--color-border)}.tc-row--selected{background:var(--color-background)}.tc-row--selected:after{background:var(--color-background)}.tc-cell{border-right:1px solid var(--color-border);padding:6px 12px;overflow:hidden;outline:none;line-break:normal}.tc-cell input{outline:none;border:none;width:100%;height:100%;padding:0;margin:0}.tc-cell--selected{background:var(--color-background)}.tc-wrap--readonly .tc-row:after{display:none}.tc-toolbox{--toolbox-padding:6px;--popover-margin:30px;--toggler-click-zone-size:30px;--toggler-dots-color:#7b7e89;--toggler-dots-color-hovered:#1d202b;position:absolute;cursor:pointer;z-index:1;opacity:0;transition:opacity .1s;will-change:left,opacity}.tc-toolbox--column{top:calc(var(--toggler-click-zone-size)*-1);transform:translate(calc(var(--toggler-click-zone-size)*-1/2));will-change:left,opacity}.tc-toolbox--row{left:calc(var(--popover-margin)*-1);transform:translateY(calc(var(--toggler-click-zone-size)*-1/2));margin-top:-1px;will-change:top,opacity}.tc-toolbox--showed{opacity:1}.tc-toolbox .tc-popover{position:absolute;top:0;left:var(--popover-margin)}.tc-toolbox__toggler{display:flex;align-items:center;justify-content:center;width:var(--toggler-click-zone-size);height:var(--toggler-click-zone-size);color:var(--toggler-dots-color);opacity:0;transition:opacity .15s ease;will-change:opacity}.tc-toolbox__toggler:hover{color:var(--toggler-dots-color-hovered)}.tc-toolbox__toggler svg{fill:currentColor}.tc-wrap:hover .tc-toolbox__toggler{opacity:1}.tc-settings .cdx-settings-button{width:50%;margin:0}.tc-popover{--color-border:#eaeaea;--color-background:#fff;--color-background-hover:rgba(232,232,235,.49);--color-background-confirm:#e24a4a;--color-background-confirm-hover:#d54040;--color-text-confirm:#fff;background:var(--color-background);border:1px solid var(--color-border);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;padding:6px;display:none;will-change:opacity,transform}.tc-popover--opened{display:block;animation:menuShowing .1s cubic-bezier(.215,.61,.355,1) forwards}.tc-popover__item{display:flex;align-items:center;padding:2px 14px 2px 2px;border-radius:5px;cursor:pointer;white-space:nowrap;-webkit-user-select:none;-moz-user-select:none;user-select:none}.tc-popover__item:hover{background:var(--color-background-hover)}.tc-popover__item:not(:last-of-type){margin-bottom:2px}.tc-popover__item-icon{display:inline-flex;width:26px;height:26px;align-items:center;justify-content:center;background:var(--color-background);border-radius:5px;border:1px solid var(--color-border);margin-right:8px}.tc-popover__item-label{line-height:22px;font-size:14px;font-weight:500}.tc-popover__item--confirm{background:var(--color-background-confirm);color:var(--color-text-confirm)}.tc-popover__item--confirm:hover{background-color:var(--color-background-confirm-hover)}.tc-popover__item--confirm .tc-popover__item-icon{background:var(--color-background-confirm);border-color:#0000001a}.tc-popover__item--confirm .tc-popover__item-icon svg{transition:transform .2s ease-in;transform:rotate(90deg) scale(1.2)}.tc-popover__item--hidden{display:none}@keyframes menuShowing{0%{opacity:0;transform:translateY(-8px) scale(.9)}70%{opacity:1;transform:translateY(2px)}to{transform:translateY(0)}}')), document.head.appendChild(e);
    }
  } catch (t) {
    console.error("vite-plugin-css-injected-by-js", t);
  }
})();
var Lb = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Ob(o) {
  return o && o.__esModule && Object.prototype.hasOwnProperty.call(o, "default") ? o.default : o;
}
var mu = { exports: {} };
/*!
 * jQuery JavaScript Library v3.7.1
 * https://jquery.com/
 *
 * Copyright OpenJS Foundation and other contributors
 * Released under the MIT license
 * https://jquery.org/license
 *
 * Date: 2023-08-28T13:37Z
 */
(function(o) {
  (function(e, t) {
    o.exports = e.document ? t(e, !0) : function(n) {
      if (!n.document)
        throw new Error("jQuery requires a window with a document");
      return t(n);
    };
  })(typeof window < "u" ? window : Lb, function(e, t) {
    var n = [], r = Object.getPrototypeOf, a = n.slice, c = n.flat ? function(i) {
      return n.flat.call(i);
    } : function(i) {
      return n.concat.apply([], i);
    }, u = n.push, p = n.indexOf, g = {}, v = g.toString, k = g.hasOwnProperty, y = k.toString, T = y.call(Object), x = {}, M = function(i) {
      return typeof i == "function" && typeof i.nodeType != "number" && typeof i.item != "function";
    }, L = function(i) {
      return i != null && i === i.window;
    }, Y = e.document, I = {
      type: !0,
      src: !0,
      nonce: !0,
      noModule: !0
    };
    function F(i, s, l) {
      l = l || Y;
      var h, f, m = l.createElement("script");
      if (m.text = i, s)
        for (h in I)
          f = s[h] || s.getAttribute && s.getAttribute(h), f && m.setAttribute(h, f);
      l.head.appendChild(m).parentNode.removeChild(m);
    }
    function Q(i) {
      return i == null ? i + "" : typeof i == "object" || typeof i == "function" ? g[v.call(i)] || "object" : typeof i;
    }
    var U = "3.7.1", z = /HTML$/i, d = function(i, s) {
      return new d.fn.init(i, s);
    };
    d.fn = d.prototype = {
      // The current version of jQuery being used
      jquery: U,
      constructor: d,
      // The default length of a jQuery object is 0
      length: 0,
      toArray: function() {
        return a.call(this);
      },
      // Get the Nth element in the matched element set OR
      // Get the whole matched element set as a clean array
      get: function(i) {
        return i == null ? a.call(this) : i < 0 ? this[i + this.length] : this[i];
      },
      // Take an array of elements and push it onto the stack
      // (returning the new matched element set)
      pushStack: function(i) {
        var s = d.merge(this.constructor(), i);
        return s.prevObject = this, s;
      },
      // Execute a callback for every element in the matched set.
      each: function(i) {
        return d.each(this, i);
      },
      map: function(i) {
        return this.pushStack(d.map(this, function(s, l) {
          return i.call(s, l, s);
        }));
      },
      slice: function() {
        return this.pushStack(a.apply(this, arguments));
      },
      first: function() {
        return this.eq(0);
      },
      last: function() {
        return this.eq(-1);
      },
      even: function() {
        return this.pushStack(d.grep(this, function(i, s) {
          return (s + 1) % 2;
        }));
      },
      odd: function() {
        return this.pushStack(d.grep(this, function(i, s) {
          return s % 2;
        }));
      },
      eq: function(i) {
        var s = this.length, l = +i + (i < 0 ? s : 0);
        return this.pushStack(l >= 0 && l < s ? [this[l]] : []);
      },
      end: function() {
        return this.prevObject || this.constructor();
      },
      // For internal use only.
      // Behaves like an Array's method, not like a jQuery method.
      push: u,
      sort: n.sort,
      splice: n.splice
    }, d.extend = d.fn.extend = function() {
      var i, s, l, h, f, m, b = arguments[0] || {}, E = 1, _ = arguments.length, O = !1;
      for (typeof b == "boolean" && (O = b, b = arguments[E] || {}, E++), typeof b != "object" && !M(b) && (b = {}), E === _ && (b = this, E--); E < _; E++)
        if ((i = arguments[E]) != null)
          for (s in i)
            h = i[s], !(s === "__proto__" || b === h) && (O && h && (d.isPlainObject(h) || (f = Array.isArray(h))) ? (l = b[s], f && !Array.isArray(l) ? m = [] : !f && !d.isPlainObject(l) ? m = {} : m = l, f = !1, b[s] = d.extend(O, m, h)) : h !== void 0 && (b[s] = h));
      return b;
    }, d.extend({
      // Unique for each copy of jQuery on the page
      expando: "jQuery" + (U + Math.random()).replace(/\D/g, ""),
      // Assume jQuery is ready without the ready module
      isReady: !0,
      error: function(i) {
        throw new Error(i);
      },
      noop: function() {
      },
      isPlainObject: function(i) {
        var s, l;
        return !i || v.call(i) !== "[object Object]" ? !1 : (s = r(i), s ? (l = k.call(s, "constructor") && s.constructor, typeof l == "function" && y.call(l) === T) : !0);
      },
      isEmptyObject: function(i) {
        var s;
        for (s in i)
          return !1;
        return !0;
      },
      // Evaluates a script in a provided context; falls back to the global one
      // if not specified.
      globalEval: function(i, s, l) {
        F(i, { nonce: s && s.nonce }, l);
      },
      each: function(i, s) {
        var l, h = 0;
        if (j(i))
          for (l = i.length; h < l && s.call(i[h], h, i[h]) !== !1; h++)
            ;
        else
          for (h in i)
            if (s.call(i[h], h, i[h]) === !1)
              break;
        return i;
      },
      // Retrieve the text value of an array of DOM nodes
      text: function(i) {
        var s, l = "", h = 0, f = i.nodeType;
        if (!f)
          for (; s = i[h++]; )
            l += d.text(s);
        return f === 1 || f === 11 ? i.textContent : f === 9 ? i.documentElement.textContent : f === 3 || f === 4 ? i.nodeValue : l;
      },
      // results is for internal usage only
      makeArray: function(i, s) {
        var l = s || [];
        return i != null && (j(Object(i)) ? d.merge(
          l,
          typeof i == "string" ? [i] : i
        ) : u.call(l, i)), l;
      },
      inArray: function(i, s, l) {
        return s == null ? -1 : p.call(s, i, l);
      },
      isXMLDoc: function(i) {
        var s = i && i.namespaceURI, l = i && (i.ownerDocument || i).documentElement;
        return !z.test(s || l && l.nodeName || "HTML");
      },
      // Support: Android <=4.0 only, PhantomJS 1 only
      // push.apply(_, arraylike) throws on ancient WebKit
      merge: function(i, s) {
        for (var l = +s.length, h = 0, f = i.length; h < l; h++)
          i[f++] = s[h];
        return i.length = f, i;
      },
      grep: function(i, s, l) {
        for (var h, f = [], m = 0, b = i.length, E = !l; m < b; m++)
          h = !s(i[m], m), h !== E && f.push(i[m]);
        return f;
      },
      // arg is for internal usage only
      map: function(i, s, l) {
        var h, f, m = 0, b = [];
        if (j(i))
          for (h = i.length; m < h; m++)
            f = s(i[m], m, l), f != null && b.push(f);
        else
          for (m in i)
            f = s(i[m], m, l), f != null && b.push(f);
        return c(b);
      },
      // A global GUID counter for objects
      guid: 1,
      // jQuery.support is not used in Core but other projects attach their
      // properties to it so it needs to exist.
      support: x
    }), typeof Symbol == "function" && (d.fn[Symbol.iterator] = n[Symbol.iterator]), d.each(
      "Boolean Number String Function Array Date RegExp Object Error Symbol".split(" "),
      function(i, s) {
        g["[object " + s + "]"] = s.toLowerCase();
      }
    );
    function j(i) {
      var s = !!i && "length" in i && i.length, l = Q(i);
      return M(i) || L(i) ? !1 : l === "array" || s === 0 || typeof s == "number" && s > 0 && s - 1 in i;
    }
    function R(i, s) {
      return i.nodeName && i.nodeName.toLowerCase() === s.toLowerCase();
    }
    var V = n.pop, K = n.sort, J = n.splice, A = "[\\x20\\t\\r\\n\\f]", se = new RegExp(
      "^" + A + "+|((?:^|[^\\\\])(?:\\\\.)*)" + A + "+$",
      "g"
    );
    d.contains = function(i, s) {
      var l = s && s.parentNode;
      return i === l || !!(l && l.nodeType === 1 && // Support: IE 9 - 11+
      // IE doesn't have `contains` on SVG.
      (i.contains ? i.contains(l) : i.compareDocumentPosition && i.compareDocumentPosition(l) & 16));
    };
    var Z = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\x80-\uFFFF\w-]/g;
    function ne(i, s) {
      return s ? i === "\0" ? "�" : i.slice(0, -1) + "\\" + i.charCodeAt(i.length - 1).toString(16) + " " : "\\" + i;
    }
    d.escapeSelector = function(i) {
      return (i + "").replace(Z, ne);
    };
    var le = Y, oe = u;
    (function() {
      var i, s, l, h, f, m = oe, b, E, _, O, W, q = d.expando, N = 0, X = 0, ge = ii(), Ce = ii(), xe = ii(), Qe = ii(), Ve = function(C, D) {
        return C === D && (f = !0), 0;
      }, Ht = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped", jt = "(?:\\\\[\\da-fA-F]{1,6}" + A + "?|\\\\[^\\r\\n\\f]|[\\w-]|[^\0-\\x7f])+", Se = "\\[" + A + "*(" + jt + ")(?:" + A + // Operator (capture 2)
      "*([*^$|!~]?=)" + A + // "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
      `*(?:'((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)"|(` + jt + "))|)" + A + "*\\]", ao = ":(" + jt + `)(?:\\((('((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)")|((?:\\\\.|[^\\\\()[\\]]|` + Se + ")*)|.*)\\)|)", De = new RegExp(A + "+", "g"), w = new RegExp("^" + A + "*," + A + "*"), S = new RegExp("^" + A + "*([>+~]|" + A + ")" + A + "*"), B = new RegExp(A + "|>"), H = new RegExp(ao), ee = new RegExp("^" + jt + "$"), he = {
        ID: new RegExp("^#(" + jt + ")"),
        CLASS: new RegExp("^\\.(" + jt + ")"),
        TAG: new RegExp("^(" + jt + "|[*])"),
        ATTR: new RegExp("^" + Se),
        PSEUDO: new RegExp("^" + ao),
        CHILD: new RegExp(
          "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + A + "*(even|odd|(([+-]|)(\\d*)n|)" + A + "*(?:([+-]|)" + A + "*(\\d+)|))" + A + "*\\)|)",
          "i"
        ),
        bool: new RegExp("^(?:" + Ht + ")$", "i"),
        // For use in libraries implementing .is()
        // We use this for POS matching in `select`
        needsContext: new RegExp("^" + A + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + A + "*((?:-\\d)?\\d*)" + A + "*\\)|)(?=[^-]|$)", "i")
      }, ke = /^(?:input|select|textarea|button)$/i, Ee = /^h\d$/i, Be = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, St = /[+~]/, it = new RegExp("\\\\[\\da-fA-F]{1,6}" + A + "?|\\\\([^\\r\\n\\f])", "g"), Tt = function(C, D) {
        var P = "0x" + C.slice(1) - 65536;
        return D || (P < 0 ? String.fromCharCode(P + 65536) : String.fromCharCode(P >> 10 | 55296, P & 1023 | 56320));
      }, Nn = function() {
        lo();
      }, yn = si(
        function(C) {
          return C.disabled === !0 && R(C, "fieldset");
        },
        { dir: "parentNode", next: "legend" }
      );
      function bl() {
        try {
          return b.activeElement;
        } catch {
        }
      }
      try {
        m.apply(
          n = a.call(le.childNodes),
          le.childNodes
        ), n[le.childNodes.length].nodeType;
      } catch {
        m = {
          apply: function(C, D) {
            oe.apply(C, a.call(D));
          },
          call: function(C) {
            oe.apply(C, a.call(arguments, 1));
          }
        };
      }
      function Ne(C, D, P, $) {
        var G, te, ie, de, re, Te, be, we = D && D.ownerDocument, _e = D ? D.nodeType : 9;
        if (P = P || [], typeof C != "string" || !C || _e !== 1 && _e !== 9 && _e !== 11)
          return P;
        if (!$ && (lo(D), D = D || b, _)) {
          if (_e !== 11 && (re = Be.exec(C)))
            if (G = re[1]) {
              if (_e === 9)
                if (ie = D.getElementById(G)) {
                  if (ie.id === G)
                    return m.call(P, ie), P;
                } else
                  return P;
              else if (we && (ie = we.getElementById(G)) && Ne.contains(D, ie) && ie.id === G)
                return m.call(P, ie), P;
            } else {
              if (re[2])
                return m.apply(P, D.getElementsByTagName(C)), P;
              if ((G = re[3]) && D.getElementsByClassName)
                return m.apply(P, D.getElementsByClassName(G)), P;
            }
          if (!Qe[C + " "] && (!O || !O.test(C))) {
            if (be = C, we = D, _e === 1 && (B.test(C) || S.test(C))) {
              for (we = St.test(C) && wr(D.parentNode) || D, (we != D || !x.scope) && ((de = D.getAttribute("id")) ? de = d.escapeSelector(de) : D.setAttribute("id", de = q)), Te = wn(C), te = Te.length; te--; )
                Te[te] = (de ? "#" + de : ":scope") + " " + ri(Te[te]);
              be = Te.join(",");
            }
            try {
              return m.apply(
                P,
                we.querySelectorAll(be)
              ), P;
            } catch {
              Qe(C, !0);
            } finally {
              de === q && D.removeAttribute("id");
            }
          }
        }
        return zs(C.replace(se, "$1"), D, P, $);
      }
      function ii() {
        var C = [];
        function D(P, $) {
          return C.push(P + " ") > s.cacheLength && delete D[C.shift()], D[P + " "] = $;
        }
        return D;
      }
      function $t(C) {
        return C[q] = !0, C;
      }
      function zo(C) {
        var D = b.createElement("fieldset");
        try {
          return !!C(D);
        } catch {
          return !1;
        } finally {
          D.parentNode && D.parentNode.removeChild(D), D = null;
        }
      }
      function vl(C) {
        return function(D) {
          return R(D, "input") && D.type === C;
        };
      }
      function yl(C) {
        return function(D) {
          return (R(D, "input") || R(D, "button")) && D.type === C;
        };
      }
      function Ys(C) {
        return function(D) {
          return "form" in D ? D.parentNode && D.disabled === !1 ? "label" in D ? "label" in D.parentNode ? D.parentNode.disabled === C : D.disabled === C : D.isDisabled === C || // Where there is no isDisabled, check manually
          D.isDisabled !== !C && yn(D) === C : D.disabled === C : "label" in D ? D.disabled === C : !1;
        };
      }
      function _o(C) {
        return $t(function(D) {
          return D = +D, $t(function(P, $) {
            for (var G, te = C([], P.length, D), ie = te.length; ie--; )
              P[G = te[ie]] && (P[G] = !($[G] = P[G]));
          });
        });
      }
      function wr(C) {
        return C && typeof C.getElementsByTagName < "u" && C;
      }
      function lo(C) {
        var D, P = C ? C.ownerDocument || C : le;
        return P == b || P.nodeType !== 9 || !P.documentElement || (b = P, E = b.documentElement, _ = !d.isXMLDoc(b), W = E.matches || E.webkitMatchesSelector || E.msMatchesSelector, E.msMatchesSelector && // Support: IE 11+, Edge 17 - 18+
        // IE/Edge sometimes throw a "Permission denied" error when strict-comparing
        // two documents; shallow comparisons work.
        // eslint-disable-next-line eqeqeq
        le != b && (D = b.defaultView) && D.top !== D && D.addEventListener("unload", Nn), x.getById = zo(function($) {
          return E.appendChild($).id = d.expando, !b.getElementsByName || !b.getElementsByName(d.expando).length;
        }), x.disconnectedMatch = zo(function($) {
          return W.call($, "*");
        }), x.scope = zo(function() {
          return b.querySelectorAll(":scope");
        }), x.cssHas = zo(function() {
          try {
            return b.querySelector(":has(*,:jqfake)"), !1;
          } catch {
            return !0;
          }
        }), x.getById ? (s.filter.ID = function($) {
          var G = $.replace(it, Tt);
          return function(te) {
            return te.getAttribute("id") === G;
          };
        }, s.find.ID = function($, G) {
          if (typeof G.getElementById < "u" && _) {
            var te = G.getElementById($);
            return te ? [te] : [];
          }
        }) : (s.filter.ID = function($) {
          var G = $.replace(it, Tt);
          return function(te) {
            var ie = typeof te.getAttributeNode < "u" && te.getAttributeNode("id");
            return ie && ie.value === G;
          };
        }, s.find.ID = function($, G) {
          if (typeof G.getElementById < "u" && _) {
            var te, ie, de, re = G.getElementById($);
            if (re) {
              if (te = re.getAttributeNode("id"), te && te.value === $)
                return [re];
              for (de = G.getElementsByName($), ie = 0; re = de[ie++]; )
                if (te = re.getAttributeNode("id"), te && te.value === $)
                  return [re];
            }
            return [];
          }
        }), s.find.TAG = function($, G) {
          return typeof G.getElementsByTagName < "u" ? G.getElementsByTagName($) : G.querySelectorAll($);
        }, s.find.CLASS = function($, G) {
          if (typeof G.getElementsByClassName < "u" && _)
            return G.getElementsByClassName($);
        }, O = [], zo(function($) {
          var G;
          E.appendChild($).innerHTML = "<a id='" + q + "' href='' disabled='disabled'></a><select id='" + q + "-\r\\' disabled='disabled'><option selected=''></option></select>", $.querySelectorAll("[selected]").length || O.push("\\[" + A + "*(?:value|" + Ht + ")"), $.querySelectorAll("[id~=" + q + "-]").length || O.push("~="), $.querySelectorAll("a#" + q + "+*").length || O.push(".#.+[+~]"), $.querySelectorAll(":checked").length || O.push(":checked"), G = b.createElement("input"), G.setAttribute("type", "hidden"), $.appendChild(G).setAttribute("name", "D"), E.appendChild($).disabled = !0, $.querySelectorAll(":disabled").length !== 2 && O.push(":enabled", ":disabled"), G = b.createElement("input"), G.setAttribute("name", ""), $.appendChild(G), $.querySelectorAll("[name='']").length || O.push("\\[" + A + "*name" + A + "*=" + A + `*(?:''|"")`);
        }), x.cssHas || O.push(":has"), O = O.length && new RegExp(O.join("|")), Ve = function($, G) {
          if ($ === G)
            return f = !0, 0;
          var te = !$.compareDocumentPosition - !G.compareDocumentPosition;
          return te || (te = ($.ownerDocument || $) == (G.ownerDocument || G) ? $.compareDocumentPosition(G) : (
            // Otherwise we know they are disconnected
            1
          ), te & 1 || !x.sortDetached && G.compareDocumentPosition($) === te ? $ === b || $.ownerDocument == le && Ne.contains(le, $) ? -1 : G === b || G.ownerDocument == le && Ne.contains(le, G) ? 1 : h ? p.call(h, $) - p.call(h, G) : 0 : te & 4 ? -1 : 1);
        }), b;
      }
      Ne.matches = function(C, D) {
        return Ne(C, null, null, D);
      }, Ne.matchesSelector = function(C, D) {
        if (lo(C), _ && !Qe[D + " "] && (!O || !O.test(D)))
          try {
            var P = W.call(C, D);
            if (P || x.disconnectedMatch || // As well, disconnected nodes are said to be in a document
            // fragment in IE 9
            C.document && C.document.nodeType !== 11)
              return P;
          } catch {
            Qe(D, !0);
          }
        return Ne(D, b, null, [C]).length > 0;
      }, Ne.contains = function(C, D) {
        return (C.ownerDocument || C) != b && lo(C), d.contains(C, D);
      }, Ne.attr = function(C, D) {
        (C.ownerDocument || C) != b && lo(C);
        var P = s.attrHandle[D.toLowerCase()], $ = P && k.call(s.attrHandle, D.toLowerCase()) ? P(C, D, !_) : void 0;
        return $ !== void 0 ? $ : C.getAttribute(D);
      }, Ne.error = function(C) {
        throw new Error("Syntax error, unrecognized expression: " + C);
      }, d.uniqueSort = function(C) {
        var D, P = [], $ = 0, G = 0;
        if (f = !x.sortStable, h = !x.sortStable && a.call(C, 0), K.call(C, Ve), f) {
          for (; D = C[G++]; )
            D === C[G] && ($ = P.push(G));
          for (; $--; )
            J.call(C, P[$], 1);
        }
        return h = null, C;
      }, d.fn.uniqueSort = function() {
        return this.pushStack(d.uniqueSort(a.apply(this)));
      }, s = d.expr = {
        // Can be adjusted by the user
        cacheLength: 50,
        createPseudo: $t,
        match: he,
        attrHandle: {},
        find: {},
        relative: {
          ">": { dir: "parentNode", first: !0 },
          " ": { dir: "parentNode" },
          "+": { dir: "previousSibling", first: !0 },
          "~": { dir: "previousSibling" }
        },
        preFilter: {
          ATTR: function(C) {
            return C[1] = C[1].replace(it, Tt), C[3] = (C[3] || C[4] || C[5] || "").replace(it, Tt), C[2] === "~=" && (C[3] = " " + C[3] + " "), C.slice(0, 4);
          },
          CHILD: function(C) {
            return C[1] = C[1].toLowerCase(), C[1].slice(0, 3) === "nth" ? (C[3] || Ne.error(C[0]), C[4] = +(C[4] ? C[5] + (C[6] || 1) : 2 * (C[3] === "even" || C[3] === "odd")), C[5] = +(C[7] + C[8] || C[3] === "odd")) : C[3] && Ne.error(C[0]), C;
          },
          PSEUDO: function(C) {
            var D, P = !C[6] && C[2];
            return he.CHILD.test(C[0]) ? null : (C[3] ? C[2] = C[4] || C[5] || "" : P && H.test(P) && // Get excess from tokenize (recursively)
            (D = wn(P, !0)) && // advance to the next closing parenthesis
            (D = P.indexOf(")", P.length - D) - P.length) && (C[0] = C[0].slice(0, D), C[2] = P.slice(0, D)), C.slice(0, 3));
          }
        },
        filter: {
          TAG: function(C) {
            var D = C.replace(it, Tt).toLowerCase();
            return C === "*" ? function() {
              return !0;
            } : function(P) {
              return R(P, D);
            };
          },
          CLASS: function(C) {
            var D = ge[C + " "];
            return D || (D = new RegExp("(^|" + A + ")" + C + "(" + A + "|$)")) && ge(C, function(P) {
              return D.test(
                typeof P.className == "string" && P.className || typeof P.getAttribute < "u" && P.getAttribute("class") || ""
              );
            });
          },
          ATTR: function(C, D, P) {
            return function($) {
              var G = Ne.attr($, C);
              return G == null ? D === "!=" : D ? (G += "", D === "=" ? G === P : D === "!=" ? G !== P : D === "^=" ? P && G.indexOf(P) === 0 : D === "*=" ? P && G.indexOf(P) > -1 : D === "$=" ? P && G.slice(-P.length) === P : D === "~=" ? (" " + G.replace(De, " ") + " ").indexOf(P) > -1 : D === "|=" ? G === P || G.slice(0, P.length + 1) === P + "-" : !1) : !0;
            };
          },
          CHILD: function(C, D, P, $, G) {
            var te = C.slice(0, 3) !== "nth", ie = C.slice(-4) !== "last", de = D === "of-type";
            return $ === 1 && G === 0 ? (
              // Shortcut for :nth-*(n)
              function(re) {
                return !!re.parentNode;
              }
            ) : function(re, Te, be) {
              var we, _e, me, $e, yt, lt = te !== ie ? "nextSibling" : "previousSibling", Et = re.parentNode, Ut = de && re.nodeName.toLowerCase(), Wo = !be && !de, gt = !1;
              if (Et) {
                if (te) {
                  for (; lt; ) {
                    for (me = re; me = me[lt]; )
                      if (de ? R(me, Ut) : me.nodeType === 1)
                        return !1;
                    yt = lt = C === "only" && !yt && "nextSibling";
                  }
                  return !0;
                }
                if (yt = [ie ? Et.firstChild : Et.lastChild], ie && Wo) {
                  for (_e = Et[q] || (Et[q] = {}), we = _e[C] || [], $e = we[0] === N && we[1], gt = $e && we[2], me = $e && Et.childNodes[$e]; me = ++$e && me && me[lt] || // Fallback to seeking `elem` from the start
                  (gt = $e = 0) || yt.pop(); )
                    if (me.nodeType === 1 && ++gt && me === re) {
                      _e[C] = [N, $e, gt];
                      break;
                    }
                } else if (Wo && (_e = re[q] || (re[q] = {}), we = _e[C] || [], $e = we[0] === N && we[1], gt = $e), gt === !1)
                  for (; (me = ++$e && me && me[lt] || (gt = $e = 0) || yt.pop()) && !((de ? R(me, Ut) : me.nodeType === 1) && ++gt && (Wo && (_e = me[q] || (me[q] = {}), _e[C] = [N, gt]), me === re)); )
                    ;
                return gt -= G, gt === $ || gt % $ === 0 && gt / $ >= 0;
              }
            };
          },
          PSEUDO: function(C, D) {
            var P, $ = s.pseudos[C] || s.setFilters[C.toLowerCase()] || Ne.error("unsupported pseudo: " + C);
            return $[q] ? $(D) : $.length > 1 ? (P = [C, C, "", D], s.setFilters.hasOwnProperty(C.toLowerCase()) ? $t(function(G, te) {
              for (var ie, de = $(G, D), re = de.length; re--; )
                ie = p.call(G, de[re]), G[ie] = !(te[ie] = de[re]);
            }) : function(G) {
              return $(G, 0, P);
            }) : $;
          }
        },
        pseudos: {
          // Potentially complex pseudos
          not: $t(function(C) {
            var D = [], P = [], $ = Sr(C.replace(se, "$1"));
            return $[q] ? $t(function(G, te, ie, de) {
              for (var re, Te = $(G, null, de, []), be = G.length; be--; )
                (re = Te[be]) && (G[be] = !(te[be] = re));
            }) : function(G, te, ie) {
              return D[0] = G, $(D, null, ie, P), D[0] = null, !P.pop();
            };
          }),
          has: $t(function(C) {
            return function(D) {
              return Ne(C, D).length > 0;
            };
          }),
          contains: $t(function(C) {
            return C = C.replace(it, Tt), function(D) {
              return (D.textContent || d.text(D)).indexOf(C) > -1;
            };
          }),
          // "Whether an element is represented by a :lang() selector
          // is based solely on the element's language value
          // being equal to the identifier C,
          // or beginning with the identifier C immediately followed by "-".
          // The matching of C against the element's language value is performed case-insensitively.
          // The identifier C does not have to be a valid language name."
          // https://www.w3.org/TR/selectors/#lang-pseudo
          lang: $t(function(C) {
            return ee.test(C || "") || Ne.error("unsupported lang: " + C), C = C.replace(it, Tt).toLowerCase(), function(D) {
              var P;
              do
                if (P = _ ? D.lang : D.getAttribute("xml:lang") || D.getAttribute("lang"))
                  return P = P.toLowerCase(), P === C || P.indexOf(C + "-") === 0;
              while ((D = D.parentNode) && D.nodeType === 1);
              return !1;
            };
          }),
          // Miscellaneous
          target: function(C) {
            var D = e.location && e.location.hash;
            return D && D.slice(1) === C.id;
          },
          root: function(C) {
            return C === E;
          },
          focus: function(C) {
            return C === bl() && b.hasFocus() && !!(C.type || C.href || ~C.tabIndex);
          },
          // Boolean properties
          enabled: Ys(!1),
          disabled: Ys(!0),
          checked: function(C) {
            return R(C, "input") && !!C.checked || R(C, "option") && !!C.selected;
          },
          selected: function(C) {
            return C.parentNode && C.parentNode.selectedIndex, C.selected === !0;
          },
          // Contents
          empty: function(C) {
            for (C = C.firstChild; C; C = C.nextSibling)
              if (C.nodeType < 6)
                return !1;
            return !0;
          },
          parent: function(C) {
            return !s.pseudos.empty(C);
          },
          // Element/input types
          header: function(C) {
            return Ee.test(C.nodeName);
          },
          input: function(C) {
            return ke.test(C.nodeName);
          },
          button: function(C) {
            return R(C, "input") && C.type === "button" || R(C, "button");
          },
          text: function(C) {
            var D;
            return R(C, "input") && C.type === "text" && // Support: IE <10 only
            // New HTML5 attribute values (e.g., "search") appear
            // with elem.type === "text"
            ((D = C.getAttribute("type")) == null || D.toLowerCase() === "text");
          },
          // Position-in-collection
          first: _o(function() {
            return [0];
          }),
          last: _o(function(C, D) {
            return [D - 1];
          }),
          eq: _o(function(C, D, P) {
            return [P < 0 ? P + D : P];
          }),
          even: _o(function(C, D) {
            for (var P = 0; P < D; P += 2)
              C.push(P);
            return C;
          }),
          odd: _o(function(C, D) {
            for (var P = 1; P < D; P += 2)
              C.push(P);
            return C;
          }),
          lt: _o(function(C, D, P) {
            var $;
            for (P < 0 ? $ = P + D : P > D ? $ = D : $ = P; --$ >= 0; )
              C.push($);
            return C;
          }),
          gt: _o(function(C, D, P) {
            for (var $ = P < 0 ? P + D : P; ++$ < D; )
              C.push($);
            return C;
          })
        }
      }, s.pseudos.nth = s.pseudos.eq;
      for (i in { radio: !0, checkbox: !0, file: !0, password: !0, image: !0 })
        s.pseudos[i] = vl(i);
      for (i in { submit: !0, reset: !0 })
        s.pseudos[i] = yl(i);
      function Fs() {
      }
      Fs.prototype = s.filters = s.pseudos, s.setFilters = new Fs();
      function wn(C, D) {
        var P, $, G, te, ie, de, re, Te = Ce[C + " "];
        if (Te)
          return D ? 0 : Te.slice(0);
        for (ie = C, de = [], re = s.preFilter; ie; ) {
          (!P || ($ = w.exec(ie))) && ($ && (ie = ie.slice($[0].length) || ie), de.push(G = [])), P = !1, ($ = S.exec(ie)) && (P = $.shift(), G.push({
            value: P,
            // Cast descendant combinators to space
            type: $[0].replace(se, " ")
          }), ie = ie.slice(P.length));
          for (te in s.filter)
            ($ = he[te].exec(ie)) && (!re[te] || ($ = re[te]($))) && (P = $.shift(), G.push({
              value: P,
              type: te,
              matches: $
            }), ie = ie.slice(P.length));
          if (!P)
            break;
        }
        return D ? ie.length : ie ? Ne.error(C) : (
          // Cache the tokens
          Ce(C, de).slice(0)
        );
      }
      function ri(C) {
        for (var D = 0, P = C.length, $ = ""; D < P; D++)
          $ += C[D].value;
        return $;
      }
      function si(C, D, P) {
        var $ = D.dir, G = D.next, te = G || $, ie = P && te === "parentNode", de = X++;
        return D.first ? (
          // Check against closest ancestor/preceding element
          function(re, Te, be) {
            for (; re = re[$]; )
              if (re.nodeType === 1 || ie)
                return C(re, Te, be);
            return !1;
          }
        ) : (
          // Check against all ancestor/preceding elements
          function(re, Te, be) {
            var we, _e, me = [N, de];
            if (be) {
              for (; re = re[$]; )
                if ((re.nodeType === 1 || ie) && C(re, Te, be))
                  return !0;
            } else
              for (; re = re[$]; )
                if (re.nodeType === 1 || ie)
                  if (_e = re[q] || (re[q] = {}), G && R(re, G))
                    re = re[$] || re;
                  else {
                    if ((we = _e[te]) && we[0] === N && we[1] === de)
                      return me[2] = we[2];
                    if (_e[te] = me, me[2] = C(re, Te, be))
                      return !0;
                  }
            return !1;
          }
        );
      }
      function kr(C) {
        return C.length > 1 ? function(D, P, $) {
          for (var G = C.length; G--; )
            if (!C[G](D, P, $))
              return !1;
          return !0;
        } : C[0];
      }
      function wl(C, D, P) {
        for (var $ = 0, G = D.length; $ < G; $++)
          Ne(C, D[$], P);
        return P;
      }
      function ai(C, D, P, $, G) {
        for (var te, ie = [], de = 0, re = C.length, Te = D != null; de < re; de++)
          (te = C[de]) && (!P || P(te, $, G)) && (ie.push(te), Te && D.push(de));
        return ie;
      }
      function xr(C, D, P, $, G, te) {
        return $ && !$[q] && ($ = xr($)), G && !G[q] && (G = xr(G, te)), $t(function(ie, de, re, Te) {
          var be, we, _e, me, $e = [], yt = [], lt = de.length, Et = ie || wl(
            D || "*",
            re.nodeType ? [re] : re,
            []
          ), Ut = C && (ie || !D) ? ai(Et, $e, C, re, Te) : Et;
          if (P ? (me = G || (ie ? C : lt || $) ? (
            // ...intermediate processing is necessary
            []
          ) : (
            // ...otherwise use results directly
            de
          ), P(Ut, me, re, Te)) : me = Ut, $)
            for (be = ai(me, yt), $(be, [], re, Te), we = be.length; we--; )
              (_e = be[we]) && (me[yt[we]] = !(Ut[yt[we]] = _e));
          if (ie) {
            if (G || C) {
              if (G) {
                for (be = [], we = me.length; we--; )
                  (_e = me[we]) && be.push(Ut[we] = _e);
                G(null, me = [], be, Te);
              }
              for (we = me.length; we--; )
                (_e = me[we]) && (be = G ? p.call(ie, _e) : $e[we]) > -1 && (ie[be] = !(de[be] = _e));
            }
          } else
            me = ai(
              me === de ? me.splice(lt, me.length) : me
            ), G ? G(null, de, me, Te) : m.apply(de, me);
        });
      }
      function Cr(C) {
        for (var D, P, $, G = C.length, te = s.relative[C[0].type], ie = te || s.relative[" "], de = te ? 1 : 0, re = si(function(we) {
          return we === D;
        }, ie, !0), Te = si(function(we) {
          return p.call(D, we) > -1;
        }, ie, !0), be = [function(we, _e, me) {
          var $e = !te && (me || _e != l) || ((D = _e).nodeType ? re(we, _e, me) : Te(we, _e, me));
          return D = null, $e;
        }]; de < G; de++)
          if (P = s.relative[C[de].type])
            be = [si(kr(be), P)];
          else {
            if (P = s.filter[C[de].type].apply(null, C[de].matches), P[q]) {
              for ($ = ++de; $ < G && !s.relative[C[$].type]; $++)
                ;
              return xr(
                de > 1 && kr(be),
                de > 1 && ri(
                  // If the preceding token was a descendant combinator, insert an implicit any-element `*`
                  C.slice(0, de - 1).concat({ value: C[de - 2].type === " " ? "*" : "" })
                ).replace(se, "$1"),
                P,
                de < $ && Cr(C.slice(de, $)),
                $ < G && Cr(C = C.slice($)),
                $ < G && ri(C)
              );
            }
            be.push(P);
          }
        return kr(be);
      }
      function kl(C, D) {
        var P = D.length > 0, $ = C.length > 0, G = function(te, ie, de, re, Te) {
          var be, we, _e, me = 0, $e = "0", yt = te && [], lt = [], Et = l, Ut = te || $ && s.find.TAG("*", Te), Wo = N += Et == null ? 1 : Math.random() || 0.1, gt = Ut.length;
          for (Te && (l = ie == b || ie || Te); $e !== gt && (be = Ut[$e]) != null; $e++) {
            if ($ && be) {
              for (we = 0, !ie && be.ownerDocument != b && (lo(be), de = !_); _e = C[we++]; )
                if (_e(be, ie || b, de)) {
                  m.call(re, be);
                  break;
                }
              Te && (N = Wo);
            }
            P && ((be = !_e && be) && me--, te && yt.push(be));
          }
          if (me += $e, P && $e !== me) {
            for (we = 0; _e = D[we++]; )
              _e(yt, lt, ie, de);
            if (te) {
              if (me > 0)
                for (; $e--; )
                  yt[$e] || lt[$e] || (lt[$e] = V.call(re));
              lt = ai(lt);
            }
            m.apply(re, lt), Te && !te && lt.length > 0 && me + D.length > 1 && d.uniqueSort(re);
          }
          return Te && (N = Wo, l = Et), yt;
        };
        return P ? $t(G) : G;
      }
      function Sr(C, D) {
        var P, $ = [], G = [], te = xe[C + " "];
        if (!te) {
          for (D || (D = wn(C)), P = D.length; P--; )
            te = Cr(D[P]), te[q] ? $.push(te) : G.push(te);
          te = xe(
            C,
            kl(G, $)
          ), te.selector = C;
        }
        return te;
      }
      function zs(C, D, P, $) {
        var G, te, ie, de, re, Te = typeof C == "function" && C, be = !$ && wn(C = Te.selector || C);
        if (P = P || [], be.length === 1) {
          if (te = be[0] = be[0].slice(0), te.length > 2 && (ie = te[0]).type === "ID" && D.nodeType === 9 && _ && s.relative[te[1].type]) {
            if (D = (s.find.ID(
              ie.matches[0].replace(it, Tt),
              D
            ) || [])[0], D)
              Te && (D = D.parentNode);
            else
              return P;
            C = C.slice(te.shift().value.length);
          }
          for (G = he.needsContext.test(C) ? 0 : te.length; G-- && (ie = te[G], !s.relative[de = ie.type]); )
            if ((re = s.find[de]) && ($ = re(
              ie.matches[0].replace(it, Tt),
              St.test(te[0].type) && wr(D.parentNode) || D
            ))) {
              if (te.splice(G, 1), C = $.length && ri(te), !C)
                return m.apply(P, $), P;
              break;
            }
        }
        return (Te || Sr(C, be))(
          $,
          D,
          !_,
          P,
          !D || St.test(C) && wr(D.parentNode) || D
        ), P;
      }
      x.sortStable = q.split("").sort(Ve).join("") === q, lo(), x.sortDetached = zo(function(C) {
        return C.compareDocumentPosition(b.createElement("fieldset")) & 1;
      }), d.find = Ne, d.expr[":"] = d.expr.pseudos, d.unique = d.uniqueSort, Ne.compile = Sr, Ne.select = zs, Ne.setDocument = lo, Ne.tokenize = wn, Ne.escape = d.escapeSelector, Ne.getText = d.text, Ne.isXML = d.isXMLDoc, Ne.selectors = d.expr, Ne.support = d.support, Ne.uniqueSort = d.uniqueSort;
    })();
    var fe = function(i, s, l) {
      for (var h = [], f = l !== void 0; (i = i[s]) && i.nodeType !== 9; )
        if (i.nodeType === 1) {
          if (f && d(i).is(l))
            break;
          h.push(i);
        }
      return h;
    }, Me = function(i, s) {
      for (var l = []; i; i = i.nextSibling)
        i.nodeType === 1 && i !== s && l.push(i);
      return l;
    }, je = d.expr.match.needsContext, Ae = /^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i;
    function Je(i, s, l) {
      return M(s) ? d.grep(i, function(h, f) {
        return !!s.call(h, f, h) !== l;
      }) : s.nodeType ? d.grep(i, function(h) {
        return h === s !== l;
      }) : typeof s != "string" ? d.grep(i, function(h) {
        return p.call(s, h) > -1 !== l;
      }) : d.filter(s, i, l);
    }
    d.filter = function(i, s, l) {
      var h = s[0];
      return l && (i = ":not(" + i + ")"), s.length === 1 && h.nodeType === 1 ? d.find.matchesSelector(h, i) ? [h] : [] : d.find.matches(i, d.grep(s, function(f) {
        return f.nodeType === 1;
      }));
    }, d.fn.extend({
      find: function(i) {
        var s, l, h = this.length, f = this;
        if (typeof i != "string")
          return this.pushStack(d(i).filter(function() {
            for (s = 0; s < h; s++)
              if (d.contains(f[s], this))
                return !0;
          }));
        for (l = this.pushStack([]), s = 0; s < h; s++)
          d.find(i, f[s], l);
        return h > 1 ? d.uniqueSort(l) : l;
      },
      filter: function(i) {
        return this.pushStack(Je(this, i || [], !1));
      },
      not: function(i) {
        return this.pushStack(Je(this, i || [], !0));
      },
      is: function(i) {
        return !!Je(
          this,
          // If this is a positional/relative selector, check membership in the returned set
          // so $("p:first").is("p:last") won't return true for a doc with two "p".
          typeof i == "string" && je.test(i) ? d(i) : i || [],
          !1
        ).length;
      }
    });
    var no, yo = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/, Qo = d.fn.init = function(i, s, l) {
      var h, f;
      if (!i)
        return this;
      if (l = l || no, typeof i == "string")
        if (i[0] === "<" && i[i.length - 1] === ">" && i.length >= 3 ? h = [null, i, null] : h = yo.exec(i), h && (h[1] || !s))
          if (h[1]) {
            if (s = s instanceof d ? s[0] : s, d.merge(this, d.parseHTML(
              h[1],
              s && s.nodeType ? s.ownerDocument || s : Y,
              !0
            )), Ae.test(h[1]) && d.isPlainObject(s))
              for (h in s)
                M(this[h]) ? this[h](s[h]) : this.attr(h, s[h]);
            return this;
          } else
            return f = Y.getElementById(h[2]), f && (this[0] = f, this.length = 1), this;
        else
          return !s || s.jquery ? (s || l).find(i) : this.constructor(s).find(i);
      else {
        if (i.nodeType)
          return this[0] = i, this.length = 1, this;
        if (M(i))
          return l.ready !== void 0 ? l.ready(i) : (
            // Execute immediately if ready is not present
            i(d)
          );
      }
      return d.makeArray(i, this);
    };
    Qo.prototype = d.fn, no = d(Y);
    var Bn = /^(?:parents|prev(?:Until|All))/, hn = {
      children: !0,
      contents: !0,
      next: !0,
      prev: !0
    };
    d.fn.extend({
      has: function(i) {
        var s = d(i, this), l = s.length;
        return this.filter(function() {
          for (var h = 0; h < l; h++)
            if (d.contains(this, s[h]))
              return !0;
        });
      },
      closest: function(i, s) {
        var l, h = 0, f = this.length, m = [], b = typeof i != "string" && d(i);
        if (!je.test(i)) {
          for (; h < f; h++)
            for (l = this[h]; l && l !== s; l = l.parentNode)
              if (l.nodeType < 11 && (b ? b.index(l) > -1 : (
                // Don't pass non-elements to jQuery#find
                l.nodeType === 1 && d.find.matchesSelector(l, i)
              ))) {
                m.push(l);
                break;
              }
        }
        return this.pushStack(m.length > 1 ? d.uniqueSort(m) : m);
      },
      // Determine the position of an element within the set
      index: function(i) {
        return i ? typeof i == "string" ? p.call(d(i), this[0]) : p.call(
          this,
          // If it receives a jQuery object, the first element is used
          i.jquery ? i[0] : i
        ) : this[0] && this[0].parentNode ? this.first().prevAll().length : -1;
      },
      add: function(i, s) {
        return this.pushStack(
          d.uniqueSort(
            d.merge(this.get(), d(i, s))
          )
        );
      },
      addBack: function(i) {
        return this.add(
          i == null ? this.prevObject : this.prevObject.filter(i)
        );
      }
    });
    function un(i, s) {
      for (; (i = i[s]) && i.nodeType !== 1; )
        ;
      return i;
    }
    d.each({
      parent: function(i) {
        var s = i.parentNode;
        return s && s.nodeType !== 11 ? s : null;
      },
      parents: function(i) {
        return fe(i, "parentNode");
      },
      parentsUntil: function(i, s, l) {
        return fe(i, "parentNode", l);
      },
      next: function(i) {
        return un(i, "nextSibling");
      },
      prev: function(i) {
        return un(i, "previousSibling");
      },
      nextAll: function(i) {
        return fe(i, "nextSibling");
      },
      prevAll: function(i) {
        return fe(i, "previousSibling");
      },
      nextUntil: function(i, s, l) {
        return fe(i, "nextSibling", l);
      },
      prevUntil: function(i, s, l) {
        return fe(i, "previousSibling", l);
      },
      siblings: function(i) {
        return Me((i.parentNode || {}).firstChild, i);
      },
      children: function(i) {
        return Me(i.firstChild);
      },
      contents: function(i) {
        return i.contentDocument != null && // Support: IE 11+
        // <object> elements with no `data` attribute has an object
        // `contentDocument` with a `null` prototype.
        r(i.contentDocument) ? i.contentDocument : (R(i, "template") && (i = i.content || i), d.merge([], i.childNodes));
      }
    }, function(i, s) {
      d.fn[i] = function(l, h) {
        var f = d.map(this, s, l);
        return i.slice(-5) !== "Until" && (h = l), h && typeof h == "string" && (f = d.filter(h, f)), this.length > 1 && (hn[i] || d.uniqueSort(f), Bn.test(i) && f.reverse()), this.pushStack(f);
      };
    });
    var Rt = /[^\x20\t\r\n\f]+/g;
    function Di(i) {
      var s = {};
      return d.each(i.match(Rt) || [], function(l, h) {
        s[h] = !0;
      }), s;
    }
    d.Callbacks = function(i) {
      i = typeof i == "string" ? Di(i) : d.extend({}, i);
      var s, l, h, f, m = [], b = [], E = -1, _ = function() {
        for (f = f || i.once, h = s = !0; b.length; E = -1)
          for (l = b.shift(); ++E < m.length; )
            m[E].apply(l[0], l[1]) === !1 && i.stopOnFalse && (E = m.length, l = !1);
        i.memory || (l = !1), s = !1, f && (l ? m = [] : m = "");
      }, O = {
        // Add a callback or a collection of callbacks to the list
        add: function() {
          return m && (l && !s && (E = m.length - 1, b.push(l)), function W(q) {
            d.each(q, function(N, X) {
              M(X) ? (!i.unique || !O.has(X)) && m.push(X) : X && X.length && Q(X) !== "string" && W(X);
            });
          }(arguments), l && !s && _()), this;
        },
        // Remove a callback from the list
        remove: function() {
          return d.each(arguments, function(W, q) {
            for (var N; (N = d.inArray(q, m, N)) > -1; )
              m.splice(N, 1), N <= E && E--;
          }), this;
        },
        // Check if a given callback is in the list.
        // If no argument is given, return whether or not list has callbacks attached.
        has: function(W) {
          return W ? d.inArray(W, m) > -1 : m.length > 0;
        },
        // Remove all callbacks from the list
        empty: function() {
          return m && (m = []), this;
        },
        // Disable .fire and .add
        // Abort any current/pending executions
        // Clear all callbacks and values
        disable: function() {
          return f = b = [], m = l = "", this;
        },
        disabled: function() {
          return !m;
        },
        // Disable .fire
        // Also disable .add unless we have memory (since it would have no effect)
        // Abort any pending executions
        lock: function() {
          return f = b = [], !l && !s && (m = l = ""), this;
        },
        locked: function() {
          return !!f;
        },
        // Call all callbacks with the given context and arguments
        fireWith: function(W, q) {
          return f || (q = q || [], q = [W, q.slice ? q.slice() : q], b.push(q), s || _()), this;
        },
        // Call all the callbacks with the given arguments
        fire: function() {
          return O.fireWith(this, arguments), this;
        },
        // To know if the callbacks have already been called at least once
        fired: function() {
          return !!h;
        }
      };
      return O;
    };
    function io(i) {
      return i;
    }
    function Ho(i) {
      throw i;
    }
    function Un(i, s, l, h) {
      var f;
      try {
        i && M(f = i.promise) ? f.call(i).done(s).fail(l) : i && M(f = i.then) ? f.call(i, s, l) : s.apply(void 0, [i].slice(h));
      } catch (m) {
        l.apply(void 0, [m]);
      }
    }
    d.extend({
      Deferred: function(i) {
        var s = [
          // action, add listener, callbacks,
          // ... .then handlers, argument index, [final state]
          [
            "notify",
            "progress",
            d.Callbacks("memory"),
            d.Callbacks("memory"),
            2
          ],
          [
            "resolve",
            "done",
            d.Callbacks("once memory"),
            d.Callbacks("once memory"),
            0,
            "resolved"
          ],
          [
            "reject",
            "fail",
            d.Callbacks("once memory"),
            d.Callbacks("once memory"),
            1,
            "rejected"
          ]
        ], l = "pending", h = {
          state: function() {
            return l;
          },
          always: function() {
            return f.done(arguments).fail(arguments), this;
          },
          catch: function(m) {
            return h.then(null, m);
          },
          // Keep pipe for back-compat
          pipe: function() {
            var m = arguments;
            return d.Deferred(function(b) {
              d.each(s, function(E, _) {
                var O = M(m[_[4]]) && m[_[4]];
                f[_[1]](function() {
                  var W = O && O.apply(this, arguments);
                  W && M(W.promise) ? W.promise().progress(b.notify).done(b.resolve).fail(b.reject) : b[_[0] + "With"](
                    this,
                    O ? [W] : arguments
                  );
                });
              }), m = null;
            }).promise();
          },
          then: function(m, b, E) {
            var _ = 0;
            function O(W, q, N, X) {
              return function() {
                var ge = this, Ce = arguments, xe = function() {
                  var Ve, Ht;
                  if (!(W < _)) {
                    if (Ve = N.apply(ge, Ce), Ve === q.promise())
                      throw new TypeError("Thenable self-resolution");
                    Ht = Ve && // Support: Promises/A+ section 2.3.4
                    // https://promisesaplus.com/#point-64
                    // Only check objects and functions for thenability
                    (typeof Ve == "object" || typeof Ve == "function") && Ve.then, M(Ht) ? X ? Ht.call(
                      Ve,
                      O(_, q, io, X),
                      O(_, q, Ho, X)
                    ) : (_++, Ht.call(
                      Ve,
                      O(_, q, io, X),
                      O(_, q, Ho, X),
                      O(
                        _,
                        q,
                        io,
                        q.notifyWith
                      )
                    )) : (N !== io && (ge = void 0, Ce = [Ve]), (X || q.resolveWith)(ge, Ce));
                  }
                }, Qe = X ? xe : function() {
                  try {
                    xe();
                  } catch (Ve) {
                    d.Deferred.exceptionHook && d.Deferred.exceptionHook(
                      Ve,
                      Qe.error
                    ), W + 1 >= _ && (N !== Ho && (ge = void 0, Ce = [Ve]), q.rejectWith(ge, Ce));
                  }
                };
                W ? Qe() : (d.Deferred.getErrorHook ? Qe.error = d.Deferred.getErrorHook() : d.Deferred.getStackHook && (Qe.error = d.Deferred.getStackHook()), e.setTimeout(Qe));
              };
            }
            return d.Deferred(function(W) {
              s[0][3].add(
                O(
                  0,
                  W,
                  M(E) ? E : io,
                  W.notifyWith
                )
              ), s[1][3].add(
                O(
                  0,
                  W,
                  M(m) ? m : io
                )
              ), s[2][3].add(
                O(
                  0,
                  W,
                  M(b) ? b : Ho
                )
              );
            }).promise();
          },
          // Get a promise for this deferred
          // If obj is provided, the promise aspect is added to the object
          promise: function(m) {
            return m != null ? d.extend(m, h) : h;
          }
        }, f = {};
        return d.each(s, function(m, b) {
          var E = b[2], _ = b[5];
          h[b[1]] = E.add, _ && E.add(
            function() {
              l = _;
            },
            // rejected_callbacks.disable
            // fulfilled_callbacks.disable
            s[3 - m][2].disable,
            // rejected_handlers.disable
            // fulfilled_handlers.disable
            s[3 - m][3].disable,
            // progress_callbacks.lock
            s[0][2].lock,
            // progress_handlers.lock
            s[0][3].lock
          ), E.add(b[3].fire), f[b[0]] = function() {
            return f[b[0] + "With"](this === f ? void 0 : this, arguments), this;
          }, f[b[0] + "With"] = E.fireWith;
        }), h.promise(f), i && i.call(f, f), f;
      },
      // Deferred helper
      when: function(i) {
        var s = arguments.length, l = s, h = Array(l), f = a.call(arguments), m = d.Deferred(), b = function(E) {
          return function(_) {
            h[E] = this, f[E] = arguments.length > 1 ? a.call(arguments) : _, --s || m.resolveWith(h, f);
          };
        };
        if (s <= 1 && (Un(
          i,
          m.done(b(l)).resolve,
          m.reject,
          !s
        ), m.state() === "pending" || M(f[l] && f[l].then)))
          return m.then();
        for (; l--; )
          Un(f[l], b(l), m.reject);
        return m.promise();
      }
    });
    var Li = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;
    d.Deferred.exceptionHook = function(i, s) {
      e.console && e.console.warn && i && Li.test(i.name) && e.console.warn(
        "jQuery.Deferred exception: " + i.message,
        i.stack,
        s
      );
    }, d.readyException = function(i) {
      e.setTimeout(function() {
        throw i;
      });
    };
    var Mo = d.Deferred();
    d.fn.ready = function(i) {
      return Mo.then(i).catch(function(s) {
        d.readyException(s);
      }), this;
    }, d.extend({
      // Is the DOM ready to be used? Set to true once it occurs.
      isReady: !1,
      // A counter to track how many items to wait for before
      // the ready event fires. See trac-6781
      readyWait: 1,
      // Handle when the DOM is ready
      ready: function(i) {
        (i === !0 ? --d.readyWait : d.isReady) || (d.isReady = !0, !(i !== !0 && --d.readyWait > 0) && Mo.resolveWith(Y, [d]));
      }
    }), d.ready.then = Mo.then;
    function Ct() {
      Y.removeEventListener("DOMContentLoaded", Ct), e.removeEventListener("load", Ct), d.ready();
    }
    Y.readyState === "complete" || Y.readyState !== "loading" && !Y.documentElement.doScroll ? e.setTimeout(d.ready) : (Y.addEventListener("DOMContentLoaded", Ct), e.addEventListener("load", Ct));
    var vt = function(i, s, l, h, f, m, b) {
      var E = 0, _ = i.length, O = l == null;
      if (Q(l) === "object") {
        f = !0;
        for (E in l)
          vt(i, s, E, l[E], !0, m, b);
      } else if (h !== void 0 && (f = !0, M(h) || (b = !0), O && (b ? (s.call(i, h), s = null) : (O = s, s = function(W, q, N) {
        return O.call(d(W), N);
      })), s))
        for (; E < _; E++)
          s(
            i[E],
            l,
            b ? h : h.call(i[E], E, s(i[E], l))
          );
      return f ? i : O ? s.call(i) : _ ? s(i[0], l) : m;
    }, uo = /^-ms-/, er = /-([a-z])/g;
    function Nr(i, s) {
      return s.toUpperCase();
    }
    function It(i) {
      return i.replace(uo, "ms-").replace(er, Nr);
    }
    var We = function(i) {
      return i.nodeType === 1 || i.nodeType === 9 || !+i.nodeType;
    };
    function Ft() {
      this.expando = d.expando + Ft.uid++;
    }
    Ft.uid = 1, Ft.prototype = {
      cache: function(i) {
        var s = i[this.expando];
        return s || (s = {}, We(i) && (i.nodeType ? i[this.expando] = s : Object.defineProperty(i, this.expando, {
          value: s,
          configurable: !0
        }))), s;
      },
      set: function(i, s, l) {
        var h, f = this.cache(i);
        if (typeof s == "string")
          f[It(s)] = l;
        else
          for (h in s)
            f[It(h)] = s[h];
        return f;
      },
      get: function(i, s) {
        return s === void 0 ? this.cache(i) : (
          // Always use camelCase key (gh-2257)
          i[this.expando] && i[this.expando][It(s)]
        );
      },
      access: function(i, s, l) {
        return s === void 0 || s && typeof s == "string" && l === void 0 ? this.get(i, s) : (this.set(i, s, l), l !== void 0 ? l : s);
      },
      remove: function(i, s) {
        var l, h = i[this.expando];
        if (h !== void 0) {
          if (s !== void 0)
            for (Array.isArray(s) ? s = s.map(It) : (s = It(s), s = s in h ? [s] : s.match(Rt) || []), l = s.length; l--; )
              delete h[s[l]];
          (s === void 0 || d.isEmptyObject(h)) && (i.nodeType ? i[this.expando] = void 0 : delete i[this.expando]);
        }
      },
      hasData: function(i) {
        var s = i[this.expando];
        return s !== void 0 && !d.isEmptyObject(s);
      }
    };
    var ce = new Ft(), nt = new Ft(), Wt = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, Kt = /[A-Z]/g;
    function Pr(i) {
      return i === "true" ? !0 : i === "false" ? !1 : i === "null" ? null : i === +i + "" ? +i : Wt.test(i) ? JSON.parse(i) : i;
    }
    function An(i, s, l) {
      var h;
      if (l === void 0 && i.nodeType === 1)
        if (h = "data-" + s.replace(Kt, "-$&").toLowerCase(), l = i.getAttribute(h), typeof l == "string") {
          try {
            l = Pr(l);
          } catch {
          }
          nt.set(i, s, l);
        } else
          l = void 0;
      return l;
    }
    d.extend({
      hasData: function(i) {
        return nt.hasData(i) || ce.hasData(i);
      },
      data: function(i, s, l) {
        return nt.access(i, s, l);
      },
      removeData: function(i, s) {
        nt.remove(i, s);
      },
      // TODO: Now that all calls to _data and _removeData have been replaced
      // with direct calls to dataPriv methods, these can be deprecated.
      _data: function(i, s, l) {
        return ce.access(i, s, l);
      },
      _removeData: function(i, s) {
        ce.remove(i, s);
      }
    }), d.fn.extend({
      data: function(i, s) {
        var l, h, f, m = this[0], b = m && m.attributes;
        if (i === void 0) {
          if (this.length && (f = nt.get(m), m.nodeType === 1 && !ce.get(m, "hasDataAttrs"))) {
            for (l = b.length; l--; )
              b[l] && (h = b[l].name, h.indexOf("data-") === 0 && (h = It(h.slice(5)), An(m, h, f[h])));
            ce.set(m, "hasDataAttrs", !0);
          }
          return f;
        }
        return typeof i == "object" ? this.each(function() {
          nt.set(this, i);
        }) : vt(this, function(E) {
          var _;
          if (m && E === void 0)
            return _ = nt.get(m, i), _ !== void 0 || (_ = An(m, i), _ !== void 0) ? _ : void 0;
          this.each(function() {
            nt.set(this, i, E);
          });
        }, null, s, arguments.length > 1, null, !0);
      },
      removeData: function(i) {
        return this.each(function() {
          nt.remove(this, i);
        });
      }
    }), d.extend({
      queue: function(i, s, l) {
        var h;
        if (i)
          return s = (s || "fx") + "queue", h = ce.get(i, s), l && (!h || Array.isArray(l) ? h = ce.access(i, s, d.makeArray(l)) : h.push(l)), h || [];
      },
      dequeue: function(i, s) {
        s = s || "fx";
        var l = d.queue(i, s), h = l.length, f = l.shift(), m = d._queueHooks(i, s), b = function() {
          d.dequeue(i, s);
        };
        f === "inprogress" && (f = l.shift(), h--), f && (s === "fx" && l.unshift("inprogress"), delete m.stop, f.call(i, b, m)), !h && m && m.empty.fire();
      },
      // Not public - generate a queueHooks object, or return the current one
      _queueHooks: function(i, s) {
        var l = s + "queueHooks";
        return ce.get(i, l) || ce.access(i, l, {
          empty: d.Callbacks("once memory").add(function() {
            ce.remove(i, [s + "queue", l]);
          })
        });
      }
    }), d.fn.extend({
      queue: function(i, s) {
        var l = 2;
        return typeof i != "string" && (s = i, i = "fx", l--), arguments.length < l ? d.queue(this[0], i) : s === void 0 ? this : this.each(function() {
          var h = d.queue(this, i, s);
          d._queueHooks(this, i), i === "fx" && h[0] !== "inprogress" && d.dequeue(this, i);
        });
      },
      dequeue: function(i) {
        return this.each(function() {
          d.dequeue(this, i);
        });
      },
      clearQueue: function(i) {
        return this.queue(i || "fx", []);
      },
      // Get a promise resolved when queues of a certain type
      // are emptied (fx is the type by default)
      promise: function(i, s) {
        var l, h = 1, f = d.Deferred(), m = this, b = this.length, E = function() {
          --h || f.resolveWith(m, [m]);
        };
        for (typeof i != "string" && (s = i, i = void 0), i = i || "fx"; b--; )
          l = ce.get(m[b], i + "queueHooks"), l && l.empty && (h++, l.empty.add(E));
        return E(), f.promise(s);
      }
    });
    var Xt = /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/.source, Do = new RegExp("^(?:([+-])=|)(" + Xt + ")([a-z%]*)$", "i"), zt = ["Top", "Right", "Bottom", "Left"], Jt = Y.documentElement, po = function(i) {
      return d.contains(i.ownerDocument, i);
    }, Ue = { composed: !0 };
    Jt.getRootNode && (po = function(i) {
      return d.contains(i.ownerDocument, i) || i.getRootNode(Ue) === i.ownerDocument;
    });
    var pn = function(i, s) {
      return i = s || i, i.style.display === "none" || i.style.display === "" && // Otherwise, check computed style
      // Support: Firefox <=43 - 45
      // Disconnected elements can have computed display: none, so first confirm that elem is
      // in the document.
      po(i) && d.css(i, "display") === "none";
    };
    function tr(i, s, l, h) {
      var f, m, b = 20, E = h ? function() {
        return h.cur();
      } : function() {
        return d.css(i, s, "");
      }, _ = E(), O = l && l[3] || (d.cssNumber[s] ? "" : "px"), W = i.nodeType && (d.cssNumber[s] || O !== "px" && +_) && Do.exec(d.css(i, s));
      if (W && W[3] !== O) {
        for (_ = _ / 2, O = O || W[3], W = +_ || 1; b--; )
          d.style(i, s, W + O), (1 - m) * (1 - (m = E() / _ || 0.5)) <= 0 && (b = 0), W = W / m;
        W = W * 2, d.style(i, s, W + O), l = l || [];
      }
      return l && (W = +W || +_ || 0, f = l[1] ? W + (l[1] + 1) * l[2] : +l[2], h && (h.unit = O, h.start = W, h.end = f)), f;
    }
    var jo = {};
    function Ss(i) {
      var s, l = i.ownerDocument, h = i.nodeName, f = jo[h];
      return f || (s = l.body.appendChild(l.createElement(h)), f = d.css(s, "display"), s.parentNode.removeChild(s), f === "none" && (f = "block"), jo[h] = f, f);
    }
    function wo(i, s) {
      for (var l, h, f = [], m = 0, b = i.length; m < b; m++)
        h = i[m], h.style && (l = h.style.display, s ? (l === "none" && (f[m] = ce.get(h, "display") || null, f[m] || (h.style.display = "")), h.style.display === "" && pn(h) && (f[m] = Ss(h))) : l !== "none" && (f[m] = "none", ce.set(h, "display", l)));
      for (m = 0; m < b; m++)
        f[m] != null && (i[m].style.display = f[m]);
      return i;
    }
    d.fn.extend({
      show: function() {
        return wo(this, !0);
      },
      hide: function() {
        return wo(this);
      },
      toggle: function(i) {
        return typeof i == "boolean" ? i ? this.show() : this.hide() : this.each(function() {
          pn(this) ? d(this).show() : d(this).hide();
        });
      }
    });
    var ko = /^(?:checkbox|radio)$/i, or = /<([a-z][^\/\0>\x20\t\r\n\f]*)/i, nr = /^$|^module$|\/(?:java|ecma)script/i;
    (function() {
      var i = Y.createDocumentFragment(), s = i.appendChild(Y.createElement("div")), l = Y.createElement("input");
      l.setAttribute("type", "radio"), l.setAttribute("checked", "checked"), l.setAttribute("name", "t"), s.appendChild(l), x.checkClone = s.cloneNode(!0).cloneNode(!0).lastChild.checked, s.innerHTML = "<textarea>x</textarea>", x.noCloneChecked = !!s.cloneNode(!0).lastChild.defaultValue, s.innerHTML = "<option></option>", x.option = !!s.lastChild;
    })();
    var pt = {
      // XHTML parsers do not magically insert elements in the
      // same way that tag soup parsers do. So we cannot shorten
      // this by omitting <tbody> or other required elements.
      thead: [1, "<table>", "</table>"],
      col: [2, "<table><colgroup>", "</colgroup></table>"],
      tr: [2, "<table><tbody>", "</tbody></table>"],
      td: [3, "<table><tbody><tr>", "</tr></tbody></table>"],
      _default: [0, "", ""]
    };
    pt.tbody = pt.tfoot = pt.colgroup = pt.caption = pt.thead, pt.th = pt.td, x.option || (pt.optgroup = pt.option = [1, "<select multiple='multiple'>", "</select>"]);
    function ft(i, s) {
      var l;
      return typeof i.getElementsByTagName < "u" ? l = i.getElementsByTagName(s || "*") : typeof i.querySelectorAll < "u" ? l = i.querySelectorAll(s || "*") : l = [], s === void 0 || s && R(i, s) ? d.merge([i], l) : l;
    }
    function fn(i, s) {
      for (var l = 0, h = i.length; l < h; l++)
        ce.set(
          i[l],
          "globalEval",
          !s || ce.get(s[l], "globalEval")
        );
    }
    var Ts = /<|&#?\w+;/;
    function qe(i, s, l, h, f) {
      for (var m, b, E, _, O, W, q = s.createDocumentFragment(), N = [], X = 0, ge = i.length; X < ge; X++)
        if (m = i[X], m || m === 0)
          if (Q(m) === "object")
            d.merge(N, m.nodeType ? [m] : m);
          else if (!Ts.test(m))
            N.push(s.createTextNode(m));
          else {
            for (b = b || q.appendChild(s.createElement("div")), E = (or.exec(m) || ["", ""])[1].toLowerCase(), _ = pt[E] || pt._default, b.innerHTML = _[1] + d.htmlPrefilter(m) + _[2], W = _[0]; W--; )
              b = b.lastChild;
            d.merge(N, b.childNodes), b = q.firstChild, b.textContent = "";
          }
      for (q.textContent = "", X = 0; m = N[X++]; ) {
        if (h && d.inArray(m, h) > -1) {
          f && f.push(m);
          continue;
        }
        if (O = po(m), b = ft(q.appendChild(m), "script"), O && fn(b), l)
          for (W = 0; m = b[W++]; )
            nr.test(m.type || "") && l.push(m);
      }
      return q;
    }
    var ir = /^([^.]*)(?:\.(.+)|)/;
    function xo() {
      return !0;
    }
    function Co() {
      return !1;
    }
    function qn(i, s, l, h, f, m) {
      var b, E;
      if (typeof s == "object") {
        typeof l != "string" && (h = h || l, l = void 0);
        for (E in s)
          qn(i, E, l, h, s[E], m);
        return i;
      }
      if (h == null && f == null ? (f = l, h = l = void 0) : f == null && (typeof l == "string" ? (f = h, h = void 0) : (f = h, h = l, l = void 0)), f === !1)
        f = Co;
      else if (!f)
        return i;
      return m === 1 && (b = f, f = function(_) {
        return d().off(_), b.apply(this, arguments);
      }, f.guid = b.guid || (b.guid = d.guid++)), i.each(function() {
        d.event.add(this, s, f, h, l);
      });
    }
    d.event = {
      global: {},
      add: function(i, s, l, h, f) {
        var m, b, E, _, O, W, q, N, X, ge, Ce, xe = ce.get(i);
        if (We(i))
          for (l.handler && (m = l, l = m.handler, f = m.selector), f && d.find.matchesSelector(Jt, f), l.guid || (l.guid = d.guid++), (_ = xe.events) || (_ = xe.events = /* @__PURE__ */ Object.create(null)), (b = xe.handle) || (b = xe.handle = function(Qe) {
            return typeof d < "u" && d.event.triggered !== Qe.type ? d.event.dispatch.apply(i, arguments) : void 0;
          }), s = (s || "").match(Rt) || [""], O = s.length; O--; )
            E = ir.exec(s[O]) || [], X = Ce = E[1], ge = (E[2] || "").split(".").sort(), X && (q = d.event.special[X] || {}, X = (f ? q.delegateType : q.bindType) || X, q = d.event.special[X] || {}, W = d.extend({
              type: X,
              origType: Ce,
              data: h,
              handler: l,
              guid: l.guid,
              selector: f,
              needsContext: f && d.expr.match.needsContext.test(f),
              namespace: ge.join(".")
            }, m), (N = _[X]) || (N = _[X] = [], N.delegateCount = 0, (!q.setup || q.setup.call(i, h, ge, b) === !1) && i.addEventListener && i.addEventListener(X, b)), q.add && (q.add.call(i, W), W.handler.guid || (W.handler.guid = l.guid)), f ? N.splice(N.delegateCount++, 0, W) : N.push(W), d.event.global[X] = !0);
      },
      // Detach an event or set of events from an element
      remove: function(i, s, l, h, f) {
        var m, b, E, _, O, W, q, N, X, ge, Ce, xe = ce.hasData(i) && ce.get(i);
        if (!(!xe || !(_ = xe.events))) {
          for (s = (s || "").match(Rt) || [""], O = s.length; O--; ) {
            if (E = ir.exec(s[O]) || [], X = Ce = E[1], ge = (E[2] || "").split(".").sort(), !X) {
              for (X in _)
                d.event.remove(i, X + s[O], l, h, !0);
              continue;
            }
            for (q = d.event.special[X] || {}, X = (h ? q.delegateType : q.bindType) || X, N = _[X] || [], E = E[2] && new RegExp("(^|\\.)" + ge.join("\\.(?:.*\\.|)") + "(\\.|$)"), b = m = N.length; m--; )
              W = N[m], (f || Ce === W.origType) && (!l || l.guid === W.guid) && (!E || E.test(W.namespace)) && (!h || h === W.selector || h === "**" && W.selector) && (N.splice(m, 1), W.selector && N.delegateCount--, q.remove && q.remove.call(i, W));
            b && !N.length && ((!q.teardown || q.teardown.call(i, ge, xe.handle) === !1) && d.removeEvent(i, X, xe.handle), delete _[X]);
          }
          d.isEmptyObject(_) && ce.remove(i, "handle events");
        }
      },
      dispatch: function(i) {
        var s, l, h, f, m, b, E = new Array(arguments.length), _ = d.event.fix(i), O = (ce.get(this, "events") || /* @__PURE__ */ Object.create(null))[_.type] || [], W = d.event.special[_.type] || {};
        for (E[0] = _, s = 1; s < arguments.length; s++)
          E[s] = arguments[s];
        if (_.delegateTarget = this, !(W.preDispatch && W.preDispatch.call(this, _) === !1)) {
          for (b = d.event.handlers.call(this, _, O), s = 0; (f = b[s++]) && !_.isPropagationStopped(); )
            for (_.currentTarget = f.elem, l = 0; (m = f.handlers[l++]) && !_.isImmediatePropagationStopped(); )
              (!_.rnamespace || m.namespace === !1 || _.rnamespace.test(m.namespace)) && (_.handleObj = m, _.data = m.data, h = ((d.event.special[m.origType] || {}).handle || m.handler).apply(f.elem, E), h !== void 0 && (_.result = h) === !1 && (_.preventDefault(), _.stopPropagation()));
          return W.postDispatch && W.postDispatch.call(this, _), _.result;
        }
      },
      handlers: function(i, s) {
        var l, h, f, m, b, E = [], _ = s.delegateCount, O = i.target;
        if (_ && // Support: IE <=9
        // Black-hole SVG <use> instance trees (trac-13180)
        O.nodeType && // Support: Firefox <=42
        // Suppress spec-violating clicks indicating a non-primary pointer button (trac-3861)
        // https://www.w3.org/TR/DOM-Level-3-Events/#event-type-click
        // Support: IE 11 only
        // ...but not arrow key "clicks" of radio inputs, which can have `button` -1 (gh-2343)
        !(i.type === "click" && i.button >= 1)) {
          for (; O !== this; O = O.parentNode || this)
            if (O.nodeType === 1 && !(i.type === "click" && O.disabled === !0)) {
              for (m = [], b = {}, l = 0; l < _; l++)
                h = s[l], f = h.selector + " ", b[f] === void 0 && (b[f] = h.needsContext ? d(f, this).index(O) > -1 : d.find(f, this, null, [O]).length), b[f] && m.push(h);
              m.length && E.push({ elem: O, handlers: m });
            }
        }
        return O = this, _ < s.length && E.push({ elem: O, handlers: s.slice(_) }), E;
      },
      addProp: function(i, s) {
        Object.defineProperty(d.Event.prototype, i, {
          enumerable: !0,
          configurable: !0,
          get: M(s) ? function() {
            if (this.originalEvent)
              return s(this.originalEvent);
          } : function() {
            if (this.originalEvent)
              return this.originalEvent[i];
          },
          set: function(l) {
            Object.defineProperty(this, i, {
              enumerable: !0,
              configurable: !0,
              writable: !0,
              value: l
            });
          }
        });
      },
      fix: function(i) {
        return i[d.expando] ? i : new d.Event(i);
      },
      special: {
        load: {
          // Prevent triggered image.load events from bubbling to window.load
          noBubble: !0
        },
        click: {
          // Utilize native event to ensure correct state for checkable inputs
          setup: function(i) {
            var s = this || i;
            return ko.test(s.type) && s.click && R(s, "input") && $o(s, "click", !0), !1;
          },
          trigger: function(i) {
            var s = this || i;
            return ko.test(s.type) && s.click && R(s, "input") && $o(s, "click"), !0;
          },
          // For cross-browser consistency, suppress native .click() on links
          // Also prevent it if we're currently inside a leveraged native-event stack
          _default: function(i) {
            var s = i.target;
            return ko.test(s.type) && s.click && R(s, "input") && ce.get(s, "click") || R(s, "a");
          }
        },
        beforeunload: {
          postDispatch: function(i) {
            i.result !== void 0 && i.originalEvent && (i.originalEvent.returnValue = i.result);
          }
        }
      }
    };
    function $o(i, s, l) {
      if (!l) {
        ce.get(i, s) === void 0 && d.event.add(i, s, xo);
        return;
      }
      ce.set(i, s, !1), d.event.add(i, s, {
        namespace: !1,
        handler: function(h) {
          var f, m = ce.get(this, s);
          if (h.isTrigger & 1 && this[s]) {
            if (m)
              (d.event.special[s] || {}).delegateType && h.stopPropagation();
            else if (m = a.call(arguments), ce.set(this, s, m), this[s](), f = ce.get(this, s), ce.set(this, s, !1), m !== f)
              return h.stopImmediatePropagation(), h.preventDefault(), f;
          } else
            m && (ce.set(this, s, d.event.trigger(
              m[0],
              m.slice(1),
              this
            )), h.stopPropagation(), h.isImmediatePropagationStopped = xo);
        }
      });
    }
    d.removeEvent = function(i, s, l) {
      i.removeEventListener && i.removeEventListener(s, l);
    }, d.Event = function(i, s) {
      if (!(this instanceof d.Event))
        return new d.Event(i, s);
      i && i.type ? (this.originalEvent = i, this.type = i.type, this.isDefaultPrevented = i.defaultPrevented || i.defaultPrevented === void 0 && // Support: Android <=2.3 only
      i.returnValue === !1 ? xo : Co, this.target = i.target && i.target.nodeType === 3 ? i.target.parentNode : i.target, this.currentTarget = i.currentTarget, this.relatedTarget = i.relatedTarget) : this.type = i, s && d.extend(this, s), this.timeStamp = i && i.timeStamp || Date.now(), this[d.expando] = !0;
    }, d.Event.prototype = {
      constructor: d.Event,
      isDefaultPrevented: Co,
      isPropagationStopped: Co,
      isImmediatePropagationStopped: Co,
      isSimulated: !1,
      preventDefault: function() {
        var i = this.originalEvent;
        this.isDefaultPrevented = xo, i && !this.isSimulated && i.preventDefault();
      },
      stopPropagation: function() {
        var i = this.originalEvent;
        this.isPropagationStopped = xo, i && !this.isSimulated && i.stopPropagation();
      },
      stopImmediatePropagation: function() {
        var i = this.originalEvent;
        this.isImmediatePropagationStopped = xo, i && !this.isSimulated && i.stopImmediatePropagation(), this.stopPropagation();
      }
    }, d.each({
      altKey: !0,
      bubbles: !0,
      cancelable: !0,
      changedTouches: !0,
      ctrlKey: !0,
      detail: !0,
      eventPhase: !0,
      metaKey: !0,
      pageX: !0,
      pageY: !0,
      shiftKey: !0,
      view: !0,
      char: !0,
      code: !0,
      charCode: !0,
      key: !0,
      keyCode: !0,
      button: !0,
      buttons: !0,
      clientX: !0,
      clientY: !0,
      offsetX: !0,
      offsetY: !0,
      pointerId: !0,
      pointerType: !0,
      screenX: !0,
      screenY: !0,
      targetTouches: !0,
      toElement: !0,
      touches: !0,
      which: !0
    }, d.event.addProp), d.each({ focus: "focusin", blur: "focusout" }, function(i, s) {
      function l(h) {
        if (Y.documentMode) {
          var f = ce.get(this, "handle"), m = d.event.fix(h);
          m.type = h.type === "focusin" ? "focus" : "blur", m.isSimulated = !0, f(h), m.target === m.currentTarget && f(m);
        } else
          d.event.simulate(
            s,
            h.target,
            d.event.fix(h)
          );
      }
      d.event.special[i] = {
        // Utilize native event if possible so blur/focus sequence is correct
        setup: function() {
          var h;
          if ($o(this, i, !0), Y.documentMode)
            h = ce.get(this, s), h || this.addEventListener(s, l), ce.set(this, s, (h || 0) + 1);
          else
            return !1;
        },
        trigger: function() {
          return $o(this, i), !0;
        },
        teardown: function() {
          var h;
          if (Y.documentMode)
            h = ce.get(this, s) - 1, h ? ce.set(this, s, h) : (this.removeEventListener(s, l), ce.remove(this, s));
          else
            return !1;
        },
        // Suppress native focus or blur if we're currently inside
        // a leveraged native-event stack
        _default: function(h) {
          return ce.get(h.target, i);
        },
        delegateType: s
      }, d.event.special[s] = {
        setup: function() {
          var h = this.ownerDocument || this.document || this, f = Y.documentMode ? this : h, m = ce.get(f, s);
          m || (Y.documentMode ? this.addEventListener(s, l) : h.addEventListener(i, l, !0)), ce.set(f, s, (m || 0) + 1);
        },
        teardown: function() {
          var h = this.ownerDocument || this.document || this, f = Y.documentMode ? this : h, m = ce.get(f, s) - 1;
          m ? ce.set(f, s, m) : (Y.documentMode ? this.removeEventListener(s, l) : h.removeEventListener(i, l, !0), ce.remove(f, s));
        }
      };
    }), d.each({
      mouseenter: "mouseover",
      mouseleave: "mouseout",
      pointerenter: "pointerover",
      pointerleave: "pointerout"
    }, function(i, s) {
      d.event.special[i] = {
        delegateType: s,
        bindType: s,
        handle: function(l) {
          var h, f = this, m = l.relatedTarget, b = l.handleObj;
          return (!m || m !== f && !d.contains(f, m)) && (l.type = b.origType, h = b.handler.apply(this, arguments), l.type = s), h;
        }
      };
    }), d.fn.extend({
      on: function(i, s, l, h) {
        return qn(this, i, s, l, h);
      },
      one: function(i, s, l, h) {
        return qn(this, i, s, l, h, 1);
      },
      off: function(i, s, l) {
        var h, f;
        if (i && i.preventDefault && i.handleObj)
          return h = i.handleObj, d(i.delegateTarget).off(
            h.namespace ? h.origType + "." + h.namespace : h.origType,
            h.selector,
            h.handler
          ), this;
        if (typeof i == "object") {
          for (f in i)
            this.off(f, s, i[f]);
          return this;
        }
        return (s === !1 || typeof s == "function") && (l = s, s = void 0), l === !1 && (l = Co), this.each(function() {
          d.event.remove(this, i, l, s);
        });
      }
    });
    var Vn = /<script|<style|<link/i, Hr = /checked\s*(?:[^=]|=\s*.checked.)/i, oo = /^\s*<!\[CDATA\[|\]\]>\s*$/g;
    function Oi(i, s) {
      return R(i, "table") && R(s.nodeType !== 11 ? s : s.firstChild, "tr") && d(i).children("tbody")[0] || i;
    }
    function _s(i) {
      return i.type = (i.getAttribute("type") !== null) + "/" + i.type, i;
    }
    function jr(i) {
      return (i.type || "").slice(0, 5) === "true/" ? i.type = i.type.slice(5) : i.removeAttribute("type"), i;
    }
    function rr(i, s) {
      var l, h, f, m, b, E, _;
      if (s.nodeType === 1) {
        if (ce.hasData(i) && (m = ce.get(i), _ = m.events, _)) {
          ce.remove(s, "handle events");
          for (f in _)
            for (l = 0, h = _[f].length; l < h; l++)
              d.event.add(s, f, _[f][l]);
        }
        nt.hasData(i) && (b = nt.access(i), E = d.extend({}, b), nt.set(s, E));
      }
    }
    function $r(i, s) {
      var l = s.nodeName.toLowerCase();
      l === "input" && ko.test(i.type) ? s.checked = i.checked : (l === "input" || l === "textarea") && (s.defaultValue = i.defaultValue);
    }
    function So(i, s, l, h) {
      s = c(s);
      var f, m, b, E, _, O, W = 0, q = i.length, N = q - 1, X = s[0], ge = M(X);
      if (ge || q > 1 && typeof X == "string" && !x.checkClone && Hr.test(X))
        return i.each(function(Ce) {
          var xe = i.eq(Ce);
          ge && (s[0] = X.call(this, Ce, xe.html())), So(xe, s, l, h);
        });
      if (q && (f = qe(s, i[0].ownerDocument, !1, i, h), m = f.firstChild, f.childNodes.length === 1 && (f = m), m || h)) {
        for (b = d.map(ft(f, "script"), _s), E = b.length; W < q; W++)
          _ = f, W !== N && (_ = d.clone(_, !0, !0), E && d.merge(b, ft(_, "script"))), l.call(i[W], _, W);
        if (E)
          for (O = b[b.length - 1].ownerDocument, d.map(b, jr), W = 0; W < E; W++)
            _ = b[W], nr.test(_.type || "") && !ce.access(_, "globalEval") && d.contains(O, _) && (_.src && (_.type || "").toLowerCase() !== "module" ? d._evalUrl && !_.noModule && d._evalUrl(_.src, {
              nonce: _.nonce || _.getAttribute("nonce")
            }, O) : F(_.textContent.replace(oo, ""), _, O));
      }
      return i;
    }
    function Bi(i, s, l) {
      for (var h, f = s ? d.filter(s, i) : i, m = 0; (h = f[m]) != null; m++)
        !l && h.nodeType === 1 && d.cleanData(ft(h)), h.parentNode && (l && po(h) && fn(ft(h, "script")), h.parentNode.removeChild(h));
      return i;
    }
    d.extend({
      htmlPrefilter: function(i) {
        return i;
      },
      clone: function(i, s, l) {
        var h, f, m, b, E = i.cloneNode(!0), _ = po(i);
        if (!x.noCloneChecked && (i.nodeType === 1 || i.nodeType === 11) && !d.isXMLDoc(i))
          for (b = ft(E), m = ft(i), h = 0, f = m.length; h < f; h++)
            $r(m[h], b[h]);
        if (s)
          if (l)
            for (m = m || ft(i), b = b || ft(E), h = 0, f = m.length; h < f; h++)
              rr(m[h], b[h]);
          else
            rr(i, E);
        return b = ft(E, "script"), b.length > 0 && fn(b, !_ && ft(i, "script")), E;
      },
      cleanData: function(i) {
        for (var s, l, h, f = d.event.special, m = 0; (l = i[m]) !== void 0; m++)
          if (We(l)) {
            if (s = l[ce.expando]) {
              if (s.events)
                for (h in s.events)
                  f[h] ? d.event.remove(l, h) : d.removeEvent(l, h, s.handle);
              l[ce.expando] = void 0;
            }
            l[nt.expando] && (l[nt.expando] = void 0);
          }
      }
    }), d.fn.extend({
      detach: function(i) {
        return Bi(this, i, !0);
      },
      remove: function(i) {
        return Bi(this, i);
      },
      text: function(i) {
        return vt(this, function(s) {
          return s === void 0 ? d.text(this) : this.empty().each(function() {
            (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) && (this.textContent = s);
          });
        }, null, i, arguments.length);
      },
      append: function() {
        return So(this, arguments, function(i) {
          if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
            var s = Oi(this, i);
            s.appendChild(i);
          }
        });
      },
      prepend: function() {
        return So(this, arguments, function(i) {
          if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
            var s = Oi(this, i);
            s.insertBefore(i, s.firstChild);
          }
        });
      },
      before: function() {
        return So(this, arguments, function(i) {
          this.parentNode && this.parentNode.insertBefore(i, this);
        });
      },
      after: function() {
        return So(this, arguments, function(i) {
          this.parentNode && this.parentNode.insertBefore(i, this.nextSibling);
        });
      },
      empty: function() {
        for (var i, s = 0; (i = this[s]) != null; s++)
          i.nodeType === 1 && (d.cleanData(ft(i, !1)), i.textContent = "");
        return this;
      },
      clone: function(i, s) {
        return i = i ?? !1, s = s ?? i, this.map(function() {
          return d.clone(this, i, s);
        });
      },
      html: function(i) {
        return vt(this, function(s) {
          var l = this[0] || {}, h = 0, f = this.length;
          if (s === void 0 && l.nodeType === 1)
            return l.innerHTML;
          if (typeof s == "string" && !Vn.test(s) && !pt[(or.exec(s) || ["", ""])[1].toLowerCase()]) {
            s = d.htmlPrefilter(s);
            try {
              for (; h < f; h++)
                l = this[h] || {}, l.nodeType === 1 && (d.cleanData(ft(l, !1)), l.innerHTML = s);
              l = 0;
            } catch {
            }
          }
          l && this.empty().append(s);
        }, null, i, arguments.length);
      },
      replaceWith: function() {
        var i = [];
        return So(this, arguments, function(s) {
          var l = this.parentNode;
          d.inArray(this, i) < 0 && (d.cleanData(ft(this)), l && l.replaceChild(s, this));
        }, i);
      }
    }), d.each({
      appendTo: "append",
      prependTo: "prepend",
      insertBefore: "before",
      insertAfter: "after",
      replaceAll: "replaceWith"
    }, function(i, s) {
      d.fn[i] = function(l) {
        for (var h, f = [], m = d(l), b = m.length - 1, E = 0; E <= b; E++)
          h = E === b ? this : this.clone(!0), d(m[E])[s](h), u.apply(f, h.get());
        return this.pushStack(f);
      };
    });
    var Gn = new RegExp("^(" + Xt + ")(?!px)[a-z%]+$", "i"), Zn = /^--/, en = function(i) {
      var s = i.ownerDocument.defaultView;
      return (!s || !s.opener) && (s = e), s.getComputedStyle(i);
    }, tn = function(i, s, l) {
      var h, f, m = {};
      for (f in s)
        m[f] = i.style[f], i.style[f] = s[f];
      h = l.call(i);
      for (f in s)
        i.style[f] = m[f];
      return h;
    }, Yr = new RegExp(zt.join("|"), "i");
    (function() {
      function i() {
        if (O) {
          _.style.cssText = "position:absolute;left:-11111px;width:60px;margin-top:1px;padding:0;border:0", O.style.cssText = "position:relative;display:block;box-sizing:border-box;overflow:scroll;margin:auto;border:1px;padding:1px;width:60%;top:1%", Jt.appendChild(_).appendChild(O);
          var W = e.getComputedStyle(O);
          l = W.top !== "1%", E = s(W.marginLeft) === 12, O.style.right = "60%", m = s(W.right) === 36, h = s(W.width) === 36, O.style.position = "absolute", f = s(O.offsetWidth / 3) === 12, Jt.removeChild(_), O = null;
        }
      }
      function s(W) {
        return Math.round(parseFloat(W));
      }
      var l, h, f, m, b, E, _ = Y.createElement("div"), O = Y.createElement("div");
      O.style && (O.style.backgroundClip = "content-box", O.cloneNode(!0).style.backgroundClip = "", x.clearCloneStyle = O.style.backgroundClip === "content-box", d.extend(x, {
        boxSizingReliable: function() {
          return i(), h;
        },
        pixelBoxStyles: function() {
          return i(), m;
        },
        pixelPosition: function() {
          return i(), l;
        },
        reliableMarginLeft: function() {
          return i(), E;
        },
        scrollboxSize: function() {
          return i(), f;
        },
        // Support: IE 9 - 11+, Edge 15 - 18+
        // IE/Edge misreport `getComputedStyle` of table rows with width/height
        // set in CSS while `offset*` properties report correct values.
        // Behavior in IE 9 is more subtle than in newer versions & it passes
        // some versions of this test; make sure not to make it pass there!
        //
        // Support: Firefox 70+
        // Only Firefox includes border widths
        // in computed dimensions. (gh-4529)
        reliableTrDimensions: function() {
          var W, q, N, X;
          return b == null && (W = Y.createElement("table"), q = Y.createElement("tr"), N = Y.createElement("div"), W.style.cssText = "position:absolute;left:-11111px;border-collapse:separate", q.style.cssText = "box-sizing:content-box;border:1px solid", q.style.height = "1px", N.style.height = "9px", N.style.display = "block", Jt.appendChild(W).appendChild(q).appendChild(N), X = e.getComputedStyle(q), b = parseInt(X.height, 10) + parseInt(X.borderTopWidth, 10) + parseInt(X.borderBottomWidth, 10) === q.offsetHeight, Jt.removeChild(W)), b;
        }
      }));
    })();
    function Lo(i, s, l) {
      var h, f, m, b, E = Zn.test(s), _ = i.style;
      return l = l || en(i), l && (b = l.getPropertyValue(s) || l[s], E && b && (b = b.replace(se, "$1") || void 0), b === "" && !po(i) && (b = d.style(i, s)), !x.pixelBoxStyles() && Gn.test(b) && Yr.test(s) && (h = _.width, f = _.minWidth, m = _.maxWidth, _.minWidth = _.maxWidth = _.width = b, b = l.width, _.width = h, _.minWidth = f, _.maxWidth = m)), b !== void 0 ? (
        // Support: IE <=9 - 11 only
        // IE returns zIndex value as an integer.
        b + ""
      ) : b;
    }
    function sr(i, s) {
      return {
        get: function() {
          if (i()) {
            delete this.get;
            return;
          }
          return (this.get = s).apply(this, arguments);
        }
      };
    }
    var ar = ["Webkit", "Moz", "ms"], lr = Y.createElement("div").style, cr = {};
    function Es(i) {
      for (var s = i[0].toUpperCase() + i.slice(1), l = ar.length; l--; )
        if (i = ar[l] + s, i in lr)
          return i;
    }
    function Kn(i) {
      var s = d.cssProps[i] || cr[i];
      return s || (i in lr ? i : cr[i] = Es(i) || i);
    }
    var Ms = /^(none|table(?!-c[ea]).+)/, mn = { position: "absolute", visibility: "hidden", display: "block" }, Ai = {
      letterSpacing: "0",
      fontWeight: "400"
    };
    function dr(i, s, l) {
      var h = Do.exec(s);
      return h ? (
        // Guard against undefined "subtract", e.g., when used as in cssHooks
        Math.max(0, h[2] - (l || 0)) + (h[3] || "px")
      ) : s;
    }
    function Xn(i, s, l, h, f, m) {
      var b = s === "width" ? 1 : 0, E = 0, _ = 0, O = 0;
      if (l === (h ? "border" : "content"))
        return 0;
      for (; b < 4; b += 2)
        l === "margin" && (O += d.css(i, l + zt[b], !0, f)), h ? (l === "content" && (_ -= d.css(i, "padding" + zt[b], !0, f)), l !== "margin" && (_ -= d.css(i, "border" + zt[b] + "Width", !0, f))) : (_ += d.css(i, "padding" + zt[b], !0, f), l !== "padding" ? _ += d.css(i, "border" + zt[b] + "Width", !0, f) : E += d.css(i, "border" + zt[b] + "Width", !0, f));
      return !h && m >= 0 && (_ += Math.max(0, Math.ceil(
        i["offset" + s[0].toUpperCase() + s.slice(1)] - m - _ - E - 0.5
        // If offsetWidth/offsetHeight is unknown, then we can't determine content-box scroll gutter
        // Use an explicit zero to avoid NaN (gh-3964)
      )) || 0), _ + O;
    }
    function hr(i, s, l) {
      var h = en(i), f = !x.boxSizingReliable() || l, m = f && d.css(i, "boxSizing", !1, h) === "border-box", b = m, E = Lo(i, s, h), _ = "offset" + s[0].toUpperCase() + s.slice(1);
      if (Gn.test(E)) {
        if (!l)
          return E;
        E = "auto";
      }
      return (!x.boxSizingReliable() && m || // Support: IE 10 - 11+, Edge 15 - 18+
      // IE/Edge misreport `getComputedStyle` of table rows with width/height
      // set in CSS while `offset*` properties report correct values.
      // Interestingly, in some cases IE 9 doesn't suffer from this issue.
      !x.reliableTrDimensions() && R(i, "tr") || // Fall back to offsetWidth/offsetHeight when value is "auto"
      // This happens for inline elements with no explicit setting (gh-3571)
      E === "auto" || // Support: Android <=4.1 - 4.3 only
      // Also use offsetWidth/offsetHeight for misreported inline dimensions (gh-3602)
      !parseFloat(E) && d.css(i, "display", !1, h) === "inline") && // Make sure the element is visible & connected
      i.getClientRects().length && (m = d.css(i, "boxSizing", !1, h) === "border-box", b = _ in i, b && (E = i[_])), E = parseFloat(E) || 0, E + Xn(
        i,
        s,
        l || (m ? "border" : "content"),
        b,
        h,
        // Provide the current computed size to request scroll gutter calculation (gh-3589)
        E
      ) + "px";
    }
    d.extend({
      // Add in style property hooks for overriding the default
      // behavior of getting and setting a style property
      cssHooks: {
        opacity: {
          get: function(i, s) {
            if (s) {
              var l = Lo(i, "opacity");
              return l === "" ? "1" : l;
            }
          }
        }
      },
      // Don't automatically add "px" to these possibly-unitless properties
      cssNumber: {
        animationIterationCount: !0,
        aspectRatio: !0,
        borderImageSlice: !0,
        columnCount: !0,
        flexGrow: !0,
        flexShrink: !0,
        fontWeight: !0,
        gridArea: !0,
        gridColumn: !0,
        gridColumnEnd: !0,
        gridColumnStart: !0,
        gridRow: !0,
        gridRowEnd: !0,
        gridRowStart: !0,
        lineHeight: !0,
        opacity: !0,
        order: !0,
        orphans: !0,
        scale: !0,
        widows: !0,
        zIndex: !0,
        zoom: !0,
        // SVG-related
        fillOpacity: !0,
        floodOpacity: !0,
        stopOpacity: !0,
        strokeMiterlimit: !0,
        strokeOpacity: !0
      },
      // Add in properties whose names you wish to fix before
      // setting or getting the value
      cssProps: {},
      // Get and set the style property on a DOM Node
      style: function(i, s, l, h) {
        if (!(!i || i.nodeType === 3 || i.nodeType === 8 || !i.style)) {
          var f, m, b, E = It(s), _ = Zn.test(s), O = i.style;
          if (_ || (s = Kn(E)), b = d.cssHooks[s] || d.cssHooks[E], l !== void 0) {
            if (m = typeof l, m === "string" && (f = Do.exec(l)) && f[1] && (l = tr(i, s, f), m = "number"), l == null || l !== l)
              return;
            m === "number" && !_ && (l += f && f[3] || (d.cssNumber[E] ? "" : "px")), !x.clearCloneStyle && l === "" && s.indexOf("background") === 0 && (O[s] = "inherit"), (!b || !("set" in b) || (l = b.set(i, l, h)) !== void 0) && (_ ? O.setProperty(s, l) : O[s] = l);
          } else
            return b && "get" in b && (f = b.get(i, !1, h)) !== void 0 ? f : O[s];
        }
      },
      css: function(i, s, l, h) {
        var f, m, b, E = It(s), _ = Zn.test(s);
        return _ || (s = Kn(E)), b = d.cssHooks[s] || d.cssHooks[E], b && "get" in b && (f = b.get(i, !0, l)), f === void 0 && (f = Lo(i, s, h)), f === "normal" && s in Ai && (f = Ai[s]), l === "" || l ? (m = parseFloat(f), l === !0 || isFinite(m) ? m || 0 : f) : f;
      }
    }), d.each(["height", "width"], function(i, s) {
      d.cssHooks[s] = {
        get: function(l, h, f) {
          if (h)
            return Ms.test(d.css(l, "display")) && // Support: Safari 8+
            // Table columns in Safari have non-zero offsetWidth & zero
            // getBoundingClientRect().width unless display is changed.
            // Support: IE <=11 only
            // Running getBoundingClientRect on a disconnected node
            // in IE throws an error.
            (!l.getClientRects().length || !l.getBoundingClientRect().width) ? tn(l, mn, function() {
              return hr(l, s, f);
            }) : hr(l, s, f);
        },
        set: function(l, h, f) {
          var m, b = en(l), E = !x.scrollboxSize() && b.position === "absolute", _ = E || f, O = _ && d.css(l, "boxSizing", !1, b) === "border-box", W = f ? Xn(
            l,
            s,
            f,
            O,
            b
          ) : 0;
          return O && E && (W -= Math.ceil(
            l["offset" + s[0].toUpperCase() + s.slice(1)] - parseFloat(b[s]) - Xn(l, s, "border", !1, b) - 0.5
          )), W && (m = Do.exec(h)) && (m[3] || "px") !== "px" && (l.style[s] = h, h = d.css(l, s)), dr(l, h, W);
        }
      };
    }), d.cssHooks.marginLeft = sr(
      x.reliableMarginLeft,
      function(i, s) {
        if (s)
          return (parseFloat(Lo(i, "marginLeft")) || i.getBoundingClientRect().left - tn(i, { marginLeft: 0 }, function() {
            return i.getBoundingClientRect().left;
          })) + "px";
      }
    ), d.each({
      margin: "",
      padding: "",
      border: "Width"
    }, function(i, s) {
      d.cssHooks[i + s] = {
        expand: function(l) {
          for (var h = 0, f = {}, m = typeof l == "string" ? l.split(" ") : [l]; h < 4; h++)
            f[i + zt[h] + s] = m[h] || m[h - 2] || m[0];
          return f;
        }
      }, i !== "margin" && (d.cssHooks[i + s].set = dr);
    }), d.fn.extend({
      css: function(i, s) {
        return vt(this, function(l, h, f) {
          var m, b, E = {}, _ = 0;
          if (Array.isArray(h)) {
            for (m = en(l), b = h.length; _ < b; _++)
              E[h[_]] = d.css(l, h[_], !1, m);
            return E;
          }
          return f !== void 0 ? d.style(l, h, f) : d.css(l, h);
        }, i, s, arguments.length > 1);
      }
    });
    function mt(i, s, l, h, f) {
      return new mt.prototype.init(i, s, l, h, f);
    }
    d.Tween = mt, mt.prototype = {
      constructor: mt,
      init: function(i, s, l, h, f, m) {
        this.elem = i, this.prop = l, this.easing = f || d.easing._default, this.options = s, this.start = this.now = this.cur(), this.end = h, this.unit = m || (d.cssNumber[l] ? "" : "px");
      },
      cur: function() {
        var i = mt.propHooks[this.prop];
        return i && i.get ? i.get(this) : mt.propHooks._default.get(this);
      },
      run: function(i) {
        var s, l = mt.propHooks[this.prop];
        return this.options.duration ? this.pos = s = d.easing[this.easing](
          i,
          this.options.duration * i,
          0,
          1,
          this.options.duration
        ) : this.pos = s = i, this.now = (this.end - this.start) * s + this.start, this.options.step && this.options.step.call(this.elem, this.now, this), l && l.set ? l.set(this) : mt.propHooks._default.set(this), this;
      }
    }, mt.prototype.init.prototype = mt.prototype, mt.propHooks = {
      _default: {
        get: function(i) {
          var s;
          return i.elem.nodeType !== 1 || i.elem[i.prop] != null && i.elem.style[i.prop] == null ? i.elem[i.prop] : (s = d.css(i.elem, i.prop, ""), !s || s === "auto" ? 0 : s);
        },
        set: function(i) {
          d.fx.step[i.prop] ? d.fx.step[i.prop](i) : i.elem.nodeType === 1 && (d.cssHooks[i.prop] || i.elem.style[Kn(i.prop)] != null) ? d.style(i.elem, i.prop, i.now + i.unit) : i.elem[i.prop] = i.now;
        }
      }
    }, mt.propHooks.scrollTop = mt.propHooks.scrollLeft = {
      set: function(i) {
        i.elem.nodeType && i.elem.parentNode && (i.elem[i.prop] = i.now);
      }
    }, d.easing = {
      linear: function(i) {
        return i;
      },
      swing: function(i) {
        return 0.5 - Math.cos(i * Math.PI) / 2;
      },
      _default: "swing"
    }, d.fx = mt.prototype.init, d.fx.step = {};
    var To, gn, Ds = /^(?:toggle|show|hide)$/, Ls = /queueHooks$/;
    function Jn() {
      gn && (Y.hidden === !1 && e.requestAnimationFrame ? e.requestAnimationFrame(Jn) : e.setTimeout(Jn, d.fx.interval), d.fx.tick());
    }
    function Ri() {
      return e.setTimeout(function() {
        To = void 0;
      }), To = Date.now();
    }
    function bn(i, s) {
      var l, h = 0, f = { height: i };
      for (s = s ? 1 : 0; h < 4; h += 2 - s)
        l = zt[h], f["margin" + l] = f["padding" + l] = i;
      return s && (f.opacity = f.width = i), f;
    }
    function ur(i, s, l) {
      for (var h, f = (_t.tweeners[s] || []).concat(_t.tweeners["*"]), m = 0, b = f.length; m < b; m++)
        if (h = f[m].call(l, s, i))
          return h;
    }
    function Os(i, s, l) {
      var h, f, m, b, E, _, O, W, q = "width" in s || "height" in s, N = this, X = {}, ge = i.style, Ce = i.nodeType && pn(i), xe = ce.get(i, "fxshow");
      l.queue || (b = d._queueHooks(i, "fx"), b.unqueued == null && (b.unqueued = 0, E = b.empty.fire, b.empty.fire = function() {
        b.unqueued || E();
      }), b.unqueued++, N.always(function() {
        N.always(function() {
          b.unqueued--, d.queue(i, "fx").length || b.empty.fire();
        });
      }));
      for (h in s)
        if (f = s[h], Ds.test(f)) {
          if (delete s[h], m = m || f === "toggle", f === (Ce ? "hide" : "show"))
            if (f === "show" && xe && xe[h] !== void 0)
              Ce = !0;
            else
              continue;
          X[h] = xe && xe[h] || d.style(i, h);
        }
      if (_ = !d.isEmptyObject(s), !(!_ && d.isEmptyObject(X))) {
        q && i.nodeType === 1 && (l.overflow = [ge.overflow, ge.overflowX, ge.overflowY], O = xe && xe.display, O == null && (O = ce.get(i, "display")), W = d.css(i, "display"), W === "none" && (O ? W = O : (wo([i], !0), O = i.style.display || O, W = d.css(i, "display"), wo([i]))), (W === "inline" || W === "inline-block" && O != null) && d.css(i, "float") === "none" && (_ || (N.done(function() {
          ge.display = O;
        }), O == null && (W = ge.display, O = W === "none" ? "" : W)), ge.display = "inline-block")), l.overflow && (ge.overflow = "hidden", N.always(function() {
          ge.overflow = l.overflow[0], ge.overflowX = l.overflow[1], ge.overflowY = l.overflow[2];
        })), _ = !1;
        for (h in X)
          _ || (xe ? "hidden" in xe && (Ce = xe.hidden) : xe = ce.access(i, "fxshow", { display: O }), m && (xe.hidden = !Ce), Ce && wo([i], !0), N.done(function() {
            Ce || wo([i]), ce.remove(i, "fxshow");
            for (h in X)
              d.style(i, h, X[h]);
          })), _ = ur(Ce ? xe[h] : 0, h, N), h in xe || (xe[h] = _.start, Ce && (_.end = _.start, _.start = 0));
      }
    }
    function Rn(i, s) {
      var l, h, f, m, b;
      for (l in i)
        if (h = It(l), f = s[h], m = i[l], Array.isArray(m) && (f = m[1], m = i[l] = m[0]), l !== h && (i[h] = m, delete i[l]), b = d.cssHooks[h], b && "expand" in b) {
          m = b.expand(m), delete i[h];
          for (l in m)
            l in i || (i[l] = m[l], s[l] = f);
        } else
          s[h] = f;
    }
    function _t(i, s, l) {
      var h, f, m = 0, b = _t.prefilters.length, E = d.Deferred().always(function() {
        delete _.elem;
      }), _ = function() {
        if (f)
          return !1;
        for (var q = To || Ri(), N = Math.max(0, O.startTime + O.duration - q), X = N / O.duration || 0, ge = 1 - X, Ce = 0, xe = O.tweens.length; Ce < xe; Ce++)
          O.tweens[Ce].run(ge);
        return E.notifyWith(i, [O, ge, N]), ge < 1 && xe ? N : (xe || E.notifyWith(i, [O, 1, 0]), E.resolveWith(i, [O]), !1);
      }, O = E.promise({
        elem: i,
        props: d.extend({}, s),
        opts: d.extend(!0, {
          specialEasing: {},
          easing: d.easing._default
        }, l),
        originalProperties: s,
        originalOptions: l,
        startTime: To || Ri(),
        duration: l.duration,
        tweens: [],
        createTween: function(q, N) {
          var X = d.Tween(
            i,
            O.opts,
            q,
            N,
            O.opts.specialEasing[q] || O.opts.easing
          );
          return O.tweens.push(X), X;
        },
        stop: function(q) {
          var N = 0, X = q ? O.tweens.length : 0;
          if (f)
            return this;
          for (f = !0; N < X; N++)
            O.tweens[N].run(1);
          return q ? (E.notifyWith(i, [O, 1, 0]), E.resolveWith(i, [O, q])) : E.rejectWith(i, [O, q]), this;
        }
      }), W = O.props;
      for (Rn(W, O.opts.specialEasing); m < b; m++)
        if (h = _t.prefilters[m].call(O, i, W, O.opts), h)
          return M(h.stop) && (d._queueHooks(O.elem, O.opts.queue).stop = h.stop.bind(h)), h;
      return d.map(W, ur, O), M(O.opts.start) && O.opts.start.call(i, O), O.progress(O.opts.progress).done(O.opts.done, O.opts.complete).fail(O.opts.fail).always(O.opts.always), d.fx.timer(
        d.extend(_, {
          elem: i,
          anim: O,
          queue: O.opts.queue
        })
      ), O;
    }
    d.Animation = d.extend(_t, {
      tweeners: {
        "*": [function(i, s) {
          var l = this.createTween(i, s);
          return tr(l.elem, i, Do.exec(s), l), l;
        }]
      },
      tweener: function(i, s) {
        M(i) ? (s = i, i = ["*"]) : i = i.match(Rt);
        for (var l, h = 0, f = i.length; h < f; h++)
          l = i[h], _t.tweeners[l] = _t.tweeners[l] || [], _t.tweeners[l].unshift(s);
      },
      prefilters: [Os],
      prefilter: function(i, s) {
        s ? _t.prefilters.unshift(i) : _t.prefilters.push(i);
      }
    }), d.speed = function(i, s, l) {
      var h = i && typeof i == "object" ? d.extend({}, i) : {
        complete: l || !l && s || M(i) && i,
        duration: i,
        easing: l && s || s && !M(s) && s
      };
      return d.fx.off ? h.duration = 0 : typeof h.duration != "number" && (h.duration in d.fx.speeds ? h.duration = d.fx.speeds[h.duration] : h.duration = d.fx.speeds._default), (h.queue == null || h.queue === !0) && (h.queue = "fx"), h.old = h.complete, h.complete = function() {
        M(h.old) && h.old.call(this), h.queue && d.dequeue(this, h.queue);
      }, h;
    }, d.fn.extend({
      fadeTo: function(i, s, l, h) {
        return this.filter(pn).css("opacity", 0).show().end().animate({ opacity: s }, i, l, h);
      },
      animate: function(i, s, l, h) {
        var f = d.isEmptyObject(i), m = d.speed(s, l, h), b = function() {
          var E = _t(this, d.extend({}, i), m);
          (f || ce.get(this, "finish")) && E.stop(!0);
        };
        return b.finish = b, f || m.queue === !1 ? this.each(b) : this.queue(m.queue, b);
      },
      stop: function(i, s, l) {
        var h = function(f) {
          var m = f.stop;
          delete f.stop, m(l);
        };
        return typeof i != "string" && (l = s, s = i, i = void 0), s && this.queue(i || "fx", []), this.each(function() {
          var f = !0, m = i != null && i + "queueHooks", b = d.timers, E = ce.get(this);
          if (m)
            E[m] && E[m].stop && h(E[m]);
          else
            for (m in E)
              E[m] && E[m].stop && Ls.test(m) && h(E[m]);
          for (m = b.length; m--; )
            b[m].elem === this && (i == null || b[m].queue === i) && (b[m].anim.stop(l), f = !1, b.splice(m, 1));
          (f || !l) && d.dequeue(this, i);
        });
      },
      finish: function(i) {
        return i !== !1 && (i = i || "fx"), this.each(function() {
          var s, l = ce.get(this), h = l[i + "queue"], f = l[i + "queueHooks"], m = d.timers, b = h ? h.length : 0;
          for (l.finish = !0, d.queue(this, i, []), f && f.stop && f.stop.call(this, !0), s = m.length; s--; )
            m[s].elem === this && m[s].queue === i && (m[s].anim.stop(!0), m.splice(s, 1));
          for (s = 0; s < b; s++)
            h[s] && h[s].finish && h[s].finish.call(this);
          delete l.finish;
        });
      }
    }), d.each(["toggle", "show", "hide"], function(i, s) {
      var l = d.fn[s];
      d.fn[s] = function(h, f, m) {
        return h == null || typeof h == "boolean" ? l.apply(this, arguments) : this.animate(bn(s, !0), h, f, m);
      };
    }), d.each({
      slideDown: bn("show"),
      slideUp: bn("hide"),
      slideToggle: bn("toggle"),
      fadeIn: { opacity: "show" },
      fadeOut: { opacity: "hide" },
      fadeToggle: { opacity: "toggle" }
    }, function(i, s) {
      d.fn[i] = function(l, h, f) {
        return this.animate(s, l, h, f);
      };
    }), d.timers = [], d.fx.tick = function() {
      var i, s = 0, l = d.timers;
      for (To = Date.now(); s < l.length; s++)
        i = l[s], !i() && l[s] === i && l.splice(s--, 1);
      l.length || d.fx.stop(), To = void 0;
    }, d.fx.timer = function(i) {
      d.timers.push(i), d.fx.start();
    }, d.fx.interval = 13, d.fx.start = function() {
      gn || (gn = !0, Jn());
    }, d.fx.stop = function() {
      gn = null;
    }, d.fx.speeds = {
      slow: 600,
      fast: 200,
      // Default speed
      _default: 400
    }, d.fn.delay = function(i, s) {
      return i = d.fx && d.fx.speeds[i] || i, s = s || "fx", this.queue(s, function(l, h) {
        var f = e.setTimeout(l, i);
        h.stop = function() {
          e.clearTimeout(f);
        };
      });
    }, function() {
      var i = Y.createElement("input"), s = Y.createElement("select"), l = s.appendChild(Y.createElement("option"));
      i.type = "checkbox", x.checkOn = i.value !== "", x.optSelected = l.selected, i = Y.createElement("input"), i.value = "t", i.type = "radio", x.radioValue = i.value === "t";
    }();
    var pr, Yo = d.expr.attrHandle;
    d.fn.extend({
      attr: function(i, s) {
        return vt(this, d.attr, i, s, arguments.length > 1);
      },
      removeAttr: function(i) {
        return this.each(function() {
          d.removeAttr(this, i);
        });
      }
    }), d.extend({
      attr: function(i, s, l) {
        var h, f, m = i.nodeType;
        if (!(m === 3 || m === 8 || m === 2)) {
          if (typeof i.getAttribute > "u")
            return d.prop(i, s, l);
          if ((m !== 1 || !d.isXMLDoc(i)) && (f = d.attrHooks[s.toLowerCase()] || (d.expr.match.bool.test(s) ? pr : void 0)), l !== void 0) {
            if (l === null) {
              d.removeAttr(i, s);
              return;
            }
            return f && "set" in f && (h = f.set(i, l, s)) !== void 0 ? h : (i.setAttribute(s, l + ""), l);
          }
          return f && "get" in f && (h = f.get(i, s)) !== null ? h : (h = d.find.attr(i, s), h ?? void 0);
        }
      },
      attrHooks: {
        type: {
          set: function(i, s) {
            if (!x.radioValue && s === "radio" && R(i, "input")) {
              var l = i.value;
              return i.setAttribute("type", s), l && (i.value = l), s;
            }
          }
        }
      },
      removeAttr: function(i, s) {
        var l, h = 0, f = s && s.match(Rt);
        if (f && i.nodeType === 1)
          for (; l = f[h++]; )
            i.removeAttribute(l);
      }
    }), pr = {
      set: function(i, s, l) {
        return s === !1 ? d.removeAttr(i, l) : i.setAttribute(l, l), l;
      }
    }, d.each(d.expr.match.bool.source.match(/\w+/g), function(i, s) {
      var l = Yo[s] || d.find.attr;
      Yo[s] = function(h, f, m) {
        var b, E, _ = f.toLowerCase();
        return m || (E = Yo[_], Yo[_] = b, b = l(h, f, m) != null ? _ : null, Yo[_] = E), b;
      };
    });
    var Bs = /^(?:input|select|textarea|button)$/i, As = /^(?:a|area)$/i;
    d.fn.extend({
      prop: function(i, s) {
        return vt(this, d.prop, i, s, arguments.length > 1);
      },
      removeProp: function(i) {
        return this.each(function() {
          delete this[d.propFix[i] || i];
        });
      }
    }), d.extend({
      prop: function(i, s, l) {
        var h, f, m = i.nodeType;
        if (!(m === 3 || m === 8 || m === 2))
          return (m !== 1 || !d.isXMLDoc(i)) && (s = d.propFix[s] || s, f = d.propHooks[s]), l !== void 0 ? f && "set" in f && (h = f.set(i, l, s)) !== void 0 ? h : i[s] = l : f && "get" in f && (h = f.get(i, s)) !== null ? h : i[s];
      },
      propHooks: {
        tabIndex: {
          get: function(i) {
            var s = d.find.attr(i, "tabindex");
            return s ? parseInt(s, 10) : Bs.test(i.nodeName) || As.test(i.nodeName) && i.href ? 0 : -1;
          }
        }
      },
      propFix: {
        for: "htmlFor",
        class: "className"
      }
    }), x.optSelected || (d.propHooks.selected = {
      get: function(i) {
        var s = i.parentNode;
        return s && s.parentNode && s.parentNode.selectedIndex, null;
      },
      set: function(i) {
        var s = i.parentNode;
        s && (s.selectedIndex, s.parentNode && s.parentNode.selectedIndex);
      }
    }), d.each([
      "tabIndex",
      "readOnly",
      "maxLength",
      "cellSpacing",
      "cellPadding",
      "rowSpan",
      "colSpan",
      "useMap",
      "frameBorder",
      "contentEditable"
    ], function() {
      d.propFix[this.toLowerCase()] = this;
    });
    function ro(i) {
      var s = i.match(Rt) || [];
      return s.join(" ");
    }
    function so(i) {
      return i.getAttribute && i.getAttribute("class") || "";
    }
    function vn(i) {
      return Array.isArray(i) ? i : typeof i == "string" ? i.match(Rt) || [] : [];
    }
    d.fn.extend({
      addClass: function(i) {
        var s, l, h, f, m, b;
        return M(i) ? this.each(function(E) {
          d(this).addClass(i.call(this, E, so(this)));
        }) : (s = vn(i), s.length ? this.each(function() {
          if (h = so(this), l = this.nodeType === 1 && " " + ro(h) + " ", l) {
            for (m = 0; m < s.length; m++)
              f = s[m], l.indexOf(" " + f + " ") < 0 && (l += f + " ");
            b = ro(l), h !== b && this.setAttribute("class", b);
          }
        }) : this);
      },
      removeClass: function(i) {
        var s, l, h, f, m, b;
        return M(i) ? this.each(function(E) {
          d(this).removeClass(i.call(this, E, so(this)));
        }) : arguments.length ? (s = vn(i), s.length ? this.each(function() {
          if (h = so(this), l = this.nodeType === 1 && " " + ro(h) + " ", l) {
            for (m = 0; m < s.length; m++)
              for (f = s[m]; l.indexOf(" " + f + " ") > -1; )
                l = l.replace(" " + f + " ", " ");
            b = ro(l), h !== b && this.setAttribute("class", b);
          }
        }) : this) : this.attr("class", "");
      },
      toggleClass: function(i, s) {
        var l, h, f, m, b = typeof i, E = b === "string" || Array.isArray(i);
        return M(i) ? this.each(function(_) {
          d(this).toggleClass(
            i.call(this, _, so(this), s),
            s
          );
        }) : typeof s == "boolean" && E ? s ? this.addClass(i) : this.removeClass(i) : (l = vn(i), this.each(function() {
          if (E)
            for (m = d(this), f = 0; f < l.length; f++)
              h = l[f], m.hasClass(h) ? m.removeClass(h) : m.addClass(h);
          else
            (i === void 0 || b === "boolean") && (h = so(this), h && ce.set(this, "__className__", h), this.setAttribute && this.setAttribute(
              "class",
              h || i === !1 ? "" : ce.get(this, "__className__") || ""
            ));
        }));
      },
      hasClass: function(i) {
        var s, l, h = 0;
        for (s = " " + i + " "; l = this[h++]; )
          if (l.nodeType === 1 && (" " + ro(so(l)) + " ").indexOf(s) > -1)
            return !0;
        return !1;
      }
    });
    var Fr = /\r/g;
    d.fn.extend({
      val: function(i) {
        var s, l, h, f = this[0];
        return arguments.length ? (h = M(i), this.each(function(m) {
          var b;
          this.nodeType === 1 && (h ? b = i.call(this, m, d(this).val()) : b = i, b == null ? b = "" : typeof b == "number" ? b += "" : Array.isArray(b) && (b = d.map(b, function(E) {
            return E == null ? "" : E + "";
          })), s = d.valHooks[this.type] || d.valHooks[this.nodeName.toLowerCase()], (!s || !("set" in s) || s.set(this, b, "value") === void 0) && (this.value = b));
        })) : f ? (s = d.valHooks[f.type] || d.valHooks[f.nodeName.toLowerCase()], s && "get" in s && (l = s.get(f, "value")) !== void 0 ? l : (l = f.value, typeof l == "string" ? l.replace(Fr, "") : l ?? "")) : void 0;
      }
    }), d.extend({
      valHooks: {
        option: {
          get: function(i) {
            var s = d.find.attr(i, "value");
            return s ?? // Support: IE <=10 - 11 only
            // option.text throws exceptions (trac-14686, trac-14858)
            // Strip and collapse whitespace
            // https://html.spec.whatwg.org/#strip-and-collapse-whitespace
            ro(d.text(i));
          }
        },
        select: {
          get: function(i) {
            var s, l, h, f = i.options, m = i.selectedIndex, b = i.type === "select-one", E = b ? null : [], _ = b ? m + 1 : f.length;
            for (m < 0 ? h = _ : h = b ? m : 0; h < _; h++)
              if (l = f[h], (l.selected || h === m) && // Don't return options that are disabled or in a disabled optgroup
              !l.disabled && (!l.parentNode.disabled || !R(l.parentNode, "optgroup"))) {
                if (s = d(l).val(), b)
                  return s;
                E.push(s);
              }
            return E;
          },
          set: function(i, s) {
            for (var l, h, f = i.options, m = d.makeArray(s), b = f.length; b--; )
              h = f[b], (h.selected = d.inArray(d.valHooks.option.get(h), m) > -1) && (l = !0);
            return l || (i.selectedIndex = -1), m;
          }
        }
      }
    }), d.each(["radio", "checkbox"], function() {
      d.valHooks[this] = {
        set: function(i, s) {
          if (Array.isArray(s))
            return i.checked = d.inArray(d(i).val(), s) > -1;
        }
      }, x.checkOn || (d.valHooks[this].get = function(i) {
        return i.getAttribute("value") === null ? "on" : i.value;
      });
    });
    var Oo = e.location, fr = { guid: Date.now() }, Fo = /\?/;
    d.parseXML = function(i) {
      var s, l;
      if (!i || typeof i != "string")
        return null;
      try {
        s = new e.DOMParser().parseFromString(i, "text/xml");
      } catch {
      }
      return l = s && s.getElementsByTagName("parsererror")[0], (!s || l) && d.error("Invalid XML: " + (l ? d.map(l.childNodes, function(h) {
        return h.textContent;
      }).join(`
`) : i)), s;
    };
    var Ii = /^(?:focusinfocus|focusoutblur)$/, Ni = function(i) {
      i.stopPropagation();
    };
    d.extend(d.event, {
      trigger: function(i, s, l, h) {
        var f, m, b, E, _, O, W, q, N = [l || Y], X = k.call(i, "type") ? i.type : i, ge = k.call(i, "namespace") ? i.namespace.split(".") : [];
        if (m = q = b = l = l || Y, !(l.nodeType === 3 || l.nodeType === 8) && !Ii.test(X + d.event.triggered) && (X.indexOf(".") > -1 && (ge = X.split("."), X = ge.shift(), ge.sort()), _ = X.indexOf(":") < 0 && "on" + X, i = i[d.expando] ? i : new d.Event(X, typeof i == "object" && i), i.isTrigger = h ? 2 : 3, i.namespace = ge.join("."), i.rnamespace = i.namespace ? new RegExp("(^|\\.)" + ge.join("\\.(?:.*\\.|)") + "(\\.|$)") : null, i.result = void 0, i.target || (i.target = l), s = s == null ? [i] : d.makeArray(s, [i]), W = d.event.special[X] || {}, !(!h && W.trigger && W.trigger.apply(l, s) === !1))) {
          if (!h && !W.noBubble && !L(l)) {
            for (E = W.delegateType || X, Ii.test(E + X) || (m = m.parentNode); m; m = m.parentNode)
              N.push(m), b = m;
            b === (l.ownerDocument || Y) && N.push(b.defaultView || b.parentWindow || e);
          }
          for (f = 0; (m = N[f++]) && !i.isPropagationStopped(); )
            q = m, i.type = f > 1 ? E : W.bindType || X, O = (ce.get(m, "events") || /* @__PURE__ */ Object.create(null))[i.type] && ce.get(m, "handle"), O && O.apply(m, s), O = _ && m[_], O && O.apply && We(m) && (i.result = O.apply(m, s), i.result === !1 && i.preventDefault());
          return i.type = X, !h && !i.isDefaultPrevented() && (!W._default || W._default.apply(N.pop(), s) === !1) && We(l) && _ && M(l[X]) && !L(l) && (b = l[_], b && (l[_] = null), d.event.triggered = X, i.isPropagationStopped() && q.addEventListener(X, Ni), l[X](), i.isPropagationStopped() && q.removeEventListener(X, Ni), d.event.triggered = void 0, b && (l[_] = b)), i.result;
        }
      },
      // Piggyback on a donor event to simulate a different one
      // Used only for `focus(in | out)` events
      simulate: function(i, s, l) {
        var h = d.extend(
          new d.Event(),
          l,
          {
            type: i,
            isSimulated: !0
          }
        );
        d.event.trigger(h, null, s);
      }
    }), d.fn.extend({
      trigger: function(i, s) {
        return this.each(function() {
          d.event.trigger(i, s, this);
        });
      },
      triggerHandler: function(i, s) {
        var l = this[0];
        if (l)
          return d.event.trigger(i, s, l, !0);
      }
    });
    var mr = /\[\]$/, Pi = /\r?\n/g, Rs = /^(?:submit|button|image|reset|file)$/i, Is = /^(?:input|select|textarea|keygen)/i;
    function Qn(i, s, l, h) {
      var f;
      if (Array.isArray(s))
        d.each(s, function(m, b) {
          l || mr.test(i) ? h(i, b) : Qn(
            i + "[" + (typeof b == "object" && b != null ? m : "") + "]",
            b,
            l,
            h
          );
        });
      else if (!l && Q(s) === "object")
        for (f in s)
          Qn(i + "[" + f + "]", s[f], l, h);
      else
        h(i, s);
    }
    d.param = function(i, s) {
      var l, h = [], f = function(m, b) {
        var E = M(b) ? b() : b;
        h[h.length] = encodeURIComponent(m) + "=" + encodeURIComponent(E ?? "");
      };
      if (i == null)
        return "";
      if (Array.isArray(i) || i.jquery && !d.isPlainObject(i))
        d.each(i, function() {
          f(this.name, this.value);
        });
      else
        for (l in i)
          Qn(l, i[l], s, f);
      return h.join("&");
    }, d.fn.extend({
      serialize: function() {
        return d.param(this.serializeArray());
      },
      serializeArray: function() {
        return this.map(function() {
          var i = d.prop(this, "elements");
          return i ? d.makeArray(i) : this;
        }).filter(function() {
          var i = this.type;
          return this.name && !d(this).is(":disabled") && Is.test(this.nodeName) && !Rs.test(i) && (this.checked || !ko.test(i));
        }).map(function(i, s) {
          var l = d(this).val();
          return l == null ? null : Array.isArray(l) ? d.map(l, function(h) {
            return { name: s.name, value: h.replace(Pi, `\r
`) };
          }) : { name: s.name, value: l.replace(Pi, `\r
`) };
        }).get();
      }
    });
    var Ns = /%20/g, Ps = /#.*$/, Hs = /([?&])_=[^&]*/, zr = /^(.*?):[ \t]*([^\r\n]*)$/mg, Hi = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/, js = /^(?:GET|HEAD)$/, ei = /^\/\//, gr = {}, ti = {}, br = "*/".concat("*"), In = Y.createElement("a");
    In.href = Oo.href;
    function ji(i) {
      return function(s, l) {
        typeof s != "string" && (l = s, s = "*");
        var h, f = 0, m = s.toLowerCase().match(Rt) || [];
        if (M(l))
          for (; h = m[f++]; )
            h[0] === "+" ? (h = h.slice(1) || "*", (i[h] = i[h] || []).unshift(l)) : (i[h] = i[h] || []).push(l);
      };
    }
    function $i(i, s, l, h) {
      var f = {}, m = i === ti;
      function b(E) {
        var _;
        return f[E] = !0, d.each(i[E] || [], function(O, W) {
          var q = W(s, l, h);
          if (typeof q == "string" && !m && !f[q])
            return s.dataTypes.unshift(q), b(q), !1;
          if (m)
            return !(_ = q);
        }), _;
      }
      return b(s.dataTypes[0]) || !f["*"] && b("*");
    }
    function oi(i, s) {
      var l, h, f = d.ajaxSettings.flatOptions || {};
      for (l in s)
        s[l] !== void 0 && ((f[l] ? i : h || (h = {}))[l] = s[l]);
      return h && d.extend(!0, i, h), i;
    }
    function $s(i, s, l) {
      for (var h, f, m, b, E = i.contents, _ = i.dataTypes; _[0] === "*"; )
        _.shift(), h === void 0 && (h = i.mimeType || s.getResponseHeader("Content-Type"));
      if (h) {
        for (f in E)
          if (E[f] && E[f].test(h)) {
            _.unshift(f);
            break;
          }
      }
      if (_[0] in l)
        m = _[0];
      else {
        for (f in l) {
          if (!_[0] || i.converters[f + " " + _[0]]) {
            m = f;
            break;
          }
          b || (b = f);
        }
        m = m || b;
      }
      if (m)
        return m !== _[0] && _.unshift(m), l[m];
    }
    function Wr(i, s, l, h) {
      var f, m, b, E, _, O = {}, W = i.dataTypes.slice();
      if (W[1])
        for (b in i.converters)
          O[b.toLowerCase()] = i.converters[b];
      for (m = W.shift(); m; )
        if (i.responseFields[m] && (l[i.responseFields[m]] = s), !_ && h && i.dataFilter && (s = i.dataFilter(s, i.dataType)), _ = m, m = W.shift(), m) {
          if (m === "*")
            m = _;
          else if (_ !== "*" && _ !== m) {
            if (b = O[_ + " " + m] || O["* " + m], !b) {
              for (f in O)
                if (E = f.split(" "), E[1] === m && (b = O[_ + " " + E[0]] || O["* " + E[0]], b)) {
                  b === !0 ? b = O[f] : O[f] !== !0 && (m = E[0], W.unshift(E[1]));
                  break;
                }
            }
            if (b !== !0)
              if (b && i.throws)
                s = b(s);
              else
                try {
                  s = b(s);
                } catch (q) {
                  return {
                    state: "parsererror",
                    error: b ? q : "No conversion from " + _ + " to " + m
                  };
                }
          }
        }
      return { state: "success", data: s };
    }
    d.extend({
      // Counter for holding the number of active queries
      active: 0,
      // Last-Modified header cache for next request
      lastModified: {},
      etag: {},
      ajaxSettings: {
        url: Oo.href,
        type: "GET",
        isLocal: Hi.test(Oo.protocol),
        global: !0,
        processData: !0,
        async: !0,
        contentType: "application/x-www-form-urlencoded; charset=UTF-8",
        /*
        timeout: 0,
        data: null,
        dataType: null,
        username: null,
        password: null,
        cache: null,
        throws: false,
        traditional: false,
        headers: {},
        */
        accepts: {
          "*": br,
          text: "text/plain",
          html: "text/html",
          xml: "application/xml, text/xml",
          json: "application/json, text/javascript"
        },
        contents: {
          xml: /\bxml\b/,
          html: /\bhtml/,
          json: /\bjson\b/
        },
        responseFields: {
          xml: "responseXML",
          text: "responseText",
          json: "responseJSON"
        },
        // Data converters
        // Keys separate source (or catchall "*") and destination types with a single space
        converters: {
          // Convert anything to text
          "* text": String,
          // Text to html (true = no transformation)
          "text html": !0,
          // Evaluate text as a json expression
          "text json": JSON.parse,
          // Parse text as xml
          "text xml": d.parseXML
        },
        // For options that shouldn't be deep extended:
        // you can add your own custom options here if
        // and when you create one that shouldn't be
        // deep extended (see ajaxExtend)
        flatOptions: {
          url: !0,
          context: !0
        }
      },
      // Creates a full fledged settings object into target
      // with both ajaxSettings and settings fields.
      // If target is omitted, writes into ajaxSettings.
      ajaxSetup: function(i, s) {
        return s ? (
          // Building a settings object
          oi(oi(i, d.ajaxSettings), s)
        ) : (
          // Extending ajaxSettings
          oi(d.ajaxSettings, i)
        );
      },
      ajaxPrefilter: ji(gr),
      ajaxTransport: ji(ti),
      // Main method
      ajax: function(i, s) {
        typeof i == "object" && (s = i, i = void 0), s = s || {};
        var l, h, f, m, b, E, _, O, W, q, N = d.ajaxSetup({}, s), X = N.context || N, ge = N.context && (X.nodeType || X.jquery) ? d(X) : d.event, Ce = d.Deferred(), xe = d.Callbacks("once memory"), Qe = N.statusCode || {}, Ve = {}, Ht = {}, jt = "canceled", Se = {
          readyState: 0,
          // Builds headers hashtable if needed
          getResponseHeader: function(De) {
            var w;
            if (_) {
              if (!m)
                for (m = {}; w = zr.exec(f); )
                  m[w[1].toLowerCase() + " "] = (m[w[1].toLowerCase() + " "] || []).concat(w[2]);
              w = m[De.toLowerCase() + " "];
            }
            return w == null ? null : w.join(", ");
          },
          // Raw string
          getAllResponseHeaders: function() {
            return _ ? f : null;
          },
          // Caches the header
          setRequestHeader: function(De, w) {
            return _ == null && (De = Ht[De.toLowerCase()] = Ht[De.toLowerCase()] || De, Ve[De] = w), this;
          },
          // Overrides response content-type header
          overrideMimeType: function(De) {
            return _ == null && (N.mimeType = De), this;
          },
          // Status-dependent callbacks
          statusCode: function(De) {
            var w;
            if (De)
              if (_)
                Se.always(De[Se.status]);
              else
                for (w in De)
                  Qe[w] = [Qe[w], De[w]];
            return this;
          },
          // Cancel the request
          abort: function(De) {
            var w = De || jt;
            return l && l.abort(w), ao(0, w), this;
          }
        };
        if (Ce.promise(Se), N.url = ((i || N.url || Oo.href) + "").replace(ei, Oo.protocol + "//"), N.type = s.method || s.type || N.method || N.type, N.dataTypes = (N.dataType || "*").toLowerCase().match(Rt) || [""], N.crossDomain == null) {
          E = Y.createElement("a");
          try {
            E.href = N.url, E.href = E.href, N.crossDomain = In.protocol + "//" + In.host != E.protocol + "//" + E.host;
          } catch {
            N.crossDomain = !0;
          }
        }
        if (N.data && N.processData && typeof N.data != "string" && (N.data = d.param(N.data, N.traditional)), $i(gr, N, s, Se), _)
          return Se;
        O = d.event && N.global, O && d.active++ === 0 && d.event.trigger("ajaxStart"), N.type = N.type.toUpperCase(), N.hasContent = !js.test(N.type), h = N.url.replace(Ps, ""), N.hasContent ? N.data && N.processData && (N.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && (N.data = N.data.replace(Ns, "+")) : (q = N.url.slice(h.length), N.data && (N.processData || typeof N.data == "string") && (h += (Fo.test(h) ? "&" : "?") + N.data, delete N.data), N.cache === !1 && (h = h.replace(Hs, "$1"), q = (Fo.test(h) ? "&" : "?") + "_=" + fr.guid++ + q), N.url = h + q), N.ifModified && (d.lastModified[h] && Se.setRequestHeader("If-Modified-Since", d.lastModified[h]), d.etag[h] && Se.setRequestHeader("If-None-Match", d.etag[h])), (N.data && N.hasContent && N.contentType !== !1 || s.contentType) && Se.setRequestHeader("Content-Type", N.contentType), Se.setRequestHeader(
          "Accept",
          N.dataTypes[0] && N.accepts[N.dataTypes[0]] ? N.accepts[N.dataTypes[0]] + (N.dataTypes[0] !== "*" ? ", " + br + "; q=0.01" : "") : N.accepts["*"]
        );
        for (W in N.headers)
          Se.setRequestHeader(W, N.headers[W]);
        if (N.beforeSend && (N.beforeSend.call(X, Se, N) === !1 || _))
          return Se.abort();
        if (jt = "abort", xe.add(N.complete), Se.done(N.success), Se.fail(N.error), l = $i(ti, N, s, Se), !l)
          ao(-1, "No Transport");
        else {
          if (Se.readyState = 1, O && ge.trigger("ajaxSend", [Se, N]), _)
            return Se;
          N.async && N.timeout > 0 && (b = e.setTimeout(function() {
            Se.abort("timeout");
          }, N.timeout));
          try {
            _ = !1, l.send(Ve, ao);
          } catch (De) {
            if (_)
              throw De;
            ao(-1, De);
          }
        }
        function ao(De, w, S, B) {
          var H, ee, he, ke, Ee, Be = w;
          _ || (_ = !0, b && e.clearTimeout(b), l = void 0, f = B || "", Se.readyState = De > 0 ? 4 : 0, H = De >= 200 && De < 300 || De === 304, S && (ke = $s(N, Se, S)), !H && d.inArray("script", N.dataTypes) > -1 && d.inArray("json", N.dataTypes) < 0 && (N.converters["text script"] = function() {
          }), ke = Wr(N, ke, Se, H), H ? (N.ifModified && (Ee = Se.getResponseHeader("Last-Modified"), Ee && (d.lastModified[h] = Ee), Ee = Se.getResponseHeader("etag"), Ee && (d.etag[h] = Ee)), De === 204 || N.type === "HEAD" ? Be = "nocontent" : De === 304 ? Be = "notmodified" : (Be = ke.state, ee = ke.data, he = ke.error, H = !he)) : (he = Be, (De || !Be) && (Be = "error", De < 0 && (De = 0))), Se.status = De, Se.statusText = (w || Be) + "", H ? Ce.resolveWith(X, [ee, Be, Se]) : Ce.rejectWith(X, [Se, Be, he]), Se.statusCode(Qe), Qe = void 0, O && ge.trigger(
            H ? "ajaxSuccess" : "ajaxError",
            [Se, N, H ? ee : he]
          ), xe.fireWith(X, [Se, Be]), O && (ge.trigger("ajaxComplete", [Se, N]), --d.active || d.event.trigger("ajaxStop")));
        }
        return Se;
      },
      getJSON: function(i, s, l) {
        return d.get(i, s, l, "json");
      },
      getScript: function(i, s) {
        return d.get(i, void 0, s, "script");
      }
    }), d.each(["get", "post"], function(i, s) {
      d[s] = function(l, h, f, m) {
        return M(h) && (m = m || f, f = h, h = void 0), d.ajax(d.extend({
          url: l,
          type: s,
          dataType: m,
          data: h,
          success: f
        }, d.isPlainObject(l) && l));
      };
    }), d.ajaxPrefilter(function(i) {
      var s;
      for (s in i.headers)
        s.toLowerCase() === "content-type" && (i.contentType = i.headers[s] || "");
    }), d._evalUrl = function(i, s, l) {
      return d.ajax({
        url: i,
        // Make this explicit, since user can override this through ajaxSetup (trac-11264)
        type: "GET",
        dataType: "script",
        cache: !0,
        async: !1,
        global: !1,
        // Only evaluate the response if it is successful (gh-4126)
        // dataFilter is not invoked for failure responses, so using it instead
        // of the default converter is kludgy but it works.
        converters: {
          "text script": function() {
          }
        },
        dataFilter: function(h) {
          d.globalEval(h, s, l);
        }
      });
    }, d.fn.extend({
      wrapAll: function(i) {
        var s;
        return this[0] && (M(i) && (i = i.call(this[0])), s = d(i, this[0].ownerDocument).eq(0).clone(!0), this[0].parentNode && s.insertBefore(this[0]), s.map(function() {
          for (var l = this; l.firstElementChild; )
            l = l.firstElementChild;
          return l;
        }).append(this)), this;
      },
      wrapInner: function(i) {
        return M(i) ? this.each(function(s) {
          d(this).wrapInner(i.call(this, s));
        }) : this.each(function() {
          var s = d(this), l = s.contents();
          l.length ? l.wrapAll(i) : s.append(i);
        });
      },
      wrap: function(i) {
        var s = M(i);
        return this.each(function(l) {
          d(this).wrapAll(s ? i.call(this, l) : i);
        });
      },
      unwrap: function(i) {
        return this.parent(i).not("body").each(function() {
          d(this).replaceWith(this.childNodes);
        }), this;
      }
    }), d.expr.pseudos.hidden = function(i) {
      return !d.expr.pseudos.visible(i);
    }, d.expr.pseudos.visible = function(i) {
      return !!(i.offsetWidth || i.offsetHeight || i.getClientRects().length);
    }, d.ajaxSettings.xhr = function() {
      try {
        return new e.XMLHttpRequest();
      } catch {
      }
    };
    var vr = {
      // File protocol always yields status code 0, assume 200
      0: 200,
      // Support: IE <=9 only
      // trac-1450: sometimes IE returns 1223 when it should be 204
      1223: 204
    }, Bo = d.ajaxSettings.xhr();
    x.cors = !!Bo && "withCredentials" in Bo, x.ajax = Bo = !!Bo, d.ajaxTransport(function(i) {
      var s, l;
      if (x.cors || Bo && !i.crossDomain)
        return {
          send: function(h, f) {
            var m, b = i.xhr();
            if (b.open(
              i.type,
              i.url,
              i.async,
              i.username,
              i.password
            ), i.xhrFields)
              for (m in i.xhrFields)
                b[m] = i.xhrFields[m];
            i.mimeType && b.overrideMimeType && b.overrideMimeType(i.mimeType), !i.crossDomain && !h["X-Requested-With"] && (h["X-Requested-With"] = "XMLHttpRequest");
            for (m in h)
              b.setRequestHeader(m, h[m]);
            s = function(E) {
              return function() {
                s && (s = l = b.onload = b.onerror = b.onabort = b.ontimeout = b.onreadystatechange = null, E === "abort" ? b.abort() : E === "error" ? typeof b.status != "number" ? f(0, "error") : f(
                  // File: protocol always yields status 0; see trac-8605, trac-14207
                  b.status,
                  b.statusText
                ) : f(
                  vr[b.status] || b.status,
                  b.statusText,
                  // Support: IE <=9 only
                  // IE9 has no XHR2 but throws on binary (trac-11426)
                  // For XHR2 non-text, let the caller handle it (gh-2498)
                  (b.responseType || "text") !== "text" || typeof b.responseText != "string" ? { binary: b.response } : { text: b.responseText },
                  b.getAllResponseHeaders()
                ));
              };
            }, b.onload = s(), l = b.onerror = b.ontimeout = s("error"), b.onabort !== void 0 ? b.onabort = l : b.onreadystatechange = function() {
              b.readyState === 4 && e.setTimeout(function() {
                s && l();
              });
            }, s = s("abort");
            try {
              b.send(i.hasContent && i.data || null);
            } catch (E) {
              if (s)
                throw E;
            }
          },
          abort: function() {
            s && s();
          }
        };
    }), d.ajaxPrefilter(function(i) {
      i.crossDomain && (i.contents.script = !1);
    }), d.ajaxSetup({
      accepts: {
        script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
      },
      contents: {
        script: /\b(?:java|ecma)script\b/
      },
      converters: {
        "text script": function(i) {
          return d.globalEval(i), i;
        }
      }
    }), d.ajaxPrefilter("script", function(i) {
      i.cache === void 0 && (i.cache = !1), i.crossDomain && (i.type = "GET");
    }), d.ajaxTransport("script", function(i) {
      if (i.crossDomain || i.scriptAttrs) {
        var s, l;
        return {
          send: function(h, f) {
            s = d("<script>").attr(i.scriptAttrs || {}).prop({ charset: i.scriptCharset, src: i.url }).on("load error", l = function(m) {
              s.remove(), l = null, m && f(m.type === "error" ? 404 : 200, m.type);
            }), Y.head.appendChild(s[0]);
          },
          abort: function() {
            l && l();
          }
        };
      }
    });
    var Yi = [], ni = /(=)\?(?=&|$)|\?\?/;
    d.ajaxSetup({
      jsonp: "callback",
      jsonpCallback: function() {
        var i = Yi.pop() || d.expando + "_" + fr.guid++;
        return this[i] = !0, i;
      }
    }), d.ajaxPrefilter("json jsonp", function(i, s, l) {
      var h, f, m, b = i.jsonp !== !1 && (ni.test(i.url) ? "url" : typeof i.data == "string" && (i.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && ni.test(i.data) && "data");
      if (b || i.dataTypes[0] === "jsonp")
        return h = i.jsonpCallback = M(i.jsonpCallback) ? i.jsonpCallback() : i.jsonpCallback, b ? i[b] = i[b].replace(ni, "$1" + h) : i.jsonp !== !1 && (i.url += (Fo.test(i.url) ? "&" : "?") + i.jsonp + "=" + h), i.converters["script json"] = function() {
          return m || d.error(h + " was not called"), m[0];
        }, i.dataTypes[0] = "json", f = e[h], e[h] = function() {
          m = arguments;
        }, l.always(function() {
          f === void 0 ? d(e).removeProp(h) : e[h] = f, i[h] && (i.jsonpCallback = s.jsonpCallback, Yi.push(h)), m && M(f) && f(m[0]), m = f = void 0;
        }), "script";
    }), x.createHTMLDocument = function() {
      var i = Y.implementation.createHTMLDocument("").body;
      return i.innerHTML = "<form></form><form></form>", i.childNodes.length === 2;
    }(), d.parseHTML = function(i, s, l) {
      if (typeof i != "string")
        return [];
      typeof s == "boolean" && (l = s, s = !1);
      var h, f, m;
      return s || (x.createHTMLDocument ? (s = Y.implementation.createHTMLDocument(""), h = s.createElement("base"), h.href = Y.location.href, s.head.appendChild(h)) : s = Y), f = Ae.exec(i), m = !l && [], f ? [s.createElement(f[1])] : (f = qe([i], s, m), m && m.length && d(m).remove(), d.merge([], f.childNodes));
    }, d.fn.load = function(i, s, l) {
      var h, f, m, b = this, E = i.indexOf(" ");
      return E > -1 && (h = ro(i.slice(E)), i = i.slice(0, E)), M(s) ? (l = s, s = void 0) : s && typeof s == "object" && (f = "POST"), b.length > 0 && d.ajax({
        url: i,
        // If "type" variable is undefined, then "GET" method will be used.
        // Make value of this field explicit since
        // user can override it through ajaxSetup method
        type: f || "GET",
        dataType: "html",
        data: s
      }).done(function(_) {
        m = arguments, b.html(h ? (
          // If a selector was specified, locate the right elements in a dummy div
          // Exclude scripts to avoid IE 'Permission Denied' errors
          d("<div>").append(d.parseHTML(_)).find(h)
        ) : (
          // Otherwise use the full result
          _
        ));
      }).always(l && function(_, O) {
        b.each(function() {
          l.apply(this, m || [_.responseText, O, _]);
        });
      }), this;
    }, d.expr.pseudos.animated = function(i) {
      return d.grep(d.timers, function(s) {
        return i === s.elem;
      }).length;
    }, d.offset = {
      setOffset: function(i, s, l) {
        var h, f, m, b, E, _, O, W = d.css(i, "position"), q = d(i), N = {};
        W === "static" && (i.style.position = "relative"), E = q.offset(), m = d.css(i, "top"), _ = d.css(i, "left"), O = (W === "absolute" || W === "fixed") && (m + _).indexOf("auto") > -1, O ? (h = q.position(), b = h.top, f = h.left) : (b = parseFloat(m) || 0, f = parseFloat(_) || 0), M(s) && (s = s.call(i, l, d.extend({}, E))), s.top != null && (N.top = s.top - E.top + b), s.left != null && (N.left = s.left - E.left + f), "using" in s ? s.using.call(i, N) : q.css(N);
      }
    }, d.fn.extend({
      // offset() relates an element's border box to the document origin
      offset: function(i) {
        if (arguments.length)
          return i === void 0 ? this : this.each(function(f) {
            d.offset.setOffset(this, i, f);
          });
        var s, l, h = this[0];
        if (h)
          return h.getClientRects().length ? (s = h.getBoundingClientRect(), l = h.ownerDocument.defaultView, {
            top: s.top + l.pageYOffset,
            left: s.left + l.pageXOffset
          }) : { top: 0, left: 0 };
      },
      // position() relates an element's margin box to its offset parent's padding box
      // This corresponds to the behavior of CSS absolute positioning
      position: function() {
        if (this[0]) {
          var i, s, l, h = this[0], f = { top: 0, left: 0 };
          if (d.css(h, "position") === "fixed")
            s = h.getBoundingClientRect();
          else {
            for (s = this.offset(), l = h.ownerDocument, i = h.offsetParent || l.documentElement; i && (i === l.body || i === l.documentElement) && d.css(i, "position") === "static"; )
              i = i.parentNode;
            i && i !== h && i.nodeType === 1 && (f = d(i).offset(), f.top += d.css(i, "borderTopWidth", !0), f.left += d.css(i, "borderLeftWidth", !0));
          }
          return {
            top: s.top - f.top - d.css(h, "marginTop", !0),
            left: s.left - f.left - d.css(h, "marginLeft", !0)
          };
        }
      },
      // This method will return documentElement in the following cases:
      // 1) For the element inside the iframe without offsetParent, this method will return
      //    documentElement of the parent window
      // 2) For the hidden or detached element
      // 3) For body or html element, i.e. in case of the html node - it will return itself
      //
      // but those exceptions were never presented as a real life use-cases
      // and might be considered as more preferable results.
      //
      // This logic, however, is not guaranteed and can change at any point in the future
      offsetParent: function() {
        return this.map(function() {
          for (var i = this.offsetParent; i && d.css(i, "position") === "static"; )
            i = i.offsetParent;
          return i || Jt;
        });
      }
    }), d.each({ scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function(i, s) {
      var l = s === "pageYOffset";
      d.fn[i] = function(h) {
        return vt(this, function(f, m, b) {
          var E;
          if (L(f) ? E = f : f.nodeType === 9 && (E = f.defaultView), b === void 0)
            return E ? E[s] : f[m];
          E ? E.scrollTo(
            l ? E.pageXOffset : b,
            l ? b : E.pageYOffset
          ) : f[m] = b;
        }, i, h, arguments.length);
      };
    }), d.each(["top", "left"], function(i, s) {
      d.cssHooks[s] = sr(
        x.pixelPosition,
        function(l, h) {
          if (h)
            return h = Lo(l, s), Gn.test(h) ? d(l).position()[s] + "px" : h;
        }
      );
    }), d.each({ Height: "height", Width: "width" }, function(i, s) {
      d.each({
        padding: "inner" + i,
        content: s,
        "": "outer" + i
      }, function(l, h) {
        d.fn[h] = function(f, m) {
          var b = arguments.length && (l || typeof f != "boolean"), E = l || (f === !0 || m === !0 ? "margin" : "border");
          return vt(this, function(_, O, W) {
            var q;
            return L(_) ? h.indexOf("outer") === 0 ? _["inner" + i] : _.document.documentElement["client" + i] : _.nodeType === 9 ? (q = _.documentElement, Math.max(
              _.body["scroll" + i],
              q["scroll" + i],
              _.body["offset" + i],
              q["offset" + i],
              q["client" + i]
            )) : W === void 0 ? (
              // Get width or height on the element, requesting but not forcing parseFloat
              d.css(_, O, E)
            ) : (
              // Set width or height on the element
              d.style(_, O, W, E)
            );
          }, s, b ? f : void 0, b);
        };
      });
    }), d.each([
      "ajaxStart",
      "ajaxStop",
      "ajaxComplete",
      "ajaxError",
      "ajaxSuccess",
      "ajaxSend"
    ], function(i, s) {
      d.fn[s] = function(l) {
        return this.on(s, l);
      };
    }), d.fn.extend({
      bind: function(i, s, l) {
        return this.on(i, null, s, l);
      },
      unbind: function(i, s) {
        return this.off(i, null, s);
      },
      delegate: function(i, s, l, h) {
        return this.on(s, i, l, h);
      },
      undelegate: function(i, s, l) {
        return arguments.length === 1 ? this.off(i, "**") : this.off(s, i || "**", l);
      },
      hover: function(i, s) {
        return this.on("mouseenter", i).on("mouseleave", s || i);
      }
    }), d.each(
      "blur focus focusin focusout resize scroll click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup contextmenu".split(" "),
      function(i, s) {
        d.fn[s] = function(l, h) {
          return arguments.length > 0 ? this.on(s, null, l, h) : this.trigger(s);
        };
      }
    );
    var yr = /^[\s\uFEFF\xA0]+|([^\s\uFEFF\xA0])[\s\uFEFF\xA0]+$/g;
    d.proxy = function(i, s) {
      var l, h, f;
      if (typeof s == "string" && (l = i[s], s = i, i = l), !!M(i))
        return h = a.call(arguments, 2), f = function() {
          return i.apply(s || this, h.concat(a.call(arguments)));
        }, f.guid = i.guid = i.guid || d.guid++, f;
    }, d.holdReady = function(i) {
      i ? d.readyWait++ : d.ready(!0);
    }, d.isArray = Array.isArray, d.parseJSON = JSON.parse, d.nodeName = R, d.isFunction = M, d.isWindow = L, d.camelCase = It, d.type = Q, d.now = Date.now, d.isNumeric = function(i) {
      var s = d.type(i);
      return (s === "number" || s === "string") && // parseFloat NaNs numeric-cast false positives ("")
      // ...but misinterprets leading-number strings, particularly hex literals ("0x...")
      // subtraction forces infinities to NaN
      !isNaN(i - parseFloat(i));
    }, d.trim = function(i) {
      return i == null ? "" : (i + "").replace(yr, "$1");
    };
    var Ur = e.jQuery, qr = e.$;
    return d.noConflict = function(i) {
      return e.$ === d && (e.$ = qr), i && e.jQuery === d && (e.jQuery = Ur), d;
    }, typeof t > "u" && (e.jQuery = e.$ = d), d;
  });
})(mu);
var Bb = mu.exports;
const Ie = /* @__PURE__ */ Ob(Bb);
//! moment.js
//! version : 2.30.1
//! authors : Tim Wood, Iskren Chernev, Moment.js contributors
//! license : MIT
//! momentjs.com
var gu;
function ue() {
  return gu.apply(null, arguments);
}
function Ab(o) {
  gu = o;
}
function Tn(o) {
  return o instanceof Array || Object.prototype.toString.call(o) === "[object Array]";
}
function Or(o) {
  return o != null && Object.prototype.toString.call(o) === "[object Object]";
}
function ct(o, e) {
  return Object.prototype.hasOwnProperty.call(o, e);
}
function yc(o) {
  if (Object.getOwnPropertyNames)
    return Object.getOwnPropertyNames(o).length === 0;
  var e;
  for (e in o)
    if (ct(o, e))
      return !1;
  return !0;
}
function No(o) {
  return o === void 0;
}
function Ci(o) {
  return typeof o == "number" || Object.prototype.toString.call(o) === "[object Number]";
}
function pa(o) {
  return o instanceof Date || Object.prototype.toString.call(o) === "[object Date]";
}
function bu(o, e) {
  var t = [], n, r = o.length;
  for (n = 0; n < r; ++n)
    t.push(e(o[n], n));
  return t;
}
function Vi(o, e) {
  for (var t in e)
    ct(e, t) && (o[t] = e[t]);
  return ct(e, "toString") && (o.toString = e.toString), ct(e, "valueOf") && (o.valueOf = e.valueOf), o;
}
function Yn(o, e, t, n) {
  return $u(o, e, t, n, !0).utc();
}
function Rb() {
  return {
    empty: !1,
    unusedTokens: [],
    unusedInput: [],
    overflow: -2,
    charsLeftOver: 0,
    nullInput: !1,
    invalidEra: null,
    invalidMonth: null,
    invalidFormat: !1,
    userInvalidated: !1,
    iso: !1,
    parsedDateParts: [],
    era: null,
    meridiem: null,
    rfc2822: !1,
    weekdayMismatch: !1
  };
}
function Ge(o) {
  return o._pf == null && (o._pf = Rb()), o._pf;
}
var Vl;
Array.prototype.some ? Vl = Array.prototype.some : Vl = function(o) {
  var e = Object(this), t = e.length >>> 0, n;
  for (n = 0; n < t; n++)
    if (n in e && o.call(this, e[n], n, e))
      return !0;
  return !1;
};
function wc(o) {
  var e = null, t = !1, n = o._d && !isNaN(o._d.getTime());
  if (n && (e = Ge(o), t = Vl.call(e.parsedDateParts, function(r) {
    return r != null;
  }), n = e.overflow < 0 && !e.empty && !e.invalidEra && !e.invalidMonth && !e.invalidWeekday && !e.weekdayMismatch && !e.nullInput && !e.invalidFormat && !e.userInvalidated && (!e.meridiem || e.meridiem && t), o._strict && (n = n && e.charsLeftOver === 0 && e.unusedTokens.length === 0 && e.bigHour === void 0)), Object.isFrozen == null || !Object.isFrozen(o))
    o._isValid = n;
  else
    return n;
  return o._isValid;
}
function Za(o) {
  var e = Yn(NaN);
  return o != null ? Vi(Ge(e), o) : Ge(e).userInvalidated = !0, e;
}
var Jd = ue.momentProperties = [], El = !1;
function kc(o, e) {
  var t, n, r, a = Jd.length;
  if (No(e._isAMomentObject) || (o._isAMomentObject = e._isAMomentObject), No(e._i) || (o._i = e._i), No(e._f) || (o._f = e._f), No(e._l) || (o._l = e._l), No(e._strict) || (o._strict = e._strict), No(e._tzm) || (o._tzm = e._tzm), No(e._isUTC) || (o._isUTC = e._isUTC), No(e._offset) || (o._offset = e._offset), No(e._pf) || (o._pf = Ge(e)), No(e._locale) || (o._locale = e._locale), a > 0)
    for (t = 0; t < a; t++)
      n = Jd[t], r = e[n], No(r) || (o[n] = r);
  return o;
}
function fa(o) {
  kc(this, o), this._d = new Date(o._d != null ? o._d.getTime() : NaN), this.isValid() || (this._d = /* @__PURE__ */ new Date(NaN)), El === !1 && (El = !0, ue.updateOffset(this), El = !1);
}
function _n(o) {
  return o instanceof fa || o != null && o._isAMomentObject != null;
}
function vu(o) {
  ue.suppressDeprecationWarnings === !1 && typeof console < "u" && console.warn && console.warn("Deprecation warning: " + o);
}
function an(o, e) {
  var t = !0;
  return Vi(function() {
    if (ue.deprecationHandler != null && ue.deprecationHandler(null, o), t) {
      var n = [], r, a, c, u = arguments.length;
      for (a = 0; a < u; a++) {
        if (r = "", typeof arguments[a] == "object") {
          r += `
[` + a + "] ";
          for (c in arguments[0])
            ct(arguments[0], c) && (r += c + ": " + arguments[0][c] + ", ");
          r = r.slice(0, -2);
        } else
          r = arguments[a];
        n.push(r);
      }
      vu(
        o + `
Arguments: ` + Array.prototype.slice.call(n).join("") + `
` + new Error().stack
      ), t = !1;
    }
    return e.apply(this, arguments);
  }, e);
}
var Qd = {};
function yu(o, e) {
  ue.deprecationHandler != null && ue.deprecationHandler(o, e), Qd[o] || (vu(e), Qd[o] = !0);
}
ue.suppressDeprecationWarnings = !1;
ue.deprecationHandler = null;
function Fn(o) {
  return typeof Function < "u" && o instanceof Function || Object.prototype.toString.call(o) === "[object Function]";
}
function Ib(o) {
  var e, t;
  for (t in o)
    ct(o, t) && (e = o[t], Fn(e) ? this[t] = e : this["_" + t] = e);
  this._config = o, this._dayOfMonthOrdinalParseLenient = new RegExp(
    (this._dayOfMonthOrdinalParse.source || this._ordinalParse.source) + "|" + /\d{1,2}/.source
  );
}
function Gl(o, e) {
  var t = Vi({}, o), n;
  for (n in e)
    ct(e, n) && (Or(o[n]) && Or(e[n]) ? (t[n] = {}, Vi(t[n], o[n]), Vi(t[n], e[n])) : e[n] != null ? t[n] = e[n] : delete t[n]);
  for (n in o)
    ct(o, n) && !ct(e, n) && Or(o[n]) && (t[n] = Vi({}, t[n]));
  return t;
}
function xc(o) {
  o != null && this.set(o);
}
var Zl;
Object.keys ? Zl = Object.keys : Zl = function(o) {
  var e, t = [];
  for (e in o)
    ct(o, e) && t.push(e);
  return t;
};
var Nb = {
  sameDay: "[Today at] LT",
  nextDay: "[Tomorrow at] LT",
  nextWeek: "dddd [at] LT",
  lastDay: "[Yesterday at] LT",
  lastWeek: "[Last] dddd [at] LT",
  sameElse: "L"
};
function Pb(o, e, t) {
  var n = this._calendar[o] || this._calendar.sameElse;
  return Fn(n) ? n.call(e, t) : n;
}
function jn(o, e, t) {
  var n = "" + Math.abs(o), r = e - n.length, a = o >= 0;
  return (a ? t ? "+" : "" : "-") + Math.pow(10, Math.max(0, r)).toString().substr(1) + n;
}
var Cc = /(\[[^\[]*\])|(\\)?([Hh]mm(ss)?|Mo|MM?M?M?|Do|DDDo|DD?D?D?|ddd?d?|do?|w[o|w]?|W[o|W]?|Qo?|N{1,5}|YYYYYY|YYYYY|YYYY|YY|y{2,4}|yo?|gg(ggg?)?|GG(GGG?)?|e|E|a|A|hh?|HH?|kk?|mm?|ss?|S{1,9}|x|X|zz?|ZZ?|.)/g, ka = /(\[[^\[]*\])|(\\)?(LTS|LT|LL?L?L?|l{1,4})/g, Ml = {}, ls = {};
function Pe(o, e, t, n) {
  var r = n;
  typeof n == "string" && (r = function() {
    return this[n]();
  }), o && (ls[o] = r), e && (ls[e[0]] = function() {
    return jn(r.apply(this, arguments), e[1], e[2]);
  }), t && (ls[t] = function() {
    return this.localeData().ordinal(
      r.apply(this, arguments),
      o
    );
  });
}
function Hb(o) {
  return o.match(/\[[\s\S]/) ? o.replace(/^\[|\]$/g, "") : o.replace(/\\/g, "");
}
function jb(o) {
  var e = o.match(Cc), t, n;
  for (t = 0, n = e.length; t < n; t++)
    ls[e[t]] ? e[t] = ls[e[t]] : e[t] = Hb(e[t]);
  return function(r) {
    var a = "", c;
    for (c = 0; c < n; c++)
      a += Fn(e[c]) ? e[c].call(r, o) : e[c];
    return a;
  };
}
function Ma(o, e) {
  return o.isValid() ? (e = wu(e, o.localeData()), Ml[e] = Ml[e] || jb(e), Ml[e](o)) : o.localeData().invalidDate();
}
function wu(o, e) {
  var t = 5;
  function n(r) {
    return e.longDateFormat(r) || r;
  }
  for (ka.lastIndex = 0; t >= 0 && ka.test(o); )
    o = o.replace(
      ka,
      n
    ), ka.lastIndex = 0, t -= 1;
  return o;
}
var $b = {
  LTS: "h:mm:ss A",
  LT: "h:mm A",
  L: "MM/DD/YYYY",
  LL: "MMMM D, YYYY",
  LLL: "MMMM D, YYYY h:mm A",
  LLLL: "dddd, MMMM D, YYYY h:mm A"
};
function Yb(o) {
  var e = this._longDateFormat[o], t = this._longDateFormat[o.toUpperCase()];
  return e || !t ? e : (this._longDateFormat[o] = t.match(Cc).map(function(n) {
    return n === "MMMM" || n === "MM" || n === "DD" || n === "dddd" ? n.slice(1) : n;
  }).join(""), this._longDateFormat[o]);
}
var Fb = "Invalid date";
function zb() {
  return this._invalidDate;
}
var Wb = "%d", Ub = /\d{1,2}/;
function qb(o) {
  return this._ordinal.replace("%d", o);
}
var Vb = {
  future: "in %s",
  past: "%s ago",
  s: "a few seconds",
  ss: "%d seconds",
  m: "a minute",
  mm: "%d minutes",
  h: "an hour",
  hh: "%d hours",
  d: "a day",
  dd: "%d days",
  w: "a week",
  ww: "%d weeks",
  M: "a month",
  MM: "%d months",
  y: "a year",
  yy: "%d years"
};
function Gb(o, e, t, n) {
  var r = this._relativeTime[t];
  return Fn(r) ? r(o, e, t, n) : r.replace(/%d/i, o);
}
function Zb(o, e) {
  var t = this._relativeTime[o > 0 ? "future" : "past"];
  return Fn(t) ? t(e) : t.replace(/%s/i, e);
}
var eh = {
  D: "date",
  dates: "date",
  date: "date",
  d: "day",
  days: "day",
  day: "day",
  e: "weekday",
  weekdays: "weekday",
  weekday: "weekday",
  E: "isoWeekday",
  isoweekdays: "isoWeekday",
  isoweekday: "isoWeekday",
  DDD: "dayOfYear",
  dayofyears: "dayOfYear",
  dayofyear: "dayOfYear",
  h: "hour",
  hours: "hour",
  hour: "hour",
  ms: "millisecond",
  milliseconds: "millisecond",
  millisecond: "millisecond",
  m: "minute",
  minutes: "minute",
  minute: "minute",
  M: "month",
  months: "month",
  month: "month",
  Q: "quarter",
  quarters: "quarter",
  quarter: "quarter",
  s: "second",
  seconds: "second",
  second: "second",
  gg: "weekYear",
  weekyears: "weekYear",
  weekyear: "weekYear",
  GG: "isoWeekYear",
  isoweekyears: "isoWeekYear",
  isoweekyear: "isoWeekYear",
  w: "week",
  weeks: "week",
  week: "week",
  W: "isoWeek",
  isoweeks: "isoWeek",
  isoweek: "isoWeek",
  y: "year",
  years: "year",
  year: "year"
};
function ln(o) {
  return typeof o == "string" ? eh[o] || eh[o.toLowerCase()] : void 0;
}
function Sc(o) {
  var e = {}, t, n;
  for (n in o)
    ct(o, n) && (t = ln(n), t && (e[t] = o[n]));
  return e;
}
var Kb = {
  date: 9,
  day: 11,
  weekday: 11,
  isoWeekday: 11,
  dayOfYear: 4,
  hour: 13,
  millisecond: 16,
  minute: 14,
  month: 8,
  quarter: 7,
  second: 15,
  weekYear: 1,
  isoWeekYear: 1,
  week: 5,
  isoWeek: 5,
  year: 1
};
function Xb(o) {
  var e = [], t;
  for (t in o)
    ct(o, t) && e.push({ unit: t, priority: Kb[t] });
  return e.sort(function(n, r) {
    return n.priority - r.priority;
  }), e;
}
var ku = /\d/, Xo = /\d\d/, xu = /\d{3}/, Tc = /\d{4}/, Ka = /[+-]?\d{6}/, Bt = /\d\d?/, Cu = /\d\d\d\d?/, Su = /\d\d\d\d\d\d?/, Xa = /\d{1,3}/, _c = /\d{1,4}/, Ja = /[+-]?\d{1,6}/, vs = /\d+/, Qa = /[+-]?\d+/, Jb = /Z|[+-]\d\d:?\d\d/gi, el = /Z|[+-]\d\d(?::?\d\d)?/gi, Qb = /[+-]?\d+(\.\d{1,3})?/, ma = /[0-9]{0,256}['a-z\u00A0-\u05FF\u0700-\uD7FF\uF900-\uFDCF\uFDF0-\uFF07\uFF10-\uFFEF]{1,256}|[\u0600-\u06FF\/]{1,256}(\s*?[\u0600-\u06FF]{1,256}){1,2}/i, ys = /^[1-9]\d?/, Ec = /^([1-9]\d|\d)/, Ia;
Ia = {};
function Le(o, e, t) {
  Ia[o] = Fn(e) ? e : function(n, r) {
    return n && t ? t : e;
  };
}
function e0(o, e) {
  return ct(Ia, o) ? Ia[o](e._strict, e._locale) : new RegExp(t0(o));
}
function t0(o) {
  return yi(
    o.replace("\\", "").replace(
      /\\(\[)|\\(\])|\[([^\]\[]*)\]|\\(.)/g,
      function(e, t, n, r, a) {
        return t || n || r || a;
      }
    )
  );
}
function yi(o) {
  return o.replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&");
}
function nn(o) {
  return o < 0 ? Math.ceil(o) || 0 : Math.floor(o);
}
function tt(o) {
  var e = +o, t = 0;
  return e !== 0 && isFinite(e) && (t = nn(e)), t;
}
var Kl = {};
function kt(o, e) {
  var t, n = e, r;
  for (typeof o == "string" && (o = [o]), Ci(e) && (n = function(a, c) {
    c[e] = tt(a);
  }), r = o.length, t = 0; t < r; t++)
    Kl[o[t]] = n;
}
function ga(o, e) {
  kt(o, function(t, n, r, a) {
    r._w = r._w || {}, e(t, r._w, r, a);
  });
}
function o0(o, e, t) {
  e != null && ct(Kl, o) && Kl[o](e, t._a, t, o);
}
function tl(o) {
  return o % 4 === 0 && o % 100 !== 0 || o % 400 === 0;
}
var bo = 0, mi = 1, Pn = 2, eo = 3, xn = 4, gi = 5, Mr = 6, n0 = 7, i0 = 8;
Pe("Y", 0, 0, function() {
  var o = this.year();
  return o <= 9999 ? jn(o, 4) : "+" + o;
});
Pe(0, ["YY", 2], 0, function() {
  return this.year() % 100;
});
Pe(0, ["YYYY", 4], 0, "year");
Pe(0, ["YYYYY", 5], 0, "year");
Pe(0, ["YYYYYY", 6, !0], 0, "year");
Le("Y", Qa);
Le("YY", Bt, Xo);
Le("YYYY", _c, Tc);
Le("YYYYY", Ja, Ka);
Le("YYYYYY", Ja, Ka);
kt(["YYYYY", "YYYYYY"], bo);
kt("YYYY", function(o, e) {
  e[bo] = o.length === 2 ? ue.parseTwoDigitYear(o) : tt(o);
});
kt("YY", function(o, e) {
  e[bo] = ue.parseTwoDigitYear(o);
});
kt("Y", function(o, e) {
  e[bo] = parseInt(o, 10);
});
function ea(o) {
  return tl(o) ? 366 : 365;
}
ue.parseTwoDigitYear = function(o) {
  return tt(o) + (tt(o) > 68 ? 1900 : 2e3);
};
var Tu = ws("FullYear", !0);
function r0() {
  return tl(this.year());
}
function ws(o, e) {
  return function(t) {
    return t != null ? (_u(this, o, t), ue.updateOffset(this, e), this) : ia(this, o);
  };
}
function ia(o, e) {
  if (!o.isValid())
    return NaN;
  var t = o._d, n = o._isUTC;
  switch (e) {
    case "Milliseconds":
      return n ? t.getUTCMilliseconds() : t.getMilliseconds();
    case "Seconds":
      return n ? t.getUTCSeconds() : t.getSeconds();
    case "Minutes":
      return n ? t.getUTCMinutes() : t.getMinutes();
    case "Hours":
      return n ? t.getUTCHours() : t.getHours();
    case "Date":
      return n ? t.getUTCDate() : t.getDate();
    case "Day":
      return n ? t.getUTCDay() : t.getDay();
    case "Month":
      return n ? t.getUTCMonth() : t.getMonth();
    case "FullYear":
      return n ? t.getUTCFullYear() : t.getFullYear();
    default:
      return NaN;
  }
}
function _u(o, e, t) {
  var n, r, a, c, u;
  if (!(!o.isValid() || isNaN(t))) {
    switch (n = o._d, r = o._isUTC, e) {
      case "Milliseconds":
        return void (r ? n.setUTCMilliseconds(t) : n.setMilliseconds(t));
      case "Seconds":
        return void (r ? n.setUTCSeconds(t) : n.setSeconds(t));
      case "Minutes":
        return void (r ? n.setUTCMinutes(t) : n.setMinutes(t));
      case "Hours":
        return void (r ? n.setUTCHours(t) : n.setHours(t));
      case "Date":
        return void (r ? n.setUTCDate(t) : n.setDate(t));
      case "FullYear":
        break;
      default:
        return;
    }
    a = t, c = o.month(), u = o.date(), u = u === 29 && c === 1 && !tl(a) ? 28 : u, r ? n.setUTCFullYear(a, c, u) : n.setFullYear(a, c, u);
  }
}
function s0(o) {
  return o = ln(o), Fn(this[o]) ? this[o]() : this;
}
function a0(o, e) {
  if (typeof o == "object") {
    o = Sc(o);
    var t = Xb(o), n, r = t.length;
    for (n = 0; n < r; n++)
      this[t[n].unit](o[t[n].unit]);
  } else if (o = ln(o), Fn(this[o]))
    return this[o](e);
  return this;
}
function l0(o, e) {
  return (o % e + e) % e;
}
var Gt;
Array.prototype.indexOf ? Gt = Array.prototype.indexOf : Gt = function(o) {
  var e;
  for (e = 0; e < this.length; ++e)
    if (this[e] === o)
      return e;
  return -1;
};
function Mc(o, e) {
  if (isNaN(o) || isNaN(e))
    return NaN;
  var t = l0(e, 12);
  return o += (e - t) / 12, t === 1 ? tl(o) ? 29 : 28 : 31 - t % 7 % 2;
}
Pe("M", ["MM", 2], "Mo", function() {
  return this.month() + 1;
});
Pe("MMM", 0, 0, function(o) {
  return this.localeData().monthsShort(this, o);
});
Pe("MMMM", 0, 0, function(o) {
  return this.localeData().months(this, o);
});
Le("M", Bt, ys);
Le("MM", Bt, Xo);
Le("MMM", function(o, e) {
  return e.monthsShortRegex(o);
});
Le("MMMM", function(o, e) {
  return e.monthsRegex(o);
});
kt(["M", "MM"], function(o, e) {
  e[mi] = tt(o) - 1;
});
kt(["MMM", "MMMM"], function(o, e, t, n) {
  var r = t._locale.monthsParse(o, n, t._strict);
  r != null ? e[mi] = r : Ge(t).invalidMonth = o;
});
var c0 = "January_February_March_April_May_June_July_August_September_October_November_December".split(
  "_"
), Eu = "Jan_Feb_Mar_Apr_May_Jun_Jul_Aug_Sep_Oct_Nov_Dec".split("_"), Mu = /D[oD]?(\[[^\[\]]*\]|\s)+MMMM?/, d0 = ma, h0 = ma;
function u0(o, e) {
  return o ? Tn(this._months) ? this._months[o.month()] : this._months[(this._months.isFormat || Mu).test(e) ? "format" : "standalone"][o.month()] : Tn(this._months) ? this._months : this._months.standalone;
}
function p0(o, e) {
  return o ? Tn(this._monthsShort) ? this._monthsShort[o.month()] : this._monthsShort[Mu.test(e) ? "format" : "standalone"][o.month()] : Tn(this._monthsShort) ? this._monthsShort : this._monthsShort.standalone;
}
function f0(o, e, t) {
  var n, r, a, c = o.toLocaleLowerCase();
  if (!this._monthsParse)
    for (this._monthsParse = [], this._longMonthsParse = [], this._shortMonthsParse = [], n = 0; n < 12; ++n)
      a = Yn([2e3, n]), this._shortMonthsParse[n] = this.monthsShort(
        a,
        ""
      ).toLocaleLowerCase(), this._longMonthsParse[n] = this.months(a, "").toLocaleLowerCase();
  return t ? e === "MMM" ? (r = Gt.call(this._shortMonthsParse, c), r !== -1 ? r : null) : (r = Gt.call(this._longMonthsParse, c), r !== -1 ? r : null) : e === "MMM" ? (r = Gt.call(this._shortMonthsParse, c), r !== -1 ? r : (r = Gt.call(this._longMonthsParse, c), r !== -1 ? r : null)) : (r = Gt.call(this._longMonthsParse, c), r !== -1 ? r : (r = Gt.call(this._shortMonthsParse, c), r !== -1 ? r : null));
}
function m0(o, e, t) {
  var n, r, a;
  if (this._monthsParseExact)
    return f0.call(this, o, e, t);
  for (this._monthsParse || (this._monthsParse = [], this._longMonthsParse = [], this._shortMonthsParse = []), n = 0; n < 12; n++)
    if (r = Yn([2e3, n]), t && !this._longMonthsParse[n] && (this._longMonthsParse[n] = new RegExp(
      "^" + this.months(r, "").replace(".", "") + "$",
      "i"
    ), this._shortMonthsParse[n] = new RegExp(
      "^" + this.monthsShort(r, "").replace(".", "") + "$",
      "i"
    )), !t && !this._monthsParse[n] && (a = "^" + this.months(r, "") + "|^" + this.monthsShort(r, ""), this._monthsParse[n] = new RegExp(a.replace(".", ""), "i")), t && e === "MMMM" && this._longMonthsParse[n].test(o) || t && e === "MMM" && this._shortMonthsParse[n].test(o) || !t && this._monthsParse[n].test(o))
      return n;
}
function Du(o, e) {
  if (!o.isValid())
    return o;
  if (typeof e == "string") {
    if (/^\d+$/.test(e))
      e = tt(e);
    else if (e = o.localeData().monthsParse(e), !Ci(e))
      return o;
  }
  var t = e, n = o.date();
  return n = n < 29 ? n : Math.min(n, Mc(o.year(), t)), o._isUTC ? o._d.setUTCMonth(t, n) : o._d.setMonth(t, n), o;
}
function Lu(o) {
  return o != null ? (Du(this, o), ue.updateOffset(this, !0), this) : ia(this, "Month");
}
function g0() {
  return Mc(this.year(), this.month());
}
function b0(o) {
  return this._monthsParseExact ? (ct(this, "_monthsRegex") || Ou.call(this), o ? this._monthsShortStrictRegex : this._monthsShortRegex) : (ct(this, "_monthsShortRegex") || (this._monthsShortRegex = d0), this._monthsShortStrictRegex && o ? this._monthsShortStrictRegex : this._monthsShortRegex);
}
function v0(o) {
  return this._monthsParseExact ? (ct(this, "_monthsRegex") || Ou.call(this), o ? this._monthsStrictRegex : this._monthsRegex) : (ct(this, "_monthsRegex") || (this._monthsRegex = h0), this._monthsStrictRegex && o ? this._monthsStrictRegex : this._monthsRegex);
}
function Ou() {
  function o(p, g) {
    return g.length - p.length;
  }
  var e = [], t = [], n = [], r, a, c, u;
  for (r = 0; r < 12; r++)
    a = Yn([2e3, r]), c = yi(this.monthsShort(a, "")), u = yi(this.months(a, "")), e.push(c), t.push(u), n.push(u), n.push(c);
  e.sort(o), t.sort(o), n.sort(o), this._monthsRegex = new RegExp("^(" + n.join("|") + ")", "i"), this._monthsShortRegex = this._monthsRegex, this._monthsStrictRegex = new RegExp(
    "^(" + t.join("|") + ")",
    "i"
  ), this._monthsShortStrictRegex = new RegExp(
    "^(" + e.join("|") + ")",
    "i"
  );
}
function y0(o, e, t, n, r, a, c) {
  var u;
  return o < 100 && o >= 0 ? (u = new Date(o + 400, e, t, n, r, a, c), isFinite(u.getFullYear()) && u.setFullYear(o)) : u = new Date(o, e, t, n, r, a, c), u;
}
function ra(o) {
  var e, t;
  return o < 100 && o >= 0 ? (t = Array.prototype.slice.call(arguments), t[0] = o + 400, e = new Date(Date.UTC.apply(null, t)), isFinite(e.getUTCFullYear()) && e.setUTCFullYear(o)) : e = new Date(Date.UTC.apply(null, arguments)), e;
}
function Na(o, e, t) {
  var n = 7 + e - t, r = (7 + ra(o, 0, n).getUTCDay() - e) % 7;
  return -r + n - 1;
}
function Bu(o, e, t, n, r) {
  var a = (7 + t - n) % 7, c = Na(o, n, r), u = 1 + 7 * (e - 1) + a + c, p, g;
  return u <= 0 ? (p = o - 1, g = ea(p) + u) : u > ea(o) ? (p = o + 1, g = u - ea(o)) : (p = o, g = u), {
    year: p,
    dayOfYear: g
  };
}
function sa(o, e, t) {
  var n = Na(o.year(), e, t), r = Math.floor((o.dayOfYear() - n - 1) / 7) + 1, a, c;
  return r < 1 ? (c = o.year() - 1, a = r + wi(c, e, t)) : r > wi(o.year(), e, t) ? (a = r - wi(o.year(), e, t), c = o.year() + 1) : (c = o.year(), a = r), {
    week: a,
    year: c
  };
}
function wi(o, e, t) {
  var n = Na(o, e, t), r = Na(o + 1, e, t);
  return (ea(o) - n + r) / 7;
}
Pe("w", ["ww", 2], "wo", "week");
Pe("W", ["WW", 2], "Wo", "isoWeek");
Le("w", Bt, ys);
Le("ww", Bt, Xo);
Le("W", Bt, ys);
Le("WW", Bt, Xo);
ga(
  ["w", "ww", "W", "WW"],
  function(o, e, t, n) {
    e[n.substr(0, 1)] = tt(o);
  }
);
function w0(o) {
  return sa(o, this._week.dow, this._week.doy).week;
}
var k0 = {
  dow: 0,
  // Sunday is the first day of the week.
  doy: 6
  // The week that contains Jan 6th is the first week of the year.
};
function x0() {
  return this._week.dow;
}
function C0() {
  return this._week.doy;
}
function S0(o) {
  var e = this.localeData().week(this);
  return o == null ? e : this.add((o - e) * 7, "d");
}
function T0(o) {
  var e = sa(this, 1, 4).week;
  return o == null ? e : this.add((o - e) * 7, "d");
}
Pe("d", 0, "do", "day");
Pe("dd", 0, 0, function(o) {
  return this.localeData().weekdaysMin(this, o);
});
Pe("ddd", 0, 0, function(o) {
  return this.localeData().weekdaysShort(this, o);
});
Pe("dddd", 0, 0, function(o) {
  return this.localeData().weekdays(this, o);
});
Pe("e", 0, 0, "weekday");
Pe("E", 0, 0, "isoWeekday");
Le("d", Bt);
Le("e", Bt);
Le("E", Bt);
Le("dd", function(o, e) {
  return e.weekdaysMinRegex(o);
});
Le("ddd", function(o, e) {
  return e.weekdaysShortRegex(o);
});
Le("dddd", function(o, e) {
  return e.weekdaysRegex(o);
});
ga(["dd", "ddd", "dddd"], function(o, e, t, n) {
  var r = t._locale.weekdaysParse(o, n, t._strict);
  r != null ? e.d = r : Ge(t).invalidWeekday = o;
});
ga(["d", "e", "E"], function(o, e, t, n) {
  e[n] = tt(o);
});
function _0(o, e) {
  return typeof o != "string" ? o : isNaN(o) ? (o = e.weekdaysParse(o), typeof o == "number" ? o : null) : parseInt(o, 10);
}
function E0(o, e) {
  return typeof o == "string" ? e.weekdaysParse(o) % 7 || 7 : isNaN(o) ? null : o;
}
function Dc(o, e) {
  return o.slice(e, 7).concat(o.slice(0, e));
}
var M0 = "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"), Au = "Sun_Mon_Tue_Wed_Thu_Fri_Sat".split("_"), D0 = "Su_Mo_Tu_We_Th_Fr_Sa".split("_"), L0 = ma, O0 = ma, B0 = ma;
function A0(o, e) {
  var t = Tn(this._weekdays) ? this._weekdays : this._weekdays[o && o !== !0 && this._weekdays.isFormat.test(e) ? "format" : "standalone"];
  return o === !0 ? Dc(t, this._week.dow) : o ? t[o.day()] : t;
}
function R0(o) {
  return o === !0 ? Dc(this._weekdaysShort, this._week.dow) : o ? this._weekdaysShort[o.day()] : this._weekdaysShort;
}
function I0(o) {
  return o === !0 ? Dc(this._weekdaysMin, this._week.dow) : o ? this._weekdaysMin[o.day()] : this._weekdaysMin;
}
function N0(o, e, t) {
  var n, r, a, c = o.toLocaleLowerCase();
  if (!this._weekdaysParse)
    for (this._weekdaysParse = [], this._shortWeekdaysParse = [], this._minWeekdaysParse = [], n = 0; n < 7; ++n)
      a = Yn([2e3, 1]).day(n), this._minWeekdaysParse[n] = this.weekdaysMin(
        a,
        ""
      ).toLocaleLowerCase(), this._shortWeekdaysParse[n] = this.weekdaysShort(
        a,
        ""
      ).toLocaleLowerCase(), this._weekdaysParse[n] = this.weekdays(a, "").toLocaleLowerCase();
  return t ? e === "dddd" ? (r = Gt.call(this._weekdaysParse, c), r !== -1 ? r : null) : e === "ddd" ? (r = Gt.call(this._shortWeekdaysParse, c), r !== -1 ? r : null) : (r = Gt.call(this._minWeekdaysParse, c), r !== -1 ? r : null) : e === "dddd" ? (r = Gt.call(this._weekdaysParse, c), r !== -1 || (r = Gt.call(this._shortWeekdaysParse, c), r !== -1) ? r : (r = Gt.call(this._minWeekdaysParse, c), r !== -1 ? r : null)) : e === "ddd" ? (r = Gt.call(this._shortWeekdaysParse, c), r !== -1 || (r = Gt.call(this._weekdaysParse, c), r !== -1) ? r : (r = Gt.call(this._minWeekdaysParse, c), r !== -1 ? r : null)) : (r = Gt.call(this._minWeekdaysParse, c), r !== -1 || (r = Gt.call(this._weekdaysParse, c), r !== -1) ? r : (r = Gt.call(this._shortWeekdaysParse, c), r !== -1 ? r : null));
}
function P0(o, e, t) {
  var n, r, a;
  if (this._weekdaysParseExact)
    return N0.call(this, o, e, t);
  for (this._weekdaysParse || (this._weekdaysParse = [], this._minWeekdaysParse = [], this._shortWeekdaysParse = [], this._fullWeekdaysParse = []), n = 0; n < 7; n++)
    if (r = Yn([2e3, 1]).day(n), t && !this._fullWeekdaysParse[n] && (this._fullWeekdaysParse[n] = new RegExp(
      "^" + this.weekdays(r, "").replace(".", "\\.?") + "$",
      "i"
    ), this._shortWeekdaysParse[n] = new RegExp(
      "^" + this.weekdaysShort(r, "").replace(".", "\\.?") + "$",
      "i"
    ), this._minWeekdaysParse[n] = new RegExp(
      "^" + this.weekdaysMin(r, "").replace(".", "\\.?") + "$",
      "i"
    )), this._weekdaysParse[n] || (a = "^" + this.weekdays(r, "") + "|^" + this.weekdaysShort(r, "") + "|^" + this.weekdaysMin(r, ""), this._weekdaysParse[n] = new RegExp(a.replace(".", ""), "i")), t && e === "dddd" && this._fullWeekdaysParse[n].test(o) || t && e === "ddd" && this._shortWeekdaysParse[n].test(o) || t && e === "dd" && this._minWeekdaysParse[n].test(o) || !t && this._weekdaysParse[n].test(o))
      return n;
}
function H0(o) {
  if (!this.isValid())
    return o != null ? this : NaN;
  var e = ia(this, "Day");
  return o != null ? (o = _0(o, this.localeData()), this.add(o - e, "d")) : e;
}
function j0(o) {
  if (!this.isValid())
    return o != null ? this : NaN;
  var e = (this.day() + 7 - this.localeData()._week.dow) % 7;
  return o == null ? e : this.add(o - e, "d");
}
function $0(o) {
  if (!this.isValid())
    return o != null ? this : NaN;
  if (o != null) {
    var e = E0(o, this.localeData());
    return this.day(this.day() % 7 ? e : e - 7);
  } else
    return this.day() || 7;
}
function Y0(o) {
  return this._weekdaysParseExact ? (ct(this, "_weekdaysRegex") || Lc.call(this), o ? this._weekdaysStrictRegex : this._weekdaysRegex) : (ct(this, "_weekdaysRegex") || (this._weekdaysRegex = L0), this._weekdaysStrictRegex && o ? this._weekdaysStrictRegex : this._weekdaysRegex);
}
function F0(o) {
  return this._weekdaysParseExact ? (ct(this, "_weekdaysRegex") || Lc.call(this), o ? this._weekdaysShortStrictRegex : this._weekdaysShortRegex) : (ct(this, "_weekdaysShortRegex") || (this._weekdaysShortRegex = O0), this._weekdaysShortStrictRegex && o ? this._weekdaysShortStrictRegex : this._weekdaysShortRegex);
}
function z0(o) {
  return this._weekdaysParseExact ? (ct(this, "_weekdaysRegex") || Lc.call(this), o ? this._weekdaysMinStrictRegex : this._weekdaysMinRegex) : (ct(this, "_weekdaysMinRegex") || (this._weekdaysMinRegex = B0), this._weekdaysMinStrictRegex && o ? this._weekdaysMinStrictRegex : this._weekdaysMinRegex);
}
function Lc() {
  function o(v, k) {
    return k.length - v.length;
  }
  var e = [], t = [], n = [], r = [], a, c, u, p, g;
  for (a = 0; a < 7; a++)
    c = Yn([2e3, 1]).day(a), u = yi(this.weekdaysMin(c, "")), p = yi(this.weekdaysShort(c, "")), g = yi(this.weekdays(c, "")), e.push(u), t.push(p), n.push(g), r.push(u), r.push(p), r.push(g);
  e.sort(o), t.sort(o), n.sort(o), r.sort(o), this._weekdaysRegex = new RegExp("^(" + r.join("|") + ")", "i"), this._weekdaysShortRegex = this._weekdaysRegex, this._weekdaysMinRegex = this._weekdaysRegex, this._weekdaysStrictRegex = new RegExp(
    "^(" + n.join("|") + ")",
    "i"
  ), this._weekdaysShortStrictRegex = new RegExp(
    "^(" + t.join("|") + ")",
    "i"
  ), this._weekdaysMinStrictRegex = new RegExp(
    "^(" + e.join("|") + ")",
    "i"
  );
}
function Oc() {
  return this.hours() % 12 || 12;
}
function W0() {
  return this.hours() || 24;
}
Pe("H", ["HH", 2], 0, "hour");
Pe("h", ["hh", 2], 0, Oc);
Pe("k", ["kk", 2], 0, W0);
Pe("hmm", 0, 0, function() {
  return "" + Oc.apply(this) + jn(this.minutes(), 2);
});
Pe("hmmss", 0, 0, function() {
  return "" + Oc.apply(this) + jn(this.minutes(), 2) + jn(this.seconds(), 2);
});
Pe("Hmm", 0, 0, function() {
  return "" + this.hours() + jn(this.minutes(), 2);
});
Pe("Hmmss", 0, 0, function() {
  return "" + this.hours() + jn(this.minutes(), 2) + jn(this.seconds(), 2);
});
function Ru(o, e) {
  Pe(o, 0, 0, function() {
    return this.localeData().meridiem(
      this.hours(),
      this.minutes(),
      e
    );
  });
}
Ru("a", !0);
Ru("A", !1);
function Iu(o, e) {
  return e._meridiemParse;
}
Le("a", Iu);
Le("A", Iu);
Le("H", Bt, Ec);
Le("h", Bt, ys);
Le("k", Bt, ys);
Le("HH", Bt, Xo);
Le("hh", Bt, Xo);
Le("kk", Bt, Xo);
Le("hmm", Cu);
Le("hmmss", Su);
Le("Hmm", Cu);
Le("Hmmss", Su);
kt(["H", "HH"], eo);
kt(["k", "kk"], function(o, e, t) {
  var n = tt(o);
  e[eo] = n === 24 ? 0 : n;
});
kt(["a", "A"], function(o, e, t) {
  t._isPm = t._locale.isPM(o), t._meridiem = o;
});
kt(["h", "hh"], function(o, e, t) {
  e[eo] = tt(o), Ge(t).bigHour = !0;
});
kt("hmm", function(o, e, t) {
  var n = o.length - 2;
  e[eo] = tt(o.substr(0, n)), e[xn] = tt(o.substr(n)), Ge(t).bigHour = !0;
});
kt("hmmss", function(o, e, t) {
  var n = o.length - 4, r = o.length - 2;
  e[eo] = tt(o.substr(0, n)), e[xn] = tt(o.substr(n, 2)), e[gi] = tt(o.substr(r)), Ge(t).bigHour = !0;
});
kt("Hmm", function(o, e, t) {
  var n = o.length - 2;
  e[eo] = tt(o.substr(0, n)), e[xn] = tt(o.substr(n));
});
kt("Hmmss", function(o, e, t) {
  var n = o.length - 4, r = o.length - 2;
  e[eo] = tt(o.substr(0, n)), e[xn] = tt(o.substr(n, 2)), e[gi] = tt(o.substr(r));
});
function U0(o) {
  return (o + "").toLowerCase().charAt(0) === "p";
}
var q0 = /[ap]\.?m?\.?/i, V0 = ws("Hours", !0);
function G0(o, e, t) {
  return o > 11 ? t ? "pm" : "PM" : t ? "am" : "AM";
}
var Nu = {
  calendar: Nb,
  longDateFormat: $b,
  invalidDate: Fb,
  ordinal: Wb,
  dayOfMonthOrdinalParse: Ub,
  relativeTime: Vb,
  months: c0,
  monthsShort: Eu,
  week: k0,
  weekdays: M0,
  weekdaysMin: D0,
  weekdaysShort: Au,
  meridiemParse: q0
}, Nt = {}, qs = {}, aa;
function Z0(o, e) {
  var t, n = Math.min(o.length, e.length);
  for (t = 0; t < n; t += 1)
    if (o[t] !== e[t])
      return t;
  return n;
}
function th(o) {
  return o && o.toLowerCase().replace("_", "-");
}
function K0(o) {
  for (var e = 0, t, n, r, a; e < o.length; ) {
    for (a = th(o[e]).split("-"), t = a.length, n = th(o[e + 1]), n = n ? n.split("-") : null; t > 0; ) {
      if (r = ol(a.slice(0, t).join("-")), r)
        return r;
      if (n && n.length >= t && Z0(a, n) >= t - 1)
        break;
      t--;
    }
    e++;
  }
  return aa;
}
function X0(o) {
  return !!(o && o.match("^[^/\\\\]*$"));
}
function ol(o) {
  var e = null, t;
  if (Nt[o] === void 0 && typeof module < "u" && module && module.exports && X0(o))
    try {
      e = aa._abbr, t = require, t("./locale/" + o), Xi(e);
    } catch {
      Nt[o] = null;
    }
  return Nt[o];
}
function Xi(o, e) {
  var t;
  return o && (No(e) ? t = Ti(o) : t = Bc(o, e), t ? aa = t : typeof console < "u" && console.warn && console.warn(
    "Locale " + o + " not found. Did you forget to load it?"
  )), aa._abbr;
}
function Bc(o, e) {
  if (e !== null) {
    var t, n = Nu;
    if (e.abbr = o, Nt[o] != null)
      yu(
        "defineLocaleOverride",
        "use moment.updateLocale(localeName, config) to change an existing locale. moment.defineLocale(localeName, config) should only be used for creating a new locale See http://momentjs.com/guides/#/warnings/define-locale/ for more info."
      ), n = Nt[o]._config;
    else if (e.parentLocale != null)
      if (Nt[e.parentLocale] != null)
        n = Nt[e.parentLocale]._config;
      else if (t = ol(e.parentLocale), t != null)
        n = t._config;
      else
        return qs[e.parentLocale] || (qs[e.parentLocale] = []), qs[e.parentLocale].push({
          name: o,
          config: e
        }), null;
    return Nt[o] = new xc(Gl(n, e)), qs[o] && qs[o].forEach(function(r) {
      Bc(r.name, r.config);
    }), Xi(o), Nt[o];
  } else
    return delete Nt[o], null;
}
function J0(o, e) {
  if (e != null) {
    var t, n, r = Nu;
    Nt[o] != null && Nt[o].parentLocale != null ? Nt[o].set(Gl(Nt[o]._config, e)) : (n = ol(o), n != null && (r = n._config), e = Gl(r, e), n == null && (e.abbr = o), t = new xc(e), t.parentLocale = Nt[o], Nt[o] = t), Xi(o);
  } else
    Nt[o] != null && (Nt[o].parentLocale != null ? (Nt[o] = Nt[o].parentLocale, o === Xi() && Xi(o)) : Nt[o] != null && delete Nt[o]);
  return Nt[o];
}
function Ti(o) {
  var e;
  if (o && o._locale && o._locale._abbr && (o = o._locale._abbr), !o)
    return aa;
  if (!Tn(o)) {
    if (e = ol(o), e)
      return e;
    o = [o];
  }
  return K0(o);
}
function Q0() {
  return Zl(Nt);
}
function Ac(o) {
  var e, t = o._a;
  return t && Ge(o).overflow === -2 && (e = t[mi] < 0 || t[mi] > 11 ? mi : t[Pn] < 1 || t[Pn] > Mc(t[bo], t[mi]) ? Pn : t[eo] < 0 || t[eo] > 24 || t[eo] === 24 && (t[xn] !== 0 || t[gi] !== 0 || t[Mr] !== 0) ? eo : t[xn] < 0 || t[xn] > 59 ? xn : t[gi] < 0 || t[gi] > 59 ? gi : t[Mr] < 0 || t[Mr] > 999 ? Mr : -1, Ge(o)._overflowDayOfYear && (e < bo || e > Pn) && (e = Pn), Ge(o)._overflowWeeks && e === -1 && (e = n0), Ge(o)._overflowWeekday && e === -1 && (e = i0), Ge(o).overflow = e), o;
}
var e1 = /^\s*((?:[+-]\d{6}|\d{4})-(?:\d\d-\d\d|W\d\d-\d|W\d\d|\d\d\d|\d\d))(?:(T| )(\d\d(?::\d\d(?::\d\d(?:[.,]\d+)?)?)?)([+-]\d\d(?::?\d\d)?|\s*Z)?)?$/, t1 = /^\s*((?:[+-]\d{6}|\d{4})(?:\d\d\d\d|W\d\d\d|W\d\d|\d\d\d|\d\d|))(?:(T| )(\d\d(?:\d\d(?:\d\d(?:[.,]\d+)?)?)?)([+-]\d\d(?::?\d\d)?|\s*Z)?)?$/, o1 = /Z|[+-]\d\d(?::?\d\d)?/, xa = [
  ["YYYYYY-MM-DD", /[+-]\d{6}-\d\d-\d\d/],
  ["YYYY-MM-DD", /\d{4}-\d\d-\d\d/],
  ["GGGG-[W]WW-E", /\d{4}-W\d\d-\d/],
  ["GGGG-[W]WW", /\d{4}-W\d\d/, !1],
  ["YYYY-DDD", /\d{4}-\d{3}/],
  ["YYYY-MM", /\d{4}-\d\d/, !1],
  ["YYYYYYMMDD", /[+-]\d{10}/],
  ["YYYYMMDD", /\d{8}/],
  ["GGGG[W]WWE", /\d{4}W\d{3}/],
  ["GGGG[W]WW", /\d{4}W\d{2}/, !1],
  ["YYYYDDD", /\d{7}/],
  ["YYYYMM", /\d{6}/, !1],
  ["YYYY", /\d{4}/, !1]
], Dl = [
  ["HH:mm:ss.SSSS", /\d\d:\d\d:\d\d\.\d+/],
  ["HH:mm:ss,SSSS", /\d\d:\d\d:\d\d,\d+/],
  ["HH:mm:ss", /\d\d:\d\d:\d\d/],
  ["HH:mm", /\d\d:\d\d/],
  ["HHmmss.SSSS", /\d\d\d\d\d\d\.\d+/],
  ["HHmmss,SSSS", /\d\d\d\d\d\d,\d+/],
  ["HHmmss", /\d\d\d\d\d\d/],
  ["HHmm", /\d\d\d\d/],
  ["HH", /\d\d/]
], n1 = /^\/?Date\((-?\d+)/i, i1 = /^(?:(Mon|Tue|Wed|Thu|Fri|Sat|Sun),?\s)?(\d{1,2})\s(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)\s(\d{2,4})\s(\d\d):(\d\d)(?::(\d\d))?\s(?:(UT|GMT|[ECMP][SD]T)|([Zz])|([+-]\d{4}))$/, r1 = {
  UT: 0,
  GMT: 0,
  EDT: -4 * 60,
  EST: -5 * 60,
  CDT: -5 * 60,
  CST: -6 * 60,
  MDT: -6 * 60,
  MST: -7 * 60,
  PDT: -7 * 60,
  PST: -8 * 60
};
function Pu(o) {
  var e, t, n = o._i, r = e1.exec(n) || t1.exec(n), a, c, u, p, g = xa.length, v = Dl.length;
  if (r) {
    for (Ge(o).iso = !0, e = 0, t = g; e < t; e++)
      if (xa[e][1].exec(r[1])) {
        c = xa[e][0], a = xa[e][2] !== !1;
        break;
      }
    if (c == null) {
      o._isValid = !1;
      return;
    }
    if (r[3]) {
      for (e = 0, t = v; e < t; e++)
        if (Dl[e][1].exec(r[3])) {
          u = (r[2] || " ") + Dl[e][0];
          break;
        }
      if (u == null) {
        o._isValid = !1;
        return;
      }
    }
    if (!a && u != null) {
      o._isValid = !1;
      return;
    }
    if (r[4])
      if (o1.exec(r[4]))
        p = "Z";
      else {
        o._isValid = !1;
        return;
      }
    o._f = c + (u || "") + (p || ""), Ic(o);
  } else
    o._isValid = !1;
}
function s1(o, e, t, n, r, a) {
  var c = [
    a1(o),
    Eu.indexOf(e),
    parseInt(t, 10),
    parseInt(n, 10),
    parseInt(r, 10)
  ];
  return a && c.push(parseInt(a, 10)), c;
}
function a1(o) {
  var e = parseInt(o, 10);
  return e <= 49 ? 2e3 + e : e <= 999 ? 1900 + e : e;
}
function l1(o) {
  return o.replace(/\([^()]*\)|[\n\t]/g, " ").replace(/(\s\s+)/g, " ").replace(/^\s\s*/, "").replace(/\s\s*$/, "");
}
function c1(o, e, t) {
  if (o) {
    var n = Au.indexOf(o), r = new Date(
      e[0],
      e[1],
      e[2]
    ).getDay();
    if (n !== r)
      return Ge(t).weekdayMismatch = !0, t._isValid = !1, !1;
  }
  return !0;
}
function d1(o, e, t) {
  if (o)
    return r1[o];
  if (e)
    return 0;
  var n = parseInt(t, 10), r = n % 100, a = (n - r) / 100;
  return a * 60 + r;
}
function Hu(o) {
  var e = i1.exec(l1(o._i)), t;
  if (e) {
    if (t = s1(
      e[4],
      e[3],
      e[2],
      e[5],
      e[6],
      e[7]
    ), !c1(e[1], t, o))
      return;
    o._a = t, o._tzm = d1(e[8], e[9], e[10]), o._d = ra.apply(null, o._a), o._d.setUTCMinutes(o._d.getUTCMinutes() - o._tzm), Ge(o).rfc2822 = !0;
  } else
    o._isValid = !1;
}
function h1(o) {
  var e = n1.exec(o._i);
  if (e !== null) {
    o._d = /* @__PURE__ */ new Date(+e[1]);
    return;
  }
  if (Pu(o), o._isValid === !1)
    delete o._isValid;
  else
    return;
  if (Hu(o), o._isValid === !1)
    delete o._isValid;
  else
    return;
  o._strict ? o._isValid = !1 : ue.createFromInputFallback(o);
}
ue.createFromInputFallback = an(
  "value provided is not in a recognized RFC2822 or ISO format. moment construction falls back to js Date(), which is not reliable across all browsers and versions. Non RFC2822/ISO date formats are discouraged. Please refer to http://momentjs.com/guides/#/warnings/js-date/ for more info.",
  function(o) {
    o._d = /* @__PURE__ */ new Date(o._i + (o._useUTC ? " UTC" : ""));
  }
);
function os(o, e, t) {
  return o ?? e ?? t;
}
function u1(o) {
  var e = new Date(ue.now());
  return o._useUTC ? [
    e.getUTCFullYear(),
    e.getUTCMonth(),
    e.getUTCDate()
  ] : [e.getFullYear(), e.getMonth(), e.getDate()];
}
function Rc(o) {
  var e, t, n = [], r, a, c;
  if (!o._d) {
    for (r = u1(o), o._w && o._a[Pn] == null && o._a[mi] == null && p1(o), o._dayOfYear != null && (c = os(o._a[bo], r[bo]), (o._dayOfYear > ea(c) || o._dayOfYear === 0) && (Ge(o)._overflowDayOfYear = !0), t = ra(c, 0, o._dayOfYear), o._a[mi] = t.getUTCMonth(), o._a[Pn] = t.getUTCDate()), e = 0; e < 3 && o._a[e] == null; ++e)
      o._a[e] = n[e] = r[e];
    for (; e < 7; e++)
      o._a[e] = n[e] = o._a[e] == null ? e === 2 ? 1 : 0 : o._a[e];
    o._a[eo] === 24 && o._a[xn] === 0 && o._a[gi] === 0 && o._a[Mr] === 0 && (o._nextDay = !0, o._a[eo] = 0), o._d = (o._useUTC ? ra : y0).apply(
      null,
      n
    ), a = o._useUTC ? o._d.getUTCDay() : o._d.getDay(), o._tzm != null && o._d.setUTCMinutes(o._d.getUTCMinutes() - o._tzm), o._nextDay && (o._a[eo] = 24), o._w && typeof o._w.d < "u" && o._w.d !== a && (Ge(o).weekdayMismatch = !0);
  }
}
function p1(o) {
  var e, t, n, r, a, c, u, p, g;
  e = o._w, e.GG != null || e.W != null || e.E != null ? (a = 1, c = 4, t = os(
    e.GG,
    o._a[bo],
    sa(Lt(), 1, 4).year
  ), n = os(e.W, 1), r = os(e.E, 1), (r < 1 || r > 7) && (p = !0)) : (a = o._locale._week.dow, c = o._locale._week.doy, g = sa(Lt(), a, c), t = os(e.gg, o._a[bo], g.year), n = os(e.w, g.week), e.d != null ? (r = e.d, (r < 0 || r > 6) && (p = !0)) : e.e != null ? (r = e.e + a, (e.e < 0 || e.e > 6) && (p = !0)) : r = a), n < 1 || n > wi(t, a, c) ? Ge(o)._overflowWeeks = !0 : p != null ? Ge(o)._overflowWeekday = !0 : (u = Bu(t, n, r, a, c), o._a[bo] = u.year, o._dayOfYear = u.dayOfYear);
}
ue.ISO_8601 = function() {
};
ue.RFC_2822 = function() {
};
function Ic(o) {
  if (o._f === ue.ISO_8601) {
    Pu(o);
    return;
  }
  if (o._f === ue.RFC_2822) {
    Hu(o);
    return;
  }
  o._a = [], Ge(o).empty = !0;
  var e = "" + o._i, t, n, r, a, c, u = e.length, p = 0, g, v;
  for (r = wu(o._f, o._locale).match(Cc) || [], v = r.length, t = 0; t < v; t++)
    a = r[t], n = (e.match(e0(a, o)) || [])[0], n && (c = e.substr(0, e.indexOf(n)), c.length > 0 && Ge(o).unusedInput.push(c), e = e.slice(
      e.indexOf(n) + n.length
    ), p += n.length), ls[a] ? (n ? Ge(o).empty = !1 : Ge(o).unusedTokens.push(a), o0(a, n, o)) : o._strict && !n && Ge(o).unusedTokens.push(a);
  Ge(o).charsLeftOver = u - p, e.length > 0 && Ge(o).unusedInput.push(e), o._a[eo] <= 12 && Ge(o).bigHour === !0 && o._a[eo] > 0 && (Ge(o).bigHour = void 0), Ge(o).parsedDateParts = o._a.slice(0), Ge(o).meridiem = o._meridiem, o._a[eo] = f1(
    o._locale,
    o._a[eo],
    o._meridiem
  ), g = Ge(o).era, g !== null && (o._a[bo] = o._locale.erasConvertYear(g, o._a[bo])), Rc(o), Ac(o);
}
function f1(o, e, t) {
  var n;
  return t == null ? e : o.meridiemHour != null ? o.meridiemHour(e, t) : (o.isPM != null && (n = o.isPM(t), n && e < 12 && (e += 12), !n && e === 12 && (e = 0)), e);
}
function m1(o) {
  var e, t, n, r, a, c, u = !1, p = o._f.length;
  if (p === 0) {
    Ge(o).invalidFormat = !0, o._d = /* @__PURE__ */ new Date(NaN);
    return;
  }
  for (r = 0; r < p; r++)
    a = 0, c = !1, e = kc({}, o), o._useUTC != null && (e._useUTC = o._useUTC), e._f = o._f[r], Ic(e), wc(e) && (c = !0), a += Ge(e).charsLeftOver, a += Ge(e).unusedTokens.length * 10, Ge(e).score = a, u ? a < n && (n = a, t = e) : (n == null || a < n || c) && (n = a, t = e, c && (u = !0));
  Vi(o, t || e);
}
function g1(o) {
  if (!o._d) {
    var e = Sc(o._i), t = e.day === void 0 ? e.date : e.day;
    o._a = bu(
      [e.year, e.month, t, e.hour, e.minute, e.second, e.millisecond],
      function(n) {
        return n && parseInt(n, 10);
      }
    ), Rc(o);
  }
}
function b1(o) {
  var e = new fa(Ac(ju(o)));
  return e._nextDay && (e.add(1, "d"), e._nextDay = void 0), e;
}
function ju(o) {
  var e = o._i, t = o._f;
  return o._locale = o._locale || Ti(o._l), e === null || t === void 0 && e === "" ? Za({ nullInput: !0 }) : (typeof e == "string" && (o._i = e = o._locale.preparse(e)), _n(e) ? new fa(Ac(e)) : (pa(e) ? o._d = e : Tn(t) ? m1(o) : t ? Ic(o) : v1(o), wc(o) || (o._d = null), o));
}
function v1(o) {
  var e = o._i;
  No(e) ? o._d = new Date(ue.now()) : pa(e) ? o._d = new Date(e.valueOf()) : typeof e == "string" ? h1(o) : Tn(e) ? (o._a = bu(e.slice(0), function(t) {
    return parseInt(t, 10);
  }), Rc(o)) : Or(e) ? g1(o) : Ci(e) ? o._d = new Date(e) : ue.createFromInputFallback(o);
}
function $u(o, e, t, n, r) {
  var a = {};
  return (e === !0 || e === !1) && (n = e, e = void 0), (t === !0 || t === !1) && (n = t, t = void 0), (Or(o) && yc(o) || Tn(o) && o.length === 0) && (o = void 0), a._isAMomentObject = !0, a._useUTC = a._isUTC = r, a._l = t, a._i = o, a._f = e, a._strict = n, b1(a);
}
function Lt(o, e, t, n) {
  return $u(o, e, t, n, !1);
}
var y1 = an(
  "moment().min is deprecated, use moment.max instead. http://momentjs.com/guides/#/warnings/min-max/",
  function() {
    var o = Lt.apply(null, arguments);
    return this.isValid() && o.isValid() ? o < this ? this : o : Za();
  }
), w1 = an(
  "moment().max is deprecated, use moment.min instead. http://momentjs.com/guides/#/warnings/min-max/",
  function() {
    var o = Lt.apply(null, arguments);
    return this.isValid() && o.isValid() ? o > this ? this : o : Za();
  }
);
function Yu(o, e) {
  var t, n;
  if (e.length === 1 && Tn(e[0]) && (e = e[0]), !e.length)
    return Lt();
  for (t = e[0], n = 1; n < e.length; ++n)
    (!e[n].isValid() || e[n][o](t)) && (t = e[n]);
  return t;
}
function k1() {
  var o = [].slice.call(arguments, 0);
  return Yu("isBefore", o);
}
function x1() {
  var o = [].slice.call(arguments, 0);
  return Yu("isAfter", o);
}
var C1 = function() {
  return Date.now ? Date.now() : +/* @__PURE__ */ new Date();
}, Vs = [
  "year",
  "quarter",
  "month",
  "week",
  "day",
  "hour",
  "minute",
  "second",
  "millisecond"
];
function S1(o) {
  var e, t = !1, n, r = Vs.length;
  for (e in o)
    if (ct(o, e) && !(Gt.call(Vs, e) !== -1 && (o[e] == null || !isNaN(o[e]))))
      return !1;
  for (n = 0; n < r; ++n)
    if (o[Vs[n]]) {
      if (t)
        return !1;
      parseFloat(o[Vs[n]]) !== tt(o[Vs[n]]) && (t = !0);
    }
  return !0;
}
function T1() {
  return this._isValid;
}
function _1() {
  return Ln(NaN);
}
function nl(o) {
  var e = Sc(o), t = e.year || 0, n = e.quarter || 0, r = e.month || 0, a = e.week || e.isoWeek || 0, c = e.day || 0, u = e.hour || 0, p = e.minute || 0, g = e.second || 0, v = e.millisecond || 0;
  this._isValid = S1(e), this._milliseconds = +v + g * 1e3 + // 1000
  p * 6e4 + // 1000 * 60
  u * 1e3 * 60 * 60, this._days = +c + a * 7, this._months = +r + n * 3 + t * 12, this._data = {}, this._locale = Ti(), this._bubble();
}
function Da(o) {
  return o instanceof nl;
}
function Xl(o) {
  return o < 0 ? Math.round(-1 * o) * -1 : Math.round(o);
}
function E1(o, e, t) {
  var n = Math.min(o.length, e.length), r = Math.abs(o.length - e.length), a = 0, c;
  for (c = 0; c < n; c++)
    (t && o[c] !== e[c] || !t && tt(o[c]) !== tt(e[c])) && a++;
  return a + r;
}
function Fu(o, e) {
  Pe(o, 0, 0, function() {
    var t = this.utcOffset(), n = "+";
    return t < 0 && (t = -t, n = "-"), n + jn(~~(t / 60), 2) + e + jn(~~t % 60, 2);
  });
}
Fu("Z", ":");
Fu("ZZ", "");
Le("Z", el);
Le("ZZ", el);
kt(["Z", "ZZ"], function(o, e, t) {
  t._useUTC = !0, t._tzm = Nc(el, o);
});
var M1 = /([\+\-]|\d\d)/gi;
function Nc(o, e) {
  var t = (e || "").match(o), n, r, a;
  return t === null ? null : (n = t[t.length - 1] || [], r = (n + "").match(M1) || ["-", 0, 0], a = +(r[1] * 60) + tt(r[2]), a === 0 ? 0 : r[0] === "+" ? a : -a);
}
function Pc(o, e) {
  var t, n;
  return e._isUTC ? (t = e.clone(), n = (_n(o) || pa(o) ? o.valueOf() : Lt(o).valueOf()) - t.valueOf(), t._d.setTime(t._d.valueOf() + n), ue.updateOffset(t, !1), t) : Lt(o).local();
}
function Jl(o) {
  return -Math.round(o._d.getTimezoneOffset());
}
ue.updateOffset = function() {
};
function D1(o, e, t) {
  var n = this._offset || 0, r;
  if (!this.isValid())
    return o != null ? this : NaN;
  if (o != null) {
    if (typeof o == "string") {
      if (o = Nc(el, o), o === null)
        return this;
    } else
      Math.abs(o) < 16 && !t && (o = o * 60);
    return !this._isUTC && e && (r = Jl(this)), this._offset = o, this._isUTC = !0, r != null && this.add(r, "m"), n !== o && (!e || this._changeInProgress ? Uu(
      this,
      Ln(o - n, "m"),
      1,
      !1
    ) : this._changeInProgress || (this._changeInProgress = !0, ue.updateOffset(this, !0), this._changeInProgress = null)), this;
  } else
    return this._isUTC ? n : Jl(this);
}
function L1(o, e) {
  return o != null ? (typeof o != "string" && (o = -o), this.utcOffset(o, e), this) : -this.utcOffset();
}
function O1(o) {
  return this.utcOffset(0, o);
}
function B1(o) {
  return this._isUTC && (this.utcOffset(0, o), this._isUTC = !1, o && this.subtract(Jl(this), "m")), this;
}
function A1() {
  if (this._tzm != null)
    this.utcOffset(this._tzm, !1, !0);
  else if (typeof this._i == "string") {
    var o = Nc(Jb, this._i);
    o != null ? this.utcOffset(o) : this.utcOffset(0, !0);
  }
  return this;
}
function R1(o) {
  return this.isValid() ? (o = o ? Lt(o).utcOffset() : 0, (this.utcOffset() - o) % 60 === 0) : !1;
}
function I1() {
  return this.utcOffset() > this.clone().month(0).utcOffset() || this.utcOffset() > this.clone().month(5).utcOffset();
}
function N1() {
  if (!No(this._isDSTShifted))
    return this._isDSTShifted;
  var o = {}, e;
  return kc(o, this), o = ju(o), o._a ? (e = o._isUTC ? Yn(o._a) : Lt(o._a), this._isDSTShifted = this.isValid() && E1(o._a, e.toArray()) > 0) : this._isDSTShifted = !1, this._isDSTShifted;
}
function P1() {
  return this.isValid() ? !this._isUTC : !1;
}
function H1() {
  return this.isValid() ? this._isUTC : !1;
}
function zu() {
  return this.isValid() ? this._isUTC && this._offset === 0 : !1;
}
var j1 = /^(-|\+)?(?:(\d*)[. ])?(\d+):(\d+)(?::(\d+)(\.\d*)?)?$/, $1 = /^(-|\+)?P(?:([-+]?[0-9,.]*)Y)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)W)?(?:([-+]?[0-9,.]*)D)?(?:T(?:([-+]?[0-9,.]*)H)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)S)?)?$/;
function Ln(o, e) {
  var t = o, n = null, r, a, c;
  return Da(o) ? t = {
    ms: o._milliseconds,
    d: o._days,
    M: o._months
  } : Ci(o) || !isNaN(+o) ? (t = {}, e ? t[e] = +o : t.milliseconds = +o) : (n = j1.exec(o)) ? (r = n[1] === "-" ? -1 : 1, t = {
    y: 0,
    d: tt(n[Pn]) * r,
    h: tt(n[eo]) * r,
    m: tt(n[xn]) * r,
    s: tt(n[gi]) * r,
    ms: tt(Xl(n[Mr] * 1e3)) * r
    // the millisecond decimal point is included in the match
  }) : (n = $1.exec(o)) ? (r = n[1] === "-" ? -1 : 1, t = {
    y: Tr(n[2], r),
    M: Tr(n[3], r),
    w: Tr(n[4], r),
    d: Tr(n[5], r),
    h: Tr(n[6], r),
    m: Tr(n[7], r),
    s: Tr(n[8], r)
  }) : t == null ? t = {} : typeof t == "object" && ("from" in t || "to" in t) && (c = Y1(
    Lt(t.from),
    Lt(t.to)
  ), t = {}, t.ms = c.milliseconds, t.M = c.months), a = new nl(t), Da(o) && ct(o, "_locale") && (a._locale = o._locale), Da(o) && ct(o, "_isValid") && (a._isValid = o._isValid), a;
}
Ln.fn = nl.prototype;
Ln.invalid = _1;
function Tr(o, e) {
  var t = o && parseFloat(o.replace(",", "."));
  return (isNaN(t) ? 0 : t) * e;
}
function oh(o, e) {
  var t = {};
  return t.months = e.month() - o.month() + (e.year() - o.year()) * 12, o.clone().add(t.months, "M").isAfter(e) && --t.months, t.milliseconds = +e - +o.clone().add(t.months, "M"), t;
}
function Y1(o, e) {
  var t;
  return o.isValid() && e.isValid() ? (e = Pc(e, o), o.isBefore(e) ? t = oh(o, e) : (t = oh(e, o), t.milliseconds = -t.milliseconds, t.months = -t.months), t) : { milliseconds: 0, months: 0 };
}
function Wu(o, e) {
  return function(t, n) {
    var r, a;
    return n !== null && !isNaN(+n) && (yu(
      e,
      "moment()." + e + "(period, number) is deprecated. Please use moment()." + e + "(number, period). See http://momentjs.com/guides/#/warnings/add-inverted-param/ for more info."
    ), a = t, t = n, n = a), r = Ln(t, n), Uu(this, r, o), this;
  };
}
function Uu(o, e, t, n) {
  var r = e._milliseconds, a = Xl(e._days), c = Xl(e._months);
  o.isValid() && (n = n ?? !0, c && Du(o, ia(o, "Month") + c * t), a && _u(o, "Date", ia(o, "Date") + a * t), r && o._d.setTime(o._d.valueOf() + r * t), n && ue.updateOffset(o, a || c));
}
var F1 = Wu(1, "add"), z1 = Wu(-1, "subtract");
function qu(o) {
  return typeof o == "string" || o instanceof String;
}
function W1(o) {
  return _n(o) || pa(o) || qu(o) || Ci(o) || q1(o) || U1(o) || o === null || o === void 0;
}
function U1(o) {
  var e = Or(o) && !yc(o), t = !1, n = [
    "years",
    "year",
    "y",
    "months",
    "month",
    "M",
    "days",
    "day",
    "d",
    "dates",
    "date",
    "D",
    "hours",
    "hour",
    "h",
    "minutes",
    "minute",
    "m",
    "seconds",
    "second",
    "s",
    "milliseconds",
    "millisecond",
    "ms"
  ], r, a, c = n.length;
  for (r = 0; r < c; r += 1)
    a = n[r], t = t || ct(o, a);
  return e && t;
}
function q1(o) {
  var e = Tn(o), t = !1;
  return e && (t = o.filter(function(n) {
    return !Ci(n) && qu(o);
  }).length === 0), e && t;
}
function V1(o) {
  var e = Or(o) && !yc(o), t = !1, n = [
    "sameDay",
    "nextDay",
    "lastDay",
    "nextWeek",
    "lastWeek",
    "sameElse"
  ], r, a;
  for (r = 0; r < n.length; r += 1)
    a = n[r], t = t || ct(o, a);
  return e && t;
}
function G1(o, e) {
  var t = o.diff(e, "days", !0);
  return t < -6 ? "sameElse" : t < -1 ? "lastWeek" : t < 0 ? "lastDay" : t < 1 ? "sameDay" : t < 2 ? "nextDay" : t < 7 ? "nextWeek" : "sameElse";
}
function Z1(o, e) {
  arguments.length === 1 && (arguments[0] ? W1(arguments[0]) ? (o = arguments[0], e = void 0) : V1(arguments[0]) && (e = arguments[0], o = void 0) : (o = void 0, e = void 0));
  var t = o || Lt(), n = Pc(t, this).startOf("day"), r = ue.calendarFormat(this, n) || "sameElse", a = e && (Fn(e[r]) ? e[r].call(this, t) : e[r]);
  return this.format(
    a || this.localeData().calendar(r, this, Lt(t))
  );
}
function K1() {
  return new fa(this);
}
function X1(o, e) {
  var t = _n(o) ? o : Lt(o);
  return this.isValid() && t.isValid() ? (e = ln(e) || "millisecond", e === "millisecond" ? this.valueOf() > t.valueOf() : t.valueOf() < this.clone().startOf(e).valueOf()) : !1;
}
function J1(o, e) {
  var t = _n(o) ? o : Lt(o);
  return this.isValid() && t.isValid() ? (e = ln(e) || "millisecond", e === "millisecond" ? this.valueOf() < t.valueOf() : this.clone().endOf(e).valueOf() < t.valueOf()) : !1;
}
function Q1(o, e, t, n) {
  var r = _n(o) ? o : Lt(o), a = _n(e) ? e : Lt(e);
  return this.isValid() && r.isValid() && a.isValid() ? (n = n || "()", (n[0] === "(" ? this.isAfter(r, t) : !this.isBefore(r, t)) && (n[1] === ")" ? this.isBefore(a, t) : !this.isAfter(a, t))) : !1;
}
function ev(o, e) {
  var t = _n(o) ? o : Lt(o), n;
  return this.isValid() && t.isValid() ? (e = ln(e) || "millisecond", e === "millisecond" ? this.valueOf() === t.valueOf() : (n = t.valueOf(), this.clone().startOf(e).valueOf() <= n && n <= this.clone().endOf(e).valueOf())) : !1;
}
function tv(o, e) {
  return this.isSame(o, e) || this.isAfter(o, e);
}
function ov(o, e) {
  return this.isSame(o, e) || this.isBefore(o, e);
}
function nv(o, e, t) {
  var n, r, a;
  if (!this.isValid())
    return NaN;
  if (n = Pc(o, this), !n.isValid())
    return NaN;
  switch (r = (n.utcOffset() - this.utcOffset()) * 6e4, e = ln(e), e) {
    case "year":
      a = La(this, n) / 12;
      break;
    case "month":
      a = La(this, n);
      break;
    case "quarter":
      a = La(this, n) / 3;
      break;
    case "second":
      a = (this - n) / 1e3;
      break;
    case "minute":
      a = (this - n) / 6e4;
      break;
    case "hour":
      a = (this - n) / 36e5;
      break;
    case "day":
      a = (this - n - r) / 864e5;
      break;
    case "week":
      a = (this - n - r) / 6048e5;
      break;
    default:
      a = this - n;
  }
  return t ? a : nn(a);
}
function La(o, e) {
  if (o.date() < e.date())
    return -La(e, o);
  var t = (e.year() - o.year()) * 12 + (e.month() - o.month()), n = o.clone().add(t, "months"), r, a;
  return e - n < 0 ? (r = o.clone().add(t - 1, "months"), a = (e - n) / (n - r)) : (r = o.clone().add(t + 1, "months"), a = (e - n) / (r - n)), -(t + a) || 0;
}
ue.defaultFormat = "YYYY-MM-DDTHH:mm:ssZ";
ue.defaultFormatUtc = "YYYY-MM-DDTHH:mm:ss[Z]";
function iv() {
  return this.clone().locale("en").format("ddd MMM DD YYYY HH:mm:ss [GMT]ZZ");
}
function rv(o) {
  if (!this.isValid())
    return null;
  var e = o !== !0, t = e ? this.clone().utc() : this;
  return t.year() < 0 || t.year() > 9999 ? Ma(
    t,
    e ? "YYYYYY-MM-DD[T]HH:mm:ss.SSS[Z]" : "YYYYYY-MM-DD[T]HH:mm:ss.SSSZ"
  ) : Fn(Date.prototype.toISOString) ? e ? this.toDate().toISOString() : new Date(this.valueOf() + this.utcOffset() * 60 * 1e3).toISOString().replace("Z", Ma(t, "Z")) : Ma(
    t,
    e ? "YYYY-MM-DD[T]HH:mm:ss.SSS[Z]" : "YYYY-MM-DD[T]HH:mm:ss.SSSZ"
  );
}
function sv() {
  if (!this.isValid())
    return "moment.invalid(/* " + this._i + " */)";
  var o = "moment", e = "", t, n, r, a;
  return this.isLocal() || (o = this.utcOffset() === 0 ? "moment.utc" : "moment.parseZone", e = "Z"), t = "[" + o + '("]', n = 0 <= this.year() && this.year() <= 9999 ? "YYYY" : "YYYYYY", r = "-MM-DD[T]HH:mm:ss.SSS", a = e + '[")]', this.format(t + n + r + a);
}
function av(o) {
  o || (o = this.isUtc() ? ue.defaultFormatUtc : ue.defaultFormat);
  var e = Ma(this, o);
  return this.localeData().postformat(e);
}
function lv(o, e) {
  return this.isValid() && (_n(o) && o.isValid() || Lt(o).isValid()) ? Ln({ to: this, from: o }).locale(this.locale()).humanize(!e) : this.localeData().invalidDate();
}
function cv(o) {
  return this.from(Lt(), o);
}
function dv(o, e) {
  return this.isValid() && (_n(o) && o.isValid() || Lt(o).isValid()) ? Ln({ from: this, to: o }).locale(this.locale()).humanize(!e) : this.localeData().invalidDate();
}
function hv(o) {
  return this.to(Lt(), o);
}
function Vu(o) {
  var e;
  return o === void 0 ? this._locale._abbr : (e = Ti(o), e != null && (this._locale = e), this);
}
var Gu = an(
  "moment().lang() is deprecated. Instead, use moment().localeData() to get the language configuration. Use moment().locale() to change languages.",
  function(o) {
    return o === void 0 ? this.localeData() : this.locale(o);
  }
);
function Zu() {
  return this._locale;
}
var Pa = 1e3, cs = 60 * Pa, Ha = 60 * cs, Ku = (365 * 400 + 97) * 24 * Ha;
function ds(o, e) {
  return (o % e + e) % e;
}
function Xu(o, e, t) {
  return o < 100 && o >= 0 ? new Date(o + 400, e, t) - Ku : new Date(o, e, t).valueOf();
}
function Ju(o, e, t) {
  return o < 100 && o >= 0 ? Date.UTC(o + 400, e, t) - Ku : Date.UTC(o, e, t);
}
function uv(o) {
  var e, t;
  if (o = ln(o), o === void 0 || o === "millisecond" || !this.isValid())
    return this;
  switch (t = this._isUTC ? Ju : Xu, o) {
    case "year":
      e = t(this.year(), 0, 1);
      break;
    case "quarter":
      e = t(
        this.year(),
        this.month() - this.month() % 3,
        1
      );
      break;
    case "month":
      e = t(this.year(), this.month(), 1);
      break;
    case "week":
      e = t(
        this.year(),
        this.month(),
        this.date() - this.weekday()
      );
      break;
    case "isoWeek":
      e = t(
        this.year(),
        this.month(),
        this.date() - (this.isoWeekday() - 1)
      );
      break;
    case "day":
    case "date":
      e = t(this.year(), this.month(), this.date());
      break;
    case "hour":
      e = this._d.valueOf(), e -= ds(
        e + (this._isUTC ? 0 : this.utcOffset() * cs),
        Ha
      );
      break;
    case "minute":
      e = this._d.valueOf(), e -= ds(e, cs);
      break;
    case "second":
      e = this._d.valueOf(), e -= ds(e, Pa);
      break;
  }
  return this._d.setTime(e), ue.updateOffset(this, !0), this;
}
function pv(o) {
  var e, t;
  if (o = ln(o), o === void 0 || o === "millisecond" || !this.isValid())
    return this;
  switch (t = this._isUTC ? Ju : Xu, o) {
    case "year":
      e = t(this.year() + 1, 0, 1) - 1;
      break;
    case "quarter":
      e = t(
        this.year(),
        this.month() - this.month() % 3 + 3,
        1
      ) - 1;
      break;
    case "month":
      e = t(this.year(), this.month() + 1, 1) - 1;
      break;
    case "week":
      e = t(
        this.year(),
        this.month(),
        this.date() - this.weekday() + 7
      ) - 1;
      break;
    case "isoWeek":
      e = t(
        this.year(),
        this.month(),
        this.date() - (this.isoWeekday() - 1) + 7
      ) - 1;
      break;
    case "day":
    case "date":
      e = t(this.year(), this.month(), this.date() + 1) - 1;
      break;
    case "hour":
      e = this._d.valueOf(), e += Ha - ds(
        e + (this._isUTC ? 0 : this.utcOffset() * cs),
        Ha
      ) - 1;
      break;
    case "minute":
      e = this._d.valueOf(), e += cs - ds(e, cs) - 1;
      break;
    case "second":
      e = this._d.valueOf(), e += Pa - ds(e, Pa) - 1;
      break;
  }
  return this._d.setTime(e), ue.updateOffset(this, !0), this;
}
function fv() {
  return this._d.valueOf() - (this._offset || 0) * 6e4;
}
function mv() {
  return Math.floor(this.valueOf() / 1e3);
}
function gv() {
  return new Date(this.valueOf());
}
function bv() {
  var o = this;
  return [
    o.year(),
    o.month(),
    o.date(),
    o.hour(),
    o.minute(),
    o.second(),
    o.millisecond()
  ];
}
function vv() {
  var o = this;
  return {
    years: o.year(),
    months: o.month(),
    date: o.date(),
    hours: o.hours(),
    minutes: o.minutes(),
    seconds: o.seconds(),
    milliseconds: o.milliseconds()
  };
}
function yv() {
  return this.isValid() ? this.toISOString() : null;
}
function wv() {
  return wc(this);
}
function kv() {
  return Vi({}, Ge(this));
}
function xv() {
  return Ge(this).overflow;
}
function Cv() {
  return {
    input: this._i,
    format: this._f,
    locale: this._locale,
    isUTC: this._isUTC,
    strict: this._strict
  };
}
Pe("N", 0, 0, "eraAbbr");
Pe("NN", 0, 0, "eraAbbr");
Pe("NNN", 0, 0, "eraAbbr");
Pe("NNNN", 0, 0, "eraName");
Pe("NNNNN", 0, 0, "eraNarrow");
Pe("y", ["y", 1], "yo", "eraYear");
Pe("y", ["yy", 2], 0, "eraYear");
Pe("y", ["yyy", 3], 0, "eraYear");
Pe("y", ["yyyy", 4], 0, "eraYear");
Le("N", Hc);
Le("NN", Hc);
Le("NNN", Hc);
Le("NNNN", Rv);
Le("NNNNN", Iv);
kt(
  ["N", "NN", "NNN", "NNNN", "NNNNN"],
  function(o, e, t, n) {
    var r = t._locale.erasParse(o, n, t._strict);
    r ? Ge(t).era = r : Ge(t).invalidEra = o;
  }
);
Le("y", vs);
Le("yy", vs);
Le("yyy", vs);
Le("yyyy", vs);
Le("yo", Nv);
kt(["y", "yy", "yyy", "yyyy"], bo);
kt(["yo"], function(o, e, t, n) {
  var r;
  t._locale._eraYearOrdinalRegex && (r = o.match(t._locale._eraYearOrdinalRegex)), t._locale.eraYearOrdinalParse ? e[bo] = t._locale.eraYearOrdinalParse(o, r) : e[bo] = parseInt(o, 10);
});
function Sv(o, e) {
  var t, n, r, a = this._eras || Ti("en")._eras;
  for (t = 0, n = a.length; t < n; ++t) {
    switch (typeof a[t].since) {
      case "string":
        r = ue(a[t].since).startOf("day"), a[t].since = r.valueOf();
        break;
    }
    switch (typeof a[t].until) {
      case "undefined":
        a[t].until = 1 / 0;
        break;
      case "string":
        r = ue(a[t].until).startOf("day").valueOf(), a[t].until = r.valueOf();
        break;
    }
  }
  return a;
}
function Tv(o, e, t) {
  var n, r, a = this.eras(), c, u, p;
  for (o = o.toUpperCase(), n = 0, r = a.length; n < r; ++n)
    if (c = a[n].name.toUpperCase(), u = a[n].abbr.toUpperCase(), p = a[n].narrow.toUpperCase(), t)
      switch (e) {
        case "N":
        case "NN":
        case "NNN":
          if (u === o)
            return a[n];
          break;
        case "NNNN":
          if (c === o)
            return a[n];
          break;
        case "NNNNN":
          if (p === o)
            return a[n];
          break;
      }
    else if ([c, u, p].indexOf(o) >= 0)
      return a[n];
}
function _v(o, e) {
  var t = o.since <= o.until ? 1 : -1;
  return e === void 0 ? ue(o.since).year() : ue(o.since).year() + (e - o.offset) * t;
}
function Ev() {
  var o, e, t, n = this.localeData().eras();
  for (o = 0, e = n.length; o < e; ++o)
    if (t = this.clone().startOf("day").valueOf(), n[o].since <= t && t <= n[o].until || n[o].until <= t && t <= n[o].since)
      return n[o].name;
  return "";
}
function Mv() {
  var o, e, t, n = this.localeData().eras();
  for (o = 0, e = n.length; o < e; ++o)
    if (t = this.clone().startOf("day").valueOf(), n[o].since <= t && t <= n[o].until || n[o].until <= t && t <= n[o].since)
      return n[o].narrow;
  return "";
}
function Dv() {
  var o, e, t, n = this.localeData().eras();
  for (o = 0, e = n.length; o < e; ++o)
    if (t = this.clone().startOf("day").valueOf(), n[o].since <= t && t <= n[o].until || n[o].until <= t && t <= n[o].since)
      return n[o].abbr;
  return "";
}
function Lv() {
  var o, e, t, n, r = this.localeData().eras();
  for (o = 0, e = r.length; o < e; ++o)
    if (t = r[o].since <= r[o].until ? 1 : -1, n = this.clone().startOf("day").valueOf(), r[o].since <= n && n <= r[o].until || r[o].until <= n && n <= r[o].since)
      return (this.year() - ue(r[o].since).year()) * t + r[o].offset;
  return this.year();
}
function Ov(o) {
  return ct(this, "_erasNameRegex") || jc.call(this), o ? this._erasNameRegex : this._erasRegex;
}
function Bv(o) {
  return ct(this, "_erasAbbrRegex") || jc.call(this), o ? this._erasAbbrRegex : this._erasRegex;
}
function Av(o) {
  return ct(this, "_erasNarrowRegex") || jc.call(this), o ? this._erasNarrowRegex : this._erasRegex;
}
function Hc(o, e) {
  return e.erasAbbrRegex(o);
}
function Rv(o, e) {
  return e.erasNameRegex(o);
}
function Iv(o, e) {
  return e.erasNarrowRegex(o);
}
function Nv(o, e) {
  return e._eraYearOrdinalRegex || vs;
}
function jc() {
  var o = [], e = [], t = [], n = [], r, a, c, u, p, g = this.eras();
  for (r = 0, a = g.length; r < a; ++r)
    c = yi(g[r].name), u = yi(g[r].abbr), p = yi(g[r].narrow), e.push(c), o.push(u), t.push(p), n.push(c), n.push(u), n.push(p);
  this._erasRegex = new RegExp("^(" + n.join("|") + ")", "i"), this._erasNameRegex = new RegExp("^(" + e.join("|") + ")", "i"), this._erasAbbrRegex = new RegExp("^(" + o.join("|") + ")", "i"), this._erasNarrowRegex = new RegExp(
    "^(" + t.join("|") + ")",
    "i"
  );
}
Pe(0, ["gg", 2], 0, function() {
  return this.weekYear() % 100;
});
Pe(0, ["GG", 2], 0, function() {
  return this.isoWeekYear() % 100;
});
function il(o, e) {
  Pe(0, [o, o.length], 0, e);
}
il("gggg", "weekYear");
il("ggggg", "weekYear");
il("GGGG", "isoWeekYear");
il("GGGGG", "isoWeekYear");
Le("G", Qa);
Le("g", Qa);
Le("GG", Bt, Xo);
Le("gg", Bt, Xo);
Le("GGGG", _c, Tc);
Le("gggg", _c, Tc);
Le("GGGGG", Ja, Ka);
Le("ggggg", Ja, Ka);
ga(
  ["gggg", "ggggg", "GGGG", "GGGGG"],
  function(o, e, t, n) {
    e[n.substr(0, 2)] = tt(o);
  }
);
ga(["gg", "GG"], function(o, e, t, n) {
  e[n] = ue.parseTwoDigitYear(o);
});
function Pv(o) {
  return Qu.call(
    this,
    o,
    this.week(),
    this.weekday() + this.localeData()._week.dow,
    this.localeData()._week.dow,
    this.localeData()._week.doy
  );
}
function Hv(o) {
  return Qu.call(
    this,
    o,
    this.isoWeek(),
    this.isoWeekday(),
    1,
    4
  );
}
function jv() {
  return wi(this.year(), 1, 4);
}
function $v() {
  return wi(this.isoWeekYear(), 1, 4);
}
function Yv() {
  var o = this.localeData()._week;
  return wi(this.year(), o.dow, o.doy);
}
function Fv() {
  var o = this.localeData()._week;
  return wi(this.weekYear(), o.dow, o.doy);
}
function Qu(o, e, t, n, r) {
  var a;
  return o == null ? sa(this, n, r).year : (a = wi(o, n, r), e > a && (e = a), zv.call(this, o, e, t, n, r));
}
function zv(o, e, t, n, r) {
  var a = Bu(o, e, t, n, r), c = ra(a.year, 0, a.dayOfYear);
  return this.year(c.getUTCFullYear()), this.month(c.getUTCMonth()), this.date(c.getUTCDate()), this;
}
Pe("Q", 0, "Qo", "quarter");
Le("Q", ku);
kt("Q", function(o, e) {
  e[mi] = (tt(o) - 1) * 3;
});
function Wv(o) {
  return o == null ? Math.ceil((this.month() + 1) / 3) : this.month((o - 1) * 3 + this.month() % 3);
}
Pe("D", ["DD", 2], "Do", "date");
Le("D", Bt, ys);
Le("DD", Bt, Xo);
Le("Do", function(o, e) {
  return o ? e._dayOfMonthOrdinalParse || e._ordinalParse : e._dayOfMonthOrdinalParseLenient;
});
kt(["D", "DD"], Pn);
kt("Do", function(o, e) {
  e[Pn] = tt(o.match(Bt)[0]);
});
var ep = ws("Date", !0);
Pe("DDD", ["DDDD", 3], "DDDo", "dayOfYear");
Le("DDD", Xa);
Le("DDDD", xu);
kt(["DDD", "DDDD"], function(o, e, t) {
  t._dayOfYear = tt(o);
});
function Uv(o) {
  var e = Math.round(
    (this.clone().startOf("day") - this.clone().startOf("year")) / 864e5
  ) + 1;
  return o == null ? e : this.add(o - e, "d");
}
Pe("m", ["mm", 2], 0, "minute");
Le("m", Bt, Ec);
Le("mm", Bt, Xo);
kt(["m", "mm"], xn);
var qv = ws("Minutes", !1);
Pe("s", ["ss", 2], 0, "second");
Le("s", Bt, Ec);
Le("ss", Bt, Xo);
kt(["s", "ss"], gi);
var Vv = ws("Seconds", !1);
Pe("S", 0, 0, function() {
  return ~~(this.millisecond() / 100);
});
Pe(0, ["SS", 2], 0, function() {
  return ~~(this.millisecond() / 10);
});
Pe(0, ["SSS", 3], 0, "millisecond");
Pe(0, ["SSSS", 4], 0, function() {
  return this.millisecond() * 10;
});
Pe(0, ["SSSSS", 5], 0, function() {
  return this.millisecond() * 100;
});
Pe(0, ["SSSSSS", 6], 0, function() {
  return this.millisecond() * 1e3;
});
Pe(0, ["SSSSSSS", 7], 0, function() {
  return this.millisecond() * 1e4;
});
Pe(0, ["SSSSSSSS", 8], 0, function() {
  return this.millisecond() * 1e5;
});
Pe(0, ["SSSSSSSSS", 9], 0, function() {
  return this.millisecond() * 1e6;
});
Le("S", Xa, ku);
Le("SS", Xa, Xo);
Le("SSS", Xa, xu);
var Gi, tp;
for (Gi = "SSSS"; Gi.length <= 9; Gi += "S")
  Le(Gi, vs);
function Gv(o, e) {
  e[Mr] = tt(("0." + o) * 1e3);
}
for (Gi = "S"; Gi.length <= 9; Gi += "S")
  kt(Gi, Gv);
tp = ws("Milliseconds", !1);
Pe("z", 0, 0, "zoneAbbr");
Pe("zz", 0, 0, "zoneName");
function Zv() {
  return this._isUTC ? "UTC" : "";
}
function Kv() {
  return this._isUTC ? "Coordinated Universal Time" : "";
}
var ve = fa.prototype;
ve.add = F1;
ve.calendar = Z1;
ve.clone = K1;
ve.diff = nv;
ve.endOf = pv;
ve.format = av;
ve.from = lv;
ve.fromNow = cv;
ve.to = dv;
ve.toNow = hv;
ve.get = s0;
ve.invalidAt = xv;
ve.isAfter = X1;
ve.isBefore = J1;
ve.isBetween = Q1;
ve.isSame = ev;
ve.isSameOrAfter = tv;
ve.isSameOrBefore = ov;
ve.isValid = wv;
ve.lang = Gu;
ve.locale = Vu;
ve.localeData = Zu;
ve.max = w1;
ve.min = y1;
ve.parsingFlags = kv;
ve.set = a0;
ve.startOf = uv;
ve.subtract = z1;
ve.toArray = bv;
ve.toObject = vv;
ve.toDate = gv;
ve.toISOString = rv;
ve.inspect = sv;
typeof Symbol < "u" && Symbol.for != null && (ve[Symbol.for("nodejs.util.inspect.custom")] = function() {
  return "Moment<" + this.format() + ">";
});
ve.toJSON = yv;
ve.toString = iv;
ve.unix = mv;
ve.valueOf = fv;
ve.creationData = Cv;
ve.eraName = Ev;
ve.eraNarrow = Mv;
ve.eraAbbr = Dv;
ve.eraYear = Lv;
ve.year = Tu;
ve.isLeapYear = r0;
ve.weekYear = Pv;
ve.isoWeekYear = Hv;
ve.quarter = ve.quarters = Wv;
ve.month = Lu;
ve.daysInMonth = g0;
ve.week = ve.weeks = S0;
ve.isoWeek = ve.isoWeeks = T0;
ve.weeksInYear = Yv;
ve.weeksInWeekYear = Fv;
ve.isoWeeksInYear = jv;
ve.isoWeeksInISOWeekYear = $v;
ve.date = ep;
ve.day = ve.days = H0;
ve.weekday = j0;
ve.isoWeekday = $0;
ve.dayOfYear = Uv;
ve.hour = ve.hours = V0;
ve.minute = ve.minutes = qv;
ve.second = ve.seconds = Vv;
ve.millisecond = ve.milliseconds = tp;
ve.utcOffset = D1;
ve.utc = O1;
ve.local = B1;
ve.parseZone = A1;
ve.hasAlignedHourOffset = R1;
ve.isDST = I1;
ve.isLocal = P1;
ve.isUtcOffset = H1;
ve.isUtc = zu;
ve.isUTC = zu;
ve.zoneAbbr = Zv;
ve.zoneName = Kv;
ve.dates = an(
  "dates accessor is deprecated. Use date instead.",
  ep
);
ve.months = an(
  "months accessor is deprecated. Use month instead",
  Lu
);
ve.years = an(
  "years accessor is deprecated. Use year instead",
  Tu
);
ve.zone = an(
  "moment().zone is deprecated, use moment().utcOffset instead. http://momentjs.com/guides/#/warnings/zone/",
  L1
);
ve.isDSTShifted = an(
  "isDSTShifted is deprecated. See http://momentjs.com/guides/#/warnings/dst-shifted/ for more information",
  N1
);
function Xv(o) {
  return Lt(o * 1e3);
}
function Jv() {
  return Lt.apply(null, arguments).parseZone();
}
function op(o) {
  return o;
}
var ht = xc.prototype;
ht.calendar = Pb;
ht.longDateFormat = Yb;
ht.invalidDate = zb;
ht.ordinal = qb;
ht.preparse = op;
ht.postformat = op;
ht.relativeTime = Gb;
ht.pastFuture = Zb;
ht.set = Ib;
ht.eras = Sv;
ht.erasParse = Tv;
ht.erasConvertYear = _v;
ht.erasAbbrRegex = Bv;
ht.erasNameRegex = Ov;
ht.erasNarrowRegex = Av;
ht.months = u0;
ht.monthsShort = p0;
ht.monthsParse = m0;
ht.monthsRegex = v0;
ht.monthsShortRegex = b0;
ht.week = w0;
ht.firstDayOfYear = C0;
ht.firstDayOfWeek = x0;
ht.weekdays = A0;
ht.weekdaysMin = I0;
ht.weekdaysShort = R0;
ht.weekdaysParse = P0;
ht.weekdaysRegex = Y0;
ht.weekdaysShortRegex = F0;
ht.weekdaysMinRegex = z0;
ht.isPM = U0;
ht.meridiem = G0;
function ja(o, e, t, n) {
  var r = Ti(), a = Yn().set(n, e);
  return r[t](a, o);
}
function np(o, e, t) {
  if (Ci(o) && (e = o, o = void 0), o = o || "", e != null)
    return ja(o, e, t, "month");
  var n, r = [];
  for (n = 0; n < 12; n++)
    r[n] = ja(o, n, t, "month");
  return r;
}
function $c(o, e, t, n) {
  typeof o == "boolean" ? (Ci(e) && (t = e, e = void 0), e = e || "") : (e = o, t = e, o = !1, Ci(e) && (t = e, e = void 0), e = e || "");
  var r = Ti(), a = o ? r._week.dow : 0, c, u = [];
  if (t != null)
    return ja(e, (t + a) % 7, n, "day");
  for (c = 0; c < 7; c++)
    u[c] = ja(e, (c + a) % 7, n, "day");
  return u;
}
function Qv(o, e) {
  return np(o, e, "months");
}
function ey(o, e) {
  return np(o, e, "monthsShort");
}
function ty(o, e, t) {
  return $c(o, e, t, "weekdays");
}
function oy(o, e, t) {
  return $c(o, e, t, "weekdaysShort");
}
function ny(o, e, t) {
  return $c(o, e, t, "weekdaysMin");
}
Xi("en", {
  eras: [
    {
      since: "0001-01-01",
      until: 1 / 0,
      offset: 1,
      name: "Anno Domini",
      narrow: "AD",
      abbr: "AD"
    },
    {
      since: "0000-12-31",
      until: -1 / 0,
      offset: 1,
      name: "Before Christ",
      narrow: "BC",
      abbr: "BC"
    }
  ],
  dayOfMonthOrdinalParse: /\d{1,2}(th|st|nd|rd)/,
  ordinal: function(o) {
    var e = o % 10, t = tt(o % 100 / 10) === 1 ? "th" : e === 1 ? "st" : e === 2 ? "nd" : e === 3 ? "rd" : "th";
    return o + t;
  }
});
ue.lang = an(
  "moment.lang is deprecated. Use moment.locale instead.",
  Xi
);
ue.langData = an(
  "moment.langData is deprecated. Use moment.localeData instead.",
  Ti
);
var li = Math.abs;
function iy() {
  var o = this._data;
  return this._milliseconds = li(this._milliseconds), this._days = li(this._days), this._months = li(this._months), o.milliseconds = li(o.milliseconds), o.seconds = li(o.seconds), o.minutes = li(o.minutes), o.hours = li(o.hours), o.months = li(o.months), o.years = li(o.years), this;
}
function ip(o, e, t, n) {
  var r = Ln(e, t);
  return o._milliseconds += n * r._milliseconds, o._days += n * r._days, o._months += n * r._months, o._bubble();
}
function ry(o, e) {
  return ip(this, o, e, 1);
}
function sy(o, e) {
  return ip(this, o, e, -1);
}
function nh(o) {
  return o < 0 ? Math.floor(o) : Math.ceil(o);
}
function ay() {
  var o = this._milliseconds, e = this._days, t = this._months, n = this._data, r, a, c, u, p;
  return o >= 0 && e >= 0 && t >= 0 || o <= 0 && e <= 0 && t <= 0 || (o += nh(Ql(t) + e) * 864e5, e = 0, t = 0), n.milliseconds = o % 1e3, r = nn(o / 1e3), n.seconds = r % 60, a = nn(r / 60), n.minutes = a % 60, c = nn(a / 60), n.hours = c % 24, e += nn(c / 24), p = nn(rp(e)), t += p, e -= nh(Ql(p)), u = nn(t / 12), t %= 12, n.days = e, n.months = t, n.years = u, this;
}
function rp(o) {
  return o * 4800 / 146097;
}
function Ql(o) {
  return o * 146097 / 4800;
}
function ly(o) {
  if (!this.isValid())
    return NaN;
  var e, t, n = this._milliseconds;
  if (o = ln(o), o === "month" || o === "quarter" || o === "year")
    switch (e = this._days + n / 864e5, t = this._months + rp(e), o) {
      case "month":
        return t;
      case "quarter":
        return t / 3;
      case "year":
        return t / 12;
    }
  else
    switch (e = this._days + Math.round(Ql(this._months)), o) {
      case "week":
        return e / 7 + n / 6048e5;
      case "day":
        return e + n / 864e5;
      case "hour":
        return e * 24 + n / 36e5;
      case "minute":
        return e * 1440 + n / 6e4;
      case "second":
        return e * 86400 + n / 1e3;
      case "millisecond":
        return Math.floor(e * 864e5) + n;
      default:
        throw new Error("Unknown unit " + o);
    }
}
function _i(o) {
  return function() {
    return this.as(o);
  };
}
var sp = _i("ms"), cy = _i("s"), dy = _i("m"), hy = _i("h"), uy = _i("d"), py = _i("w"), fy = _i("M"), my = _i("Q"), gy = _i("y"), by = sp;
function vy() {
  return Ln(this);
}
function yy(o) {
  return o = ln(o), this.isValid() ? this[o + "s"]() : NaN;
}
function Rr(o) {
  return function() {
    return this.isValid() ? this._data[o] : NaN;
  };
}
var wy = Rr("milliseconds"), ky = Rr("seconds"), xy = Rr("minutes"), Cy = Rr("hours"), Sy = Rr("days"), Ty = Rr("months"), _y = Rr("years");
function Ey() {
  return nn(this.days() / 7);
}
var di = Math.round, ss = {
  ss: 44,
  // a few seconds to seconds
  s: 45,
  // seconds to minute
  m: 45,
  // minutes to hour
  h: 22,
  // hours to day
  d: 26,
  // days to month/week
  w: null,
  // weeks to month
  M: 11
  // months to year
};
function My(o, e, t, n, r) {
  return r.relativeTime(e || 1, !!t, o, n);
}
function Dy(o, e, t, n) {
  var r = Ln(o).abs(), a = di(r.as("s")), c = di(r.as("m")), u = di(r.as("h")), p = di(r.as("d")), g = di(r.as("M")), v = di(r.as("w")), k = di(r.as("y")), y = a <= t.ss && ["s", a] || a < t.s && ["ss", a] || c <= 1 && ["m"] || c < t.m && ["mm", c] || u <= 1 && ["h"] || u < t.h && ["hh", u] || p <= 1 && ["d"] || p < t.d && ["dd", p];
  return t.w != null && (y = y || v <= 1 && ["w"] || v < t.w && ["ww", v]), y = y || g <= 1 && ["M"] || g < t.M && ["MM", g] || k <= 1 && ["y"] || ["yy", k], y[2] = e, y[3] = +o > 0, y[4] = n, My.apply(null, y);
}
function Ly(o) {
  return o === void 0 ? di : typeof o == "function" ? (di = o, !0) : !1;
}
function Oy(o, e) {
  return ss[o] === void 0 ? !1 : e === void 0 ? ss[o] : (ss[o] = e, o === "s" && (ss.ss = e - 1), !0);
}
function By(o, e) {
  if (!this.isValid())
    return this.localeData().invalidDate();
  var t = !1, n = ss, r, a;
  return typeof o == "object" && (e = o, o = !1), typeof o == "boolean" && (t = o), typeof e == "object" && (n = Object.assign({}, ss, e), e.s != null && e.ss == null && (n.ss = e.s - 1)), r = this.localeData(), a = Dy(this, !t, n, r), t && (a = r.pastFuture(+this, a)), r.postformat(a);
}
var Ll = Math.abs;
function Zr(o) {
  return (o > 0) - (o < 0) || +o;
}
function rl() {
  if (!this.isValid())
    return this.localeData().invalidDate();
  var o = Ll(this._milliseconds) / 1e3, e = Ll(this._days), t = Ll(this._months), n, r, a, c, u = this.asSeconds(), p, g, v, k;
  return u ? (n = nn(o / 60), r = nn(n / 60), o %= 60, n %= 60, a = nn(t / 12), t %= 12, c = o ? o.toFixed(3).replace(/\.?0+$/, "") : "", p = u < 0 ? "-" : "", g = Zr(this._months) !== Zr(u) ? "-" : "", v = Zr(this._days) !== Zr(u) ? "-" : "", k = Zr(this._milliseconds) !== Zr(u) ? "-" : "", p + "P" + (a ? g + a + "Y" : "") + (t ? g + t + "M" : "") + (e ? v + e + "D" : "") + (r || n || o ? "T" : "") + (r ? k + r + "H" : "") + (n ? k + n + "M" : "") + (o ? k + c + "S" : "")) : "P0D";
}
var st = nl.prototype;
st.isValid = T1;
st.abs = iy;
st.add = ry;
st.subtract = sy;
st.as = ly;
st.asMilliseconds = sp;
st.asSeconds = cy;
st.asMinutes = dy;
st.asHours = hy;
st.asDays = uy;
st.asWeeks = py;
st.asMonths = fy;
st.asQuarters = my;
st.asYears = gy;
st.valueOf = by;
st._bubble = ay;
st.clone = vy;
st.get = yy;
st.milliseconds = wy;
st.seconds = ky;
st.minutes = xy;
st.hours = Cy;
st.days = Sy;
st.weeks = Ey;
st.months = Ty;
st.years = _y;
st.humanize = By;
st.toISOString = rl;
st.toString = rl;
st.toJSON = rl;
st.locale = Vu;
st.localeData = Zu;
st.toIsoString = an(
  "toIsoString() is deprecated. Please use toISOString() instead (notice the capitals)",
  rl
);
st.lang = Gu;
Pe("X", 0, 0, "unix");
Pe("x", 0, 0, "valueOf");
Le("x", Qa);
Le("X", Qb);
kt("X", function(o, e, t) {
  t._d = new Date(parseFloat(o) * 1e3);
});
kt("x", function(o, e, t) {
  t._d = new Date(tt(o));
});
//! moment.js
ue.version = "2.30.1";
Ab(Lt);
ue.fn = ve;
ue.min = k1;
ue.max = x1;
ue.now = C1;
ue.utc = Yn;
ue.unix = Xv;
ue.months = Qv;
ue.isDate = pa;
ue.locale = Xi;
ue.invalid = Za;
ue.duration = Ln;
ue.isMoment = _n;
ue.weekdays = ty;
ue.parseZone = Jv;
ue.localeData = Ti;
ue.isDuration = Da;
ue.monthsShort = ey;
ue.weekdaysMin = ny;
ue.defineLocale = Bc;
ue.updateLocale = J0;
ue.locales = Q0;
ue.weekdaysShort = oy;
ue.normalizeUnits = ln;
ue.relativeTimeRounding = Ly;
ue.relativeTimeThreshold = Oy;
ue.calendarFormat = G1;
ue.prototype = ve;
ue.HTML5_FMT = {
  DATETIME_LOCAL: "YYYY-MM-DDTHH:mm",
  // <input type="datetime-local" />
  DATETIME_LOCAL_SECONDS: "YYYY-MM-DDTHH:mm:ss",
  // <input type="datetime-local" step="1" />
  DATETIME_LOCAL_MS: "YYYY-MM-DDTHH:mm:ss.SSS",
  // <input type="datetime-local" step="0.001" />
  DATE: "YYYY-MM-DD",
  // <input type="date" />
  TIME: "HH:mm",
  // <input type="time" />
  TIME_SECONDS: "HH:mm:ss",
  // <input type="time" step="1" />
  TIME_MS: "HH:mm:ss.SSS",
  // <input type="time" step="0.001" />
  WEEK: "GGGG-[W]WW",
  // <input type="week" />
  MONTH: "YYYY-MM"
  // <input type="month" />
};
function Yt(o, e, t = {}) {
  const n = document.createElement(o);
  Array.isArray(e) ? n.classList.add(...e) : e && n.classList.add(e);
  for (const r in t)
    Object.prototype.hasOwnProperty.call(t, r) && (n[r] = t[r]);
  return n;
}
function ih(o) {
  const e = o.getBoundingClientRect();
  return {
    y1: Math.floor(e.top + window.pageYOffset),
    x1: Math.floor(e.left + window.pageXOffset),
    x2: Math.floor(e.right + window.pageXOffset),
    y2: Math.floor(e.bottom + window.pageYOffset)
  };
}
function Ol(o, e) {
  const t = ih(o), n = ih(e);
  return {
    fromTopBorder: n.y1 - t.y1,
    fromLeftBorder: n.x1 - t.x1,
    fromRightBorder: t.x2 - n.x2,
    fromBottomBorder: t.y2 - n.y2
  };
}
function Ay(o, e) {
  const t = o.getBoundingClientRect(), { width: n, height: r, x: a, y: c } = t, { clientX: u, clientY: p } = e;
  return {
    width: n,
    height: r,
    x: u - a,
    y: p - c
  };
}
function rh(o, e) {
  return e.parentNode.insertBefore(o, e);
}
function sh(o, e = !0) {
  const t = document.createRange(), n = window.getSelection();
  t.selectNodeContents(o), t.collapse(e), n.removeAllRanges(), n.addRange(t);
}
let Ry = class Vo {
  /**
   * @param {object} options - constructor options
   * @param {PopoverItem[]} options.items - constructor options
   */
  constructor({ items: e }) {
    this.items = e, this.wrapper = void 0, this.itemEls = [];
  }
  /**
   * Set of CSS classnames used in popover
   *
   * @returns {object}
   */
  static get CSS() {
    return {
      popover: "tc-popover",
      popoverOpened: "tc-popover--opened",
      item: "tc-popover__item",
      itemHidden: "tc-popover__item--hidden",
      itemConfirmState: "tc-popover__item--confirm",
      itemIcon: "tc-popover__item-icon",
      itemLabel: "tc-popover__item-label"
    };
  }
  /**
   * Returns the popover element
   *
   * @returns {Element}
   */
  render() {
    return this.wrapper = Yt("div", Vo.CSS.popover), this.items.forEach((e, t) => {
      const n = Yt("div", Vo.CSS.item), r = Yt("div", Vo.CSS.itemIcon, {
        innerHTML: e.icon
      }), a = Yt("div", Vo.CSS.itemLabel, {
        textContent: e.label
      });
      n.dataset.index = t, n.appendChild(r), n.appendChild(a), this.wrapper.appendChild(n), this.itemEls.push(n);
    }), this.wrapper.addEventListener("click", (e) => {
      this.popoverClicked(e);
    }), this.wrapper;
  }
  /**
   * Popover wrapper click listener
   * Used to delegate clicks in items
   *
   * @returns {void}
   */
  popoverClicked(e) {
    const t = e.target.closest(`.${Vo.CSS.item}`);
    if (!t)
      return;
    const n = t.dataset.index, r = this.items[n];
    if (r.confirmationRequired && !this.hasConfirmationState(t)) {
      this.setConfirmationState(t);
      return;
    }
    r.onClick();
  }
  /**
   * Enable the confirmation state on passed item
   *
   * @returns {void}
   */
  setConfirmationState(e) {
    e.classList.add(Vo.CSS.itemConfirmState);
  }
  /**
   * Disable the confirmation state on passed item
   *
   * @returns {void}
   */
  clearConfirmationState(e) {
    e.classList.remove(Vo.CSS.itemConfirmState);
  }
  /**
   * Check if passed item has the confirmation state
   *
   * @returns {boolean}
   */
  hasConfirmationState(e) {
    return e.classList.contains(Vo.CSS.itemConfirmState);
  }
  /**
   * Return an opening state
   *
   * @returns {boolean}
   */
  get opened() {
    return this.wrapper.classList.contains(Vo.CSS.popoverOpened);
  }
  /**
   * Opens the popover
   *
   * @returns {void}
   */
  open() {
    this.items.forEach((e, t) => {
      typeof e.hideIf == "function" && this.itemEls[t].classList.toggle(Vo.CSS.itemHidden, e.hideIf());
    }), this.wrapper.classList.add(Vo.CSS.popoverOpened);
  }
  /**
   * Closes the popover
   *
   * @returns {void}
   */
  close() {
    this.wrapper.classList.remove(Vo.CSS.popoverOpened), this.itemEls.forEach((e) => {
      this.clearConfirmationState(e);
    });
  }
};
const ah = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 8L12 12M12 12L16 16M12 12L16 8M12 12L8 16"/></svg>', Iy = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 9.16666L18.2167 12.5M18.2167 12.5L14.8833 15.8333M18.2167 12.5H10.05C9.16594 12.5 8.31809 12.1488 7.69297 11.5237C7.06785 10.8986 6.71666 10.0507 6.71666 9.16666"/></svg>', Ny = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.9167 14.9167L11.5833 18.25M11.5833 18.25L8.25 14.9167M11.5833 18.25L11.5833 10.0833C11.5833 9.19928 11.9345 8.35143 12.5596 7.72631C13.1848 7.10119 14.0326 6.75 14.9167 6.75"/></svg>', Py = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 15.8333L18.2167 12.5M18.2167 12.5L14.8833 9.16667M18.2167 12.5L10.05 12.5C9.16595 12.5 8.31811 12.8512 7.69299 13.4763C7.06787 14.1014 6.71667 14.9493 6.71667 15.8333"/></svg>', Hy = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.41 9.66H9.4"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 9.66H14.59"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.31 14.36H9.3"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 14.36H14.59"/></svg>', lh = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M12 7V12M12 17V12M17 12H12M12 12H7"/></svg>', jy = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>', $y = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M10 5V18.5"/><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>';
let ch = class ns {
  /**
   * Creates toolbox buttons and toolbox menus
   *
   * @param {object} api - Editor.js api
   * @param {PopoverItem[]} items - Editor.js api
   * @param {function} onOpen - callback fired when the Popover is opening
   * @param {function} onClose - callback fired when the Popover is closing
   * @param {string} [cssModifier] - the modifier for the Toolbox. Allows to add some specific styles.
   */
  constructor({ api: e, items: t, onOpen: n, onClose: r, cssModifier: a = "" }) {
    this.api = e, this.items = t, this.onOpen = n, this.onClose = r, this.cssModifier = a, this.popover = null, this.wrapper = this.createToolbox();
  }
  /**
   * Style classes
   */
  static get CSS() {
    return {
      toolbox: "tc-toolbox",
      toolboxShowed: "tc-toolbox--showed",
      toggler: "tc-toolbox__toggler"
    };
  }
  /**
   * Returns rendered Toolbox element
   */
  get element() {
    return this.wrapper;
  }
  /**
   * Creating a toolbox to open menu for a manipulating columns
   *
   * @returns {Element}
   */
  createToolbox() {
    const e = Yt("div", [
      ns.CSS.toolbox,
      this.cssModifier ? `${ns.CSS.toolbox}--${this.cssModifier}` : ""
    ]);
    e.dataset.mutationFree = "true";
    const t = this.createPopover(), n = this.createToggler();
    return e.appendChild(n), e.appendChild(t), e;
  }
  /**
   * Creates the Toggler
   *
   * @returns {Element}
   */
  createToggler() {
    const e = Yt("div", ns.CSS.toggler, {
      innerHTML: Hy
    });
    return e.addEventListener("click", () => {
      this.togglerClicked();
    }), e;
  }
  /**
   * Creates the Popover instance and render it
   *
   * @returns {Element}
   */
  createPopover() {
    return this.popover = new Ry({
      items: this.items
    }), this.popover.render();
  }
  /**
   * Toggler click handler. Opens/Closes the popover
   *
   * @returns {void}
   */
  togglerClicked() {
    this.popover.opened ? (this.popover.close(), this.onClose()) : (this.popover.open(), this.onOpen());
  }
  /**
   * Shows the Toolbox
   *
   * @param {function} computePositionMethod - method that returns the position coordinate
   * @returns {void}
   */
  show(e) {
    const t = e();
    Object.entries(t).forEach(([n, r]) => {
      this.wrapper.style[n] = r;
    }), this.wrapper.classList.add(ns.CSS.toolboxShowed);
  }
  /**
   * Hides the Toolbox
   *
   * @returns {void}
   */
  hide() {
    this.popover.close(), this.wrapper.classList.remove(ns.CSS.toolboxShowed);
  }
};
function Yy(o, e) {
  let t = 0;
  return function(...n) {
    const r = (/* @__PURE__ */ new Date()).getTime();
    if (!(r - t < o))
      return t = r, e(...n);
  };
}
let Fy = class {
  constructor(e, t, n) {
    if (this.parentEl = "body", this.element = Ie(e), this.startDate = ue().startOf("day"), this.endDate = ue().endOf("day"), this.minDate = !1, this.maxDate = !1, this.maxSpan = !1, this.autoApply = !1, this.singleDatePicker = !1, this.showDropdowns = !1, this.minYear = ue().subtract(100, "year").format("YYYY"), this.maxYear = ue().add(100, "year").format("YYYY"), this.showWeekNumbers = !1, this.showISOWeekNumbers = !1, this.showCustomRangeLabel = !0, this.timePicker = !1, this.timePicker24Hour = !1, this.timePickerIncrement = 1, this.timePickerSeconds = !1, this.linkedCalendars = !0, this.autoUpdateInput = !0, this.alwaysShowCalendars = !1, this.ranges = {}, this.opens = "right", this.element.hasClass("pull-right") && (this.opens = "left"), this.drops = "down", this.element.hasClass("dropup") && (this.drops = "up"), this.buttonClasses = "btn btn-sm", this.applyButtonClasses = "btn-primary", this.cancelButtonClasses = "btn-default", this.locale = {
      direction: "ltr",
      format: ue.localeData().firstDayOfWeek(),
      separator: " - ",
      applyLabel: "Apply",
      cancelLabel: "Cancel",
      weekLabel: "W",
      customRangeLabel: "Custom Range",
      daysOfWeek: ue.weekdaysMin(),
      monthNames: ue.monthsShort(),
      firstDay: ue.localeData().firstDayOfWeek()
    }, this.callback = function() {
    }, this.isShowing = !1, this.leftCalendar = {}, this.rightCalendar = {}, (typeof t != "object" || t === null) && (t = {}), t = Ie.extend(this.element.data(), t), typeof t.template != "string" && !(t.template instanceof Ie) && (t.template = '<div class="daterangepicker"><div class="ranges"></div><div class="drp-calendar left"><div class="calendar-table"></div><div class="calendar-time"></div></div><div class="drp-calendar right"><div class="calendar-table"></div><div class="calendar-time"></div></div><div class="drp-buttons"><span class="drp-selected"></span><button class="cancelBtn" type="button"></button><button class="applyBtn" disabled="disabled" type="button"></button> </div></div>'), this.parentEl = t.parentEl && Ie(t.parentEl).length ? Ie(t.parentEl) : Ie(this.parentEl), this.container = Ie(t.template).appendTo(this.parentEl), typeof t.locale == "object" && (typeof t.locale.direction == "string" && (this.locale.direction = t.locale.direction), typeof t.locale.format == "string" && (this.locale.format = t.locale.format), typeof t.locale.separator == "string" && (this.locale.separator = t.locale.separator), typeof t.locale.daysOfWeek == "object" && (this.locale.daysOfWeek = t.locale.daysOfWeek.slice()), typeof t.locale.monthNames == "object" && (this.locale.monthNames = t.locale.monthNames.slice()), typeof t.locale.firstDay == "number" && (this.locale.firstDay = t.locale.firstDay), typeof t.locale.applyLabel == "string" && (this.locale.applyLabel = t.locale.applyLabel), typeof t.locale.cancelLabel == "string" && (this.locale.cancelLabel = t.locale.cancelLabel), typeof t.locale.weekLabel == "string" && (this.locale.weekLabel = t.locale.weekLabel), typeof t.locale.customRangeLabel == "string")) {
      var r = document.createElement("textarea");
      r.innerHTML = t.locale.customRangeLabel;
      var a = r.value;
      this.locale.customRangeLabel = a;
    }
    if (this.container.addClass(this.locale.direction), typeof t.startDate == "string" && (this.startDate = ue(t.startDate, this.locale.format)), typeof t.endDate == "string" && (this.endDate = ue(t.endDate, this.locale.format)), typeof t.minDate == "string" && (this.minDate = ue(t.minDate, this.locale.format)), typeof t.maxDate == "string" && (this.maxDate = ue(t.maxDate, this.locale.format)), typeof t.startDate == "object" && (this.startDate = ue(t.startDate)), typeof t.endDate == "object" && (this.endDate = ue(t.endDate)), typeof t.minDate == "object" && (this.minDate = ue(t.minDate)), typeof t.maxDate == "object" && (this.maxDate = ue(t.maxDate)), this.minDate && this.startDate.isBefore(this.minDate) && (this.startDate = this.minDate.clone()), this.maxDate && this.endDate.isAfter(this.maxDate) && (this.endDate = this.maxDate.clone()), typeof t.applyButtonClasses == "string" && (this.applyButtonClasses = t.applyButtonClasses), typeof t.applyClass == "string" && (this.applyButtonClasses = t.applyClass), typeof t.cancelButtonClasses == "string" && (this.cancelButtonClasses = t.cancelButtonClasses), typeof t.cancelClass == "string" && (this.cancelButtonClasses = t.cancelClass), typeof t.maxSpan == "object" && (this.maxSpan = t.maxSpan), typeof t.dateLimit == "object" && (this.maxSpan = t.dateLimit), typeof t.opens == "string" && (this.opens = t.opens), typeof t.drops == "string" && (this.drops = t.drops), typeof t.showWeekNumbers == "boolean" && (this.showWeekNumbers = t.showWeekNumbers), typeof t.showISOWeekNumbers == "boolean" && (this.showISOWeekNumbers = t.showISOWeekNumbers), typeof t.buttonClasses == "string" && (this.buttonClasses = t.buttonClasses), typeof t.buttonClasses == "object" && (this.buttonClasses = t.buttonClasses.join(" ")), typeof t.showDropdowns == "boolean" && (this.showDropdowns = t.showDropdowns), typeof t.minYear == "number" && (this.minYear = t.minYear), typeof t.maxYear == "number" && (this.maxYear = t.maxYear), typeof t.showCustomRangeLabel == "boolean" && (this.showCustomRangeLabel = t.showCustomRangeLabel), typeof t.singleDatePicker == "boolean" && (this.singleDatePicker = t.singleDatePicker, this.singleDatePicker && (this.endDate = this.startDate.clone())), typeof t.timePicker == "boolean" && (this.timePicker = t.timePicker), typeof t.timePickerSeconds == "boolean" && (this.timePickerSeconds = t.timePickerSeconds), typeof t.timePickerIncrement == "number" && (this.timePickerIncrement = t.timePickerIncrement), typeof t.timePicker24Hour == "boolean" && (this.timePicker24Hour = t.timePicker24Hour), typeof t.autoApply == "boolean" && (this.autoApply = t.autoApply), typeof t.autoUpdateInput == "boolean" && (this.autoUpdateInput = t.autoUpdateInput), typeof t.linkedCalendars == "boolean" && (this.linkedCalendars = t.linkedCalendars), typeof t.isInvalidDate == "function" && (this.isInvalidDate = t.isInvalidDate), typeof t.isCustomDate == "function" && (this.isCustomDate = t.isCustomDate), typeof t.alwaysShowCalendars == "boolean" && (this.alwaysShowCalendars = t.alwaysShowCalendars), this.locale.firstDay != 0)
      for (var c = this.locale.firstDay; c > 0; )
        this.locale.daysOfWeek.push(this.locale.daysOfWeek.shift()), c--;
    var u, p, g;
    if (typeof t.startDate > "u" && typeof t.endDate > "u" && Ie(this.element).is(":text")) {
      var v = Ie(this.element).val(), k = v.split(this.locale.separator);
      u = p = null, k.length == 2 ? (u = ue(k[0], this.locale.format), p = ue(k[1], this.locale.format)) : this.singleDatePicker && v !== "" && (u = ue(v, this.locale.format), p = ue(v, this.locale.format)), u !== null && p !== null && (this.setStartDate(u), this.setEndDate(p));
    }
    if (typeof t.ranges == "object") {
      for (g in t.ranges) {
        typeof t.ranges[g][0] == "string" ? u = ue(t.ranges[g][0], this.locale.format) : u = ue(t.ranges[g][0]), typeof t.ranges[g][1] == "string" ? p = ue(t.ranges[g][1], this.locale.format) : p = ue(t.ranges[g][1]), this.minDate && u.isBefore(this.minDate) && (u = this.minDate.clone());
        var y = this.maxDate;
        if (this.maxSpan && y && u.clone().add(this.maxSpan).isAfter(y) && (y = u.clone().add(this.maxSpan)), y && p.isAfter(y) && (p = y.clone()), !(this.minDate && p.isBefore(this.minDate, this.timepicker ? "minute" : "day") || y && u.isAfter(y, this.timepicker ? "minute" : "day"))) {
          var r = document.createElement("textarea");
          r.innerHTML = g;
          var a = r.value;
          this.ranges[a] = [u, p];
        }
      }
      var T = "<ul>";
      for (g in this.ranges)
        T += '<li data-range-key="' + g + '">' + g + "</li>";
      this.showCustomRangeLabel && (T += '<li data-range-key="' + this.locale.customRangeLabel + '">' + this.locale.customRangeLabel + "</li>"), T += "</ul>", this.container.find(".ranges").prepend(T);
    }
    typeof n == "function" && (this.callback = n), this.timePicker || (this.startDate = this.startDate.startOf("day"), this.endDate = this.endDate.endOf("day"), this.container.find(".calendar-time").hide()), this.timePicker && this.autoApply && (this.autoApply = !1), this.autoApply && this.container.addClass("auto-apply"), typeof t.ranges == "object" && this.container.addClass("show-ranges"), this.singleDatePicker && (this.container.addClass("single"), this.container.find(".drp-calendar.left").addClass("single"), this.container.find(".drp-calendar.left").show(), this.container.find(".drp-calendar.right").hide(), !this.timePicker && this.autoApply && this.container.addClass("auto-apply")), (typeof t.ranges > "u" && !this.singleDatePicker || this.alwaysShowCalendars) && this.container.addClass("show-calendar"), this.container.addClass("opens" + this.opens), this.container.find(".applyBtn, .cancelBtn").addClass(this.buttonClasses), this.applyButtonClasses.length && this.container.find(".applyBtn").addClass(this.applyButtonClasses), this.cancelButtonClasses.length && this.container.find(".cancelBtn").addClass(this.cancelButtonClasses), this.container.find(".applyBtn").html(this.locale.applyLabel), this.container.find(".cancelBtn").html(this.locale.cancelLabel), this.container.find(".drp-calendar").on("click.daterangepicker", ".prev", Ie.proxy(this.clickPrev, this)).on("click.daterangepicker", ".next", Ie.proxy(this.clickNext, this)).on("mousedown.daterangepicker", "td.available", Ie.proxy(this.clickDate, this)).on("mouseenter.daterangepicker", "td.available", Ie.proxy(this.hoverDate, this)).on("change.daterangepicker", "select.yearselect", Ie.proxy(this.monthOrYearChanged, this)).on("change.daterangepicker", "select.monthselect", Ie.proxy(this.monthOrYearChanged, this)).on("change.daterangepicker", "select.hourselect,select.minuteselect,select.secondselect,select.ampmselect", Ie.proxy(this.timeChanged, this)), this.container.find(".ranges").on("click.daterangepicker", "li", Ie.proxy(this.clickRange, this)), this.container.find(".drp-buttons").on("click.daterangepicker", "button.applyBtn", Ie.proxy(this.clickApply, this)).on("click.daterangepicker", "button.cancelBtn", Ie.proxy(this.clickCancel, this)), this.element.is("input") || this.element.is("button") ? this.element.on({
      "click.daterangepicker": Ie.proxy(this.show, this),
      "focus.daterangepicker": Ie.proxy(this.show, this),
      "keyup.daterangepicker": Ie.proxy(this.elementChanged, this),
      "keydown.daterangepicker": Ie.proxy(this.keydown, this)
      //IE 11 compatibility
    }) : (this.element.on("click.daterangepicker", Ie.proxy(this.toggle, this)), this.element.on("keydown.daterangepicker", Ie.proxy(this.toggle, this))), this.updateElement();
  }
  setStartDate(e) {
    typeof e == "string" && (this.startDate = ue(e, this.locale.format)), typeof e == "object" && (this.startDate = ue(e)), this.timePicker || (this.startDate = this.startDate.startOf("day")), this.timePicker && this.timePickerIncrement && this.startDate.minute(Math.round(this.startDate.minute() / this.timePickerIncrement) * this.timePickerIncrement), this.minDate && this.startDate.isBefore(this.minDate) && (this.startDate = this.minDate.clone(), this.timePicker && this.timePickerIncrement && this.startDate.minute(Math.round(this.startDate.minute() / this.timePickerIncrement) * this.timePickerIncrement)), this.maxDate && this.startDate.isAfter(this.maxDate) && (this.startDate = this.maxDate.clone(), this.timePicker && this.timePickerIncrement && this.startDate.minute(Math.floor(this.startDate.minute() / this.timePickerIncrement) * this.timePickerIncrement)), this.isShowing || this.updateElement(), this.updateMonthsInView();
  }
  setEndDate(e) {
    typeof e == "string" && (this.endDate = ue(e, this.locale.format)), typeof e == "object" && (this.endDate = ue(e)), this.timePicker || (this.endDate = this.endDate.endOf("day")), this.timePicker && this.timePickerIncrement && this.endDate.minute(Math.round(this.endDate.minute() / this.timePickerIncrement) * this.timePickerIncrement), this.endDate.isBefore(this.startDate) && (this.endDate = this.startDate.clone()), this.maxDate && this.endDate.isAfter(this.maxDate) && (this.endDate = this.maxDate.clone()), this.maxSpan && this.startDate.clone().add(this.maxSpan).isBefore(this.endDate) && (this.endDate = this.startDate.clone().add(this.maxSpan)), this.previousRightTime = this.endDate.clone(), this.container.find(".drp-selected").html(this.startDate.format(this.locale.format) + this.locale.separator + this.endDate.format(this.locale.format)), this.isShowing || this.updateElement(), this.updateMonthsInView();
  }
  isInvalidDate() {
    return !1;
  }
  isCustomDate() {
    return !1;
  }
  updateView() {
    this.timePicker && (this.renderTimePicker("left"), this.renderTimePicker("right"), this.endDate ? this.container.find(".right .calendar-time select").prop("disabled", !1).removeClass("disabled") : this.container.find(".right .calendar-time select").prop("disabled", !0).addClass("disabled")), this.endDate && this.container.find(".drp-selected").html(this.startDate.format(this.locale.format) + this.locale.separator + this.endDate.format(this.locale.format)), this.updateMonthsInView(), this.updateCalendars(), this.updateFormInputs();
  }
  updateMonthsInView() {
    if (this.endDate) {
      if (!this.singleDatePicker && this.leftCalendar.month && this.rightCalendar.month && (this.startDate.format("YYYY-MM") == this.leftCalendar.month.format("YYYY-MM") || this.startDate.format("YYYY-MM") == this.rightCalendar.month.format("YYYY-MM")) && (this.endDate.format("YYYY-MM") == this.leftCalendar.month.format("YYYY-MM") || this.endDate.format("YYYY-MM") == this.rightCalendar.month.format("YYYY-MM")))
        return;
      this.leftCalendar.month = this.startDate.clone().date(2), !this.linkedCalendars && (this.endDate.month() != this.startDate.month() || this.endDate.year() != this.startDate.year()) ? this.rightCalendar.month = this.endDate.clone().date(2) : this.rightCalendar.month = this.startDate.clone().date(2).add(1, "month");
    } else
      this.leftCalendar.month.format("YYYY-MM") != this.startDate.format("YYYY-MM") && this.rightCalendar.month.format("YYYY-MM") != this.startDate.format("YYYY-MM") && (this.leftCalendar.month = this.startDate.clone().date(2), this.rightCalendar.month = this.startDate.clone().date(2).add(1, "month"));
    this.maxDate && this.linkedCalendars && !this.singleDatePicker && this.rightCalendar.month > this.maxDate && (this.rightCalendar.month = this.maxDate.clone().date(2), this.leftCalendar.month = this.maxDate.clone().date(2).subtract(1, "month"));
  }
  updateCalendars() {
    if (this.timePicker) {
      var e, t, n;
      if (this.endDate) {
        if (e = parseInt(this.container.find(".left .hourselect").val(), 10), t = parseInt(this.container.find(".left .minuteselect").val(), 10), isNaN(t) && (t = parseInt(this.container.find(".left .minuteselect option:last").val(), 10)), n = this.timePickerSeconds ? parseInt(this.container.find(".left .secondselect").val(), 10) : 0, !this.timePicker24Hour) {
          var r = this.container.find(".left .ampmselect").val();
          r === "PM" && e < 12 && (e += 12), r === "AM" && e === 12 && (e = 0);
        }
      } else if (e = parseInt(this.container.find(".right .hourselect").val(), 10), t = parseInt(this.container.find(".right .minuteselect").val(), 10), isNaN(t) && (t = parseInt(this.container.find(".right .minuteselect option:last").val(), 10)), n = this.timePickerSeconds ? parseInt(this.container.find(".right .secondselect").val(), 10) : 0, !this.timePicker24Hour) {
        var r = this.container.find(".right .ampmselect").val();
        r === "PM" && e < 12 && (e += 12), r === "AM" && e === 12 && (e = 0);
      }
      this.leftCalendar.month.hour(e).minute(t).second(n), this.rightCalendar.month.hour(e).minute(t).second(n);
    }
    this.renderCalendar("left"), this.renderCalendar("right"), this.container.find(".ranges li").removeClass("active"), this.endDate != null && this.calculateChosenLabel();
  }
  renderCalendar(e) {
    var x = e == "left" ? this.leftCalendar : this.rightCalendar, t = x.month.month(), n = x.month.year(), r = x.month.hour(), a = x.month.minute(), c = x.month.second(), u = ue([n, t]).daysInMonth(), p = ue([n, t, 1]), g = ue([n, t, u]), v = ue(p).subtract(1, "month").month(), k = ue(p).subtract(1, "month").year(), y = ue([k, v]).daysInMonth(), T = p.day(), x = [];
    x.firstDay = p, x.lastDay = g;
    for (var M = 0; M < 6; M++)
      x[M] = [];
    var L = y - T + this.locale.firstDay + 1;
    L > y && (L -= 7), T == this.locale.firstDay && (L = y - 6);
    for (var Y = ue([k, v, L, 12, a, c]), I, F, M = 0, I = 0, F = 0; M < 42; M++, I++, Y = ue(Y).add(24, "hour"))
      M > 0 && I % 7 === 0 && (I = 0, F++), x[F][I] = Y.clone().hour(r).minute(a).second(c), Y.hour(12), this.minDate && x[F][I].format("YYYY-MM-DD") == this.minDate.format("YYYY-MM-DD") && x[F][I].isBefore(this.minDate) && e == "left" && (x[F][I] = this.minDate.clone()), this.maxDate && x[F][I].format("YYYY-MM-DD") == this.maxDate.format("YYYY-MM-DD") && x[F][I].isAfter(this.maxDate) && e == "right" && (x[F][I] = this.maxDate.clone());
    e == "left" ? this.leftCalendar.calendar = x : this.rightCalendar.calendar = x;
    var Q = e == "left" ? this.minDate : this.startDate, U = this.maxDate;
    e == "left" ? this.startDate : this.endDate, this.locale.direction == "ltr";
    var z = '<table class="table-condensed">';
    z += "<thead>", z += "<tr>", (this.showWeekNumbers || this.showISOWeekNumbers) && (z += "<th></th>"), (!Q || Q.isBefore(x.firstDay)) && (!this.linkedCalendars || e == "left") ? z += '<th class="prev available"><span></span></th>' : z += "<th></th>";
    var d = this.locale.monthNames[x[1][1].month()] + x[1][1].format(" YYYY");
    if (this.showDropdowns) {
      for (var j = x[1][1].month(), R = x[1][1].year(), V = U && U.year() || this.maxYear, K = Q && Q.year() || this.minYear, J = R == K, A = R == V, se = '<select class="monthselect">', Z = 0; Z < 12; Z++)
        (!J || Q && Z >= Q.month()) && (!A || U && Z <= U.month()) ? se += "<option value='" + Z + "'" + (Z === j ? " selected='selected'" : "") + ">" + this.locale.monthNames[Z] + "</option>" : se += "<option value='" + Z + "'" + (Z === j ? " selected='selected'" : "") + " disabled='disabled'>" + this.locale.monthNames[Z] + "</option>";
      se += "</select>";
      for (var ne = '<select class="yearselect">', le = K; le <= V; le++)
        ne += '<option value="' + le + '"' + (le === R ? ' selected="selected"' : "") + ">" + le + "</option>";
      ne += "</select>", d = se + ne;
    }
    if (z += '<th colspan="5" class="month">' + d + "</th>", (!U || U.isAfter(x.lastDay)) && (!this.linkedCalendars || e == "right" || this.singleDatePicker) ? z += '<th class="next available"><span></span></th>' : z += "<th></th>", z += "</tr>", z += "<tr>", (this.showWeekNumbers || this.showISOWeekNumbers) && (z += '<th class="week">' + this.locale.weekLabel + "</th>"), Ie.each(this.locale.daysOfWeek, function(Je, no) {
      z += "<th>" + no + "</th>";
    }), z += "</tr>", z += "</thead>", z += "<tbody>", this.endDate == null && this.maxSpan) {
      var oe = this.startDate.clone().add(this.maxSpan).endOf("day");
      (!U || oe.isBefore(U)) && (U = oe);
    }
    for (var F = 0; F < 6; F++) {
      z += "<tr>", this.showWeekNumbers ? z += '<td class="week">' + x[F][0].week() + "</td>" : this.showISOWeekNumbers && (z += '<td class="week">' + x[F][0].isoWeek() + "</td>");
      for (var I = 0; I < 7; I++) {
        var fe = [];
        x[F][I].isSame(/* @__PURE__ */ new Date(), "day") && fe.push("today"), x[F][I].isoWeekday() > 5 && fe.push("weekend"), x[F][I].month() != x[1][1].month() && fe.push("off", "ends"), this.minDate && x[F][I].isBefore(this.minDate, "day") && fe.push("off", "disabled"), U && x[F][I].isAfter(U, "day") && fe.push("off", "disabled"), this.isInvalidDate(x[F][I]) && fe.push("off", "disabled"), x[F][I].format("YYYY-MM-DD") == this.startDate.format("YYYY-MM-DD") && fe.push("active", "start-date"), this.endDate != null && x[F][I].format("YYYY-MM-DD") == this.endDate.format("YYYY-MM-DD") && fe.push("active", "end-date"), this.endDate != null && x[F][I] > this.startDate && x[F][I] < this.endDate && fe.push("in-range");
        var Me = this.isCustomDate(x[F][I]);
        Me !== !1 && (typeof Me == "string" ? fe.push(Me) : Array.prototype.push.apply(fe, Me));
        for (var je = "", Ae = !1, M = 0; M < fe.length; M++)
          je += fe[M] + " ", fe[M] == "disabled" && (Ae = !0);
        Ae || (je += "available"), z += '<td class="' + je.replace(/^\s+|\s+$/g, "") + '" data-title="r' + F + "c" + I + '">' + x[F][I].date() + "</td>";
      }
      z += "</tr>";
    }
    z += "</tbody>", z += "</table>", this.container.find(".drp-calendar." + e + " .calendar-table").html(z);
  }
  renderTimePicker(e) {
    if (!(e == "right" && !this.endDate)) {
      var t, n, r, a = this.maxDate;
      if (this.maxSpan && (!this.maxDate || this.startDate.clone().add(this.maxSpan).isBefore(this.maxDate)) && (a = this.startDate.clone().add(this.maxSpan)), e == "left")
        n = this.startDate.clone(), r = this.minDate;
      else if (e == "right") {
        n = this.endDate.clone(), r = this.startDate;
        var c = this.container.find(".drp-calendar.right .calendar-time");
        if (c.html() != "" && (n.hour(isNaN(n.hour()) ? c.find(".hourselect option:selected").val() : n.hour()), n.minute(isNaN(n.minute()) ? c.find(".minuteselect option:selected").val() : n.minute()), n.second(isNaN(n.second()) ? c.find(".secondselect option:selected").val() : n.second()), !this.timePicker24Hour)) {
          var u = c.find(".ampmselect option:selected").val();
          u === "PM" && n.hour() < 12 && n.hour(n.hour() + 12), u === "AM" && n.hour() === 12 && n.hour(0);
        }
        n.isBefore(this.startDate) && (n = this.startDate.clone()), a && n.isAfter(a) && (n = a.clone());
      }
      t = '<select class="hourselect">';
      for (var p = this.timePicker24Hour ? 0 : 1, g = this.timePicker24Hour ? 23 : 12, v = p; v <= g; v++) {
        var k = v;
        this.timePicker24Hour || (k = n.hour() >= 12 ? v == 12 ? 12 : v + 12 : v == 12 ? 0 : v);
        var y = n.clone().hour(k), T = !1;
        r && y.minute(59).isBefore(r) && (T = !0), a && y.minute(0).isAfter(a) && (T = !0), k == n.hour() && !T ? t += '<option value="' + v + '" selected="selected">' + v + "</option>" : T ? t += '<option value="' + v + '" disabled="disabled" class="disabled">' + v + "</option>" : t += '<option value="' + v + '">' + v + "</option>";
      }
      t += "</select> ", t += ': <select class="minuteselect">';
      for (var v = 0; v < 60; v += this.timePickerIncrement) {
        var x = v < 10 ? "0" + v : v, y = n.clone().minute(v), T = !1;
        r && y.second(59).isBefore(r) && (T = !0), a && y.second(0).isAfter(a) && (T = !0), n.minute() == v && !T ? t += '<option value="' + v + '" selected="selected">' + x + "</option>" : T ? t += '<option value="' + v + '" disabled="disabled" class="disabled">' + x + "</option>" : t += '<option value="' + v + '">' + x + "</option>";
      }
      if (t += "</select> ", this.timePickerSeconds) {
        t += ': <select class="secondselect">';
        for (var v = 0; v < 60; v++) {
          var x = v < 10 ? "0" + v : v, y = n.clone().second(v), T = !1;
          r && y.isBefore(r) && (T = !0), a && y.isAfter(a) && (T = !0), n.second() == v && !T ? t += '<option value="' + v + '" selected="selected">' + x + "</option>" : T ? t += '<option value="' + v + '" disabled="disabled" class="disabled">' + x + "</option>" : t += '<option value="' + v + '">' + x + "</option>";
        }
        t += "</select> ";
      }
      if (!this.timePicker24Hour) {
        t += '<select class="ampmselect">';
        var M = "", L = "";
        r && n.clone().hour(12).minute(0).second(0).isBefore(r) && (M = ' disabled="disabled" class="disabled"'), a && n.clone().hour(0).minute(0).second(0).isAfter(a) && (L = ' disabled="disabled" class="disabled"'), n.hour() >= 12 ? t += '<option value="AM"' + M + '>AM</option><option value="PM" selected="selected"' + L + ">PM</option>" : t += '<option value="AM" selected="selected"' + M + '>AM</option><option value="PM"' + L + ">PM</option>", t += "</select>";
      }
      this.container.find(".drp-calendar." + e + " .calendar-time").html(t);
    }
  }
  updateFormInputs() {
    this.singleDatePicker || this.endDate && (this.startDate.isBefore(this.endDate) || this.startDate.isSame(this.endDate)) ? this.container.find("button.applyBtn").prop("disabled", !1) : this.container.find("button.applyBtn").prop("disabled", !0);
  }
  move() {
    var e = { top: 0, left: 0 }, t, n = this.drops, r = Ie(window).width();
    switch (this.parentEl.is("body") || (e = {
      top: this.parentEl.offset().top - this.parentEl.scrollTop(),
      left: this.parentEl.offset().left - this.parentEl.scrollLeft()
    }, r = this.parentEl[0].clientWidth + this.parentEl.offset().left), n) {
      case "auto":
        t = this.element.offset().top + this.element.outerHeight() - e.top, t + this.container.outerHeight() >= this.parentEl[0].scrollHeight && (t = this.element.offset().top - this.container.outerHeight() - e.top, n = "up");
        break;
      case "up":
        t = this.element.offset().top - this.container.outerHeight() - e.top;
        break;
      default:
        t = this.element.offset().top + this.element.outerHeight() - e.top;
        break;
    }
    this.container.css({
      top: 0,
      left: 0,
      right: "auto"
    });
    var a = this.container.outerWidth();
    if (this.container.toggleClass("drop-up", n == "up"), this.opens == "left") {
      var c = r - this.element.offset().left - this.element.outerWidth();
      a + c > Ie(window).width() ? this.container.css({
        top: t,
        right: "auto",
        left: 9
      }) : this.container.css({
        top: t,
        right: c,
        left: "auto"
      });
    } else if (this.opens == "center") {
      var u = this.element.offset().left - e.left + this.element.outerWidth() / 2 - a / 2;
      u < 0 ? this.container.css({
        top: t,
        right: "auto",
        left: 9
      }) : u + a > Ie(window).width() ? this.container.css({
        top: t,
        left: "auto",
        right: 0
      }) : this.container.css({
        top: t,
        left: u,
        right: "auto"
      });
    } else {
      var u = this.element.offset().left - e.left;
      u + a > Ie(window).width() ? this.container.css({
        top: t,
        left: "auto",
        right: 0
      }) : this.container.css({
        top: t,
        left: u,
        right: "auto"
      });
    }
  }
  show(e) {
    this.isShowing || (this._outsideClickProxy = Ie.proxy(function(t) {
      this.outsideClick(t);
    }, this), Ie(document).on("mousedown.daterangepicker", this._outsideClickProxy).on("touchend.daterangepicker", this._outsideClickProxy).on("click.daterangepicker", "[data-toggle=dropdown]", this._outsideClickProxy).on("focusin.daterangepicker", this._outsideClickProxy), Ie(window).on("resize.daterangepicker", Ie.proxy(function(t) {
      this.move(t);
    }, this)), this.oldStartDate = this.startDate.clone(), this.oldEndDate = this.endDate.clone(), this.previousRightTime = this.endDate.clone(), this.updateView(), this.container.show(), this.move(), this.element.trigger("show.daterangepicker", this), this.isShowing = !0);
  }
  hide(e) {
    this.isShowing && (this.endDate || (this.startDate = this.oldStartDate.clone(), this.endDate = this.oldEndDate.clone()), (!this.startDate.isSame(this.oldStartDate) || !this.endDate.isSame(this.oldEndDate)) && this.callback(this.startDate.clone(), this.endDate.clone(), this.chosenLabel), this.updateElement(), Ie(document).off(".daterangepicker"), Ie(window).off(".daterangepicker"), this.container.hide(), this.element.trigger("hide.daterangepicker", this), this.isShowing = !1);
  }
  toggle(e) {
    this.isShowing ? this.hide() : this.show();
  }
  outsideClick(e) {
    var t = Ie(e.target);
    e.type == "focusin" || t.closest(this.element).length || t.closest(this.container).length || t.closest(".calendar-table").length || (this.hide(), this.element.trigger("outsideClick.daterangepicker", this));
  }
  showCalendars() {
    this.container.addClass("show-calendar"), this.move(), this.element.trigger("showCalendar.daterangepicker", this);
  }
  hideCalendars() {
    this.container.removeClass("show-calendar"), this.element.trigger("hideCalendar.daterangepicker", this);
  }
  clickRange(e) {
    var t = e.target.getAttribute("data-range-key");
    if (this.chosenLabel = t, t == this.locale.customRangeLabel)
      this.showCalendars();
    else {
      var n = this.ranges[t];
      this.startDate = n[0], this.endDate = n[1], this.timePicker || (this.startDate.startOf("day"), this.endDate.endOf("day")), this.alwaysShowCalendars || this.hideCalendars(), this.clickApply();
    }
  }
  clickPrev(e) {
    var t = Ie(e.target).parents(".drp-calendar");
    t.hasClass("left") ? (this.leftCalendar.month.subtract(1, "month"), this.linkedCalendars && this.rightCalendar.month.subtract(1, "month")) : this.rightCalendar.month.subtract(1, "month"), this.updateCalendars();
  }
  clickNext(e) {
    var t = Ie(e.target).parents(".drp-calendar");
    t.hasClass("left") ? this.leftCalendar.month.add(1, "month") : (this.rightCalendar.month.add(1, "month"), this.linkedCalendars && this.leftCalendar.month.add(1, "month")), this.updateCalendars();
  }
  hoverDate(e) {
    if (Ie(e.target).hasClass("available")) {
      var t = Ie(e.target).attr("data-title"), n = t.substr(1, 1), r = t.substr(3, 1), a = Ie(e.target).parents(".drp-calendar"), c = a.hasClass("left") ? this.leftCalendar.calendar[n][r] : this.rightCalendar.calendar[n][r], u = this.leftCalendar, p = this.rightCalendar, g = this.startDate;
      this.endDate || this.container.find(".drp-calendar tbody td").each(function(v, k) {
        if (!Ie(k).hasClass("week")) {
          var y = Ie(k).attr("data-title"), T = y.substr(1, 1), x = y.substr(3, 1), M = Ie(k).parents(".drp-calendar"), L = M.hasClass("left") ? u.calendar[T][x] : p.calendar[T][x];
          L.isAfter(g) && L.isBefore(c) || L.isSame(c, "day") ? Ie(k).addClass("in-range") : Ie(k).removeClass("in-range");
        }
      });
    }
  }
  clickDate(e) {
    if (Ie(e.target).hasClass("available")) {
      var t = Ie(e.target).attr("data-title"), n = t.substr(1, 1), r = t.substr(3, 1), a = Ie(e.target).parents(".drp-calendar"), c = a.hasClass("left") ? this.leftCalendar.calendar[n][r] : this.rightCalendar.calendar[n][r];
      if (this.endDate || c.isBefore(this.startDate, "day")) {
        if (this.timePicker) {
          var u = parseInt(this.container.find(".left .hourselect").val(), 10);
          if (!this.timePicker24Hour) {
            var p = this.container.find(".left .ampmselect").val();
            p === "PM" && u < 12 && (u += 12), p === "AM" && u === 12 && (u = 0);
          }
          var g = parseInt(this.container.find(".left .minuteselect").val(), 10);
          isNaN(g) && (g = parseInt(this.container.find(".left .minuteselect option:last").val(), 10));
          var v = this.timePickerSeconds ? parseInt(this.container.find(".left .secondselect").val(), 10) : 0;
          c = c.clone().hour(u).minute(g).second(v);
        }
        this.endDate = null, this.setStartDate(c.clone());
      } else if (!this.endDate && c.isBefore(this.startDate))
        this.setEndDate(this.startDate.clone());
      else {
        if (this.timePicker) {
          var u = parseInt(this.container.find(".right .hourselect").val(), 10);
          if (!this.timePicker24Hour) {
            var p = this.container.find(".right .ampmselect").val();
            p === "PM" && u < 12 && (u += 12), p === "AM" && u === 12 && (u = 0);
          }
          var g = parseInt(this.container.find(".right .minuteselect").val(), 10);
          isNaN(g) && (g = parseInt(this.container.find(".right .minuteselect option:last").val(), 10));
          var v = this.timePickerSeconds ? parseInt(this.container.find(".right .secondselect").val(), 10) : 0;
          c = c.clone().hour(u).minute(g).second(v);
        }
        this.setEndDate(c.clone()), this.autoApply && (this.calculateChosenLabel(), this.clickApply());
      }
      this.singleDatePicker && (this.setEndDate(this.startDate), !this.timePicker && this.autoApply && this.clickApply()), this.updateView(), e.stopPropagation();
    }
  }
  calculateChosenLabel() {
    var e = !0, t = 0;
    for (var n in this.ranges) {
      if (this.timePicker) {
        var r = this.timePickerSeconds ? "YYYY-MM-DD HH:mm:ss" : "YYYY-MM-DD HH:mm";
        if (this.startDate.format(r) == this.ranges[n][0].format(r) && this.endDate.format(r) == this.ranges[n][1].format(r)) {
          e = !1, this.chosenLabel = this.container.find(".ranges li:eq(" + t + ")").addClass("active").attr("data-range-key");
          break;
        }
      } else if (this.startDate.format("YYYY-MM-DD") == this.ranges[n][0].format("YYYY-MM-DD") && this.endDate.format("YYYY-MM-DD") == this.ranges[n][1].format("YYYY-MM-DD")) {
        e = !1, this.chosenLabel = this.container.find(".ranges li:eq(" + t + ")").addClass("active").attr("data-range-key");
        break;
      }
      t++;
    }
    e && (this.showCustomRangeLabel ? this.chosenLabel = this.container.find(".ranges li:last").addClass("active").attr("data-range-key") : this.chosenLabel = null, this.showCalendars());
  }
  clickApply(e) {
    this.hide(), this.element.trigger("apply.daterangepicker", this);
  }
  clickCancel(e) {
    this.startDate = this.oldStartDate, this.endDate = this.oldEndDate, this.hide(), this.element.trigger("cancel.daterangepicker", this);
  }
  monthOrYearChanged(e) {
    var t = Ie(e.target).closest(".drp-calendar").hasClass("left"), n = t ? "left" : "right", r = this.container.find(".drp-calendar." + n), a = parseInt(r.find(".monthselect").val(), 10), c = r.find(".yearselect").val();
    t || (c < this.startDate.year() || c == this.startDate.year() && a < this.startDate.month()) && (a = this.startDate.month(), c = this.startDate.year()), this.minDate && (c < this.minDate.year() || c == this.minDate.year() && a < this.minDate.month()) && (a = this.minDate.month(), c = this.minDate.year()), this.maxDate && (c > this.maxDate.year() || c == this.maxDate.year() && a > this.maxDate.month()) && (a = this.maxDate.month(), c = this.maxDate.year()), t ? (this.leftCalendar.month.month(a).year(c), this.linkedCalendars && (this.rightCalendar.month = this.leftCalendar.month.clone().add(1, "month"))) : (this.rightCalendar.month.month(a).year(c), this.linkedCalendars && (this.leftCalendar.month = this.rightCalendar.month.clone().subtract(1, "month"))), this.updateCalendars();
  }
  timeChanged(e) {
    var t = Ie(e.target).closest(".drp-calendar"), n = t.hasClass("left"), r = parseInt(t.find(".hourselect").val(), 10), a = parseInt(t.find(".minuteselect").val(), 10);
    isNaN(a) && (a = parseInt(t.find(".minuteselect option:last").val(), 10));
    var c = this.timePickerSeconds ? parseInt(t.find(".secondselect").val(), 10) : 0;
    if (!this.timePicker24Hour) {
      var u = t.find(".ampmselect").val();
      u === "PM" && r < 12 && (r += 12), u === "AM" && r === 12 && (r = 0);
    }
    if (n) {
      var p = this.startDate.clone();
      p.hour(r), p.minute(a), p.second(c), this.setStartDate(p), this.singleDatePicker ? this.endDate = this.startDate.clone() : this.endDate && this.endDate.format("YYYY-MM-DD") == p.format("YYYY-MM-DD") && this.endDate.isBefore(p) && this.setEndDate(p.clone());
    } else if (this.endDate) {
      var g = this.endDate.clone();
      g.hour(r), g.minute(a), g.second(c), this.setEndDate(g);
    }
    this.updateCalendars(), this.updateFormInputs(), this.renderTimePicker("left"), this.renderTimePicker("right");
  }
  elementChanged() {
    if (this.element.is("input") && this.element.val().length) {
      var e = this.element.val().split(this.locale.separator), t = null, n = null;
      e.length === 2 && (t = ue(e[0], this.locale.format), n = ue(e[1], this.locale.format)), (this.singleDatePicker || t === null || n === null) && (t = ue(this.element.val(), this.locale.format), n = t), !(!t.isValid() || !n.isValid()) && (this.setStartDate(t), this.setEndDate(n), this.updateView());
    }
  }
  keydown(e) {
    (e.keyCode === 9 || e.keyCode === 13) && this.hide(), e.keyCode === 27 && (e.preventDefault(), e.stopPropagation(), this.hide());
  }
  updateElement() {
    if (this.element.is("input") && this.autoUpdateInput) {
      var e = this.startDate.format(this.locale.format);
      this.singleDatePicker || (e += this.locale.separator + this.endDate.format(this.locale.format)), e !== this.element.val() && this.element.val(e).trigger("change");
    }
  }
  remove() {
    this.container.remove(), this.element.off(".daterangepicker"), this.element.removeData();
  }
};
Ie.fn.daterangepicker = function(o, e) {
  var t = Ie.extend(!0, {}, Ie.fn.daterangepicker.defaultOptions, o);
  return this.each(function() {
    var n = Ie(this);
    n.data("daterangepicker") && n.data("daterangepicker").remove(), n.data("daterangepicker", new Fy(n, t, e));
  }), this;
};
const et = {
  wrapper: "tc-wrap",
  wrapperReadOnly: "tc-wrap--readonly",
  table: "tc-table",
  row: "tc-row",
  withHeadings: "tc-table--heading",
  rowSelected: "tc-row--selected",
  cell: "tc-cell",
  cellSelected: "tc-cell--selected",
  addRow: "tc-add-row",
  addColumn: "tc-add-column",
  formInput: "vc-form-input"
};
let zy = class {
  /**
   * Creates
   *
   * @constructor
   * @param {boolean} readOnly - read-only mode flag
   * @param {object} api - Editor.js API
   * @param {TableData} data - Editor.js API
   * @param {TableConfig} config - Editor.js API
   */
  constructor(e, t, n, r, a) {
    var c;
    if (this.readOnly = e, this.api = t, this.block = a, this.config = r, this.isJapan = ((c = this.config) == null ? void 0 : c.locale) == "jp", n.content.length > 1) {
      const u = r.data.data.length;
      n.content.map((p, g) => p.splice(u));
    }
    n.heading = r.data.data.map((u) => this.isJapan ? u.heading_jp : u.heading), this.data = n, this.wrapper = null, this.table = null, this.interval = null, this.customDatePicker = null, this.toolboxRow = this.createRowToolbox(), this.toolBoxDue = null, this.createTableWrapper(), this.hoveredRow = 0, this.hoveredColumn = 0, this.selectedRow = 0, this.selectedColumn = 0, this.tunes = {
      withHeadings: !1
    }, this.resize(), this.fill(), setTimeout(() => {
      this.checkInput();
    }, 0), this.focusedCell = {
      row: 0,
      column: 0
    }, this.documentClicked = (u) => {
      const p = u.target.closest(`.${et.table}`) !== null, g = u.target.closest(`.${et.wrapper}`) === null;
      (p || g) && this.hideToolboxes();
      const v = u.target.closest(`.${et.addRow}`), k = u.target.closest(`.${et.addColumn}`);
      v && v.parentNode === this.wrapper ? (this.addRow(void 0, !0), this.hideToolboxes()) : k && k.parentNode === this.wrapper && this.hideToolboxes();
    }, this.readOnly || this.bindEvents();
  }
  checkInput() {
    this.table.querySelectorAll(`.${et.row}`).forEach((e, t) => {
      var n;
      t != 0 && ((n = e == null ? void 0 : e.firstChild) == null ? void 0 : n.childElementCount) == 0 && this.createColumnInput(t);
    });
  }
  createColumnInput(e) {
    var t, n, r, a;
    this.isJapan = ((t = this.config) == null ? void 0 : t.locale) == "jp";
    for (let c = 0; c < this.config.data.data.length; c++) {
      const u = this.config.data.data[c], p = this.getCell(e + 1, c + 1);
      if (!p)
        return;
      const g = ((a = (r = (n = this.data) == null ? void 0 : n.content) == null ? void 0 : r[e - 1]) == null ? void 0 : a[c]) || "";
      if (u.type == "search") {
        const v = Yt("input", et.formInput, {
          value: g,
          disabled: this.readOnly
        });
        this.api.listeners.on(v, "keyup", (k) => {
          const y = k.target.value.trim().replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
          clearTimeout(this.interval), this.interval = setTimeout(() => {
            this.fetchData(y, v);
          }, 500);
        }), p.appendChild(v);
      } else if (u.type == "date") {
        const v = Yt("input", et.formInput, {
          value: g,
          disabled: this.readOnly
        });
        Ie(v).daterangepicker({
          autohide: !0,
          singleDatePicker: !0,
          autoUpdateInput: !1,
          autoApply: !0,
          locale: {
            format: "DD-MM-YYYY"
          }
        }), Ie(v).on("apply.daterangepicker", (k, y) => {
          v.value = y.startDate.format("DD-MM-YYYY"), this.block.dispatchChange();
        }), p.appendChild(v);
      } else if (u.type == "textDate") {
        const v = Yt("input", et.formInput, {
          value: g,
          disabled: this.readOnly
        });
        Ie(v).daterangepicker({
          autohide: !0,
          singleDatePicker: !0,
          autoUpdateInput: !1,
          autoApply: !0,
          ranges: {
            "1 Week": [ue().add(1, "week")],
            "8 Week": [ue().add(8, "week")],
            "1 Year": [ue().add(1, "year")]
          },
          locale: {
            format: "DD-MM-YYYY"
          }
        }), Ie(v).on("apply.daterangepicker", (k, y) => {
          v.value = y.startDate.format("DD-MM-YYYY"), this.block.dispatchChange();
        }), p.appendChild(v);
      } else if (u.type == "select") {
        const v = Yt("select", et.formInput, {
          value: g,
          disabled: this.readOnly
        });
        let k = !1;
        u.options.map((y) => {
          y.label == g && (k = !0);
          const T = Yt("option", "", {
            label: this.isJapan ? y.label_jp : y.label,
            value: y.label,
            selected: g == y.label
          });
          v.appendChild(T);
        }), k || v.prepend(Yt("option", "", {
          label: g,
          value: g,
          selected: !0
        })), this.api.listeners.on(v, "change", (y) => {
          var T, x;
          if (y.target.value == "Others") {
            const M = y.target.parentElement, L = Yt("input", et.formInput, {
              placeholder: this.api.i18n.t("Enter text")
            });
            this.api.listeners.on(L, "focusout", () => {
              this.block.dispatchChange();
            }), (x = (T = M.children) == null ? void 0 : T[0]) == null || x.remove(), M.appendChild(L);
          }
          this.block.dispatchChange();
        }), p.appendChild(v);
      } else if (u.type == "text") {
        const v = Yt("input", et.formInput, {
          value: g,
          disabled: this.readOnly
        });
        p.appendChild(v);
      } else if (u.type == "checkbox") {
        const v = Yt("input", et.formInput, {
          checked: g,
          type: "checkbox",
          disabled: this.readOnly
        });
        this.api.listeners.on(v, "change", () => {
          this.block.dispatchChange();
        }), p.appendChild(v);
      }
    }
  }
  async fetchData(e = "", t) {
    const n = new Headers();
    n.append("Content-Type", "application/json");
    const r = JSON.stringify({
      search: e
    }), a = {
      method: "POST",
      headers: n,
      body: r
    }, c = await fetch(this.config.apiUrl, a).then((p) => p.json()).catch((p) => console.log("error", p)), u = [{
      label: this.api.i18n.t("Delete row"),
      icon: ah,
      hideIf: () => this.numberOfRows === 1,
      confirmationRequired: !0,
      onClick: () => {
        this.deleteRow(this.focusedCell.row), this.hideToolboxes();
      }
    }];
    this.focusedCell.row - 1, c.data.map((p) => {
      u.push({
        label: p.medicine,
        icon: Ny,
        confirmationRequired: !1,
        onClick: (g) => {
          p.medicine && (t.value = p.medicine), this.hideToolboxes(), this.block.dispatchChange();
        }
      });
    }), this.renderToolBox(u);
  }
  /**
   * Returns the rendered table wrapper
   *
   * @returns {Element}
   */
  getWrapper() {
    return this.wrapper;
  }
  /**
   * Hangs the necessary handlers to events
   */
  bindEvents() {
    this.wrapper.addEventListener("click", this.documentClicked), this.table.addEventListener("mousemove", Yy(150, (e) => this.onMouseMoveInTable(e)), { passive: !0 }), this.table.onkeypress = (e) => this.onKeyPressListener(e), this.table.addEventListener("keydown", (e) => this.onKeyDownListener(e)), this.table.addEventListener("keyup", (e) => this.onKeyUpListener(e)), this.table.addEventListener("focusin", (e) => this.focusInTableListener(e));
  }
  /**
  * Configures and creates the toolbox for manipulating with rows
  *
  * @returns {Toolbox}
  */
  createRowToolbox() {
    return new ch({
      api: this.api,
      cssModifier: "row",
      items: [
        {
          label: this.api.i18n.t("Add row above"),
          icon: Py,
          onClick: () => {
            this.selectedRow != 1 && (this.addRow(this.selectedRow, !0), this.hideToolboxes());
          }
        },
        {
          label: this.api.i18n.t("Add row below"),
          icon: Iy,
          onClick: () => {
            this.addRow(this.selectedRow + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete row"),
          icon: ah,
          hideIf: () => this.numberOfRows === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteRow(this.selectedRow), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  createCustomToolbox(e = []) {
    return new ch({
      table: this,
      api: this.api,
      cssModifier: "row",
      items: e,
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  renderToolBox(e = []) {
    this.toolBoxDue && this.toolBoxDue.element.remove(), this.toolBoxDue = this.createCustomToolbox(e), this.wrapper.appendChild(this.toolBoxDue.element), this.toolBoxDue.togglerClicked(), this.toolBoxDue.show(() => this.getUpdateLocation());
  }
  getUpdateLocation() {
    const e = this.hoveredRow, t = this.hoveredColumn, n = this.getRow(e), { fromTopBorder: r } = Ol(this.table, n), { height: a } = n.getBoundingClientRect();
    return {
      top: `${Math.ceil(r + a / 0.8)}px`,
      left: `calc((100% - var(--cell-size)) / (${this.numberOfColumns} * 2) * ( (${t} - 1) * 2))`
    };
  }
  /**
   * When you press enter it moves the cursor down to the next row
   * or creates it if the click occurred on the last one
   */
  moveCursorToNextRow() {
    this.focusedCell.row !== this.numberOfRows ? (this.focusedCell.row += 1, this.focusCell(this.focusedCell)) : (this.addRow(), this.focusedCell.row += 1, this.focusCell(this.focusedCell), this.updateToolboxesPosition(0, 0));
  }
  /**
   * Get table cell by row and col index
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @returns {HTMLElement}
   */
  getCell(e, t) {
    return this.table.querySelectorAll(`.${et.row}:nth-child(${e}) .${et.cell}`)[t - 1];
  }
  /**
   * Get table row by index
   *
   * @param {number} row - row coordinate
   * @returns {HTMLElement}
   */
  getRow(e) {
    return this.table.querySelector(`.${et.row}:nth-child(${e})`);
  }
  /**
   * The parent of the cell which is the row
   *
   * @param {HTMLElement} cell - cell element
   * @returns {HTMLElement}
   */
  getRowByCell(e) {
    return e.parentElement;
  }
  /**
   * Ger row's first cell
   *
   * @param {Element} row - row to find its first cell
   * @returns {Element}
   */
  getRowFirstCell(e) {
    return e.querySelector(`.${et.cell}:first-child`);
  }
  /**
   * Set the sell's content by row and column numbers
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @param {string} content - cell HTML content
   */
  setCellContent(e, t, n) {
    const r = this.getCell(e, t);
    "innerHTML" in r && (r.innerHTML = n);
  }
  /**
   * Add column in table on index place
   * Add cells in each row
   *
   * @param {number} columnIndex - number in the array of columns, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the first cell
   */
  addColumn(e = -1, t = !1) {
    let n = this.numberOfColumns;
    for (let r = 1; r <= this.numberOfRows; r++) {
      let a;
      const c = this.createCell();
      if (e > 0 && e <= n ? (a = this.getCell(r, e), rh(c, a)) : a = this.getRow(r).appendChild(c), r === 1) {
        const u = this.getCell(r, e > 0 ? e : n + 1);
        u && t && sh(u);
      }
    }
    this.addHeadingAttrToFirstRow();
  }
  /**
   * Add row in table on index place
   *
   * @param {number} index - number in the array of rows, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the inserted row
   * @returns {HTMLElement} row
   */
  addRow(e = -1, t = !1) {
    let n, r = Yt("div", et.row);
    this.tunes.withHeadings && this.removeHeadingAttrFromFirstRow();
    let a = this.numberOfColumns;
    if (e > 0 && e <= this.numberOfRows) {
      let u = this.getRow(e);
      n = rh(r, u);
    } else
      n = this.table.appendChild(r);
    this.fillRow(n, a), this.tunes.withHeadings && this.addHeadingAttrToFirstRow();
    const c = this.getRowFirstCell(n);
    return c && t && sh(c), this.checkInput(), n;
  }
  /**
   * Delete a column by index
   *
   * @param {number} index
   */
  deleteColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      if (!n)
        return;
      n.remove();
    }
  }
  /**
   * Delete a row by index
   *
   * @param {number} index
   */
  deleteRow(e) {
    this.getRow(e).remove(), this.addHeadingAttrToFirstRow();
  }
  /**
   * Create a wrapper containing a table, toolboxes
   * and buttons for adding rows and columns
   *
   * @returns {HTMLElement} wrapper - where all buttons for a table and the table itself will be
   */
  createTableWrapper() {
    if (this.wrapper ? this.table.remove() : this.wrapper = Yt("div", et.wrapper), this.table = Yt("div", et.table), this.wrapper.classList.add(et.wrapperReadOnly), this.wrapper.appendChild(this.toolboxRow.element), this.wrapper.appendChild(this.table), !this.readOnly) {
      const e = Yt("div", et.addColumn, {
        innerHTML: lh
      }), t = Yt("div", et.addRow, {
        innerHTML: lh
      });
      this.wrapper.appendChild(e), this.wrapper.appendChild(t);
    }
  }
  /**
   * Returns the size of the table based on initial data or config "size" property
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  computeInitialSize() {
    const e = this.data && this.data.content, t = Array.isArray(e), n = t ? e.length : !1, r = t ? e.length : void 0, a = n ? e[0].length : void 0, c = Number.parseInt(this.config && this.config.rows), u = Number.parseInt(this.config && this.config.cols), p = !isNaN(c) && c > 0 ? c : void 0, g = !isNaN(u) && u > 0 ? u : void 0;
    return {
      rows: r || p || 2,
      cols: a || g || 2
    };
  }
  /**
   * Resize table to match config size or transmitted data size
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  resize() {
    const e = (this.data.content.length || 1) + 1, t = this.data.heading.length;
    for (let n = 0; n < e; n++)
      this.addRow();
    for (let n = 0; n < t; n++)
      this.addColumn();
  }
  /**
   * Fills the table with data passed to the constructor
   *
   * @returns {void}
   */
  fill() {
    const e = this.data;
    if (e && e.heading)
      for (let t = 0; t < e.heading.length; t++)
        this.setCellContent(1, t + 1, e.heading[t]);
  }
  /**
   * Fills a row with cells
   *
   * @param {HTMLElement} row - row to fill
   * @param {number} numberOfColumns - how many cells should be in a row
   */
  fillRow(e, t) {
    for (let n = 1; n <= t; n++) {
      const r = this.createCell();
      e.appendChild(r);
    }
  }
  /**
   * Creating a cell element
   *
   * @return {Element}
   */
  createCell() {
    return Yt("div", et.cell, {
      contentEditable: !1
    });
  }
  /**
   * Get number of rows in the table
   */
  get numberOfRows() {
    return this.table.childElementCount;
  }
  /**
   * Get number of columns in the table
   */
  get numberOfColumns() {
    return this.numberOfRows ? this.table.querySelectorAll(`.${et.row}:first-child .${et.cell}`).length : 0;
  }
  /**
   * Is the column toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isColumnMenuShowing() {
    return this.selectedColumn !== 0;
  }
  /**
   * Is the row toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isRowMenuShowing() {
    return this.selectedRow !== 0;
  }
  /**
   * Recalculate position of toolbox icons
   *
   * @param {Event} event - mouse move event
   */
  onMouseMoveInTable(e) {
    const { row: t, column: n } = this.getHoveredCell(e);
    this.hoveredColumn = n, this.hoveredRow = t, this.updateToolboxesPosition();
  }
  /**
   * Prevents default Enter behaviors
   * Adds Shift+Enter processing
   *
   * @param {KeyboardEvent} event - keypress event
   */
  onKeyPressListener(e) {
    return e.key === "Enter" && e.shiftKey ? !0 : e.key !== "Enter";
  }
  /**
   * Prevents tab keydown event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyDownListener(e) {
    e.key === "Tab" && e.stopPropagation();
  }
  /**
   * Prevents tab keyup event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyUpListener(e) {
    if (e.key === "Tab") {
      e.stopPropagation();
      return;
    }
    if (e.key === "Enter") {
      e.stopPropagation();
      return;
    }
  }
  /**
   * Set the coordinates of the cell that the focus has moved to
   *
   * @param {FocusEvent} event - focusin event
   */
  focusInTableListener(e) {
    const t = e.target, n = this.getRowByCell(t);
    this.focusedCell = {
      row: Array.from(this.table.querySelectorAll(`.${et.row}`)).indexOf(n) + 1,
      column: Array.from(n.querySelectorAll(`.${et.cell}`)).indexOf(t) + 1
    };
  }
  /**
   * Unselect row/column
   * Close toolbox menu
   * Hide toolboxes
   *
   * @returns {void}
   */
  hideToolboxes() {
    this.hideRowToolbox(), this.updateToolboxesPosition();
  }
  /**
   * Unselect row, close toolbox
   *
   * @returns {void}
   */
  hideRowToolbox() {
    var e;
    this.unselectRow(), this.toolboxRow.hide(), (e = this.toolBoxDue) == null || e.hide();
  }
  /**
   * Unselect column, close toolbox
   *
   * @returns {void}
   */
  hideColumnToolbox() {
    this.unselectColumn();
  }
  /**
   * Set the cursor focus to the focused cell
   *
   * @returns {void}
   */
  focusCell() {
    var e;
    (e = this.focusedCellElem) == null || e.focus();
  }
  /**
   * Get current focused element
   *
   * @returns {HTMLElement} - focused cell
   */
  get focusedCellElem() {
    const { row: e, column: t } = this.focusedCell;
    return this.getCell(e, t);
  }
  /**
   * Update toolboxes position
   *
   * @param {number} row - hovered row
   * @param {number} column - hovered column
   */
  updateToolboxesPosition(e = this.hoveredRow, t = this.hoveredColumn) {
    this.isRowMenuShowing || e > 0 && e <= this.numberOfRows && this.toolboxRow.show(() => {
      const n = this.getRow(e), { fromTopBorder: r } = Ol(this.table, n), { height: a } = n.getBoundingClientRect();
      return {
        top: `${Math.ceil(r + a / 2)}px`
      };
    });
  }
  /**
   * Makes the first row headings
   *
   * @param {boolean} withHeadings - use headings row or not
   */
  setHeadingsSetting(e) {
    this.tunes.withHeadings = e, e ? (this.table.classList.add(et.withHeadings), this.addHeadingAttrToFirstRow()) : (this.table.classList.remove(et.withHeadings), this.removeHeadingAttrFromFirstRow());
  }
  /**
   * Adds an attribute for displaying the placeholder in the cell
   */
  addHeadingAttrToFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.setAttribute("heading", this.api.i18n.t("Heading"));
    }
  }
  /**
   * Removes an attribute for displaying the placeholder in the cell
   */
  removeHeadingAttrFromFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.removeAttribute("heading");
    }
  }
  /**
   * Add effect of a selected row
   *
   * @param {number} index
   */
  selectRow(e) {
    const t = this.getRow(e);
    t && (this.selectedRow = e, t.classList.add(et.rowSelected));
  }
  /**
   * Remove effect of a selected row
   */
  unselectRow() {
    if (this.selectedRow <= 0)
      return;
    const e = this.table.querySelector(`.${et.rowSelected}`);
    e && e.classList.remove(et.rowSelected), this.selectedRow = 0;
  }
  /**
   * Add effect of a selected column
   *
   * @param {number} index
   */
  selectColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      n && n.classList.add(et.cellSelected);
    }
    this.selectedColumn = e;
  }
  /**
   * Remove effect of a selected column
   */
  unselectColumn() {
    if (this.selectedColumn <= 0)
      return;
    let e = this.table.querySelectorAll(`.${et.cellSelected}`);
    Array.from(e).forEach((t) => {
      t.classList.remove(et.cellSelected);
    }), this.selectedColumn = 0;
  }
  /**
   * Calculates the row and column that the cursor is currently hovering over
   * The search was optimized from O(n) to O (log n) via bin search to reduce the number of calculations
   *
   * @param {Event} event - mousemove event
   * @returns hovered cell coordinates as an integer row and column
   */
  getHoveredCell(e) {
    let t = this.hoveredRow, n = this.hoveredColumn;
    const { width: r, height: a, x: c, y: u } = Ay(this.table, e);
    return c >= 0 && (n = this.binSearch(
      this.numberOfColumns,
      (p) => this.getCell(1, p),
      ({ fromLeftBorder: p }) => c < p,
      ({ fromRightBorder: p }) => c > r - p
    )), u >= 0 && (t = this.binSearch(
      this.numberOfRows,
      (p) => this.getCell(p, 1),
      ({ fromTopBorder: p }) => u < p,
      ({ fromBottomBorder: p }) => u > a - p
    )), {
      row: t || this.hoveredRow,
      column: n || this.hoveredColumn
    };
  }
  /**
   * Looks for the index of the cell the mouse is hovering over.
   * Cells can be represented as ordered intervals with left and
   * right (upper and lower for rows) borders inside the table, if the mouse enters it, then this is our index
   *
   * @param {number} numberOfCells - upper bound of binary search
   * @param {function} getCell - function to take the currently viewed cell
   * @param {function} beforeTheLeftBorder - determines the cursor position, to the left of the cell or not
   * @param {function} afterTheRightBorder - determines the cursor position, to the right of the cell or not
   * @returns {number}
   */
  binSearch(e, t, n, r) {
    let a = 0, c = e + 1, u = 0, p;
    for (; a < c - 1 && u < 10; ) {
      p = Math.ceil((a + c) / 2);
      const g = t(p), v = Ol(this.table, g);
      if (n(v))
        c = p;
      else if (r(v))
        a = p;
      else
        break;
      u++;
    }
    return p;
  }
  /**
   * Collects data from cells into a two-dimensional array
   *
   * @returns {string[][]}
   */
  getData() {
    const e = [];
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.table.querySelector(`.${et.row}:nth-child(${t})`), r = Array.from(n.querySelectorAll(`.${et.formInput}`));
      t != 1 && e.push(r.map((a) => {
        if (["text", "select-one", "number"].includes(a == null ? void 0 : a.type))
          return a == null ? void 0 : a.value;
        if ((a == null ? void 0 : a.type) === "checkbox")
          return a == null ? void 0 : a.checked;
      }));
    }
    return e;
  }
  /**
   * Remove listeners on the document
   */
  destroy() {
    document.removeEventListener("click", this.documentClicked);
  }
};
const Wy = {
  combinationVaccine: {
    title: "Combination Vaccine",
    title_jp: "混合ワクチン",
    data: [
      {
        heading: "Vaccination Medicine",
        heading_jp: "予防接種医学",
        type: "search",
        key: "vaccinationMedicine"
      },
      {
        heading: "Administration Date",
        heading_jp: "投与日",
        type: "date",
        key: "administrationDate"
      },
      {
        heading: "Due Date",
        heading_jp: "期日",
        type: "textDate",
        key: "dueDate"
      },
      {
        heading: "Lot Number",
        heading_jp: "整理番号",
        type: "text",
        key: "lotNumber"
      },
      {
        heading: "Allergic reaction",
        heading_jp: "アレルギー反応",
        type: "checkbox",
        key: "allergicReaction"
      },
      {
        heading: "Remarks",
        heading_jp: "備考",
        type: "text",
        key: "remarks"
      }
    ]
  },
  rabiesVaccine: {
    title: "Rabies Vaccine",
    title_jp: "狂犬病ワクチン",
    data: [
      {
        heading: "Vaccination Medicine",
        heading_jp: "予防接種医学",
        type: "search",
        key: "vaccinationMedicine"
      },
      {
        heading: "Administration Date",
        heading_jp: "投与日",
        type: "date",
        key: "administrationDate"
      },
      {
        heading: "Due date",
        heading_jp: "期日",
        type: "textDate",
        key: "dueDate"
      },
      {
        heading: "Lot number",
        heading_jp: "整理番号",
        type: "text",
        key: "lotNumber"
      },
      {
        heading: "(Rabies Vaccination Number)",
        heading_jp: "（狂犬病予防接種番号）",
        type: "text",
        key: "rabbiesVaccineNumber"
      },
      {
        heading: "Allergic reaction",
        heading_jp: "アレルギー反応",
        type: "checkbox",
        key: "allergicReaction"
      },
      {
        heading: "Remarks",
        heading_jp: "備考",
        type: "text",
        key: "remarks"
      }
    ]
  },
  rabiesDeferment: {
    title: "Rabies Deferment",
    title_jp: "狂犬病の延期",
    data: [
      {
        heading: "Title",
        heading_jp: "タイトル",
        type: "search",
        key: "vaccinationMedicine"
      },
      {
        heading: "Exemption Date",
        heading_jp: "免除日",
        type: "date",
        key: "exemptionDate"
      },
      {
        heading: "Deferral Period",
        heading_jp: "延期期間",
        type: "date",
        key: "deferralPeriod"
      },
      {
        heading: "Deferment reason",
        heading_jp: "延期理由",
        type: "select",
        key: "defermentReason",
        options: [
          {
            label: "May have heart failure, kidney failure, or other serious illness",
            label_jp: "心不全、腎不全、その他の深刻な病気にかかっている可能性がある",
            value: 1
          },
          {
            label: "Previous vaccination has caused anaphylaxis",
            label_jp: "以前のワクチン接種により、アナフィラキシーの症状を示した経験がある",
            value: 2
          },
          {
            label: "May have an infectious disease",
            label_jp: "感染症にかかっている可能性がある",
            value: 3
          },
          {
            label: "History of seizures and ongoing condition",
            label_jp: "痙攣の病歴があり、現在もその状態が続いている",
            value: 4
          },
          {
            label: "Pregnant dog",
            label_jp: "妊娠中の犬で、時期的に母犬への影響が懸念される",
            value: 5
          },
          {
            label: "The bite accident occurred and is in the appraisal period",
            label_jp: "咬傷事故を起こしたことにより、鑑定期間中である",
            value: 6
          },
          {
            label: "Others",
            label_jp: "その他",
            value: 7
          }
        ]
      },
      {
        heading: "Remarks",
        heading_jp: "備考",
        type: "text",
        key: "remarks"
      }
    ]
  }
}, Kr = {
  default: "combinationVaccine",
  data: Wy
};
class Uy {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to press Enter inside the CodeTool textarea
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {TableData} data — previously saved data
   * @param {TableConfig} config - user config for Tool
   * @param {object} api - Editor.js API
   * @param {boolean} readOnly - read-only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r, block: a }) {
    if (this.api = n, this.block = a, this.title = n.i18n.t("Vaccination"), this.readOnly = r, this.config = t, !t.apiUrl)
      throw new Error("Please provide API url for Vacination Search");
    this.data = {
      current: e && e.current ? e.current : Kr.default,
      content: e && e.content ? e.content : []
    }, setTimeout(() => {
      if (e != null && e.content)
        return;
      const c = this.block.id;
      this.api.toolbar.toggleBlockSettingsById(c);
    }, 500), this.table = null;
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: $y,
      title: this.title
    };
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLDivElement}
   */
  render() {
    var e;
    this.config.data = Kr.data[this.data.current], this.container ? (this.table.getWrapper().remove(), this.container.innerHTML = "") : this.container = Yt("div", this.api.styles.block), this.table = new zy(this.readOnly, this.api, this.data, this.config, this.block);
    const t = Yt("h5", "tc-header");
    return t.innerHTML = ((e = this.config) == null ? void 0 : e.locale) == "jp" ? this.config.data.title_jp : this.config.data.title, this.container.appendChild(t), this.container.appendChild(this.table.getWrapper()), this.table.setHeadingsSetting(!0), this.container;
  }
  /**
   * Returns plugin settings
   *
   * @returns {Array}
   */
  renderSettings() {
    var e;
    const t = [], n = ((e = this.config) == null ? void 0 : e.locale) === "jp";
    for (const r in Kr.data)
      if (Object.hasOwnProperty.call(Kr.data, r)) {
        const a = Kr.data[r];
        t.push({
          label: n ? a.title_jp : a.title,
          icon: jy,
          isActive: this.data.current == r,
          closeOnActivate: !0,
          toggle: !0,
          onActivate: () => {
            this.data.current = r, this.data.content = [], this.render();
          }
        });
      }
    return t;
  }
  /**
   * Extract table data from the view
   *
   * @returns {TableData} - saved data
   */
  save() {
    var e;
    const t = this.table.getData();
    return {
      current: ((e = this.data) == null ? void 0 : e.current) || Kr.default,
      content: t
    };
  }
  /**
   * Plugin destroyer
   *
   * @returns {void}
   */
  destroy() {
    this.table.destroy();
  }
  /**
   * A helper to get config value.
   * 
   * @param {string} configName - the key to get from the config. 
   * @param {any} defaultValue - default value if config doesn't have passed key
   * @param {object} savedData - previously saved data. If passed, the key will be got from there, otherwise from the config
   * @returns {any} - config value.
   */
  getConfig(e, t = void 0, n = void 0) {
    const r = this.data || n;
    return r ? r[e] ? r[e] : t : this.config && this.config[e] ? this.config[e] : t;
  }
  /**  
   * Table onPaste configuration
   *
   * @public
   */
  static get pasteConfig() {
    return { tags: ["TABLE", "TR", "TH", "TD"] };
  }
  /**
   * On paste callback that is fired from Editor
   *
   * @param {PasteEvent} event - event with pasted data
   */
  onPaste(e) {
    const t = e.detail.data, n = t.querySelector(":scope > thead, tr:first-of-type th"), r = Array.from(t.querySelectorAll("tr")).map((a) => Array.from(a.querySelectorAll("th, td")).map((c) => c.innerHTML));
    this.data = {
      withHeadings: n !== null,
      content: r
    }, this.table.wrapper && this.table.wrapper.replaceWith(this.render());
  }
}
(function() {
  var o;
  try {
    if (typeof document < "u") {
      var e = document.createElement("style");
      e.nonce = (o = document.head.querySelector("meta[property=csp-nonce]")) == null ? void 0 : o.content, e.appendChild(document.createTextNode('.daterangepicker{position:absolute;color:inherit;background-color:#fff;border-radius:4px;border:1px solid #ddd;width:278px;max-width:none;padding:0;margin-top:7px;top:100px;left:20px;z-index:3001;display:none;font-family:arial;font-size:15px;line-height:1em}.daterangepicker:after,.daterangepicker:before{position:absolute;display:inline-block;border-bottom-color:#0003;content:""}.daterangepicker:before{top:-7px;border-right:7px solid transparent;border-left:7px solid transparent;border-bottom:7px solid #ccc}.daterangepicker:after{top:-6px;border-right:6px solid transparent;border-bottom:6px solid #fff;border-left:6px solid transparent}.daterangepicker.opensleft:before{right:9px}.daterangepicker.opensleft:after{right:10px}.daterangepicker.openscenter:after,.daterangepicker.openscenter:before{left:0;right:0;width:0;margin-left:auto;margin-right:auto}.daterangepicker.opensright:before{left:9px}.daterangepicker.opensright:after{left:10px}.daterangepicker.drop-up{margin-top:-7px}.daterangepicker.drop-up:before{top:auto;bottom:-7px;border-bottom:initial;border-top:7px solid #ccc}.daterangepicker.drop-up:after{top:auto;bottom:-6px;border-bottom:initial;border-top:6px solid #fff}.daterangepicker.single .daterangepicker .ranges,.daterangepicker.single .drp-calendar{float:none}.daterangepicker.single .drp-selected{display:none}.daterangepicker.show-calendar .drp-buttons,.daterangepicker.show-calendar .drp-calendar{display:block}.daterangepicker.auto-apply .drp-buttons{display:none}.daterangepicker .drp-calendar{display:none;max-width:270px}.daterangepicker .drp-calendar.left{padding:8px 0 8px 8px}.daterangepicker .drp-calendar.right{padding:8px}.daterangepicker .drp-calendar.single .calendar-table{border:none}.daterangepicker .calendar-table .next span,.daterangepicker .calendar-table .prev span{color:#fff;border:solid #000;border-width:0 2px 2px 0;border-radius:0;display:inline-block;padding:3px}.daterangepicker .calendar-table .next span{transform:rotate(-45deg);-webkit-transform:rotate(-45deg)}.daterangepicker .calendar-table .prev span{transform:rotate(135deg);-webkit-transform:rotate(135deg)}.daterangepicker .calendar-table td,.daterangepicker .calendar-table th{text-align:center;vertical-align:middle;min-width:32px;width:32px;height:24px;line-height:24px;font-size:12px;border-radius:4px;border:1px solid transparent;white-space:nowrap;cursor:pointer}.daterangepicker .calendar-table{border:1px solid #fff;border-radius:4px;background-color:#fff}.daterangepicker .calendar-table table{width:100%;margin:0;border-spacing:0;border-collapse:collapse}.daterangepicker td.available:hover,.daterangepicker th.available:hover{background-color:#eee;border-color:transparent;color:inherit}.daterangepicker td.week,.daterangepicker th.week{font-size:80%;color:#ccc}.daterangepicker td.off,.daterangepicker td.off.end-date,.daterangepicker td.off.in-range,.daterangepicker td.off.start-date{background-color:#fff;border-color:transparent;color:#999}.daterangepicker td.in-range{background-color:#ebf4f8;border-color:transparent;color:#000;border-radius:0}.daterangepicker td.start-date{border-radius:4px 0 0 4px}.daterangepicker td.end-date{border-radius:0 4px 4px 0}.daterangepicker td.start-date.end-date{border-radius:4px}.daterangepicker td.active,.daterangepicker td.active:hover{background-color:#357ebd;border-color:transparent;color:#fff}.daterangepicker th.month{width:auto}.daterangepicker option.disabled,.daterangepicker td.disabled{color:#999;cursor:not-allowed;text-decoration:line-through}.daterangepicker select.monthselect,.daterangepicker select.yearselect{font-size:12px;padding:1px;height:auto;margin:0;cursor:default}.daterangepicker select.monthselect{margin-right:2%;width:56%}.daterangepicker select.yearselect{width:40%}.daterangepicker select.ampmselect,.daterangepicker select.hourselect,.daterangepicker select.minuteselect,.daterangepicker select.secondselect{width:50px;margin:0 auto;background:#eee;border:1px solid #eee;padding:2px;outline:0;font-size:12px}.daterangepicker .calendar-time{text-align:center;margin:4px auto 0;line-height:30px;position:relative}.daterangepicker .calendar-time select.disabled{color:#ccc;cursor:not-allowed}.daterangepicker .drp-buttons{clear:both;text-align:right;padding:8px;border-top:1px solid #ddd;display:none;line-height:12px;vertical-align:middle}.daterangepicker .drp-selected{display:inline-block;font-size:12px;padding-right:8px}.daterangepicker .drp-buttons .btn{margin-left:8px;font-size:12px;font-weight:700;padding:4px 8px}.daterangepicker.show-ranges.single.rtl .drp-calendar.left{border-right:1px solid #ddd}.daterangepicker.show-ranges.single.ltr .drp-calendar.left{border-left:1px solid #ddd}.daterangepicker.show-ranges.rtl .drp-calendar.right{border-right:1px solid #ddd}.daterangepicker.show-ranges.ltr .drp-calendar.left{border-left:1px solid #ddd}.daterangepicker .ranges{float:none;text-align:left;margin:0}.daterangepicker.show-calendar .ranges{margin-top:8px}.daterangepicker .ranges ul{list-style:none;margin:0 auto;padding:0;width:100%}.daterangepicker .ranges li{font-size:12px;padding:8px 12px;cursor:pointer}.daterangepicker .ranges li:hover{background-color:#eee}.daterangepicker .ranges li.active{background-color:#08c;color:#fff}@media (min-width:564px){.daterangepicker{width:auto}.daterangepicker .ranges ul{width:140px}.daterangepicker.single .ranges ul{width:100%}.daterangepicker.single .drp-calendar.left{clear:none}.daterangepicker.single .drp-calendar,.daterangepicker.single .ranges{float:left}.daterangepicker{direction:ltr;text-align:left}.daterangepicker .drp-calendar.left{clear:left;margin-right:0}.daterangepicker .drp-calendar.left .calendar-table{border-right:none;border-top-right-radius:0;border-bottom-right-radius:0}.daterangepicker .drp-calendar.right{margin-left:0}.daterangepicker .drp-calendar.right .calendar-table{border-left:none;border-top-left-radius:0;border-bottom-left-radius:0}.daterangepicker .drp-calendar.left .calendar-table{padding-right:8px}.daterangepicker .drp-calendar,.daterangepicker .ranges{float:left}}@media (min-width:730px){.daterangepicker .ranges{width:auto;float:left}.daterangepicker.rtl .ranges{float:right}.daterangepicker .drp-calendar.left{clear:none!important}}.tc-header{margin:0;padding:0;position:relative;bottom:-1rem;text-align:left}.tc-wrap{--color-background:#f9f9fb;--color-text-secondary:#7b7e89;--color-border:#e8e8eb;--cell-size:34px;--toolbox-icon-size:18px;--toolbox-padding:6px;--toolbox-aiming-field-size:calc(var(--toolbox-icon-size) + var(--toolbox-padding)*2);border-left:0;margin-top:var(--toolbox-icon-size);box-sizing:border-box;grid-template-columns:calc(100% - var(--cell-size)) var(--cell-size)}.tc-wrap--readonly{grid-template-columns:100% var(--cell-size)}.tc-wrap svg{vertical-align:top}@media print{.tc-wrap{border-left-color:var(--color-border);border-left-style:solid;border-left-width:1px;grid-template-columns:100% var(--cell-size)}}@media print{.tc-wrap .tc-row:after{display:none}}.tc-table,.tc-wrap{position:relative;height:100%;width:100%;display:grid}.tc-table{font-size:14px;border-top:1px solid var(--color-border);line-height:1.4}.tc-table:after{width:calc(var(--cell-size));left:calc(var(--cell-size)*-1);top:0}.tc-table:after,.tc-table:before{position:absolute;content:""}.tc-table:before{height:var(--toolbox-aiming-field-size);top:calc(var(--toolbox-aiming-field-size)*-1);left:0}.tc-table--heading .tc-row:first-child{font-weight:600;border-bottom:2px solid var(--color-border)}.tc-table--heading .tc-row:first-child [contenteditable]:empty:before{content:attr(heading);color:var(--color-text-secondary)}.tc-table--heading .tc-row:first-child:after{bottom:-2px;border-bottom:2px solid var(--color-border)}.tc-add-column,.tc-add-row{display:flex;color:var(--color-text-secondary)}@media print{.tc-add{display:none}}.tc-add-column{padding:4px 0;justify-content:center;border-top:1px solid var(--color-border);visibility:hidden}@media print{.tc-add-column{display:none}}.tc-add-row{height:var(--cell-size);align-items:center;padding-left:4px;position:relative}.tc-add-row:before{content:"";position:absolute;right:calc(var(--cell-size)*-1);width:var(--cell-size);height:100%}@media print{.tc-add-row{display:none}}.tc-add-column,.tc-add-row{transition:0s;cursor:pointer;will-change:background-color}.tc-add-column:hover,.tc-add-row:hover{transition:background-color .1s ease;background-color:var(--color-background)}.tc-add-row{margin-top:1px}.tc-add-row:hover:before{transition:.1s;background-color:var(--color-background)}.tc-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(10px,1fr));position:relative;border-bottom:1px solid var(--color-border)}.tc-row:after{content:"";pointer-events:none;position:absolute;width:var(--cell-size);height:100%;bottom:-1px;right:calc(var(--cell-size)*-1);border-bottom:1px solid var(--color-border)}.tc-row--selected{background:var(--color-background)}.tc-row--selected:after{background:var(--color-background)}.tc-cell{border-right:1px solid var(--color-border);padding:6px 12px;overflow:hidden;outline:none;line-break:normal}.tc-cell input{outline:none;border:none;width:100%;height:100%;padding:0;margin:0}.tc-cell--selected{background:var(--color-background)}.tc-wrap--readonly .tc-row:after{display:none}.tc-toolbox{--toolbox-padding:6px;--popover-margin:30px;--toggler-click-zone-size:30px;--toggler-dots-color:#7b7e89;--toggler-dots-color-hovered:#1d202b;position:absolute;cursor:pointer;z-index:1;opacity:0;transition:opacity .1s;will-change:left,opacity}.tc-toolbox--column{top:calc(var(--toggler-click-zone-size)*-1);transform:translate(calc(var(--toggler-click-zone-size)*-1/2));will-change:left,opacity}.tc-toolbox--row{left:calc(var(--popover-margin)*-1);transform:translateY(calc(var(--toggler-click-zone-size)*-1/2));margin-top:-1px;will-change:top,opacity}.tc-toolbox--showed{opacity:1}.tc-toolbox .tc-popover{position:absolute;top:0;left:var(--popover-margin)}.tc-toolbox__toggler{display:flex;align-items:center;justify-content:center;width:var(--toggler-click-zone-size);height:var(--toggler-click-zone-size);color:var(--toggler-dots-color);opacity:0;transition:opacity .15s ease;will-change:opacity}.tc-toolbox__toggler:hover{color:var(--toggler-dots-color-hovered)}.tc-toolbox__toggler svg{fill:currentColor}.tc-wrap:hover .tc-toolbox__toggler{opacity:1}.tc-settings .cdx-settings-button{width:50%;margin:0}.tc-popover{--color-border:#eaeaea;--color-background:#fff;--color-background-hover:rgba(232,232,235,.49);--color-background-confirm:#e24a4a;--color-background-confirm-hover:#d54040;--color-text-confirm:#fff;background:var(--color-background);border:1px solid var(--color-border);box-shadow:0 3px 15px -3px #0d142121;border-radius:6px;padding:6px;display:none;will-change:opacity,transform}.tc-popover--opened{display:block;animation:menuShowing .1s cubic-bezier(.215,.61,.355,1) forwards}.tc-popover__item{display:flex;align-items:center;padding:2px 14px 2px 2px;border-radius:5px;cursor:pointer;white-space:nowrap;-webkit-user-select:none;-moz-user-select:none;user-select:none}.tc-popover__item:hover{background:var(--color-background-hover)}.tc-popover__item:not(:last-of-type){margin-bottom:2px}.tc-popover__item-icon{display:inline-flex;width:26px;height:26px;align-items:center;justify-content:center;background:var(--color-background);border-radius:5px;border:1px solid var(--color-border);margin-right:8px}.tc-popover__item-label{line-height:22px;font-size:14px;font-weight:500}.tc-popover__item--confirm{background:var(--color-background-confirm);color:var(--color-text-confirm)}.tc-popover__item--confirm:hover{background-color:var(--color-background-confirm-hover)}.tc-popover__item--confirm .tc-popover__item-icon{background:var(--color-background-confirm);border-color:#0000001a}.tc-popover__item--confirm .tc-popover__item-icon svg{transition:transform .2s ease-in;transform:rotate(90deg) scale(1.2)}.tc-popover__item--hidden{display:none}@keyframes menuShowing{0%{opacity:0;transform:translateY(-8px) scale(.9)}70%{opacity:1;transform:translateY(2px)}to{transform:translateY(0)}}')), document.head.appendChild(e);
    }
  } catch (t) {
    console.error("vite-plugin-css-injected-by-js", t);
  }
})();
var qy = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Vy(o) {
  return o && o.__esModule && Object.prototype.hasOwnProperty.call(o, "default") ? o.default : o;
}
var ap = { exports: {} };
/*!
 * jQuery JavaScript Library v3.7.1
 * https://jquery.com/
 *
 * Copyright OpenJS Foundation and other contributors
 * Released under the MIT license
 * https://jquery.org/license
 *
 * Date: 2023-08-28T13:37Z
 */
(function(o) {
  (function(e, t) {
    o.exports = e.document ? t(e, !0) : function(n) {
      if (!n.document)
        throw new Error("jQuery requires a window with a document");
      return t(n);
    };
  })(typeof window < "u" ? window : qy, function(e, t) {
    var n = [], r = Object.getPrototypeOf, a = n.slice, c = n.flat ? function(i) {
      return n.flat.call(i);
    } : function(i) {
      return n.concat.apply([], i);
    }, u = n.push, p = n.indexOf, g = {}, v = g.toString, k = g.hasOwnProperty, y = k.toString, T = y.call(Object), x = {}, M = function(i) {
      return typeof i == "function" && typeof i.nodeType != "number" && typeof i.item != "function";
    }, L = function(i) {
      return i != null && i === i.window;
    }, Y = e.document, I = {
      type: !0,
      src: !0,
      nonce: !0,
      noModule: !0
    };
    function F(i, s, l) {
      l = l || Y;
      var h, f, m = l.createElement("script");
      if (m.text = i, s)
        for (h in I)
          f = s[h] || s.getAttribute && s.getAttribute(h), f && m.setAttribute(h, f);
      l.head.appendChild(m).parentNode.removeChild(m);
    }
    function Q(i) {
      return i == null ? i + "" : typeof i == "object" || typeof i == "function" ? g[v.call(i)] || "object" : typeof i;
    }
    var U = "3.7.1", z = /HTML$/i, d = function(i, s) {
      return new d.fn.init(i, s);
    };
    d.fn = d.prototype = {
      // The current version of jQuery being used
      jquery: U,
      constructor: d,
      // The default length of a jQuery object is 0
      length: 0,
      toArray: function() {
        return a.call(this);
      },
      // Get the Nth element in the matched element set OR
      // Get the whole matched element set as a clean array
      get: function(i) {
        return i == null ? a.call(this) : i < 0 ? this[i + this.length] : this[i];
      },
      // Take an array of elements and push it onto the stack
      // (returning the new matched element set)
      pushStack: function(i) {
        var s = d.merge(this.constructor(), i);
        return s.prevObject = this, s;
      },
      // Execute a callback for every element in the matched set.
      each: function(i) {
        return d.each(this, i);
      },
      map: function(i) {
        return this.pushStack(d.map(this, function(s, l) {
          return i.call(s, l, s);
        }));
      },
      slice: function() {
        return this.pushStack(a.apply(this, arguments));
      },
      first: function() {
        return this.eq(0);
      },
      last: function() {
        return this.eq(-1);
      },
      even: function() {
        return this.pushStack(d.grep(this, function(i, s) {
          return (s + 1) % 2;
        }));
      },
      odd: function() {
        return this.pushStack(d.grep(this, function(i, s) {
          return s % 2;
        }));
      },
      eq: function(i) {
        var s = this.length, l = +i + (i < 0 ? s : 0);
        return this.pushStack(l >= 0 && l < s ? [this[l]] : []);
      },
      end: function() {
        return this.prevObject || this.constructor();
      },
      // For internal use only.
      // Behaves like an Array's method, not like a jQuery method.
      push: u,
      sort: n.sort,
      splice: n.splice
    }, d.extend = d.fn.extend = function() {
      var i, s, l, h, f, m, b = arguments[0] || {}, E = 1, _ = arguments.length, O = !1;
      for (typeof b == "boolean" && (O = b, b = arguments[E] || {}, E++), typeof b != "object" && !M(b) && (b = {}), E === _ && (b = this, E--); E < _; E++)
        if ((i = arguments[E]) != null)
          for (s in i)
            h = i[s], !(s === "__proto__" || b === h) && (O && h && (d.isPlainObject(h) || (f = Array.isArray(h))) ? (l = b[s], f && !Array.isArray(l) ? m = [] : !f && !d.isPlainObject(l) ? m = {} : m = l, f = !1, b[s] = d.extend(O, m, h)) : h !== void 0 && (b[s] = h));
      return b;
    }, d.extend({
      // Unique for each copy of jQuery on the page
      expando: "jQuery" + (U + Math.random()).replace(/\D/g, ""),
      // Assume jQuery is ready without the ready module
      isReady: !0,
      error: function(i) {
        throw new Error(i);
      },
      noop: function() {
      },
      isPlainObject: function(i) {
        var s, l;
        return !i || v.call(i) !== "[object Object]" ? !1 : (s = r(i), s ? (l = k.call(s, "constructor") && s.constructor, typeof l == "function" && y.call(l) === T) : !0);
      },
      isEmptyObject: function(i) {
        var s;
        for (s in i)
          return !1;
        return !0;
      },
      // Evaluates a script in a provided context; falls back to the global one
      // if not specified.
      globalEval: function(i, s, l) {
        F(i, { nonce: s && s.nonce }, l);
      },
      each: function(i, s) {
        var l, h = 0;
        if (j(i))
          for (l = i.length; h < l && s.call(i[h], h, i[h]) !== !1; h++)
            ;
        else
          for (h in i)
            if (s.call(i[h], h, i[h]) === !1)
              break;
        return i;
      },
      // Retrieve the text value of an array of DOM nodes
      text: function(i) {
        var s, l = "", h = 0, f = i.nodeType;
        if (!f)
          for (; s = i[h++]; )
            l += d.text(s);
        return f === 1 || f === 11 ? i.textContent : f === 9 ? i.documentElement.textContent : f === 3 || f === 4 ? i.nodeValue : l;
      },
      // results is for internal usage only
      makeArray: function(i, s) {
        var l = s || [];
        return i != null && (j(Object(i)) ? d.merge(
          l,
          typeof i == "string" ? [i] : i
        ) : u.call(l, i)), l;
      },
      inArray: function(i, s, l) {
        return s == null ? -1 : p.call(s, i, l);
      },
      isXMLDoc: function(i) {
        var s = i && i.namespaceURI, l = i && (i.ownerDocument || i).documentElement;
        return !z.test(s || l && l.nodeName || "HTML");
      },
      // Support: Android <=4.0 only, PhantomJS 1 only
      // push.apply(_, arraylike) throws on ancient WebKit
      merge: function(i, s) {
        for (var l = +s.length, h = 0, f = i.length; h < l; h++)
          i[f++] = s[h];
        return i.length = f, i;
      },
      grep: function(i, s, l) {
        for (var h, f = [], m = 0, b = i.length, E = !l; m < b; m++)
          h = !s(i[m], m), h !== E && f.push(i[m]);
        return f;
      },
      // arg is for internal usage only
      map: function(i, s, l) {
        var h, f, m = 0, b = [];
        if (j(i))
          for (h = i.length; m < h; m++)
            f = s(i[m], m, l), f != null && b.push(f);
        else
          for (m in i)
            f = s(i[m], m, l), f != null && b.push(f);
        return c(b);
      },
      // A global GUID counter for objects
      guid: 1,
      // jQuery.support is not used in Core but other projects attach their
      // properties to it so it needs to exist.
      support: x
    }), typeof Symbol == "function" && (d.fn[Symbol.iterator] = n[Symbol.iterator]), d.each(
      "Boolean Number String Function Array Date RegExp Object Error Symbol".split(" "),
      function(i, s) {
        g["[object " + s + "]"] = s.toLowerCase();
      }
    );
    function j(i) {
      var s = !!i && "length" in i && i.length, l = Q(i);
      return M(i) || L(i) ? !1 : l === "array" || s === 0 || typeof s == "number" && s > 0 && s - 1 in i;
    }
    function R(i, s) {
      return i.nodeName && i.nodeName.toLowerCase() === s.toLowerCase();
    }
    var V = n.pop, K = n.sort, J = n.splice, A = "[\\x20\\t\\r\\n\\f]", se = new RegExp(
      "^" + A + "+|((?:^|[^\\\\])(?:\\\\.)*)" + A + "+$",
      "g"
    );
    d.contains = function(i, s) {
      var l = s && s.parentNode;
      return i === l || !!(l && l.nodeType === 1 && // Support: IE 9 - 11+
      // IE doesn't have `contains` on SVG.
      (i.contains ? i.contains(l) : i.compareDocumentPosition && i.compareDocumentPosition(l) & 16));
    };
    var Z = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\x80-\uFFFF\w-]/g;
    function ne(i, s) {
      return s ? i === "\0" ? "�" : i.slice(0, -1) + "\\" + i.charCodeAt(i.length - 1).toString(16) + " " : "\\" + i;
    }
    d.escapeSelector = function(i) {
      return (i + "").replace(Z, ne);
    };
    var le = Y, oe = u;
    (function() {
      var i, s, l, h, f, m = oe, b, E, _, O, W, q = d.expando, N = 0, X = 0, ge = ii(), Ce = ii(), xe = ii(), Qe = ii(), Ve = function(C, D) {
        return C === D && (f = !0), 0;
      }, Ht = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped", jt = "(?:\\\\[\\da-fA-F]{1,6}" + A + "?|\\\\[^\\r\\n\\f]|[\\w-]|[^\0-\\x7f])+", Se = "\\[" + A + "*(" + jt + ")(?:" + A + // Operator (capture 2)
      "*([*^$|!~]?=)" + A + // "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
      `*(?:'((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)"|(` + jt + "))|)" + A + "*\\]", ao = ":(" + jt + `)(?:\\((('((?:\\\\.|[^\\\\'])*)'|"((?:\\\\.|[^\\\\"])*)")|((?:\\\\.|[^\\\\()[\\]]|` + Se + ")*)|.*)\\)|)", De = new RegExp(A + "+", "g"), w = new RegExp("^" + A + "*," + A + "*"), S = new RegExp("^" + A + "*([>+~]|" + A + ")" + A + "*"), B = new RegExp(A + "|>"), H = new RegExp(ao), ee = new RegExp("^" + jt + "$"), he = {
        ID: new RegExp("^#(" + jt + ")"),
        CLASS: new RegExp("^\\.(" + jt + ")"),
        TAG: new RegExp("^(" + jt + "|[*])"),
        ATTR: new RegExp("^" + Se),
        PSEUDO: new RegExp("^" + ao),
        CHILD: new RegExp(
          "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + A + "*(even|odd|(([+-]|)(\\d*)n|)" + A + "*(?:([+-]|)" + A + "*(\\d+)|))" + A + "*\\)|)",
          "i"
        ),
        bool: new RegExp("^(?:" + Ht + ")$", "i"),
        // For use in libraries implementing .is()
        // We use this for POS matching in `select`
        needsContext: new RegExp("^" + A + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" + A + "*((?:-\\d)?\\d*)" + A + "*\\)|)(?=[^-]|$)", "i")
      }, ke = /^(?:input|select|textarea|button)$/i, Ee = /^h\d$/i, Be = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, St = /[+~]/, it = new RegExp("\\\\[\\da-fA-F]{1,6}" + A + "?|\\\\([^\\r\\n\\f])", "g"), Tt = function(C, D) {
        var P = "0x" + C.slice(1) - 65536;
        return D || (P < 0 ? String.fromCharCode(P + 65536) : String.fromCharCode(P >> 10 | 55296, P & 1023 | 56320));
      }, Nn = function() {
        lo();
      }, yn = si(
        function(C) {
          return C.disabled === !0 && R(C, "fieldset");
        },
        { dir: "parentNode", next: "legend" }
      );
      function bl() {
        try {
          return b.activeElement;
        } catch {
        }
      }
      try {
        m.apply(
          n = a.call(le.childNodes),
          le.childNodes
        ), n[le.childNodes.length].nodeType;
      } catch {
        m = {
          apply: function(C, D) {
            oe.apply(C, a.call(D));
          },
          call: function(C) {
            oe.apply(C, a.call(arguments, 1));
          }
        };
      }
      function Ne(C, D, P, $) {
        var G, te, ie, de, re, Te, be, we = D && D.ownerDocument, _e = D ? D.nodeType : 9;
        if (P = P || [], typeof C != "string" || !C || _e !== 1 && _e !== 9 && _e !== 11)
          return P;
        if (!$ && (lo(D), D = D || b, _)) {
          if (_e !== 11 && (re = Be.exec(C)))
            if (G = re[1]) {
              if (_e === 9)
                if (ie = D.getElementById(G)) {
                  if (ie.id === G)
                    return m.call(P, ie), P;
                } else
                  return P;
              else if (we && (ie = we.getElementById(G)) && Ne.contains(D, ie) && ie.id === G)
                return m.call(P, ie), P;
            } else {
              if (re[2])
                return m.apply(P, D.getElementsByTagName(C)), P;
              if ((G = re[3]) && D.getElementsByClassName)
                return m.apply(P, D.getElementsByClassName(G)), P;
            }
          if (!Qe[C + " "] && (!O || !O.test(C))) {
            if (be = C, we = D, _e === 1 && (B.test(C) || S.test(C))) {
              for (we = St.test(C) && wr(D.parentNode) || D, (we != D || !x.scope) && ((de = D.getAttribute("id")) ? de = d.escapeSelector(de) : D.setAttribute("id", de = q)), Te = wn(C), te = Te.length; te--; )
                Te[te] = (de ? "#" + de : ":scope") + " " + ri(Te[te]);
              be = Te.join(",");
            }
            try {
              return m.apply(
                P,
                we.querySelectorAll(be)
              ), P;
            } catch {
              Qe(C, !0);
            } finally {
              de === q && D.removeAttribute("id");
            }
          }
        }
        return zs(C.replace(se, "$1"), D, P, $);
      }
      function ii() {
        var C = [];
        function D(P, $) {
          return C.push(P + " ") > s.cacheLength && delete D[C.shift()], D[P + " "] = $;
        }
        return D;
      }
      function $t(C) {
        return C[q] = !0, C;
      }
      function zo(C) {
        var D = b.createElement("fieldset");
        try {
          return !!C(D);
        } catch {
          return !1;
        } finally {
          D.parentNode && D.parentNode.removeChild(D), D = null;
        }
      }
      function vl(C) {
        return function(D) {
          return R(D, "input") && D.type === C;
        };
      }
      function yl(C) {
        return function(D) {
          return (R(D, "input") || R(D, "button")) && D.type === C;
        };
      }
      function Ys(C) {
        return function(D) {
          return "form" in D ? D.parentNode && D.disabled === !1 ? "label" in D ? "label" in D.parentNode ? D.parentNode.disabled === C : D.disabled === C : D.isDisabled === C || // Where there is no isDisabled, check manually
          D.isDisabled !== !C && yn(D) === C : D.disabled === C : "label" in D ? D.disabled === C : !1;
        };
      }
      function _o(C) {
        return $t(function(D) {
          return D = +D, $t(function(P, $) {
            for (var G, te = C([], P.length, D), ie = te.length; ie--; )
              P[G = te[ie]] && (P[G] = !($[G] = P[G]));
          });
        });
      }
      function wr(C) {
        return C && typeof C.getElementsByTagName < "u" && C;
      }
      function lo(C) {
        var D, P = C ? C.ownerDocument || C : le;
        return P == b || P.nodeType !== 9 || !P.documentElement || (b = P, E = b.documentElement, _ = !d.isXMLDoc(b), W = E.matches || E.webkitMatchesSelector || E.msMatchesSelector, E.msMatchesSelector && // Support: IE 11+, Edge 17 - 18+
        // IE/Edge sometimes throw a "Permission denied" error when strict-comparing
        // two documents; shallow comparisons work.
        // eslint-disable-next-line eqeqeq
        le != b && (D = b.defaultView) && D.top !== D && D.addEventListener("unload", Nn), x.getById = zo(function($) {
          return E.appendChild($).id = d.expando, !b.getElementsByName || !b.getElementsByName(d.expando).length;
        }), x.disconnectedMatch = zo(function($) {
          return W.call($, "*");
        }), x.scope = zo(function() {
          return b.querySelectorAll(":scope");
        }), x.cssHas = zo(function() {
          try {
            return b.querySelector(":has(*,:jqfake)"), !1;
          } catch {
            return !0;
          }
        }), x.getById ? (s.filter.ID = function($) {
          var G = $.replace(it, Tt);
          return function(te) {
            return te.getAttribute("id") === G;
          };
        }, s.find.ID = function($, G) {
          if (typeof G.getElementById < "u" && _) {
            var te = G.getElementById($);
            return te ? [te] : [];
          }
        }) : (s.filter.ID = function($) {
          var G = $.replace(it, Tt);
          return function(te) {
            var ie = typeof te.getAttributeNode < "u" && te.getAttributeNode("id");
            return ie && ie.value === G;
          };
        }, s.find.ID = function($, G) {
          if (typeof G.getElementById < "u" && _) {
            var te, ie, de, re = G.getElementById($);
            if (re) {
              if (te = re.getAttributeNode("id"), te && te.value === $)
                return [re];
              for (de = G.getElementsByName($), ie = 0; re = de[ie++]; )
                if (te = re.getAttributeNode("id"), te && te.value === $)
                  return [re];
            }
            return [];
          }
        }), s.find.TAG = function($, G) {
          return typeof G.getElementsByTagName < "u" ? G.getElementsByTagName($) : G.querySelectorAll($);
        }, s.find.CLASS = function($, G) {
          if (typeof G.getElementsByClassName < "u" && _)
            return G.getElementsByClassName($);
        }, O = [], zo(function($) {
          var G;
          E.appendChild($).innerHTML = "<a id='" + q + "' href='' disabled='disabled'></a><select id='" + q + "-\r\\' disabled='disabled'><option selected=''></option></select>", $.querySelectorAll("[selected]").length || O.push("\\[" + A + "*(?:value|" + Ht + ")"), $.querySelectorAll("[id~=" + q + "-]").length || O.push("~="), $.querySelectorAll("a#" + q + "+*").length || O.push(".#.+[+~]"), $.querySelectorAll(":checked").length || O.push(":checked"), G = b.createElement("input"), G.setAttribute("type", "hidden"), $.appendChild(G).setAttribute("name", "D"), E.appendChild($).disabled = !0, $.querySelectorAll(":disabled").length !== 2 && O.push(":enabled", ":disabled"), G = b.createElement("input"), G.setAttribute("name", ""), $.appendChild(G), $.querySelectorAll("[name='']").length || O.push("\\[" + A + "*name" + A + "*=" + A + `*(?:''|"")`);
        }), x.cssHas || O.push(":has"), O = O.length && new RegExp(O.join("|")), Ve = function($, G) {
          if ($ === G)
            return f = !0, 0;
          var te = !$.compareDocumentPosition - !G.compareDocumentPosition;
          return te || (te = ($.ownerDocument || $) == (G.ownerDocument || G) ? $.compareDocumentPosition(G) : (
            // Otherwise we know they are disconnected
            1
          ), te & 1 || !x.sortDetached && G.compareDocumentPosition($) === te ? $ === b || $.ownerDocument == le && Ne.contains(le, $) ? -1 : G === b || G.ownerDocument == le && Ne.contains(le, G) ? 1 : h ? p.call(h, $) - p.call(h, G) : 0 : te & 4 ? -1 : 1);
        }), b;
      }
      Ne.matches = function(C, D) {
        return Ne(C, null, null, D);
      }, Ne.matchesSelector = function(C, D) {
        if (lo(C), _ && !Qe[D + " "] && (!O || !O.test(D)))
          try {
            var P = W.call(C, D);
            if (P || x.disconnectedMatch || // As well, disconnected nodes are said to be in a document
            // fragment in IE 9
            C.document && C.document.nodeType !== 11)
              return P;
          } catch {
            Qe(D, !0);
          }
        return Ne(D, b, null, [C]).length > 0;
      }, Ne.contains = function(C, D) {
        return (C.ownerDocument || C) != b && lo(C), d.contains(C, D);
      }, Ne.attr = function(C, D) {
        (C.ownerDocument || C) != b && lo(C);
        var P = s.attrHandle[D.toLowerCase()], $ = P && k.call(s.attrHandle, D.toLowerCase()) ? P(C, D, !_) : void 0;
        return $ !== void 0 ? $ : C.getAttribute(D);
      }, Ne.error = function(C) {
        throw new Error("Syntax error, unrecognized expression: " + C);
      }, d.uniqueSort = function(C) {
        var D, P = [], $ = 0, G = 0;
        if (f = !x.sortStable, h = !x.sortStable && a.call(C, 0), K.call(C, Ve), f) {
          for (; D = C[G++]; )
            D === C[G] && ($ = P.push(G));
          for (; $--; )
            J.call(C, P[$], 1);
        }
        return h = null, C;
      }, d.fn.uniqueSort = function() {
        return this.pushStack(d.uniqueSort(a.apply(this)));
      }, s = d.expr = {
        // Can be adjusted by the user
        cacheLength: 50,
        createPseudo: $t,
        match: he,
        attrHandle: {},
        find: {},
        relative: {
          ">": { dir: "parentNode", first: !0 },
          " ": { dir: "parentNode" },
          "+": { dir: "previousSibling", first: !0 },
          "~": { dir: "previousSibling" }
        },
        preFilter: {
          ATTR: function(C) {
            return C[1] = C[1].replace(it, Tt), C[3] = (C[3] || C[4] || C[5] || "").replace(it, Tt), C[2] === "~=" && (C[3] = " " + C[3] + " "), C.slice(0, 4);
          },
          CHILD: function(C) {
            return C[1] = C[1].toLowerCase(), C[1].slice(0, 3) === "nth" ? (C[3] || Ne.error(C[0]), C[4] = +(C[4] ? C[5] + (C[6] || 1) : 2 * (C[3] === "even" || C[3] === "odd")), C[5] = +(C[7] + C[8] || C[3] === "odd")) : C[3] && Ne.error(C[0]), C;
          },
          PSEUDO: function(C) {
            var D, P = !C[6] && C[2];
            return he.CHILD.test(C[0]) ? null : (C[3] ? C[2] = C[4] || C[5] || "" : P && H.test(P) && // Get excess from tokenize (recursively)
            (D = wn(P, !0)) && // advance to the next closing parenthesis
            (D = P.indexOf(")", P.length - D) - P.length) && (C[0] = C[0].slice(0, D), C[2] = P.slice(0, D)), C.slice(0, 3));
          }
        },
        filter: {
          TAG: function(C) {
            var D = C.replace(it, Tt).toLowerCase();
            return C === "*" ? function() {
              return !0;
            } : function(P) {
              return R(P, D);
            };
          },
          CLASS: function(C) {
            var D = ge[C + " "];
            return D || (D = new RegExp("(^|" + A + ")" + C + "(" + A + "|$)")) && ge(C, function(P) {
              return D.test(
                typeof P.className == "string" && P.className || typeof P.getAttribute < "u" && P.getAttribute("class") || ""
              );
            });
          },
          ATTR: function(C, D, P) {
            return function($) {
              var G = Ne.attr($, C);
              return G == null ? D === "!=" : D ? (G += "", D === "=" ? G === P : D === "!=" ? G !== P : D === "^=" ? P && G.indexOf(P) === 0 : D === "*=" ? P && G.indexOf(P) > -1 : D === "$=" ? P && G.slice(-P.length) === P : D === "~=" ? (" " + G.replace(De, " ") + " ").indexOf(P) > -1 : D === "|=" ? G === P || G.slice(0, P.length + 1) === P + "-" : !1) : !0;
            };
          },
          CHILD: function(C, D, P, $, G) {
            var te = C.slice(0, 3) !== "nth", ie = C.slice(-4) !== "last", de = D === "of-type";
            return $ === 1 && G === 0 ? (
              // Shortcut for :nth-*(n)
              function(re) {
                return !!re.parentNode;
              }
            ) : function(re, Te, be) {
              var we, _e, me, $e, yt, lt = te !== ie ? "nextSibling" : "previousSibling", Et = re.parentNode, Ut = de && re.nodeName.toLowerCase(), Wo = !be && !de, gt = !1;
              if (Et) {
                if (te) {
                  for (; lt; ) {
                    for (me = re; me = me[lt]; )
                      if (de ? R(me, Ut) : me.nodeType === 1)
                        return !1;
                    yt = lt = C === "only" && !yt && "nextSibling";
                  }
                  return !0;
                }
                if (yt = [ie ? Et.firstChild : Et.lastChild], ie && Wo) {
                  for (_e = Et[q] || (Et[q] = {}), we = _e[C] || [], $e = we[0] === N && we[1], gt = $e && we[2], me = $e && Et.childNodes[$e]; me = ++$e && me && me[lt] || // Fallback to seeking `elem` from the start
                  (gt = $e = 0) || yt.pop(); )
                    if (me.nodeType === 1 && ++gt && me === re) {
                      _e[C] = [N, $e, gt];
                      break;
                    }
                } else if (Wo && (_e = re[q] || (re[q] = {}), we = _e[C] || [], $e = we[0] === N && we[1], gt = $e), gt === !1)
                  for (; (me = ++$e && me && me[lt] || (gt = $e = 0) || yt.pop()) && !((de ? R(me, Ut) : me.nodeType === 1) && ++gt && (Wo && (_e = me[q] || (me[q] = {}), _e[C] = [N, gt]), me === re)); )
                    ;
                return gt -= G, gt === $ || gt % $ === 0 && gt / $ >= 0;
              }
            };
          },
          PSEUDO: function(C, D) {
            var P, $ = s.pseudos[C] || s.setFilters[C.toLowerCase()] || Ne.error("unsupported pseudo: " + C);
            return $[q] ? $(D) : $.length > 1 ? (P = [C, C, "", D], s.setFilters.hasOwnProperty(C.toLowerCase()) ? $t(function(G, te) {
              for (var ie, de = $(G, D), re = de.length; re--; )
                ie = p.call(G, de[re]), G[ie] = !(te[ie] = de[re]);
            }) : function(G) {
              return $(G, 0, P);
            }) : $;
          }
        },
        pseudos: {
          // Potentially complex pseudos
          not: $t(function(C) {
            var D = [], P = [], $ = Sr(C.replace(se, "$1"));
            return $[q] ? $t(function(G, te, ie, de) {
              for (var re, Te = $(G, null, de, []), be = G.length; be--; )
                (re = Te[be]) && (G[be] = !(te[be] = re));
            }) : function(G, te, ie) {
              return D[0] = G, $(D, null, ie, P), D[0] = null, !P.pop();
            };
          }),
          has: $t(function(C) {
            return function(D) {
              return Ne(C, D).length > 0;
            };
          }),
          contains: $t(function(C) {
            return C = C.replace(it, Tt), function(D) {
              return (D.textContent || d.text(D)).indexOf(C) > -1;
            };
          }),
          // "Whether an element is represented by a :lang() selector
          // is based solely on the element's language value
          // being equal to the identifier C,
          // or beginning with the identifier C immediately followed by "-".
          // The matching of C against the element's language value is performed case-insensitively.
          // The identifier C does not have to be a valid language name."
          // https://www.w3.org/TR/selectors/#lang-pseudo
          lang: $t(function(C) {
            return ee.test(C || "") || Ne.error("unsupported lang: " + C), C = C.replace(it, Tt).toLowerCase(), function(D) {
              var P;
              do
                if (P = _ ? D.lang : D.getAttribute("xml:lang") || D.getAttribute("lang"))
                  return P = P.toLowerCase(), P === C || P.indexOf(C + "-") === 0;
              while ((D = D.parentNode) && D.nodeType === 1);
              return !1;
            };
          }),
          // Miscellaneous
          target: function(C) {
            var D = e.location && e.location.hash;
            return D && D.slice(1) === C.id;
          },
          root: function(C) {
            return C === E;
          },
          focus: function(C) {
            return C === bl() && b.hasFocus() && !!(C.type || C.href || ~C.tabIndex);
          },
          // Boolean properties
          enabled: Ys(!1),
          disabled: Ys(!0),
          checked: function(C) {
            return R(C, "input") && !!C.checked || R(C, "option") && !!C.selected;
          },
          selected: function(C) {
            return C.parentNode && C.parentNode.selectedIndex, C.selected === !0;
          },
          // Contents
          empty: function(C) {
            for (C = C.firstChild; C; C = C.nextSibling)
              if (C.nodeType < 6)
                return !1;
            return !0;
          },
          parent: function(C) {
            return !s.pseudos.empty(C);
          },
          // Element/input types
          header: function(C) {
            return Ee.test(C.nodeName);
          },
          input: function(C) {
            return ke.test(C.nodeName);
          },
          button: function(C) {
            return R(C, "input") && C.type === "button" || R(C, "button");
          },
          text: function(C) {
            var D;
            return R(C, "input") && C.type === "text" && // Support: IE <10 only
            // New HTML5 attribute values (e.g., "search") appear
            // with elem.type === "text"
            ((D = C.getAttribute("type")) == null || D.toLowerCase() === "text");
          },
          // Position-in-collection
          first: _o(function() {
            return [0];
          }),
          last: _o(function(C, D) {
            return [D - 1];
          }),
          eq: _o(function(C, D, P) {
            return [P < 0 ? P + D : P];
          }),
          even: _o(function(C, D) {
            for (var P = 0; P < D; P += 2)
              C.push(P);
            return C;
          }),
          odd: _o(function(C, D) {
            for (var P = 1; P < D; P += 2)
              C.push(P);
            return C;
          }),
          lt: _o(function(C, D, P) {
            var $;
            for (P < 0 ? $ = P + D : P > D ? $ = D : $ = P; --$ >= 0; )
              C.push($);
            return C;
          }),
          gt: _o(function(C, D, P) {
            for (var $ = P < 0 ? P + D : P; ++$ < D; )
              C.push($);
            return C;
          })
        }
      }, s.pseudos.nth = s.pseudos.eq;
      for (i in { radio: !0, checkbox: !0, file: !0, password: !0, image: !0 })
        s.pseudos[i] = vl(i);
      for (i in { submit: !0, reset: !0 })
        s.pseudos[i] = yl(i);
      function Fs() {
      }
      Fs.prototype = s.filters = s.pseudos, s.setFilters = new Fs();
      function wn(C, D) {
        var P, $, G, te, ie, de, re, Te = Ce[C + " "];
        if (Te)
          return D ? 0 : Te.slice(0);
        for (ie = C, de = [], re = s.preFilter; ie; ) {
          (!P || ($ = w.exec(ie))) && ($ && (ie = ie.slice($[0].length) || ie), de.push(G = [])), P = !1, ($ = S.exec(ie)) && (P = $.shift(), G.push({
            value: P,
            // Cast descendant combinators to space
            type: $[0].replace(se, " ")
          }), ie = ie.slice(P.length));
          for (te in s.filter)
            ($ = he[te].exec(ie)) && (!re[te] || ($ = re[te]($))) && (P = $.shift(), G.push({
              value: P,
              type: te,
              matches: $
            }), ie = ie.slice(P.length));
          if (!P)
            break;
        }
        return D ? ie.length : ie ? Ne.error(C) : (
          // Cache the tokens
          Ce(C, de).slice(0)
        );
      }
      function ri(C) {
        for (var D = 0, P = C.length, $ = ""; D < P; D++)
          $ += C[D].value;
        return $;
      }
      function si(C, D, P) {
        var $ = D.dir, G = D.next, te = G || $, ie = P && te === "parentNode", de = X++;
        return D.first ? (
          // Check against closest ancestor/preceding element
          function(re, Te, be) {
            for (; re = re[$]; )
              if (re.nodeType === 1 || ie)
                return C(re, Te, be);
            return !1;
          }
        ) : (
          // Check against all ancestor/preceding elements
          function(re, Te, be) {
            var we, _e, me = [N, de];
            if (be) {
              for (; re = re[$]; )
                if ((re.nodeType === 1 || ie) && C(re, Te, be))
                  return !0;
            } else
              for (; re = re[$]; )
                if (re.nodeType === 1 || ie)
                  if (_e = re[q] || (re[q] = {}), G && R(re, G))
                    re = re[$] || re;
                  else {
                    if ((we = _e[te]) && we[0] === N && we[1] === de)
                      return me[2] = we[2];
                    if (_e[te] = me, me[2] = C(re, Te, be))
                      return !0;
                  }
            return !1;
          }
        );
      }
      function kr(C) {
        return C.length > 1 ? function(D, P, $) {
          for (var G = C.length; G--; )
            if (!C[G](D, P, $))
              return !1;
          return !0;
        } : C[0];
      }
      function wl(C, D, P) {
        for (var $ = 0, G = D.length; $ < G; $++)
          Ne(C, D[$], P);
        return P;
      }
      function ai(C, D, P, $, G) {
        for (var te, ie = [], de = 0, re = C.length, Te = D != null; de < re; de++)
          (te = C[de]) && (!P || P(te, $, G)) && (ie.push(te), Te && D.push(de));
        return ie;
      }
      function xr(C, D, P, $, G, te) {
        return $ && !$[q] && ($ = xr($)), G && !G[q] && (G = xr(G, te)), $t(function(ie, de, re, Te) {
          var be, we, _e, me, $e = [], yt = [], lt = de.length, Et = ie || wl(
            D || "*",
            re.nodeType ? [re] : re,
            []
          ), Ut = C && (ie || !D) ? ai(Et, $e, C, re, Te) : Et;
          if (P ? (me = G || (ie ? C : lt || $) ? (
            // ...intermediate processing is necessary
            []
          ) : (
            // ...otherwise use results directly
            de
          ), P(Ut, me, re, Te)) : me = Ut, $)
            for (be = ai(me, yt), $(be, [], re, Te), we = be.length; we--; )
              (_e = be[we]) && (me[yt[we]] = !(Ut[yt[we]] = _e));
          if (ie) {
            if (G || C) {
              if (G) {
                for (be = [], we = me.length; we--; )
                  (_e = me[we]) && be.push(Ut[we] = _e);
                G(null, me = [], be, Te);
              }
              for (we = me.length; we--; )
                (_e = me[we]) && (be = G ? p.call(ie, _e) : $e[we]) > -1 && (ie[be] = !(de[be] = _e));
            }
          } else
            me = ai(
              me === de ? me.splice(lt, me.length) : me
            ), G ? G(null, de, me, Te) : m.apply(de, me);
        });
      }
      function Cr(C) {
        for (var D, P, $, G = C.length, te = s.relative[C[0].type], ie = te || s.relative[" "], de = te ? 1 : 0, re = si(function(we) {
          return we === D;
        }, ie, !0), Te = si(function(we) {
          return p.call(D, we) > -1;
        }, ie, !0), be = [function(we, _e, me) {
          var $e = !te && (me || _e != l) || ((D = _e).nodeType ? re(we, _e, me) : Te(we, _e, me));
          return D = null, $e;
        }]; de < G; de++)
          if (P = s.relative[C[de].type])
            be = [si(kr(be), P)];
          else {
            if (P = s.filter[C[de].type].apply(null, C[de].matches), P[q]) {
              for ($ = ++de; $ < G && !s.relative[C[$].type]; $++)
                ;
              return xr(
                de > 1 && kr(be),
                de > 1 && ri(
                  // If the preceding token was a descendant combinator, insert an implicit any-element `*`
                  C.slice(0, de - 1).concat({ value: C[de - 2].type === " " ? "*" : "" })
                ).replace(se, "$1"),
                P,
                de < $ && Cr(C.slice(de, $)),
                $ < G && Cr(C = C.slice($)),
                $ < G && ri(C)
              );
            }
            be.push(P);
          }
        return kr(be);
      }
      function kl(C, D) {
        var P = D.length > 0, $ = C.length > 0, G = function(te, ie, de, re, Te) {
          var be, we, _e, me = 0, $e = "0", yt = te && [], lt = [], Et = l, Ut = te || $ && s.find.TAG("*", Te), Wo = N += Et == null ? 1 : Math.random() || 0.1, gt = Ut.length;
          for (Te && (l = ie == b || ie || Te); $e !== gt && (be = Ut[$e]) != null; $e++) {
            if ($ && be) {
              for (we = 0, !ie && be.ownerDocument != b && (lo(be), de = !_); _e = C[we++]; )
                if (_e(be, ie || b, de)) {
                  m.call(re, be);
                  break;
                }
              Te && (N = Wo);
            }
            P && ((be = !_e && be) && me--, te && yt.push(be));
          }
          if (me += $e, P && $e !== me) {
            for (we = 0; _e = D[we++]; )
              _e(yt, lt, ie, de);
            if (te) {
              if (me > 0)
                for (; $e--; )
                  yt[$e] || lt[$e] || (lt[$e] = V.call(re));
              lt = ai(lt);
            }
            m.apply(re, lt), Te && !te && lt.length > 0 && me + D.length > 1 && d.uniqueSort(re);
          }
          return Te && (N = Wo, l = Et), yt;
        };
        return P ? $t(G) : G;
      }
      function Sr(C, D) {
        var P, $ = [], G = [], te = xe[C + " "];
        if (!te) {
          for (D || (D = wn(C)), P = D.length; P--; )
            te = Cr(D[P]), te[q] ? $.push(te) : G.push(te);
          te = xe(
            C,
            kl(G, $)
          ), te.selector = C;
        }
        return te;
      }
      function zs(C, D, P, $) {
        var G, te, ie, de, re, Te = typeof C == "function" && C, be = !$ && wn(C = Te.selector || C);
        if (P = P || [], be.length === 1) {
          if (te = be[0] = be[0].slice(0), te.length > 2 && (ie = te[0]).type === "ID" && D.nodeType === 9 && _ && s.relative[te[1].type]) {
            if (D = (s.find.ID(
              ie.matches[0].replace(it, Tt),
              D
            ) || [])[0], D)
              Te && (D = D.parentNode);
            else
              return P;
            C = C.slice(te.shift().value.length);
          }
          for (G = he.needsContext.test(C) ? 0 : te.length; G-- && (ie = te[G], !s.relative[de = ie.type]); )
            if ((re = s.find[de]) && ($ = re(
              ie.matches[0].replace(it, Tt),
              St.test(te[0].type) && wr(D.parentNode) || D
            ))) {
              if (te.splice(G, 1), C = $.length && ri(te), !C)
                return m.apply(P, $), P;
              break;
            }
        }
        return (Te || Sr(C, be))(
          $,
          D,
          !_,
          P,
          !D || St.test(C) && wr(D.parentNode) || D
        ), P;
      }
      x.sortStable = q.split("").sort(Ve).join("") === q, lo(), x.sortDetached = zo(function(C) {
        return C.compareDocumentPosition(b.createElement("fieldset")) & 1;
      }), d.find = Ne, d.expr[":"] = d.expr.pseudos, d.unique = d.uniqueSort, Ne.compile = Sr, Ne.select = zs, Ne.setDocument = lo, Ne.tokenize = wn, Ne.escape = d.escapeSelector, Ne.getText = d.text, Ne.isXML = d.isXMLDoc, Ne.selectors = d.expr, Ne.support = d.support, Ne.uniqueSort = d.uniqueSort;
    })();
    var fe = function(i, s, l) {
      for (var h = [], f = l !== void 0; (i = i[s]) && i.nodeType !== 9; )
        if (i.nodeType === 1) {
          if (f && d(i).is(l))
            break;
          h.push(i);
        }
      return h;
    }, Me = function(i, s) {
      for (var l = []; i; i = i.nextSibling)
        i.nodeType === 1 && i !== s && l.push(i);
      return l;
    }, je = d.expr.match.needsContext, Ae = /^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i;
    function Je(i, s, l) {
      return M(s) ? d.grep(i, function(h, f) {
        return !!s.call(h, f, h) !== l;
      }) : s.nodeType ? d.grep(i, function(h) {
        return h === s !== l;
      }) : typeof s != "string" ? d.grep(i, function(h) {
        return p.call(s, h) > -1 !== l;
      }) : d.filter(s, i, l);
    }
    d.filter = function(i, s, l) {
      var h = s[0];
      return l && (i = ":not(" + i + ")"), s.length === 1 && h.nodeType === 1 ? d.find.matchesSelector(h, i) ? [h] : [] : d.find.matches(i, d.grep(s, function(f) {
        return f.nodeType === 1;
      }));
    }, d.fn.extend({
      find: function(i) {
        var s, l, h = this.length, f = this;
        if (typeof i != "string")
          return this.pushStack(d(i).filter(function() {
            for (s = 0; s < h; s++)
              if (d.contains(f[s], this))
                return !0;
          }));
        for (l = this.pushStack([]), s = 0; s < h; s++)
          d.find(i, f[s], l);
        return h > 1 ? d.uniqueSort(l) : l;
      },
      filter: function(i) {
        return this.pushStack(Je(this, i || [], !1));
      },
      not: function(i) {
        return this.pushStack(Je(this, i || [], !0));
      },
      is: function(i) {
        return !!Je(
          this,
          // If this is a positional/relative selector, check membership in the returned set
          // so $("p:first").is("p:last") won't return true for a doc with two "p".
          typeof i == "string" && je.test(i) ? d(i) : i || [],
          !1
        ).length;
      }
    });
    var no, yo = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/, Qo = d.fn.init = function(i, s, l) {
      var h, f;
      if (!i)
        return this;
      if (l = l || no, typeof i == "string")
        if (i[0] === "<" && i[i.length - 1] === ">" && i.length >= 3 ? h = [null, i, null] : h = yo.exec(i), h && (h[1] || !s))
          if (h[1]) {
            if (s = s instanceof d ? s[0] : s, d.merge(this, d.parseHTML(
              h[1],
              s && s.nodeType ? s.ownerDocument || s : Y,
              !0
            )), Ae.test(h[1]) && d.isPlainObject(s))
              for (h in s)
                M(this[h]) ? this[h](s[h]) : this.attr(h, s[h]);
            return this;
          } else
            return f = Y.getElementById(h[2]), f && (this[0] = f, this.length = 1), this;
        else
          return !s || s.jquery ? (s || l).find(i) : this.constructor(s).find(i);
      else {
        if (i.nodeType)
          return this[0] = i, this.length = 1, this;
        if (M(i))
          return l.ready !== void 0 ? l.ready(i) : (
            // Execute immediately if ready is not present
            i(d)
          );
      }
      return d.makeArray(i, this);
    };
    Qo.prototype = d.fn, no = d(Y);
    var Bn = /^(?:parents|prev(?:Until|All))/, hn = {
      children: !0,
      contents: !0,
      next: !0,
      prev: !0
    };
    d.fn.extend({
      has: function(i) {
        var s = d(i, this), l = s.length;
        return this.filter(function() {
          for (var h = 0; h < l; h++)
            if (d.contains(this, s[h]))
              return !0;
        });
      },
      closest: function(i, s) {
        var l, h = 0, f = this.length, m = [], b = typeof i != "string" && d(i);
        if (!je.test(i)) {
          for (; h < f; h++)
            for (l = this[h]; l && l !== s; l = l.parentNode)
              if (l.nodeType < 11 && (b ? b.index(l) > -1 : (
                // Don't pass non-elements to jQuery#find
                l.nodeType === 1 && d.find.matchesSelector(l, i)
              ))) {
                m.push(l);
                break;
              }
        }
        return this.pushStack(m.length > 1 ? d.uniqueSort(m) : m);
      },
      // Determine the position of an element within the set
      index: function(i) {
        return i ? typeof i == "string" ? p.call(d(i), this[0]) : p.call(
          this,
          // If it receives a jQuery object, the first element is used
          i.jquery ? i[0] : i
        ) : this[0] && this[0].parentNode ? this.first().prevAll().length : -1;
      },
      add: function(i, s) {
        return this.pushStack(
          d.uniqueSort(
            d.merge(this.get(), d(i, s))
          )
        );
      },
      addBack: function(i) {
        return this.add(
          i == null ? this.prevObject : this.prevObject.filter(i)
        );
      }
    });
    function un(i, s) {
      for (; (i = i[s]) && i.nodeType !== 1; )
        ;
      return i;
    }
    d.each({
      parent: function(i) {
        var s = i.parentNode;
        return s && s.nodeType !== 11 ? s : null;
      },
      parents: function(i) {
        return fe(i, "parentNode");
      },
      parentsUntil: function(i, s, l) {
        return fe(i, "parentNode", l);
      },
      next: function(i) {
        return un(i, "nextSibling");
      },
      prev: function(i) {
        return un(i, "previousSibling");
      },
      nextAll: function(i) {
        return fe(i, "nextSibling");
      },
      prevAll: function(i) {
        return fe(i, "previousSibling");
      },
      nextUntil: function(i, s, l) {
        return fe(i, "nextSibling", l);
      },
      prevUntil: function(i, s, l) {
        return fe(i, "previousSibling", l);
      },
      siblings: function(i) {
        return Me((i.parentNode || {}).firstChild, i);
      },
      children: function(i) {
        return Me(i.firstChild);
      },
      contents: function(i) {
        return i.contentDocument != null && // Support: IE 11+
        // <object> elements with no `data` attribute has an object
        // `contentDocument` with a `null` prototype.
        r(i.contentDocument) ? i.contentDocument : (R(i, "template") && (i = i.content || i), d.merge([], i.childNodes));
      }
    }, function(i, s) {
      d.fn[i] = function(l, h) {
        var f = d.map(this, s, l);
        return i.slice(-5) !== "Until" && (h = l), h && typeof h == "string" && (f = d.filter(h, f)), this.length > 1 && (hn[i] || d.uniqueSort(f), Bn.test(i) && f.reverse()), this.pushStack(f);
      };
    });
    var Rt = /[^\x20\t\r\n\f]+/g;
    function Di(i) {
      var s = {};
      return d.each(i.match(Rt) || [], function(l, h) {
        s[h] = !0;
      }), s;
    }
    d.Callbacks = function(i) {
      i = typeof i == "string" ? Di(i) : d.extend({}, i);
      var s, l, h, f, m = [], b = [], E = -1, _ = function() {
        for (f = f || i.once, h = s = !0; b.length; E = -1)
          for (l = b.shift(); ++E < m.length; )
            m[E].apply(l[0], l[1]) === !1 && i.stopOnFalse && (E = m.length, l = !1);
        i.memory || (l = !1), s = !1, f && (l ? m = [] : m = "");
      }, O = {
        // Add a callback or a collection of callbacks to the list
        add: function() {
          return m && (l && !s && (E = m.length - 1, b.push(l)), function W(q) {
            d.each(q, function(N, X) {
              M(X) ? (!i.unique || !O.has(X)) && m.push(X) : X && X.length && Q(X) !== "string" && W(X);
            });
          }(arguments), l && !s && _()), this;
        },
        // Remove a callback from the list
        remove: function() {
          return d.each(arguments, function(W, q) {
            for (var N; (N = d.inArray(q, m, N)) > -1; )
              m.splice(N, 1), N <= E && E--;
          }), this;
        },
        // Check if a given callback is in the list.
        // If no argument is given, return whether or not list has callbacks attached.
        has: function(W) {
          return W ? d.inArray(W, m) > -1 : m.length > 0;
        },
        // Remove all callbacks from the list
        empty: function() {
          return m && (m = []), this;
        },
        // Disable .fire and .add
        // Abort any current/pending executions
        // Clear all callbacks and values
        disable: function() {
          return f = b = [], m = l = "", this;
        },
        disabled: function() {
          return !m;
        },
        // Disable .fire
        // Also disable .add unless we have memory (since it would have no effect)
        // Abort any pending executions
        lock: function() {
          return f = b = [], !l && !s && (m = l = ""), this;
        },
        locked: function() {
          return !!f;
        },
        // Call all callbacks with the given context and arguments
        fireWith: function(W, q) {
          return f || (q = q || [], q = [W, q.slice ? q.slice() : q], b.push(q), s || _()), this;
        },
        // Call all the callbacks with the given arguments
        fire: function() {
          return O.fireWith(this, arguments), this;
        },
        // To know if the callbacks have already been called at least once
        fired: function() {
          return !!h;
        }
      };
      return O;
    };
    function io(i) {
      return i;
    }
    function Ho(i) {
      throw i;
    }
    function Un(i, s, l, h) {
      var f;
      try {
        i && M(f = i.promise) ? f.call(i).done(s).fail(l) : i && M(f = i.then) ? f.call(i, s, l) : s.apply(void 0, [i].slice(h));
      } catch (m) {
        l.apply(void 0, [m]);
      }
    }
    d.extend({
      Deferred: function(i) {
        var s = [
          // action, add listener, callbacks,
          // ... .then handlers, argument index, [final state]
          [
            "notify",
            "progress",
            d.Callbacks("memory"),
            d.Callbacks("memory"),
            2
          ],
          [
            "resolve",
            "done",
            d.Callbacks("once memory"),
            d.Callbacks("once memory"),
            0,
            "resolved"
          ],
          [
            "reject",
            "fail",
            d.Callbacks("once memory"),
            d.Callbacks("once memory"),
            1,
            "rejected"
          ]
        ], l = "pending", h = {
          state: function() {
            return l;
          },
          always: function() {
            return f.done(arguments).fail(arguments), this;
          },
          catch: function(m) {
            return h.then(null, m);
          },
          // Keep pipe for back-compat
          pipe: function() {
            var m = arguments;
            return d.Deferred(function(b) {
              d.each(s, function(E, _) {
                var O = M(m[_[4]]) && m[_[4]];
                f[_[1]](function() {
                  var W = O && O.apply(this, arguments);
                  W && M(W.promise) ? W.promise().progress(b.notify).done(b.resolve).fail(b.reject) : b[_[0] + "With"](
                    this,
                    O ? [W] : arguments
                  );
                });
              }), m = null;
            }).promise();
          },
          then: function(m, b, E) {
            var _ = 0;
            function O(W, q, N, X) {
              return function() {
                var ge = this, Ce = arguments, xe = function() {
                  var Ve, Ht;
                  if (!(W < _)) {
                    if (Ve = N.apply(ge, Ce), Ve === q.promise())
                      throw new TypeError("Thenable self-resolution");
                    Ht = Ve && // Support: Promises/A+ section 2.3.4
                    // https://promisesaplus.com/#point-64
                    // Only check objects and functions for thenability
                    (typeof Ve == "object" || typeof Ve == "function") && Ve.then, M(Ht) ? X ? Ht.call(
                      Ve,
                      O(_, q, io, X),
                      O(_, q, Ho, X)
                    ) : (_++, Ht.call(
                      Ve,
                      O(_, q, io, X),
                      O(_, q, Ho, X),
                      O(
                        _,
                        q,
                        io,
                        q.notifyWith
                      )
                    )) : (N !== io && (ge = void 0, Ce = [Ve]), (X || q.resolveWith)(ge, Ce));
                  }
                }, Qe = X ? xe : function() {
                  try {
                    xe();
                  } catch (Ve) {
                    d.Deferred.exceptionHook && d.Deferred.exceptionHook(
                      Ve,
                      Qe.error
                    ), W + 1 >= _ && (N !== Ho && (ge = void 0, Ce = [Ve]), q.rejectWith(ge, Ce));
                  }
                };
                W ? Qe() : (d.Deferred.getErrorHook ? Qe.error = d.Deferred.getErrorHook() : d.Deferred.getStackHook && (Qe.error = d.Deferred.getStackHook()), e.setTimeout(Qe));
              };
            }
            return d.Deferred(function(W) {
              s[0][3].add(
                O(
                  0,
                  W,
                  M(E) ? E : io,
                  W.notifyWith
                )
              ), s[1][3].add(
                O(
                  0,
                  W,
                  M(m) ? m : io
                )
              ), s[2][3].add(
                O(
                  0,
                  W,
                  M(b) ? b : Ho
                )
              );
            }).promise();
          },
          // Get a promise for this deferred
          // If obj is provided, the promise aspect is added to the object
          promise: function(m) {
            return m != null ? d.extend(m, h) : h;
          }
        }, f = {};
        return d.each(s, function(m, b) {
          var E = b[2], _ = b[5];
          h[b[1]] = E.add, _ && E.add(
            function() {
              l = _;
            },
            // rejected_callbacks.disable
            // fulfilled_callbacks.disable
            s[3 - m][2].disable,
            // rejected_handlers.disable
            // fulfilled_handlers.disable
            s[3 - m][3].disable,
            // progress_callbacks.lock
            s[0][2].lock,
            // progress_handlers.lock
            s[0][3].lock
          ), E.add(b[3].fire), f[b[0]] = function() {
            return f[b[0] + "With"](this === f ? void 0 : this, arguments), this;
          }, f[b[0] + "With"] = E.fireWith;
        }), h.promise(f), i && i.call(f, f), f;
      },
      // Deferred helper
      when: function(i) {
        var s = arguments.length, l = s, h = Array(l), f = a.call(arguments), m = d.Deferred(), b = function(E) {
          return function(_) {
            h[E] = this, f[E] = arguments.length > 1 ? a.call(arguments) : _, --s || m.resolveWith(h, f);
          };
        };
        if (s <= 1 && (Un(
          i,
          m.done(b(l)).resolve,
          m.reject,
          !s
        ), m.state() === "pending" || M(f[l] && f[l].then)))
          return m.then();
        for (; l--; )
          Un(f[l], b(l), m.reject);
        return m.promise();
      }
    });
    var Li = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;
    d.Deferred.exceptionHook = function(i, s) {
      e.console && e.console.warn && i && Li.test(i.name) && e.console.warn(
        "jQuery.Deferred exception: " + i.message,
        i.stack,
        s
      );
    }, d.readyException = function(i) {
      e.setTimeout(function() {
        throw i;
      });
    };
    var Mo = d.Deferred();
    d.fn.ready = function(i) {
      return Mo.then(i).catch(function(s) {
        d.readyException(s);
      }), this;
    }, d.extend({
      // Is the DOM ready to be used? Set to true once it occurs.
      isReady: !1,
      // A counter to track how many items to wait for before
      // the ready event fires. See trac-6781
      readyWait: 1,
      // Handle when the DOM is ready
      ready: function(i) {
        (i === !0 ? --d.readyWait : d.isReady) || (d.isReady = !0, !(i !== !0 && --d.readyWait > 0) && Mo.resolveWith(Y, [d]));
      }
    }), d.ready.then = Mo.then;
    function Ct() {
      Y.removeEventListener("DOMContentLoaded", Ct), e.removeEventListener("load", Ct), d.ready();
    }
    Y.readyState === "complete" || Y.readyState !== "loading" && !Y.documentElement.doScroll ? e.setTimeout(d.ready) : (Y.addEventListener("DOMContentLoaded", Ct), e.addEventListener("load", Ct));
    var vt = function(i, s, l, h, f, m, b) {
      var E = 0, _ = i.length, O = l == null;
      if (Q(l) === "object") {
        f = !0;
        for (E in l)
          vt(i, s, E, l[E], !0, m, b);
      } else if (h !== void 0 && (f = !0, M(h) || (b = !0), O && (b ? (s.call(i, h), s = null) : (O = s, s = function(W, q, N) {
        return O.call(d(W), N);
      })), s))
        for (; E < _; E++)
          s(
            i[E],
            l,
            b ? h : h.call(i[E], E, s(i[E], l))
          );
      return f ? i : O ? s.call(i) : _ ? s(i[0], l) : m;
    }, uo = /^-ms-/, er = /-([a-z])/g;
    function Nr(i, s) {
      return s.toUpperCase();
    }
    function It(i) {
      return i.replace(uo, "ms-").replace(er, Nr);
    }
    var We = function(i) {
      return i.nodeType === 1 || i.nodeType === 9 || !+i.nodeType;
    };
    function Ft() {
      this.expando = d.expando + Ft.uid++;
    }
    Ft.uid = 1, Ft.prototype = {
      cache: function(i) {
        var s = i[this.expando];
        return s || (s = {}, We(i) && (i.nodeType ? i[this.expando] = s : Object.defineProperty(i, this.expando, {
          value: s,
          configurable: !0
        }))), s;
      },
      set: function(i, s, l) {
        var h, f = this.cache(i);
        if (typeof s == "string")
          f[It(s)] = l;
        else
          for (h in s)
            f[It(h)] = s[h];
        return f;
      },
      get: function(i, s) {
        return s === void 0 ? this.cache(i) : (
          // Always use camelCase key (gh-2257)
          i[this.expando] && i[this.expando][It(s)]
        );
      },
      access: function(i, s, l) {
        return s === void 0 || s && typeof s == "string" && l === void 0 ? this.get(i, s) : (this.set(i, s, l), l !== void 0 ? l : s);
      },
      remove: function(i, s) {
        var l, h = i[this.expando];
        if (h !== void 0) {
          if (s !== void 0)
            for (Array.isArray(s) ? s = s.map(It) : (s = It(s), s = s in h ? [s] : s.match(Rt) || []), l = s.length; l--; )
              delete h[s[l]];
          (s === void 0 || d.isEmptyObject(h)) && (i.nodeType ? i[this.expando] = void 0 : delete i[this.expando]);
        }
      },
      hasData: function(i) {
        var s = i[this.expando];
        return s !== void 0 && !d.isEmptyObject(s);
      }
    };
    var ce = new Ft(), nt = new Ft(), Wt = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, Kt = /[A-Z]/g;
    function Pr(i) {
      return i === "true" ? !0 : i === "false" ? !1 : i === "null" ? null : i === +i + "" ? +i : Wt.test(i) ? JSON.parse(i) : i;
    }
    function An(i, s, l) {
      var h;
      if (l === void 0 && i.nodeType === 1)
        if (h = "data-" + s.replace(Kt, "-$&").toLowerCase(), l = i.getAttribute(h), typeof l == "string") {
          try {
            l = Pr(l);
          } catch {
          }
          nt.set(i, s, l);
        } else
          l = void 0;
      return l;
    }
    d.extend({
      hasData: function(i) {
        return nt.hasData(i) || ce.hasData(i);
      },
      data: function(i, s, l) {
        return nt.access(i, s, l);
      },
      removeData: function(i, s) {
        nt.remove(i, s);
      },
      // TODO: Now that all calls to _data and _removeData have been replaced
      // with direct calls to dataPriv methods, these can be deprecated.
      _data: function(i, s, l) {
        return ce.access(i, s, l);
      },
      _removeData: function(i, s) {
        ce.remove(i, s);
      }
    }), d.fn.extend({
      data: function(i, s) {
        var l, h, f, m = this[0], b = m && m.attributes;
        if (i === void 0) {
          if (this.length && (f = nt.get(m), m.nodeType === 1 && !ce.get(m, "hasDataAttrs"))) {
            for (l = b.length; l--; )
              b[l] && (h = b[l].name, h.indexOf("data-") === 0 && (h = It(h.slice(5)), An(m, h, f[h])));
            ce.set(m, "hasDataAttrs", !0);
          }
          return f;
        }
        return typeof i == "object" ? this.each(function() {
          nt.set(this, i);
        }) : vt(this, function(E) {
          var _;
          if (m && E === void 0)
            return _ = nt.get(m, i), _ !== void 0 || (_ = An(m, i), _ !== void 0) ? _ : void 0;
          this.each(function() {
            nt.set(this, i, E);
          });
        }, null, s, arguments.length > 1, null, !0);
      },
      removeData: function(i) {
        return this.each(function() {
          nt.remove(this, i);
        });
      }
    }), d.extend({
      queue: function(i, s, l) {
        var h;
        if (i)
          return s = (s || "fx") + "queue", h = ce.get(i, s), l && (!h || Array.isArray(l) ? h = ce.access(i, s, d.makeArray(l)) : h.push(l)), h || [];
      },
      dequeue: function(i, s) {
        s = s || "fx";
        var l = d.queue(i, s), h = l.length, f = l.shift(), m = d._queueHooks(i, s), b = function() {
          d.dequeue(i, s);
        };
        f === "inprogress" && (f = l.shift(), h--), f && (s === "fx" && l.unshift("inprogress"), delete m.stop, f.call(i, b, m)), !h && m && m.empty.fire();
      },
      // Not public - generate a queueHooks object, or return the current one
      _queueHooks: function(i, s) {
        var l = s + "queueHooks";
        return ce.get(i, l) || ce.access(i, l, {
          empty: d.Callbacks("once memory").add(function() {
            ce.remove(i, [s + "queue", l]);
          })
        });
      }
    }), d.fn.extend({
      queue: function(i, s) {
        var l = 2;
        return typeof i != "string" && (s = i, i = "fx", l--), arguments.length < l ? d.queue(this[0], i) : s === void 0 ? this : this.each(function() {
          var h = d.queue(this, i, s);
          d._queueHooks(this, i), i === "fx" && h[0] !== "inprogress" && d.dequeue(this, i);
        });
      },
      dequeue: function(i) {
        return this.each(function() {
          d.dequeue(this, i);
        });
      },
      clearQueue: function(i) {
        return this.queue(i || "fx", []);
      },
      // Get a promise resolved when queues of a certain type
      // are emptied (fx is the type by default)
      promise: function(i, s) {
        var l, h = 1, f = d.Deferred(), m = this, b = this.length, E = function() {
          --h || f.resolveWith(m, [m]);
        };
        for (typeof i != "string" && (s = i, i = void 0), i = i || "fx"; b--; )
          l = ce.get(m[b], i + "queueHooks"), l && l.empty && (h++, l.empty.add(E));
        return E(), f.promise(s);
      }
    });
    var Xt = /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/.source, Do = new RegExp("^(?:([+-])=|)(" + Xt + ")([a-z%]*)$", "i"), zt = ["Top", "Right", "Bottom", "Left"], Jt = Y.documentElement, po = function(i) {
      return d.contains(i.ownerDocument, i);
    }, Ue = { composed: !0 };
    Jt.getRootNode && (po = function(i) {
      return d.contains(i.ownerDocument, i) || i.getRootNode(Ue) === i.ownerDocument;
    });
    var pn = function(i, s) {
      return i = s || i, i.style.display === "none" || i.style.display === "" && // Otherwise, check computed style
      // Support: Firefox <=43 - 45
      // Disconnected elements can have computed display: none, so first confirm that elem is
      // in the document.
      po(i) && d.css(i, "display") === "none";
    };
    function tr(i, s, l, h) {
      var f, m, b = 20, E = h ? function() {
        return h.cur();
      } : function() {
        return d.css(i, s, "");
      }, _ = E(), O = l && l[3] || (d.cssNumber[s] ? "" : "px"), W = i.nodeType && (d.cssNumber[s] || O !== "px" && +_) && Do.exec(d.css(i, s));
      if (W && W[3] !== O) {
        for (_ = _ / 2, O = O || W[3], W = +_ || 1; b--; )
          d.style(i, s, W + O), (1 - m) * (1 - (m = E() / _ || 0.5)) <= 0 && (b = 0), W = W / m;
        W = W * 2, d.style(i, s, W + O), l = l || [];
      }
      return l && (W = +W || +_ || 0, f = l[1] ? W + (l[1] + 1) * l[2] : +l[2], h && (h.unit = O, h.start = W, h.end = f)), f;
    }
    var jo = {};
    function Ss(i) {
      var s, l = i.ownerDocument, h = i.nodeName, f = jo[h];
      return f || (s = l.body.appendChild(l.createElement(h)), f = d.css(s, "display"), s.parentNode.removeChild(s), f === "none" && (f = "block"), jo[h] = f, f);
    }
    function wo(i, s) {
      for (var l, h, f = [], m = 0, b = i.length; m < b; m++)
        h = i[m], h.style && (l = h.style.display, s ? (l === "none" && (f[m] = ce.get(h, "display") || null, f[m] || (h.style.display = "")), h.style.display === "" && pn(h) && (f[m] = Ss(h))) : l !== "none" && (f[m] = "none", ce.set(h, "display", l)));
      for (m = 0; m < b; m++)
        f[m] != null && (i[m].style.display = f[m]);
      return i;
    }
    d.fn.extend({
      show: function() {
        return wo(this, !0);
      },
      hide: function() {
        return wo(this);
      },
      toggle: function(i) {
        return typeof i == "boolean" ? i ? this.show() : this.hide() : this.each(function() {
          pn(this) ? d(this).show() : d(this).hide();
        });
      }
    });
    var ko = /^(?:checkbox|radio)$/i, or = /<([a-z][^\/\0>\x20\t\r\n\f]*)/i, nr = /^$|^module$|\/(?:java|ecma)script/i;
    (function() {
      var i = Y.createDocumentFragment(), s = i.appendChild(Y.createElement("div")), l = Y.createElement("input");
      l.setAttribute("type", "radio"), l.setAttribute("checked", "checked"), l.setAttribute("name", "t"), s.appendChild(l), x.checkClone = s.cloneNode(!0).cloneNode(!0).lastChild.checked, s.innerHTML = "<textarea>x</textarea>", x.noCloneChecked = !!s.cloneNode(!0).lastChild.defaultValue, s.innerHTML = "<option></option>", x.option = !!s.lastChild;
    })();
    var pt = {
      // XHTML parsers do not magically insert elements in the
      // same way that tag soup parsers do. So we cannot shorten
      // this by omitting <tbody> or other required elements.
      thead: [1, "<table>", "</table>"],
      col: [2, "<table><colgroup>", "</colgroup></table>"],
      tr: [2, "<table><tbody>", "</tbody></table>"],
      td: [3, "<table><tbody><tr>", "</tr></tbody></table>"],
      _default: [0, "", ""]
    };
    pt.tbody = pt.tfoot = pt.colgroup = pt.caption = pt.thead, pt.th = pt.td, x.option || (pt.optgroup = pt.option = [1, "<select multiple='multiple'>", "</select>"]);
    function ft(i, s) {
      var l;
      return typeof i.getElementsByTagName < "u" ? l = i.getElementsByTagName(s || "*") : typeof i.querySelectorAll < "u" ? l = i.querySelectorAll(s || "*") : l = [], s === void 0 || s && R(i, s) ? d.merge([i], l) : l;
    }
    function fn(i, s) {
      for (var l = 0, h = i.length; l < h; l++)
        ce.set(
          i[l],
          "globalEval",
          !s || ce.get(s[l], "globalEval")
        );
    }
    var Ts = /<|&#?\w+;/;
    function qe(i, s, l, h, f) {
      for (var m, b, E, _, O, W, q = s.createDocumentFragment(), N = [], X = 0, ge = i.length; X < ge; X++)
        if (m = i[X], m || m === 0)
          if (Q(m) === "object")
            d.merge(N, m.nodeType ? [m] : m);
          else if (!Ts.test(m))
            N.push(s.createTextNode(m));
          else {
            for (b = b || q.appendChild(s.createElement("div")), E = (or.exec(m) || ["", ""])[1].toLowerCase(), _ = pt[E] || pt._default, b.innerHTML = _[1] + d.htmlPrefilter(m) + _[2], W = _[0]; W--; )
              b = b.lastChild;
            d.merge(N, b.childNodes), b = q.firstChild, b.textContent = "";
          }
      for (q.textContent = "", X = 0; m = N[X++]; ) {
        if (h && d.inArray(m, h) > -1) {
          f && f.push(m);
          continue;
        }
        if (O = po(m), b = ft(q.appendChild(m), "script"), O && fn(b), l)
          for (W = 0; m = b[W++]; )
            nr.test(m.type || "") && l.push(m);
      }
      return q;
    }
    var ir = /^([^.]*)(?:\.(.+)|)/;
    function xo() {
      return !0;
    }
    function Co() {
      return !1;
    }
    function qn(i, s, l, h, f, m) {
      var b, E;
      if (typeof s == "object") {
        typeof l != "string" && (h = h || l, l = void 0);
        for (E in s)
          qn(i, E, l, h, s[E], m);
        return i;
      }
      if (h == null && f == null ? (f = l, h = l = void 0) : f == null && (typeof l == "string" ? (f = h, h = void 0) : (f = h, h = l, l = void 0)), f === !1)
        f = Co;
      else if (!f)
        return i;
      return m === 1 && (b = f, f = function(_) {
        return d().off(_), b.apply(this, arguments);
      }, f.guid = b.guid || (b.guid = d.guid++)), i.each(function() {
        d.event.add(this, s, f, h, l);
      });
    }
    d.event = {
      global: {},
      add: function(i, s, l, h, f) {
        var m, b, E, _, O, W, q, N, X, ge, Ce, xe = ce.get(i);
        if (We(i))
          for (l.handler && (m = l, l = m.handler, f = m.selector), f && d.find.matchesSelector(Jt, f), l.guid || (l.guid = d.guid++), (_ = xe.events) || (_ = xe.events = /* @__PURE__ */ Object.create(null)), (b = xe.handle) || (b = xe.handle = function(Qe) {
            return typeof d < "u" && d.event.triggered !== Qe.type ? d.event.dispatch.apply(i, arguments) : void 0;
          }), s = (s || "").match(Rt) || [""], O = s.length; O--; )
            E = ir.exec(s[O]) || [], X = Ce = E[1], ge = (E[2] || "").split(".").sort(), X && (q = d.event.special[X] || {}, X = (f ? q.delegateType : q.bindType) || X, q = d.event.special[X] || {}, W = d.extend({
              type: X,
              origType: Ce,
              data: h,
              handler: l,
              guid: l.guid,
              selector: f,
              needsContext: f && d.expr.match.needsContext.test(f),
              namespace: ge.join(".")
            }, m), (N = _[X]) || (N = _[X] = [], N.delegateCount = 0, (!q.setup || q.setup.call(i, h, ge, b) === !1) && i.addEventListener && i.addEventListener(X, b)), q.add && (q.add.call(i, W), W.handler.guid || (W.handler.guid = l.guid)), f ? N.splice(N.delegateCount++, 0, W) : N.push(W), d.event.global[X] = !0);
      },
      // Detach an event or set of events from an element
      remove: function(i, s, l, h, f) {
        var m, b, E, _, O, W, q, N, X, ge, Ce, xe = ce.hasData(i) && ce.get(i);
        if (!(!xe || !(_ = xe.events))) {
          for (s = (s || "").match(Rt) || [""], O = s.length; O--; ) {
            if (E = ir.exec(s[O]) || [], X = Ce = E[1], ge = (E[2] || "").split(".").sort(), !X) {
              for (X in _)
                d.event.remove(i, X + s[O], l, h, !0);
              continue;
            }
            for (q = d.event.special[X] || {}, X = (h ? q.delegateType : q.bindType) || X, N = _[X] || [], E = E[2] && new RegExp("(^|\\.)" + ge.join("\\.(?:.*\\.|)") + "(\\.|$)"), b = m = N.length; m--; )
              W = N[m], (f || Ce === W.origType) && (!l || l.guid === W.guid) && (!E || E.test(W.namespace)) && (!h || h === W.selector || h === "**" && W.selector) && (N.splice(m, 1), W.selector && N.delegateCount--, q.remove && q.remove.call(i, W));
            b && !N.length && ((!q.teardown || q.teardown.call(i, ge, xe.handle) === !1) && d.removeEvent(i, X, xe.handle), delete _[X]);
          }
          d.isEmptyObject(_) && ce.remove(i, "handle events");
        }
      },
      dispatch: function(i) {
        var s, l, h, f, m, b, E = new Array(arguments.length), _ = d.event.fix(i), O = (ce.get(this, "events") || /* @__PURE__ */ Object.create(null))[_.type] || [], W = d.event.special[_.type] || {};
        for (E[0] = _, s = 1; s < arguments.length; s++)
          E[s] = arguments[s];
        if (_.delegateTarget = this, !(W.preDispatch && W.preDispatch.call(this, _) === !1)) {
          for (b = d.event.handlers.call(this, _, O), s = 0; (f = b[s++]) && !_.isPropagationStopped(); )
            for (_.currentTarget = f.elem, l = 0; (m = f.handlers[l++]) && !_.isImmediatePropagationStopped(); )
              (!_.rnamespace || m.namespace === !1 || _.rnamespace.test(m.namespace)) && (_.handleObj = m, _.data = m.data, h = ((d.event.special[m.origType] || {}).handle || m.handler).apply(f.elem, E), h !== void 0 && (_.result = h) === !1 && (_.preventDefault(), _.stopPropagation()));
          return W.postDispatch && W.postDispatch.call(this, _), _.result;
        }
      },
      handlers: function(i, s) {
        var l, h, f, m, b, E = [], _ = s.delegateCount, O = i.target;
        if (_ && // Support: IE <=9
        // Black-hole SVG <use> instance trees (trac-13180)
        O.nodeType && // Support: Firefox <=42
        // Suppress spec-violating clicks indicating a non-primary pointer button (trac-3861)
        // https://www.w3.org/TR/DOM-Level-3-Events/#event-type-click
        // Support: IE 11 only
        // ...but not arrow key "clicks" of radio inputs, which can have `button` -1 (gh-2343)
        !(i.type === "click" && i.button >= 1)) {
          for (; O !== this; O = O.parentNode || this)
            if (O.nodeType === 1 && !(i.type === "click" && O.disabled === !0)) {
              for (m = [], b = {}, l = 0; l < _; l++)
                h = s[l], f = h.selector + " ", b[f] === void 0 && (b[f] = h.needsContext ? d(f, this).index(O) > -1 : d.find(f, this, null, [O]).length), b[f] && m.push(h);
              m.length && E.push({ elem: O, handlers: m });
            }
        }
        return O = this, _ < s.length && E.push({ elem: O, handlers: s.slice(_) }), E;
      },
      addProp: function(i, s) {
        Object.defineProperty(d.Event.prototype, i, {
          enumerable: !0,
          configurable: !0,
          get: M(s) ? function() {
            if (this.originalEvent)
              return s(this.originalEvent);
          } : function() {
            if (this.originalEvent)
              return this.originalEvent[i];
          },
          set: function(l) {
            Object.defineProperty(this, i, {
              enumerable: !0,
              configurable: !0,
              writable: !0,
              value: l
            });
          }
        });
      },
      fix: function(i) {
        return i[d.expando] ? i : new d.Event(i);
      },
      special: {
        load: {
          // Prevent triggered image.load events from bubbling to window.load
          noBubble: !0
        },
        click: {
          // Utilize native event to ensure correct state for checkable inputs
          setup: function(i) {
            var s = this || i;
            return ko.test(s.type) && s.click && R(s, "input") && $o(s, "click", !0), !1;
          },
          trigger: function(i) {
            var s = this || i;
            return ko.test(s.type) && s.click && R(s, "input") && $o(s, "click"), !0;
          },
          // For cross-browser consistency, suppress native .click() on links
          // Also prevent it if we're currently inside a leveraged native-event stack
          _default: function(i) {
            var s = i.target;
            return ko.test(s.type) && s.click && R(s, "input") && ce.get(s, "click") || R(s, "a");
          }
        },
        beforeunload: {
          postDispatch: function(i) {
            i.result !== void 0 && i.originalEvent && (i.originalEvent.returnValue = i.result);
          }
        }
      }
    };
    function $o(i, s, l) {
      if (!l) {
        ce.get(i, s) === void 0 && d.event.add(i, s, xo);
        return;
      }
      ce.set(i, s, !1), d.event.add(i, s, {
        namespace: !1,
        handler: function(h) {
          var f, m = ce.get(this, s);
          if (h.isTrigger & 1 && this[s]) {
            if (m)
              (d.event.special[s] || {}).delegateType && h.stopPropagation();
            else if (m = a.call(arguments), ce.set(this, s, m), this[s](), f = ce.get(this, s), ce.set(this, s, !1), m !== f)
              return h.stopImmediatePropagation(), h.preventDefault(), f;
          } else
            m && (ce.set(this, s, d.event.trigger(
              m[0],
              m.slice(1),
              this
            )), h.stopPropagation(), h.isImmediatePropagationStopped = xo);
        }
      });
    }
    d.removeEvent = function(i, s, l) {
      i.removeEventListener && i.removeEventListener(s, l);
    }, d.Event = function(i, s) {
      if (!(this instanceof d.Event))
        return new d.Event(i, s);
      i && i.type ? (this.originalEvent = i, this.type = i.type, this.isDefaultPrevented = i.defaultPrevented || i.defaultPrevented === void 0 && // Support: Android <=2.3 only
      i.returnValue === !1 ? xo : Co, this.target = i.target && i.target.nodeType === 3 ? i.target.parentNode : i.target, this.currentTarget = i.currentTarget, this.relatedTarget = i.relatedTarget) : this.type = i, s && d.extend(this, s), this.timeStamp = i && i.timeStamp || Date.now(), this[d.expando] = !0;
    }, d.Event.prototype = {
      constructor: d.Event,
      isDefaultPrevented: Co,
      isPropagationStopped: Co,
      isImmediatePropagationStopped: Co,
      isSimulated: !1,
      preventDefault: function() {
        var i = this.originalEvent;
        this.isDefaultPrevented = xo, i && !this.isSimulated && i.preventDefault();
      },
      stopPropagation: function() {
        var i = this.originalEvent;
        this.isPropagationStopped = xo, i && !this.isSimulated && i.stopPropagation();
      },
      stopImmediatePropagation: function() {
        var i = this.originalEvent;
        this.isImmediatePropagationStopped = xo, i && !this.isSimulated && i.stopImmediatePropagation(), this.stopPropagation();
      }
    }, d.each({
      altKey: !0,
      bubbles: !0,
      cancelable: !0,
      changedTouches: !0,
      ctrlKey: !0,
      detail: !0,
      eventPhase: !0,
      metaKey: !0,
      pageX: !0,
      pageY: !0,
      shiftKey: !0,
      view: !0,
      char: !0,
      code: !0,
      charCode: !0,
      key: !0,
      keyCode: !0,
      button: !0,
      buttons: !0,
      clientX: !0,
      clientY: !0,
      offsetX: !0,
      offsetY: !0,
      pointerId: !0,
      pointerType: !0,
      screenX: !0,
      screenY: !0,
      targetTouches: !0,
      toElement: !0,
      touches: !0,
      which: !0
    }, d.event.addProp), d.each({ focus: "focusin", blur: "focusout" }, function(i, s) {
      function l(h) {
        if (Y.documentMode) {
          var f = ce.get(this, "handle"), m = d.event.fix(h);
          m.type = h.type === "focusin" ? "focus" : "blur", m.isSimulated = !0, f(h), m.target === m.currentTarget && f(m);
        } else
          d.event.simulate(
            s,
            h.target,
            d.event.fix(h)
          );
      }
      d.event.special[i] = {
        // Utilize native event if possible so blur/focus sequence is correct
        setup: function() {
          var h;
          if ($o(this, i, !0), Y.documentMode)
            h = ce.get(this, s), h || this.addEventListener(s, l), ce.set(this, s, (h || 0) + 1);
          else
            return !1;
        },
        trigger: function() {
          return $o(this, i), !0;
        },
        teardown: function() {
          var h;
          if (Y.documentMode)
            h = ce.get(this, s) - 1, h ? ce.set(this, s, h) : (this.removeEventListener(s, l), ce.remove(this, s));
          else
            return !1;
        },
        // Suppress native focus or blur if we're currently inside
        // a leveraged native-event stack
        _default: function(h) {
          return ce.get(h.target, i);
        },
        delegateType: s
      }, d.event.special[s] = {
        setup: function() {
          var h = this.ownerDocument || this.document || this, f = Y.documentMode ? this : h, m = ce.get(f, s);
          m || (Y.documentMode ? this.addEventListener(s, l) : h.addEventListener(i, l, !0)), ce.set(f, s, (m || 0) + 1);
        },
        teardown: function() {
          var h = this.ownerDocument || this.document || this, f = Y.documentMode ? this : h, m = ce.get(f, s) - 1;
          m ? ce.set(f, s, m) : (Y.documentMode ? this.removeEventListener(s, l) : h.removeEventListener(i, l, !0), ce.remove(f, s));
        }
      };
    }), d.each({
      mouseenter: "mouseover",
      mouseleave: "mouseout",
      pointerenter: "pointerover",
      pointerleave: "pointerout"
    }, function(i, s) {
      d.event.special[i] = {
        delegateType: s,
        bindType: s,
        handle: function(l) {
          var h, f = this, m = l.relatedTarget, b = l.handleObj;
          return (!m || m !== f && !d.contains(f, m)) && (l.type = b.origType, h = b.handler.apply(this, arguments), l.type = s), h;
        }
      };
    }), d.fn.extend({
      on: function(i, s, l, h) {
        return qn(this, i, s, l, h);
      },
      one: function(i, s, l, h) {
        return qn(this, i, s, l, h, 1);
      },
      off: function(i, s, l) {
        var h, f;
        if (i && i.preventDefault && i.handleObj)
          return h = i.handleObj, d(i.delegateTarget).off(
            h.namespace ? h.origType + "." + h.namespace : h.origType,
            h.selector,
            h.handler
          ), this;
        if (typeof i == "object") {
          for (f in i)
            this.off(f, s, i[f]);
          return this;
        }
        return (s === !1 || typeof s == "function") && (l = s, s = void 0), l === !1 && (l = Co), this.each(function() {
          d.event.remove(this, i, l, s);
        });
      }
    });
    var Vn = /<script|<style|<link/i, Hr = /checked\s*(?:[^=]|=\s*.checked.)/i, oo = /^\s*<!\[CDATA\[|\]\]>\s*$/g;
    function Oi(i, s) {
      return R(i, "table") && R(s.nodeType !== 11 ? s : s.firstChild, "tr") && d(i).children("tbody")[0] || i;
    }
    function _s(i) {
      return i.type = (i.getAttribute("type") !== null) + "/" + i.type, i;
    }
    function jr(i) {
      return (i.type || "").slice(0, 5) === "true/" ? i.type = i.type.slice(5) : i.removeAttribute("type"), i;
    }
    function rr(i, s) {
      var l, h, f, m, b, E, _;
      if (s.nodeType === 1) {
        if (ce.hasData(i) && (m = ce.get(i), _ = m.events, _)) {
          ce.remove(s, "handle events");
          for (f in _)
            for (l = 0, h = _[f].length; l < h; l++)
              d.event.add(s, f, _[f][l]);
        }
        nt.hasData(i) && (b = nt.access(i), E = d.extend({}, b), nt.set(s, E));
      }
    }
    function $r(i, s) {
      var l = s.nodeName.toLowerCase();
      l === "input" && ko.test(i.type) ? s.checked = i.checked : (l === "input" || l === "textarea") && (s.defaultValue = i.defaultValue);
    }
    function So(i, s, l, h) {
      s = c(s);
      var f, m, b, E, _, O, W = 0, q = i.length, N = q - 1, X = s[0], ge = M(X);
      if (ge || q > 1 && typeof X == "string" && !x.checkClone && Hr.test(X))
        return i.each(function(Ce) {
          var xe = i.eq(Ce);
          ge && (s[0] = X.call(this, Ce, xe.html())), So(xe, s, l, h);
        });
      if (q && (f = qe(s, i[0].ownerDocument, !1, i, h), m = f.firstChild, f.childNodes.length === 1 && (f = m), m || h)) {
        for (b = d.map(ft(f, "script"), _s), E = b.length; W < q; W++)
          _ = f, W !== N && (_ = d.clone(_, !0, !0), E && d.merge(b, ft(_, "script"))), l.call(i[W], _, W);
        if (E)
          for (O = b[b.length - 1].ownerDocument, d.map(b, jr), W = 0; W < E; W++)
            _ = b[W], nr.test(_.type || "") && !ce.access(_, "globalEval") && d.contains(O, _) && (_.src && (_.type || "").toLowerCase() !== "module" ? d._evalUrl && !_.noModule && d._evalUrl(_.src, {
              nonce: _.nonce || _.getAttribute("nonce")
            }, O) : F(_.textContent.replace(oo, ""), _, O));
      }
      return i;
    }
    function Bi(i, s, l) {
      for (var h, f = s ? d.filter(s, i) : i, m = 0; (h = f[m]) != null; m++)
        !l && h.nodeType === 1 && d.cleanData(ft(h)), h.parentNode && (l && po(h) && fn(ft(h, "script")), h.parentNode.removeChild(h));
      return i;
    }
    d.extend({
      htmlPrefilter: function(i) {
        return i;
      },
      clone: function(i, s, l) {
        var h, f, m, b, E = i.cloneNode(!0), _ = po(i);
        if (!x.noCloneChecked && (i.nodeType === 1 || i.nodeType === 11) && !d.isXMLDoc(i))
          for (b = ft(E), m = ft(i), h = 0, f = m.length; h < f; h++)
            $r(m[h], b[h]);
        if (s)
          if (l)
            for (m = m || ft(i), b = b || ft(E), h = 0, f = m.length; h < f; h++)
              rr(m[h], b[h]);
          else
            rr(i, E);
        return b = ft(E, "script"), b.length > 0 && fn(b, !_ && ft(i, "script")), E;
      },
      cleanData: function(i) {
        for (var s, l, h, f = d.event.special, m = 0; (l = i[m]) !== void 0; m++)
          if (We(l)) {
            if (s = l[ce.expando]) {
              if (s.events)
                for (h in s.events)
                  f[h] ? d.event.remove(l, h) : d.removeEvent(l, h, s.handle);
              l[ce.expando] = void 0;
            }
            l[nt.expando] && (l[nt.expando] = void 0);
          }
      }
    }), d.fn.extend({
      detach: function(i) {
        return Bi(this, i, !0);
      },
      remove: function(i) {
        return Bi(this, i);
      },
      text: function(i) {
        return vt(this, function(s) {
          return s === void 0 ? d.text(this) : this.empty().each(function() {
            (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) && (this.textContent = s);
          });
        }, null, i, arguments.length);
      },
      append: function() {
        return So(this, arguments, function(i) {
          if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
            var s = Oi(this, i);
            s.appendChild(i);
          }
        });
      },
      prepend: function() {
        return So(this, arguments, function(i) {
          if (this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9) {
            var s = Oi(this, i);
            s.insertBefore(i, s.firstChild);
          }
        });
      },
      before: function() {
        return So(this, arguments, function(i) {
          this.parentNode && this.parentNode.insertBefore(i, this);
        });
      },
      after: function() {
        return So(this, arguments, function(i) {
          this.parentNode && this.parentNode.insertBefore(i, this.nextSibling);
        });
      },
      empty: function() {
        for (var i, s = 0; (i = this[s]) != null; s++)
          i.nodeType === 1 && (d.cleanData(ft(i, !1)), i.textContent = "");
        return this;
      },
      clone: function(i, s) {
        return i = i ?? !1, s = s ?? i, this.map(function() {
          return d.clone(this, i, s);
        });
      },
      html: function(i) {
        return vt(this, function(s) {
          var l = this[0] || {}, h = 0, f = this.length;
          if (s === void 0 && l.nodeType === 1)
            return l.innerHTML;
          if (typeof s == "string" && !Vn.test(s) && !pt[(or.exec(s) || ["", ""])[1].toLowerCase()]) {
            s = d.htmlPrefilter(s);
            try {
              for (; h < f; h++)
                l = this[h] || {}, l.nodeType === 1 && (d.cleanData(ft(l, !1)), l.innerHTML = s);
              l = 0;
            } catch {
            }
          }
          l && this.empty().append(s);
        }, null, i, arguments.length);
      },
      replaceWith: function() {
        var i = [];
        return So(this, arguments, function(s) {
          var l = this.parentNode;
          d.inArray(this, i) < 0 && (d.cleanData(ft(this)), l && l.replaceChild(s, this));
        }, i);
      }
    }), d.each({
      appendTo: "append",
      prependTo: "prepend",
      insertBefore: "before",
      insertAfter: "after",
      replaceAll: "replaceWith"
    }, function(i, s) {
      d.fn[i] = function(l) {
        for (var h, f = [], m = d(l), b = m.length - 1, E = 0; E <= b; E++)
          h = E === b ? this : this.clone(!0), d(m[E])[s](h), u.apply(f, h.get());
        return this.pushStack(f);
      };
    });
    var Gn = new RegExp("^(" + Xt + ")(?!px)[a-z%]+$", "i"), Zn = /^--/, en = function(i) {
      var s = i.ownerDocument.defaultView;
      return (!s || !s.opener) && (s = e), s.getComputedStyle(i);
    }, tn = function(i, s, l) {
      var h, f, m = {};
      for (f in s)
        m[f] = i.style[f], i.style[f] = s[f];
      h = l.call(i);
      for (f in s)
        i.style[f] = m[f];
      return h;
    }, Yr = new RegExp(zt.join("|"), "i");
    (function() {
      function i() {
        if (O) {
          _.style.cssText = "position:absolute;left:-11111px;width:60px;margin-top:1px;padding:0;border:0", O.style.cssText = "position:relative;display:block;box-sizing:border-box;overflow:scroll;margin:auto;border:1px;padding:1px;width:60%;top:1%", Jt.appendChild(_).appendChild(O);
          var W = e.getComputedStyle(O);
          l = W.top !== "1%", E = s(W.marginLeft) === 12, O.style.right = "60%", m = s(W.right) === 36, h = s(W.width) === 36, O.style.position = "absolute", f = s(O.offsetWidth / 3) === 12, Jt.removeChild(_), O = null;
        }
      }
      function s(W) {
        return Math.round(parseFloat(W));
      }
      var l, h, f, m, b, E, _ = Y.createElement("div"), O = Y.createElement("div");
      O.style && (O.style.backgroundClip = "content-box", O.cloneNode(!0).style.backgroundClip = "", x.clearCloneStyle = O.style.backgroundClip === "content-box", d.extend(x, {
        boxSizingReliable: function() {
          return i(), h;
        },
        pixelBoxStyles: function() {
          return i(), m;
        },
        pixelPosition: function() {
          return i(), l;
        },
        reliableMarginLeft: function() {
          return i(), E;
        },
        scrollboxSize: function() {
          return i(), f;
        },
        // Support: IE 9 - 11+, Edge 15 - 18+
        // IE/Edge misreport `getComputedStyle` of table rows with width/height
        // set in CSS while `offset*` properties report correct values.
        // Behavior in IE 9 is more subtle than in newer versions & it passes
        // some versions of this test; make sure not to make it pass there!
        //
        // Support: Firefox 70+
        // Only Firefox includes border widths
        // in computed dimensions. (gh-4529)
        reliableTrDimensions: function() {
          var W, q, N, X;
          return b == null && (W = Y.createElement("table"), q = Y.createElement("tr"), N = Y.createElement("div"), W.style.cssText = "position:absolute;left:-11111px;border-collapse:separate", q.style.cssText = "box-sizing:content-box;border:1px solid", q.style.height = "1px", N.style.height = "9px", N.style.display = "block", Jt.appendChild(W).appendChild(q).appendChild(N), X = e.getComputedStyle(q), b = parseInt(X.height, 10) + parseInt(X.borderTopWidth, 10) + parseInt(X.borderBottomWidth, 10) === q.offsetHeight, Jt.removeChild(W)), b;
        }
      }));
    })();
    function Lo(i, s, l) {
      var h, f, m, b, E = Zn.test(s), _ = i.style;
      return l = l || en(i), l && (b = l.getPropertyValue(s) || l[s], E && b && (b = b.replace(se, "$1") || void 0), b === "" && !po(i) && (b = d.style(i, s)), !x.pixelBoxStyles() && Gn.test(b) && Yr.test(s) && (h = _.width, f = _.minWidth, m = _.maxWidth, _.minWidth = _.maxWidth = _.width = b, b = l.width, _.width = h, _.minWidth = f, _.maxWidth = m)), b !== void 0 ? (
        // Support: IE <=9 - 11 only
        // IE returns zIndex value as an integer.
        b + ""
      ) : b;
    }
    function sr(i, s) {
      return {
        get: function() {
          if (i()) {
            delete this.get;
            return;
          }
          return (this.get = s).apply(this, arguments);
        }
      };
    }
    var ar = ["Webkit", "Moz", "ms"], lr = Y.createElement("div").style, cr = {};
    function Es(i) {
      for (var s = i[0].toUpperCase() + i.slice(1), l = ar.length; l--; )
        if (i = ar[l] + s, i in lr)
          return i;
    }
    function Kn(i) {
      var s = d.cssProps[i] || cr[i];
      return s || (i in lr ? i : cr[i] = Es(i) || i);
    }
    var Ms = /^(none|table(?!-c[ea]).+)/, mn = { position: "absolute", visibility: "hidden", display: "block" }, Ai = {
      letterSpacing: "0",
      fontWeight: "400"
    };
    function dr(i, s, l) {
      var h = Do.exec(s);
      return h ? (
        // Guard against undefined "subtract", e.g., when used as in cssHooks
        Math.max(0, h[2] - (l || 0)) + (h[3] || "px")
      ) : s;
    }
    function Xn(i, s, l, h, f, m) {
      var b = s === "width" ? 1 : 0, E = 0, _ = 0, O = 0;
      if (l === (h ? "border" : "content"))
        return 0;
      for (; b < 4; b += 2)
        l === "margin" && (O += d.css(i, l + zt[b], !0, f)), h ? (l === "content" && (_ -= d.css(i, "padding" + zt[b], !0, f)), l !== "margin" && (_ -= d.css(i, "border" + zt[b] + "Width", !0, f))) : (_ += d.css(i, "padding" + zt[b], !0, f), l !== "padding" ? _ += d.css(i, "border" + zt[b] + "Width", !0, f) : E += d.css(i, "border" + zt[b] + "Width", !0, f));
      return !h && m >= 0 && (_ += Math.max(0, Math.ceil(
        i["offset" + s[0].toUpperCase() + s.slice(1)] - m - _ - E - 0.5
        // If offsetWidth/offsetHeight is unknown, then we can't determine content-box scroll gutter
        // Use an explicit zero to avoid NaN (gh-3964)
      )) || 0), _ + O;
    }
    function hr(i, s, l) {
      var h = en(i), f = !x.boxSizingReliable() || l, m = f && d.css(i, "boxSizing", !1, h) === "border-box", b = m, E = Lo(i, s, h), _ = "offset" + s[0].toUpperCase() + s.slice(1);
      if (Gn.test(E)) {
        if (!l)
          return E;
        E = "auto";
      }
      return (!x.boxSizingReliable() && m || // Support: IE 10 - 11+, Edge 15 - 18+
      // IE/Edge misreport `getComputedStyle` of table rows with width/height
      // set in CSS while `offset*` properties report correct values.
      // Interestingly, in some cases IE 9 doesn't suffer from this issue.
      !x.reliableTrDimensions() && R(i, "tr") || // Fall back to offsetWidth/offsetHeight when value is "auto"
      // This happens for inline elements with no explicit setting (gh-3571)
      E === "auto" || // Support: Android <=4.1 - 4.3 only
      // Also use offsetWidth/offsetHeight for misreported inline dimensions (gh-3602)
      !parseFloat(E) && d.css(i, "display", !1, h) === "inline") && // Make sure the element is visible & connected
      i.getClientRects().length && (m = d.css(i, "boxSizing", !1, h) === "border-box", b = _ in i, b && (E = i[_])), E = parseFloat(E) || 0, E + Xn(
        i,
        s,
        l || (m ? "border" : "content"),
        b,
        h,
        // Provide the current computed size to request scroll gutter calculation (gh-3589)
        E
      ) + "px";
    }
    d.extend({
      // Add in style property hooks for overriding the default
      // behavior of getting and setting a style property
      cssHooks: {
        opacity: {
          get: function(i, s) {
            if (s) {
              var l = Lo(i, "opacity");
              return l === "" ? "1" : l;
            }
          }
        }
      },
      // Don't automatically add "px" to these possibly-unitless properties
      cssNumber: {
        animationIterationCount: !0,
        aspectRatio: !0,
        borderImageSlice: !0,
        columnCount: !0,
        flexGrow: !0,
        flexShrink: !0,
        fontWeight: !0,
        gridArea: !0,
        gridColumn: !0,
        gridColumnEnd: !0,
        gridColumnStart: !0,
        gridRow: !0,
        gridRowEnd: !0,
        gridRowStart: !0,
        lineHeight: !0,
        opacity: !0,
        order: !0,
        orphans: !0,
        scale: !0,
        widows: !0,
        zIndex: !0,
        zoom: !0,
        // SVG-related
        fillOpacity: !0,
        floodOpacity: !0,
        stopOpacity: !0,
        strokeMiterlimit: !0,
        strokeOpacity: !0
      },
      // Add in properties whose names you wish to fix before
      // setting or getting the value
      cssProps: {},
      // Get and set the style property on a DOM Node
      style: function(i, s, l, h) {
        if (!(!i || i.nodeType === 3 || i.nodeType === 8 || !i.style)) {
          var f, m, b, E = It(s), _ = Zn.test(s), O = i.style;
          if (_ || (s = Kn(E)), b = d.cssHooks[s] || d.cssHooks[E], l !== void 0) {
            if (m = typeof l, m === "string" && (f = Do.exec(l)) && f[1] && (l = tr(i, s, f), m = "number"), l == null || l !== l)
              return;
            m === "number" && !_ && (l += f && f[3] || (d.cssNumber[E] ? "" : "px")), !x.clearCloneStyle && l === "" && s.indexOf("background") === 0 && (O[s] = "inherit"), (!b || !("set" in b) || (l = b.set(i, l, h)) !== void 0) && (_ ? O.setProperty(s, l) : O[s] = l);
          } else
            return b && "get" in b && (f = b.get(i, !1, h)) !== void 0 ? f : O[s];
        }
      },
      css: function(i, s, l, h) {
        var f, m, b, E = It(s), _ = Zn.test(s);
        return _ || (s = Kn(E)), b = d.cssHooks[s] || d.cssHooks[E], b && "get" in b && (f = b.get(i, !0, l)), f === void 0 && (f = Lo(i, s, h)), f === "normal" && s in Ai && (f = Ai[s]), l === "" || l ? (m = parseFloat(f), l === !0 || isFinite(m) ? m || 0 : f) : f;
      }
    }), d.each(["height", "width"], function(i, s) {
      d.cssHooks[s] = {
        get: function(l, h, f) {
          if (h)
            return Ms.test(d.css(l, "display")) && // Support: Safari 8+
            // Table columns in Safari have non-zero offsetWidth & zero
            // getBoundingClientRect().width unless display is changed.
            // Support: IE <=11 only
            // Running getBoundingClientRect on a disconnected node
            // in IE throws an error.
            (!l.getClientRects().length || !l.getBoundingClientRect().width) ? tn(l, mn, function() {
              return hr(l, s, f);
            }) : hr(l, s, f);
        },
        set: function(l, h, f) {
          var m, b = en(l), E = !x.scrollboxSize() && b.position === "absolute", _ = E || f, O = _ && d.css(l, "boxSizing", !1, b) === "border-box", W = f ? Xn(
            l,
            s,
            f,
            O,
            b
          ) : 0;
          return O && E && (W -= Math.ceil(
            l["offset" + s[0].toUpperCase() + s.slice(1)] - parseFloat(b[s]) - Xn(l, s, "border", !1, b) - 0.5
          )), W && (m = Do.exec(h)) && (m[3] || "px") !== "px" && (l.style[s] = h, h = d.css(l, s)), dr(l, h, W);
        }
      };
    }), d.cssHooks.marginLeft = sr(
      x.reliableMarginLeft,
      function(i, s) {
        if (s)
          return (parseFloat(Lo(i, "marginLeft")) || i.getBoundingClientRect().left - tn(i, { marginLeft: 0 }, function() {
            return i.getBoundingClientRect().left;
          })) + "px";
      }
    ), d.each({
      margin: "",
      padding: "",
      border: "Width"
    }, function(i, s) {
      d.cssHooks[i + s] = {
        expand: function(l) {
          for (var h = 0, f = {}, m = typeof l == "string" ? l.split(" ") : [l]; h < 4; h++)
            f[i + zt[h] + s] = m[h] || m[h - 2] || m[0];
          return f;
        }
      }, i !== "margin" && (d.cssHooks[i + s].set = dr);
    }), d.fn.extend({
      css: function(i, s) {
        return vt(this, function(l, h, f) {
          var m, b, E = {}, _ = 0;
          if (Array.isArray(h)) {
            for (m = en(l), b = h.length; _ < b; _++)
              E[h[_]] = d.css(l, h[_], !1, m);
            return E;
          }
          return f !== void 0 ? d.style(l, h, f) : d.css(l, h);
        }, i, s, arguments.length > 1);
      }
    });
    function mt(i, s, l, h, f) {
      return new mt.prototype.init(i, s, l, h, f);
    }
    d.Tween = mt, mt.prototype = {
      constructor: mt,
      init: function(i, s, l, h, f, m) {
        this.elem = i, this.prop = l, this.easing = f || d.easing._default, this.options = s, this.start = this.now = this.cur(), this.end = h, this.unit = m || (d.cssNumber[l] ? "" : "px");
      },
      cur: function() {
        var i = mt.propHooks[this.prop];
        return i && i.get ? i.get(this) : mt.propHooks._default.get(this);
      },
      run: function(i) {
        var s, l = mt.propHooks[this.prop];
        return this.options.duration ? this.pos = s = d.easing[this.easing](
          i,
          this.options.duration * i,
          0,
          1,
          this.options.duration
        ) : this.pos = s = i, this.now = (this.end - this.start) * s + this.start, this.options.step && this.options.step.call(this.elem, this.now, this), l && l.set ? l.set(this) : mt.propHooks._default.set(this), this;
      }
    }, mt.prototype.init.prototype = mt.prototype, mt.propHooks = {
      _default: {
        get: function(i) {
          var s;
          return i.elem.nodeType !== 1 || i.elem[i.prop] != null && i.elem.style[i.prop] == null ? i.elem[i.prop] : (s = d.css(i.elem, i.prop, ""), !s || s === "auto" ? 0 : s);
        },
        set: function(i) {
          d.fx.step[i.prop] ? d.fx.step[i.prop](i) : i.elem.nodeType === 1 && (d.cssHooks[i.prop] || i.elem.style[Kn(i.prop)] != null) ? d.style(i.elem, i.prop, i.now + i.unit) : i.elem[i.prop] = i.now;
        }
      }
    }, mt.propHooks.scrollTop = mt.propHooks.scrollLeft = {
      set: function(i) {
        i.elem.nodeType && i.elem.parentNode && (i.elem[i.prop] = i.now);
      }
    }, d.easing = {
      linear: function(i) {
        return i;
      },
      swing: function(i) {
        return 0.5 - Math.cos(i * Math.PI) / 2;
      },
      _default: "swing"
    }, d.fx = mt.prototype.init, d.fx.step = {};
    var To, gn, Ds = /^(?:toggle|show|hide)$/, Ls = /queueHooks$/;
    function Jn() {
      gn && (Y.hidden === !1 && e.requestAnimationFrame ? e.requestAnimationFrame(Jn) : e.setTimeout(Jn, d.fx.interval), d.fx.tick());
    }
    function Ri() {
      return e.setTimeout(function() {
        To = void 0;
      }), To = Date.now();
    }
    function bn(i, s) {
      var l, h = 0, f = { height: i };
      for (s = s ? 1 : 0; h < 4; h += 2 - s)
        l = zt[h], f["margin" + l] = f["padding" + l] = i;
      return s && (f.opacity = f.width = i), f;
    }
    function ur(i, s, l) {
      for (var h, f = (_t.tweeners[s] || []).concat(_t.tweeners["*"]), m = 0, b = f.length; m < b; m++)
        if (h = f[m].call(l, s, i))
          return h;
    }
    function Os(i, s, l) {
      var h, f, m, b, E, _, O, W, q = "width" in s || "height" in s, N = this, X = {}, ge = i.style, Ce = i.nodeType && pn(i), xe = ce.get(i, "fxshow");
      l.queue || (b = d._queueHooks(i, "fx"), b.unqueued == null && (b.unqueued = 0, E = b.empty.fire, b.empty.fire = function() {
        b.unqueued || E();
      }), b.unqueued++, N.always(function() {
        N.always(function() {
          b.unqueued--, d.queue(i, "fx").length || b.empty.fire();
        });
      }));
      for (h in s)
        if (f = s[h], Ds.test(f)) {
          if (delete s[h], m = m || f === "toggle", f === (Ce ? "hide" : "show"))
            if (f === "show" && xe && xe[h] !== void 0)
              Ce = !0;
            else
              continue;
          X[h] = xe && xe[h] || d.style(i, h);
        }
      if (_ = !d.isEmptyObject(s), !(!_ && d.isEmptyObject(X))) {
        q && i.nodeType === 1 && (l.overflow = [ge.overflow, ge.overflowX, ge.overflowY], O = xe && xe.display, O == null && (O = ce.get(i, "display")), W = d.css(i, "display"), W === "none" && (O ? W = O : (wo([i], !0), O = i.style.display || O, W = d.css(i, "display"), wo([i]))), (W === "inline" || W === "inline-block" && O != null) && d.css(i, "float") === "none" && (_ || (N.done(function() {
          ge.display = O;
        }), O == null && (W = ge.display, O = W === "none" ? "" : W)), ge.display = "inline-block")), l.overflow && (ge.overflow = "hidden", N.always(function() {
          ge.overflow = l.overflow[0], ge.overflowX = l.overflow[1], ge.overflowY = l.overflow[2];
        })), _ = !1;
        for (h in X)
          _ || (xe ? "hidden" in xe && (Ce = xe.hidden) : xe = ce.access(i, "fxshow", { display: O }), m && (xe.hidden = !Ce), Ce && wo([i], !0), N.done(function() {
            Ce || wo([i]), ce.remove(i, "fxshow");
            for (h in X)
              d.style(i, h, X[h]);
          })), _ = ur(Ce ? xe[h] : 0, h, N), h in xe || (xe[h] = _.start, Ce && (_.end = _.start, _.start = 0));
      }
    }
    function Rn(i, s) {
      var l, h, f, m, b;
      for (l in i)
        if (h = It(l), f = s[h], m = i[l], Array.isArray(m) && (f = m[1], m = i[l] = m[0]), l !== h && (i[h] = m, delete i[l]), b = d.cssHooks[h], b && "expand" in b) {
          m = b.expand(m), delete i[h];
          for (l in m)
            l in i || (i[l] = m[l], s[l] = f);
        } else
          s[h] = f;
    }
    function _t(i, s, l) {
      var h, f, m = 0, b = _t.prefilters.length, E = d.Deferred().always(function() {
        delete _.elem;
      }), _ = function() {
        if (f)
          return !1;
        for (var q = To || Ri(), N = Math.max(0, O.startTime + O.duration - q), X = N / O.duration || 0, ge = 1 - X, Ce = 0, xe = O.tweens.length; Ce < xe; Ce++)
          O.tweens[Ce].run(ge);
        return E.notifyWith(i, [O, ge, N]), ge < 1 && xe ? N : (xe || E.notifyWith(i, [O, 1, 0]), E.resolveWith(i, [O]), !1);
      }, O = E.promise({
        elem: i,
        props: d.extend({}, s),
        opts: d.extend(!0, {
          specialEasing: {},
          easing: d.easing._default
        }, l),
        originalProperties: s,
        originalOptions: l,
        startTime: To || Ri(),
        duration: l.duration,
        tweens: [],
        createTween: function(q, N) {
          var X = d.Tween(
            i,
            O.opts,
            q,
            N,
            O.opts.specialEasing[q] || O.opts.easing
          );
          return O.tweens.push(X), X;
        },
        stop: function(q) {
          var N = 0, X = q ? O.tweens.length : 0;
          if (f)
            return this;
          for (f = !0; N < X; N++)
            O.tweens[N].run(1);
          return q ? (E.notifyWith(i, [O, 1, 0]), E.resolveWith(i, [O, q])) : E.rejectWith(i, [O, q]), this;
        }
      }), W = O.props;
      for (Rn(W, O.opts.specialEasing); m < b; m++)
        if (h = _t.prefilters[m].call(O, i, W, O.opts), h)
          return M(h.stop) && (d._queueHooks(O.elem, O.opts.queue).stop = h.stop.bind(h)), h;
      return d.map(W, ur, O), M(O.opts.start) && O.opts.start.call(i, O), O.progress(O.opts.progress).done(O.opts.done, O.opts.complete).fail(O.opts.fail).always(O.opts.always), d.fx.timer(
        d.extend(_, {
          elem: i,
          anim: O,
          queue: O.opts.queue
        })
      ), O;
    }
    d.Animation = d.extend(_t, {
      tweeners: {
        "*": [function(i, s) {
          var l = this.createTween(i, s);
          return tr(l.elem, i, Do.exec(s), l), l;
        }]
      },
      tweener: function(i, s) {
        M(i) ? (s = i, i = ["*"]) : i = i.match(Rt);
        for (var l, h = 0, f = i.length; h < f; h++)
          l = i[h], _t.tweeners[l] = _t.tweeners[l] || [], _t.tweeners[l].unshift(s);
      },
      prefilters: [Os],
      prefilter: function(i, s) {
        s ? _t.prefilters.unshift(i) : _t.prefilters.push(i);
      }
    }), d.speed = function(i, s, l) {
      var h = i && typeof i == "object" ? d.extend({}, i) : {
        complete: l || !l && s || M(i) && i,
        duration: i,
        easing: l && s || s && !M(s) && s
      };
      return d.fx.off ? h.duration = 0 : typeof h.duration != "number" && (h.duration in d.fx.speeds ? h.duration = d.fx.speeds[h.duration] : h.duration = d.fx.speeds._default), (h.queue == null || h.queue === !0) && (h.queue = "fx"), h.old = h.complete, h.complete = function() {
        M(h.old) && h.old.call(this), h.queue && d.dequeue(this, h.queue);
      }, h;
    }, d.fn.extend({
      fadeTo: function(i, s, l, h) {
        return this.filter(pn).css("opacity", 0).show().end().animate({ opacity: s }, i, l, h);
      },
      animate: function(i, s, l, h) {
        var f = d.isEmptyObject(i), m = d.speed(s, l, h), b = function() {
          var E = _t(this, d.extend({}, i), m);
          (f || ce.get(this, "finish")) && E.stop(!0);
        };
        return b.finish = b, f || m.queue === !1 ? this.each(b) : this.queue(m.queue, b);
      },
      stop: function(i, s, l) {
        var h = function(f) {
          var m = f.stop;
          delete f.stop, m(l);
        };
        return typeof i != "string" && (l = s, s = i, i = void 0), s && this.queue(i || "fx", []), this.each(function() {
          var f = !0, m = i != null && i + "queueHooks", b = d.timers, E = ce.get(this);
          if (m)
            E[m] && E[m].stop && h(E[m]);
          else
            for (m in E)
              E[m] && E[m].stop && Ls.test(m) && h(E[m]);
          for (m = b.length; m--; )
            b[m].elem === this && (i == null || b[m].queue === i) && (b[m].anim.stop(l), f = !1, b.splice(m, 1));
          (f || !l) && d.dequeue(this, i);
        });
      },
      finish: function(i) {
        return i !== !1 && (i = i || "fx"), this.each(function() {
          var s, l = ce.get(this), h = l[i + "queue"], f = l[i + "queueHooks"], m = d.timers, b = h ? h.length : 0;
          for (l.finish = !0, d.queue(this, i, []), f && f.stop && f.stop.call(this, !0), s = m.length; s--; )
            m[s].elem === this && m[s].queue === i && (m[s].anim.stop(!0), m.splice(s, 1));
          for (s = 0; s < b; s++)
            h[s] && h[s].finish && h[s].finish.call(this);
          delete l.finish;
        });
      }
    }), d.each(["toggle", "show", "hide"], function(i, s) {
      var l = d.fn[s];
      d.fn[s] = function(h, f, m) {
        return h == null || typeof h == "boolean" ? l.apply(this, arguments) : this.animate(bn(s, !0), h, f, m);
      };
    }), d.each({
      slideDown: bn("show"),
      slideUp: bn("hide"),
      slideToggle: bn("toggle"),
      fadeIn: { opacity: "show" },
      fadeOut: { opacity: "hide" },
      fadeToggle: { opacity: "toggle" }
    }, function(i, s) {
      d.fn[i] = function(l, h, f) {
        return this.animate(s, l, h, f);
      };
    }), d.timers = [], d.fx.tick = function() {
      var i, s = 0, l = d.timers;
      for (To = Date.now(); s < l.length; s++)
        i = l[s], !i() && l[s] === i && l.splice(s--, 1);
      l.length || d.fx.stop(), To = void 0;
    }, d.fx.timer = function(i) {
      d.timers.push(i), d.fx.start();
    }, d.fx.interval = 13, d.fx.start = function() {
      gn || (gn = !0, Jn());
    }, d.fx.stop = function() {
      gn = null;
    }, d.fx.speeds = {
      slow: 600,
      fast: 200,
      // Default speed
      _default: 400
    }, d.fn.delay = function(i, s) {
      return i = d.fx && d.fx.speeds[i] || i, s = s || "fx", this.queue(s, function(l, h) {
        var f = e.setTimeout(l, i);
        h.stop = function() {
          e.clearTimeout(f);
        };
      });
    }, function() {
      var i = Y.createElement("input"), s = Y.createElement("select"), l = s.appendChild(Y.createElement("option"));
      i.type = "checkbox", x.checkOn = i.value !== "", x.optSelected = l.selected, i = Y.createElement("input"), i.value = "t", i.type = "radio", x.radioValue = i.value === "t";
    }();
    var pr, Yo = d.expr.attrHandle;
    d.fn.extend({
      attr: function(i, s) {
        return vt(this, d.attr, i, s, arguments.length > 1);
      },
      removeAttr: function(i) {
        return this.each(function() {
          d.removeAttr(this, i);
        });
      }
    }), d.extend({
      attr: function(i, s, l) {
        var h, f, m = i.nodeType;
        if (!(m === 3 || m === 8 || m === 2)) {
          if (typeof i.getAttribute > "u")
            return d.prop(i, s, l);
          if ((m !== 1 || !d.isXMLDoc(i)) && (f = d.attrHooks[s.toLowerCase()] || (d.expr.match.bool.test(s) ? pr : void 0)), l !== void 0) {
            if (l === null) {
              d.removeAttr(i, s);
              return;
            }
            return f && "set" in f && (h = f.set(i, l, s)) !== void 0 ? h : (i.setAttribute(s, l + ""), l);
          }
          return f && "get" in f && (h = f.get(i, s)) !== null ? h : (h = d.find.attr(i, s), h ?? void 0);
        }
      },
      attrHooks: {
        type: {
          set: function(i, s) {
            if (!x.radioValue && s === "radio" && R(i, "input")) {
              var l = i.value;
              return i.setAttribute("type", s), l && (i.value = l), s;
            }
          }
        }
      },
      removeAttr: function(i, s) {
        var l, h = 0, f = s && s.match(Rt);
        if (f && i.nodeType === 1)
          for (; l = f[h++]; )
            i.removeAttribute(l);
      }
    }), pr = {
      set: function(i, s, l) {
        return s === !1 ? d.removeAttr(i, l) : i.setAttribute(l, l), l;
      }
    }, d.each(d.expr.match.bool.source.match(/\w+/g), function(i, s) {
      var l = Yo[s] || d.find.attr;
      Yo[s] = function(h, f, m) {
        var b, E, _ = f.toLowerCase();
        return m || (E = Yo[_], Yo[_] = b, b = l(h, f, m) != null ? _ : null, Yo[_] = E), b;
      };
    });
    var Bs = /^(?:input|select|textarea|button)$/i, As = /^(?:a|area)$/i;
    d.fn.extend({
      prop: function(i, s) {
        return vt(this, d.prop, i, s, arguments.length > 1);
      },
      removeProp: function(i) {
        return this.each(function() {
          delete this[d.propFix[i] || i];
        });
      }
    }), d.extend({
      prop: function(i, s, l) {
        var h, f, m = i.nodeType;
        if (!(m === 3 || m === 8 || m === 2))
          return (m !== 1 || !d.isXMLDoc(i)) && (s = d.propFix[s] || s, f = d.propHooks[s]), l !== void 0 ? f && "set" in f && (h = f.set(i, l, s)) !== void 0 ? h : i[s] = l : f && "get" in f && (h = f.get(i, s)) !== null ? h : i[s];
      },
      propHooks: {
        tabIndex: {
          get: function(i) {
            var s = d.find.attr(i, "tabindex");
            return s ? parseInt(s, 10) : Bs.test(i.nodeName) || As.test(i.nodeName) && i.href ? 0 : -1;
          }
        }
      },
      propFix: {
        for: "htmlFor",
        class: "className"
      }
    }), x.optSelected || (d.propHooks.selected = {
      get: function(i) {
        var s = i.parentNode;
        return s && s.parentNode && s.parentNode.selectedIndex, null;
      },
      set: function(i) {
        var s = i.parentNode;
        s && (s.selectedIndex, s.parentNode && s.parentNode.selectedIndex);
      }
    }), d.each([
      "tabIndex",
      "readOnly",
      "maxLength",
      "cellSpacing",
      "cellPadding",
      "rowSpan",
      "colSpan",
      "useMap",
      "frameBorder",
      "contentEditable"
    ], function() {
      d.propFix[this.toLowerCase()] = this;
    });
    function ro(i) {
      var s = i.match(Rt) || [];
      return s.join(" ");
    }
    function so(i) {
      return i.getAttribute && i.getAttribute("class") || "";
    }
    function vn(i) {
      return Array.isArray(i) ? i : typeof i == "string" ? i.match(Rt) || [] : [];
    }
    d.fn.extend({
      addClass: function(i) {
        var s, l, h, f, m, b;
        return M(i) ? this.each(function(E) {
          d(this).addClass(i.call(this, E, so(this)));
        }) : (s = vn(i), s.length ? this.each(function() {
          if (h = so(this), l = this.nodeType === 1 && " " + ro(h) + " ", l) {
            for (m = 0; m < s.length; m++)
              f = s[m], l.indexOf(" " + f + " ") < 0 && (l += f + " ");
            b = ro(l), h !== b && this.setAttribute("class", b);
          }
        }) : this);
      },
      removeClass: function(i) {
        var s, l, h, f, m, b;
        return M(i) ? this.each(function(E) {
          d(this).removeClass(i.call(this, E, so(this)));
        }) : arguments.length ? (s = vn(i), s.length ? this.each(function() {
          if (h = so(this), l = this.nodeType === 1 && " " + ro(h) + " ", l) {
            for (m = 0; m < s.length; m++)
              for (f = s[m]; l.indexOf(" " + f + " ") > -1; )
                l = l.replace(" " + f + " ", " ");
            b = ro(l), h !== b && this.setAttribute("class", b);
          }
        }) : this) : this.attr("class", "");
      },
      toggleClass: function(i, s) {
        var l, h, f, m, b = typeof i, E = b === "string" || Array.isArray(i);
        return M(i) ? this.each(function(_) {
          d(this).toggleClass(
            i.call(this, _, so(this), s),
            s
          );
        }) : typeof s == "boolean" && E ? s ? this.addClass(i) : this.removeClass(i) : (l = vn(i), this.each(function() {
          if (E)
            for (m = d(this), f = 0; f < l.length; f++)
              h = l[f], m.hasClass(h) ? m.removeClass(h) : m.addClass(h);
          else
            (i === void 0 || b === "boolean") && (h = so(this), h && ce.set(this, "__className__", h), this.setAttribute && this.setAttribute(
              "class",
              h || i === !1 ? "" : ce.get(this, "__className__") || ""
            ));
        }));
      },
      hasClass: function(i) {
        var s, l, h = 0;
        for (s = " " + i + " "; l = this[h++]; )
          if (l.nodeType === 1 && (" " + ro(so(l)) + " ").indexOf(s) > -1)
            return !0;
        return !1;
      }
    });
    var Fr = /\r/g;
    d.fn.extend({
      val: function(i) {
        var s, l, h, f = this[0];
        return arguments.length ? (h = M(i), this.each(function(m) {
          var b;
          this.nodeType === 1 && (h ? b = i.call(this, m, d(this).val()) : b = i, b == null ? b = "" : typeof b == "number" ? b += "" : Array.isArray(b) && (b = d.map(b, function(E) {
            return E == null ? "" : E + "";
          })), s = d.valHooks[this.type] || d.valHooks[this.nodeName.toLowerCase()], (!s || !("set" in s) || s.set(this, b, "value") === void 0) && (this.value = b));
        })) : f ? (s = d.valHooks[f.type] || d.valHooks[f.nodeName.toLowerCase()], s && "get" in s && (l = s.get(f, "value")) !== void 0 ? l : (l = f.value, typeof l == "string" ? l.replace(Fr, "") : l ?? "")) : void 0;
      }
    }), d.extend({
      valHooks: {
        option: {
          get: function(i) {
            var s = d.find.attr(i, "value");
            return s ?? // Support: IE <=10 - 11 only
            // option.text throws exceptions (trac-14686, trac-14858)
            // Strip and collapse whitespace
            // https://html.spec.whatwg.org/#strip-and-collapse-whitespace
            ro(d.text(i));
          }
        },
        select: {
          get: function(i) {
            var s, l, h, f = i.options, m = i.selectedIndex, b = i.type === "select-one", E = b ? null : [], _ = b ? m + 1 : f.length;
            for (m < 0 ? h = _ : h = b ? m : 0; h < _; h++)
              if (l = f[h], (l.selected || h === m) && // Don't return options that are disabled or in a disabled optgroup
              !l.disabled && (!l.parentNode.disabled || !R(l.parentNode, "optgroup"))) {
                if (s = d(l).val(), b)
                  return s;
                E.push(s);
              }
            return E;
          },
          set: function(i, s) {
            for (var l, h, f = i.options, m = d.makeArray(s), b = f.length; b--; )
              h = f[b], (h.selected = d.inArray(d.valHooks.option.get(h), m) > -1) && (l = !0);
            return l || (i.selectedIndex = -1), m;
          }
        }
      }
    }), d.each(["radio", "checkbox"], function() {
      d.valHooks[this] = {
        set: function(i, s) {
          if (Array.isArray(s))
            return i.checked = d.inArray(d(i).val(), s) > -1;
        }
      }, x.checkOn || (d.valHooks[this].get = function(i) {
        return i.getAttribute("value") === null ? "on" : i.value;
      });
    });
    var Oo = e.location, fr = { guid: Date.now() }, Fo = /\?/;
    d.parseXML = function(i) {
      var s, l;
      if (!i || typeof i != "string")
        return null;
      try {
        s = new e.DOMParser().parseFromString(i, "text/xml");
      } catch {
      }
      return l = s && s.getElementsByTagName("parsererror")[0], (!s || l) && d.error("Invalid XML: " + (l ? d.map(l.childNodes, function(h) {
        return h.textContent;
      }).join(`
`) : i)), s;
    };
    var Ii = /^(?:focusinfocus|focusoutblur)$/, Ni = function(i) {
      i.stopPropagation();
    };
    d.extend(d.event, {
      trigger: function(i, s, l, h) {
        var f, m, b, E, _, O, W, q, N = [l || Y], X = k.call(i, "type") ? i.type : i, ge = k.call(i, "namespace") ? i.namespace.split(".") : [];
        if (m = q = b = l = l || Y, !(l.nodeType === 3 || l.nodeType === 8) && !Ii.test(X + d.event.triggered) && (X.indexOf(".") > -1 && (ge = X.split("."), X = ge.shift(), ge.sort()), _ = X.indexOf(":") < 0 && "on" + X, i = i[d.expando] ? i : new d.Event(X, typeof i == "object" && i), i.isTrigger = h ? 2 : 3, i.namespace = ge.join("."), i.rnamespace = i.namespace ? new RegExp("(^|\\.)" + ge.join("\\.(?:.*\\.|)") + "(\\.|$)") : null, i.result = void 0, i.target || (i.target = l), s = s == null ? [i] : d.makeArray(s, [i]), W = d.event.special[X] || {}, !(!h && W.trigger && W.trigger.apply(l, s) === !1))) {
          if (!h && !W.noBubble && !L(l)) {
            for (E = W.delegateType || X, Ii.test(E + X) || (m = m.parentNode); m; m = m.parentNode)
              N.push(m), b = m;
            b === (l.ownerDocument || Y) && N.push(b.defaultView || b.parentWindow || e);
          }
          for (f = 0; (m = N[f++]) && !i.isPropagationStopped(); )
            q = m, i.type = f > 1 ? E : W.bindType || X, O = (ce.get(m, "events") || /* @__PURE__ */ Object.create(null))[i.type] && ce.get(m, "handle"), O && O.apply(m, s), O = _ && m[_], O && O.apply && We(m) && (i.result = O.apply(m, s), i.result === !1 && i.preventDefault());
          return i.type = X, !h && !i.isDefaultPrevented() && (!W._default || W._default.apply(N.pop(), s) === !1) && We(l) && _ && M(l[X]) && !L(l) && (b = l[_], b && (l[_] = null), d.event.triggered = X, i.isPropagationStopped() && q.addEventListener(X, Ni), l[X](), i.isPropagationStopped() && q.removeEventListener(X, Ni), d.event.triggered = void 0, b && (l[_] = b)), i.result;
        }
      },
      // Piggyback on a donor event to simulate a different one
      // Used only for `focus(in | out)` events
      simulate: function(i, s, l) {
        var h = d.extend(
          new d.Event(),
          l,
          {
            type: i,
            isSimulated: !0
          }
        );
        d.event.trigger(h, null, s);
      }
    }), d.fn.extend({
      trigger: function(i, s) {
        return this.each(function() {
          d.event.trigger(i, s, this);
        });
      },
      triggerHandler: function(i, s) {
        var l = this[0];
        if (l)
          return d.event.trigger(i, s, l, !0);
      }
    });
    var mr = /\[\]$/, Pi = /\r?\n/g, Rs = /^(?:submit|button|image|reset|file)$/i, Is = /^(?:input|select|textarea|keygen)/i;
    function Qn(i, s, l, h) {
      var f;
      if (Array.isArray(s))
        d.each(s, function(m, b) {
          l || mr.test(i) ? h(i, b) : Qn(
            i + "[" + (typeof b == "object" && b != null ? m : "") + "]",
            b,
            l,
            h
          );
        });
      else if (!l && Q(s) === "object")
        for (f in s)
          Qn(i + "[" + f + "]", s[f], l, h);
      else
        h(i, s);
    }
    d.param = function(i, s) {
      var l, h = [], f = function(m, b) {
        var E = M(b) ? b() : b;
        h[h.length] = encodeURIComponent(m) + "=" + encodeURIComponent(E ?? "");
      };
      if (i == null)
        return "";
      if (Array.isArray(i) || i.jquery && !d.isPlainObject(i))
        d.each(i, function() {
          f(this.name, this.value);
        });
      else
        for (l in i)
          Qn(l, i[l], s, f);
      return h.join("&");
    }, d.fn.extend({
      serialize: function() {
        return d.param(this.serializeArray());
      },
      serializeArray: function() {
        return this.map(function() {
          var i = d.prop(this, "elements");
          return i ? d.makeArray(i) : this;
        }).filter(function() {
          var i = this.type;
          return this.name && !d(this).is(":disabled") && Is.test(this.nodeName) && !Rs.test(i) && (this.checked || !ko.test(i));
        }).map(function(i, s) {
          var l = d(this).val();
          return l == null ? null : Array.isArray(l) ? d.map(l, function(h) {
            return { name: s.name, value: h.replace(Pi, `\r
`) };
          }) : { name: s.name, value: l.replace(Pi, `\r
`) };
        }).get();
      }
    });
    var Ns = /%20/g, Ps = /#.*$/, Hs = /([?&])_=[^&]*/, zr = /^(.*?):[ \t]*([^\r\n]*)$/mg, Hi = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/, js = /^(?:GET|HEAD)$/, ei = /^\/\//, gr = {}, ti = {}, br = "*/".concat("*"), In = Y.createElement("a");
    In.href = Oo.href;
    function ji(i) {
      return function(s, l) {
        typeof s != "string" && (l = s, s = "*");
        var h, f = 0, m = s.toLowerCase().match(Rt) || [];
        if (M(l))
          for (; h = m[f++]; )
            h[0] === "+" ? (h = h.slice(1) || "*", (i[h] = i[h] || []).unshift(l)) : (i[h] = i[h] || []).push(l);
      };
    }
    function $i(i, s, l, h) {
      var f = {}, m = i === ti;
      function b(E) {
        var _;
        return f[E] = !0, d.each(i[E] || [], function(O, W) {
          var q = W(s, l, h);
          if (typeof q == "string" && !m && !f[q])
            return s.dataTypes.unshift(q), b(q), !1;
          if (m)
            return !(_ = q);
        }), _;
      }
      return b(s.dataTypes[0]) || !f["*"] && b("*");
    }
    function oi(i, s) {
      var l, h, f = d.ajaxSettings.flatOptions || {};
      for (l in s)
        s[l] !== void 0 && ((f[l] ? i : h || (h = {}))[l] = s[l]);
      return h && d.extend(!0, i, h), i;
    }
    function $s(i, s, l) {
      for (var h, f, m, b, E = i.contents, _ = i.dataTypes; _[0] === "*"; )
        _.shift(), h === void 0 && (h = i.mimeType || s.getResponseHeader("Content-Type"));
      if (h) {
        for (f in E)
          if (E[f] && E[f].test(h)) {
            _.unshift(f);
            break;
          }
      }
      if (_[0] in l)
        m = _[0];
      else {
        for (f in l) {
          if (!_[0] || i.converters[f + " " + _[0]]) {
            m = f;
            break;
          }
          b || (b = f);
        }
        m = m || b;
      }
      if (m)
        return m !== _[0] && _.unshift(m), l[m];
    }
    function Wr(i, s, l, h) {
      var f, m, b, E, _, O = {}, W = i.dataTypes.slice();
      if (W[1])
        for (b in i.converters)
          O[b.toLowerCase()] = i.converters[b];
      for (m = W.shift(); m; )
        if (i.responseFields[m] && (l[i.responseFields[m]] = s), !_ && h && i.dataFilter && (s = i.dataFilter(s, i.dataType)), _ = m, m = W.shift(), m) {
          if (m === "*")
            m = _;
          else if (_ !== "*" && _ !== m) {
            if (b = O[_ + " " + m] || O["* " + m], !b) {
              for (f in O)
                if (E = f.split(" "), E[1] === m && (b = O[_ + " " + E[0]] || O["* " + E[0]], b)) {
                  b === !0 ? b = O[f] : O[f] !== !0 && (m = E[0], W.unshift(E[1]));
                  break;
                }
            }
            if (b !== !0)
              if (b && i.throws)
                s = b(s);
              else
                try {
                  s = b(s);
                } catch (q) {
                  return {
                    state: "parsererror",
                    error: b ? q : "No conversion from " + _ + " to " + m
                  };
                }
          }
        }
      return { state: "success", data: s };
    }
    d.extend({
      // Counter for holding the number of active queries
      active: 0,
      // Last-Modified header cache for next request
      lastModified: {},
      etag: {},
      ajaxSettings: {
        url: Oo.href,
        type: "GET",
        isLocal: Hi.test(Oo.protocol),
        global: !0,
        processData: !0,
        async: !0,
        contentType: "application/x-www-form-urlencoded; charset=UTF-8",
        /*
        timeout: 0,
        data: null,
        dataType: null,
        username: null,
        password: null,
        cache: null,
        throws: false,
        traditional: false,
        headers: {},
        */
        accepts: {
          "*": br,
          text: "text/plain",
          html: "text/html",
          xml: "application/xml, text/xml",
          json: "application/json, text/javascript"
        },
        contents: {
          xml: /\bxml\b/,
          html: /\bhtml/,
          json: /\bjson\b/
        },
        responseFields: {
          xml: "responseXML",
          text: "responseText",
          json: "responseJSON"
        },
        // Data converters
        // Keys separate source (or catchall "*") and destination types with a single space
        converters: {
          // Convert anything to text
          "* text": String,
          // Text to html (true = no transformation)
          "text html": !0,
          // Evaluate text as a json expression
          "text json": JSON.parse,
          // Parse text as xml
          "text xml": d.parseXML
        },
        // For options that shouldn't be deep extended:
        // you can add your own custom options here if
        // and when you create one that shouldn't be
        // deep extended (see ajaxExtend)
        flatOptions: {
          url: !0,
          context: !0
        }
      },
      // Creates a full fledged settings object into target
      // with both ajaxSettings and settings fields.
      // If target is omitted, writes into ajaxSettings.
      ajaxSetup: function(i, s) {
        return s ? (
          // Building a settings object
          oi(oi(i, d.ajaxSettings), s)
        ) : (
          // Extending ajaxSettings
          oi(d.ajaxSettings, i)
        );
      },
      ajaxPrefilter: ji(gr),
      ajaxTransport: ji(ti),
      // Main method
      ajax: function(i, s) {
        typeof i == "object" && (s = i, i = void 0), s = s || {};
        var l, h, f, m, b, E, _, O, W, q, N = d.ajaxSetup({}, s), X = N.context || N, ge = N.context && (X.nodeType || X.jquery) ? d(X) : d.event, Ce = d.Deferred(), xe = d.Callbacks("once memory"), Qe = N.statusCode || {}, Ve = {}, Ht = {}, jt = "canceled", Se = {
          readyState: 0,
          // Builds headers hashtable if needed
          getResponseHeader: function(De) {
            var w;
            if (_) {
              if (!m)
                for (m = {}; w = zr.exec(f); )
                  m[w[1].toLowerCase() + " "] = (m[w[1].toLowerCase() + " "] || []).concat(w[2]);
              w = m[De.toLowerCase() + " "];
            }
            return w == null ? null : w.join(", ");
          },
          // Raw string
          getAllResponseHeaders: function() {
            return _ ? f : null;
          },
          // Caches the header
          setRequestHeader: function(De, w) {
            return _ == null && (De = Ht[De.toLowerCase()] = Ht[De.toLowerCase()] || De, Ve[De] = w), this;
          },
          // Overrides response content-type header
          overrideMimeType: function(De) {
            return _ == null && (N.mimeType = De), this;
          },
          // Status-dependent callbacks
          statusCode: function(De) {
            var w;
            if (De)
              if (_)
                Se.always(De[Se.status]);
              else
                for (w in De)
                  Qe[w] = [Qe[w], De[w]];
            return this;
          },
          // Cancel the request
          abort: function(De) {
            var w = De || jt;
            return l && l.abort(w), ao(0, w), this;
          }
        };
        if (Ce.promise(Se), N.url = ((i || N.url || Oo.href) + "").replace(ei, Oo.protocol + "//"), N.type = s.method || s.type || N.method || N.type, N.dataTypes = (N.dataType || "*").toLowerCase().match(Rt) || [""], N.crossDomain == null) {
          E = Y.createElement("a");
          try {
            E.href = N.url, E.href = E.href, N.crossDomain = In.protocol + "//" + In.host != E.protocol + "//" + E.host;
          } catch {
            N.crossDomain = !0;
          }
        }
        if (N.data && N.processData && typeof N.data != "string" && (N.data = d.param(N.data, N.traditional)), $i(gr, N, s, Se), _)
          return Se;
        O = d.event && N.global, O && d.active++ === 0 && d.event.trigger("ajaxStart"), N.type = N.type.toUpperCase(), N.hasContent = !js.test(N.type), h = N.url.replace(Ps, ""), N.hasContent ? N.data && N.processData && (N.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && (N.data = N.data.replace(Ns, "+")) : (q = N.url.slice(h.length), N.data && (N.processData || typeof N.data == "string") && (h += (Fo.test(h) ? "&" : "?") + N.data, delete N.data), N.cache === !1 && (h = h.replace(Hs, "$1"), q = (Fo.test(h) ? "&" : "?") + "_=" + fr.guid++ + q), N.url = h + q), N.ifModified && (d.lastModified[h] && Se.setRequestHeader("If-Modified-Since", d.lastModified[h]), d.etag[h] && Se.setRequestHeader("If-None-Match", d.etag[h])), (N.data && N.hasContent && N.contentType !== !1 || s.contentType) && Se.setRequestHeader("Content-Type", N.contentType), Se.setRequestHeader(
          "Accept",
          N.dataTypes[0] && N.accepts[N.dataTypes[0]] ? N.accepts[N.dataTypes[0]] + (N.dataTypes[0] !== "*" ? ", " + br + "; q=0.01" : "") : N.accepts["*"]
        );
        for (W in N.headers)
          Se.setRequestHeader(W, N.headers[W]);
        if (N.beforeSend && (N.beforeSend.call(X, Se, N) === !1 || _))
          return Se.abort();
        if (jt = "abort", xe.add(N.complete), Se.done(N.success), Se.fail(N.error), l = $i(ti, N, s, Se), !l)
          ao(-1, "No Transport");
        else {
          if (Se.readyState = 1, O && ge.trigger("ajaxSend", [Se, N]), _)
            return Se;
          N.async && N.timeout > 0 && (b = e.setTimeout(function() {
            Se.abort("timeout");
          }, N.timeout));
          try {
            _ = !1, l.send(Ve, ao);
          } catch (De) {
            if (_)
              throw De;
            ao(-1, De);
          }
        }
        function ao(De, w, S, B) {
          var H, ee, he, ke, Ee, Be = w;
          _ || (_ = !0, b && e.clearTimeout(b), l = void 0, f = B || "", Se.readyState = De > 0 ? 4 : 0, H = De >= 200 && De < 300 || De === 304, S && (ke = $s(N, Se, S)), !H && d.inArray("script", N.dataTypes) > -1 && d.inArray("json", N.dataTypes) < 0 && (N.converters["text script"] = function() {
          }), ke = Wr(N, ke, Se, H), H ? (N.ifModified && (Ee = Se.getResponseHeader("Last-Modified"), Ee && (d.lastModified[h] = Ee), Ee = Se.getResponseHeader("etag"), Ee && (d.etag[h] = Ee)), De === 204 || N.type === "HEAD" ? Be = "nocontent" : De === 304 ? Be = "notmodified" : (Be = ke.state, ee = ke.data, he = ke.error, H = !he)) : (he = Be, (De || !Be) && (Be = "error", De < 0 && (De = 0))), Se.status = De, Se.statusText = (w || Be) + "", H ? Ce.resolveWith(X, [ee, Be, Se]) : Ce.rejectWith(X, [Se, Be, he]), Se.statusCode(Qe), Qe = void 0, O && ge.trigger(
            H ? "ajaxSuccess" : "ajaxError",
            [Se, N, H ? ee : he]
          ), xe.fireWith(X, [Se, Be]), O && (ge.trigger("ajaxComplete", [Se, N]), --d.active || d.event.trigger("ajaxStop")));
        }
        return Se;
      },
      getJSON: function(i, s, l) {
        return d.get(i, s, l, "json");
      },
      getScript: function(i, s) {
        return d.get(i, void 0, s, "script");
      }
    }), d.each(["get", "post"], function(i, s) {
      d[s] = function(l, h, f, m) {
        return M(h) && (m = m || f, f = h, h = void 0), d.ajax(d.extend({
          url: l,
          type: s,
          dataType: m,
          data: h,
          success: f
        }, d.isPlainObject(l) && l));
      };
    }), d.ajaxPrefilter(function(i) {
      var s;
      for (s in i.headers)
        s.toLowerCase() === "content-type" && (i.contentType = i.headers[s] || "");
    }), d._evalUrl = function(i, s, l) {
      return d.ajax({
        url: i,
        // Make this explicit, since user can override this through ajaxSetup (trac-11264)
        type: "GET",
        dataType: "script",
        cache: !0,
        async: !1,
        global: !1,
        // Only evaluate the response if it is successful (gh-4126)
        // dataFilter is not invoked for failure responses, so using it instead
        // of the default converter is kludgy but it works.
        converters: {
          "text script": function() {
          }
        },
        dataFilter: function(h) {
          d.globalEval(h, s, l);
        }
      });
    }, d.fn.extend({
      wrapAll: function(i) {
        var s;
        return this[0] && (M(i) && (i = i.call(this[0])), s = d(i, this[0].ownerDocument).eq(0).clone(!0), this[0].parentNode && s.insertBefore(this[0]), s.map(function() {
          for (var l = this; l.firstElementChild; )
            l = l.firstElementChild;
          return l;
        }).append(this)), this;
      },
      wrapInner: function(i) {
        return M(i) ? this.each(function(s) {
          d(this).wrapInner(i.call(this, s));
        }) : this.each(function() {
          var s = d(this), l = s.contents();
          l.length ? l.wrapAll(i) : s.append(i);
        });
      },
      wrap: function(i) {
        var s = M(i);
        return this.each(function(l) {
          d(this).wrapAll(s ? i.call(this, l) : i);
        });
      },
      unwrap: function(i) {
        return this.parent(i).not("body").each(function() {
          d(this).replaceWith(this.childNodes);
        }), this;
      }
    }), d.expr.pseudos.hidden = function(i) {
      return !d.expr.pseudos.visible(i);
    }, d.expr.pseudos.visible = function(i) {
      return !!(i.offsetWidth || i.offsetHeight || i.getClientRects().length);
    }, d.ajaxSettings.xhr = function() {
      try {
        return new e.XMLHttpRequest();
      } catch {
      }
    };
    var vr = {
      // File protocol always yields status code 0, assume 200
      0: 200,
      // Support: IE <=9 only
      // trac-1450: sometimes IE returns 1223 when it should be 204
      1223: 204
    }, Bo = d.ajaxSettings.xhr();
    x.cors = !!Bo && "withCredentials" in Bo, x.ajax = Bo = !!Bo, d.ajaxTransport(function(i) {
      var s, l;
      if (x.cors || Bo && !i.crossDomain)
        return {
          send: function(h, f) {
            var m, b = i.xhr();
            if (b.open(
              i.type,
              i.url,
              i.async,
              i.username,
              i.password
            ), i.xhrFields)
              for (m in i.xhrFields)
                b[m] = i.xhrFields[m];
            i.mimeType && b.overrideMimeType && b.overrideMimeType(i.mimeType), !i.crossDomain && !h["X-Requested-With"] && (h["X-Requested-With"] = "XMLHttpRequest");
            for (m in h)
              b.setRequestHeader(m, h[m]);
            s = function(E) {
              return function() {
                s && (s = l = b.onload = b.onerror = b.onabort = b.ontimeout = b.onreadystatechange = null, E === "abort" ? b.abort() : E === "error" ? typeof b.status != "number" ? f(0, "error") : f(
                  // File: protocol always yields status 0; see trac-8605, trac-14207
                  b.status,
                  b.statusText
                ) : f(
                  vr[b.status] || b.status,
                  b.statusText,
                  // Support: IE <=9 only
                  // IE9 has no XHR2 but throws on binary (trac-11426)
                  // For XHR2 non-text, let the caller handle it (gh-2498)
                  (b.responseType || "text") !== "text" || typeof b.responseText != "string" ? { binary: b.response } : { text: b.responseText },
                  b.getAllResponseHeaders()
                ));
              };
            }, b.onload = s(), l = b.onerror = b.ontimeout = s("error"), b.onabort !== void 0 ? b.onabort = l : b.onreadystatechange = function() {
              b.readyState === 4 && e.setTimeout(function() {
                s && l();
              });
            }, s = s("abort");
            try {
              b.send(i.hasContent && i.data || null);
            } catch (E) {
              if (s)
                throw E;
            }
          },
          abort: function() {
            s && s();
          }
        };
    }), d.ajaxPrefilter(function(i) {
      i.crossDomain && (i.contents.script = !1);
    }), d.ajaxSetup({
      accepts: {
        script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
      },
      contents: {
        script: /\b(?:java|ecma)script\b/
      },
      converters: {
        "text script": function(i) {
          return d.globalEval(i), i;
        }
      }
    }), d.ajaxPrefilter("script", function(i) {
      i.cache === void 0 && (i.cache = !1), i.crossDomain && (i.type = "GET");
    }), d.ajaxTransport("script", function(i) {
      if (i.crossDomain || i.scriptAttrs) {
        var s, l;
        return {
          send: function(h, f) {
            s = d("<script>").attr(i.scriptAttrs || {}).prop({ charset: i.scriptCharset, src: i.url }).on("load error", l = function(m) {
              s.remove(), l = null, m && f(m.type === "error" ? 404 : 200, m.type);
            }), Y.head.appendChild(s[0]);
          },
          abort: function() {
            l && l();
          }
        };
      }
    });
    var Yi = [], ni = /(=)\?(?=&|$)|\?\?/;
    d.ajaxSetup({
      jsonp: "callback",
      jsonpCallback: function() {
        var i = Yi.pop() || d.expando + "_" + fr.guid++;
        return this[i] = !0, i;
      }
    }), d.ajaxPrefilter("json jsonp", function(i, s, l) {
      var h, f, m, b = i.jsonp !== !1 && (ni.test(i.url) ? "url" : typeof i.data == "string" && (i.contentType || "").indexOf("application/x-www-form-urlencoded") === 0 && ni.test(i.data) && "data");
      if (b || i.dataTypes[0] === "jsonp")
        return h = i.jsonpCallback = M(i.jsonpCallback) ? i.jsonpCallback() : i.jsonpCallback, b ? i[b] = i[b].replace(ni, "$1" + h) : i.jsonp !== !1 && (i.url += (Fo.test(i.url) ? "&" : "?") + i.jsonp + "=" + h), i.converters["script json"] = function() {
          return m || d.error(h + " was not called"), m[0];
        }, i.dataTypes[0] = "json", f = e[h], e[h] = function() {
          m = arguments;
        }, l.always(function() {
          f === void 0 ? d(e).removeProp(h) : e[h] = f, i[h] && (i.jsonpCallback = s.jsonpCallback, Yi.push(h)), m && M(f) && f(m[0]), m = f = void 0;
        }), "script";
    }), x.createHTMLDocument = function() {
      var i = Y.implementation.createHTMLDocument("").body;
      return i.innerHTML = "<form></form><form></form>", i.childNodes.length === 2;
    }(), d.parseHTML = function(i, s, l) {
      if (typeof i != "string")
        return [];
      typeof s == "boolean" && (l = s, s = !1);
      var h, f, m;
      return s || (x.createHTMLDocument ? (s = Y.implementation.createHTMLDocument(""), h = s.createElement("base"), h.href = Y.location.href, s.head.appendChild(h)) : s = Y), f = Ae.exec(i), m = !l && [], f ? [s.createElement(f[1])] : (f = qe([i], s, m), m && m.length && d(m).remove(), d.merge([], f.childNodes));
    }, d.fn.load = function(i, s, l) {
      var h, f, m, b = this, E = i.indexOf(" ");
      return E > -1 && (h = ro(i.slice(E)), i = i.slice(0, E)), M(s) ? (l = s, s = void 0) : s && typeof s == "object" && (f = "POST"), b.length > 0 && d.ajax({
        url: i,
        // If "type" variable is undefined, then "GET" method will be used.
        // Make value of this field explicit since
        // user can override it through ajaxSetup method
        type: f || "GET",
        dataType: "html",
        data: s
      }).done(function(_) {
        m = arguments, b.html(h ? (
          // If a selector was specified, locate the right elements in a dummy div
          // Exclude scripts to avoid IE 'Permission Denied' errors
          d("<div>").append(d.parseHTML(_)).find(h)
        ) : (
          // Otherwise use the full result
          _
        ));
      }).always(l && function(_, O) {
        b.each(function() {
          l.apply(this, m || [_.responseText, O, _]);
        });
      }), this;
    }, d.expr.pseudos.animated = function(i) {
      return d.grep(d.timers, function(s) {
        return i === s.elem;
      }).length;
    }, d.offset = {
      setOffset: function(i, s, l) {
        var h, f, m, b, E, _, O, W = d.css(i, "position"), q = d(i), N = {};
        W === "static" && (i.style.position = "relative"), E = q.offset(), m = d.css(i, "top"), _ = d.css(i, "left"), O = (W === "absolute" || W === "fixed") && (m + _).indexOf("auto") > -1, O ? (h = q.position(), b = h.top, f = h.left) : (b = parseFloat(m) || 0, f = parseFloat(_) || 0), M(s) && (s = s.call(i, l, d.extend({}, E))), s.top != null && (N.top = s.top - E.top + b), s.left != null && (N.left = s.left - E.left + f), "using" in s ? s.using.call(i, N) : q.css(N);
      }
    }, d.fn.extend({
      // offset() relates an element's border box to the document origin
      offset: function(i) {
        if (arguments.length)
          return i === void 0 ? this : this.each(function(f) {
            d.offset.setOffset(this, i, f);
          });
        var s, l, h = this[0];
        if (h)
          return h.getClientRects().length ? (s = h.getBoundingClientRect(), l = h.ownerDocument.defaultView, {
            top: s.top + l.pageYOffset,
            left: s.left + l.pageXOffset
          }) : { top: 0, left: 0 };
      },
      // position() relates an element's margin box to its offset parent's padding box
      // This corresponds to the behavior of CSS absolute positioning
      position: function() {
        if (this[0]) {
          var i, s, l, h = this[0], f = { top: 0, left: 0 };
          if (d.css(h, "position") === "fixed")
            s = h.getBoundingClientRect();
          else {
            for (s = this.offset(), l = h.ownerDocument, i = h.offsetParent || l.documentElement; i && (i === l.body || i === l.documentElement) && d.css(i, "position") === "static"; )
              i = i.parentNode;
            i && i !== h && i.nodeType === 1 && (f = d(i).offset(), f.top += d.css(i, "borderTopWidth", !0), f.left += d.css(i, "borderLeftWidth", !0));
          }
          return {
            top: s.top - f.top - d.css(h, "marginTop", !0),
            left: s.left - f.left - d.css(h, "marginLeft", !0)
          };
        }
      },
      // This method will return documentElement in the following cases:
      // 1) For the element inside the iframe without offsetParent, this method will return
      //    documentElement of the parent window
      // 2) For the hidden or detached element
      // 3) For body or html element, i.e. in case of the html node - it will return itself
      //
      // but those exceptions were never presented as a real life use-cases
      // and might be considered as more preferable results.
      //
      // This logic, however, is not guaranteed and can change at any point in the future
      offsetParent: function() {
        return this.map(function() {
          for (var i = this.offsetParent; i && d.css(i, "position") === "static"; )
            i = i.offsetParent;
          return i || Jt;
        });
      }
    }), d.each({ scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function(i, s) {
      var l = s === "pageYOffset";
      d.fn[i] = function(h) {
        return vt(this, function(f, m, b) {
          var E;
          if (L(f) ? E = f : f.nodeType === 9 && (E = f.defaultView), b === void 0)
            return E ? E[s] : f[m];
          E ? E.scrollTo(
            l ? E.pageXOffset : b,
            l ? b : E.pageYOffset
          ) : f[m] = b;
        }, i, h, arguments.length);
      };
    }), d.each(["top", "left"], function(i, s) {
      d.cssHooks[s] = sr(
        x.pixelPosition,
        function(l, h) {
          if (h)
            return h = Lo(l, s), Gn.test(h) ? d(l).position()[s] + "px" : h;
        }
      );
    }), d.each({ Height: "height", Width: "width" }, function(i, s) {
      d.each({
        padding: "inner" + i,
        content: s,
        "": "outer" + i
      }, function(l, h) {
        d.fn[h] = function(f, m) {
          var b = arguments.length && (l || typeof f != "boolean"), E = l || (f === !0 || m === !0 ? "margin" : "border");
          return vt(this, function(_, O, W) {
            var q;
            return L(_) ? h.indexOf("outer") === 0 ? _["inner" + i] : _.document.documentElement["client" + i] : _.nodeType === 9 ? (q = _.documentElement, Math.max(
              _.body["scroll" + i],
              q["scroll" + i],
              _.body["offset" + i],
              q["offset" + i],
              q["client" + i]
            )) : W === void 0 ? (
              // Get width or height on the element, requesting but not forcing parseFloat
              d.css(_, O, E)
            ) : (
              // Set width or height on the element
              d.style(_, O, W, E)
            );
          }, s, b ? f : void 0, b);
        };
      });
    }), d.each([
      "ajaxStart",
      "ajaxStop",
      "ajaxComplete",
      "ajaxError",
      "ajaxSuccess",
      "ajaxSend"
    ], function(i, s) {
      d.fn[s] = function(l) {
        return this.on(s, l);
      };
    }), d.fn.extend({
      bind: function(i, s, l) {
        return this.on(i, null, s, l);
      },
      unbind: function(i, s) {
        return this.off(i, null, s);
      },
      delegate: function(i, s, l, h) {
        return this.on(s, i, l, h);
      },
      undelegate: function(i, s, l) {
        return arguments.length === 1 ? this.off(i, "**") : this.off(s, i || "**", l);
      },
      hover: function(i, s) {
        return this.on("mouseenter", i).on("mouseleave", s || i);
      }
    }), d.each(
      "blur focus focusin focusout resize scroll click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup contextmenu".split(" "),
      function(i, s) {
        d.fn[s] = function(l, h) {
          return arguments.length > 0 ? this.on(s, null, l, h) : this.trigger(s);
        };
      }
    );
    var yr = /^[\s\uFEFF\xA0]+|([^\s\uFEFF\xA0])[\s\uFEFF\xA0]+$/g;
    d.proxy = function(i, s) {
      var l, h, f;
      if (typeof s == "string" && (l = i[s], s = i, i = l), !!M(i))
        return h = a.call(arguments, 2), f = function() {
          return i.apply(s || this, h.concat(a.call(arguments)));
        }, f.guid = i.guid = i.guid || d.guid++, f;
    }, d.holdReady = function(i) {
      i ? d.readyWait++ : d.ready(!0);
    }, d.isArray = Array.isArray, d.parseJSON = JSON.parse, d.nodeName = R, d.isFunction = M, d.isWindow = L, d.camelCase = It, d.type = Q, d.now = Date.now, d.isNumeric = function(i) {
      var s = d.type(i);
      return (s === "number" || s === "string") && // parseFloat NaNs numeric-cast false positives ("")
      // ...but misinterprets leading-number strings, particularly hex literals ("0x...")
      // subtraction forces infinities to NaN
      !isNaN(i - parseFloat(i));
    }, d.trim = function(i) {
      return i == null ? "" : (i + "").replace(yr, "$1");
    };
    var Ur = e.jQuery, qr = e.$;
    return d.noConflict = function(i) {
      return e.$ === d && (e.$ = qr), i && e.jQuery === d && (e.jQuery = Ur), d;
    }, typeof t > "u" && (e.jQuery = e.$ = d), d;
  });
})(ap);
var Gy = ap.exports;
const Re = /* @__PURE__ */ Vy(Gy);
//! moment.js
//! version : 2.30.1
//! authors : Tim Wood, Iskren Chernev, Moment.js contributors
//! license : MIT
//! momentjs.com
var lp;
function pe() {
  return lp.apply(null, arguments);
}
function Zy(o) {
  lp = o;
}
function En(o) {
  return o instanceof Array || Object.prototype.toString.call(o) === "[object Array]";
}
function Br(o) {
  return o != null && Object.prototype.toString.call(o) === "[object Object]";
}
function dt(o, e) {
  return Object.prototype.hasOwnProperty.call(o, e);
}
function Yc(o) {
  if (Object.getOwnPropertyNames)
    return Object.getOwnPropertyNames(o).length === 0;
  var e;
  for (e in o)
    if (dt(o, e))
      return !1;
  return !0;
}
function Po(o) {
  return o === void 0;
}
function Si(o) {
  return typeof o == "number" || Object.prototype.toString.call(o) === "[object Number]";
}
function ba(o) {
  return o instanceof Date || Object.prototype.toString.call(o) === "[object Date]";
}
function cp(o, e) {
  var t = [], n, r = o.length;
  for (n = 0; n < r; ++n)
    t.push(e(o[n], n));
  return t;
}
function Zi(o, e) {
  for (var t in e)
    dt(e, t) && (o[t] = e[t]);
  return dt(e, "toString") && (o.toString = e.toString), dt(e, "valueOf") && (o.valueOf = e.valueOf), o;
}
function zn(o, e, t, n) {
  return Bp(o, e, t, n, !0).utc();
}
function Ky() {
  return {
    empty: !1,
    unusedTokens: [],
    unusedInput: [],
    overflow: -2,
    charsLeftOver: 0,
    nullInput: !1,
    invalidEra: null,
    invalidMonth: null,
    invalidFormat: !1,
    userInvalidated: !1,
    iso: !1,
    parsedDateParts: [],
    era: null,
    meridiem: null,
    rfc2822: !1,
    weekdayMismatch: !1
  };
}
function Ze(o) {
  return o._pf == null && (o._pf = Ky()), o._pf;
}
var ec;
Array.prototype.some ? ec = Array.prototype.some : ec = function(o) {
  var e = Object(this), t = e.length >>> 0, n;
  for (n = 0; n < t; n++)
    if (n in e && o.call(this, e[n], n, e))
      return !0;
  return !1;
};
function Fc(o) {
  var e = null, t = !1, n = o._d && !isNaN(o._d.getTime());
  if (n && (e = Ze(o), t = ec.call(e.parsedDateParts, function(r) {
    return r != null;
  }), n = e.overflow < 0 && !e.empty && !e.invalidEra && !e.invalidMonth && !e.invalidWeekday && !e.weekdayMismatch && !e.nullInput && !e.invalidFormat && !e.userInvalidated && (!e.meridiem || e.meridiem && t), o._strict && (n = n && e.charsLeftOver === 0 && e.unusedTokens.length === 0 && e.bigHour === void 0)), Object.isFrozen == null || !Object.isFrozen(o))
    o._isValid = n;
  else
    return n;
  return o._isValid;
}
function sl(o) {
  var e = zn(NaN);
  return o != null ? Zi(Ze(e), o) : Ze(e).userInvalidated = !0, e;
}
var dh = pe.momentProperties = [], Bl = !1;
function zc(o, e) {
  var t, n, r, a = dh.length;
  if (Po(e._isAMomentObject) || (o._isAMomentObject = e._isAMomentObject), Po(e._i) || (o._i = e._i), Po(e._f) || (o._f = e._f), Po(e._l) || (o._l = e._l), Po(e._strict) || (o._strict = e._strict), Po(e._tzm) || (o._tzm = e._tzm), Po(e._isUTC) || (o._isUTC = e._isUTC), Po(e._offset) || (o._offset = e._offset), Po(e._pf) || (o._pf = Ze(e)), Po(e._locale) || (o._locale = e._locale), a > 0)
    for (t = 0; t < a; t++)
      n = dh[t], r = e[n], Po(r) || (o[n] = r);
  return o;
}
function va(o) {
  zc(this, o), this._d = new Date(o._d != null ? o._d.getTime() : NaN), this.isValid() || (this._d = /* @__PURE__ */ new Date(NaN)), Bl === !1 && (Bl = !0, pe.updateOffset(this), Bl = !1);
}
function Mn(o) {
  return o instanceof va || o != null && o._isAMomentObject != null;
}
function dp(o) {
  pe.suppressDeprecationWarnings === !1 && typeof console < "u" && console.warn && console.warn("Deprecation warning: " + o);
}
function cn(o, e) {
  var t = !0;
  return Zi(function() {
    if (pe.deprecationHandler != null && pe.deprecationHandler(null, o), t) {
      var n = [], r, a, c, u = arguments.length;
      for (a = 0; a < u; a++) {
        if (r = "", typeof arguments[a] == "object") {
          r += `
[` + a + "] ";
          for (c in arguments[0])
            dt(arguments[0], c) && (r += c + ": " + arguments[0][c] + ", ");
          r = r.slice(0, -2);
        } else
          r = arguments[a];
        n.push(r);
      }
      dp(
        o + `
Arguments: ` + Array.prototype.slice.call(n).join("") + `
` + new Error().stack
      ), t = !1;
    }
    return e.apply(this, arguments);
  }, e);
}
var hh = {};
function hp(o, e) {
  pe.deprecationHandler != null && pe.deprecationHandler(o, e), hh[o] || (dp(e), hh[o] = !0);
}
pe.suppressDeprecationWarnings = !1;
pe.deprecationHandler = null;
function Wn(o) {
  return typeof Function < "u" && o instanceof Function || Object.prototype.toString.call(o) === "[object Function]";
}
function Xy(o) {
  var e, t;
  for (t in o)
    dt(o, t) && (e = o[t], Wn(e) ? this[t] = e : this["_" + t] = e);
  this._config = o, this._dayOfMonthOrdinalParseLenient = new RegExp(
    (this._dayOfMonthOrdinalParse.source || this._ordinalParse.source) + "|" + /\d{1,2}/.source
  );
}
function tc(o, e) {
  var t = Zi({}, o), n;
  for (n in e)
    dt(e, n) && (Br(o[n]) && Br(e[n]) ? (t[n] = {}, Zi(t[n], o[n]), Zi(t[n], e[n])) : e[n] != null ? t[n] = e[n] : delete t[n]);
  for (n in o)
    dt(o, n) && !dt(e, n) && Br(o[n]) && (t[n] = Zi({}, t[n]));
  return t;
}
function Wc(o) {
  o != null && this.set(o);
}
var oc;
Object.keys ? oc = Object.keys : oc = function(o) {
  var e, t = [];
  for (e in o)
    dt(o, e) && t.push(e);
  return t;
};
var Jy = {
  sameDay: "[Today at] LT",
  nextDay: "[Tomorrow at] LT",
  nextWeek: "dddd [at] LT",
  lastDay: "[Yesterday at] LT",
  lastWeek: "[Last] dddd [at] LT",
  sameElse: "L"
};
function Qy(o, e, t) {
  var n = this._calendar[o] || this._calendar.sameElse;
  return Wn(n) ? n.call(e, t) : n;
}
function $n(o, e, t) {
  var n = "" + Math.abs(o), r = e - n.length, a = o >= 0;
  return (a ? t ? "+" : "" : "-") + Math.pow(10, Math.max(0, r)).toString().substr(1) + n;
}
var Uc = /(\[[^\[]*\])|(\\)?([Hh]mm(ss)?|Mo|MM?M?M?|Do|DDDo|DD?D?D?|ddd?d?|do?|w[o|w]?|W[o|W]?|Qo?|N{1,5}|YYYYYY|YYYYY|YYYY|YY|y{2,4}|yo?|gg(ggg?)?|GG(GGG?)?|e|E|a|A|hh?|HH?|kk?|mm?|ss?|S{1,9}|x|X|zz?|ZZ?|.)/g, Ca = /(\[[^\[]*\])|(\\)?(LTS|LT|LL?L?L?|l{1,4})/g, Al = {}, hs = {};
function He(o, e, t, n) {
  var r = n;
  typeof n == "string" && (r = function() {
    return this[n]();
  }), o && (hs[o] = r), e && (hs[e[0]] = function() {
    return $n(r.apply(this, arguments), e[1], e[2]);
  }), t && (hs[t] = function() {
    return this.localeData().ordinal(
      r.apply(this, arguments),
      o
    );
  });
}
function ew(o) {
  return o.match(/\[[\s\S]/) ? o.replace(/^\[|\]$/g, "") : o.replace(/\\/g, "");
}
function tw(o) {
  var e = o.match(Uc), t, n;
  for (t = 0, n = e.length; t < n; t++)
    hs[e[t]] ? e[t] = hs[e[t]] : e[t] = ew(e[t]);
  return function(r) {
    var a = "", c;
    for (c = 0; c < n; c++)
      a += Wn(e[c]) ? e[c].call(r, o) : e[c];
    return a;
  };
}
function Oa(o, e) {
  return o.isValid() ? (e = up(e, o.localeData()), Al[e] = Al[e] || tw(e), Al[e](o)) : o.localeData().invalidDate();
}
function up(o, e) {
  var t = 5;
  function n(r) {
    return e.longDateFormat(r) || r;
  }
  for (Ca.lastIndex = 0; t >= 0 && Ca.test(o); )
    o = o.replace(
      Ca,
      n
    ), Ca.lastIndex = 0, t -= 1;
  return o;
}
var ow = {
  LTS: "h:mm:ss A",
  LT: "h:mm A",
  L: "MM/DD/YYYY",
  LL: "MMMM D, YYYY",
  LLL: "MMMM D, YYYY h:mm A",
  LLLL: "dddd, MMMM D, YYYY h:mm A"
};
function nw(o) {
  var e = this._longDateFormat[o], t = this._longDateFormat[o.toUpperCase()];
  return e || !t ? e : (this._longDateFormat[o] = t.match(Uc).map(function(n) {
    return n === "MMMM" || n === "MM" || n === "DD" || n === "dddd" ? n.slice(1) : n;
  }).join(""), this._longDateFormat[o]);
}
var iw = "Invalid date";
function rw() {
  return this._invalidDate;
}
var sw = "%d", aw = /\d{1,2}/;
function lw(o) {
  return this._ordinal.replace("%d", o);
}
var cw = {
  future: "in %s",
  past: "%s ago",
  s: "a few seconds",
  ss: "%d seconds",
  m: "a minute",
  mm: "%d minutes",
  h: "an hour",
  hh: "%d hours",
  d: "a day",
  dd: "%d days",
  w: "a week",
  ww: "%d weeks",
  M: "a month",
  MM: "%d months",
  y: "a year",
  yy: "%d years"
};
function dw(o, e, t, n) {
  var r = this._relativeTime[t];
  return Wn(r) ? r(o, e, t, n) : r.replace(/%d/i, o);
}
function hw(o, e) {
  var t = this._relativeTime[o > 0 ? "future" : "past"];
  return Wn(t) ? t(e) : t.replace(/%s/i, e);
}
var uh = {
  D: "date",
  dates: "date",
  date: "date",
  d: "day",
  days: "day",
  day: "day",
  e: "weekday",
  weekdays: "weekday",
  weekday: "weekday",
  E: "isoWeekday",
  isoweekdays: "isoWeekday",
  isoweekday: "isoWeekday",
  DDD: "dayOfYear",
  dayofyears: "dayOfYear",
  dayofyear: "dayOfYear",
  h: "hour",
  hours: "hour",
  hour: "hour",
  ms: "millisecond",
  milliseconds: "millisecond",
  millisecond: "millisecond",
  m: "minute",
  minutes: "minute",
  minute: "minute",
  M: "month",
  months: "month",
  month: "month",
  Q: "quarter",
  quarters: "quarter",
  quarter: "quarter",
  s: "second",
  seconds: "second",
  second: "second",
  gg: "weekYear",
  weekyears: "weekYear",
  weekyear: "weekYear",
  GG: "isoWeekYear",
  isoweekyears: "isoWeekYear",
  isoweekyear: "isoWeekYear",
  w: "week",
  weeks: "week",
  week: "week",
  W: "isoWeek",
  isoweeks: "isoWeek",
  isoweek: "isoWeek",
  y: "year",
  years: "year",
  year: "year"
};
function dn(o) {
  return typeof o == "string" ? uh[o] || uh[o.toLowerCase()] : void 0;
}
function qc(o) {
  var e = {}, t, n;
  for (n in o)
    dt(o, n) && (t = dn(n), t && (e[t] = o[n]));
  return e;
}
var uw = {
  date: 9,
  day: 11,
  weekday: 11,
  isoWeekday: 11,
  dayOfYear: 4,
  hour: 13,
  millisecond: 16,
  minute: 14,
  month: 8,
  quarter: 7,
  second: 15,
  weekYear: 1,
  isoWeekYear: 1,
  week: 5,
  isoWeek: 5,
  year: 1
};
function pw(o) {
  var e = [], t;
  for (t in o)
    dt(o, t) && e.push({ unit: t, priority: uw[t] });
  return e.sort(function(n, r) {
    return n.priority - r.priority;
  }), e;
}
var pp = /\d/, Jo = /\d\d/, fp = /\d{3}/, Vc = /\d{4}/, al = /[+-]?\d{6}/, At = /\d\d?/, mp = /\d\d\d\d?/, gp = /\d\d\d\d\d\d?/, ll = /\d{1,3}/, Gc = /\d{1,4}/, cl = /[+-]?\d{1,6}/, ks = /\d+/, dl = /[+-]?\d+/, fw = /Z|[+-]\d\d:?\d\d/gi, hl = /Z|[+-]\d\d(?::?\d\d)?/gi, mw = /[+-]?\d+(\.\d{1,3})?/, ya = /[0-9]{0,256}['a-z\u00A0-\u05FF\u0700-\uD7FF\uF900-\uFDCF\uFDF0-\uFF07\uFF10-\uFFEF]{1,256}|[\u0600-\u06FF\/]{1,256}(\s*?[\u0600-\u06FF]{1,256}){1,2}/i, xs = /^[1-9]\d?/, Zc = /^([1-9]\d|\d)/, $a;
$a = {};
function Oe(o, e, t) {
  $a[o] = Wn(e) ? e : function(n, r) {
    return n && t ? t : e;
  };
}
function gw(o, e) {
  return dt($a, o) ? $a[o](e._strict, e._locale) : new RegExp(bw(o));
}
function bw(o) {
  return ki(
    o.replace("\\", "").replace(
      /\\(\[)|\\(\])|\[([^\]\[]*)\]|\\(.)/g,
      function(e, t, n, r, a) {
        return t || n || r || a;
      }
    )
  );
}
function ki(o) {
  return o.replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&");
}
function rn(o) {
  return o < 0 ? Math.ceil(o) || 0 : Math.floor(o);
}
function ot(o) {
  var e = +o, t = 0;
  return e !== 0 && isFinite(e) && (t = rn(e)), t;
}
var nc = {};
function xt(o, e) {
  var t, n = e, r;
  for (typeof o == "string" && (o = [o]), Si(e) && (n = function(a, c) {
    c[e] = ot(a);
  }), r = o.length, t = 0; t < r; t++)
    nc[o[t]] = n;
}
function wa(o, e) {
  xt(o, function(t, n, r, a) {
    r._w = r._w || {}, e(t, r._w, r, a);
  });
}
function vw(o, e, t) {
  e != null && dt(nc, o) && nc[o](e, t._a, t, o);
}
function ul(o) {
  return o % 4 === 0 && o % 100 !== 0 || o % 400 === 0;
}
var vo = 0, bi = 1, Hn = 2, to = 3, Cn = 4, vi = 5, Dr = 6, yw = 7, ww = 8;
He("Y", 0, 0, function() {
  var o = this.year();
  return o <= 9999 ? $n(o, 4) : "+" + o;
});
He(0, ["YY", 2], 0, function() {
  return this.year() % 100;
});
He(0, ["YYYY", 4], 0, "year");
He(0, ["YYYYY", 5], 0, "year");
He(0, ["YYYYYY", 6, !0], 0, "year");
Oe("Y", dl);
Oe("YY", At, Jo);
Oe("YYYY", Gc, Vc);
Oe("YYYYY", cl, al);
Oe("YYYYYY", cl, al);
xt(["YYYYY", "YYYYYY"], vo);
xt("YYYY", function(o, e) {
  e[vo] = o.length === 2 ? pe.parseTwoDigitYear(o) : ot(o);
});
xt("YY", function(o, e) {
  e[vo] = pe.parseTwoDigitYear(o);
});
xt("Y", function(o, e) {
  e[vo] = parseInt(o, 10);
});
function ta(o) {
  return ul(o) ? 366 : 365;
}
pe.parseTwoDigitYear = function(o) {
  return ot(o) + (ot(o) > 68 ? 1900 : 2e3);
};
var bp = Cs("FullYear", !0);
function kw() {
  return ul(this.year());
}
function Cs(o, e) {
  return function(t) {
    return t != null ? (vp(this, o, t), pe.updateOffset(this, e), this) : la(this, o);
  };
}
function la(o, e) {
  if (!o.isValid())
    return NaN;
  var t = o._d, n = o._isUTC;
  switch (e) {
    case "Milliseconds":
      return n ? t.getUTCMilliseconds() : t.getMilliseconds();
    case "Seconds":
      return n ? t.getUTCSeconds() : t.getSeconds();
    case "Minutes":
      return n ? t.getUTCMinutes() : t.getMinutes();
    case "Hours":
      return n ? t.getUTCHours() : t.getHours();
    case "Date":
      return n ? t.getUTCDate() : t.getDate();
    case "Day":
      return n ? t.getUTCDay() : t.getDay();
    case "Month":
      return n ? t.getUTCMonth() : t.getMonth();
    case "FullYear":
      return n ? t.getUTCFullYear() : t.getFullYear();
    default:
      return NaN;
  }
}
function vp(o, e, t) {
  var n, r, a, c, u;
  if (!(!o.isValid() || isNaN(t))) {
    switch (n = o._d, r = o._isUTC, e) {
      case "Milliseconds":
        return void (r ? n.setUTCMilliseconds(t) : n.setMilliseconds(t));
      case "Seconds":
        return void (r ? n.setUTCSeconds(t) : n.setSeconds(t));
      case "Minutes":
        return void (r ? n.setUTCMinutes(t) : n.setMinutes(t));
      case "Hours":
        return void (r ? n.setUTCHours(t) : n.setHours(t));
      case "Date":
        return void (r ? n.setUTCDate(t) : n.setDate(t));
      case "FullYear":
        break;
      default:
        return;
    }
    a = t, c = o.month(), u = o.date(), u = u === 29 && c === 1 && !ul(a) ? 28 : u, r ? n.setUTCFullYear(a, c, u) : n.setFullYear(a, c, u);
  }
}
function xw(o) {
  return o = dn(o), Wn(this[o]) ? this[o]() : this;
}
function Cw(o, e) {
  if (typeof o == "object") {
    o = qc(o);
    var t = pw(o), n, r = t.length;
    for (n = 0; n < r; n++)
      this[t[n].unit](o[t[n].unit]);
  } else if (o = dn(o), Wn(this[o]))
    return this[o](e);
  return this;
}
function Sw(o, e) {
  return (o % e + e) % e;
}
var Zt;
Array.prototype.indexOf ? Zt = Array.prototype.indexOf : Zt = function(o) {
  var e;
  for (e = 0; e < this.length; ++e)
    if (this[e] === o)
      return e;
  return -1;
};
function Kc(o, e) {
  if (isNaN(o) || isNaN(e))
    return NaN;
  var t = Sw(e, 12);
  return o += (e - t) / 12, t === 1 ? ul(o) ? 29 : 28 : 31 - t % 7 % 2;
}
He("M", ["MM", 2], "Mo", function() {
  return this.month() + 1;
});
He("MMM", 0, 0, function(o) {
  return this.localeData().monthsShort(this, o);
});
He("MMMM", 0, 0, function(o) {
  return this.localeData().months(this, o);
});
Oe("M", At, xs);
Oe("MM", At, Jo);
Oe("MMM", function(o, e) {
  return e.monthsShortRegex(o);
});
Oe("MMMM", function(o, e) {
  return e.monthsRegex(o);
});
xt(["M", "MM"], function(o, e) {
  e[bi] = ot(o) - 1;
});
xt(["MMM", "MMMM"], function(o, e, t, n) {
  var r = t._locale.monthsParse(o, n, t._strict);
  r != null ? e[bi] = r : Ze(t).invalidMonth = o;
});
var Tw = "January_February_March_April_May_June_July_August_September_October_November_December".split(
  "_"
), yp = "Jan_Feb_Mar_Apr_May_Jun_Jul_Aug_Sep_Oct_Nov_Dec".split("_"), wp = /D[oD]?(\[[^\[\]]*\]|\s)+MMMM?/, _w = ya, Ew = ya;
function Mw(o, e) {
  return o ? En(this._months) ? this._months[o.month()] : this._months[(this._months.isFormat || wp).test(e) ? "format" : "standalone"][o.month()] : En(this._months) ? this._months : this._months.standalone;
}
function Dw(o, e) {
  return o ? En(this._monthsShort) ? this._monthsShort[o.month()] : this._monthsShort[wp.test(e) ? "format" : "standalone"][o.month()] : En(this._monthsShort) ? this._monthsShort : this._monthsShort.standalone;
}
function Lw(o, e, t) {
  var n, r, a, c = o.toLocaleLowerCase();
  if (!this._monthsParse)
    for (this._monthsParse = [], this._longMonthsParse = [], this._shortMonthsParse = [], n = 0; n < 12; ++n)
      a = zn([2e3, n]), this._shortMonthsParse[n] = this.monthsShort(
        a,
        ""
      ).toLocaleLowerCase(), this._longMonthsParse[n] = this.months(a, "").toLocaleLowerCase();
  return t ? e === "MMM" ? (r = Zt.call(this._shortMonthsParse, c), r !== -1 ? r : null) : (r = Zt.call(this._longMonthsParse, c), r !== -1 ? r : null) : e === "MMM" ? (r = Zt.call(this._shortMonthsParse, c), r !== -1 ? r : (r = Zt.call(this._longMonthsParse, c), r !== -1 ? r : null)) : (r = Zt.call(this._longMonthsParse, c), r !== -1 ? r : (r = Zt.call(this._shortMonthsParse, c), r !== -1 ? r : null));
}
function Ow(o, e, t) {
  var n, r, a;
  if (this._monthsParseExact)
    return Lw.call(this, o, e, t);
  for (this._monthsParse || (this._monthsParse = [], this._longMonthsParse = [], this._shortMonthsParse = []), n = 0; n < 12; n++)
    if (r = zn([2e3, n]), t && !this._longMonthsParse[n] && (this._longMonthsParse[n] = new RegExp(
      "^" + this.months(r, "").replace(".", "") + "$",
      "i"
    ), this._shortMonthsParse[n] = new RegExp(
      "^" + this.monthsShort(r, "").replace(".", "") + "$",
      "i"
    )), !t && !this._monthsParse[n] && (a = "^" + this.months(r, "") + "|^" + this.monthsShort(r, ""), this._monthsParse[n] = new RegExp(a.replace(".", ""), "i")), t && e === "MMMM" && this._longMonthsParse[n].test(o) || t && e === "MMM" && this._shortMonthsParse[n].test(o) || !t && this._monthsParse[n].test(o))
      return n;
}
function kp(o, e) {
  if (!o.isValid())
    return o;
  if (typeof e == "string") {
    if (/^\d+$/.test(e))
      e = ot(e);
    else if (e = o.localeData().monthsParse(e), !Si(e))
      return o;
  }
  var t = e, n = o.date();
  return n = n < 29 ? n : Math.min(n, Kc(o.year(), t)), o._isUTC ? o._d.setUTCMonth(t, n) : o._d.setMonth(t, n), o;
}
function xp(o) {
  return o != null ? (kp(this, o), pe.updateOffset(this, !0), this) : la(this, "Month");
}
function Bw() {
  return Kc(this.year(), this.month());
}
function Aw(o) {
  return this._monthsParseExact ? (dt(this, "_monthsRegex") || Cp.call(this), o ? this._monthsShortStrictRegex : this._monthsShortRegex) : (dt(this, "_monthsShortRegex") || (this._monthsShortRegex = _w), this._monthsShortStrictRegex && o ? this._monthsShortStrictRegex : this._monthsShortRegex);
}
function Rw(o) {
  return this._monthsParseExact ? (dt(this, "_monthsRegex") || Cp.call(this), o ? this._monthsStrictRegex : this._monthsRegex) : (dt(this, "_monthsRegex") || (this._monthsRegex = Ew), this._monthsStrictRegex && o ? this._monthsStrictRegex : this._monthsRegex);
}
function Cp() {
  function o(p, g) {
    return g.length - p.length;
  }
  var e = [], t = [], n = [], r, a, c, u;
  for (r = 0; r < 12; r++)
    a = zn([2e3, r]), c = ki(this.monthsShort(a, "")), u = ki(this.months(a, "")), e.push(c), t.push(u), n.push(u), n.push(c);
  e.sort(o), t.sort(o), n.sort(o), this._monthsRegex = new RegExp("^(" + n.join("|") + ")", "i"), this._monthsShortRegex = this._monthsRegex, this._monthsStrictRegex = new RegExp(
    "^(" + t.join("|") + ")",
    "i"
  ), this._monthsShortStrictRegex = new RegExp(
    "^(" + e.join("|") + ")",
    "i"
  );
}
function Iw(o, e, t, n, r, a, c) {
  var u;
  return o < 100 && o >= 0 ? (u = new Date(o + 400, e, t, n, r, a, c), isFinite(u.getFullYear()) && u.setFullYear(o)) : u = new Date(o, e, t, n, r, a, c), u;
}
function ca(o) {
  var e, t;
  return o < 100 && o >= 0 ? (t = Array.prototype.slice.call(arguments), t[0] = o + 400, e = new Date(Date.UTC.apply(null, t)), isFinite(e.getUTCFullYear()) && e.setUTCFullYear(o)) : e = new Date(Date.UTC.apply(null, arguments)), e;
}
function Ya(o, e, t) {
  var n = 7 + e - t, r = (7 + ca(o, 0, n).getUTCDay() - e) % 7;
  return -r + n - 1;
}
function Sp(o, e, t, n, r) {
  var a = (7 + t - n) % 7, c = Ya(o, n, r), u = 1 + 7 * (e - 1) + a + c, p, g;
  return u <= 0 ? (p = o - 1, g = ta(p) + u) : u > ta(o) ? (p = o + 1, g = u - ta(o)) : (p = o, g = u), {
    year: p,
    dayOfYear: g
  };
}
function da(o, e, t) {
  var n = Ya(o.year(), e, t), r = Math.floor((o.dayOfYear() - n - 1) / 7) + 1, a, c;
  return r < 1 ? (c = o.year() - 1, a = r + xi(c, e, t)) : r > xi(o.year(), e, t) ? (a = r - xi(o.year(), e, t), c = o.year() + 1) : (c = o.year(), a = r), {
    week: a,
    year: c
  };
}
function xi(o, e, t) {
  var n = Ya(o, e, t), r = Ya(o + 1, e, t);
  return (ta(o) - n + r) / 7;
}
He("w", ["ww", 2], "wo", "week");
He("W", ["WW", 2], "Wo", "isoWeek");
Oe("w", At, xs);
Oe("ww", At, Jo);
Oe("W", At, xs);
Oe("WW", At, Jo);
wa(
  ["w", "ww", "W", "WW"],
  function(o, e, t, n) {
    e[n.substr(0, 1)] = ot(o);
  }
);
function Nw(o) {
  return da(o, this._week.dow, this._week.doy).week;
}
var Pw = {
  dow: 0,
  // Sunday is the first day of the week.
  doy: 6
  // The week that contains Jan 6th is the first week of the year.
};
function Hw() {
  return this._week.dow;
}
function jw() {
  return this._week.doy;
}
function $w(o) {
  var e = this.localeData().week(this);
  return o == null ? e : this.add((o - e) * 7, "d");
}
function Yw(o) {
  var e = da(this, 1, 4).week;
  return o == null ? e : this.add((o - e) * 7, "d");
}
He("d", 0, "do", "day");
He("dd", 0, 0, function(o) {
  return this.localeData().weekdaysMin(this, o);
});
He("ddd", 0, 0, function(o) {
  return this.localeData().weekdaysShort(this, o);
});
He("dddd", 0, 0, function(o) {
  return this.localeData().weekdays(this, o);
});
He("e", 0, 0, "weekday");
He("E", 0, 0, "isoWeekday");
Oe("d", At);
Oe("e", At);
Oe("E", At);
Oe("dd", function(o, e) {
  return e.weekdaysMinRegex(o);
});
Oe("ddd", function(o, e) {
  return e.weekdaysShortRegex(o);
});
Oe("dddd", function(o, e) {
  return e.weekdaysRegex(o);
});
wa(["dd", "ddd", "dddd"], function(o, e, t, n) {
  var r = t._locale.weekdaysParse(o, n, t._strict);
  r != null ? e.d = r : Ze(t).invalidWeekday = o;
});
wa(["d", "e", "E"], function(o, e, t, n) {
  e[n] = ot(o);
});
function Fw(o, e) {
  return typeof o != "string" ? o : isNaN(o) ? (o = e.weekdaysParse(o), typeof o == "number" ? o : null) : parseInt(o, 10);
}
function zw(o, e) {
  return typeof o == "string" ? e.weekdaysParse(o) % 7 || 7 : isNaN(o) ? null : o;
}
function Xc(o, e) {
  return o.slice(e, 7).concat(o.slice(0, e));
}
var Ww = "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"), Tp = "Sun_Mon_Tue_Wed_Thu_Fri_Sat".split("_"), Uw = "Su_Mo_Tu_We_Th_Fr_Sa".split("_"), qw = ya, Vw = ya, Gw = ya;
function Zw(o, e) {
  var t = En(this._weekdays) ? this._weekdays : this._weekdays[o && o !== !0 && this._weekdays.isFormat.test(e) ? "format" : "standalone"];
  return o === !0 ? Xc(t, this._week.dow) : o ? t[o.day()] : t;
}
function Kw(o) {
  return o === !0 ? Xc(this._weekdaysShort, this._week.dow) : o ? this._weekdaysShort[o.day()] : this._weekdaysShort;
}
function Xw(o) {
  return o === !0 ? Xc(this._weekdaysMin, this._week.dow) : o ? this._weekdaysMin[o.day()] : this._weekdaysMin;
}
function Jw(o, e, t) {
  var n, r, a, c = o.toLocaleLowerCase();
  if (!this._weekdaysParse)
    for (this._weekdaysParse = [], this._shortWeekdaysParse = [], this._minWeekdaysParse = [], n = 0; n < 7; ++n)
      a = zn([2e3, 1]).day(n), this._minWeekdaysParse[n] = this.weekdaysMin(
        a,
        ""
      ).toLocaleLowerCase(), this._shortWeekdaysParse[n] = this.weekdaysShort(
        a,
        ""
      ).toLocaleLowerCase(), this._weekdaysParse[n] = this.weekdays(a, "").toLocaleLowerCase();
  return t ? e === "dddd" ? (r = Zt.call(this._weekdaysParse, c), r !== -1 ? r : null) : e === "ddd" ? (r = Zt.call(this._shortWeekdaysParse, c), r !== -1 ? r : null) : (r = Zt.call(this._minWeekdaysParse, c), r !== -1 ? r : null) : e === "dddd" ? (r = Zt.call(this._weekdaysParse, c), r !== -1 || (r = Zt.call(this._shortWeekdaysParse, c), r !== -1) ? r : (r = Zt.call(this._minWeekdaysParse, c), r !== -1 ? r : null)) : e === "ddd" ? (r = Zt.call(this._shortWeekdaysParse, c), r !== -1 || (r = Zt.call(this._weekdaysParse, c), r !== -1) ? r : (r = Zt.call(this._minWeekdaysParse, c), r !== -1 ? r : null)) : (r = Zt.call(this._minWeekdaysParse, c), r !== -1 || (r = Zt.call(this._weekdaysParse, c), r !== -1) ? r : (r = Zt.call(this._shortWeekdaysParse, c), r !== -1 ? r : null));
}
function Qw(o, e, t) {
  var n, r, a;
  if (this._weekdaysParseExact)
    return Jw.call(this, o, e, t);
  for (this._weekdaysParse || (this._weekdaysParse = [], this._minWeekdaysParse = [], this._shortWeekdaysParse = [], this._fullWeekdaysParse = []), n = 0; n < 7; n++)
    if (r = zn([2e3, 1]).day(n), t && !this._fullWeekdaysParse[n] && (this._fullWeekdaysParse[n] = new RegExp(
      "^" + this.weekdays(r, "").replace(".", "\\.?") + "$",
      "i"
    ), this._shortWeekdaysParse[n] = new RegExp(
      "^" + this.weekdaysShort(r, "").replace(".", "\\.?") + "$",
      "i"
    ), this._minWeekdaysParse[n] = new RegExp(
      "^" + this.weekdaysMin(r, "").replace(".", "\\.?") + "$",
      "i"
    )), this._weekdaysParse[n] || (a = "^" + this.weekdays(r, "") + "|^" + this.weekdaysShort(r, "") + "|^" + this.weekdaysMin(r, ""), this._weekdaysParse[n] = new RegExp(a.replace(".", ""), "i")), t && e === "dddd" && this._fullWeekdaysParse[n].test(o) || t && e === "ddd" && this._shortWeekdaysParse[n].test(o) || t && e === "dd" && this._minWeekdaysParse[n].test(o) || !t && this._weekdaysParse[n].test(o))
      return n;
}
function ek(o) {
  if (!this.isValid())
    return o != null ? this : NaN;
  var e = la(this, "Day");
  return o != null ? (o = Fw(o, this.localeData()), this.add(o - e, "d")) : e;
}
function tk(o) {
  if (!this.isValid())
    return o != null ? this : NaN;
  var e = (this.day() + 7 - this.localeData()._week.dow) % 7;
  return o == null ? e : this.add(o - e, "d");
}
function ok(o) {
  if (!this.isValid())
    return o != null ? this : NaN;
  if (o != null) {
    var e = zw(o, this.localeData());
    return this.day(this.day() % 7 ? e : e - 7);
  } else
    return this.day() || 7;
}
function nk(o) {
  return this._weekdaysParseExact ? (dt(this, "_weekdaysRegex") || Jc.call(this), o ? this._weekdaysStrictRegex : this._weekdaysRegex) : (dt(this, "_weekdaysRegex") || (this._weekdaysRegex = qw), this._weekdaysStrictRegex && o ? this._weekdaysStrictRegex : this._weekdaysRegex);
}
function ik(o) {
  return this._weekdaysParseExact ? (dt(this, "_weekdaysRegex") || Jc.call(this), o ? this._weekdaysShortStrictRegex : this._weekdaysShortRegex) : (dt(this, "_weekdaysShortRegex") || (this._weekdaysShortRegex = Vw), this._weekdaysShortStrictRegex && o ? this._weekdaysShortStrictRegex : this._weekdaysShortRegex);
}
function rk(o) {
  return this._weekdaysParseExact ? (dt(this, "_weekdaysRegex") || Jc.call(this), o ? this._weekdaysMinStrictRegex : this._weekdaysMinRegex) : (dt(this, "_weekdaysMinRegex") || (this._weekdaysMinRegex = Gw), this._weekdaysMinStrictRegex && o ? this._weekdaysMinStrictRegex : this._weekdaysMinRegex);
}
function Jc() {
  function o(v, k) {
    return k.length - v.length;
  }
  var e = [], t = [], n = [], r = [], a, c, u, p, g;
  for (a = 0; a < 7; a++)
    c = zn([2e3, 1]).day(a), u = ki(this.weekdaysMin(c, "")), p = ki(this.weekdaysShort(c, "")), g = ki(this.weekdays(c, "")), e.push(u), t.push(p), n.push(g), r.push(u), r.push(p), r.push(g);
  e.sort(o), t.sort(o), n.sort(o), r.sort(o), this._weekdaysRegex = new RegExp("^(" + r.join("|") + ")", "i"), this._weekdaysShortRegex = this._weekdaysRegex, this._weekdaysMinRegex = this._weekdaysRegex, this._weekdaysStrictRegex = new RegExp(
    "^(" + n.join("|") + ")",
    "i"
  ), this._weekdaysShortStrictRegex = new RegExp(
    "^(" + t.join("|") + ")",
    "i"
  ), this._weekdaysMinStrictRegex = new RegExp(
    "^(" + e.join("|") + ")",
    "i"
  );
}
function Qc() {
  return this.hours() % 12 || 12;
}
function sk() {
  return this.hours() || 24;
}
He("H", ["HH", 2], 0, "hour");
He("h", ["hh", 2], 0, Qc);
He("k", ["kk", 2], 0, sk);
He("hmm", 0, 0, function() {
  return "" + Qc.apply(this) + $n(this.minutes(), 2);
});
He("hmmss", 0, 0, function() {
  return "" + Qc.apply(this) + $n(this.minutes(), 2) + $n(this.seconds(), 2);
});
He("Hmm", 0, 0, function() {
  return "" + this.hours() + $n(this.minutes(), 2);
});
He("Hmmss", 0, 0, function() {
  return "" + this.hours() + $n(this.minutes(), 2) + $n(this.seconds(), 2);
});
function _p(o, e) {
  He(o, 0, 0, function() {
    return this.localeData().meridiem(
      this.hours(),
      this.minutes(),
      e
    );
  });
}
_p("a", !0);
_p("A", !1);
function Ep(o, e) {
  return e._meridiemParse;
}
Oe("a", Ep);
Oe("A", Ep);
Oe("H", At, Zc);
Oe("h", At, xs);
Oe("k", At, xs);
Oe("HH", At, Jo);
Oe("hh", At, Jo);
Oe("kk", At, Jo);
Oe("hmm", mp);
Oe("hmmss", gp);
Oe("Hmm", mp);
Oe("Hmmss", gp);
xt(["H", "HH"], to);
xt(["k", "kk"], function(o, e, t) {
  var n = ot(o);
  e[to] = n === 24 ? 0 : n;
});
xt(["a", "A"], function(o, e, t) {
  t._isPm = t._locale.isPM(o), t._meridiem = o;
});
xt(["h", "hh"], function(o, e, t) {
  e[to] = ot(o), Ze(t).bigHour = !0;
});
xt("hmm", function(o, e, t) {
  var n = o.length - 2;
  e[to] = ot(o.substr(0, n)), e[Cn] = ot(o.substr(n)), Ze(t).bigHour = !0;
});
xt("hmmss", function(o, e, t) {
  var n = o.length - 4, r = o.length - 2;
  e[to] = ot(o.substr(0, n)), e[Cn] = ot(o.substr(n, 2)), e[vi] = ot(o.substr(r)), Ze(t).bigHour = !0;
});
xt("Hmm", function(o, e, t) {
  var n = o.length - 2;
  e[to] = ot(o.substr(0, n)), e[Cn] = ot(o.substr(n));
});
xt("Hmmss", function(o, e, t) {
  var n = o.length - 4, r = o.length - 2;
  e[to] = ot(o.substr(0, n)), e[Cn] = ot(o.substr(n, 2)), e[vi] = ot(o.substr(r));
});
function ak(o) {
  return (o + "").toLowerCase().charAt(0) === "p";
}
var lk = /[ap]\.?m?\.?/i, ck = Cs("Hours", !0);
function dk(o, e, t) {
  return o > 11 ? t ? "pm" : "PM" : t ? "am" : "AM";
}
var Mp = {
  calendar: Jy,
  longDateFormat: ow,
  invalidDate: iw,
  ordinal: sw,
  dayOfMonthOrdinalParse: aw,
  relativeTime: cw,
  months: Tw,
  monthsShort: yp,
  week: Pw,
  weekdays: Ww,
  weekdaysMin: Uw,
  weekdaysShort: Tp,
  meridiemParse: lk
}, Pt = {}, Gs = {}, ha;
function hk(o, e) {
  var t, n = Math.min(o.length, e.length);
  for (t = 0; t < n; t += 1)
    if (o[t] !== e[t])
      return t;
  return n;
}
function ph(o) {
  return o && o.toLowerCase().replace("_", "-");
}
function uk(o) {
  for (var e = 0, t, n, r, a; e < o.length; ) {
    for (a = ph(o[e]).split("-"), t = a.length, n = ph(o[e + 1]), n = n ? n.split("-") : null; t > 0; ) {
      if (r = pl(a.slice(0, t).join("-")), r)
        return r;
      if (n && n.length >= t && hk(a, n) >= t - 1)
        break;
      t--;
    }
    e++;
  }
  return ha;
}
function pk(o) {
  return !!(o && o.match("^[^/\\\\]*$"));
}
function pl(o) {
  var e = null, t;
  if (Pt[o] === void 0 && typeof module < "u" && module && module.exports && pk(o))
    try {
      e = ha._abbr, t = require, t("./locale/" + o), Ji(e);
    } catch {
      Pt[o] = null;
    }
  return Pt[o];
}
function Ji(o, e) {
  var t;
  return o && (Po(e) ? t = Ei(o) : t = ed(o, e), t ? ha = t : typeof console < "u" && console.warn && console.warn(
    "Locale " + o + " not found. Did you forget to load it?"
  )), ha._abbr;
}
function ed(o, e) {
  if (e !== null) {
    var t, n = Mp;
    if (e.abbr = o, Pt[o] != null)
      hp(
        "defineLocaleOverride",
        "use moment.updateLocale(localeName, config) to change an existing locale. moment.defineLocale(localeName, config) should only be used for creating a new locale See http://momentjs.com/guides/#/warnings/define-locale/ for more info."
      ), n = Pt[o]._config;
    else if (e.parentLocale != null)
      if (Pt[e.parentLocale] != null)
        n = Pt[e.parentLocale]._config;
      else if (t = pl(e.parentLocale), t != null)
        n = t._config;
      else
        return Gs[e.parentLocale] || (Gs[e.parentLocale] = []), Gs[e.parentLocale].push({
          name: o,
          config: e
        }), null;
    return Pt[o] = new Wc(tc(n, e)), Gs[o] && Gs[o].forEach(function(r) {
      ed(r.name, r.config);
    }), Ji(o), Pt[o];
  } else
    return delete Pt[o], null;
}
function fk(o, e) {
  if (e != null) {
    var t, n, r = Mp;
    Pt[o] != null && Pt[o].parentLocale != null ? Pt[o].set(tc(Pt[o]._config, e)) : (n = pl(o), n != null && (r = n._config), e = tc(r, e), n == null && (e.abbr = o), t = new Wc(e), t.parentLocale = Pt[o], Pt[o] = t), Ji(o);
  } else
    Pt[o] != null && (Pt[o].parentLocale != null ? (Pt[o] = Pt[o].parentLocale, o === Ji() && Ji(o)) : Pt[o] != null && delete Pt[o]);
  return Pt[o];
}
function Ei(o) {
  var e;
  if (o && o._locale && o._locale._abbr && (o = o._locale._abbr), !o)
    return ha;
  if (!En(o)) {
    if (e = pl(o), e)
      return e;
    o = [o];
  }
  return uk(o);
}
function mk() {
  return oc(Pt);
}
function td(o) {
  var e, t = o._a;
  return t && Ze(o).overflow === -2 && (e = t[bi] < 0 || t[bi] > 11 ? bi : t[Hn] < 1 || t[Hn] > Kc(t[vo], t[bi]) ? Hn : t[to] < 0 || t[to] > 24 || t[to] === 24 && (t[Cn] !== 0 || t[vi] !== 0 || t[Dr] !== 0) ? to : t[Cn] < 0 || t[Cn] > 59 ? Cn : t[vi] < 0 || t[vi] > 59 ? vi : t[Dr] < 0 || t[Dr] > 999 ? Dr : -1, Ze(o)._overflowDayOfYear && (e < vo || e > Hn) && (e = Hn), Ze(o)._overflowWeeks && e === -1 && (e = yw), Ze(o)._overflowWeekday && e === -1 && (e = ww), Ze(o).overflow = e), o;
}
var gk = /^\s*((?:[+-]\d{6}|\d{4})-(?:\d\d-\d\d|W\d\d-\d|W\d\d|\d\d\d|\d\d))(?:(T| )(\d\d(?::\d\d(?::\d\d(?:[.,]\d+)?)?)?)([+-]\d\d(?::?\d\d)?|\s*Z)?)?$/, bk = /^\s*((?:[+-]\d{6}|\d{4})(?:\d\d\d\d|W\d\d\d|W\d\d|\d\d\d|\d\d|))(?:(T| )(\d\d(?:\d\d(?:\d\d(?:[.,]\d+)?)?)?)([+-]\d\d(?::?\d\d)?|\s*Z)?)?$/, vk = /Z|[+-]\d\d(?::?\d\d)?/, Sa = [
  ["YYYYYY-MM-DD", /[+-]\d{6}-\d\d-\d\d/],
  ["YYYY-MM-DD", /\d{4}-\d\d-\d\d/],
  ["GGGG-[W]WW-E", /\d{4}-W\d\d-\d/],
  ["GGGG-[W]WW", /\d{4}-W\d\d/, !1],
  ["YYYY-DDD", /\d{4}-\d{3}/],
  ["YYYY-MM", /\d{4}-\d\d/, !1],
  ["YYYYYYMMDD", /[+-]\d{10}/],
  ["YYYYMMDD", /\d{8}/],
  ["GGGG[W]WWE", /\d{4}W\d{3}/],
  ["GGGG[W]WW", /\d{4}W\d{2}/, !1],
  ["YYYYDDD", /\d{7}/],
  ["YYYYMM", /\d{6}/, !1],
  ["YYYY", /\d{4}/, !1]
], Rl = [
  ["HH:mm:ss.SSSS", /\d\d:\d\d:\d\d\.\d+/],
  ["HH:mm:ss,SSSS", /\d\d:\d\d:\d\d,\d+/],
  ["HH:mm:ss", /\d\d:\d\d:\d\d/],
  ["HH:mm", /\d\d:\d\d/],
  ["HHmmss.SSSS", /\d\d\d\d\d\d\.\d+/],
  ["HHmmss,SSSS", /\d\d\d\d\d\d,\d+/],
  ["HHmmss", /\d\d\d\d\d\d/],
  ["HHmm", /\d\d\d\d/],
  ["HH", /\d\d/]
], yk = /^\/?Date\((-?\d+)/i, wk = /^(?:(Mon|Tue|Wed|Thu|Fri|Sat|Sun),?\s)?(\d{1,2})\s(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)\s(\d{2,4})\s(\d\d):(\d\d)(?::(\d\d))?\s(?:(UT|GMT|[ECMP][SD]T)|([Zz])|([+-]\d{4}))$/, kk = {
  UT: 0,
  GMT: 0,
  EDT: -4 * 60,
  EST: -5 * 60,
  CDT: -5 * 60,
  CST: -6 * 60,
  MDT: -6 * 60,
  MST: -7 * 60,
  PDT: -7 * 60,
  PST: -8 * 60
};
function Dp(o) {
  var e, t, n = o._i, r = gk.exec(n) || bk.exec(n), a, c, u, p, g = Sa.length, v = Rl.length;
  if (r) {
    for (Ze(o).iso = !0, e = 0, t = g; e < t; e++)
      if (Sa[e][1].exec(r[1])) {
        c = Sa[e][0], a = Sa[e][2] !== !1;
        break;
      }
    if (c == null) {
      o._isValid = !1;
      return;
    }
    if (r[3]) {
      for (e = 0, t = v; e < t; e++)
        if (Rl[e][1].exec(r[3])) {
          u = (r[2] || " ") + Rl[e][0];
          break;
        }
      if (u == null) {
        o._isValid = !1;
        return;
      }
    }
    if (!a && u != null) {
      o._isValid = !1;
      return;
    }
    if (r[4])
      if (vk.exec(r[4]))
        p = "Z";
      else {
        o._isValid = !1;
        return;
      }
    o._f = c + (u || "") + (p || ""), nd(o);
  } else
    o._isValid = !1;
}
function xk(o, e, t, n, r, a) {
  var c = [
    Ck(o),
    yp.indexOf(e),
    parseInt(t, 10),
    parseInt(n, 10),
    parseInt(r, 10)
  ];
  return a && c.push(parseInt(a, 10)), c;
}
function Ck(o) {
  var e = parseInt(o, 10);
  return e <= 49 ? 2e3 + e : e <= 999 ? 1900 + e : e;
}
function Sk(o) {
  return o.replace(/\([^()]*\)|[\n\t]/g, " ").replace(/(\s\s+)/g, " ").replace(/^\s\s*/, "").replace(/\s\s*$/, "");
}
function Tk(o, e, t) {
  if (o) {
    var n = Tp.indexOf(o), r = new Date(
      e[0],
      e[1],
      e[2]
    ).getDay();
    if (n !== r)
      return Ze(t).weekdayMismatch = !0, t._isValid = !1, !1;
  }
  return !0;
}
function _k(o, e, t) {
  if (o)
    return kk[o];
  if (e)
    return 0;
  var n = parseInt(t, 10), r = n % 100, a = (n - r) / 100;
  return a * 60 + r;
}
function Lp(o) {
  var e = wk.exec(Sk(o._i)), t;
  if (e) {
    if (t = xk(
      e[4],
      e[3],
      e[2],
      e[5],
      e[6],
      e[7]
    ), !Tk(e[1], t, o))
      return;
    o._a = t, o._tzm = _k(e[8], e[9], e[10]), o._d = ca.apply(null, o._a), o._d.setUTCMinutes(o._d.getUTCMinutes() - o._tzm), Ze(o).rfc2822 = !0;
  } else
    o._isValid = !1;
}
function Ek(o) {
  var e = yk.exec(o._i);
  if (e !== null) {
    o._d = /* @__PURE__ */ new Date(+e[1]);
    return;
  }
  if (Dp(o), o._isValid === !1)
    delete o._isValid;
  else
    return;
  if (Lp(o), o._isValid === !1)
    delete o._isValid;
  else
    return;
  o._strict ? o._isValid = !1 : pe.createFromInputFallback(o);
}
pe.createFromInputFallback = cn(
  "value provided is not in a recognized RFC2822 or ISO format. moment construction falls back to js Date(), which is not reliable across all browsers and versions. Non RFC2822/ISO date formats are discouraged. Please refer to http://momentjs.com/guides/#/warnings/js-date/ for more info.",
  function(o) {
    o._d = /* @__PURE__ */ new Date(o._i + (o._useUTC ? " UTC" : ""));
  }
);
function is(o, e, t) {
  return o ?? e ?? t;
}
function Mk(o) {
  var e = new Date(pe.now());
  return o._useUTC ? [
    e.getUTCFullYear(),
    e.getUTCMonth(),
    e.getUTCDate()
  ] : [e.getFullYear(), e.getMonth(), e.getDate()];
}
function od(o) {
  var e, t, n = [], r, a, c;
  if (!o._d) {
    for (r = Mk(o), o._w && o._a[Hn] == null && o._a[bi] == null && Dk(o), o._dayOfYear != null && (c = is(o._a[vo], r[vo]), (o._dayOfYear > ta(c) || o._dayOfYear === 0) && (Ze(o)._overflowDayOfYear = !0), t = ca(c, 0, o._dayOfYear), o._a[bi] = t.getUTCMonth(), o._a[Hn] = t.getUTCDate()), e = 0; e < 3 && o._a[e] == null; ++e)
      o._a[e] = n[e] = r[e];
    for (; e < 7; e++)
      o._a[e] = n[e] = o._a[e] == null ? e === 2 ? 1 : 0 : o._a[e];
    o._a[to] === 24 && o._a[Cn] === 0 && o._a[vi] === 0 && o._a[Dr] === 0 && (o._nextDay = !0, o._a[to] = 0), o._d = (o._useUTC ? ca : Iw).apply(
      null,
      n
    ), a = o._useUTC ? o._d.getUTCDay() : o._d.getDay(), o._tzm != null && o._d.setUTCMinutes(o._d.getUTCMinutes() - o._tzm), o._nextDay && (o._a[to] = 24), o._w && typeof o._w.d < "u" && o._w.d !== a && (Ze(o).weekdayMismatch = !0);
  }
}
function Dk(o) {
  var e, t, n, r, a, c, u, p, g;
  e = o._w, e.GG != null || e.W != null || e.E != null ? (a = 1, c = 4, t = is(
    e.GG,
    o._a[vo],
    da(Ot(), 1, 4).year
  ), n = is(e.W, 1), r = is(e.E, 1), (r < 1 || r > 7) && (p = !0)) : (a = o._locale._week.dow, c = o._locale._week.doy, g = da(Ot(), a, c), t = is(e.gg, o._a[vo], g.year), n = is(e.w, g.week), e.d != null ? (r = e.d, (r < 0 || r > 6) && (p = !0)) : e.e != null ? (r = e.e + a, (e.e < 0 || e.e > 6) && (p = !0)) : r = a), n < 1 || n > xi(t, a, c) ? Ze(o)._overflowWeeks = !0 : p != null ? Ze(o)._overflowWeekday = !0 : (u = Sp(t, n, r, a, c), o._a[vo] = u.year, o._dayOfYear = u.dayOfYear);
}
pe.ISO_8601 = function() {
};
pe.RFC_2822 = function() {
};
function nd(o) {
  if (o._f === pe.ISO_8601) {
    Dp(o);
    return;
  }
  if (o._f === pe.RFC_2822) {
    Lp(o);
    return;
  }
  o._a = [], Ze(o).empty = !0;
  var e = "" + o._i, t, n, r, a, c, u = e.length, p = 0, g, v;
  for (r = up(o._f, o._locale).match(Uc) || [], v = r.length, t = 0; t < v; t++)
    a = r[t], n = (e.match(gw(a, o)) || [])[0], n && (c = e.substr(0, e.indexOf(n)), c.length > 0 && Ze(o).unusedInput.push(c), e = e.slice(
      e.indexOf(n) + n.length
    ), p += n.length), hs[a] ? (n ? Ze(o).empty = !1 : Ze(o).unusedTokens.push(a), vw(a, n, o)) : o._strict && !n && Ze(o).unusedTokens.push(a);
  Ze(o).charsLeftOver = u - p, e.length > 0 && Ze(o).unusedInput.push(e), o._a[to] <= 12 && Ze(o).bigHour === !0 && o._a[to] > 0 && (Ze(o).bigHour = void 0), Ze(o).parsedDateParts = o._a.slice(0), Ze(o).meridiem = o._meridiem, o._a[to] = Lk(
    o._locale,
    o._a[to],
    o._meridiem
  ), g = Ze(o).era, g !== null && (o._a[vo] = o._locale.erasConvertYear(g, o._a[vo])), od(o), td(o);
}
function Lk(o, e, t) {
  var n;
  return t == null ? e : o.meridiemHour != null ? o.meridiemHour(e, t) : (o.isPM != null && (n = o.isPM(t), n && e < 12 && (e += 12), !n && e === 12 && (e = 0)), e);
}
function Ok(o) {
  var e, t, n, r, a, c, u = !1, p = o._f.length;
  if (p === 0) {
    Ze(o).invalidFormat = !0, o._d = /* @__PURE__ */ new Date(NaN);
    return;
  }
  for (r = 0; r < p; r++)
    a = 0, c = !1, e = zc({}, o), o._useUTC != null && (e._useUTC = o._useUTC), e._f = o._f[r], nd(e), Fc(e) && (c = !0), a += Ze(e).charsLeftOver, a += Ze(e).unusedTokens.length * 10, Ze(e).score = a, u ? a < n && (n = a, t = e) : (n == null || a < n || c) && (n = a, t = e, c && (u = !0));
  Zi(o, t || e);
}
function Bk(o) {
  if (!o._d) {
    var e = qc(o._i), t = e.day === void 0 ? e.date : e.day;
    o._a = cp(
      [e.year, e.month, t, e.hour, e.minute, e.second, e.millisecond],
      function(n) {
        return n && parseInt(n, 10);
      }
    ), od(o);
  }
}
function Ak(o) {
  var e = new va(td(Op(o)));
  return e._nextDay && (e.add(1, "d"), e._nextDay = void 0), e;
}
function Op(o) {
  var e = o._i, t = o._f;
  return o._locale = o._locale || Ei(o._l), e === null || t === void 0 && e === "" ? sl({ nullInput: !0 }) : (typeof e == "string" && (o._i = e = o._locale.preparse(e)), Mn(e) ? new va(td(e)) : (ba(e) ? o._d = e : En(t) ? Ok(o) : t ? nd(o) : Rk(o), Fc(o) || (o._d = null), o));
}
function Rk(o) {
  var e = o._i;
  Po(e) ? o._d = new Date(pe.now()) : ba(e) ? o._d = new Date(e.valueOf()) : typeof e == "string" ? Ek(o) : En(e) ? (o._a = cp(e.slice(0), function(t) {
    return parseInt(t, 10);
  }), od(o)) : Br(e) ? Bk(o) : Si(e) ? o._d = new Date(e) : pe.createFromInputFallback(o);
}
function Bp(o, e, t, n, r) {
  var a = {};
  return (e === !0 || e === !1) && (n = e, e = void 0), (t === !0 || t === !1) && (n = t, t = void 0), (Br(o) && Yc(o) || En(o) && o.length === 0) && (o = void 0), a._isAMomentObject = !0, a._useUTC = a._isUTC = r, a._l = t, a._i = o, a._f = e, a._strict = n, Ak(a);
}
function Ot(o, e, t, n) {
  return Bp(o, e, t, n, !1);
}
var Ik = cn(
  "moment().min is deprecated, use moment.max instead. http://momentjs.com/guides/#/warnings/min-max/",
  function() {
    var o = Ot.apply(null, arguments);
    return this.isValid() && o.isValid() ? o < this ? this : o : sl();
  }
), Nk = cn(
  "moment().max is deprecated, use moment.min instead. http://momentjs.com/guides/#/warnings/min-max/",
  function() {
    var o = Ot.apply(null, arguments);
    return this.isValid() && o.isValid() ? o > this ? this : o : sl();
  }
);
function Ap(o, e) {
  var t, n;
  if (e.length === 1 && En(e[0]) && (e = e[0]), !e.length)
    return Ot();
  for (t = e[0], n = 1; n < e.length; ++n)
    (!e[n].isValid() || e[n][o](t)) && (t = e[n]);
  return t;
}
function Pk() {
  var o = [].slice.call(arguments, 0);
  return Ap("isBefore", o);
}
function Hk() {
  var o = [].slice.call(arguments, 0);
  return Ap("isAfter", o);
}
var jk = function() {
  return Date.now ? Date.now() : +/* @__PURE__ */ new Date();
}, Zs = [
  "year",
  "quarter",
  "month",
  "week",
  "day",
  "hour",
  "minute",
  "second",
  "millisecond"
];
function $k(o) {
  var e, t = !1, n, r = Zs.length;
  for (e in o)
    if (dt(o, e) && !(Zt.call(Zs, e) !== -1 && (o[e] == null || !isNaN(o[e]))))
      return !1;
  for (n = 0; n < r; ++n)
    if (o[Zs[n]]) {
      if (t)
        return !1;
      parseFloat(o[Zs[n]]) !== ot(o[Zs[n]]) && (t = !0);
    }
  return !0;
}
function Yk() {
  return this._isValid;
}
function Fk() {
  return On(NaN);
}
function fl(o) {
  var e = qc(o), t = e.year || 0, n = e.quarter || 0, r = e.month || 0, a = e.week || e.isoWeek || 0, c = e.day || 0, u = e.hour || 0, p = e.minute || 0, g = e.second || 0, v = e.millisecond || 0;
  this._isValid = $k(e), this._milliseconds = +v + g * 1e3 + // 1000
  p * 6e4 + // 1000 * 60
  u * 1e3 * 60 * 60, this._days = +c + a * 7, this._months = +r + n * 3 + t * 12, this._data = {}, this._locale = Ei(), this._bubble();
}
function Ba(o) {
  return o instanceof fl;
}
function ic(o) {
  return o < 0 ? Math.round(-1 * o) * -1 : Math.round(o);
}
function zk(o, e, t) {
  var n = Math.min(o.length, e.length), r = Math.abs(o.length - e.length), a = 0, c;
  for (c = 0; c < n; c++)
    (t && o[c] !== e[c] || !t && ot(o[c]) !== ot(e[c])) && a++;
  return a + r;
}
function Rp(o, e) {
  He(o, 0, 0, function() {
    var t = this.utcOffset(), n = "+";
    return t < 0 && (t = -t, n = "-"), n + $n(~~(t / 60), 2) + e + $n(~~t % 60, 2);
  });
}
Rp("Z", ":");
Rp("ZZ", "");
Oe("Z", hl);
Oe("ZZ", hl);
xt(["Z", "ZZ"], function(o, e, t) {
  t._useUTC = !0, t._tzm = id(hl, o);
});
var Wk = /([\+\-]|\d\d)/gi;
function id(o, e) {
  var t = (e || "").match(o), n, r, a;
  return t === null ? null : (n = t[t.length - 1] || [], r = (n + "").match(Wk) || ["-", 0, 0], a = +(r[1] * 60) + ot(r[2]), a === 0 ? 0 : r[0] === "+" ? a : -a);
}
function rd(o, e) {
  var t, n;
  return e._isUTC ? (t = e.clone(), n = (Mn(o) || ba(o) ? o.valueOf() : Ot(o).valueOf()) - t.valueOf(), t._d.setTime(t._d.valueOf() + n), pe.updateOffset(t, !1), t) : Ot(o).local();
}
function rc(o) {
  return -Math.round(o._d.getTimezoneOffset());
}
pe.updateOffset = function() {
};
function Uk(o, e, t) {
  var n = this._offset || 0, r;
  if (!this.isValid())
    return o != null ? this : NaN;
  if (o != null) {
    if (typeof o == "string") {
      if (o = id(hl, o), o === null)
        return this;
    } else
      Math.abs(o) < 16 && !t && (o = o * 60);
    return !this._isUTC && e && (r = rc(this)), this._offset = o, this._isUTC = !0, r != null && this.add(r, "m"), n !== o && (!e || this._changeInProgress ? Pp(
      this,
      On(o - n, "m"),
      1,
      !1
    ) : this._changeInProgress || (this._changeInProgress = !0, pe.updateOffset(this, !0), this._changeInProgress = null)), this;
  } else
    return this._isUTC ? n : rc(this);
}
function qk(o, e) {
  return o != null ? (typeof o != "string" && (o = -o), this.utcOffset(o, e), this) : -this.utcOffset();
}
function Vk(o) {
  return this.utcOffset(0, o);
}
function Gk(o) {
  return this._isUTC && (this.utcOffset(0, o), this._isUTC = !1, o && this.subtract(rc(this), "m")), this;
}
function Zk() {
  if (this._tzm != null)
    this.utcOffset(this._tzm, !1, !0);
  else if (typeof this._i == "string") {
    var o = id(fw, this._i);
    o != null ? this.utcOffset(o) : this.utcOffset(0, !0);
  }
  return this;
}
function Kk(o) {
  return this.isValid() ? (o = o ? Ot(o).utcOffset() : 0, (this.utcOffset() - o) % 60 === 0) : !1;
}
function Xk() {
  return this.utcOffset() > this.clone().month(0).utcOffset() || this.utcOffset() > this.clone().month(5).utcOffset();
}
function Jk() {
  if (!Po(this._isDSTShifted))
    return this._isDSTShifted;
  var o = {}, e;
  return zc(o, this), o = Op(o), o._a ? (e = o._isUTC ? zn(o._a) : Ot(o._a), this._isDSTShifted = this.isValid() && zk(o._a, e.toArray()) > 0) : this._isDSTShifted = !1, this._isDSTShifted;
}
function Qk() {
  return this.isValid() ? !this._isUTC : !1;
}
function ex() {
  return this.isValid() ? this._isUTC : !1;
}
function Ip() {
  return this.isValid() ? this._isUTC && this._offset === 0 : !1;
}
var tx = /^(-|\+)?(?:(\d*)[. ])?(\d+):(\d+)(?::(\d+)(\.\d*)?)?$/, ox = /^(-|\+)?P(?:([-+]?[0-9,.]*)Y)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)W)?(?:([-+]?[0-9,.]*)D)?(?:T(?:([-+]?[0-9,.]*)H)?(?:([-+]?[0-9,.]*)M)?(?:([-+]?[0-9,.]*)S)?)?$/;
function On(o, e) {
  var t = o, n = null, r, a, c;
  return Ba(o) ? t = {
    ms: o._milliseconds,
    d: o._days,
    M: o._months
  } : Si(o) || !isNaN(+o) ? (t = {}, e ? t[e] = +o : t.milliseconds = +o) : (n = tx.exec(o)) ? (r = n[1] === "-" ? -1 : 1, t = {
    y: 0,
    d: ot(n[Hn]) * r,
    h: ot(n[to]) * r,
    m: ot(n[Cn]) * r,
    s: ot(n[vi]) * r,
    ms: ot(ic(n[Dr] * 1e3)) * r
    // the millisecond decimal point is included in the match
  }) : (n = ox.exec(o)) ? (r = n[1] === "-" ? -1 : 1, t = {
    y: _r(n[2], r),
    M: _r(n[3], r),
    w: _r(n[4], r),
    d: _r(n[5], r),
    h: _r(n[6], r),
    m: _r(n[7], r),
    s: _r(n[8], r)
  }) : t == null ? t = {} : typeof t == "object" && ("from" in t || "to" in t) && (c = nx(
    Ot(t.from),
    Ot(t.to)
  ), t = {}, t.ms = c.milliseconds, t.M = c.months), a = new fl(t), Ba(o) && dt(o, "_locale") && (a._locale = o._locale), Ba(o) && dt(o, "_isValid") && (a._isValid = o._isValid), a;
}
On.fn = fl.prototype;
On.invalid = Fk;
function _r(o, e) {
  var t = o && parseFloat(o.replace(",", "."));
  return (isNaN(t) ? 0 : t) * e;
}
function fh(o, e) {
  var t = {};
  return t.months = e.month() - o.month() + (e.year() - o.year()) * 12, o.clone().add(t.months, "M").isAfter(e) && --t.months, t.milliseconds = +e - +o.clone().add(t.months, "M"), t;
}
function nx(o, e) {
  var t;
  return o.isValid() && e.isValid() ? (e = rd(e, o), o.isBefore(e) ? t = fh(o, e) : (t = fh(e, o), t.milliseconds = -t.milliseconds, t.months = -t.months), t) : { milliseconds: 0, months: 0 };
}
function Np(o, e) {
  return function(t, n) {
    var r, a;
    return n !== null && !isNaN(+n) && (hp(
      e,
      "moment()." + e + "(period, number) is deprecated. Please use moment()." + e + "(number, period). See http://momentjs.com/guides/#/warnings/add-inverted-param/ for more info."
    ), a = t, t = n, n = a), r = On(t, n), Pp(this, r, o), this;
  };
}
function Pp(o, e, t, n) {
  var r = e._milliseconds, a = ic(e._days), c = ic(e._months);
  o.isValid() && (n = n ?? !0, c && kp(o, la(o, "Month") + c * t), a && vp(o, "Date", la(o, "Date") + a * t), r && o._d.setTime(o._d.valueOf() + r * t), n && pe.updateOffset(o, a || c));
}
var ix = Np(1, "add"), rx = Np(-1, "subtract");
function Hp(o) {
  return typeof o == "string" || o instanceof String;
}
function sx(o) {
  return Mn(o) || ba(o) || Hp(o) || Si(o) || lx(o) || ax(o) || o === null || o === void 0;
}
function ax(o) {
  var e = Br(o) && !Yc(o), t = !1, n = [
    "years",
    "year",
    "y",
    "months",
    "month",
    "M",
    "days",
    "day",
    "d",
    "dates",
    "date",
    "D",
    "hours",
    "hour",
    "h",
    "minutes",
    "minute",
    "m",
    "seconds",
    "second",
    "s",
    "milliseconds",
    "millisecond",
    "ms"
  ], r, a, c = n.length;
  for (r = 0; r < c; r += 1)
    a = n[r], t = t || dt(o, a);
  return e && t;
}
function lx(o) {
  var e = En(o), t = !1;
  return e && (t = o.filter(function(n) {
    return !Si(n) && Hp(o);
  }).length === 0), e && t;
}
function cx(o) {
  var e = Br(o) && !Yc(o), t = !1, n = [
    "sameDay",
    "nextDay",
    "lastDay",
    "nextWeek",
    "lastWeek",
    "sameElse"
  ], r, a;
  for (r = 0; r < n.length; r += 1)
    a = n[r], t = t || dt(o, a);
  return e && t;
}
function dx(o, e) {
  var t = o.diff(e, "days", !0);
  return t < -6 ? "sameElse" : t < -1 ? "lastWeek" : t < 0 ? "lastDay" : t < 1 ? "sameDay" : t < 2 ? "nextDay" : t < 7 ? "nextWeek" : "sameElse";
}
function hx(o, e) {
  arguments.length === 1 && (arguments[0] ? sx(arguments[0]) ? (o = arguments[0], e = void 0) : cx(arguments[0]) && (e = arguments[0], o = void 0) : (o = void 0, e = void 0));
  var t = o || Ot(), n = rd(t, this).startOf("day"), r = pe.calendarFormat(this, n) || "sameElse", a = e && (Wn(e[r]) ? e[r].call(this, t) : e[r]);
  return this.format(
    a || this.localeData().calendar(r, this, Ot(t))
  );
}
function ux() {
  return new va(this);
}
function px(o, e) {
  var t = Mn(o) ? o : Ot(o);
  return this.isValid() && t.isValid() ? (e = dn(e) || "millisecond", e === "millisecond" ? this.valueOf() > t.valueOf() : t.valueOf() < this.clone().startOf(e).valueOf()) : !1;
}
function fx(o, e) {
  var t = Mn(o) ? o : Ot(o);
  return this.isValid() && t.isValid() ? (e = dn(e) || "millisecond", e === "millisecond" ? this.valueOf() < t.valueOf() : this.clone().endOf(e).valueOf() < t.valueOf()) : !1;
}
function mx(o, e, t, n) {
  var r = Mn(o) ? o : Ot(o), a = Mn(e) ? e : Ot(e);
  return this.isValid() && r.isValid() && a.isValid() ? (n = n || "()", (n[0] === "(" ? this.isAfter(r, t) : !this.isBefore(r, t)) && (n[1] === ")" ? this.isBefore(a, t) : !this.isAfter(a, t))) : !1;
}
function gx(o, e) {
  var t = Mn(o) ? o : Ot(o), n;
  return this.isValid() && t.isValid() ? (e = dn(e) || "millisecond", e === "millisecond" ? this.valueOf() === t.valueOf() : (n = t.valueOf(), this.clone().startOf(e).valueOf() <= n && n <= this.clone().endOf(e).valueOf())) : !1;
}
function bx(o, e) {
  return this.isSame(o, e) || this.isAfter(o, e);
}
function vx(o, e) {
  return this.isSame(o, e) || this.isBefore(o, e);
}
function yx(o, e, t) {
  var n, r, a;
  if (!this.isValid())
    return NaN;
  if (n = rd(o, this), !n.isValid())
    return NaN;
  switch (r = (n.utcOffset() - this.utcOffset()) * 6e4, e = dn(e), e) {
    case "year":
      a = Aa(this, n) / 12;
      break;
    case "month":
      a = Aa(this, n);
      break;
    case "quarter":
      a = Aa(this, n) / 3;
      break;
    case "second":
      a = (this - n) / 1e3;
      break;
    case "minute":
      a = (this - n) / 6e4;
      break;
    case "hour":
      a = (this - n) / 36e5;
      break;
    case "day":
      a = (this - n - r) / 864e5;
      break;
    case "week":
      a = (this - n - r) / 6048e5;
      break;
    default:
      a = this - n;
  }
  return t ? a : rn(a);
}
function Aa(o, e) {
  if (o.date() < e.date())
    return -Aa(e, o);
  var t = (e.year() - o.year()) * 12 + (e.month() - o.month()), n = o.clone().add(t, "months"), r, a;
  return e - n < 0 ? (r = o.clone().add(t - 1, "months"), a = (e - n) / (n - r)) : (r = o.clone().add(t + 1, "months"), a = (e - n) / (r - n)), -(t + a) || 0;
}
pe.defaultFormat = "YYYY-MM-DDTHH:mm:ssZ";
pe.defaultFormatUtc = "YYYY-MM-DDTHH:mm:ss[Z]";
function wx() {
  return this.clone().locale("en").format("ddd MMM DD YYYY HH:mm:ss [GMT]ZZ");
}
function kx(o) {
  if (!this.isValid())
    return null;
  var e = o !== !0, t = e ? this.clone().utc() : this;
  return t.year() < 0 || t.year() > 9999 ? Oa(
    t,
    e ? "YYYYYY-MM-DD[T]HH:mm:ss.SSS[Z]" : "YYYYYY-MM-DD[T]HH:mm:ss.SSSZ"
  ) : Wn(Date.prototype.toISOString) ? e ? this.toDate().toISOString() : new Date(this.valueOf() + this.utcOffset() * 60 * 1e3).toISOString().replace("Z", Oa(t, "Z")) : Oa(
    t,
    e ? "YYYY-MM-DD[T]HH:mm:ss.SSS[Z]" : "YYYY-MM-DD[T]HH:mm:ss.SSSZ"
  );
}
function xx() {
  if (!this.isValid())
    return "moment.invalid(/* " + this._i + " */)";
  var o = "moment", e = "", t, n, r, a;
  return this.isLocal() || (o = this.utcOffset() === 0 ? "moment.utc" : "moment.parseZone", e = "Z"), t = "[" + o + '("]', n = 0 <= this.year() && this.year() <= 9999 ? "YYYY" : "YYYYYY", r = "-MM-DD[T]HH:mm:ss.SSS", a = e + '[")]', this.format(t + n + r + a);
}
function Cx(o) {
  o || (o = this.isUtc() ? pe.defaultFormatUtc : pe.defaultFormat);
  var e = Oa(this, o);
  return this.localeData().postformat(e);
}
function Sx(o, e) {
  return this.isValid() && (Mn(o) && o.isValid() || Ot(o).isValid()) ? On({ to: this, from: o }).locale(this.locale()).humanize(!e) : this.localeData().invalidDate();
}
function Tx(o) {
  return this.from(Ot(), o);
}
function _x(o, e) {
  return this.isValid() && (Mn(o) && o.isValid() || Ot(o).isValid()) ? On({ from: this, to: o }).locale(this.locale()).humanize(!e) : this.localeData().invalidDate();
}
function Ex(o) {
  return this.to(Ot(), o);
}
function jp(o) {
  var e;
  return o === void 0 ? this._locale._abbr : (e = Ei(o), e != null && (this._locale = e), this);
}
var $p = cn(
  "moment().lang() is deprecated. Instead, use moment().localeData() to get the language configuration. Use moment().locale() to change languages.",
  function(o) {
    return o === void 0 ? this.localeData() : this.locale(o);
  }
);
function Yp() {
  return this._locale;
}
var Fa = 1e3, us = 60 * Fa, za = 60 * us, Fp = (365 * 400 + 97) * 24 * za;
function ps(o, e) {
  return (o % e + e) % e;
}
function zp(o, e, t) {
  return o < 100 && o >= 0 ? new Date(o + 400, e, t) - Fp : new Date(o, e, t).valueOf();
}
function Wp(o, e, t) {
  return o < 100 && o >= 0 ? Date.UTC(o + 400, e, t) - Fp : Date.UTC(o, e, t);
}
function Mx(o) {
  var e, t;
  if (o = dn(o), o === void 0 || o === "millisecond" || !this.isValid())
    return this;
  switch (t = this._isUTC ? Wp : zp, o) {
    case "year":
      e = t(this.year(), 0, 1);
      break;
    case "quarter":
      e = t(
        this.year(),
        this.month() - this.month() % 3,
        1
      );
      break;
    case "month":
      e = t(this.year(), this.month(), 1);
      break;
    case "week":
      e = t(
        this.year(),
        this.month(),
        this.date() - this.weekday()
      );
      break;
    case "isoWeek":
      e = t(
        this.year(),
        this.month(),
        this.date() - (this.isoWeekday() - 1)
      );
      break;
    case "day":
    case "date":
      e = t(this.year(), this.month(), this.date());
      break;
    case "hour":
      e = this._d.valueOf(), e -= ps(
        e + (this._isUTC ? 0 : this.utcOffset() * us),
        za
      );
      break;
    case "minute":
      e = this._d.valueOf(), e -= ps(e, us);
      break;
    case "second":
      e = this._d.valueOf(), e -= ps(e, Fa);
      break;
  }
  return this._d.setTime(e), pe.updateOffset(this, !0), this;
}
function Dx(o) {
  var e, t;
  if (o = dn(o), o === void 0 || o === "millisecond" || !this.isValid())
    return this;
  switch (t = this._isUTC ? Wp : zp, o) {
    case "year":
      e = t(this.year() + 1, 0, 1) - 1;
      break;
    case "quarter":
      e = t(
        this.year(),
        this.month() - this.month() % 3 + 3,
        1
      ) - 1;
      break;
    case "month":
      e = t(this.year(), this.month() + 1, 1) - 1;
      break;
    case "week":
      e = t(
        this.year(),
        this.month(),
        this.date() - this.weekday() + 7
      ) - 1;
      break;
    case "isoWeek":
      e = t(
        this.year(),
        this.month(),
        this.date() - (this.isoWeekday() - 1) + 7
      ) - 1;
      break;
    case "day":
    case "date":
      e = t(this.year(), this.month(), this.date() + 1) - 1;
      break;
    case "hour":
      e = this._d.valueOf(), e += za - ps(
        e + (this._isUTC ? 0 : this.utcOffset() * us),
        za
      ) - 1;
      break;
    case "minute":
      e = this._d.valueOf(), e += us - ps(e, us) - 1;
      break;
    case "second":
      e = this._d.valueOf(), e += Fa - ps(e, Fa) - 1;
      break;
  }
  return this._d.setTime(e), pe.updateOffset(this, !0), this;
}
function Lx() {
  return this._d.valueOf() - (this._offset || 0) * 6e4;
}
function Ox() {
  return Math.floor(this.valueOf() / 1e3);
}
function Bx() {
  return new Date(this.valueOf());
}
function Ax() {
  var o = this;
  return [
    o.year(),
    o.month(),
    o.date(),
    o.hour(),
    o.minute(),
    o.second(),
    o.millisecond()
  ];
}
function Rx() {
  var o = this;
  return {
    years: o.year(),
    months: o.month(),
    date: o.date(),
    hours: o.hours(),
    minutes: o.minutes(),
    seconds: o.seconds(),
    milliseconds: o.milliseconds()
  };
}
function Ix() {
  return this.isValid() ? this.toISOString() : null;
}
function Nx() {
  return Fc(this);
}
function Px() {
  return Zi({}, Ze(this));
}
function Hx() {
  return Ze(this).overflow;
}
function jx() {
  return {
    input: this._i,
    format: this._f,
    locale: this._locale,
    isUTC: this._isUTC,
    strict: this._strict
  };
}
He("N", 0, 0, "eraAbbr");
He("NN", 0, 0, "eraAbbr");
He("NNN", 0, 0, "eraAbbr");
He("NNNN", 0, 0, "eraName");
He("NNNNN", 0, 0, "eraNarrow");
He("y", ["y", 1], "yo", "eraYear");
He("y", ["yy", 2], 0, "eraYear");
He("y", ["yyy", 3], 0, "eraYear");
He("y", ["yyyy", 4], 0, "eraYear");
Oe("N", sd);
Oe("NN", sd);
Oe("NNN", sd);
Oe("NNNN", Kx);
Oe("NNNNN", Xx);
xt(
  ["N", "NN", "NNN", "NNNN", "NNNNN"],
  function(o, e, t, n) {
    var r = t._locale.erasParse(o, n, t._strict);
    r ? Ze(t).era = r : Ze(t).invalidEra = o;
  }
);
Oe("y", ks);
Oe("yy", ks);
Oe("yyy", ks);
Oe("yyyy", ks);
Oe("yo", Jx);
xt(["y", "yy", "yyy", "yyyy"], vo);
xt(["yo"], function(o, e, t, n) {
  var r;
  t._locale._eraYearOrdinalRegex && (r = o.match(t._locale._eraYearOrdinalRegex)), t._locale.eraYearOrdinalParse ? e[vo] = t._locale.eraYearOrdinalParse(o, r) : e[vo] = parseInt(o, 10);
});
function $x(o, e) {
  var t, n, r, a = this._eras || Ei("en")._eras;
  for (t = 0, n = a.length; t < n; ++t) {
    switch (typeof a[t].since) {
      case "string":
        r = pe(a[t].since).startOf("day"), a[t].since = r.valueOf();
        break;
    }
    switch (typeof a[t].until) {
      case "undefined":
        a[t].until = 1 / 0;
        break;
      case "string":
        r = pe(a[t].until).startOf("day").valueOf(), a[t].until = r.valueOf();
        break;
    }
  }
  return a;
}
function Yx(o, e, t) {
  var n, r, a = this.eras(), c, u, p;
  for (o = o.toUpperCase(), n = 0, r = a.length; n < r; ++n)
    if (c = a[n].name.toUpperCase(), u = a[n].abbr.toUpperCase(), p = a[n].narrow.toUpperCase(), t)
      switch (e) {
        case "N":
        case "NN":
        case "NNN":
          if (u === o)
            return a[n];
          break;
        case "NNNN":
          if (c === o)
            return a[n];
          break;
        case "NNNNN":
          if (p === o)
            return a[n];
          break;
      }
    else if ([c, u, p].indexOf(o) >= 0)
      return a[n];
}
function Fx(o, e) {
  var t = o.since <= o.until ? 1 : -1;
  return e === void 0 ? pe(o.since).year() : pe(o.since).year() + (e - o.offset) * t;
}
function zx() {
  var o, e, t, n = this.localeData().eras();
  for (o = 0, e = n.length; o < e; ++o)
    if (t = this.clone().startOf("day").valueOf(), n[o].since <= t && t <= n[o].until || n[o].until <= t && t <= n[o].since)
      return n[o].name;
  return "";
}
function Wx() {
  var o, e, t, n = this.localeData().eras();
  for (o = 0, e = n.length; o < e; ++o)
    if (t = this.clone().startOf("day").valueOf(), n[o].since <= t && t <= n[o].until || n[o].until <= t && t <= n[o].since)
      return n[o].narrow;
  return "";
}
function Ux() {
  var o, e, t, n = this.localeData().eras();
  for (o = 0, e = n.length; o < e; ++o)
    if (t = this.clone().startOf("day").valueOf(), n[o].since <= t && t <= n[o].until || n[o].until <= t && t <= n[o].since)
      return n[o].abbr;
  return "";
}
function qx() {
  var o, e, t, n, r = this.localeData().eras();
  for (o = 0, e = r.length; o < e; ++o)
    if (t = r[o].since <= r[o].until ? 1 : -1, n = this.clone().startOf("day").valueOf(), r[o].since <= n && n <= r[o].until || r[o].until <= n && n <= r[o].since)
      return (this.year() - pe(r[o].since).year()) * t + r[o].offset;
  return this.year();
}
function Vx(o) {
  return dt(this, "_erasNameRegex") || ad.call(this), o ? this._erasNameRegex : this._erasRegex;
}
function Gx(o) {
  return dt(this, "_erasAbbrRegex") || ad.call(this), o ? this._erasAbbrRegex : this._erasRegex;
}
function Zx(o) {
  return dt(this, "_erasNarrowRegex") || ad.call(this), o ? this._erasNarrowRegex : this._erasRegex;
}
function sd(o, e) {
  return e.erasAbbrRegex(o);
}
function Kx(o, e) {
  return e.erasNameRegex(o);
}
function Xx(o, e) {
  return e.erasNarrowRegex(o);
}
function Jx(o, e) {
  return e._eraYearOrdinalRegex || ks;
}
function ad() {
  var o = [], e = [], t = [], n = [], r, a, c, u, p, g = this.eras();
  for (r = 0, a = g.length; r < a; ++r)
    c = ki(g[r].name), u = ki(g[r].abbr), p = ki(g[r].narrow), e.push(c), o.push(u), t.push(p), n.push(c), n.push(u), n.push(p);
  this._erasRegex = new RegExp("^(" + n.join("|") + ")", "i"), this._erasNameRegex = new RegExp("^(" + e.join("|") + ")", "i"), this._erasAbbrRegex = new RegExp("^(" + o.join("|") + ")", "i"), this._erasNarrowRegex = new RegExp(
    "^(" + t.join("|") + ")",
    "i"
  );
}
He(0, ["gg", 2], 0, function() {
  return this.weekYear() % 100;
});
He(0, ["GG", 2], 0, function() {
  return this.isoWeekYear() % 100;
});
function ml(o, e) {
  He(0, [o, o.length], 0, e);
}
ml("gggg", "weekYear");
ml("ggggg", "weekYear");
ml("GGGG", "isoWeekYear");
ml("GGGGG", "isoWeekYear");
Oe("G", dl);
Oe("g", dl);
Oe("GG", At, Jo);
Oe("gg", At, Jo);
Oe("GGGG", Gc, Vc);
Oe("gggg", Gc, Vc);
Oe("GGGGG", cl, al);
Oe("ggggg", cl, al);
wa(
  ["gggg", "ggggg", "GGGG", "GGGGG"],
  function(o, e, t, n) {
    e[n.substr(0, 2)] = ot(o);
  }
);
wa(["gg", "GG"], function(o, e, t, n) {
  e[n] = pe.parseTwoDigitYear(o);
});
function Qx(o) {
  return Up.call(
    this,
    o,
    this.week(),
    this.weekday() + this.localeData()._week.dow,
    this.localeData()._week.dow,
    this.localeData()._week.doy
  );
}
function e2(o) {
  return Up.call(
    this,
    o,
    this.isoWeek(),
    this.isoWeekday(),
    1,
    4
  );
}
function t2() {
  return xi(this.year(), 1, 4);
}
function o2() {
  return xi(this.isoWeekYear(), 1, 4);
}
function n2() {
  var o = this.localeData()._week;
  return xi(this.year(), o.dow, o.doy);
}
function i2() {
  var o = this.localeData()._week;
  return xi(this.weekYear(), o.dow, o.doy);
}
function Up(o, e, t, n, r) {
  var a;
  return o == null ? da(this, n, r).year : (a = xi(o, n, r), e > a && (e = a), r2.call(this, o, e, t, n, r));
}
function r2(o, e, t, n, r) {
  var a = Sp(o, e, t, n, r), c = ca(a.year, 0, a.dayOfYear);
  return this.year(c.getUTCFullYear()), this.month(c.getUTCMonth()), this.date(c.getUTCDate()), this;
}
He("Q", 0, "Qo", "quarter");
Oe("Q", pp);
xt("Q", function(o, e) {
  e[bi] = (ot(o) - 1) * 3;
});
function s2(o) {
  return o == null ? Math.ceil((this.month() + 1) / 3) : this.month((o - 1) * 3 + this.month() % 3);
}
He("D", ["DD", 2], "Do", "date");
Oe("D", At, xs);
Oe("DD", At, Jo);
Oe("Do", function(o, e) {
  return o ? e._dayOfMonthOrdinalParse || e._ordinalParse : e._dayOfMonthOrdinalParseLenient;
});
xt(["D", "DD"], Hn);
xt("Do", function(o, e) {
  e[Hn] = ot(o.match(At)[0]);
});
var qp = Cs("Date", !0);
He("DDD", ["DDDD", 3], "DDDo", "dayOfYear");
Oe("DDD", ll);
Oe("DDDD", fp);
xt(["DDD", "DDDD"], function(o, e, t) {
  t._dayOfYear = ot(o);
});
function a2(o) {
  var e = Math.round(
    (this.clone().startOf("day") - this.clone().startOf("year")) / 864e5
  ) + 1;
  return o == null ? e : this.add(o - e, "d");
}
He("m", ["mm", 2], 0, "minute");
Oe("m", At, Zc);
Oe("mm", At, Jo);
xt(["m", "mm"], Cn);
var l2 = Cs("Minutes", !1);
He("s", ["ss", 2], 0, "second");
Oe("s", At, Zc);
Oe("ss", At, Jo);
xt(["s", "ss"], vi);
var c2 = Cs("Seconds", !1);
He("S", 0, 0, function() {
  return ~~(this.millisecond() / 100);
});
He(0, ["SS", 2], 0, function() {
  return ~~(this.millisecond() / 10);
});
He(0, ["SSS", 3], 0, "millisecond");
He(0, ["SSSS", 4], 0, function() {
  return this.millisecond() * 10;
});
He(0, ["SSSSS", 5], 0, function() {
  return this.millisecond() * 100;
});
He(0, ["SSSSSS", 6], 0, function() {
  return this.millisecond() * 1e3;
});
He(0, ["SSSSSSS", 7], 0, function() {
  return this.millisecond() * 1e4;
});
He(0, ["SSSSSSSS", 8], 0, function() {
  return this.millisecond() * 1e5;
});
He(0, ["SSSSSSSSS", 9], 0, function() {
  return this.millisecond() * 1e6;
});
Oe("S", ll, pp);
Oe("SS", ll, Jo);
Oe("SSS", ll, fp);
var Ki, Vp;
for (Ki = "SSSS"; Ki.length <= 9; Ki += "S")
  Oe(Ki, ks);
function d2(o, e) {
  e[Dr] = ot(("0." + o) * 1e3);
}
for (Ki = "S"; Ki.length <= 9; Ki += "S")
  xt(Ki, d2);
Vp = Cs("Milliseconds", !1);
He("z", 0, 0, "zoneAbbr");
He("zz", 0, 0, "zoneName");
function h2() {
  return this._isUTC ? "UTC" : "";
}
function u2() {
  return this._isUTC ? "Coordinated Universal Time" : "";
}
var ye = va.prototype;
ye.add = ix;
ye.calendar = hx;
ye.clone = ux;
ye.diff = yx;
ye.endOf = Dx;
ye.format = Cx;
ye.from = Sx;
ye.fromNow = Tx;
ye.to = _x;
ye.toNow = Ex;
ye.get = xw;
ye.invalidAt = Hx;
ye.isAfter = px;
ye.isBefore = fx;
ye.isBetween = mx;
ye.isSame = gx;
ye.isSameOrAfter = bx;
ye.isSameOrBefore = vx;
ye.isValid = Nx;
ye.lang = $p;
ye.locale = jp;
ye.localeData = Yp;
ye.max = Nk;
ye.min = Ik;
ye.parsingFlags = Px;
ye.set = Cw;
ye.startOf = Mx;
ye.subtract = rx;
ye.toArray = Ax;
ye.toObject = Rx;
ye.toDate = Bx;
ye.toISOString = kx;
ye.inspect = xx;
typeof Symbol < "u" && Symbol.for != null && (ye[Symbol.for("nodejs.util.inspect.custom")] = function() {
  return "Moment<" + this.format() + ">";
});
ye.toJSON = Ix;
ye.toString = wx;
ye.unix = Ox;
ye.valueOf = Lx;
ye.creationData = jx;
ye.eraName = zx;
ye.eraNarrow = Wx;
ye.eraAbbr = Ux;
ye.eraYear = qx;
ye.year = bp;
ye.isLeapYear = kw;
ye.weekYear = Qx;
ye.isoWeekYear = e2;
ye.quarter = ye.quarters = s2;
ye.month = xp;
ye.daysInMonth = Bw;
ye.week = ye.weeks = $w;
ye.isoWeek = ye.isoWeeks = Yw;
ye.weeksInYear = n2;
ye.weeksInWeekYear = i2;
ye.isoWeeksInYear = t2;
ye.isoWeeksInISOWeekYear = o2;
ye.date = qp;
ye.day = ye.days = ek;
ye.weekday = tk;
ye.isoWeekday = ok;
ye.dayOfYear = a2;
ye.hour = ye.hours = ck;
ye.minute = ye.minutes = l2;
ye.second = ye.seconds = c2;
ye.millisecond = ye.milliseconds = Vp;
ye.utcOffset = Uk;
ye.utc = Vk;
ye.local = Gk;
ye.parseZone = Zk;
ye.hasAlignedHourOffset = Kk;
ye.isDST = Xk;
ye.isLocal = Qk;
ye.isUtcOffset = ex;
ye.isUtc = Ip;
ye.isUTC = Ip;
ye.zoneAbbr = h2;
ye.zoneName = u2;
ye.dates = cn(
  "dates accessor is deprecated. Use date instead.",
  qp
);
ye.months = cn(
  "months accessor is deprecated. Use month instead",
  xp
);
ye.years = cn(
  "years accessor is deprecated. Use year instead",
  bp
);
ye.zone = cn(
  "moment().zone is deprecated, use moment().utcOffset instead. http://momentjs.com/guides/#/warnings/zone/",
  qk
);
ye.isDSTShifted = cn(
  "isDSTShifted is deprecated. See http://momentjs.com/guides/#/warnings/dst-shifted/ for more information",
  Jk
);
function p2(o) {
  return Ot(o * 1e3);
}
function f2() {
  return Ot.apply(null, arguments).parseZone();
}
function Gp(o) {
  return o;
}
var ut = Wc.prototype;
ut.calendar = Qy;
ut.longDateFormat = nw;
ut.invalidDate = rw;
ut.ordinal = lw;
ut.preparse = Gp;
ut.postformat = Gp;
ut.relativeTime = dw;
ut.pastFuture = hw;
ut.set = Xy;
ut.eras = $x;
ut.erasParse = Yx;
ut.erasConvertYear = Fx;
ut.erasAbbrRegex = Gx;
ut.erasNameRegex = Vx;
ut.erasNarrowRegex = Zx;
ut.months = Mw;
ut.monthsShort = Dw;
ut.monthsParse = Ow;
ut.monthsRegex = Rw;
ut.monthsShortRegex = Aw;
ut.week = Nw;
ut.firstDayOfYear = jw;
ut.firstDayOfWeek = Hw;
ut.weekdays = Zw;
ut.weekdaysMin = Xw;
ut.weekdaysShort = Kw;
ut.weekdaysParse = Qw;
ut.weekdaysRegex = nk;
ut.weekdaysShortRegex = ik;
ut.weekdaysMinRegex = rk;
ut.isPM = ak;
ut.meridiem = dk;
function Wa(o, e, t, n) {
  var r = Ei(), a = zn().set(n, e);
  return r[t](a, o);
}
function Zp(o, e, t) {
  if (Si(o) && (e = o, o = void 0), o = o || "", e != null)
    return Wa(o, e, t, "month");
  var n, r = [];
  for (n = 0; n < 12; n++)
    r[n] = Wa(o, n, t, "month");
  return r;
}
function ld(o, e, t, n) {
  typeof o == "boolean" ? (Si(e) && (t = e, e = void 0), e = e || "") : (e = o, t = e, o = !1, Si(e) && (t = e, e = void 0), e = e || "");
  var r = Ei(), a = o ? r._week.dow : 0, c, u = [];
  if (t != null)
    return Wa(e, (t + a) % 7, n, "day");
  for (c = 0; c < 7; c++)
    u[c] = Wa(e, (c + a) % 7, n, "day");
  return u;
}
function m2(o, e) {
  return Zp(o, e, "months");
}
function g2(o, e) {
  return Zp(o, e, "monthsShort");
}
function b2(o, e, t) {
  return ld(o, e, t, "weekdays");
}
function v2(o, e, t) {
  return ld(o, e, t, "weekdaysShort");
}
function y2(o, e, t) {
  return ld(o, e, t, "weekdaysMin");
}
Ji("en", {
  eras: [
    {
      since: "0001-01-01",
      until: 1 / 0,
      offset: 1,
      name: "Anno Domini",
      narrow: "AD",
      abbr: "AD"
    },
    {
      since: "0000-12-31",
      until: -1 / 0,
      offset: 1,
      name: "Before Christ",
      narrow: "BC",
      abbr: "BC"
    }
  ],
  dayOfMonthOrdinalParse: /\d{1,2}(th|st|nd|rd)/,
  ordinal: function(o) {
    var e = o % 10, t = ot(o % 100 / 10) === 1 ? "th" : e === 1 ? "st" : e === 2 ? "nd" : e === 3 ? "rd" : "th";
    return o + t;
  }
});
pe.lang = cn(
  "moment.lang is deprecated. Use moment.locale instead.",
  Ji
);
pe.langData = cn(
  "moment.langData is deprecated. Use moment.localeData instead.",
  Ei
);
var ci = Math.abs;
function w2() {
  var o = this._data;
  return this._milliseconds = ci(this._milliseconds), this._days = ci(this._days), this._months = ci(this._months), o.milliseconds = ci(o.milliseconds), o.seconds = ci(o.seconds), o.minutes = ci(o.minutes), o.hours = ci(o.hours), o.months = ci(o.months), o.years = ci(o.years), this;
}
function Kp(o, e, t, n) {
  var r = On(e, t);
  return o._milliseconds += n * r._milliseconds, o._days += n * r._days, o._months += n * r._months, o._bubble();
}
function k2(o, e) {
  return Kp(this, o, e, 1);
}
function x2(o, e) {
  return Kp(this, o, e, -1);
}
function mh(o) {
  return o < 0 ? Math.floor(o) : Math.ceil(o);
}
function C2() {
  var o = this._milliseconds, e = this._days, t = this._months, n = this._data, r, a, c, u, p;
  return o >= 0 && e >= 0 && t >= 0 || o <= 0 && e <= 0 && t <= 0 || (o += mh(sc(t) + e) * 864e5, e = 0, t = 0), n.milliseconds = o % 1e3, r = rn(o / 1e3), n.seconds = r % 60, a = rn(r / 60), n.minutes = a % 60, c = rn(a / 60), n.hours = c % 24, e += rn(c / 24), p = rn(Xp(e)), t += p, e -= mh(sc(p)), u = rn(t / 12), t %= 12, n.days = e, n.months = t, n.years = u, this;
}
function Xp(o) {
  return o * 4800 / 146097;
}
function sc(o) {
  return o * 146097 / 4800;
}
function S2(o) {
  if (!this.isValid())
    return NaN;
  var e, t, n = this._milliseconds;
  if (o = dn(o), o === "month" || o === "quarter" || o === "year")
    switch (e = this._days + n / 864e5, t = this._months + Xp(e), o) {
      case "month":
        return t;
      case "quarter":
        return t / 3;
      case "year":
        return t / 12;
    }
  else
    switch (e = this._days + Math.round(sc(this._months)), o) {
      case "week":
        return e / 7 + n / 6048e5;
      case "day":
        return e + n / 864e5;
      case "hour":
        return e * 24 + n / 36e5;
      case "minute":
        return e * 1440 + n / 6e4;
      case "second":
        return e * 86400 + n / 1e3;
      case "millisecond":
        return Math.floor(e * 864e5) + n;
      default:
        throw new Error("Unknown unit " + o);
    }
}
function Mi(o) {
  return function() {
    return this.as(o);
  };
}
var Jp = Mi("ms"), T2 = Mi("s"), _2 = Mi("m"), E2 = Mi("h"), M2 = Mi("d"), D2 = Mi("w"), L2 = Mi("M"), O2 = Mi("Q"), B2 = Mi("y"), A2 = Jp;
function R2() {
  return On(this);
}
function I2(o) {
  return o = dn(o), this.isValid() ? this[o + "s"]() : NaN;
}
function Ir(o) {
  return function() {
    return this.isValid() ? this._data[o] : NaN;
  };
}
var N2 = Ir("milliseconds"), P2 = Ir("seconds"), H2 = Ir("minutes"), j2 = Ir("hours"), $2 = Ir("days"), Y2 = Ir("months"), F2 = Ir("years");
function z2() {
  return rn(this.days() / 7);
}
var hi = Math.round, as = {
  ss: 44,
  // a few seconds to seconds
  s: 45,
  // seconds to minute
  m: 45,
  // minutes to hour
  h: 22,
  // hours to day
  d: 26,
  // days to month/week
  w: null,
  // weeks to month
  M: 11
  // months to year
};
function W2(o, e, t, n, r) {
  return r.relativeTime(e || 1, !!t, o, n);
}
function U2(o, e, t, n) {
  var r = On(o).abs(), a = hi(r.as("s")), c = hi(r.as("m")), u = hi(r.as("h")), p = hi(r.as("d")), g = hi(r.as("M")), v = hi(r.as("w")), k = hi(r.as("y")), y = a <= t.ss && ["s", a] || a < t.s && ["ss", a] || c <= 1 && ["m"] || c < t.m && ["mm", c] || u <= 1 && ["h"] || u < t.h && ["hh", u] || p <= 1 && ["d"] || p < t.d && ["dd", p];
  return t.w != null && (y = y || v <= 1 && ["w"] || v < t.w && ["ww", v]), y = y || g <= 1 && ["M"] || g < t.M && ["MM", g] || k <= 1 && ["y"] || ["yy", k], y[2] = e, y[3] = +o > 0, y[4] = n, W2.apply(null, y);
}
function q2(o) {
  return o === void 0 ? hi : typeof o == "function" ? (hi = o, !0) : !1;
}
function V2(o, e) {
  return as[o] === void 0 ? !1 : e === void 0 ? as[o] : (as[o] = e, o === "s" && (as.ss = e - 1), !0);
}
function G2(o, e) {
  if (!this.isValid())
    return this.localeData().invalidDate();
  var t = !1, n = as, r, a;
  return typeof o == "object" && (e = o, o = !1), typeof o == "boolean" && (t = o), typeof e == "object" && (n = Object.assign({}, as, e), e.s != null && e.ss == null && (n.ss = e.s - 1)), r = this.localeData(), a = U2(this, !t, n, r), t && (a = r.pastFuture(+this, a)), r.postformat(a);
}
var Il = Math.abs;
function Xr(o) {
  return (o > 0) - (o < 0) || +o;
}
function gl() {
  if (!this.isValid())
    return this.localeData().invalidDate();
  var o = Il(this._milliseconds) / 1e3, e = Il(this._days), t = Il(this._months), n, r, a, c, u = this.asSeconds(), p, g, v, k;
  return u ? (n = rn(o / 60), r = rn(n / 60), o %= 60, n %= 60, a = rn(t / 12), t %= 12, c = o ? o.toFixed(3).replace(/\.?0+$/, "") : "", p = u < 0 ? "-" : "", g = Xr(this._months) !== Xr(u) ? "-" : "", v = Xr(this._days) !== Xr(u) ? "-" : "", k = Xr(this._milliseconds) !== Xr(u) ? "-" : "", p + "P" + (a ? g + a + "Y" : "") + (t ? g + t + "M" : "") + (e ? v + e + "D" : "") + (r || n || o ? "T" : "") + (r ? k + r + "H" : "") + (n ? k + n + "M" : "") + (o ? k + c + "S" : "")) : "P0D";
}
var at = fl.prototype;
at.isValid = Yk;
at.abs = w2;
at.add = k2;
at.subtract = x2;
at.as = S2;
at.asMilliseconds = Jp;
at.asSeconds = T2;
at.asMinutes = _2;
at.asHours = E2;
at.asDays = M2;
at.asWeeks = D2;
at.asMonths = L2;
at.asQuarters = O2;
at.asYears = B2;
at.valueOf = A2;
at._bubble = C2;
at.clone = R2;
at.get = I2;
at.milliseconds = N2;
at.seconds = P2;
at.minutes = H2;
at.hours = j2;
at.days = $2;
at.weeks = z2;
at.months = Y2;
at.years = F2;
at.humanize = G2;
at.toISOString = gl;
at.toString = gl;
at.toJSON = gl;
at.locale = jp;
at.localeData = Yp;
at.toIsoString = cn(
  "toIsoString() is deprecated. Please use toISOString() instead (notice the capitals)",
  gl
);
at.lang = $p;
He("X", 0, 0, "unix");
He("x", 0, 0, "valueOf");
Oe("x", dl);
Oe("X", mw);
xt("X", function(o, e, t) {
  t._d = new Date(parseFloat(o) * 1e3);
});
xt("x", function(o, e, t) {
  t._d = new Date(ot(o));
});
//! moment.js
pe.version = "2.30.1";
Zy(Ot);
pe.fn = ye;
pe.min = Pk;
pe.max = Hk;
pe.now = jk;
pe.utc = zn;
pe.unix = p2;
pe.months = m2;
pe.isDate = ba;
pe.locale = Ji;
pe.invalid = sl;
pe.duration = On;
pe.isMoment = Mn;
pe.weekdays = b2;
pe.parseZone = f2;
pe.localeData = Ei;
pe.isDuration = Ba;
pe.monthsShort = g2;
pe.weekdaysMin = y2;
pe.defineLocale = ed;
pe.updateLocale = fk;
pe.locales = mk;
pe.weekdaysShort = v2;
pe.normalizeUnits = dn;
pe.relativeTimeRounding = q2;
pe.relativeTimeThreshold = V2;
pe.calendarFormat = dx;
pe.prototype = ye;
pe.HTML5_FMT = {
  DATETIME_LOCAL: "YYYY-MM-DDTHH:mm",
  // <input type="datetime-local" />
  DATETIME_LOCAL_SECONDS: "YYYY-MM-DDTHH:mm:ss",
  // <input type="datetime-local" step="1" />
  DATETIME_LOCAL_MS: "YYYY-MM-DDTHH:mm:ss.SSS",
  // <input type="datetime-local" step="0.001" />
  DATE: "YYYY-MM-DD",
  // <input type="date" />
  TIME: "HH:mm",
  // <input type="time" />
  TIME_SECONDS: "HH:mm:ss",
  // <input type="time" step="1" />
  TIME_MS: "HH:mm:ss.SSS",
  // <input type="time" step="0.001" />
  WEEK: "GGGG-[W]WW",
  // <input type="week" />
  MONTH: "YYYY-MM"
  // <input type="month" />
};
function Mt(o, e, t = {}) {
  const n = document.createElement(o);
  Array.isArray(e) ? n.classList.add(...e) : e && n.classList.add(e);
  for (const r in t)
    Object.prototype.hasOwnProperty.call(t, r) && (n[r] = t[r]);
  return n;
}
function gh(o) {
  const e = o.getBoundingClientRect();
  return {
    y1: Math.floor(e.top + window.pageYOffset),
    x1: Math.floor(e.left + window.pageXOffset),
    x2: Math.floor(e.right + window.pageXOffset),
    y2: Math.floor(e.bottom + window.pageYOffset)
  };
}
function Nl(o, e) {
  const t = gh(o), n = gh(e);
  return {
    fromTopBorder: n.y1 - t.y1,
    fromLeftBorder: n.x1 - t.x1,
    fromRightBorder: t.x2 - n.x2,
    fromBottomBorder: t.y2 - n.y2
  };
}
function Z2(o, e) {
  const t = o.getBoundingClientRect(), { width: n, height: r, x: a, y: c } = t, { clientX: u, clientY: p } = e;
  return {
    width: n,
    height: r,
    x: u - a,
    y: p - c
  };
}
function bh(o, e) {
  return e.parentNode.insertBefore(o, e);
}
function vh(o, e = !0) {
  const t = document.createRange(), n = window.getSelection();
  t.selectNodeContents(o), t.collapse(e), n.removeAllRanges(), n.addRange(t);
}
class Io {
  /**
   * @param {object} options - constructor options
   * @param {PopoverItem[]} options.items - constructor options
   */
  constructor({ items: e }) {
    this.items = e, this.wrapper = void 0, this.itemEls = [];
  }
  /**
   * Set of CSS classnames used in popover
   *
   * @returns {object}
   */
  static get CSS() {
    return {
      popover: "tc-popover",
      popoverOpened: "tc-popover--opened",
      item: "tc-popover__item",
      itemHidden: "tc-popover__item--hidden",
      itemConfirmState: "tc-popover__item--confirm",
      itemIcon: "tc-popover__item-icon",
      itemLabel: "tc-popover__item-label"
    };
  }
  /**
   * Returns the popover element
   *
   * @returns {Element}
   */
  render() {
    return this.wrapper = Mt("div", Io.CSS.popover), this.items.forEach((e, t) => {
      const n = Mt("div", Io.CSS.item), r = Mt("div", Io.CSS.itemIcon, {
        innerHTML: e.icon
      }), a = Mt("div", Io.CSS.itemLabel, {
        textContent: e.label
      });
      n.dataset.index = t, n.appendChild(r), n.appendChild(a), this.wrapper.appendChild(n), this.itemEls.push(n);
    }), this.wrapper.addEventListener("click", (e) => {
      this.popoverClicked(e);
    }), this.wrapper;
  }
  /**
   * Popover wrapper click listener
   * Used to delegate clicks in items
   *
   * @returns {void}
   */
  popoverClicked(e) {
    const t = e.target.closest(`.${Io.CSS.item}`);
    if (!t)
      return;
    const n = t.dataset.index, r = this.items[n];
    if (r.confirmationRequired && !this.hasConfirmationState(t)) {
      this.setConfirmationState(t);
      return;
    }
    r.onClick();
  }
  /**
   * Enable the confirmation state on passed item
   *
   * @returns {void}
   */
  setConfirmationState(e) {
    e.classList.add(Io.CSS.itemConfirmState);
  }
  /**
   * Disable the confirmation state on passed item
   *
   * @returns {void}
   */
  clearConfirmationState(e) {
    e.classList.remove(Io.CSS.itemConfirmState);
  }
  /**
   * Check if passed item has the confirmation state
   *
   * @returns {boolean}
   */
  hasConfirmationState(e) {
    return e.classList.contains(Io.CSS.itemConfirmState);
  }
  /**
   * Return an opening state
   *
   * @returns {boolean}
   */
  get opened() {
    return this.wrapper.classList.contains(Io.CSS.popoverOpened);
  }
  /**
   * Opens the popover
   *
   * @returns {void}
   */
  open() {
    this.items.forEach((e, t) => {
      typeof e.hideIf == "function" && this.itemEls[t].classList.toggle(Io.CSS.itemHidden, e.hideIf());
    }), this.wrapper.classList.add(Io.CSS.popoverOpened);
  }
  /**
   * Closes the popover
   *
   * @returns {void}
   */
  close() {
    this.wrapper.classList.remove(Io.CSS.popoverOpened), this.itemEls.forEach((e) => {
      this.clearConfirmationState(e);
    });
  }
}
const yh = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M8 8L12 12M12 12L16 16M12 12L16 8M12 12L8 16"/></svg>', K2 = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 9.16666L18.2167 12.5M18.2167 12.5L14.8833 15.8333M18.2167 12.5H10.05C9.16594 12.5 8.31809 12.1488 7.69297 11.5237C7.06785 10.8986 6.71666 10.0507 6.71666 9.16666"/></svg>', X2 = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.9167 14.9167L11.5833 18.25M11.5833 18.25L8.25 14.9167M11.5833 18.25L11.5833 10.0833C11.5833 9.19928 11.9345 8.35143 12.5596 7.72631C13.1848 7.10119 14.0326 6.75 14.9167 6.75"/></svg>', J2 = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.8833 15.8333L18.2167 12.5M18.2167 12.5L14.8833 9.16667M18.2167 12.5L10.05 12.5C9.16595 12.5 8.31811 12.8512 7.69299 13.4763C7.06787 14.1014 6.71667 14.9493 6.71667 15.8333"/></svg>', Q2 = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.41 9.66H9.4"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 9.66H14.59"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M9.31 14.36H9.3"/><path stroke="currentColor" stroke-linecap="round" stroke-width="2.6" d="M14.6 14.36H14.59"/></svg>', wh = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="M12 7V12M12 17V12M17 12H12M12 12H7"/></svg>', eC = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-width="2" d="M10 5V18.5"/><path stroke="currentColor" stroke-width="2" d="M5 10H19"/><rect width="14" height="14" x="5" y="5" stroke="currentColor" stroke-width="2" rx="4"/></svg>';
class qi {
  /**
   * Creates toolbox buttons and toolbox menus
   *
   * @param {object} api - Editor.js api
   * @param {PopoverItem[]} items - Editor.js api
   * @param {function} onOpen - callback fired when the Popover is opening
   * @param {function} onClose - callback fired when the Popover is closing
   * @param {string} [cssModifier] - the modifier for the Toolbox. Allows to add some specific styles.
   */
  constructor({ api: e, items: t, onOpen: n, onClose: r, cssModifier: a = "" }) {
    this.api = e, this.items = t, this.onOpen = n, this.onClose = r, this.cssModifier = a, this.popover = null, this.wrapper = this.createToolbox();
  }
  /**
   * Style classes
   */
  static get CSS() {
    return {
      toolbox: "tc-toolbox",
      toolboxShowed: "tc-toolbox--showed",
      toggler: "tc-toolbox__toggler"
    };
  }
  /**
   * Returns rendered Toolbox element
   */
  get element() {
    return this.wrapper;
  }
  /**
   * Creating a toolbox to open menu for a manipulating columns
   *
   * @returns {Element}
   */
  createToolbox() {
    const e = Mt("div", [
      qi.CSS.toolbox,
      this.cssModifier ? `${qi.CSS.toolbox}--${this.cssModifier}` : ""
    ]);
    e.dataset.mutationFree = "true";
    const t = this.createPopover(), n = this.createToggler();
    return e.appendChild(n), e.appendChild(t), e;
  }
  /**
   * Creates the Toggler
   *
   * @returns {Element}
   */
  createToggler() {
    const e = Mt("div", qi.CSS.toggler, {
      innerHTML: Q2
    });
    return e.addEventListener("click", () => {
      this.togglerClicked();
    }), e;
  }
  /**
   * Creates the Popover instance and render it
   *
   * @returns {Element}
   */
  createPopover() {
    return this.popover = new Io({
      items: this.items
    }), this.popover.render();
  }
  /**
   * Toggler click handler. Opens/Closes the popover
   *
   * @returns {void}
   */
  togglerClicked() {
    this.popover.opened ? (this.popover.close(), this.onClose()) : (this.popover.open(), this.onOpen());
  }
  /**
   * Shows the Toolbox
   *
   * @param {function} computePositionMethod - method that returns the position coordinate
   * @returns {void}
   */
  show(e) {
    const t = e();
    Object.entries(t).forEach(([n, r]) => {
      this.wrapper.style[n] = r;
    }), this.wrapper.classList.add(qi.CSS.toolboxShowed);
  }
  /**
   * Hides the Toolbox
   *
   * @returns {void}
   */
  hide() {
    this.popover.close(), this.wrapper.classList.remove(qi.CSS.toolboxShowed);
  }
}
function tC(o, e) {
  let t = 0;
  return function(...n) {
    const r = (/* @__PURE__ */ new Date()).getTime();
    if (!(r - t < o))
      return t = r, e(...n);
  };
}
class oC {
  constructor(e, t, n) {
    if (this.parentEl = "body", this.element = Re(e), this.startDate = pe().startOf("day"), this.endDate = pe().endOf("day"), this.minDate = !1, this.maxDate = !1, this.maxSpan = !1, this.autoApply = !1, this.singleDatePicker = !1, this.showDropdowns = !1, this.minYear = pe().subtract(100, "year").format("YYYY"), this.maxYear = pe().add(100, "year").format("YYYY"), this.showWeekNumbers = !1, this.showISOWeekNumbers = !1, this.showCustomRangeLabel = !0, this.timePicker = !1, this.timePicker24Hour = !1, this.timePickerIncrement = 1, this.timePickerSeconds = !1, this.linkedCalendars = !0, this.autoUpdateInput = !0, this.alwaysShowCalendars = !1, this.ranges = {}, this.opens = "right", this.element.hasClass("pull-right") && (this.opens = "left"), this.drops = "down", this.element.hasClass("dropup") && (this.drops = "up"), this.buttonClasses = "btn btn-sm", this.applyButtonClasses = "btn-primary", this.cancelButtonClasses = "btn-default", this.locale = {
      direction: "ltr",
      format: pe.localeData().firstDayOfWeek(),
      separator: " - ",
      applyLabel: "Apply",
      cancelLabel: "Cancel",
      weekLabel: "W",
      customRangeLabel: "Custom Range",
      daysOfWeek: pe.weekdaysMin(),
      monthNames: pe.monthsShort(),
      firstDay: pe.localeData().firstDayOfWeek()
    }, this.callback = function() {
    }, this.isShowing = !1, this.leftCalendar = {}, this.rightCalendar = {}, (typeof t != "object" || t === null) && (t = {}), t = Re.extend(this.element.data(), t), typeof t.template != "string" && !(t.template instanceof Re) && (t.template = '<div class="daterangepicker"><div class="ranges"></div><div class="drp-calendar left"><div class="calendar-table"></div><div class="calendar-time"></div></div><div class="drp-calendar right"><div class="calendar-table"></div><div class="calendar-time"></div></div><div class="drp-buttons"><span class="drp-selected"></span><button class="cancelBtn" type="button"></button><button class="applyBtn" disabled="disabled" type="button"></button> </div></div>'), this.parentEl = t.parentEl && Re(t.parentEl).length ? Re(t.parentEl) : Re(this.parentEl), this.container = Re(t.template).appendTo(this.parentEl), typeof t.locale == "object" && (typeof t.locale.direction == "string" && (this.locale.direction = t.locale.direction), typeof t.locale.format == "string" && (this.locale.format = t.locale.format), typeof t.locale.separator == "string" && (this.locale.separator = t.locale.separator), typeof t.locale.daysOfWeek == "object" && (this.locale.daysOfWeek = t.locale.daysOfWeek.slice()), typeof t.locale.monthNames == "object" && (this.locale.monthNames = t.locale.monthNames.slice()), typeof t.locale.firstDay == "number" && (this.locale.firstDay = t.locale.firstDay), typeof t.locale.applyLabel == "string" && (this.locale.applyLabel = t.locale.applyLabel), typeof t.locale.cancelLabel == "string" && (this.locale.cancelLabel = t.locale.cancelLabel), typeof t.locale.weekLabel == "string" && (this.locale.weekLabel = t.locale.weekLabel), typeof t.locale.customRangeLabel == "string")) {
      var r = document.createElement("textarea");
      r.innerHTML = t.locale.customRangeLabel;
      var a = r.value;
      this.locale.customRangeLabel = a;
    }
    if (this.container.addClass(this.locale.direction), typeof t.startDate == "string" && (this.startDate = pe(t.startDate, this.locale.format)), typeof t.endDate == "string" && (this.endDate = pe(t.endDate, this.locale.format)), typeof t.minDate == "string" && (this.minDate = pe(t.minDate, this.locale.format)), typeof t.maxDate == "string" && (this.maxDate = pe(t.maxDate, this.locale.format)), typeof t.startDate == "object" && (this.startDate = pe(t.startDate)), typeof t.endDate == "object" && (this.endDate = pe(t.endDate)), typeof t.minDate == "object" && (this.minDate = pe(t.minDate)), typeof t.maxDate == "object" && (this.maxDate = pe(t.maxDate)), this.minDate && this.startDate.isBefore(this.minDate) && (this.startDate = this.minDate.clone()), this.maxDate && this.endDate.isAfter(this.maxDate) && (this.endDate = this.maxDate.clone()), typeof t.applyButtonClasses == "string" && (this.applyButtonClasses = t.applyButtonClasses), typeof t.applyClass == "string" && (this.applyButtonClasses = t.applyClass), typeof t.cancelButtonClasses == "string" && (this.cancelButtonClasses = t.cancelButtonClasses), typeof t.cancelClass == "string" && (this.cancelButtonClasses = t.cancelClass), typeof t.maxSpan == "object" && (this.maxSpan = t.maxSpan), typeof t.dateLimit == "object" && (this.maxSpan = t.dateLimit), typeof t.opens == "string" && (this.opens = t.opens), typeof t.drops == "string" && (this.drops = t.drops), typeof t.showWeekNumbers == "boolean" && (this.showWeekNumbers = t.showWeekNumbers), typeof t.showISOWeekNumbers == "boolean" && (this.showISOWeekNumbers = t.showISOWeekNumbers), typeof t.buttonClasses == "string" && (this.buttonClasses = t.buttonClasses), typeof t.buttonClasses == "object" && (this.buttonClasses = t.buttonClasses.join(" ")), typeof t.showDropdowns == "boolean" && (this.showDropdowns = t.showDropdowns), typeof t.minYear == "number" && (this.minYear = t.minYear), typeof t.maxYear == "number" && (this.maxYear = t.maxYear), typeof t.showCustomRangeLabel == "boolean" && (this.showCustomRangeLabel = t.showCustomRangeLabel), typeof t.singleDatePicker == "boolean" && (this.singleDatePicker = t.singleDatePicker, this.singleDatePicker && (this.endDate = this.startDate.clone())), typeof t.timePicker == "boolean" && (this.timePicker = t.timePicker), typeof t.timePickerSeconds == "boolean" && (this.timePickerSeconds = t.timePickerSeconds), typeof t.timePickerIncrement == "number" && (this.timePickerIncrement = t.timePickerIncrement), typeof t.timePicker24Hour == "boolean" && (this.timePicker24Hour = t.timePicker24Hour), typeof t.autoApply == "boolean" && (this.autoApply = t.autoApply), typeof t.autoUpdateInput == "boolean" && (this.autoUpdateInput = t.autoUpdateInput), typeof t.linkedCalendars == "boolean" && (this.linkedCalendars = t.linkedCalendars), typeof t.isInvalidDate == "function" && (this.isInvalidDate = t.isInvalidDate), typeof t.isCustomDate == "function" && (this.isCustomDate = t.isCustomDate), typeof t.alwaysShowCalendars == "boolean" && (this.alwaysShowCalendars = t.alwaysShowCalendars), this.locale.firstDay != 0)
      for (var c = this.locale.firstDay; c > 0; )
        this.locale.daysOfWeek.push(this.locale.daysOfWeek.shift()), c--;
    var u, p, g;
    if (typeof t.startDate > "u" && typeof t.endDate > "u" && Re(this.element).is(":text")) {
      var v = Re(this.element).val(), k = v.split(this.locale.separator);
      u = p = null, k.length == 2 ? (u = pe(k[0], this.locale.format), p = pe(k[1], this.locale.format)) : this.singleDatePicker && v !== "" && (u = pe(v, this.locale.format), p = pe(v, this.locale.format)), u !== null && p !== null && (this.setStartDate(u), this.setEndDate(p));
    }
    if (typeof t.ranges == "object") {
      for (g in t.ranges) {
        typeof t.ranges[g][0] == "string" ? u = pe(t.ranges[g][0], this.locale.format) : u = pe(t.ranges[g][0]), typeof t.ranges[g][1] == "string" ? p = pe(t.ranges[g][1], this.locale.format) : p = pe(t.ranges[g][1]), this.minDate && u.isBefore(this.minDate) && (u = this.minDate.clone());
        var y = this.maxDate;
        if (this.maxSpan && y && u.clone().add(this.maxSpan).isAfter(y) && (y = u.clone().add(this.maxSpan)), y && p.isAfter(y) && (p = y.clone()), !(this.minDate && p.isBefore(this.minDate, this.timepicker ? "minute" : "day") || y && u.isAfter(y, this.timepicker ? "minute" : "day"))) {
          var r = document.createElement("textarea");
          r.innerHTML = g;
          var a = r.value;
          this.ranges[a] = [u, p];
        }
      }
      var T = "<ul>";
      for (g in this.ranges)
        T += '<li data-range-key="' + g + '">' + g + "</li>";
      this.showCustomRangeLabel && (T += '<li data-range-key="' + this.locale.customRangeLabel + '">' + this.locale.customRangeLabel + "</li>"), T += "</ul>", this.container.find(".ranges").prepend(T);
    }
    typeof n == "function" && (this.callback = n), this.timePicker || (this.startDate = this.startDate.startOf("day"), this.endDate = this.endDate.endOf("day"), this.container.find(".calendar-time").hide()), this.timePicker && this.autoApply && (this.autoApply = !1), this.autoApply && this.container.addClass("auto-apply"), typeof t.ranges == "object" && this.container.addClass("show-ranges"), this.singleDatePicker && (this.container.addClass("single"), this.container.find(".drp-calendar.left").addClass("single"), this.container.find(".drp-calendar.left").show(), this.container.find(".drp-calendar.right").hide(), !this.timePicker && this.autoApply && this.container.addClass("auto-apply")), (typeof t.ranges > "u" && !this.singleDatePicker || this.alwaysShowCalendars) && this.container.addClass("show-calendar"), this.container.addClass("opens" + this.opens), this.container.find(".applyBtn, .cancelBtn").addClass(this.buttonClasses), this.applyButtonClasses.length && this.container.find(".applyBtn").addClass(this.applyButtonClasses), this.cancelButtonClasses.length && this.container.find(".cancelBtn").addClass(this.cancelButtonClasses), this.container.find(".applyBtn").html(this.locale.applyLabel), this.container.find(".cancelBtn").html(this.locale.cancelLabel), this.container.find(".drp-calendar").on("click.daterangepicker", ".prev", Re.proxy(this.clickPrev, this)).on("click.daterangepicker", ".next", Re.proxy(this.clickNext, this)).on("mousedown.daterangepicker", "td.available", Re.proxy(this.clickDate, this)).on("mouseenter.daterangepicker", "td.available", Re.proxy(this.hoverDate, this)).on("change.daterangepicker", "select.yearselect", Re.proxy(this.monthOrYearChanged, this)).on("change.daterangepicker", "select.monthselect", Re.proxy(this.monthOrYearChanged, this)).on("change.daterangepicker", "select.hourselect,select.minuteselect,select.secondselect,select.ampmselect", Re.proxy(this.timeChanged, this)), this.container.find(".ranges").on("click.daterangepicker", "li", Re.proxy(this.clickRange, this)), this.container.find(".drp-buttons").on("click.daterangepicker", "button.applyBtn", Re.proxy(this.clickApply, this)).on("click.daterangepicker", "button.cancelBtn", Re.proxy(this.clickCancel, this)), this.element.is("input") || this.element.is("button") ? this.element.on({
      "click.daterangepicker": Re.proxy(this.show, this),
      "focus.daterangepicker": Re.proxy(this.show, this),
      "keyup.daterangepicker": Re.proxy(this.elementChanged, this),
      "keydown.daterangepicker": Re.proxy(this.keydown, this)
      //IE 11 compatibility
    }) : (this.element.on("click.daterangepicker", Re.proxy(this.toggle, this)), this.element.on("keydown.daterangepicker", Re.proxy(this.toggle, this))), this.updateElement();
  }
  setStartDate(e) {
    typeof e == "string" && (this.startDate = pe(e, this.locale.format)), typeof e == "object" && (this.startDate = pe(e)), this.timePicker || (this.startDate = this.startDate.startOf("day")), this.timePicker && this.timePickerIncrement && this.startDate.minute(Math.round(this.startDate.minute() / this.timePickerIncrement) * this.timePickerIncrement), this.minDate && this.startDate.isBefore(this.minDate) && (this.startDate = this.minDate.clone(), this.timePicker && this.timePickerIncrement && this.startDate.minute(Math.round(this.startDate.minute() / this.timePickerIncrement) * this.timePickerIncrement)), this.maxDate && this.startDate.isAfter(this.maxDate) && (this.startDate = this.maxDate.clone(), this.timePicker && this.timePickerIncrement && this.startDate.minute(Math.floor(this.startDate.minute() / this.timePickerIncrement) * this.timePickerIncrement)), this.isShowing || this.updateElement(), this.updateMonthsInView();
  }
  setEndDate(e) {
    typeof e == "string" && (this.endDate = pe(e, this.locale.format)), typeof e == "object" && (this.endDate = pe(e)), this.timePicker || (this.endDate = this.endDate.endOf("day")), this.timePicker && this.timePickerIncrement && this.endDate.minute(Math.round(this.endDate.minute() / this.timePickerIncrement) * this.timePickerIncrement), this.endDate.isBefore(this.startDate) && (this.endDate = this.startDate.clone()), this.maxDate && this.endDate.isAfter(this.maxDate) && (this.endDate = this.maxDate.clone()), this.maxSpan && this.startDate.clone().add(this.maxSpan).isBefore(this.endDate) && (this.endDate = this.startDate.clone().add(this.maxSpan)), this.previousRightTime = this.endDate.clone(), this.container.find(".drp-selected").html(this.startDate.format(this.locale.format) + this.locale.separator + this.endDate.format(this.locale.format)), this.isShowing || this.updateElement(), this.updateMonthsInView();
  }
  isInvalidDate() {
    return !1;
  }
  isCustomDate() {
    return !1;
  }
  updateView() {
    this.timePicker && (this.renderTimePicker("left"), this.renderTimePicker("right"), this.endDate ? this.container.find(".right .calendar-time select").prop("disabled", !1).removeClass("disabled") : this.container.find(".right .calendar-time select").prop("disabled", !0).addClass("disabled")), this.endDate && this.container.find(".drp-selected").html(this.startDate.format(this.locale.format) + this.locale.separator + this.endDate.format(this.locale.format)), this.updateMonthsInView(), this.updateCalendars(), this.updateFormInputs();
  }
  updateMonthsInView() {
    if (this.endDate) {
      if (!this.singleDatePicker && this.leftCalendar.month && this.rightCalendar.month && (this.startDate.format("YYYY-MM") == this.leftCalendar.month.format("YYYY-MM") || this.startDate.format("YYYY-MM") == this.rightCalendar.month.format("YYYY-MM")) && (this.endDate.format("YYYY-MM") == this.leftCalendar.month.format("YYYY-MM") || this.endDate.format("YYYY-MM") == this.rightCalendar.month.format("YYYY-MM")))
        return;
      this.leftCalendar.month = this.startDate.clone().date(2), !this.linkedCalendars && (this.endDate.month() != this.startDate.month() || this.endDate.year() != this.startDate.year()) ? this.rightCalendar.month = this.endDate.clone().date(2) : this.rightCalendar.month = this.startDate.clone().date(2).add(1, "month");
    } else
      this.leftCalendar.month.format("YYYY-MM") != this.startDate.format("YYYY-MM") && this.rightCalendar.month.format("YYYY-MM") != this.startDate.format("YYYY-MM") && (this.leftCalendar.month = this.startDate.clone().date(2), this.rightCalendar.month = this.startDate.clone().date(2).add(1, "month"));
    this.maxDate && this.linkedCalendars && !this.singleDatePicker && this.rightCalendar.month > this.maxDate && (this.rightCalendar.month = this.maxDate.clone().date(2), this.leftCalendar.month = this.maxDate.clone().date(2).subtract(1, "month"));
  }
  updateCalendars() {
    if (this.timePicker) {
      var e, t, n;
      if (this.endDate) {
        if (e = parseInt(this.container.find(".left .hourselect").val(), 10), t = parseInt(this.container.find(".left .minuteselect").val(), 10), isNaN(t) && (t = parseInt(this.container.find(".left .minuteselect option:last").val(), 10)), n = this.timePickerSeconds ? parseInt(this.container.find(".left .secondselect").val(), 10) : 0, !this.timePicker24Hour) {
          var r = this.container.find(".left .ampmselect").val();
          r === "PM" && e < 12 && (e += 12), r === "AM" && e === 12 && (e = 0);
        }
      } else if (e = parseInt(this.container.find(".right .hourselect").val(), 10), t = parseInt(this.container.find(".right .minuteselect").val(), 10), isNaN(t) && (t = parseInt(this.container.find(".right .minuteselect option:last").val(), 10)), n = this.timePickerSeconds ? parseInt(this.container.find(".right .secondselect").val(), 10) : 0, !this.timePicker24Hour) {
        var r = this.container.find(".right .ampmselect").val();
        r === "PM" && e < 12 && (e += 12), r === "AM" && e === 12 && (e = 0);
      }
      this.leftCalendar.month.hour(e).minute(t).second(n), this.rightCalendar.month.hour(e).minute(t).second(n);
    }
    this.renderCalendar("left"), this.renderCalendar("right"), this.container.find(".ranges li").removeClass("active"), this.endDate != null && this.calculateChosenLabel();
  }
  renderCalendar(e) {
    var x = e == "left" ? this.leftCalendar : this.rightCalendar, t = x.month.month(), n = x.month.year(), r = x.month.hour(), a = x.month.minute(), c = x.month.second(), u = pe([n, t]).daysInMonth(), p = pe([n, t, 1]), g = pe([n, t, u]), v = pe(p).subtract(1, "month").month(), k = pe(p).subtract(1, "month").year(), y = pe([k, v]).daysInMonth(), T = p.day(), x = [];
    x.firstDay = p, x.lastDay = g;
    for (var M = 0; M < 6; M++)
      x[M] = [];
    var L = y - T + this.locale.firstDay + 1;
    L > y && (L -= 7), T == this.locale.firstDay && (L = y - 6);
    for (var Y = pe([k, v, L, 12, a, c]), I, F, M = 0, I = 0, F = 0; M < 42; M++, I++, Y = pe(Y).add(24, "hour"))
      M > 0 && I % 7 === 0 && (I = 0, F++), x[F][I] = Y.clone().hour(r).minute(a).second(c), Y.hour(12), this.minDate && x[F][I].format("YYYY-MM-DD") == this.minDate.format("YYYY-MM-DD") && x[F][I].isBefore(this.minDate) && e == "left" && (x[F][I] = this.minDate.clone()), this.maxDate && x[F][I].format("YYYY-MM-DD") == this.maxDate.format("YYYY-MM-DD") && x[F][I].isAfter(this.maxDate) && e == "right" && (x[F][I] = this.maxDate.clone());
    e == "left" ? this.leftCalendar.calendar = x : this.rightCalendar.calendar = x;
    var Q = e == "left" ? this.minDate : this.startDate, U = this.maxDate;
    e == "left" ? this.startDate : this.endDate, this.locale.direction == "ltr";
    var z = '<table class="table-condensed">';
    z += "<thead>", z += "<tr>", (this.showWeekNumbers || this.showISOWeekNumbers) && (z += "<th></th>"), (!Q || Q.isBefore(x.firstDay)) && (!this.linkedCalendars || e == "left") ? z += '<th class="prev available"><span></span></th>' : z += "<th></th>";
    var d = this.locale.monthNames[x[1][1].month()] + x[1][1].format(" YYYY");
    if (this.showDropdowns) {
      for (var j = x[1][1].month(), R = x[1][1].year(), V = U && U.year() || this.maxYear, K = Q && Q.year() || this.minYear, J = R == K, A = R == V, se = '<select class="monthselect">', Z = 0; Z < 12; Z++)
        (!J || Q && Z >= Q.month()) && (!A || U && Z <= U.month()) ? se += "<option value='" + Z + "'" + (Z === j ? " selected='selected'" : "") + ">" + this.locale.monthNames[Z] + "</option>" : se += "<option value='" + Z + "'" + (Z === j ? " selected='selected'" : "") + " disabled='disabled'>" + this.locale.monthNames[Z] + "</option>";
      se += "</select>";
      for (var ne = '<select class="yearselect">', le = K; le <= V; le++)
        ne += '<option value="' + le + '"' + (le === R ? ' selected="selected"' : "") + ">" + le + "</option>";
      ne += "</select>", d = se + ne;
    }
    if (z += '<th colspan="5" class="month">' + d + "</th>", (!U || U.isAfter(x.lastDay)) && (!this.linkedCalendars || e == "right" || this.singleDatePicker) ? z += '<th class="next available"><span></span></th>' : z += "<th></th>", z += "</tr>", z += "<tr>", (this.showWeekNumbers || this.showISOWeekNumbers) && (z += '<th class="week">' + this.locale.weekLabel + "</th>"), Re.each(this.locale.daysOfWeek, function(Je, no) {
      z += "<th>" + no + "</th>";
    }), z += "</tr>", z += "</thead>", z += "<tbody>", this.endDate == null && this.maxSpan) {
      var oe = this.startDate.clone().add(this.maxSpan).endOf("day");
      (!U || oe.isBefore(U)) && (U = oe);
    }
    for (var F = 0; F < 6; F++) {
      z += "<tr>", this.showWeekNumbers ? z += '<td class="week">' + x[F][0].week() + "</td>" : this.showISOWeekNumbers && (z += '<td class="week">' + x[F][0].isoWeek() + "</td>");
      for (var I = 0; I < 7; I++) {
        var fe = [];
        x[F][I].isSame(/* @__PURE__ */ new Date(), "day") && fe.push("today"), x[F][I].isoWeekday() > 5 && fe.push("weekend"), x[F][I].month() != x[1][1].month() && fe.push("off", "ends"), this.minDate && x[F][I].isBefore(this.minDate, "day") && fe.push("off", "disabled"), U && x[F][I].isAfter(U, "day") && fe.push("off", "disabled"), this.isInvalidDate(x[F][I]) && fe.push("off", "disabled"), x[F][I].format("YYYY-MM-DD") == this.startDate.format("YYYY-MM-DD") && fe.push("active", "start-date"), this.endDate != null && x[F][I].format("YYYY-MM-DD") == this.endDate.format("YYYY-MM-DD") && fe.push("active", "end-date"), this.endDate != null && x[F][I] > this.startDate && x[F][I] < this.endDate && fe.push("in-range");
        var Me = this.isCustomDate(x[F][I]);
        Me !== !1 && (typeof Me == "string" ? fe.push(Me) : Array.prototype.push.apply(fe, Me));
        for (var je = "", Ae = !1, M = 0; M < fe.length; M++)
          je += fe[M] + " ", fe[M] == "disabled" && (Ae = !0);
        Ae || (je += "available"), z += '<td class="' + je.replace(/^\s+|\s+$/g, "") + '" data-title="r' + F + "c" + I + '">' + x[F][I].date() + "</td>";
      }
      z += "</tr>";
    }
    z += "</tbody>", z += "</table>", this.container.find(".drp-calendar." + e + " .calendar-table").html(z);
  }
  renderTimePicker(e) {
    if (!(e == "right" && !this.endDate)) {
      var t, n, r, a = this.maxDate;
      if (this.maxSpan && (!this.maxDate || this.startDate.clone().add(this.maxSpan).isBefore(this.maxDate)) && (a = this.startDate.clone().add(this.maxSpan)), e == "left")
        n = this.startDate.clone(), r = this.minDate;
      else if (e == "right") {
        n = this.endDate.clone(), r = this.startDate;
        var c = this.container.find(".drp-calendar.right .calendar-time");
        if (c.html() != "" && (n.hour(isNaN(n.hour()) ? c.find(".hourselect option:selected").val() : n.hour()), n.minute(isNaN(n.minute()) ? c.find(".minuteselect option:selected").val() : n.minute()), n.second(isNaN(n.second()) ? c.find(".secondselect option:selected").val() : n.second()), !this.timePicker24Hour)) {
          var u = c.find(".ampmselect option:selected").val();
          u === "PM" && n.hour() < 12 && n.hour(n.hour() + 12), u === "AM" && n.hour() === 12 && n.hour(0);
        }
        n.isBefore(this.startDate) && (n = this.startDate.clone()), a && n.isAfter(a) && (n = a.clone());
      }
      t = '<select class="hourselect">';
      for (var p = this.timePicker24Hour ? 0 : 1, g = this.timePicker24Hour ? 23 : 12, v = p; v <= g; v++) {
        var k = v;
        this.timePicker24Hour || (k = n.hour() >= 12 ? v == 12 ? 12 : v + 12 : v == 12 ? 0 : v);
        var y = n.clone().hour(k), T = !1;
        r && y.minute(59).isBefore(r) && (T = !0), a && y.minute(0).isAfter(a) && (T = !0), k == n.hour() && !T ? t += '<option value="' + v + '" selected="selected">' + v + "</option>" : T ? t += '<option value="' + v + '" disabled="disabled" class="disabled">' + v + "</option>" : t += '<option value="' + v + '">' + v + "</option>";
      }
      t += "</select> ", t += ': <select class="minuteselect">';
      for (var v = 0; v < 60; v += this.timePickerIncrement) {
        var x = v < 10 ? "0" + v : v, y = n.clone().minute(v), T = !1;
        r && y.second(59).isBefore(r) && (T = !0), a && y.second(0).isAfter(a) && (T = !0), n.minute() == v && !T ? t += '<option value="' + v + '" selected="selected">' + x + "</option>" : T ? t += '<option value="' + v + '" disabled="disabled" class="disabled">' + x + "</option>" : t += '<option value="' + v + '">' + x + "</option>";
      }
      if (t += "</select> ", this.timePickerSeconds) {
        t += ': <select class="secondselect">';
        for (var v = 0; v < 60; v++) {
          var x = v < 10 ? "0" + v : v, y = n.clone().second(v), T = !1;
          r && y.isBefore(r) && (T = !0), a && y.isAfter(a) && (T = !0), n.second() == v && !T ? t += '<option value="' + v + '" selected="selected">' + x + "</option>" : T ? t += '<option value="' + v + '" disabled="disabled" class="disabled">' + x + "</option>" : t += '<option value="' + v + '">' + x + "</option>";
        }
        t += "</select> ";
      }
      if (!this.timePicker24Hour) {
        t += '<select class="ampmselect">';
        var M = "", L = "";
        r && n.clone().hour(12).minute(0).second(0).isBefore(r) && (M = ' disabled="disabled" class="disabled"'), a && n.clone().hour(0).minute(0).second(0).isAfter(a) && (L = ' disabled="disabled" class="disabled"'), n.hour() >= 12 ? t += '<option value="AM"' + M + '>AM</option><option value="PM" selected="selected"' + L + ">PM</option>" : t += '<option value="AM" selected="selected"' + M + '>AM</option><option value="PM"' + L + ">PM</option>", t += "</select>";
      }
      this.container.find(".drp-calendar." + e + " .calendar-time").html(t);
    }
  }
  updateFormInputs() {
    this.singleDatePicker || this.endDate && (this.startDate.isBefore(this.endDate) || this.startDate.isSame(this.endDate)) ? this.container.find("button.applyBtn").prop("disabled", !1) : this.container.find("button.applyBtn").prop("disabled", !0);
  }
  move() {
    var e = { top: 0, left: 0 }, t, n = this.drops, r = Re(window).width();
    switch (this.parentEl.is("body") || (e = {
      top: this.parentEl.offset().top - this.parentEl.scrollTop(),
      left: this.parentEl.offset().left - this.parentEl.scrollLeft()
    }, r = this.parentEl[0].clientWidth + this.parentEl.offset().left), n) {
      case "auto":
        t = this.element.offset().top + this.element.outerHeight() - e.top, t + this.container.outerHeight() >= this.parentEl[0].scrollHeight && (t = this.element.offset().top - this.container.outerHeight() - e.top, n = "up");
        break;
      case "up":
        t = this.element.offset().top - this.container.outerHeight() - e.top;
        break;
      default:
        t = this.element.offset().top + this.element.outerHeight() - e.top;
        break;
    }
    this.container.css({
      top: 0,
      left: 0,
      right: "auto"
    });
    var a = this.container.outerWidth();
    if (this.container.toggleClass("drop-up", n == "up"), this.opens == "left") {
      var c = r - this.element.offset().left - this.element.outerWidth();
      a + c > Re(window).width() ? this.container.css({
        top: t,
        right: "auto",
        left: 9
      }) : this.container.css({
        top: t,
        right: c,
        left: "auto"
      });
    } else if (this.opens == "center") {
      var u = this.element.offset().left - e.left + this.element.outerWidth() / 2 - a / 2;
      u < 0 ? this.container.css({
        top: t,
        right: "auto",
        left: 9
      }) : u + a > Re(window).width() ? this.container.css({
        top: t,
        left: "auto",
        right: 0
      }) : this.container.css({
        top: t,
        left: u,
        right: "auto"
      });
    } else {
      var u = this.element.offset().left - e.left;
      u + a > Re(window).width() ? this.container.css({
        top: t,
        left: "auto",
        right: 0
      }) : this.container.css({
        top: t,
        left: u,
        right: "auto"
      });
    }
  }
  show(e) {
    this.isShowing || (this._outsideClickProxy = Re.proxy(function(t) {
      this.outsideClick(t);
    }, this), Re(document).on("mousedown.daterangepicker", this._outsideClickProxy).on("touchend.daterangepicker", this._outsideClickProxy).on("click.daterangepicker", "[data-toggle=dropdown]", this._outsideClickProxy).on("focusin.daterangepicker", this._outsideClickProxy), Re(window).on("resize.daterangepicker", Re.proxy(function(t) {
      this.move(t);
    }, this)), this.oldStartDate = this.startDate.clone(), this.oldEndDate = this.endDate.clone(), this.previousRightTime = this.endDate.clone(), this.updateView(), this.container.show(), this.move(), this.element.trigger("show.daterangepicker", this), this.isShowing = !0);
  }
  hide(e) {
    this.isShowing && (this.endDate || (this.startDate = this.oldStartDate.clone(), this.endDate = this.oldEndDate.clone()), (!this.startDate.isSame(this.oldStartDate) || !this.endDate.isSame(this.oldEndDate)) && this.callback(this.startDate.clone(), this.endDate.clone(), this.chosenLabel), this.updateElement(), Re(document).off(".daterangepicker"), Re(window).off(".daterangepicker"), this.container.hide(), this.element.trigger("hide.daterangepicker", this), this.isShowing = !1);
  }
  toggle(e) {
    this.isShowing ? this.hide() : this.show();
  }
  outsideClick(e) {
    var t = Re(e.target);
    e.type == "focusin" || t.closest(this.element).length || t.closest(this.container).length || t.closest(".calendar-table").length || (this.hide(), this.element.trigger("outsideClick.daterangepicker", this));
  }
  showCalendars() {
    this.container.addClass("show-calendar"), this.move(), this.element.trigger("showCalendar.daterangepicker", this);
  }
  hideCalendars() {
    this.container.removeClass("show-calendar"), this.element.trigger("hideCalendar.daterangepicker", this);
  }
  clickRange(e) {
    var t = e.target.getAttribute("data-range-key");
    if (this.chosenLabel = t, t == this.locale.customRangeLabel)
      this.showCalendars();
    else {
      var n = this.ranges[t];
      this.startDate = n[0], this.endDate = n[1], this.timePicker || (this.startDate.startOf("day"), this.endDate.endOf("day")), this.alwaysShowCalendars || this.hideCalendars(), this.clickApply();
    }
  }
  clickPrev(e) {
    var t = Re(e.target).parents(".drp-calendar");
    t.hasClass("left") ? (this.leftCalendar.month.subtract(1, "month"), this.linkedCalendars && this.rightCalendar.month.subtract(1, "month")) : this.rightCalendar.month.subtract(1, "month"), this.updateCalendars();
  }
  clickNext(e) {
    var t = Re(e.target).parents(".drp-calendar");
    t.hasClass("left") ? this.leftCalendar.month.add(1, "month") : (this.rightCalendar.month.add(1, "month"), this.linkedCalendars && this.leftCalendar.month.add(1, "month")), this.updateCalendars();
  }
  hoverDate(e) {
    if (Re(e.target).hasClass("available")) {
      var t = Re(e.target).attr("data-title"), n = t.substr(1, 1), r = t.substr(3, 1), a = Re(e.target).parents(".drp-calendar"), c = a.hasClass("left") ? this.leftCalendar.calendar[n][r] : this.rightCalendar.calendar[n][r], u = this.leftCalendar, p = this.rightCalendar, g = this.startDate;
      this.endDate || this.container.find(".drp-calendar tbody td").each(function(v, k) {
        if (!Re(k).hasClass("week")) {
          var y = Re(k).attr("data-title"), T = y.substr(1, 1), x = y.substr(3, 1), M = Re(k).parents(".drp-calendar"), L = M.hasClass("left") ? u.calendar[T][x] : p.calendar[T][x];
          L.isAfter(g) && L.isBefore(c) || L.isSame(c, "day") ? Re(k).addClass("in-range") : Re(k).removeClass("in-range");
        }
      });
    }
  }
  clickDate(e) {
    if (Re(e.target).hasClass("available")) {
      var t = Re(e.target).attr("data-title"), n = t.substr(1, 1), r = t.substr(3, 1), a = Re(e.target).parents(".drp-calendar"), c = a.hasClass("left") ? this.leftCalendar.calendar[n][r] : this.rightCalendar.calendar[n][r];
      if (this.endDate || c.isBefore(this.startDate, "day")) {
        if (this.timePicker) {
          var u = parseInt(this.container.find(".left .hourselect").val(), 10);
          if (!this.timePicker24Hour) {
            var p = this.container.find(".left .ampmselect").val();
            p === "PM" && u < 12 && (u += 12), p === "AM" && u === 12 && (u = 0);
          }
          var g = parseInt(this.container.find(".left .minuteselect").val(), 10);
          isNaN(g) && (g = parseInt(this.container.find(".left .minuteselect option:last").val(), 10));
          var v = this.timePickerSeconds ? parseInt(this.container.find(".left .secondselect").val(), 10) : 0;
          c = c.clone().hour(u).minute(g).second(v);
        }
        this.endDate = null, this.setStartDate(c.clone());
      } else if (!this.endDate && c.isBefore(this.startDate))
        this.setEndDate(this.startDate.clone());
      else {
        if (this.timePicker) {
          var u = parseInt(this.container.find(".right .hourselect").val(), 10);
          if (!this.timePicker24Hour) {
            var p = this.container.find(".right .ampmselect").val();
            p === "PM" && u < 12 && (u += 12), p === "AM" && u === 12 && (u = 0);
          }
          var g = parseInt(this.container.find(".right .minuteselect").val(), 10);
          isNaN(g) && (g = parseInt(this.container.find(".right .minuteselect option:last").val(), 10));
          var v = this.timePickerSeconds ? parseInt(this.container.find(".right .secondselect").val(), 10) : 0;
          c = c.clone().hour(u).minute(g).second(v);
        }
        this.setEndDate(c.clone()), this.autoApply && (this.calculateChosenLabel(), this.clickApply());
      }
      this.singleDatePicker && (this.setEndDate(this.startDate), !this.timePicker && this.autoApply && this.clickApply()), this.updateView(), e.stopPropagation();
    }
  }
  calculateChosenLabel() {
    var e = !0, t = 0;
    for (var n in this.ranges) {
      if (this.timePicker) {
        var r = this.timePickerSeconds ? "YYYY-MM-DD HH:mm:ss" : "YYYY-MM-DD HH:mm";
        if (this.startDate.format(r) == this.ranges[n][0].format(r) && this.endDate.format(r) == this.ranges[n][1].format(r)) {
          e = !1, this.chosenLabel = this.container.find(".ranges li:eq(" + t + ")").addClass("active").attr("data-range-key");
          break;
        }
      } else if (this.startDate.format("YYYY-MM-DD") == this.ranges[n][0].format("YYYY-MM-DD") && this.endDate.format("YYYY-MM-DD") == this.ranges[n][1].format("YYYY-MM-DD")) {
        e = !1, this.chosenLabel = this.container.find(".ranges li:eq(" + t + ")").addClass("active").attr("data-range-key");
        break;
      }
      t++;
    }
    e && (this.showCustomRangeLabel ? this.chosenLabel = this.container.find(".ranges li:last").addClass("active").attr("data-range-key") : this.chosenLabel = null, this.showCalendars());
  }
  clickApply(e) {
    this.hide(), this.element.trigger("apply.daterangepicker", this);
  }
  clickCancel(e) {
    this.startDate = this.oldStartDate, this.endDate = this.oldEndDate, this.hide(), this.element.trigger("cancel.daterangepicker", this);
  }
  monthOrYearChanged(e) {
    var t = Re(e.target).closest(".drp-calendar").hasClass("left"), n = t ? "left" : "right", r = this.container.find(".drp-calendar." + n), a = parseInt(r.find(".monthselect").val(), 10), c = r.find(".yearselect").val();
    t || (c < this.startDate.year() || c == this.startDate.year() && a < this.startDate.month()) && (a = this.startDate.month(), c = this.startDate.year()), this.minDate && (c < this.minDate.year() || c == this.minDate.year() && a < this.minDate.month()) && (a = this.minDate.month(), c = this.minDate.year()), this.maxDate && (c > this.maxDate.year() || c == this.maxDate.year() && a > this.maxDate.month()) && (a = this.maxDate.month(), c = this.maxDate.year()), t ? (this.leftCalendar.month.month(a).year(c), this.linkedCalendars && (this.rightCalendar.month = this.leftCalendar.month.clone().add(1, "month"))) : (this.rightCalendar.month.month(a).year(c), this.linkedCalendars && (this.leftCalendar.month = this.rightCalendar.month.clone().subtract(1, "month"))), this.updateCalendars();
  }
  timeChanged(e) {
    var t = Re(e.target).closest(".drp-calendar"), n = t.hasClass("left"), r = parseInt(t.find(".hourselect").val(), 10), a = parseInt(t.find(".minuteselect").val(), 10);
    isNaN(a) && (a = parseInt(t.find(".minuteselect option:last").val(), 10));
    var c = this.timePickerSeconds ? parseInt(t.find(".secondselect").val(), 10) : 0;
    if (!this.timePicker24Hour) {
      var u = t.find(".ampmselect").val();
      u === "PM" && r < 12 && (r += 12), u === "AM" && r === 12 && (r = 0);
    }
    if (n) {
      var p = this.startDate.clone();
      p.hour(r), p.minute(a), p.second(c), this.setStartDate(p), this.singleDatePicker ? this.endDate = this.startDate.clone() : this.endDate && this.endDate.format("YYYY-MM-DD") == p.format("YYYY-MM-DD") && this.endDate.isBefore(p) && this.setEndDate(p.clone());
    } else if (this.endDate) {
      var g = this.endDate.clone();
      g.hour(r), g.minute(a), g.second(c), this.setEndDate(g);
    }
    this.updateCalendars(), this.updateFormInputs(), this.renderTimePicker("left"), this.renderTimePicker("right");
  }
  elementChanged() {
    if (this.element.is("input") && this.element.val().length) {
      var e = this.element.val().split(this.locale.separator), t = null, n = null;
      e.length === 2 && (t = pe(e[0], this.locale.format), n = pe(e[1], this.locale.format)), (this.singleDatePicker || t === null || n === null) && (t = pe(this.element.val(), this.locale.format), n = t), !(!t.isValid() || !n.isValid()) && (this.setStartDate(t), this.setEndDate(n), this.updateView());
    }
  }
  keydown(e) {
    (e.keyCode === 9 || e.keyCode === 13) && this.hide(), e.keyCode === 27 && (e.preventDefault(), e.stopPropagation(), this.hide());
  }
  updateElement() {
    if (this.element.is("input") && this.autoUpdateInput) {
      var e = this.startDate.format(this.locale.format);
      this.singleDatePicker || (e += this.locale.separator + this.endDate.format(this.locale.format)), e !== this.element.val() && this.element.val(e).trigger("change");
    }
  }
  remove() {
    this.container.remove(), this.element.off(".daterangepicker"), this.element.removeData();
  }
}
Re.fn.daterangepicker = function(o, e) {
  var t = Re.extend(!0, {}, Re.fn.daterangepicker.defaultOptions, o);
  return this.each(function() {
    var n = Re(this);
    n.data("daterangepicker") && n.data("daterangepicker").remove(), n.data("daterangepicker", new oC(n, t, e));
  }), this;
};
const Xe = {
  wrapper: "tc-wrap",
  wrapperReadOnly: "tc-wrap--readonly",
  table: "tc-table",
  row: "tc-row",
  withHeadings: "tc-table--heading",
  rowSelected: "tc-row--selected",
  cell: "tc-cell",
  cellSelected: "tc-cell--selected",
  addRow: "tc-add-row",
  addColumn: "tc-add-column",
  formInput: "ap-form-input"
};
class nC {
  /**
   * Creates
   *
   * @constructor
   * @param {boolean} readOnly - read-only mode flag
   * @param {object} api - Editor.js API
   * @param {TableData} data - Editor.js API
   * @param {TableConfig} config - Editor.js API
   */
  constructor(e, t, n, r, a) {
    var c;
    if (this.readOnly = e, this.api = t, this.block = a, this.config = r, this.isJapan = ((c = this.config) == null ? void 0 : c.locale) == "jp", n.content.length > 1) {
      const u = r.data.data.length;
      n.content.map((p, g) => p.splice(u));
    }
    n.heading = r.data.data.map((u) => this.isJapan ? u.heading_jp : u.heading), this.data = n, this.wrapper = null, this.table = null, this.interval = null, this.customDatePicker = null, this.toolboxRow = this.createRowToolbox(), this.toolBoxDue = null, this.createTableWrapper(), this.hoveredRow = 0, this.hoveredColumn = 0, this.selectedRow = 0, this.selectedColumn = 0, this.tunes = {
      withHeadings: !1
    }, this.resize(), this.fill(), setTimeout(() => {
      this.checkInput();
    }, 0), this.focusedCell = {
      row: 0,
      column: 0
    }, this.documentClicked = (u) => {
      const p = u.target.closest(`.${Xe.table}`) !== null, g = u.target.closest(`.${Xe.wrapper}`) === null;
      (p || g) && this.hideToolboxes();
      const v = u.target.closest(`.${Xe.addRow}`), k = u.target.closest(`.${Xe.addColumn}`);
      v && v.parentNode === this.wrapper ? (this.addRow(void 0, !0), this.hideToolboxes()) : k && k.parentNode === this.wrapper && this.hideToolboxes();
    }, this.readOnly || this.bindEvents();
  }
  checkInput() {
    this.table.querySelectorAll(`.${Xe.row}`).forEach((e, t) => {
      var n;
      t != 0 && ((n = e == null ? void 0 : e.firstChild) == null ? void 0 : n.childElementCount) == 0 && this.createColumnInput(t);
    });
  }
  createColumnInput(e) {
    var t, n, r, a;
    this.isJapan = ((t = this.config) == null ? void 0 : t.locale) == "jp";
    for (let c = 0; c < this.config.data.data.length; c++) {
      const u = this.config.data.data[c], p = this.getCell(e + 1, c + 1);
      if (!p)
        return;
      const g = ((a = (r = (n = this.data) == null ? void 0 : n.content) == null ? void 0 : r[e - 1]) == null ? void 0 : a[c]) || "";
      if (u.type === "search") {
        const v = Mt("input", Xe.formInput, {
          value: g,
          disabled: this.readOnly
        });
        this.api.listeners.on(v, "keyup", (k) => {
          const y = k.target.value.trim().replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
          clearTimeout(this.interval), this.interval = setTimeout(() => {
            this.fetchData(y, v);
          }, 500);
        }), p.appendChild(v);
      } else if (u.type === "date") {
        const v = Mt("input", Xe.formInput, {
          value: g,
          disabled: this.readOnly
        });
        Re(v).daterangepicker({
          autohide: !0,
          singleDatePicker: !0,
          // autoUpdateInput: false,
          autoApply: !0,
          locale: {
            format: "DD-MM-YYYY"
          }
        }), Re(v).on("apply.daterangepicker", (k, y) => {
          v.value = y.startDate.format("DD-MM-YYYY"), this.block.dispatchChange();
        }), p.appendChild(v);
      } else if (u.type === "select") {
        const v = Mt("select", Xe.formInput, {
          value: g,
          disabled: this.readOnly
        });
        u.options.map((k) => {
          console.log("DATA ", !!g, g == "", k == null ? void 0 : k.default);
          const y = Mt("option", "", {
            label: this.isJapan ? k.label_jp : k.label,
            value: k.label,
            selected: g ? g == k.label : !!k.default
          });
          v.appendChild(y);
        }), this.api.listeners.on(v, "change", (k) => {
          this.block.dispatchChange();
        }), p.appendChild(v);
      } else if (u.type === "optionDate") {
        const v = Mt("select", Xe.formInput, {
          value: g,
          disabled: this.readOnly
        });
        let k = !1;
        u.options.map((y) => {
          y.value == g && (k = !0);
          const T = Mt("option", "", {
            label: this.isJapan ? y.label_jp : y.label,
            value: y.value,
            selected: g == y.value
          });
          v.appendChild(T);
        }), k || v.prepend(Mt("option", "", {
          label: g,
          value: g,
          selected: !0
        })), this.api.listeners.on(v, "change", (y) => {
          var T, x;
          if (y.target.value == "other") {
            const M = y.target.parentElement, L = Mt("input", Xe.formInput, {
              placeholder: this.api.i18n.t("Select date")
            });
            Re(L).daterangepicker({
              autohide: !0,
              singleDatePicker: !0,
              autoUpdateInput: !1,
              autoApply: !0,
              locale: {
                format: "DD-MM-YYYY"
              }
            }), Re(L).on("apply.daterangepicker", (Y, I) => {
              L.value = I.startDate.format("DD-MM-YYYY"), this.block.dispatchChange();
            }), (x = (T = M.children) == null ? void 0 : T[0]) == null || x.remove(), M.appendChild(L), Re(L).data("daterangepicker").show();
          }
          this.block.dispatchChange();
        }), p.appendChild(v);
      } else if (u.type === "text") {
        const v = Mt("input", Xe.formInput, {
          value: g,
          disabled: this.readOnly
        });
        p.appendChild(v);
      } else if (u.type === "number") {
        const v = Mt("input", Xe.formInput, {
          disabled: this.readOnly,
          type: "number"
        });
        v.value = g || (u == null ? void 0 : u.default), p.appendChild(v);
      } else if (u.type === "checkbox") {
        const v = Mt("input", Xe.formInput, {
          checked: g,
          type: "checkbox",
          disabled: this.readOnly
        });
        this.api.listeners.on(v, "change", () => {
          this.block.dispatchChange();
        }), p.appendChild(v);
      }
    }
  }
  async fetchData(e = "", t) {
    const n = new Headers();
    n.append("Content-Type", "application/json");
    const r = JSON.stringify({
      search: e
    }), a = {
      method: "POST",
      headers: n,
      body: r
    }, c = await fetch(this.config.apiUrl, a).then((p) => p.json()).catch((p) => console.log("error", p)), u = [{
      label: this.api.i18n.t("Delete row"),
      icon: yh,
      hideIf: () => this.numberOfRows === 1,
      confirmationRequired: !0,
      onClick: () => {
        this.deleteRow(this.focusedCell.row), this.hideToolboxes();
      }
    }];
    this.focusedCell.row - 1, c.data.map((p) => {
      u.push({
        label: p.medicine,
        icon: X2,
        confirmationRequired: !1,
        onClick: (g) => {
          p.medicine && (t.value = p.medicine), this.hideToolboxes(), this.block.dispatchChange();
        }
      });
    }), this.renderToolBox(u);
  }
  /**
   * Returns the rendered table wrapper
   *
   * @returns {Element}
   */
  getWrapper() {
    return this.wrapper;
  }
  /**
   * Hangs the necessary handlers to events
   */
  bindEvents() {
    this.wrapper.addEventListener("click", this.documentClicked), this.table.addEventListener("mousemove", tC(150, (e) => this.onMouseMoveInTable(e)), { passive: !0 }), this.table.onkeypress = (e) => this.onKeyPressListener(e), this.table.addEventListener("keydown", (e) => this.onKeyDownListener(e)), this.table.addEventListener("keyup", (e) => this.onKeyUpListener(e)), this.table.addEventListener("focusin", (e) => this.focusInTableListener(e));
  }
  /**
  * Configures and creates the toolbox for manipulating with rows
  *
  * @returns {Toolbox}
  */
  createRowToolbox() {
    return new qi({
      api: this.api,
      cssModifier: "row",
      items: [
        {
          label: this.api.i18n.t("Add row above"),
          icon: J2,
          onClick: () => {
            this.selectedRow != 1 && (this.addRow(this.selectedRow, !0), this.hideToolboxes());
          }
        },
        {
          label: this.api.i18n.t("Add row below"),
          icon: K2,
          onClick: () => {
            this.addRow(this.selectedRow + 1, !0), this.hideToolboxes();
          }
        },
        {
          label: this.api.i18n.t("Delete row"),
          icon: yh,
          hideIf: () => this.numberOfRows === 1,
          confirmationRequired: !0,
          onClick: () => {
            this.deleteRow(this.selectedRow), this.hideToolboxes();
          }
        }
      ],
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  createCustomToolbox(e = []) {
    return new qi({
      table: this,
      api: this.api,
      cssModifier: "row",
      items: e,
      onOpen: () => {
        this.selectRow(this.hoveredRow), this.hideColumnToolbox();
      },
      onClose: () => {
        this.unselectRow();
      }
    });
  }
  renderToolBox(e = []) {
    this.toolBoxDue && this.toolBoxDue.element.remove(), this.toolBoxDue = this.createCustomToolbox(e), this.wrapper.appendChild(this.toolBoxDue.element), this.toolBoxDue.togglerClicked(), this.toolBoxDue.show(() => this.getUpdateLocation());
  }
  getUpdateLocation() {
    const e = this.hoveredRow, t = this.hoveredColumn, n = this.getRow(e), { fromTopBorder: r } = Nl(this.table, n), { height: a } = n.getBoundingClientRect();
    return {
      top: `${Math.ceil(r + a / 0.8)}px`,
      left: `calc((100% - var(--cell-size)) / (${this.numberOfColumns} * 2) * ( (${t} - 1) * 2))`
    };
  }
  /**
   * When you press enter it moves the cursor down to the next row
   * or creates it if the click occurred on the last one
   */
  moveCursorToNextRow() {
    this.focusedCell.row !== this.numberOfRows ? (this.focusedCell.row += 1, this.focusCell(this.focusedCell)) : (this.addRow(), this.focusedCell.row += 1, this.focusCell(this.focusedCell), this.updateToolboxesPosition(0, 0));
  }
  /**
   * Get table cell by row and col index
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @returns {HTMLElement}
   */
  getCell(e, t) {
    return this.table.querySelectorAll(`.${Xe.row}:nth-child(${e}) .${Xe.cell}`)[t - 1];
  }
  /**
   * Get table row by index
   *
   * @param {number} row - row coordinate
   * @returns {HTMLElement}
   */
  getRow(e) {
    return this.table.querySelector(`.${Xe.row}:nth-child(${e})`);
  }
  /**
   * The parent of the cell which is the row
   *
   * @param {HTMLElement} cell - cell element
   * @returns {HTMLElement}
   */
  getRowByCell(e) {
    return e.parentElement;
  }
  /**
   * Ger row's first cell
   *
   * @param {Element} row - row to find its first cell
   * @returns {Element}
   */
  getRowFirstCell(e) {
    return e.querySelector(`.${Xe.cell}:first-child`);
  }
  /**
   * Set the sell's content by row and column numbers
   *
   * @param {number} row - cell row coordinate
   * @param {number} column - cell column coordinate
   * @param {string} content - cell HTML content
   */
  setCellContent(e, t, n) {
    const r = this.getCell(e, t);
    "innerHTML" in r && (r.innerHTML = n);
  }
  /**
   * Add column in table on index place
   * Add cells in each row
   *
   * @param {number} columnIndex - number in the array of columns, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the first cell
   */
  addColumn(e = -1, t = !1) {
    let n = this.numberOfColumns;
    for (let r = 1; r <= this.numberOfRows; r++) {
      let a;
      const c = this.createCell();
      if (e > 0 && e <= n ? (a = this.getCell(r, e), bh(c, a)) : a = this.getRow(r).appendChild(c), r === 1) {
        const u = this.getCell(r, e > 0 ? e : n + 1);
        u && t && vh(u);
      }
    }
    this.addHeadingAttrToFirstRow();
  }
  /**
   * Add row in table on index place
   *
   * @param {number} index - number in the array of rows, where new column to insert, -1 if insert at the end
   * @param {boolean} [setFocus] - pass true to focus the inserted row
   * @returns {HTMLElement} row
   */
  addRow(e = -1, t = !1) {
    let n, r = Mt("div", Xe.row);
    this.tunes.withHeadings && this.removeHeadingAttrFromFirstRow();
    let a = this.numberOfColumns;
    if (e > 0 && e <= this.numberOfRows) {
      let u = this.getRow(e);
      n = bh(r, u);
    } else
      n = this.table.appendChild(r);
    this.fillRow(n, a), this.tunes.withHeadings && this.addHeadingAttrToFirstRow();
    const c = this.getRowFirstCell(n);
    return c && t && vh(c), this.checkInput(), n;
  }
  /**
   * Delete a column by index
   *
   * @param {number} index
   */
  deleteColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      if (!n)
        return;
      n.remove();
    }
  }
  /**
   * Delete a row by index
   *
   * @param {number} index
   */
  deleteRow(e) {
    this.getRow(e).remove(), this.addHeadingAttrToFirstRow();
  }
  /**
   * Create a wrapper containing a table, toolboxes
   * and buttons for adding rows and columns
   *
   * @returns {HTMLElement} wrapper - where all buttons for a table and the table itself will be
   */
  createTableWrapper() {
    if (this.wrapper ? this.table.remove() : this.wrapper = Mt("div", Xe.wrapper), this.table = Mt("div", Xe.table), this.wrapper.classList.add(Xe.wrapperReadOnly), this.wrapper.appendChild(this.toolboxRow.element), this.wrapper.appendChild(this.table), !this.readOnly) {
      const e = Mt("div", Xe.addColumn, {
        innerHTML: wh
      }), t = Mt("div", Xe.addRow, {
        innerHTML: wh
      });
      this.wrapper.appendChild(e), this.wrapper.appendChild(t);
    }
  }
  /**
   * Returns the size of the table based on initial data or config "size" property
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  computeInitialSize() {
    const e = this.data && this.data.content, t = Array.isArray(e), n = t ? e.length : !1, r = t ? e.length : void 0, a = n ? e[0].length : void 0, c = Number.parseInt(this.config && this.config.rows), u = Number.parseInt(this.config && this.config.cols), p = !isNaN(c) && c > 0 ? c : void 0, g = !isNaN(u) && u > 0 ? u : void 0;
    return {
      rows: r || p || 2,
      cols: a || g || 2
    };
  }
  /**
   * Resize table to match config size or transmitted data size
   *
   * @return {{rows: number, cols: number}} - number of cols and rows
   */
  resize() {
    const e = (this.data.content.length || 1) + 1, t = this.data.heading.length;
    for (let n = 0; n < e; n++)
      this.addRow();
    for (let n = 0; n < t; n++)
      this.addColumn();
  }
  /**
   * Fills the table with data passed to the constructor
   *
   * @returns {void}
   */
  fill() {
    const e = this.data;
    if (e && e.heading)
      for (let t = 0; t < e.heading.length; t++)
        this.setCellContent(1, t + 1, e.heading[t]);
  }
  /**
   * Fills a row with cells
   *
   * @param {HTMLElement} row - row to fill
   * @param {number} numberOfColumns - how many cells should be in a row
   */
  fillRow(e, t) {
    for (let n = 1; n <= t; n++) {
      const r = this.createCell();
      e.appendChild(r);
    }
  }
  /**
   * Creating a cell element
   *
   * @return {Element}
   */
  createCell() {
    return Mt("div", Xe.cell, {
      contentEditable: !1
    });
  }
  /**
   * Get number of rows in the table
   */
  get numberOfRows() {
    return this.table.childElementCount;
  }
  /**
   * Get number of columns in the table
   */
  get numberOfColumns() {
    return this.numberOfRows ? this.table.querySelectorAll(`.${Xe.row}:first-child .${Xe.cell}`).length : 0;
  }
  /**
   * Is the column toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isColumnMenuShowing() {
    return this.selectedColumn !== 0;
  }
  /**
   * Is the row toolbox menu displayed or not
   *
   * @returns {boolean}
   */
  get isRowMenuShowing() {
    return this.selectedRow !== 0;
  }
  /**
   * Recalculate position of toolbox icons
   *
   * @param {Event} event - mouse move event
   */
  onMouseMoveInTable(e) {
    const { row: t, column: n } = this.getHoveredCell(e);
    this.hoveredColumn = n, this.hoveredRow = t, this.updateToolboxesPosition();
  }
  /**
   * Prevents default Enter behaviors
   * Adds Shift+Enter processing
   *
   * @param {KeyboardEvent} event - keypress event
   */
  onKeyPressListener(e) {
    return e.key === "Enter" && e.shiftKey ? !0 : e.key !== "Enter";
  }
  /**
   * Prevents tab keydown event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyDownListener(e) {
    e.key === "Tab" && e.stopPropagation();
  }
  /**
   * Prevents tab keyup event from bubbling
   * so that it only works inside the table
   *
   * @param {KeyboardEvent} event - keydown event
   */
  onKeyUpListener(e) {
    if (e.key === "Tab") {
      e.stopPropagation();
      return;
    }
    if (e.key === "Enter") {
      e.stopPropagation();
      return;
    }
  }
  /**
   * Set the coordinates of the cell that the focus has moved to
   *
   * @param {FocusEvent} event - focusin event
   */
  focusInTableListener(e) {
    const t = e.target, n = this.getRowByCell(t);
    this.focusedCell = {
      row: Array.from(this.table.querySelectorAll(`.${Xe.row}`)).indexOf(n) + 1,
      column: Array.from(n.querySelectorAll(`.${Xe.cell}`)).indexOf(t) + 1
    };
  }
  /**
   * Unselect row/column
   * Close toolbox menu
   * Hide toolboxes
   *
   * @returns {void}
   */
  hideToolboxes() {
    this.hideRowToolbox(), this.updateToolboxesPosition();
  }
  /**
   * Unselect row, close toolbox
   *
   * @returns {void}
   */
  hideRowToolbox() {
    var e;
    this.unselectRow(), this.toolboxRow.hide(), (e = this.toolBoxDue) == null || e.hide();
  }
  /**
   * Unselect column, close toolbox
   *
   * @returns {void}
   */
  hideColumnToolbox() {
    this.unselectColumn();
  }
  /**
   * Set the cursor focus to the focused cell
   *
   * @returns {void}
   */
  focusCell() {
    var e;
    (e = this.focusedCellElem) == null || e.focus();
  }
  /**
   * Get current focused element
   *
   * @returns {HTMLElement} - focused cell
   */
  get focusedCellElem() {
    const { row: e, column: t } = this.focusedCell;
    return this.getCell(e, t);
  }
  /**
   * Update toolboxes position
   *
   * @param {number} row - hovered row
   * @param {number} column - hovered column
   */
  updateToolboxesPosition(e = this.hoveredRow, t = this.hoveredColumn) {
    this.isRowMenuShowing || e > 0 && e <= this.numberOfRows && this.toolboxRow.show(() => {
      const n = this.getRow(e), { fromTopBorder: r } = Nl(this.table, n), { height: a } = n.getBoundingClientRect();
      return {
        top: `${Math.ceil(r + a / 2)}px`
      };
    });
  }
  /**
   * Makes the first row headings
   *
   * @param {boolean} withHeadings - use headings row or not
   */
  setHeadingsSetting(e) {
    this.tunes.withHeadings = e, e ? (this.table.classList.add(Xe.withHeadings), this.addHeadingAttrToFirstRow()) : (this.table.classList.remove(Xe.withHeadings), this.removeHeadingAttrFromFirstRow());
  }
  /**
   * Adds an attribute for displaying the placeholder in the cell
   */
  addHeadingAttrToFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.setAttribute("heading", this.api.i18n.t("Heading"));
    }
  }
  /**
   * Removes an attribute for displaying the placeholder in the cell
   */
  removeHeadingAttrFromFirstRow() {
    for (let e = 1; e <= this.numberOfColumns; e++) {
      let t = this.getCell(1, e);
      t && t.removeAttribute("heading");
    }
  }
  /**
   * Add effect of a selected row
   *
   * @param {number} index
   */
  selectRow(e) {
    const t = this.getRow(e);
    t && (this.selectedRow = e, t.classList.add(Xe.rowSelected));
  }
  /**
   * Remove effect of a selected row
   */
  unselectRow() {
    if (this.selectedRow <= 0)
      return;
    const e = this.table.querySelector(`.${Xe.rowSelected}`);
    e && e.classList.remove(Xe.rowSelected), this.selectedRow = 0;
  }
  /**
   * Add effect of a selected column
   *
   * @param {number} index
   */
  selectColumn(e) {
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.getCell(t, e);
      n && n.classList.add(Xe.cellSelected);
    }
    this.selectedColumn = e;
  }
  /**
   * Remove effect of a selected column
   */
  unselectColumn() {
    if (this.selectedColumn <= 0)
      return;
    let e = this.table.querySelectorAll(`.${Xe.cellSelected}`);
    Array.from(e).forEach((t) => {
      t.classList.remove(Xe.cellSelected);
    }), this.selectedColumn = 0;
  }
  /**
   * Calculates the row and column that the cursor is currently hovering over
   * The search was optimized from O(n) to O (log n) via bin search to reduce the number of calculations
   *
   * @param {Event} event - mousemove event
   * @returns hovered cell coordinates as an integer row and column
   */
  getHoveredCell(e) {
    let t = this.hoveredRow, n = this.hoveredColumn;
    const { width: r, height: a, x: c, y: u } = Z2(this.table, e);
    return c >= 0 && (n = this.binSearch(
      this.numberOfColumns,
      (p) => this.getCell(1, p),
      ({ fromLeftBorder: p }) => c < p,
      ({ fromRightBorder: p }) => c > r - p
    )), u >= 0 && (t = this.binSearch(
      this.numberOfRows,
      (p) => this.getCell(p, 1),
      ({ fromTopBorder: p }) => u < p,
      ({ fromBottomBorder: p }) => u > a - p
    )), {
      row: t || this.hoveredRow,
      column: n || this.hoveredColumn
    };
  }
  /**
   * Looks for the index of the cell the mouse is hovering over.
   * Cells can be represented as ordered intervals with left and
   * right (upper and lower for rows) borders inside the table, if the mouse enters it, then this is our index
   *
   * @param {number} numberOfCells - upper bound of binary search
   * @param {function} getCell - function to take the currently viewed cell
   * @param {function} beforeTheLeftBorder - determines the cursor position, to the left of the cell or not
   * @param {function} afterTheRightBorder - determines the cursor position, to the right of the cell or not
   * @returns {number}
   */
  binSearch(e, t, n, r) {
    let a = 0, c = e + 1, u = 0, p;
    for (; a < c - 1 && u < 10; ) {
      p = Math.ceil((a + c) / 2);
      const g = t(p), v = Nl(this.table, g);
      if (n(v))
        c = p;
      else if (r(v))
        a = p;
      else
        break;
      u++;
    }
    return p;
  }
  /**
   * Collects data from cells into a two-dimensional array
   *
   * @returns {string[][]}
   */
  getData() {
    const e = [];
    for (let t = 1; t <= this.numberOfRows; t++) {
      const n = this.table.querySelector(`.${Xe.row}:nth-child(${t})`), r = Array.from(n.querySelectorAll(`.${Xe.formInput}`));
      t != 1 && e.push(r.map((a) => {
        if (["text", "select-one", "number"].includes(a == null ? void 0 : a.type))
          return a == null ? void 0 : a.value;
        if ((a == null ? void 0 : a.type) === "checkbox")
          return a == null ? void 0 : a.checked;
      }));
    }
    return e;
  }
  /**
   * Remove listeners on the document
   */
  destroy() {
    document.removeEventListener("click", this.documentClicked);
  }
}
const iC = {
  antiParasitic: {
    title: "Anti Parasitic",
    title_jp: "混合ワクチン",
    data: [
      {
        heading: "Prevention Medicine",
        heading_jp: "予防接種医学",
        type: "search",
        key: "vaccinationMedicine"
      },
      {
        heading: "Prevention Type",
        heading_jp: "投与日",
        type: "select",
        options: [
          {
            label: "All-in-one",
            label_jp: "予防接種医学",
            value: "all-in-one",
            default: !0
          },
          {
            label: "Heartworm",
            label_jp: "予防接種医学",
            value: "heartworm"
          },
          {
            label: "Flea & Tick",
            label_jp: "予防接種医学",
            value: "flea-and-tick"
          }
        ],
        key: "administrationDate"
      },
      {
        heading: "Quantity",
        heading_jp: "期日",
        type: "number",
        default: 1,
        key: "quantity"
      },
      {
        heading: "Prescription Date",
        heading_jp: "整理番号",
        key: "prescriptionDate",
        type: "date"
      },
      {
        heading: "Due date",
        heading_jp: "アレルギー反応",
        key: "dueDate",
        type: "optionDate",
        options: [
          {
            label: "1 Month",
            label_jp: "整理番号",
            value: "1-month"
          },
          {
            label: "2 Month",
            label_jp: "整理番号",
            value: "2-month"
          },
          {
            label: "3 Month",
            label_jp: "整理番号",
            value: "3-month"
          },
          {
            label: "4 Month",
            label_jp: "整理番号",
            value: "4-month"
          },
          {
            label: "5 Month",
            label_jp: "整理番号",
            value: "5-month"
          },
          {
            label: "6 Month",
            label_jp: "整理番号",
            value: "6-month"
          },
          {
            label: "7 Month",
            label_jp: "整理番号",
            value: "7-month"
          },
          {
            label: "8 Month",
            label_jp: "整理番号",
            value: "8-month"
          },
          {
            label: "9 Month",
            label_jp: "整理番号",
            value: "9-month"
          },
          {
            label: "10 Month",
            label_jp: "整理番号",
            value: "10-month"
          },
          {
            label: "11 Month",
            label_jp: "整理番号",
            value: "11-month"
          },
          {
            label: "12 Month",
            label_jp: "整理番号",
            value: "12-month"
          },
          {
            label: "Custom",
            label_jp: "整理番号",
            value: "other"
          }
        ]
      },
      {
        heading: "Remarks",
        heading_jp: "備考",
        type: "text",
        key: "remarks"
      }
    ]
  }
}, Pl = {
  default: "antiParasitic",
  data: iC
};
class rC {
  /**
   * Notify core that read-only mode is supported
   *
   * @returns {boolean}
   */
  static get isReadOnlySupported() {
    return !0;
  }
  /**
   * Allow to press Enter inside the CodeTool textarea
   *
   * @returns {boolean}
   * @public
   */
  static get enableLineBreaks() {
    return !0;
  }
  /**
   * Render plugin`s main Element and fill it with saved data
   *
   * @param {TableData} data — previously saved data
   * @param {TableConfig} config - user config for Tool
   * @param {object} api - Editor.js API
   * @param {boolean} readOnly - read-only mode flag
   */
  constructor({ data: e, config: t, api: n, readOnly: r, block: a }) {
    if (this.api = n, this.block = a, this.title = n.i18n.t("Anti Parasitic"), this.readOnly = r, this.config = t, !t.apiUrl)
      throw new Error("Please provide API url for Anti Parasitic Search");
    this.data = {
      current: e && e.current ? e.current : Pl.default,
      content: e && e.content ? e.content : []
    }, this.table = null;
  }
  /**
   * Get Tool toolbox settings
   * icon - Tool icon's SVG
   * title - title to show in toolbox
   *
   * @returns {{icon: string, title: string}}
   */
  static get toolbox() {
    return {
      icon: eC,
      title: this.title
    };
  }
  /**
   * Return Tool's view
   *
   * @returns {HTMLDivElement}
   */
  render() {
    var e;
    this.config.data = Pl.data[this.data.current], this.container ? (this.table.getWrapper().remove(), this.container.innerHTML = "") : this.container = Mt("div", this.api.styles.block), this.table = new nC(this.readOnly, this.api, this.data, this.config, this.block);
    const t = Mt("h5", "tc-header");
    return t.innerHTML = ((e = this.config) == null ? void 0 : e.locale) == "jp" ? this.config.data.title_jp : this.config.data.title, this.container.appendChild(t), this.container.appendChild(this.table.getWrapper()), this.table.setHeadingsSetting(!0), this.container;
  }
  /**
   * Returns plugin settings
   *
   * @returns {Array}
   */
  renderSettings() {
    return [];
  }
  /**
   * Extract table data from the view
   *
   * @returns {TableData} - saved data
   */
  save() {
    var e;
    const t = this.table.getData();
    return {
      current: ((e = this.data) == null ? void 0 : e.current) || Pl.default,
      content: t
    };
  }
  /**
   * Plugin destroyer
   *
   * @returns {void}
   */
  destroy() {
    this.table.destroy();
  }
  /**
   * A helper to get config value.
   * 
   * @param {string} configName - the key to get from the config. 
   * @param {any} defaultValue - default value if config doesn't have passed key
   * @param {object} savedData - previously saved data. If passed, the key will be got from there, otherwise from the config
   * @returns {any} - config value.
   */
  getConfig(e, t = void 0, n = void 0) {
    const r = this.data || n;
    return r ? r[e] ? r[e] : t : this.config && this.config[e] ? this.config[e] : t;
  }
  /**  
   * Table onPaste configuration
   *
   * @public
   */
  static get pasteConfig() {
    return { tags: ["TABLE", "TR", "TH", "TD"] };
  }
  /**
   * On paste callback that is fired from Editor
   *
   * @param {PasteEvent} event - event with pasted data
   */
  onPaste(e) {
    const t = e.detail.data, n = t.querySelector(":scope > thead, tr:first-of-type th"), r = Array.from(t.querySelectorAll("tr")).map((a) => Array.from(a.querySelectorAll("th, td")).map((c) => c.innerHTML));
    this.data = {
      withHeadings: n !== null,
      content: r
    }, this.table.wrapper && this.table.wrapper.replaceWith(this.render());
  }
}
class qC {
  constructor(e = {}) {
    this.config = e, this.editor = null, this.config.locale = (e == null ? void 0 : e.locale) || "en";
  }
  mergeObjectsByKey(e, t, n) {
    if (!e || !t || !e[n] || !t[n])
      return e;
    const r = { ...e };
    for (const a in t[n])
      t[n].hasOwnProperty(a) && (r[n][a] = t[n][a]);
    return r;
  }
  init() {
    var n, r, a, c, u, p, g, v, k, y, T, x, M, L, Y;
    let e = {
      header: {
        class: kd,
        inlineToolbar: !0
      },
      alert: Ld,
      paragraph: Ng,
      delimiter: qd
      // table: {
      //     class: Table
      // }
    }, t = {
      autofocus: !1,
      holder: this.config.holder || "editorjs",
      placeholder: "いらっしゃいませ。ここから始める...",
      tools: {
        header: {
          class: kd,
          config: {
            placeholder: "Enter a header"
          },
          inlineToolbar: !0
        },
        table: {
          class: Eg,
          inlineToolbar: !0,
          tunes: ["alignmentTool"]
        },
        alert: Ld,
        checklist: {
          class: zg,
          inlineToolbar: !0
        },
        nestedList: {
          class: jg,
          inlineToolbar: !0,
          config: {
            defaultStyle: "unordered"
          }
        },
        Color: {
          class: wd,
          config: {
            colorCollections: ["#EC7878", "#9C27B0", "#673AB7", "#3F51B5", "#0070FF", "#03A9F4", "#00BCD4", "#4CAF50", "#8BC34A", "#CDDC39", "#FFF"],
            defaultColor: "#FF1300",
            type: "text",
            customPicker: !0
            // add a button to allow selecting any colour  
          }
        },
        Marker: {
          class: wd,
          config: {
            defaultColor: "#FFBF00",
            type: "marker",
            icon: '<svg fill="#000000" height="200px" width="200px" version="1.1" id="Icons" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" viewBox="0 0 32 32" xml:space="preserve"><g id="SVGRepo_bgCarrier" stroke-width="0"></g><g id="SVGRepo_tracerCarrier" stroke-linecap="round" stroke-linejoin="round"></g><g id="SVGRepo_iconCarrier"> <g> <path d="M17.6,6L6.9,16.7c-0.2,0.2-0.3,0.4-0.3,0.6L6,23.9c0,0.3,0.1,0.6,0.3,0.8C6.5,24.9,6.7,25,7,25c0,0,0.1,0,0.1,0l6.6-0.6 c0.2,0,0.5-0.1,0.6-0.3L25,13.4L17.6,6z"></path> <path d="M26.4,12l1.4-1.4c1.2-1.2,1.1-3.1-0.1-4.3l-3-3c-0.6-0.6-1.3-0.9-2.2-0.9c-0.8,0-1.6,0.3-2.2,0.9L19,4.6L26.4,12z"></path> </g> <g> <path d="M28,29H4c-0.6,0-1-0.4-1-1s0.4-1,1-1h24c0.6,0,1,0.4,1,1S28.6,29,28,29z"></path> </g> </g></svg>'
          }
        },
        underline: { class: Gm },
        textVariant: { class: tg },
        strikethrough: { class: Jm },
        list: {
          class: ug,
          config: {
            toolbar: ["Header"]
          },
          // tunes: ['alignmentTool'],
          inlineToolbar: !0
        },
        paragraph: {
          class: fg,
          inlineToolbar: !0
          // tunes: ['alignmentTool'],
        },
        alignmentTool: {
          class: ng,
          config: {
            default: "left",
            blocks: {
              header: "left",
              list: "left"
            }
          },
          inlineToolbar: !0
        },
        tooltip: {
          class: Km,
          config: {
            location: "left",
            highlightColor: "#FFEFD5",
            underline: !0,
            backgroundColor: "#154360",
            textColor: "#FDFEFE"
          },
          inlineToolbar: !0
        },
        columns: {
          class: Rg,
          config: {
            EditorJsLibrary: yd,
            // Pass the library instance to the columns instance.
            tools: e
            // IMPORTANT! ref the column_tools
          }
        },
        // medicine: {
        //     class: AaldaMedicine,
        //     inlineToolbar: true,
        // },
        delimiter: {
          class: qd
        },
        vitals: {
          class: nb,
          inlineToolbar: !0,
          config: {
            locale: (n = this.config) == null ? void 0 : n.locale
          }
        },
        peros: {
          class: Db,
          // inlineToolbar: true,
          config: {
            locale: (r = this.config) == null ? void 0 : r.locale
          }
        },
        testReport: {
          class: gb,
          config: {
            locale: (a = this.config) == null ? void 0 : a.locale,
            apiUrl: ((u = (c = this.config) == null ? void 0 : c.api) == null ? void 0 : u.test) || "http://localhost:8000/api/medicine"
          }
        },
        vaccination: {
          class: Uy,
          inlineToolbar: !0,
          config: {
            locale: (p = this.config) == null ? void 0 : p.locale,
            apiUrl: ((v = (g = this.config) == null ? void 0 : g.api) == null ? void 0 : v.vaccination) || "http://localhost:8000/api/medicine"
          }
        },
        antiParasitic: {
          class: rC,
          inlineToolbar: !0,
          config: {
            locale: (k = this.config) == null ? void 0 : k.locale,
            apiUrl: ((T = (y = this.config) == null ? void 0 : y.api) == null ? void 0 : T.medicine) || "http://localhost:8000/api/medicine"
          }
        }
      },
      data: {},
      /**
       * onReady callback
       */
      onReady: () => {
        new Og({ editor: this.editor }), new Dg(this.editor);
      },
      /**
       * onChange callback
       */
      onChange: (I, F) => {
        var Q, U;
        (Q = this.config) != null && Q.onChange && ((U = this.config) == null || U.onChange(I, F));
      },
      // tunes: ['textVariant', 'alignmentTool'],
      /**
       * Internationalzation config
       */
      i18n: {
        /**
         * @type {I18nDictionary}
         */
        messages: {
          /**
           * Other below: translation of different UI components of the editor.js core
           */
          ui: {
            blockTunes: {
              toggler: {
                "Click to tune": "クリックしてチューニング",
                "or drag to move": "またはドラッグして移動します"
              }
            },
            inlineToolbar: {
              converter: {
                "Convert to": "に変換"
              }
            },
            toolbar: {
              toolbox: {
                Add: "追加"
              }
            }
          },
          /**
           * Section for translation Tool Names: both block and inline tools
           */
          toolNames: {
            Text: "文章",
            Heading: "見出し",
            List: "リスト",
            Warning: "警告",
            Checklist: "チェックリスト",
            Delimiter: "デリミタ",
            Table: "テーブル",
            Link: "リンク",
            Marker: "マーカー",
            Bold: "大胆な",
            Italic: "イタリック",
            Alert: "アラート",
            Peros: "身体検査所見",
            Vaccination: "予防接種",
            Vitals: "バイタル"
          },
          /**
           * Section for passing translations to the external tools classes
           */
          tools: {
            /**
             * Each subsection is the i18n dictionary that will be passed to the corresponded plugin
             * The name of a plugin should be equal the name you specify in the 'tool' section for that plugin
             */
            warning: {
              // <-- 'Warning' tool will accept this dictionary section
              Title: "タイトル",
              Message: "メッセージ"
            },
            /**
             * Link is the internal Inline Tool
             */
            link: {
              "Add a link": "リンクを追加する"
            },
            /**
             * The "stub" is an internal block tool, used to fit blocks that does not have the corresponded plugin
             */
            stub: {
              "The block can not be displayed correctly.": "ブロックが正しく表示されません。"
            },
            vitals: {
              "Body Weight": "体重",
              Temprature: "温度",
              "Heart Rate": "心拍数",
              Respiratory: "呼吸器系",
              Vitals: "バイタル",
              Value: "価値",
              Unit: "ユニット"
            },
            peros: {
              "PE/ROS": "観察",
              Observation: "観察",
              Result: "結果",
              Remarks: "備考"
            },
            vaccination: {
              Vaccination: "予防接種"
            }
          },
          /**
           * Section allows to translate Block Tunes
           */
          blockTunes: {
            /**
             * Each subsection is the i18n dictionary that will be passed to the corresponded Block Tune plugin
             * The name of a plugin should be equal the name you specify in the 'tunes' section for that plugin
             *
             * Also, there are few internal block tunes: "delete", "moveUp" and "moveDown"
             */
            delete: {
              Delete: "消去"
            },
            moveUp: {
              "Move up": "上に移動"
            },
            moveDown: {
              "Move down": "下に移動"
            }
          }
        }
      }
    };
    t = this.mergeObjectsByKey(t, this.config, "tools"), t = this.mergeObjectsByKey(t, this.config, "data"), t = this.mergeObjectsByKey(t, this.config, "i18n"), (x = this.config) != null && x.locale && ((M = this.config) == null ? void 0 : M.locale) !== "jp" && delete t.i18n, t = this.mergeObjectsByKey(t, this.config, "onReady"), t = this.mergeObjectsByKey(t, this.config, "onChange"), t = this.mergeObjectsByKey(t, this.config, "tunes"), t = { ...t, holder: ((L = this.config) == null ? void 0 : L.holder) || t.holder, placeholder: ((Y = this.config) == null ? void 0 : Y.placeholder) || t.placeholder }, this.editor = new yd(t);
  }
  getInstance() {
    return this.editor;
  }
  destroy() {
    "destroy" in this.editor && this.editor.destroy();
  }
}
export {
  qC as default
};
